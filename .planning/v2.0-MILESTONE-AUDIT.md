---
milestone: v2.0
audited: 2026-07-17T22:30:00Z
status: gaps_found
scores:
  requirements: 10/30 satisfied, 1/30 partial, 19/30 orphaned (Phases 13-17 not yet started — expected, not a defect)
  phases: 3/8 v2.0 phases executed (10, 11, 12); all 3 have VERIFICATION.md; 2/3 passed clean (10, 12), 1/3 (Phase 11) has a stale VERIFICATION.md record (status: gaps_found) whose sole gap is independently confirmed fixed in source + a same-day SECURITY.md, but the record itself was never re-run
  integration: 2 findings — 1 latent (ResilienceModule not wired into 8 gRPC scaffolds, harmless today, blocks Phase 17), 1 newly discovered and more severe than previously documented (compiled `@iseyaa/proto` package has no build step — `require()` fails at runtime even though `nest build` passes)
  flows: N/A — Phases 10-12 are backend/infra-only (UI hint: no on all three); no user-facing E2E flow changed
gaps:
  requirements: []
  # No requirement claimed by an executed phase is unsatisfied. RESIL-02 is "partial" (see below), not unsatisfied — its
  # code-level deliverable is fully verified; only a live-dashboard human-verification step remains open, which was
  # already correctly scoped as human_verification (not a code defect) in 11-VERIFICATION.md.
  orphaned_requirements:
    - { id: "SETTLE-03", phase: "Phase 13", reason: "Phase 13 has 0/0 plans — not started" }
    - { id: "SETTLE-04", phase: "Phase 13", reason: "Phase 13 has 0/0 plans — not started" }
    - { id: "SETTLE-09", phase: "Phase 13", reason: "Phase 13 has 0/0 plans — not started" }
    - { id: "MIN-01", phase: "Phase 14", reason: "Phase 14 has 0/0 plans — not started" }
    - { id: "MIN-02", phase: "Phase 14", reason: "Phase 14 has 0/0 plans — not started" }
    - { id: "MIN-03", phase: "Phase 14", reason: "Phase 14 has 0/0 plans — not started" }
    - { id: "MIN-04", phase: "Phase 14", reason: "Phase 14 has 0/0 plans — not started; also depends on SETTLE-02 (now satisfied) via Ministry wallet" }
    - { id: "MIN-05", phase: "Phase 14", reason: "Phase 14 has 0/0 plans — not started" }
    - { id: "MIN-06", phase: "Phase 14", reason: "Phase 14 has 0/0 plans — not started" }
    - { id: "MIN-07", phase: "Phase 14", reason: "Phase 14 has 0/0 plans — not started" }
    - { id: "OTP-01", phase: "Phase 15", reason: "Phase 15 has 0/0 plans — not started" }
    - { id: "OTP-02", phase: "Phase 15", reason: "Phase 15 has 0/0 plans — not started" }
    - { id: "OTP-03", phase: "Phase 15", reason: "Phase 15 has 0/0 plans — not started" }
    - { id: "OTP-04", phase: "Phase 15", reason: "Phase 15 has 0/0 plans — not started" }
    - { id: "POOL-01", phase: "Phase 16", reason: "Phase 16 has 0/0 plans — not started" }
    - { id: "POOL-02", phase: "Phase 16", reason: "Phase 16 has 0/0 plans — not started" }
    - { id: "GRPC-03", phase: "Phase 17", reason: "Phase 17 has 0/0 plans — not started; will inherit the ResilienceModule DI gap and the @iseyaa/proto runtime-resolution gap found below" }
    - { id: "GRPC-04", phase: "Phase 17", reason: "Phase 17 has 0/0 plans — not started" }
    - { id: "GRPC-05", phase: "Phase 17", reason: "Phase 17 has 0/0 plans — not started" }
  integration:
    - id: "INT-01"
      severity: "blocker-for-Phase-17 (latent, harmless today)"
      finding: "ResilienceModule is not imported by any of the 8 backend/apps/*-service gRPC scaffolds. Reproduced dynamically (not just via grep) via Test.createTestingModule for all 8 — 7 fail through CommonModule's PaystackService(ConfigService, ?), 1 (auth-service) fails independently through AuthModule's AuthService(..., ResilienceService). Nothing boots these scaffolds in production today (no docker-compose/Railway reference), so this is currently inert. It will block Phase 17's first live extraction (notifications-service) unless fixed first — recommend importing ResilienceModule into each scaffold's app.module.ts as a Phase 16 or Phase 17 prerequisite task."
    - id: "INT-02"
      severity: "new-finding, more severe than Phase 10's disclosed gap"
      finding: "packages/proto/package.json declares main: generated/index.js and types: generated/index.d.ts, but generate.sh only ever emits .ts source — no build/compile step exists anywhere for the @iseyaa/proto package (no \"build\" script; root build:all runs --if-present and silently skips it). `nest build <service>` passes for all 8 scaffolds only because tsc's resolver is lenient and extension-swaps to the .ts source directly. Proven at actual Node.js require() time: `node -e \"require('@iseyaa/proto')\"` from backend/ fails with Cannot find module '...\\node_modules\\@iseyaa\\proto\\generated\\index.js', and directly requiring compiled dist output (dist/apps/wallet-service/src/wallet-grpc.controller.js) fails identically. This means Phase 10's verified 'nest build exits 0' success criterion does not imply the compiled service can actually run — a deeper failure mode than the previously-known Docker-build gap (10-VERIFICATION.md's Additional Finding), which only caught the Docker-build-time symptom, not this runtime one. Recommend: add a real build script to packages/proto/package.json (tsc emitting .js + .d.ts) and wire it into root build:all before Phase 17 attempts any live extraction."
  tech_debt: []  # see tech_debt section below for non-blocking items
nyquist:
  compliant_phases: [10, 11, 12]
  partial_phases: []
  missing_phases: []
  overall: "COMPLIANT — all 3 executed v2.0 phases have VALIDATION.md with nyquist_compliant: true and wave_0_complete: true"
tech_debt:
  - phase: 10-documentation-correction-grpc-build-fix
    items:
      - "Docker image build fails for all 8 backend/apps/*-service images: TS2307 Cannot find module '@iseyaa/proto' — backend/package.json never declares it as a dependency, and Dockerfile npm ci never links packages/proto. Documented in 10-VERIFICATION.md as an out-of-declared-scope finding, now compounded by INT-02 above (the deeper runtime-require gap) — recommend fixing both together before Phase 17."
      - "Two pre-existing wallet-security warnings in the (unwired, scaffold-only) gRPC controllers: wallet-service's Debit handler has no SELECT FOR UPDATE / idempotency check (TOCTOU double-spend race), marketplace-service's ReserveStock has a check-then-decrement oversell race. Flagged in 10-REVIEW.md as CR-02/WR-01, not fixed post-merge since zero live traffic can reach these scaffolds yet — must be fixed before Phase 17 wires any of them live."
  - phase: 11-resilience-wrapping
    items:
      - "11-VERIFICATION.md (frozen at 2026-07-16T20:15:00Z, status: gaps_found) was never re-run after commit b0fcb3c (2026-07-16T21:49:43Z) closed its sole blocking gap (raw vendor error object logged on circuit-breaker open, a secret-leak risk for Paystack/FCM). Independently re-confirmed via direct source read (resilience.service.ts:126-134, sanitized safeReason string, no raw error reaches any Logger call in the file) and via 11-SECURITY.md (same day, status: verified, threats_open: 0, explicitly cites b0fcb3c). Recommend re-running /gsd-verifier on Phase 11 to correct the stale record before this milestone archives."
      - "11-SECURITY.md's threat T-11-03 claims blanket closure ('push/SMS catch blocks log only err.message, never phone numbers, OTPs, or push token bodies') but this overstates what was fixed — 5 call sites still pass a raw err object to Logger.error(): auth.service.ts:323,364 (Termii/Twilio fallback), delivery.service.ts:349 (Termii delivery OTP), ai.service.ts:353,546,579 (Anthropic). The b0fcb3c fix was scoped only to resilience.service.ts's onBreak(). Practical severity is lower than the original Paystack/FCM finding (fetch()-based Termii/Twilio errors don't carry .config.headers the way axios errors do, and Anthropic's APIError.headers comes from the response, not the outbound API key), but the anti-pattern is structurally identical to a Critical finding elsewhere in the same phase, and Phase 15 (Multi-Channel OTP) will add more Termii/WhatsApp call sites on this same pattern. Recommend re-scoping T-11-03 to explicitly enumerate these 5 remaining sites before treating it as fully closed."
      - "3 lower-severity Warnings from the final 11-REVIEW.md pass remain unaddressed and are not phase-goal blockers: NotificationsService.sendPush's Google OAuth token fetch runs outside resilience.execute('fcm', ...) (only the final axios.post is wrapped); AiService's 3-turn agentic loop silently drops the final answer if the turn cap is hit mid tool-use with no distinguishing SSE signal; resilience.service.spec.ts's malformed-config regression coverage only exercises 2 of 4 hardened readConfig() keys."
      - "RESIL-02's live-observability human-verification item remains genuinely open: a real vendor outage against the live Grafana Cloud/Sentry/OTel pipeline has not been confirmed to produce a visible span + Sentry event. Code-level wiring (OTel span attributes, Sentry.captureMessage, both correctly sanitized) is confirmed present, unit-tested, and — per the integration checker — genuinely loaded in the production boot path via backend/start.sh's node --require ./dist/instrumentation.js. Live Railway env var presence for OTel/Sentry credentials was not independently confirmed this audit."
  - phase: 12-settlement-engine-foundation
    items:
      - "backend/src/modules/tour-bookings/__tests__/wallet-invariant.e2e-spec.ts is dead test code — its filename doesn't match jest.config.js's testRegex ('.*\\.spec\\.ts$' requires a literal dot before spec.ts), so it's never executed by npx jest, and even if renamed it would now fail DI resolution (constructs TourSettlementService without providing SettlementService). Pre-existing before Phase 12, not caused by any Phase 12 plan, does not affect the phase goal. Logged in .planning/phases/12-settlement-engine-foundation/deferred-items.md."
  - phase: cross-phase / documentation
    items:
      - "STATE.md's 'Current Status' narrative section (lines ~35-45) still lists Phases 10, 11, 12 as 'NOT STARTED' even though the file's own frontmatter (completed_phases: 6, stopped_at: Phase 12 context gathered) and ROADMAP.md both correctly show all three complete. This is a self-contradictory state document — recommend regenerating STATE.md's Current Status section on next /gsd-progress run."
      - "REQUIREMENTS.md's traceability table and per-requirement checkboxes were out of sync with actual phase completion for RESIL-01, RESIL-02, and all of SETTLE-01/02/05/06/07/08 (still shown 'Pending'/unchecked despite ROADMAP.md marking Phases 10-12 complete and VERIFICATION.md confirming them satisfied). Corrected as part of this audit (see REQUIREMENTS.md diff) — RESIL-01 and SETTLE-01/02/05/06/07/08 now checked complete; RESIL-02 marked partial pending live-dashboard confirmation."
---

# v2.0 Milestone Audit — Microservices, Multi-Channel Auth & Government Partnership

**Audited:** 2026-07-17
**Status:** ⚠ Gaps Found (expected at this stage — milestone is genuinely in progress, not a quality regression)

## Summary

v2.0 is **3 of 8 phases complete** (Phase 10: Documentation Correction + gRPC Build Fix; Phase 11: Resilience Wrapping; Phase 12: Settlement Engine Foundation). Phases 13-17 (Settlement Cutover, Ministry Dashboard, Multi-Channel OTP, Connection Pooling, gRPC Extraction) have **not been started** — 0/0 plans each, as ROADMAP.md's own progress table already states. This is not a defect; it means **v2.0 is not ready for archival**. Of the 30 requirements mapped to this milestone, 10 are satisfied, 1 is partial (code done, live-dashboard confirmation pending), and 19 are legitimately orphaned because their owning phases haven't started yet.

The three executed phases are in solid shape:
- **Phase 10** — clean pass, 4/4 roadmap success criteria + 14/14 plan-level truths verified.
- **Phase 12** — clean pass, 6/6 truths verified, all 9 code-review findings (1 Critical, 8 Warnings) confirmed fixed in source.
- **Phase 11** — functionally complete (its one Critical gap, a vendor-secret log leak, is independently confirmed fixed in source and closed in 11-SECURITY.md), but its own **11-VERIFICATION.md record is stale** — it was never re-run after the fix landed, so it still reads `status: gaps_found`. This is a process/documentation gap, not a live defect.

The integration check surfaced two things worth tracking before Phase 17 (gRPC extraction) begins: (1) none of the 8 gRPC service scaffolds actually import `ResilienceModule`, so if they were extracted today every vendor call inside them would run unwrapped; (2) a newly-discovered, deeper version of Phase 10's already-known Docker-build gap — `@iseyaa/proto` has no compiled JS output at all, so even a service whose `nest build` passes cleanly cannot actually `require()` the proto package at runtime. Neither blocks anything live today (nothing boots these scaffolds yet), but both should be fixed as Phase 16/17 prerequisites.

## Requirements Coverage

| Requirement | Phase | Status | Evidence |
|---|---|---|---|
| DOC-01 | 10 | ✓ Satisfied | 10-VERIFICATION.md Truth #1; grep confirms zero overstated claims remain in ROADMAP.md/PROJECT.md |
| GRPC-01 | 10 | ✓ Satisfied | 10-VERIFICATION.md Truth #2-3; all 8 scaffolds `nest build` exit 0. See INT-02 below for a downstream runtime caveat not covered by this requirement's literal wording. |
| GRPC-02 | 10 | ✓ Satisfied | 10-VERIFICATION.md Truth #4; 7 new .proto files + generate.sh produces TS for all 15 modules |
| RESIL-01 | 11 | ✓ Satisfied | 11-VERIFICATION.md Truth #1 (21/22 truths verified); all 9 wrapped call sites function under real latency, confirmed by fake-timer regression tests |
| RESIL-02 | 11 | ⚠ Partial | Code-level sanitized OTel span + Sentry capture confirmed wired and loaded in the production boot path (integration check); live Grafana/Sentry dashboard delivery is an open human-verification item, not a code defect |
| SETTLE-01 | 12 | ✓ Satisfied | 12-VERIFICATION.md Truth #1; 5 live callers confirmed (Tour, Marketplace, Events, Studio, Stays), integration-checker independently reproduced the delegation |
| SETTLE-02 | 12 | ✓ Satisfied | 12-VERIFICATION.md Truth #2; live DB row confirmed for Ministry User+Wallet |
| SETTLE-03 | 13 | Orphaned | Phase 13 not started |
| SETTLE-04 | 13 | Orphaned | Phase 13 not started |
| SETTLE-05 | 12 | ✓ Satisfied | 12-VERIFICATION.md Truth #4; regression test proves host credited 42750 not 45000 on a 45000 total at 5% levy |
| SETTLE-06 | 12 | ✓ Satisfied | 12-VERIFICATION.md Truth #3; integration checker confirmed event-name match between WebhooksService.emit() and all 4 @OnEvent consumers |
| SETTLE-07 | 12 | ✓ Satisfied | 12-VERIFICATION.md Truth #5; IDOR-proof, LGA-scoped |
| SETTLE-08 | 12 | ✓ Satisfied | 12-VERIFICATION.md Truth #6; parametrized test across 5 non-round amounts, zero drift |
| SETTLE-09 | 13 | Orphaned | Phase 13 not started |
| MIN-01..07 | 14 | Orphaned (7) | Phase 14 not started |
| OTP-01..04 | 15 | Orphaned (4) | Phase 15 not started |
| POOL-01..02 | 16 | Orphaned (2) | Phase 16 not started |
| GRPC-03..05 | 17 | Orphaned (3) | Phase 17 not started |

**No requirement claimed by an executed phase is unsatisfied.** All gaps trace to either (a) not-yet-started phases (expected) or (b) RESIL-02's single open human-verification item (already correctly scoped as such by Phase 11's own verification, not a new finding).

## Phase Verification Status

| Phase | Plans | VERIFICATION.md Status | Notes |
|---|---|---|---|
| 10 — Documentation Correction + gRPC Build Fix | 3/3 | ✓ passed (4/4 SC, 14/14 plan-level) | Clean. One out-of-scope Docker-build finding carried as tech debt (see INT-02 for a deeper version). |
| 11 — Resilience Wrapping | 11/11 | ⚠ gaps_found (stale) | Sole gap (secret-leak in onBreak() logging) independently confirmed fixed in source (commit b0fcb3c, timestamped after the verification run) and closed in same-day 11-SECURITY.md. Record itself needs a re-run for hygiene — not a live defect. |
| 12 — Settlement Engine Foundation | 9/9 | ✓ passed (6/6) | Clean. All 9 code-review findings (1 Critical, 8 Warnings) confirmed fixed in source. UAT: 10/10 tests passed, 0 issues. |

## Cross-Phase Integration Findings

| Finding | Severity | Blocks Today? | Blocks |
|---|---|---|---|
| INT-01 — ResilienceModule not imported by any of the 8 `backend/apps/*-service` scaffolds; reproduced via `Test.createTestingModule` for all 8 (DI resolution fails) | Latent | No — nothing boots these scaffolds in production | Phase 17 (first live extraction) |
| INT-02 — `@iseyaa/proto` package has no compile step; `nest build` passes via lenient tsc resolution but `require('@iseyaa/proto')` fails at real Node.js runtime | New, deeper than the known Docker-build gap | No — same reason as INT-01 | Phase 16 (pooling load test needs a second real running service) and Phase 17 |
| SettlementService → Paystack refund path | Verified WIRED, no gap | — | Confirms `RefundService.refund()` correctly routes through the resilience-wrapped, zero-retry `paystackRefund` policy — Phase 11 and Phase 12 interoperate correctly |

## Nyquist Compliance

All 3 executed phases (10, 11, 12) have `VALIDATION.md` with `nyquist_compliant: true` and `wave_0_complete: true`. No gaps.

## Recommended Next Steps

This is a **mid-milestone checkpoint**, not a completion gate — v2.0 is 3/8 phases done and continuing per plan is the expected path, not a deviation. Before starting Phase 13:

1. **Re-run Phase 11 verification** (`/gsd-verifier` or equivalent) so `11-VERIFICATION.md` reflects the current, already-fixed state instead of a stale `gaps_found`.
2. **File the two integration findings (INT-01, INT-02) as explicit prerequisite tasks** on Phase 16 and/or Phase 17's plan, since both are latent today but will hard-block Phase 17's first live extraction if not fixed first.
3. **Tighten T-11-03's disposition** in `11-SECURITY.md` to enumerate the 5 remaining raw-`err` Logger call sites (auth.service.ts, delivery.service.ts, ai.service.ts) before Phase 15 adds more Termii/WhatsApp call sites on the same pattern.
4. Continue to Phase 13 (Settlement Cutover — Transport & Delivery) as roadmapped — its dependency (Phase 12) is genuinely complete and verified.

No closure phase is needed for this audit — the "gaps" here are either already-scheduled future phases or small hygiene items that can be folded into Phase 13-17 planning rather than requiring a dedicated insert.

---
*Auditor: Claude (gsd-audit-milestone), with cross-phase integration verification by gsd-integration-checker*
