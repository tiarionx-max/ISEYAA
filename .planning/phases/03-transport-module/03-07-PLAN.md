---
phase: 03-transport-module
plan: 07
type: execute
wave: 6
depends_on: ["03-05"]
files_modified:
  - mobile/app/(tabs)/driver.tsx
  - mobile/app/(tabs)/_layout.tsx
autonomous: true
requirements:
  - TRANSPORT-01
  - TRANSPORT-04
  - TRANSPORT-06
  - TRANSPORT-07
  - TRANSPORT-08

must_haves:
  truths:
    - "Driver tab renders five screens: Home (D-1), Incoming Request (D-2), Active Pickup (D-3), Active Trip (D-4), Earnings Dashboard (D-5)"
    - "Go-online toggle calls POST /transport/go-online; go-offline calls POST /transport/go-offline"
    - "Driver GPS streams via expo-location.watchPositionAsync at 2-second intervals while online"
    - "GPS updates emit `driver:location` over WebSocket and PATCH the heartbeat"
    - "Earnings screen displays today/week toggle with totalEarnings, tripCount, acceptanceRate, avgRating"
    - "_layout.tsx registers BOTH 'transport' and 'driver' tabs (this plan adds them as siblings to existing tabs)"
    - "Drivers in PENDING_REVIEW / REJECTED / SUSPENDED states see warning banner and cannot tap go-online"
  artifacts:
    - path: mobile/app/(tabs)/driver.tsx
      provides: "Driver Tab UI"
      min_lines: 400
    - path: mobile/app/(tabs)/_layout.tsx
      provides: "Tab navigator including transport + driver tabs"
      contains: "transport"
  key_links:
    - from: mobile/app/(tabs)/driver.tsx
      to: backend POST /api/v1/transport/go-online
      via: "api.post('/transport/go-online', { lat, lng })"
      pattern: "/transport/go-online"
    - from: mobile/app/(tabs)/driver.tsx
      to: backend WebSocket gateway
      via: "socket.emit('driver:location', { tripId, lat, lng }) on each Location update"
      pattern: "driver:location"
    - from: mobile/app/(tabs)/driver.tsx
      to: backend GET /api/v1/transport/drivers/earnings
      via: "api.get('/transport/drivers/earnings?period=today|week')"
      pattern: "/transport/drivers/earnings"
    - from: mobile/app/(tabs)/_layout.tsx
      to: mobile/app/(tabs)/transport.tsx, driver.tsx
      via: "Tabs.Screen name='transport' / name='driver'"
      pattern: "name=\"transport\""
---

<objective>
Build the driver-side mobile experience — the second half of TRANSPORT-08. The driver tab is operationally critical: it controls when a driver receives ride requests (online toggle), reads incoming requests (15-second window), navigates to pickup, completes trips, and shows earnings. Also wires both new tabs into the tab navigator.

Purpose: Without this tab, drivers cannot use the transport module — and without the tab navigator update, neither tab is reachable. Shares the dependencies installed in Plan 06 but operates on a separate file (no parallel-execution conflict with Plan 06).

Output: `driver.tsx` (~450 lines) implementing five screens with go-online toggle, incoming-request modal, live navigation, complete-trip flow, and earnings dashboard. `_layout.tsx` updated with two new Tabs.Screen entries placed before the profile tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-transport-module/03-05-SUMMARY.md
@.planning/phases/03-transport-module/03-RESEARCH.md
@.planning/phases/03-transport-module/03-PATTERNS.md
@.planning/phases/03-transport-module/03-UI-SPEC.md

<interfaces>
<!-- Backend endpoints from Plan 05 controller -->
POST  /api/v1/transport/go-online   Body: { lat, lng } — 200 OK
POST  /api/v1/transport/go-offline  — 200 OK
PATCH /api/v1/transport/trips/:id/accept   — 200 OK
PATCH /api/v1/transport/trips/:id/decline  — 200 OK
PATCH /api/v1/transport/trips/:id/complete  Body: CompleteTripDto — 200 OK
GET   /api/v1/transport/drivers/earnings?period=today|week
       Returns: { totalEarnings, tripCount, acceptanceRate, avgRating, history?: Trip[] }

<!-- Backend WebSocket events received by driver -->
'ride:request' -> driver receives { tripId, pickup, dropoff, fare, riderName } (server emits to driver:{driverId} room when requestRide finds driver)

<!-- Backend WebSocket events emitted by driver -->
'join:driver'     -> driver socket joins driver:{userId} room on connect (so server can target this driver)
'driver:location' -> driver emits { tripId, lat, lng } every 2s during a trip

<!-- expo-location pattern -->
const subscription = await Location.watchPositionAsync(
  { accuracy: Location.Accuracy.High, timeInterval: 2000, distanceInterval: 0 },
  (location) => { ... }
);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add transport + driver tabs to (tabs)/_layout.tsx</name>
  <files>mobile/app/(tabs)/_layout.tsx</files>
  <read_first>
    - mobile/app/(tabs)/_layout.tsx (full file — find existing Tabs.Screen entries; locate the profile tab to insert before it)
    - .planning/phases/03-transport-module/03-PATTERNS.md (mobile _layout section — Tabs.Screen pattern)
  </read_first>
  <action>
    Edit `mobile/app/(tabs)/_layout.tsx`:

    1. Add to the lucide-react-native import block: `Car, Truck` (preserve existing imports).

    2. After the existing tab screens (events, stays, etc.) and BEFORE the `profile` tab, insert two new Tabs.Screen entries:
       ```
       <Tabs.Screen
         name="transport"
         options={{
           title: 'Transport',
           tabBarIcon: ({ color, size }) => <Car color={color} size={size} />,
         }}
       />
       <Tabs.Screen
         name="driver"
         options={{
           title: 'Driver',
           tabBarIcon: ({ color, size }) => <Truck color={color} size={size} />,
         }}
       />
       ```

    3. Do NOT modify `screenOptions` at the parent — header settings inherit correctly.

    4. Run `npx tsc --noEmit -p mobile/tsconfig.json` — must exit 0.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p mobile/tsconfig.json && grep -c "name=\"transport\"" mobile/app/\(tabs\)/_layout.tsx && grep -c "name=\"driver\"" mobile/app/\(tabs\)/_layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'name="transport"' mobile/app/\\(tabs\\)/_layout.tsx` returns 1
    - `grep -c 'name="driver"' mobile/app/\\(tabs\\)/_layout.tsx` returns 1
    - `grep -c "title: 'Transport'" mobile/app/\\(tabs\\)/_layout.tsx` returns 1
    - `grep -c "title: 'Driver'" mobile/app/\\(tabs\\)/_layout.tsx` returns 1
    - `grep -c "Car\\|Truck" mobile/app/\\(tabs\\)/_layout.tsx` returns at least 2
    - `npx tsc --noEmit -p mobile/tsconfig.json` exits 0
  </acceptance_criteria>
  <done>
    Two new tab screens registered before profile; type-check passes; tabs are reachable in the running app.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement driver.tsx — five screens (D-1 through D-5)</name>
  <files>mobile/app/(tabs)/driver.tsx</files>
  <read_first>
    - mobile/app/(tabs)/stays.tsx (full file — copy SafeAreaView wrapper, color tokens, StyleSheet idioms, useQuery patterns)
    - mobile/app/(tabs)/transport.tsx (Plan 06 — already imports MapView, socket.io-client, expo-location; copy the socket-connect useEffect skeleton)
    - mobile/lib/api.ts (axios + 'access_token' SecureStore key)
    - .planning/phases/03-transport-module/03-UI-SPEC.md (Screens D-1 through D-5; Copywriting Contract for driver lines)
    - .planning/phases/03-transport-module/03-RESEARCH.md (TRANSPORT-04 GPS watch pattern; Pattern 5 socket connect; Pitfall 6 Apple Maps for iOS)
  </read_first>
  <action>
    Create `mobile/app/(tabs)/driver.tsx` implementing the driver flow per UI-SPEC.md D-1..D-5.

    Top-of-file imports (same set as transport.tsx with additions):
    - `View, Text, TouchableOpacity, StyleSheet, ActivityIndicator, Platform, ScrollView, FlatList, Animated, Modal, Alert`
    - `SafeAreaView` from `react-native-safe-area-context`
    - `useEffect, useState, useRef` from `react`
    - `useQuery` from `@tanstack/react-query`
    - `api` from `../../lib/api`
    - `MapView, Marker` from `react-native-maps`
    - `io, Socket` from `socket.io-client`
    - `* as SecureStore` from `expo-secure-store`
    - `* as Location` from `expo-location`
    - lucide icons: `Car, MapPin, Star, Clock, AlertCircle, DollarSign, Navigation, CheckCircle`
    - Color constants: `FOREST`, `GOLD`, `JUNGLE` (same values as transport.tsx)

    State machine:
    ```
    type Screen = 'home' | 'incoming' | 'pickup' | 'active' | 'earnings';
    type DriverStatus = 'PENDING_REVIEW' | 'APPROVED' | 'REJECTED' | 'SUSPENDED' | null;
    ```

    Component state: `screen`, `isOnline`, `driverStatus`, `currentTrip`, `riderLocation`, `earningsPeriod` ('today' | 'week'), `respondCountdown` (15s for incoming requests), `socketRef`, `locationWatchRef`, `goOnlineConfirmOpen`, `goOfflineConfirmOpen`, `cancelOpen`, `completeConfirmOpen`.

    **D-1 Driver Home:**
    - Header "Driver"
    - Driver status row (dot 10×10 + label) — green dot when online, gray when offline
    - Large 120×120 circular toggle button:
      - Online state: `backgroundColor: FOREST, borderColor: '#22C55E', borderWidth: 3`, label "GO\nOFFLINE"
      - Offline state: `backgroundColor: 'rgba(255,255,255,0.07)', borderColor: '#6B7280', borderWidth: 3`, label "GO\nONLINE"
    - When tapped offline → request location permission → on grant POST /transport/go-online with current GPS → optimistic toggle → start `Location.watchPositionAsync` at 2s interval → emit `socket.emit('driver:location', ...)` on each update; if permission denied → Alert with "Location access is required..."
    - When tapped online → open Go-offline confirmation bottom sheet → on confirm POST /transport/go-offline → stop watchPositionAsync (locationWatchRef.current?.remove()) → toggle to offline
    - Earnings chip below toggle: "Today's earnings" + amount in GOLD 24px (uses `useQuery` against /transport/drivers/earnings?period=today)
    - Driver-status warning banner shown when driverStatus !== 'APPROVED' (red-tinted card, copy varies per status from UI-SPEC.md Copywriting Contract)
    - "View Earnings Dashboard" text button → transitions to 'earnings'

    **D-2 Incoming Request:** (modal-style overlay)
    - Triggered by WS `ride:request` event
    - Animated.timing progress bar (full width to 0% over 15s; switches red < 5s remaining)
    - "{N}s to respond" label
    - Pickup address, Dropoff address, Distance, Fare highlight (24px GOLD)
    - Two side-by-side buttons: Accept (FOREST bg) and Decline (red destructive)
    - On Accept → PATCH /transport/trips/:id/accept → transitions to 'pickup'
    - On Decline → PATCH /transport/trips/:id/decline → returns to 'home'
    - On 15s timeout → automatically dismiss; trip stays in SEARCHING (server may retry next driver); affects driver acceptance rate

    **D-3 Active Pickup:**
    - MapView fullscreen with single Marker at rider pickup location (GOLD pin); driver's current GPS shown as heading-arrow marker
    - Bottom-sheet overlay: rider name, pickup address (MapPin icon GOLD), ETA "~N min to pickup"
    - "I've Arrived" CTA — disabled (opacity 0.4) when driver GPS distance to pickup > 200m; label changes to "Approach pickup point" when disabled
    - Compute distance via Haversine inline (no need for backend call)
    - On tap (when enabled) → PATCH /transport/trips/:id (set status ARRIVED — Phase 3 may not have a separate endpoint; use accept→arrived state via /transport/trips/:id/start or simply set arrivedAt by including in the trip update payload — coordinate with Plan 04 service contract; if no separate endpoint exists, the rider sees driver arriving via the driver:location stream and the driver advances to D-4 by tapping) → transitions to 'active'

    **D-4 Active Trip:**
    - MapView with destination Marker (GOLD); driver heading-arrow marker
    - Bottom-sheet: destination address (Navigation icon), fare reminder "₦{fare} — your share: ₦{driverEarnings}" in 24px GOLD
    - "Complete Trip" CTA (FOREST bg) → opens Complete confirmation bottom sheet → on confirm PATCH /transport/trips/:id/complete → on success show inline green success banner "₦{driverEarnings} credited to your wallet." → wait 2 seconds → transitions to 'earnings'
    - WS `driver:location` continues emitting every 2s

    **D-5 Earnings Dashboard:**
    - Header "Earnings"
    - Period segmented control (Today | This Week) — active segment FOREST bg
    - Total earnings 48px GOLD centered
    - Stats row: Trips count, Acceptance rate %, Avg rating ★ — three equal-width chips
    - FlatList of trip history rows (date + route summary + fare in GOLD)
    - Empty state: "No trips completed {period}. Go online to start earning."
    - useQuery for `/transport/drivers/earnings?period={period}` invalidates on period change

    **GPS lifecycle requirements:**
    - `Location.watchPositionAsync` started ONLY when isOnline becomes true
    - Stopped on go-offline or component unmount (`locationWatchRef.current?.remove()`)
    - Each location update: `socket.emit('driver:location', { tripId: currentTrip?.id ?? null, lat, lng })` AND `api.post('/transport/go-online', { lat, lng })` (heartbeat refresh — backend treats as upsert)

    **Socket lifecycle (useEffect on mount):**
    Same shape as transport.tsx, plus:
    ```
    socket.on('connect', () => socket.emit('join:driver'));
    socket.on('ride:request', (req) => { setCurrentTrip(req); setRespondCountdown(15); setScreen('incoming'); });
    ```

    File target: ~450 lines.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p mobile/tsconfig.json && wc -l mobile/app/\(tabs\)/driver.tsx | awk '{ if ($1 < 400) { print "TOO SHORT: " $1; exit 1 } else { print "OK: " $1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at least 400 lines
    - Default export `function DriverScreen` declared
    - `grep -c "transports: \\['websocket'\\]" mobile/app/\\(tabs\\)/driver.tsx` returns at least 1
    - `grep -c "Platform.OS === 'android' ? 'google' : undefined" mobile/app/\\(tabs\\)/driver.tsx` returns at least 1
    - `grep -c "watchPositionAsync" mobile/app/\\(tabs\\)/driver.tsx` returns at least 1
    - `grep -c "timeInterval: 2000" mobile/app/\\(tabs\\)/driver.tsx` returns at least 1
    - `grep -c "/transport/go-online" mobile/app/\\(tabs\\)/driver.tsx` returns at least 1
    - `grep -c "/transport/go-offline" mobile/app/\\(tabs\\)/driver.tsx` returns at least 1
    - `grep -c "/transport/trips/" mobile/app/\\(tabs\\)/driver.tsx` returns at least 3 (accept, decline, complete)
    - `grep -c "/transport/drivers/earnings" mobile/app/\\(tabs\\)/driver.tsx` returns at least 1
    - `grep -c "ride:request" mobile/app/\\(tabs\\)/driver.tsx` returns at least 1
    - `grep -c "join:driver" mobile/app/\\(tabs\\)/driver.tsx` returns at least 1
    - `grep -c "credited to your wallet" mobile/app/\\(tabs\\)/driver.tsx` returns at least 1 (UI-SPEC verbatim)
    - `grep -c "GO\\\\nOFFLINE\\|GO\\\\nONLINE" mobile/app/\\(tabs\\)/driver.tsx` returns at least 1
    - `grep -c "PENDING_REVIEW\\|REJECTED\\|SUSPENDED" mobile/app/\\(tabs\\)/driver.tsx` returns at least 3 (driver-status branches)
    - `npx tsc --noEmit -p mobile/tsconfig.json` exits 0
  </acceptance_criteria>
  <done>
    driver.tsx implements all five driver screens with GPS watch, WS emit, REST calls, and the driver-status guard banners; type-check passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Driver mobile → Backend WS (driver:location) | Driver self-reports location; backend trusts but RedisService writes are throttled by heartbeat TTL |
| Driver mobile → Backend REST (go-online, accept) | JWT-authenticated; @Roles(DRIVER) gate at controller |
| User → expo-location | OS-level permission required; denial gracefully blocks go-online |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-28 | Spoofing | Driver fakes GPS to appear closer to riders | accept | No anti-spoofing in Phase 3 MVP; Phase 6 will add Haversine plausibility check on consecutive positions |
| T-03-29 | Information Disclosure | Driver sees rider exact home address before accepting | accept | UI-SPEC shows pickup address on incoming-request card — necessary for accept/decline decision; no PII reduction in MVP |
| T-03-30 | Repudiation | Driver claims they never accepted a trip | mitigate | TripEvent log records DRIVER_MATCHED with timestamp + driverId on accept |
| T-03-31 | Tampering | Driver replays accept after another driver took the trip | mitigate | acceptTrip checks `trip.status === 'SEARCHING'` and `trip.driverId === null` — second accept fails with BadRequest |
| T-03-32 | Tampering | Suspended driver reactivates by clearing app data | mitigate | goOnline server-side checks driver.status === 'APPROVED' on every call — local state is advisory only |
</threat_model>

<verification>
- `_layout.tsx` registers transport + driver tabs
- `driver.tsx` ≥400 lines covering 5 screens
- All grep gates pass
- `npx tsc --noEmit -p mobile/tsconfig.json` exits 0
</verification>

<success_criteria>
- Two new tabs reachable in mobile app
- Go-online/offline lifecycle correct (REST + GPS watch start/stop)
- Incoming request modal with 15s timer
- Active pickup/trip flows with map + WS GPS emit
- Earnings dashboard with period toggle
- Driver-status banners for non-APPROVED states
- Visual verification deferred to Plan 08
</success_criteria>

<output>
After completion, create `.planning/phases/03-transport-module/03-07-SUMMARY.md` documenting:
- Files modified
- Confirmation that Plans 06 and 07 do not share files (parallel-safe)
- Note that human visual verification happens in Plan 08
</output>
