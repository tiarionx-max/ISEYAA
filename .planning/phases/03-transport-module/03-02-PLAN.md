---
phase: 03-transport-module
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/prisma/seed.ts
autonomous: true
requirements:
  - TRANSPORT-02
  - TRANSPORT-05
  - TRANSPORT-06

must_haves:
  truths:
    - "Database physically contains drivers, vehicles, trips, trip_events tables matching schema.prisma"
    - "Prisma Client (node_modules/.prisma/client) re-generated with Driver, Vehicle, Trip, TripEvent types"
    - "platform_configs table has rows for transport_platform_fee_pct, transport_base_fare_*, transport_per_km_*, transport_surge_threshold, transport_match_radius_km"
  artifacts:
    - path: backend/prisma/seed.ts
      provides: "Idempotent seed for transport PlatformConfig keys"
      contains: "transport_platform_fee_pct"
  key_links:
    - from: backend/src/modules/transport/transport.service.ts (future)
      to: platform_configs table (PlatformConfig model)
      via: "prisma.platformConfig.findUnique({ where: { key: 'transport_platform_fee_pct' } })"
      pattern: "transport_platform_fee_pct"
---

<objective>
Push the schema changes from Plan 01 to the live database, regenerate Prisma Client, and seed the eleven PlatformConfig rows that drive fare calculation, surge thresholds, and match radius. This is the [BLOCKING] schema-push gate — no later plan can compile against the new models until this completes successfully.

Purpose: TransportService (Plan 04) and the test scaffolding (Plan 03) both reference `prisma.driver`, `prisma.trip`, `prisma.vehicle`, and `prisma.tripEvent` — these accessors only exist after `prisma generate` runs against the updated schema. Surge calculation and fare estimation read multipliers/rates from the platform_configs table, so the seed must run before any TransportService unit test that mocks PlatformConfig lookups (the mocks must match real seed values).

Output: Live Neon DB has new tables, Prisma Client has new types, seed inserts/updates 11 transport PlatformConfig rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-transport-module/03-01-SUMMARY.md
@.planning/phases/03-transport-module/03-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING] Push Prisma schema to database and regenerate Prisma Client</name>
  <files>(database state — drivers, vehicles, trips, trip_events tables; node_modules/.prisma/client/*)</files>
  <read_first>
    - backend/prisma/schema.prisma (confirm Plan 01 additions are present and `prisma validate` would pass)
    - .env.example (confirm DATABASE_URL is the env var name used)
    - .planning/phases/03-transport-module/03-01-SUMMARY.md (verify Plan 01 is complete)
  </read_first>
  <action>
    Run from repo root, in order:

    1. `npx prisma generate --schema=backend/prisma/schema.prisma` — regenerate the Prisma Client with the new models. Must produce no errors.

    2. `npx prisma db push --schema=backend/prisma/schema.prisma --accept-data-loss=false` — push schema to the Neon dev branch pointed at by `DATABASE_URL`. This creates the `drivers`, `vehicles`, `trips`, `trip_events` tables and adds the 3 new enum types (vehicle_type, driver_status, trip_status). Use `db push` not `migrate dev` because Phase 2 established `db push` as the standard for Neon dev (no migration files committed for the dev branch — production branch uses `migrate deploy` separately).

    Per the schema_push_requirement in the orchestrator init: this task is mandatory — phase verification cannot pass without these tables existing.

    If `db push` fails with a "DATABASE_URL not set" error, surface the error and halt — do not attempt to bypass with a default URL.
  </action>
  <verify>
    <automated>npx prisma generate --schema=backend/prisma/schema.prisma && node -e "const { PrismaClient } = require('./backend/node_modules/@prisma/client'); const p = new PrismaClient(); Promise.all([p.driver.count(), p.vehicle.count(), p.trip.count(), p.tripEvent.count()]).then(r => { console.log('drivers:',r[0],'vehicles:',r[1],'trips:',r[2],'trip_events:',r[3]); process.exit(0); }).catch(e => { console.error(e.message); process.exit(1); });"</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma generate` exits 0 with no "Schema parsing" or "validation" errors
    - `npx prisma db push` exits 0 and reports either "Your database is now in sync with your Prisma schema" OR lists the four new tables under "Added"
    - Inline node script that calls `prisma.driver.count()`, `prisma.vehicle.count()`, `prisma.trip.count()`, `prisma.tripEvent.count()` exits 0 and prints all four counts (counts may be 0; non-existence raises a Prisma error and exits 1)
    - Generated Prisma Client file `backend/node_modules/@prisma/client/index.d.ts` (or runtime/library.d.ts) contains the type names `Driver`, `Vehicle`, `Trip`, `TripEvent` (verify with grep)
    - No `_prisma_migrations` table churn for production branch (db push only)
  </acceptance_criteria>
  <done>
    Live database has the 4 new tables, Prisma Client has matching TypeScript types, all four model accessors work without error.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Seed transport PlatformConfig rows (idempotent upsert)</name>
  <files>backend/prisma/seed.ts</files>
  <read_first>
    - backend/prisma/seed.ts (full file — find existing PlatformConfig seed pattern, append transport keys; use the same `prisma.platformConfig.upsert` pattern as existing seeds)
    - .planning/phases/03-transport-module/03-RESEARCH.md (PlatformConfig Seeds section — exact key names and values)
    - CLAUDE.md (constraint: "Platform fee source: Always from DB (platformConfig table), never hardcoded" — confirms why these must be seeded, not constants)
  </read_first>
  <action>
    Edit `backend/prisma/seed.ts`. Locate the existing PlatformConfig seed block (look for any existing `prisma.platformConfig.upsert` calls or a `PlatformConfig` section). Append upsert calls for these eleven rows, each using `prisma.platformConfig.upsert({ where: { key }, create: { key, value, isPublic: false }, update: { value } })`:

    | key | value (JSON) |
    |---|---|
    | `transport_platform_fee_pct` | `15` |
    | `transport_base_fare_bike` | `200` |
    | `transport_base_fare_tricycle` | `350` |
    | `transport_base_fare_car` | `500` |
    | `transport_base_fare_minibus` | `700` |
    | `transport_per_km_bike` | `50` |
    | `transport_per_km_tricycle` | `80` |
    | `transport_per_km_car` | `120` |
    | `transport_per_km_minibus` | `150` |
    | `transport_surge_threshold` | `1.5` |
    | `transport_match_radius_km` | `5` |

    All `value` fields are stored as JSON numbers (not strings — the column is `Json` per the PlatformConfig model). Mark `isPublic: false` for all (these are server-only configs; only fare estimate response exposes the computed fare and surge multiplier, never the raw config).

    Then run the seed: `npx prisma db seed --schema=backend/prisma/schema.prisma` (or whatever script is wired in `backend/package.json` `prisma.seed` field — read it first; if no seed runner is configured, run with `npx ts-node backend/prisma/seed.ts`).

    Wrap each upsert in a try/catch that logs failures but does not abort the seed (idempotency).
  </action>
  <verify>
    <automated>node -e "const { PrismaClient } = require('./backend/node_modules/@prisma/client'); const p = new PrismaClient(); const keys = ['transport_platform_fee_pct','transport_base_fare_bike','transport_base_fare_tricycle','transport_base_fare_car','transport_base_fare_minibus','transport_per_km_bike','transport_per_km_tricycle','transport_per_km_car','transport_per_km_minibus','transport_surge_threshold','transport_match_radius_km']; Promise.all(keys.map(k => p.platformConfig.findUnique({ where: { key: k } }))).then(rows => { const missing = keys.filter((k,i)=>!rows[i]); if (missing.length) { console.error('MISSING',missing); process.exit(1); } console.log('all 11 transport keys present'); process.exit(0); });"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "transport_platform_fee_pct" backend/prisma/seed.ts` returns at least 1
    - `grep -c "transport_base_fare_" backend/prisma/seed.ts` returns at least 4 (one per vehicle type)
    - `grep -c "transport_per_km_" backend/prisma/seed.ts` returns at least 4
    - `grep -c "transport_surge_threshold" backend/prisma/seed.ts` returns at least 1
    - `grep -c "transport_match_radius_km" backend/prisma/seed.ts` returns at least 1
    - `grep -c "platformConfig.upsert" backend/prisma/seed.ts` returns at least 11 new occurrences (or fewer if existing call signature is reused)
    - Seed script runs to completion without throwing (last log line is the seed completion message)
    - Verification node script confirms all 11 rows exist in `platform_configs` table and exits 0
    - `transport_platform_fee_pct` row has `value: 15` (read via Prisma — assert `Number(row.value) === 15`)
    - `transport_surge_threshold` row has `value: 1.5`
  </acceptance_criteria>
  <done>
    Seed file contains all 11 transport platform config upsert calls; seed runs idempotently; database has all 11 rows with correct numeric values.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed script → DB | Seed runs with elevated DB privileges; idempotent upserts must not corrupt existing PlatformConfig rows |
| Migration tool → Production data | `db push` on dev branch is safe; production branch will use `migrate deploy` in Phase 7 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | PlatformConfig values overwritten by stale seed | mitigate | Upsert `update: { value }` deliberately overwrites — but this only runs in dev/seed environments; production config changes go through admin module (Phase 5+) |
| T-03-06 | Denial of Service | `db push` against production branch by accident | mitigate | DATABASE_URL pinned to dev branch in dev env; production branch uses `migrate deploy` only (separate Plan in Phase 7); no destructive flags used (`--accept-data-loss=false`) |
| T-03-07 | Information Disclosure | Public surface of platform_configs reveals fee structure | accept | All 11 rows have `isPublic: false`; only computed fare estimate is exposed via API |
</threat_model>

<verification>
- `prisma generate` succeeds
- `prisma db push` succeeds against the configured DATABASE_URL
- All 11 transport PlatformConfig rows exist with the exact numeric values specified
- Seed re-run is idempotent (no duplicate rows; second run does not error)
</verification>

<success_criteria>
- Database has drivers, vehicles, trips, trip_events tables
- Prisma Client TypeScript types exported for Driver/Vehicle/Trip/TripEvent
- 11 transport_* PlatformConfig rows seeded
- No production data lost (dev branch only)
</success_criteria>

<output>
After completion, create `.planning/phases/03-transport-module/03-02-SUMMARY.md` documenting:
- Output of `prisma db push` (Added tables list)
- Confirmation of all 11 PlatformConfig rows
- Confirmation that production branch was NOT touched
</output>
