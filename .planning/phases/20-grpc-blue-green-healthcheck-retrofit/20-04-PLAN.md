---
phase: 20-grpc-blue-green-healthcheck-retrofit
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/tour-bookings/__tests__/wallet-invariant.e2e-spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [GRPC-06c]

must_haves:
  truths:
    - "npm run test:e2e:tours passes green locally against a real Postgres + Redis instance (D-09 folded scope's exit criterion)"
    - "wallet-invariant.e2e-spec.ts's 6 INV-* tests assert against the actual current architecture (TourSettlementService delegating wallet fan-out to the shared SettlementService.settle()) instead of a stale, no-longer-executed direct $transaction mock"
    - "CI runs test:e2e:tours on every PR/push alongside the existing test:e2e:settlement-splits step, so this suite can never silently regress to broken again without anyone noticing"
  artifacts:
    - path: "backend/src/modules/tour-bookings/__tests__/wallet-invariant.e2e-spec.ts"
      provides: "TOUR-10 wallet invariant regression coverage, rewritten to mock SettlementService.settle() and VisitorLogService.record() directly"
    - path: ".github/workflows/ci.yml"
      provides: "New 'E2E tests (tour booking + wallet invariant)' step running test:e2e:tours"
  key_links:
    - from: "backend/src/modules/tour-bookings/__tests__/wallet-invariant.e2e-spec.ts"
      to: "SettlementService.settle()'s SettlementInput.recipients array"
      via: "mockSettlementService.settle.mock.calls[N][0].recipients assertions"
      pattern: "mockSettlementService"
---

<objective>
Finish D-09's folded scope: get `npm run test:e2e:tours` fully green by fixing `wallet-invariant.e2e-spec.ts` — a test file that has drifted from the codebase it's meant to guard since Phase 12/18's `SettlementService` centralization moved the actual wallet-mutation logic out of `TourSettlementService` entirely — then wire the now-green suite into CI permanently.

Purpose: `TourSettlementService.handleTourBookingPayment()` no longer runs its own `$transaction` — it resolves the N-way vendor split and hands the result to `this.settlementService.settle({ recipients, ... })`. `wallet-invariant.e2e-spec.ts` still mocks `prisma.$transaction` directly and asserts on captured wallet-update/transaction-create calls that never happen anymore in the real code path — every assertion in this file is currently exercising dead code, not the TOUR-10 invariant it claims to protect. This plan re-targets every assertion at the actual current boundary (the `recipients` array and `onSettled`/`onFailure` callbacks TourSettlementService hands to the mocked `SettlementService`), which IS TourSettlementService's real, remaining responsibility (vendor-resolution logic — the transactional primitives themselves are SettlementService's job, already covered by `settlement.service.spec.ts`).
Output: A rewritten `wallet-invariant.e2e-spec.ts` that meaningfully tests TourSettlementService's actual vendor-resolution logic, `test:e2e:tours` green, and permanent CI coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-grpc-blue-green-healthcheck-retrofit/20-CONTEXT.md
@.planning/phases/20-grpc-blue-green-healthcheck-retrofit/20-RESEARCH.md

<interfaces>
CONFIRMED via live reproduction during planning (`npm run test:e2e:tours` run against real local Postgres/Redis): `wallet-invariant.e2e-spec.ts` fails with `Nest can't resolve dependencies of the TourSettlementService (PrismaService, RefundService, EventEmitter, KafkaService, ?, VisitorLogService). Please make sure that the argument SettlementService at index [4] is available` — its hand-built `Test.createTestingModule` in `makeService()` (lines 183-191) only provides `PrismaService`/`RefundService`/`EventEmitter2`/`KafkaService`, missing the 2 constructor params `TourSettlementService` gained in Phase 12/18/19: `SettlementService` and `VisitorLogService`.

`TourSettlementService`'s actual current constructor (`backend/src/modules/tour-bookings/tour-settlement.service.ts`, confirmed by direct read): `constructor(private prisma: PrismaService, private refundService: RefundService, private eventEmitter: EventEmitter2, private kafka: KafkaService, private settlementService: SettlementService, private visitorLogService: VisitorLogService) {}`.

`TourSettlementService.handleTourBookingPayment()`'s actual current flow (confirmed by direct read, lines 116-332 — this is the ground truth this spec must be rewritten against): resolves each `SplitEntry` from `booking.snapshot.settlementSplit` into a `ResolvedSplit` (walletUserId lookup per vendorType: GUIDE→`tourGuide.findUnique().userId`, HOST→`property.findUnique().hostId`, ORGANISER→`event.findUnique().organizerId`, ATTRACTION→`tour.government_wallet_user_id` PlatformConfig value; then `wallet.findUnique({where:{userId}})` for the resolved wallet id — `walletId` is `null` if the vendor/wallet lookup fails, this entry is still INCLUDED in the recipients array, not filtered out). Maps ALL resolved entries (including null-walletId ones) into `SettlementRecipient[]`: `{ tag: entry.vendorType, refSuffix: 'V-' + idx, walletId, amountNgn: round(percentage/100 * chargeAmountNgn, 2 decimals), metadata: { vendorId, packageName, percentage } }`. Computes `attractionsRolledIn` separately (ATTRACTION entries with null walletId, by vendorId). Calls exactly once: `await this.settlementService.settle({ module: 'tour_booking', reference: payload.reference, gateway: 'PAYSTACK', amountKobo: payload.amount, recipients, buyerWalletId: buyerWallet?.id, description: 'Tour booking commission', platformMetadata: { bookingId, attractionsRolledIn }, onSettled: async (tx) => { if (!isSplitBillChild) await tx.tourBooking.update({...CONFIRMED...}); }, onFailure: async (err) => { await this.prisma.tourBooking.update({ where: { id: booking.id }, data: { status: 'REFUNDED', metadata: {...settlementError: err.message...} } }); } })`. The return value of `settle()` is NEVER inspected/branched on. AFTER this call, unconditionally proceeds to split-bill bookkeeping / `recordVisitorEntry()` / `eventEmitter.emit('tour_booking.confirmed', ...)`.

The ONLY remaining direct call to `this.refundService.refund(...)` inside `TourSettlementService` is in the separate pre-flight `refundInvalidSplit()` method (sum of split percentages > 100%, BEFORE `settle()` is ever called) — this path is UNCHANGED by Phase 12/18 and the OLD test never exercised it, so it needs no new coverage here (out of this plan's scope — not part of the 6 existing INV-* tests).

`SettlementRecipient` / `SettlementInput` / `SettlementResult` types (`backend/src/common/services/settlement.service.ts:34-67`, confirmed by direct read): `SettlementRecipient = { tag: string; refSuffix: string; walletId: string | null; amountNgn: number; metadata?: Record<string, unknown> }`; `SettlementInput` includes `onSettled?: (tx: Prisma.TransactionClient) => Promise<void>` and `onFailure?: (err: Error) => Promise<void>`; `SettlementResult = { status: 'SETTLED' | 'REPLAYED'; platformAmountNgn: number; recipientCredits: {...}[] }`.

Existing mocking convention already used by `stays.service.spec.ts` (confirmed) for `SettlementService`/`VisitorLogService`: `const mockSettlement = { settle: jest.fn().mockResolvedValue({ status: 'SETTLED', platformAmountNgn: 0, recipientCredits: [] }), resolveMinistryWallet: jest.fn(), resolveSplit: jest.fn() };` and `const mockVisitorLog = { record: jest.fn().mockResolvedValue(undefined) };` — reuse this exact shape (TourSettlementService only calls `settle()` on the mocked SettlementService, never `resolveMinistryWallet`/`resolveSplit`, but include stub jest.fn()s for type-completeness since `SettlementService`'s real class exposes them).

Current `wallet-invariant.e2e-spec.ts` full structure (413 lines, confirmed by direct read): `makeService()` (lines 162-196) builds `mockPrisma`/`mockRefund`/`mockEvents`/`mockKafka` and a `Test.createTestingModule` with 4 providers — needs 2 more. `wireTransaction()` (lines 87-130) mocks `mockPrisma.$transaction` to capture wallet/transaction/booking updates into a `txn` object — THIS HELPER AND THE `txn` CAPTURE OBJECT ARE NO LONGER MEANINGFUL and must be removed; `TourSettlementService` never calls `this.prisma.$transaction` anymore, all 6 `wireTransaction()` call sites in the 6 `it(...)` blocks must be deleted along with their `txn.*` assertions. 6 test bodies at lines 228-412 (`INV-1` through `INV-6`) each currently assert against `txn.transactionCreates`/`txn.walletUpdates` — every one of these assertions must be re-targeted at `mockSettlementService.settle.mock.calls[i][0]` (the `SettlementInput` argument) instead.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite wallet-invariant.e2e-spec.ts to mock the centralized SettlementService boundary</name>
  <files>backend/src/modules/tour-bookings/__tests__/wallet-invariant.e2e-spec.ts</files>
  <read_first>backend/src/modules/tour-bookings/__tests__/wallet-invariant.e2e-spec.ts, backend/src/modules/tour-bookings/tour-settlement.service.ts, backend/src/common/services/settlement.service.ts, backend/src/modules/stays/__tests__/stays.service.spec.ts</read_first>
  <behavior>
    - INV-1 (100% split, GUIDE 70% + HOST 30%, ₦10,000 charge): `settle()` called exactly once; `recipients` has length 2; the GUIDE entry has `{ tag: 'GUIDE', refSuffix: 'V-0', walletId: GUIDE_WALLET_ID, amountNgn: 7000 }`; the HOST entry has `{ tag: 'HOST', refSuffix: 'V-1', walletId: HOST_WALLET_ID, amountNgn: 3000 }`; sum of `recipients.map(r => r.amountNgn)` equals 10000 (invariant: a 100%-claimed split leaves nothing for platform to absorb).
    - INV-2 (partial split, GUIDE 50% + HOST 30%, ₦10,000 charge): sum of `recipients.map(r => r.amountNgn)` equals 8000 — the unclaimed 20% (₦2,000) is deliberately NOT present in `recipients` (platform absorbs it inside `SettlementService.settle()`, out of this test's boundary).
    - INV-3 (ATTRACTION with unset gov wallet, GUIDE 60% + ATTRACTION 40%, ₦5,000 charge): `recipients` has length 2 (GUIDE resolved + ATTRACTION included with `walletId: null` — NOT filtered out, per the confirmed current resolution logic); the ATTRACTION entry has `amountNgn: 2000` and `walletId: null`; `input.platformMetadata.attractionsRolledIn` contains `ATTRACTION_VENDOR_ID`.
    - INV-4 (idempotency-adjacent — reframed): idempotency itself now lives entirely inside `SettlementService.settle()` (confirmed: `TourSettlementService` has no local `transaction.findFirst` precheck of its own). Rewritten intent: prove `TourSettlementService` does NOT short-circuit its own call to `settle()` based on any local state — mock `mockSettlementService.settle` to resolve `{ status: 'REPLAYED', platformAmountNgn: 0, recipientCredits: [] }` and assert `settle()` was still called exactly once with the correctly resolved `recipients` (proving TourSettlementService always delegates to SettlementService's own idempotency check rather than adding a redundant one).
    - INV-5 (failure path): mock `mockSettlementService.settle` with a `mockImplementation` that (a) invokes the `input.onFailure(new Error('boom — vendor wallet update failed'))` callback it was passed (simulating what the real `SettlementService.settle()` does internally on failure) and (b) rejects/throws so the promise the caller awaits rejects. Assert `svc.handleTourBookingPayment(...)` rejects with `/boom/`. Assert `mockPrisma.tourBooking.update` was called with `{ where: { id: BOOKING_ID }, data: expect.objectContaining({ status: 'REFUNDED' }) }` — this assertion is UNCHANGED from the original test since `onFailure`'s body in `TourSettlementService` itself is untouched by Phase 12/18. REMOVE the `mockRefund.refund` assertion entirely — the actual `RefundService.refund()` call for this failure path now happens inside the real `SettlementService.settle()`, which is mocked out in this unit test and is already covered by `settlement.service.spec.ts`'s own tests, not this file's job to re-verify.
    - INV-6 (multiple charge amounts, GUIDE 60% + HOST 25% = 85% claimed): for each of the 3 test amounts, assert sum of `recipients.map(r => r.amountNgn)` is within 0.02 of `expectedNgn * 0.85` (NOT `expectedNgn` — the unclaimed 15% is, as in INV-2, deliberately absent from `recipients`).
  </behavior>
  <action>
    Add imports: `SettlementService, SettlementRecipient` from `'../../../common/services/settlement.service'` and `VisitorLogService` from `'../../../common/services/visitor-log.service'`.

    Delete the `wireTransaction()` function (lines 87-130) and the `TxnCapture` interface plus the `txn` variable and its `beforeEach` reset (lines 68-78, 219-222) entirely — none of this is meaningful against the current architecture.

    Add `let mockSettlementService: { settle: jest.Mock; resolveMinistryWallet: jest.Mock; resolveSplit: jest.Mock };` and `let mockVisitorLog: { record: jest.Mock };` near the other `let` mock declarations. Inside `makeService()`, initialize `mockSettlementService = { settle: jest.fn().mockResolvedValue({ status: 'SETTLED', platformAmountNgn: 0, recipientCredits: [] }), resolveMinistryWallet: jest.fn(), resolveSplit: jest.fn() };` and `mockVisitorLog = { record: jest.fn().mockResolvedValue(undefined) };`. Add `{ provide: SettlementService, useValue: mockSettlementService }` and `{ provide: VisitorLogService, useValue: mockVisitorLog }` to the `providers` array inside `Test.createTestingModule({...})` (alongside the existing `PrismaService`/`RefundService`/`EventEmitter2`/`KafkaService` entries).

    Rewrite each of the 6 `it(...)` test bodies (INV-1 through INV-6) per the exact assertion targets specified in this task's `<behavior>` block above — for each, remove the `wireTransaction()` call, remove all `txn.*` references, and instead read `const input = mockSettlementService.settle.mock.calls[<callIndex>][0];` (or, for INV-6's loop, `mockSettlementService.settle.mock.calls[i][0]` per iteration after `jest.clearAllMocks()`) to assert against `input.recipients`, `input.platformMetadata`, and (for INV-5) trigger `input.onFailure` from within the mock's `mockImplementation`. Keep all fixture builders (`buildBooking`, `buildPayload`, `primeVendorResolvers`) and constants unchanged — only the mocking boundary and assertions change, not the test scenarios' setup data.
  </action>
  <verify>
    <automated>cd backend && npm run test:e2e:tours -- --testPathPattern="wallet-invariant" --forceExit --passWithNoTests</automated>
  </verify>
  <done>All 6 INV-* tests pass; zero references to `txn`, `wireTransaction`, or `mockPrisma.$transaction` remain in the file; every test asserts against `mockSettlementService.settle`'s captured call arguments.</done>
</task>

<task type="auto">
  <name>Task 2: Wire test:e2e:tours into CI + full regression confirmation</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml, backend/package.json</read_first>
  <action>
    In `.github/workflows/ci.yml`'s `backend` job, add a new step immediately adjacent to the existing `- name: E2E tests (settlement split tier audit trail)` step (lines 88-90), using the identical shape: `- name: E2E tests (tour booking + wallet invariant + KYC encryption)` with `working-directory: backend` and `run: npm run test:e2e:tours -- --forceExit --passWithNoTests`. No `package.json` script changes needed — `test:e2e:tours` already exists at `backend/package.json:19`.
  </action>
  <verify>
    <automated>cd backend && npm run test:e2e:tours -- --forceExit --passWithNoTests && npm run test:e2e:settlement-splits -- --forceExit --passWithNoTests</automated>
  </verify>
  <done>`.github/workflows/ci.yml` contains the new step; running `npm run test:e2e:tours` locally (against the real Postgres/Redis already running) exits 0 with all suites green (both `wallet-invariant.e2e-spec.ts` and `e2e-tour-booking.e2e-spec.ts`); the pre-existing `test:e2e:settlement-splits` step is unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI pipeline (GitHub Actions) → merge gate | Adding a previously-broken-and-silently-skipped e2e suite as a required check changes what blocks a merge going forward |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-10 | Repudiation (a broken suite silently disabled again) | `.github/workflows/ci.yml` | mitigate | `test:e2e:tours` is now a permanent CI step alongside `test:e2e:settlement-splits` — the exact failure mode that let this suite silently rot (per the D-09 todo's own discovery note) can't recur without a visible CI failure |
| T-20-11 | Tampering (test-only changes, zero production surface) | `wallet-invariant.e2e-spec.ts` | accept | This task touches only test files — no runtime behavior changes, no new trust boundary introduced |
</threat_model>

<verification>
Run `cd backend && npm run test:e2e:tours -- --forceExit --passWithNoTests` — must exit 0. Run `cd backend && npm test -- --forceExit --passWithNoTests` — full unit suite must show zero regressions. Confirm `.github/workflows/ci.yml`'s YAML is well-formed (GitHub Actions will reject a malformed workflow file at the next push — no local linter required, visual diff review against the existing step's exact indentation is sufficient).
</verification>

<success_criteria>
- `npm run test:e2e:tours` passes green locally against real Postgres/Redis
- `wallet-invariant.e2e-spec.ts` tests the actual current SettlementService-delegation architecture, not dead code
- `.github/workflows/ci.yml` runs `test:e2e:tours` on every PR/push permanently
</success_criteria>

<output>
After completion, create `.planning/phases/20-grpc-blue-green-healthcheck-retrofit/20-04-SUMMARY.md`
</output>
