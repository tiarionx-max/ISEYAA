---
phase: 22-scheduled-ministry-exports-lga-heatmap
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/admin/ministry/LgaMonthHeatmap.tsx
  - web/src/components/admin/ministry/__tests__/LgaMonthHeatmap.test.tsx
  - web/src/app/admin/ministry/page.tsx
autonomous: true
requirements: [MIN-09]

must_haves:
  truths:
    - "The Ministry dashboard shows an LGA x month visitor-density heatmap built on the existing MinistryService query shapes and the existing recharts-adjacent dashboard, with no new mapping/GeoJSON dependency introduced (MIN-09)"
    - "The heatmap consumes the SAME already-fetched /ministry/visitor-entries response the existing VisitorEntriesChart panel uses — zero new network request"
    - "The heatmap's aggregation sums count across userRole while KEEPING month as a distinct axis (group by (lgaName, month), not lgaName alone) — no double-counting or dropped axis"
    - "All 20 Ogun State LGAs are always shown as rows, even at zero count"
  artifacts:
    - path: "web/src/components/admin/ministry/LgaMonthHeatmap.tsx"
      provides: "LGA x month color-intensity grid component + pure buildGrid() aggregation function"
      contains: "buildGrid"
    - path: "web/src/app/admin/ministry/page.tsx"
      provides: "4th dashboard panel mounting LgaMonthHeatmap, reusing the existing visitorEntries query"
      contains: "LgaMonthHeatmap"
  key_links:
    - from: "web/src/app/admin/ministry/page.tsx"
      to: "web/src/components/admin/ministry/LgaMonthHeatmap.tsx"
      via: "data={visitorEntries ?? []} prop, same query result as VisitorEntriesChart"
      pattern: "<LgaMonthHeatmap"
---

<objective>
Ship MIN-09: an LGA x month visitor-density heatmap on the Ministry dashboard, built entirely client-side from data the page already fetches — zero new backend query work (D-05), zero new mapping/charting dependency (D-06, explicitly not a `recharts` chart type). Per CONTEXT.md D-09/D-08, this is the ONLY UI surface this phase touches — no admin settings panel, no new export route, dashboard display only.

This plan has no backend dependency (the `/ministry/visitor-entries` route it consumes already exists, unchanged, from Phase 14) and no dependency on Plans 22-01/22-02/22-03 — it can run fully in parallel with them.

Purpose: satisfies MIN-09 literally — the heatmap is a plain CSS-grid/Tailwind component, not a `react-simple-maps`/`nivo`/`d3-scale-chromatic` addition, matching REQUIREMENTS.md's explicit "no new mapping dependency this milestone" constraint.

Output: `LgaMonthHeatmap.tsx` (new component, exports a pure `buildGrid()` aggregation function plus the rendered grid), mounted as a 4th panel in `web/src/app/admin/ministry/page.tsx`, matching `22-UI-SPEC.md`'s Component Contract exactly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-scheduled-ministry-exports-lga-heatmap/22-CONTEXT.md
@.planning/phases/22-scheduled-ministry-exports-lga-heatmap/22-RESEARCH.md
@.planning/phases/22-scheduled-ministry-exports-lga-heatmap/22-PATTERNS.md
@.planning/phases/22-scheduled-ministry-exports-lga-heatmap/22-UI-SPEC.md

<interfaces>
<!-- Existing response shape this component consumes, verbatim, no backend change. -->

```typescript
// web/src/components/admin/ministry/VisitorEntriesChart.tsx (existing, UNCHANGED — reuse this exact type)
export interface VisitorEntryRow {
  lgaId: string | null;
  lgaName: string | null;
  month: string;      // 'YYYY-MM'
  userRole: string;
  count: number;
}
```

```typescript
// shared/src/constants/index.ts (existing, UNCHANGED)
export const OGUN_LGA_NAMES = [
  'Abeokuta North', 'Abeokuta South', 'Ado-Odo/Ota', 'Egbado North',
  'Egbado South', 'Ewekoro', 'Ifo', 'Ijebu East', 'Ijebu North',
  'Ijebu North East', 'Ijebu Ode', 'Ikenne', 'Imeko Afon',
  'Ipokia', 'Obafemi Owode', 'Odeda', 'Odogbolu', 'Ogun Waterside',
  'Remo North', 'Shagamu',
] as const; // 20 entries
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LgaMonthHeatmap component — buildGrid() aggregation + color-intensity grid render</name>
  <files>web/src/components/admin/ministry/LgaMonthHeatmap.tsx, web/src/components/admin/ministry/__tests__/LgaMonthHeatmap.test.tsx</files>
  <read_first>
    web/src/components/admin/ministry/VisitorEntriesChart.tsx (full file — `VisitorEntryRow` interface at lines 14-20 to reuse verbatim; `aggregateByLgaAndRole()` at lines 39-48 shows the SAME summing-across-userRole technique this component needs, but COLLAPSES month — RESEARCH.md Pitfall 5 warns this component's own aggregation must group by `(lgaName, month)` and KEEP month as a distinct axis, not repeat that collapse; the empty-state pattern at lines 88-94 and the `CustomTooltip` glass-style shell at lines 56-76 are both direct copy targets)
    .planning/phases/22-scheduled-ministry-exports-lga-heatmap/22-UI-SPEC.md (Component Contract — `LgaMonthHeatmap` section: exact panel shell classes, grid cell size `h-7 w-7`/`gap-1`, sticky row/column labels, 5-bucket color-intensity scale table, legend labels, tooltip copy format, scroll behavior)
    web/src/components/ui/__tests__/PageTransition.test.tsx (full file — the existing React Testing Library `render`/`screen` usage pattern already configured and working in this workspace's Jest setup; this is the only existing web component test to model style from)
  </read_first>
  <behavior>
    - Test: `buildGrid(data)` (exported pure function) groups rows by `(lgaName, month)`, summing `count` across all `userRole` values for that exact pair — given 2 rows `{lgaName:'Ifo', month:'2026-01', userRole:'TOURIST', count:5}` and `{lgaName:'Ifo', month:'2026-01', userRole:'CITIZEN', count:3}`, the resulting grid cell for `('Ifo','2026-01')` is `8` (NOT 2 separate values, NOT collapsed across months)
    - Test: `buildGrid(data)` returns `months` as the sorted list of distinct months present in `data`, and a `grid` whose keys cover all 20 `OGUN_LGA_NAMES` (including LGAs with zero rows in `data`, initialized to `0` for every month) — proves D-07's "always show all 20 LGAs" even at zero count
    - Test: a row with `lgaName: null` is bucketed under `'Unknown'` (mirrors `VisitorEntriesChart`'s existing `?? 'Unknown'` fallback) and does not throw
    - Test: rendering `<LgaMonthHeatmap data={[]} />` shows the empty-state text "No entries for this period" (verbatim reuse of the page's existing `EmptyPanelState` copy per UI-SPEC.md) and does NOT render a grid
    - Test: rendering `<LgaMonthHeatmap data={sampleRows} />` (2+ months, 2+ LGAs with nonzero counts) renders exactly 20 LGA row labels (one per `OGUN_LGA_NAMES` entry) regardless of how many LGAs appear in `sampleRows`
  </behavior>
  <action>
    Create `web/src/components/admin/ministry/LgaMonthHeatmap.tsx` with `'use client';` at the top. Import `OGUN_LGA_NAMES` from `'@iseyaa/shared'`. Re-declare (do not re-import, to avoid a circular/unnecessary cross-component dependency) the `VisitorEntryRow` interface locally, identical field-for-field to `VisitorEntriesChart.tsx`'s. Export a pure function `export function buildGrid(data: VisitorEntryRow[]): { months: string[]; grid: Map<string, Map<string, number>> }` exactly per RESEARCH.md's Pattern 5 / PATTERNS.md's `buildGrid()` example: compute `months` as the sorted unique set of `r.month` values; seed a `grid` Map with all 20 `OGUN_LGA_NAMES` as keys, each mapping to a `Map<month, 0>` for every month in `months`; then iterate `data`, resolving `lgaName = row.lgaName ?? 'Unknown'` (adding a new grid entry if `lgaName` is not one of the 20, e.g. `'Unknown'`), and increment `monthMap.get(row.month)` by `row.count` (default `0` if the key wasn't pre-seeded, e.g. `'Unknown'`'s month map). Implement `intensityStyle(count: number, max: number): React.CSSProperties` (or a Tailwind-class-returning function) per UI-SPEC.md's 5-bucket color table: `count === 0` -> `bg-white/5`; `ratio <= 0.25` -> `rgba(26,107,60,0.25)`; `<= 0.5` -> `rgba(26,107,60,0.45)`; `<= 0.75` -> `rgba(26,107,60,0.65)`; `> 0.75` -> `rgba(26,107,60,0.9)` (use inline `style={{ backgroundColor: ... }}` for the rgba tiers since Tailwind has no arbitrary-opacity-of-a-custom-rgb utility class configured for these exact values, matching how `VisitorEntriesChart.tsx` itself uses inline `fill="rgba(...)"` on its `<Bar>` elements rather than a Tailwind class). Render the panel-internal content only (NOT the outer `.glass` panel shell — that shell is added by `page.tsx` in Task 2, matching how `VisitorEntriesChart`/`PurposeBreakdownChart`/`RevenueChart` are each mounted inside their own panel wrapper in `page.tsx`, not self-wrapping): an empty-state block (`data.length === 0` -> the exact `EmptyPanelState` copy: heading "No entries for this period", body "Try a wider date range or clear the LGA filter to see more results.") ; otherwise, call `buildGrid(data)`, compute `max` as the maximum cell value across the grid (for ratio scoping, scoped to the CURRENT filtered result set per UI-SPEC.md), and render: an `overflow-x-auto` wrapper containing a CSS grid/table with sticky-left LGA-name row labels (`text-xs text-white/60 truncate`, ~120px fixed width) and sticky-top month column labels (`text-[10px] uppercase tracking-widest text-white/35`), grid cells sized `h-7 w-7 rounded-sm` with `gap-1`, each cell carrying a `title` attribute with the tooltip copy `` `${lgaName} — ${month}: ${count} visitor${count === 1 ? '' : 's'}` `` (simple native `title` tooltip is acceptable — UI-SPEC.md describes the glass tooltip style as a nice-to-have visual match, not a hard requirement for this pass; a native title attribute satisfies the same informational need with materially less component complexity), wrapped in `max-h-[420px] overflow-y-auto` for the 20-row list. Beneath the grid, render the 5-swatch legend row (`h-3 w-3 rounded-sm` swatches + `text-[10px] uppercase tracking-widest text-white/30` labels: "None"/"Low"/"Medium"/"High"/"Peak"). Write the test file per the 5 behaviors above, using `render`/`screen` from `@testing-library/react` for the component-level assertions and plain function calls for `buildGrid()`'s pure-logic assertions.
  </action>
  <verify>
    <automated>cd web && npm run test -- LgaMonthHeatmap</automated>
  </verify>
  <done>LgaMonthHeatmap.tsx exists with an exported, independently-tested buildGrid() function and a component rendering all 20 LGA rows, sorted month columns, 5-bucket color-intensity cells, empty state, and legend; LgaMonthHeatmap.test.tsx is green.</done>
  <acceptance_criteria>
    - `grep -c "export function buildGrid" web/src/components/admin/ministry/LgaMonthHeatmap.tsx` returns 1
    - `grep -c "OGUN_LGA_NAMES" web/src/components/admin/ministry/LgaMonthHeatmap.tsx` returns 1 or more
    - `grep -ci "no new mapping\|react-simple-maps\|nivo\|d3-scale" web/src/components/admin/ministry/LgaMonthHeatmap.tsx` — the file imports NONE of `react-simple-maps`/`nivo`/`d3-scale-chromatic` (verify via `grep -c "from 'react-simple-maps'\|from 'nivo'\|from 'd3-scale" web/src/components/admin/ministry/LgaMonthHeatmap.tsx` returning 0)
    - `cd web && npm run test -- LgaMonthHeatmap` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mount LgaMonthHeatmap as the dashboard's 4th panel</name>
  <files>web/src/app/admin/ministry/page.tsx</files>
  <read_first>
    web/src/app/admin/ministry/page.tsx (full file — the Visitor Entries panel shell at lines 229-270 is the closest analog to copy for the new panel's wrapper markup; the existing `visitorEntries` `useQuery` at lines 96-104 is the SAME data this new panel consumes, no new query; `ALLOWED_ROLES`/`EmptyPanelState`/`ErrorPanelState` at lines 22, 48-66 are reused verbatim)
    .planning/phases/22-scheduled-ministry-exports-lga-heatmap/22-UI-SPEC.md (Component Contract section — panel positioning "after the existing Revenue panel, before the closing `</div>`", header shape with NO button group per D-08, `Map` icon from `lucide-react` in a `bg-forest/20 border border-forest/30` badge matching the Visitor Entries/Purpose panels' non-financial forest-family icon treatment)
  </read_first>
  <action>
    In `web/src/app/admin/ministry/page.tsx`: add `Map` to the existing `lucide-react` import list (alongside `Users, Tag, TrendingUp, FileText, Table2`). Add `import { LgaMonthHeatmap } from '@/components/admin/ministry/LgaMonthHeatmap';`. After the existing Revenue to Government panel's closing `</div>` (end of the 3rd panel, before the outer page-content `</div>`), add a 4th panel block using the identical `className="glass rounded-2xl border border-white/6 overflow-hidden mb-6"` shell: header `flex items-center justify-between gap-2 px-5 py-4 border-b border-white/6` containing an icon badge `w-8 h-8 rounded-lg bg-forest/20 border border-forest/30 flex items-center justify-center` with `<Map size={14} className="text-forest-light" />`, and `<h2 className="font-bold text-white text-sm">LGA × Month Visitor Density</h2>` — no button group in this header (unlike the other 3 panels, per D-08: no export route for this panel). Body wrapper `px-4 py-5` containing the same loading/error/empty branching already used by the Visitor Entries panel (`isVisitorLoading` -> `.skeleton` block; `isVisitorError` -> `<ErrorPanelState />`; otherwise `<LgaMonthHeatmap data={visitorEntries ?? []} />`) — reusing the EXACT SAME `visitorEntries`/`isVisitorLoading`/`isVisitorError` state already fetched for the first panel (no new `useQuery` call, per D-05/UI-SPEC.md's "no new network request").
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>The Ministry dashboard page renders a 4th panel titled "LGA × Month Visitor Density" beneath the Revenue panel, reusing the already-fetched visitorEntries query, with no export buttons and no new API call.</done>
  <acceptance_criteria>
    - `grep -c "LgaMonthHeatmap" web/src/app/admin/ministry/page.tsx` returns 2 or more (import + usage)
    - `grep -c "LGA × Month Visitor Density" web/src/app/admin/ministry/page.tsx` returns 1
    - `grep -c "useQuery" web/src/app/admin/ministry/page.tsx` returns 4 (lgas + visitorEntries + purposeBreakdown + revenue) — identical to the pre-task baseline, proving no new query was added for the heatmap panel
    - `cd web && npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `/ministry/visitor-entries` response → `LgaMonthHeatmap` | Existing, already-authenticated (`MINISTRY_VIEWER`/`STATE_ADMIN`/`SUPER_ADMIN`-gated) data source, unchanged by this plan — no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-22-08 | Information Disclosure | `LgaMonthHeatmap` rendering | accept | Consumes the exact same non-PII visitor-count data already displayed by `VisitorEntriesChart` on the same page, behind the same role gate — no new data exposure |
</threat_model>

<verification>
- `cd web && npm run test -- LgaMonthHeatmap` passes
- `cd web && npx tsc --noEmit -p tsconfig.json` passes
- No new npm dependency added to `web/package.json` (verify via `git diff web/package.json` showing no changes)
</verification>

<success_criteria>
- `LgaMonthHeatmap.tsx` exists with an independently-tested `buildGrid()` pure aggregation function and a rendered color-intensity grid covering all 20 LGAs
- The heatmap is mounted as the Ministry dashboard's 4th panel, consuming the already-fetched `visitorEntries` query with zero new network calls and zero new dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/22-scheduled-ministry-exports-lga-heatmap/22-04-SUMMARY.md`
</output>
