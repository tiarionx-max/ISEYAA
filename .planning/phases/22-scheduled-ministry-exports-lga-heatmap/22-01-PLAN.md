---
phase: 22-scheduled-ministry-exports-lga-heatmap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/
  - backend/src/common/services/sendgrid.service.ts
  - backend/src/common/services/__tests__/sendgrid.service.spec.ts
autonomous: true
requirements: [MIN-08a]

must_haves:
  truths:
    - "A new MinistryExportSubscription table exists with typed recipients/cadence/lastSentAt/lastStatus/lastError columns (D-02, D-03, D-04, D-11, D-12), not a PlatformConfig JSON blob"
    - "SendgridService can send an email with a base64 PDF + CSV attachment array via a dedicated sendMinistryDigest() method, without touching sendEmail()'s or sendOtpEmail()'s existing signatures (D-14, D-15)"
    - "sendMinistryDigest() propagates a sgMail.send() rejection to its caller (does NOT swallow it) so the future scheduler's try/catch can mark a subscription FAILED"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "MinistryExportSubscription model + ExportCadence/ExportDeliveryStatus enums"
      contains: "model MinistryExportSubscription"
    - path: "backend/src/common/services/sendgrid.service.ts"
      provides: "sendMinistryDigest() dedicated method with attachments support"
      contains: "async sendMinistryDigest"
  key_links:
    - from: "backend/src/common/services/sendgrid.service.ts (sendMinistryDigest)"
      to: "@sendgrid/mail sgMail.send()"
      via: "attachments: [{content, filename, type, disposition}]"
      pattern: "attachments"
---

<objective>
Lay the foundation Plans 22-02 and 22-03 build on: add the `MinistryExportSubscription` Prisma model (typed `recipients`/`cadence`/`lastSentAt`/`lastStatus`/`lastError` columns per CONTEXT.md D-02/D-11/D-12, mirroring `SettlementSplitTier`'s schema-design precedent but with in-place field updates, not an audit-trail insert/deactivate pattern — see RESEARCH.md "Alternatives Considered"), push the migration so the generated Prisma Client exposes the `ministryExportSubscription` delegate, and add `SendgridService.sendMinistryDigest()` — the one genuinely new SendGrid capability this phase needs (attachments support does not exist on any current method).

This plan does NOT touch the subscription CRUD routes (22-02) or the `@Cron` scheduler (22-03) — both depend on this plan's schema/Prisma-Client output and this plan's new SendGrid method existing first.

Purpose: every downstream plan in this phase needs `MinistryExportSubscription` to exist in the live database (Prisma Client types are generated from the pushed schema, not the schema file alone) and needs a callable `sendMinistryDigest()` to send the digest with attachments.

Output: `MinistryExportSubscription` model + 2 new enums + applied migration, and `SendgridService.sendMinistryDigest()` with test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-scheduled-ministry-exports-lga-heatmap/22-CONTEXT.md
@.planning/phases/22-scheduled-ministry-exports-lga-heatmap/22-RESEARCH.md
@.planning/phases/22-scheduled-ministry-exports-lga-heatmap/22-PATTERNS.md

<interfaces>
<!-- New Prisma model + enums that Plans 22-02/22-03 consume directly. -->

```prisma
// backend/prisma/schema.prisma (new, this plan) — insert directly after the
// SettlementSplitTier model (after line 720), before ShadowSettlementComparison.
enum ExportCadence {
  WEEKLY
  MONTHLY
  QUARTERLY
}

enum ExportDeliveryStatus {
  PENDING
  SUCCESS
  FAILED
}

model MinistryExportSubscription {
  id          String                @id @default(uuid())
  recipients  String[]              // D-11: free-text emails, not FK to User
  cadence     ExportCadence
  isActive    Boolean               @default(true)
  lastSentAt  DateTime?             // D-04: rolling-window anchor; null until first successful send
  lastStatus  ExportDeliveryStatus  @default(PENDING)
  lastError   String?               // D-12; truncated to <=500 chars before persisting (22-03)
  createdAt   DateTime              @default(now())
  updatedAt   DateTime              @updatedAt

  @@index([isActive])
  @@map("ministry_export_subscriptions")
}
```

```typescript
// backend/src/common/services/sendgrid.service.ts (new method, this plan)
async sendMinistryDigest(params: {
  to: string[];
  subject: string;
  html: string;
  attachments?: Array<{ content: string; filename: string; type: string; disposition: string }>;
}): Promise<void>;
// Deliberately NO try/catch (mirrors sendOtpEmail(), NOT sendEmail()'s swallow
// behavior) — the caller (22-03's scheduler, via resilience.execute('sendgrid', ...))
// depends on a real rejection propagating here to mark lastStatus = FAILED.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MinistryExportSubscription model + ExportCadence/ExportDeliveryStatus enums to schema.prisma</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>
    backend/prisma/schema.prisma (lines 683-720: PlatformConfig + SettlementSplitTier — the two colocated models whose UUID-PK + `@@map(snake_case)` + `Decimal`/array-field style this new model must match; SettlementSplitTier's own comment block at lines 711-716 explaining why it has NO `@@unique` on [module, tierName] — this new model needs no such caveat since it uses in-place updates, not insert-new-row/deactivate-old)
  </read_first>
  <action>
    Insert the `ExportCadence` enum, `ExportDeliveryStatus` enum, and `MinistryExportSubscription` model exactly as shown in this plan's `<interfaces>` block, placed directly after the `SettlementSplitTier` model closing brace (after line 720) and before `model ShadowSettlementComparison`. Field types verbatim: `id String @id @default(uuid())`; `recipients String[]` (array-field convention already used elsewhere in this schema for `imageUrls`/`amenities`-style fields — no new Prisma pattern); `cadence ExportCadence`; `isActive Boolean @default(true)`; `lastSentAt DateTime?`; `lastStatus ExportDeliveryStatus @default(PENDING)`; `lastError String?`; `createdAt DateTime @default(now())`; `updatedAt DateTime @updatedAt`. Add `@@index([isActive])` and `@@map("ministry_export_subscriptions")`. No `@@unique` constraint on this model (unlike `SettlementSplitTier`) — recipients/cadence combinations are not required to be unique, and mutations are in-place `update()` calls, not insert-new-row/deactivate-old (per CONTEXT.md's own code_context note and RESEARCH.md's "Alternatives Considered").
  </action>
  <verify>
    <automated>cd backend && npx prisma validate</automated>
  </verify>
  <done>ExportCadence, ExportDeliveryStatus, and MinistryExportSubscription exist in schema.prisma exactly as specified; `npx prisma validate` exits 0.</done>
  <acceptance_criteria>
    - `grep -c "model MinistryExportSubscription" backend/prisma/schema.prisma` returns 1
    - `grep -c "enum ExportCadence" backend/prisma/schema.prisma` returns 1
    - `grep -c "enum ExportDeliveryStatus" backend/prisma/schema.prisma` returns 1
    - `grep -c "recipients  String\[\]" backend/prisma/schema.prisma` (or equivalent whitespace) returns 1
    - `cd backend && npx prisma validate` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>[BLOCKING] Task 2: Push the schema migration and generate the Prisma Client</name>
  <files>backend/prisma/schema.prisma, backend/prisma/migrations/</files>
  <read_first>
    backend/package.json (lines 15-18: `prisma:generate` = `prisma generate`, `prisma:migrate` = `prisma migrate dev` — the project's own convention for pushing schema changes, per CLAUDE.md's "prefer `prisma migrate dev` over `db push`")
  </read_first>
  <action>
    From `backend/`, run `npx prisma migrate dev --name add_ministry_export_subscription` non-interactively (this repo's dev-lifecycle convention per CLAUDE.md — avoids the interactive migration-name prompt). This both creates the migration SQL under `backend/prisma/migrations/*_add_ministry_export_subscription/` and applies it against the dev database. If `prisma migrate dev` requires an interactive TTY prompt that cannot be suppressed non-interactively, fall back to `npx prisma db push --accept-data-loss` and flag this task for manual follow-up (there is no data-loss risk for a purely additive new table, so this fallback is safe if needed). Run `npx prisma generate` afterward only if the generated client was not already regenerated as part of `migrate dev` (it normally is). This task MUST run after Task 1's schema edit and BEFORE any task in Plans 22-02/22-03 that references `prisma.ministryExportSubscription` — those tasks' TypeScript will not compile against the generated Prisma Client until this migration has been applied.
  </action>
  <verify>
    <automated>cd backend && npx prisma migrate status</automated>
  </verify>
  <done>A migration directory matching `backend/prisma/migrations/*_add_ministry_export_subscription/` exists with a `migration.sql` containing `CREATE TABLE` and `"ministry_export_subscriptions"`; `npx prisma migrate status` reports no pending migrations; the generated Prisma Client exposes a `ministryExportSubscription` delegate.</done>
  <acceptance_criteria>
    - A directory matching `backend/prisma/migrations/*_add_ministry_export_subscription` exists
    - Its `migration.sql` contains both `CREATE TABLE` and `"ministry_export_subscriptions"`
    - `cd backend && npx prisma migrate status` exits 0 with no pending migrations reported
    - `grep -c "ministryExportSubscription" backend/node_modules/.prisma/client/index.d.ts` is greater than 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add SendgridService.sendMinistryDigest() with attachment support</name>
  <files>backend/src/common/services/sendgrid.service.ts, backend/src/common/services/__tests__/sendgrid.service.spec.ts</files>
  <read_first>
    backend/src/common/services/sendgrid.service.ts (full file — `sendOtpEmail()` at lines 29-41 is the exact NO-swallow/no-try-catch shape to replicate; `sendEmail()`/`sendBookingConfirmation()`'s dedicated-method-with-params-object shape at lines 18-24 and 122-152 is the parameter convention to match; constructor's `this.from`/`sgMail.setApiKey()` init at lines 10-16 is unchanged)
    backend/src/common/services/__tests__/sendgrid.service.spec.ts (full file — existing `jest.mock('@sendgrid/mail', ...)`, `mockConfig`, and the `sendOtpEmail` Test 1/Test 2 resolve-vs-reject pair is the exact test shape to replicate for `sendMinistryDigest`)
  </read_first>
  <behavior>
    - Test: `sendMinistryDigest({ to: ['a@gov.ng','b@gov.ng'], subject: 'Ministry Export Digest', html: '<p>...</p>', attachments: [{content:'base64pdf', filename:'ministry-digest.pdf', type:'application/pdf', disposition:'attachment'}, {content:'base64csv', filename:'ministry-digest.csv', type:'text/csv', disposition:'attachment'}] })` resolves when `sgMail.send()` resolves, and the captured `sgMail.send` call args include `to: ['a@gov.ng','b@gov.ng']` and an `attachments` array with exactly 2 entries matching the input shape verbatim
    - Test: omitting `attachments` (or passing `attachments: []`) results in `sgMail.send()` being called WITHOUT an `attachments` key at all (not `attachments: []`) — this is the shape the D-15 size-guard fallback in 22-03 depends on
    - Test: `sendMinistryDigest(...)` REJECTS (propagates the `sgMail.send()` error) when `sgMail.send()` rejects — proves the same NO-swallow contract as `sendOtpEmail`, required so the future scheduler's `resilience.execute('sendgrid', ...)` call sees the real failure
  </behavior>
  <action>
    In `backend/src/common/services/sendgrid.service.ts`, add `async sendMinistryDigest(params: { to: string[]; subject: string; html: string; attachments?: Array<{ content: string; filename: string; type: string; disposition: string }> }): Promise<void>` as a new method after `sendBookingConfirmation()` (end of class, before the closing brace). Destructure `{ to, subject, html, attachments }`. Call `await sgMail.send({ to, from: this.from, subject, html, ...(attachments && attachments.length > 0 ? { attachments } : {}) })` directly — no `try/catch`, mirroring `sendOtpEmail()`'s deliberate no-swallow shape (add a one-line comment referencing this, matching the existing comment style above `sendOtpEmail()`). Do NOT modify `sendEmail()`'s signature or any other existing method (D-14 — new dedicated method only). In the spec file, add a new `describe('sendMinistryDigest', ...)` block implementing the 3 behaviors above, following the existing `sendOtpEmail` describe block's mock-setup style (`(sgMail.send as jest.Mock).mockResolvedValue([{}, {}])` / `.mockRejectedValue(new Error(...))`).
  </action>
  <verify>
    <automated>cd backend && npm run test -- sendgrid.service</automated>
  </verify>
  <done>sendgrid.service.spec.ts has a new passing `sendMinistryDigest` describe block covering attachment-shape, omitted-attachments, and rejection-propagation; full sendgrid.service spec file is green.</done>
  <acceptance_criteria>
    - `grep -c "async sendMinistryDigest" backend/src/common/services/sendgrid.service.ts` returns 1
    - `grep -c "sendEmail(to: string, subject: string, html: string)" backend/src/common/services/sendgrid.service.ts` returns 1 — proves `sendEmail()`'s existing signature is untouched (D-14)
    - `grep -ci "describe('sendMinistryDigest'" backend/src/common/services/__tests__/sendgrid.service.spec.ts` returns 1
    - `cd backend && npm run test -- sendgrid.service` exits 0 with `sendMinistryDigest` test names visible as passing in output
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `MinistryExportSubscription` row → SendGrid | `recipients` array is stored free-text (D-11); no DTO validation exists yet in this plan (validation is added in 22-02's CRUD DTOs) — this plan's schema alone does not enforce email shape |
| `SendgridService.sendMinistryDigest()` → `@sendgrid/mail` | Internal call, params supplied by 22-03's scheduler — no untrusted client input reaches this method directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-22-01 | Tampering | `MinistryExportSubscription.recipients` | mitigate (deferred to 22-02) | Column itself has no CHECK constraint — `@IsEmail({}, {each:true})` validation is enforced at the DTO boundary in 22-02's create/update routes, before any row reaches this table via the API (raw seed/migration writes are trusted, per D-10's CRUD-route audit-trail rationale) |
| T-22-02 | Information Disclosure | `sendMinistryDigest()` attachments | accept | Attachment `content` is base64-encoded report data (visitor counts, revenue aggregates) already gated behind `SUPER_ADMIN`/`MINISTRY_VIEWER` read routes elsewhere in this codebase — no new PII exposure surface (Ministry exports contain zero PII per Phase 14's MIN-01 precedent) |
</threat_model>

<verification>
- `cd backend && npx prisma validate` passes
- `cd backend && npx prisma migrate status` shows no pending migrations
- `cd backend && npm run test -- sendgrid.service` passes with new `sendMinistryDigest` coverage
- Full backend suite still green: `cd backend && npm run test`
</verification>

<success_criteria>
- `MinistryExportSubscription` model + `ExportCadence`/`ExportDeliveryStatus` enums exist in `schema.prisma`, migration applied, Prisma Client regenerated
- `SendgridService.sendMinistryDigest()` exists, supports base64 PDF+CSV attachments, propagates rejections (does not swallow), and leaves every other `SendgridService` method's signature untouched
</success_criteria>

<output>
After completion, create `.planning/phases/22-scheduled-ministry-exports-lga-heatmap/22-01-SUMMARY.md`
</output>
