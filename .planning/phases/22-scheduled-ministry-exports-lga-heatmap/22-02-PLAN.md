---
phase: 22-scheduled-ministry-exports-lga-heatmap
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - backend/src/modules/ministry/dto/create-export-subscription.dto.ts
  - backend/src/modules/ministry/dto/update-export-subscription.dto.ts
  - backend/src/modules/ministry/ministry-export-subscription.service.ts
  - backend/src/modules/ministry/ministry-export-subscription.controller.ts
  - backend/src/modules/ministry/ministry.module.ts
  - backend/src/modules/ministry/__tests__/ministry-export-subscription.service.spec.ts
  - backend/src/modules/ministry/__tests__/ministry-export-subscription.controller.spec.ts
autonomous: true
requirements: [MIN-08b]

must_haves:
  truths:
    - "An operator can change the export recipient list and delivery cadence via a database-backed REST route with no redeploy required (MIN-08b)"
    - "Only SUPER_ADMIN can read or mutate MinistryExportSubscription rows — no MINISTRY_VIEWER/STATE_ADMIN/LGA_ADMIN access, stricter than the read-only MinistryController's role set"
    - "Malformed email addresses in recipients are rejected at the DTO boundary before ever reaching a database row"
  artifacts:
    - path: "backend/src/modules/ministry/ministry-export-subscription.controller.ts"
      provides: "SUPER_ADMIN-gated CRUD REST routes (GET/POST/PATCH/DELETE), Swagger-visible"
      contains: "@Roles(UserRole.SUPER_ADMIN)"
    - path: "backend/src/modules/ministry/ministry-export-subscription.service.ts"
      provides: "CRUD against prisma.ministryExportSubscription, in-place update (no audit-trail insert/deactivate)"
      contains: "ministryExportSubscription"
    - path: "backend/src/modules/ministry/dto/create-export-subscription.dto.ts"
      provides: "recipients/cadence validation at the API boundary"
      contains: "IsEmail"
  key_links:
    - from: "backend/src/modules/ministry/ministry-export-subscription.controller.ts"
      to: "backend/src/modules/ministry/ministry-export-subscription.service.ts"
      via: "constructor injection"
      pattern: "MinistryExportSubscriptionService"
    - from: "backend/src/modules/ministry/ministry.module.ts"
      to: "backend/src/modules/ministry/ministry-export-subscription.controller.ts"
      via: "controllers array registration"
      pattern: "MinistryExportSubscriptionController"
---

<objective>
Give an operator a real, auditable way to configure who receives the Ministry digest and how often (MIN-08b), entirely at the backend layer — per CONTEXT.md D-09, `web/src/app/admin/ministry/page.tsx` gets NO new settings panel this phase; the routes are Swagger-visible and callable via Postman/curl only, mirroring Phase 18's `SettlementSplitTier` `SUPER_ADMIN`-only CRUD pattern exactly (D-10).

This plan depends on 22-01's `MinistryExportSubscription` Prisma model existing in the generated client. It does NOT touch the `@Cron` scheduler (22-03, which depends on this plan's `ministry.module.ts` registration existing first) or any web UI.

Purpose: satisfies MIN-08b's "recipient list + cadence configurable via the database, no redeploy required" literally — full CRUD (GET list, POST create, PATCH update, DELETE) rather than settlement-splits' GET+PATCH-only (tiers there are seeded; subscriptions here are operator-created from scratch).

Output: `MinistryExportSubscriptionService` (CRUD, in-place field updates), `MinistryExportSubscriptionController` (`SUPER_ADMIN`-gated, under `/admin/ministry-export-subscriptions`), 2 DTOs, `ministry.module.ts` wiring, full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-scheduled-ministry-exports-lga-heatmap/22-CONTEXT.md
@.planning/phases/22-scheduled-ministry-exports-lga-heatmap/22-RESEARCH.md
@.planning/phases/22-scheduled-ministry-exports-lga-heatmap/22-PATTERNS.md

<interfaces>
<!-- Prisma model from 22-01, consumed directly by this plan's service. -->

```prisma
// backend/prisma/schema.prisma (already applied by 22-01)
model MinistryExportSubscription {
  id          String                @id @default(uuid())
  recipients  String[]
  cadence     ExportCadence         // WEEKLY | MONTHLY | QUARTERLY
  isActive    Boolean               @default(true)
  lastSentAt  DateTime?
  lastStatus  ExportDeliveryStatus  @default(PENDING) // PENDING | SUCCESS | FAILED
  lastError   String?
  createdAt   DateTime              @default(now())
  updatedAt   DateTime              @updatedAt
}
```

<!-- Route/method shapes this plan creates, for the executor's direct use. -->

```typescript
// backend/src/modules/ministry/ministry-export-subscription.service.ts (new, this plan)
class MinistryExportSubscriptionService {
  list(): Promise<MinistryExportSubscription[]>;
  create(dto: CreateExportSubscriptionDto): Promise<MinistryExportSubscription>;
  findOne(id: string): Promise<MinistryExportSubscription>; // throws NotFoundException if absent
  update(id: string, dto: UpdateExportSubscriptionDto): Promise<MinistryExportSubscription>; // in-place, throws NotFoundException
  remove(id: string): Promise<MinistryExportSubscription>; // throws NotFoundException
}
```

```typescript
// backend/src/modules/ministry/ministry-export-subscription.controller.ts (new, this plan)
// @Controller('admin/ministry-export-subscriptions'), @Roles(UserRole.SUPER_ADMIN) — mirrors
// AdminController's settlement-splits routes exactly (D-10), but as its OWN controller class,
// never added to the existing read-only MinistryController (which per MIN-01 must never gain
// a mutation handler).
GET    /admin/ministry-export-subscriptions        -> list()
POST   /admin/ministry-export-subscriptions         -> create(dto)
PATCH  /admin/ministry-export-subscriptions/:id     -> update(id, dto)
DELETE /admin/ministry-export-subscriptions/:id     -> remove(id)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CreateExportSubscriptionDto + UpdateExportSubscriptionDto</name>
  <files>backend/src/modules/ministry/dto/create-export-subscription.dto.ts, backend/src/modules/ministry/dto/update-export-subscription.dto.ts</files>
  <read_first>
    backend/src/modules/admin/dto/update-split-tier.dto.ts (full file — the `@ApiPropertyOptional` + `@IsOptional()` PATCH-DTO shape to mirror for `UpdateExportSubscriptionDto`)
    backend/src/modules/tour-packages/dto/create-tour-package.dto.ts (line 91: `@IsArray() @ArrayMinSize(1) @ArrayMaxSize(10) @IsUUID('all', { each: true }) attractionIds!: string[];` — the array-of-primitives validation shape to mirror, swapping the item validator to `@IsEmail`)
    backend/src/modules/ministry/dto/ministry-query.dto.ts (full file — `@IsIn(['csv','pdf'])` shows this codebase's enum-like-literal validation style; this plan uses `@IsEnum(ExportCadence)` instead since `ExportCadence` is a real Prisma-generated TS enum, not a literal union)
  </read_first>
  <action>
    Create `CreateExportSubscriptionDto` in `backend/src/modules/ministry/dto/create-export-subscription.dto.ts`: import `ExportCadence` from `@prisma/client`. Fields: `recipients: string[]` — `@ApiProperty({ type: [String] }) @IsArray() @ArrayMinSize(1) @IsEmail({}, { each: true }) recipients!: string[];`; `cadence: ExportCadence` — `@ApiProperty({ enum: ExportCadence }) @IsEnum(ExportCadence, { message: 'cadence must be WEEKLY, MONTHLY, or QUARTERLY' }) cadence!: ExportCadence;`; `isActive?: boolean` — `@ApiPropertyOptional({ default: true }) @IsOptional() @IsBoolean() isActive?: boolean;`. Create `UpdateExportSubscriptionDto` in the sibling file with all 3 fields made `@IsOptional()` and `@ApiPropertyOptional()` (same validators as create, just optional) — mirror `UpdateSplitTierDto`'s all-fields-optional PATCH shape exactly. Import `class-validator` decorators from `'class-validator'` and Swagger decorators from `'@nestjs/swagger'`, matching every other DTO file in this codebase.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Both DTO files exist, compile cleanly, and export a class each with the fields/validators specified.</done>
  <acceptance_criteria>
    - `grep -c "IsEmail({}, { each: true })" backend/src/modules/ministry/dto/create-export-subscription.dto.ts` returns 1
    - `grep -c "IsEnum(ExportCadence" backend/src/modules/ministry/dto/create-export-subscription.dto.ts` returns 1
    - `grep -c "IsOptional" backend/src/modules/ministry/dto/update-export-subscription.dto.ts` returns 3 (recipients, cadence, isActive all optional)
    - `cd backend && npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MinistryExportSubscriptionService — CRUD against prisma.ministryExportSubscription</name>
  <files>backend/src/modules/ministry/ministry-export-subscription.service.ts, backend/src/modules/ministry/__tests__/ministry-export-subscription.service.spec.ts</files>
  <read_first>
    backend/src/modules/admin/admin.service.ts (lines 174-217: `listSplitTiers`/`updateSplitTier` — the `findUnique` -> `NotFoundException` -> mutate pattern to mirror, EXCEPT this plan's `update()` uses a plain in-place `prisma.ministryExportSubscription.update()`, NOT `updateSplitTier`'s `$transaction` insert-new-row/deactivate-old — per CONTEXT.md's own code_context note that `lastSentAt`/`lastStatus`/`lastError` are operational fields, not audit-trail-versioned)
  </read_first>
  <behavior>
    - Test: `list()` calls `prisma.ministryExportSubscription.findMany()` and returns its result verbatim
    - Test: `create(dto)` calls `prisma.ministryExportSubscription.create({ data: { recipients: dto.recipients, cadence: dto.cadence, isActive: dto.isActive ?? true } })`
    - Test: `findOne(id)` throws `NotFoundException` when `prisma.ministryExportSubscription.findUnique` resolves `null`
    - Test: `update(id, dto)` throws `NotFoundException` when the row does not exist (mirrors `updateSplitTier`'s existing-row check), and otherwise calls `prisma.ministryExportSubscription.update({ where: { id }, data: dto })` with ONLY the fields present in `dto` (no accidental overwrite of `lastSentAt`/`lastStatus`/`lastError` — those are never settable via this DTO)
    - Test: `remove(id)` throws `NotFoundException` when the row does not exist, and otherwise calls `prisma.ministryExportSubscription.delete({ where: { id } })`
  </behavior>
  <action>
    Create `MinistryExportSubscriptionService` in `backend/src/modules/ministry/ministry-export-subscription.service.ts`, `@Injectable()`, constructor-injects `PrismaService`. Implement `list()`, `create(dto: CreateExportSubscriptionDto)`, `findOne(id: string)` (throw `new NotFoundException('Ministry export subscription not found')` if `findUnique` returns null), `update(id: string, dto: UpdateExportSubscriptionDto)` (call `findOne(id)` first to get the existing-row 404 check, then `prisma.ministryExportSubscription.update({ where: { id }, data: dto })` — a plain in-place update, NOT `admin.service.ts`'s `$transaction` insert-new-row/deactivate-old pattern), `remove(id: string)` (same existence check via `findOne`, then `prisma.ministryExportSubscription.delete({ where: { id } })`). Import `NotFoundException` from `@nestjs/common`. Write the spec file with a `mockPrisma = { ministryExportSubscription: { findMany: jest.fn(), create: jest.fn(), findUnique: jest.fn(), update: jest.fn(), delete: jest.fn() } }` fixture and cover the 5 behaviors above.
  </action>
  <verify>
    <automated>cd backend && npm run test -- ministry-export-subscription.service</automated>
  </verify>
  <done>ministry-export-subscription.service.spec.ts is green covering list/create/findOne-404/update-404/update-success/remove-404/remove-success.</done>
  <acceptance_criteria>
    - `grep -c "class MinistryExportSubscriptionService" backend/src/modules/ministry/ministry-export-subscription.service.ts` returns 1
    - `grep -c "NotFoundException" backend/src/modules/ministry/ministry-export-subscription.service.ts` returns 3 or more (import + findOne + at least one call site)
    - `grep -c "\\$transaction" backend/src/modules/ministry/ministry-export-subscription.service.ts` returns 0 — proves in-place update, not the audit-trail pattern
    - `cd backend && npm run test -- ministry-export-subscription.service` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MinistryExportSubscriptionController + ministry.module.ts wiring + role-gating/validation tests</name>
  <files>backend/src/modules/ministry/ministry-export-subscription.controller.ts, backend/src/modules/ministry/ministry.module.ts, backend/src/modules/ministry/__tests__/ministry-export-subscription.controller.spec.ts</files>
  <read_first>
    backend/src/modules/admin/admin.controller.ts (lines 1-20, 102-114: imports + `@Roles(UserRole.SUPER_ADMIN)` route-level decorator shape for `settlement-splits` — exact precedent for this new controller, per D-10)
    backend/src/modules/ministry/ministry.module.ts (current 9-line file, full — this plan extends its `controllers`/`providers` arrays; does NOT touch `MinistryController`/`MinistryService`, which stay untouched per MIN-01)
    backend/src/modules/ministry/__tests__/ministry.controller.spec.ts (lines 1-56: the real-`RolesGuard`-plus-`Reflector` role-gating test harness shape — `createContext()` helper — to replicate for the new controller's `SUPER_ADMIN`-only gating, denying `MINISTRY_VIEWER`/`STATE_ADMIN` in addition to every other role)
  </read_first>
  <behavior>
    - Test: the controller class carries `@Roles(UserRole.SUPER_ADMIN)` metadata and NO other role — `MINISTRY_VIEWER` and `STATE_ADMIN` (both allowed on the read-only `MinistryController`) are explicitly DENIED here, proving this surface is stricter (D-10)
    - Test: `POST /admin/ministry-export-subscriptions` with a malformed email in `recipients` is rejected by the DTO's `@IsEmail({}, { each: true })` validator (unit-level: construct the DTO instance via `class-validator`'s `validate()` and assert a validation error is present for `recipients`)
    - Test: `GET/POST/PATCH/DELETE` handlers each delegate to the corresponding `MinistryExportSubscriptionService` method with the correct arguments (mocked service, verify call args)
  </behavior>
  <action>
    Create `MinistryExportSubscriptionController` in `backend/src/modules/ministry/ministry-export-subscription.controller.ts`: `@ApiTags('admin') @ApiBearerAuth() @UseGuards(JwtAuthGuard, RolesGuard) @Roles(UserRole.SUPER_ADMIN) @Controller('admin/ministry-export-subscriptions')`. Constructor-injects `MinistryExportSubscriptionService`. Routes: `@Get() list()`, `@Post() @Body() dto: CreateExportSubscriptionDto -> create(dto)`, `@Patch(':id') @Param('id') id: string, @Body() dto: UpdateExportSubscriptionDto -> update(id, dto)`, `@Delete(':id') @Param('id') id: string -> remove(id)`. Every route gets an `@ApiOperation({ summary: ... })` for Swagger visibility (D-10 requires this). Import guards/decorators using the exact same relative paths `admin.controller.ts` uses (`'../auth/guards/jwt-auth.guard'`, `'../../common/guards/roles.guard'`, `'../../common/decorators/roles.decorator'`, `'../../common/enums/user-role.enum'`) since this controller lives at the same directory depth (`backend/src/modules/ministry/`). Update `backend/src/modules/ministry/ministry.module.ts`: add `MinistryExportSubscriptionController` to `controllers`, add `MinistryExportSubscriptionService` to `providers`. Do not add any new `imports` entries — `PrismaModule` is `@Global()`. Write the controller spec mirroring `ministry.controller.spec.ts`'s `createContext()` role-gating harness, asserting `SUPER_ADMIN` is allowed and `MINISTRY_VIEWER`/`STATE_ADMIN`/every other role is denied, plus a DTO validation test using `class-validator`'s `validate(Object.assign(new CreateExportSubscriptionDto(), { recipients: ['not-an-email'], cadence: 'WEEKLY' }))` asserting a non-empty error array, plus mocked-service delegation tests for all 4 routes.
  </action>
  <verify>
    <automated>cd backend && npm run test -- ministry-export-subscription.controller</automated>
  </verify>
  <done>ministry-export-subscription.controller.spec.ts is green; ministry.module.ts registers both new providers/controller; MinistryController/MinistryService remain byte-for-byte untouched.</done>
  <acceptance_criteria>
    - `grep -c "@Roles(UserRole.SUPER_ADMIN)" backend/src/modules/ministry/ministry-export-subscription.controller.ts` returns 1 or more, and `grep -c "MINISTRY_VIEWER" backend/src/modules/ministry/ministry-export-subscription.controller.ts` returns 0 (no weaker role ever granted)
    - `grep -c "MinistryExportSubscriptionController" backend/src/modules/ministry/ministry.module.ts` returns 1
    - `grep -c "MinistryExportSubscriptionService" backend/src/modules/ministry/ministry.module.ts` returns 1
    - `git diff --stat backend/src/modules/ministry/ministry.controller.ts backend/src/modules/ministry/ministry.service.ts` (run before committing) shows no changes to either file
    - `cd backend && npm run test -- ministry-export-subscription.controller` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| HTTP client → `MinistryExportSubscriptionController` | Authenticated (`JwtAuthGuard`), `SUPER_ADMIN`-only (`RolesGuard` + `@Roles`) — no untrusted/anonymous input reaches the service layer |
| `recipients` DTO field → `MinistryExportSubscription.recipients` | `@IsEmail({}, {each:true})` validated at the global `ValidationPipe` (`whitelist:true, forbidNonWhitelisted:true`) boundary before any value reaches Prisma |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-22-03 | Elevation of Privilege | `ministry-export-subscription.controller.ts` | mitigate | `@Roles(UserRole.SUPER_ADMIN)` only — `MINISTRY_VIEWER`/`STATE_ADMIN` (both allowed on the read-only dashboard routes) are explicitly denied here; verified by an automated role-gating test, not manual review |
| T-22-04 | Tampering | `recipients: string[]` DTO field | mitigate | `@IsEmail({}, {each:true})` rejects malformed entries before they reach `sgMail.send()` downstream in 22-03, preventing header-injection-shaped strings from ever being persisted |
</threat_model>

<verification>
- `cd backend && npm run test -- ministry-export-subscription` passes (service + controller specs)
- `cd backend && npx tsc --noEmit -p tsconfig.json` passes
- `MinistryController`/`MinistryService` remain unmodified (MIN-01 invariant)
- Full backend suite still green: `cd backend && npm run test`
</verification>

<success_criteria>
- Full CRUD (`GET`/`POST`/`PATCH`/`DELETE`) exists for `MinistryExportSubscription`, `SUPER_ADMIN`-only, Swagger-visible under `/admin/ministry-export-subscriptions`
- Malformed emails are rejected at the DTO boundary before reaching the database
- `ministry.module.ts` registers the new controller/service; the existing read-only `MinistryController`/`MinistryService` are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/22-scheduled-ministry-exports-lga-heatmap/22-02-SUMMARY.md`
</output>
