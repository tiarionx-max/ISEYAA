---
phase: 21
plan: 05
type: execute
wave: 5
depends_on: ["21-04"]
files_modified:
  - backend/src/modules/reviews-client/reviews-client.constants.ts
  - backend/src/modules/reviews-client/reviews-client.service.ts
  - backend/src/modules/reviews-client/reviews-client.module.ts
  - backend/src/modules/reviews-client/__tests__/reviews-client.service.spec.ts
  - backend/src/modules/reviews/reviews.module.ts
  - backend/src/modules/reviews/reviews.controller.ts
  - backend/src/app.module.ts
autonomous: false
requirements: [GRPC-08]

must_haves:
  truths:
    - "POST /api/v1/reviews returns the identical full Review row shape as before extraction, including photos, even though CreateReviewRequest's proto has no photos field"
    - "GET /api/v1/reviews?targetType=&targetId=&page=&limit= returns the identical {data, pagination} shape with embedded user {id, firstName, lastName, avatarUrl} per review, reconstructed from a thin gRPC response via a follow-up Prisma read"
    - "Setting grpc.reviews_service.canary_enabled to false makes both createReview and findByTarget throw 503 without any gRPC or Prisma call"
    - "ReviewsClientService has no resolveFlag/getFlagQueue/getFlag methods — those 3 admin operations are confirmed to stay in-process via ReviewsAdminController/ReviewsAdminModule (21-04), never routed through this facade"
    - "Before reviews-service's canary flag is flipped on in any environment, an operator has explicitly sized the in-memory-pagination approach (D-08) against real review-count-per-target volumes and recorded a pass/fail verdict — this is a gated checkpoint task (Task 4), not a passive checklist row"
  artifacts:
    - path: backend/src/modules/reviews-client/reviews-client.service.ts
      provides: "ReviewsClientService.createReview (photos write-back + full-row refetch) and .findByTarget (user-embed enrichment + in-memory pagination, with a documented D-08 volume-sizing tradeoff)"
    - path: backend/src/modules/reviews-client/__tests__/reviews-client.service.spec.ts
      provides: "Unit coverage for photo write-back, full-row shape, user-embed enrichment, in-memory pagination, canary-off, and admin-bypass confirmation"
  key_links:
    - from: backend/src/modules/reviews/reviews.controller.ts
      to: backend/src/modules/reviews-client/reviews-client.service.ts
      via: "constructor injection; ReviewsController now registered inside ReviewsClientModule"
      pattern: "ReviewsClientService"
    - from: backend/src/app.module.ts
      to: backend/src/modules/reviews-client/reviews-client.module.ts
      via: "imports array (replaces the plain ReviewsModule import; ReviewsAdminModule from 21-04 stays)"
      pattern: "ReviewsClientModule"
---

<objective>
Complete the Reviews extraction (position 3 of 4 in D-05's risk-ascending order) by landing the monolith-side `ReviewsClientService` facade and finishing the module split started in Plan 21-04. This is the plan with the most genuine new engineering in the whole phase: `reviews.proto`'s `CreateReviewRequest` has no `photos` field (a write-side data-loss risk if left unaddressed — not merely a read-shape cosmetic gap) and `ListReviewsResponse`/`CreateReviewResponse` are both thinner than the current REST response shapes (no pagination envelope, no embedded `user`, no full row). Every one of these gaps is resolved in the facade using direct Prisma access — the established "no data-ownership boundary enforced" pattern — never by silently dropping data or changing REST-visible shape.

Per CONTEXT.md D-08, the in-memory pagination approach (fetch up to 1000 rows server-side, paginate client-side) is an accepted implementation approach ONLY conditional on a sizing check against realistic review-count-per-target volumes — D-08 explicitly requires the planner to gate this, not silently accept it. Task 4 below is that gate: a blocking checkpoint that must be resolved before reviews-service's canary flag is ever flipped on.

Purpose: Preserve Success Criteria #1's "zero client-visible behavior change" exactly, including the one gap (photos) that RESEARCH.md/21-PATTERNS.md did not explicitly flag but that direct source comparison surfaced: `CreateReviewDto.photos` has no proto field to travel through, so without an explicit write-back step, uploaded review photos would silently vanish.
Output: `backend/src/modules/reviews-client/` (new) + finished `reviews.module.ts`/`reviews.controller.ts`/`app.module.ts` wiring + a recorded D-08 sizing verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-low-risk-grpc-extraction-news-waitlist-reviews-scoped-delive/21-CONTEXT.md
@.planning/phases/21-low-risk-grpc-extraction-news-waitlist-reviews-scoped-delive/21-RESEARCH.md
@.planning/phases/21-low-risk-grpc-extraction-news-waitlist-reviews-scoped-delive/21-PATTERNS.md
</context>

<interfaces>
```typescript
// packages/proto/generated/reviews.ts (already exists, read-only reference)
export interface CreateReviewResponse { id: string; flagged: boolean; }
export interface ReviewSummary { id: string; userId: string; rating: number; comment: string; flagged: boolean; createdAt: string; }
export interface ListReviewsResponse { reviews: ReviewSummary[]; }
```

Current REST shapes to preserve exactly (from `backend/src/modules/reviews/reviews.service.ts`):
- `createReview()` returns the FULL `Review` Prisma row as created by `tx.review.create({data: {...}})` — no `include`, so no embedded `user` on create, but all columns including `photos`.
- `findByTarget()` returns `{ data: Review[], pagination: {page, limit, total, pages} }` where each row has `include: { user: { select: { id, firstName, lastName, avatarUrl } } }`, `orderBy: { createdAt: 'desc' }`, `limit = Math.min(50, Math.max(1, opts.limit ?? 24))`.
- `CreateReviewDto` has an optional `photos?: string[]` field (max 5 URLs) that `CreateReviewRequest` proto has NO field for at all.
- Plan 21-04's `reviews-grpc.controller.ts` ListReviews RPC returns up to 1000 rows for the target (bypassing the 50-row cap server-side), unsorted-by-caller-guarantee once re-fetched via `id: {in: [...]}`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reviews-client facade — photos write-back, full-row refetch, user-embed enrichment, in-memory pagination</name>
  <files>
    backend/src/modules/reviews-client/reviews-client.constants.ts, backend/src/modules/reviews-client/reviews-client.service.ts,
    backend/src/modules/reviews-client/reviews-client.module.ts
  </files>
  <read_first>
    backend/src/modules/notifications-client/notifications-client.service.ts
    backend/src/modules/notifications-client/notifications-client.module.ts
    backend/src/modules/waitlist-client/waitlist-client.service.ts
    backend/src/modules/reviews/reviews.service.ts
    backend/src/modules/reviews/dto/create-review.dto.ts
    backend/src/modules/reviews/reviews.controller.ts
    packages/proto/reviews.proto
  </read_first>
  <behavior>
    - createReview(actorUserId, dto) without photos, on gRPC success: returns the full Prisma Review row fetched via prisma.review.findUnique({where:{id}}), no update call made
    - createReview(actorUserId, dto) WITH photos, on gRPC success: calls prisma.review.update({where:{id}, data:{photos: dto.photos}}) BEFORE returning the enriched row, so the returned row's photos field matches dto.photos exactly
    - createReview on gRPC/resilience failure: throws ServiceUnavailableException, message containing "Reviews service is temporarily unavailable"
    - createReview when grpc.reviews_service.canary_enabled === false: throws ServiceUnavailableException WITHOUT calling resilience.execute, the gRPC client, or any Prisma method
    - findByTarget(targetType, targetId, {page, limit}) on success: fetches all matching rows via gRPC, re-enriches via prisma.review.findMany({where:{id:{in:[...]}}, include:{user:{select:...}}}), re-sorts by createdAt desc, then paginates the enriched array IN MEMORY using the same page/limit clamping as the current ReviewsService.findByTarget (page = Math.max(1,...), limit = Math.min(50, Math.max(1,...))), returning {data, pagination:{page,limit,total,pages}} where total = the full enriched-array length (not the page slice length)
    - findByTarget when canary flag is false: throws ServiceUnavailableException WITHOUT calling resilience.execute, the gRPC client, or Prisma
    - both methods route every network call through resilience.execute with 'reviewsGrpc' as the first argument
    - ReviewsClientService has no resolveFlag, getFlagQueue, or getFlagById methods defined
  </behavior>
  <action>
    Create `reviews-client.constants.ts`: `export const REVIEWS_PACKAGE = 'REVIEWS_PACKAGE';` (zero-import leaf file).
    Create `reviews-client.service.ts`: `CANARY_FLAG_KEY = 'grpc.reviews_service.canary_enabled'`, `UNAVAILABLE_MESSAGE = 'Reviews service is temporarily unavailable, please try again shortly'`. Constructor injects `PrismaService`, `@Inject(REVIEWS_PACKAGE) client: ClientGrpc`, `ResilienceService`; `onModuleInit()` -> `this.grpcService = this.client.getService<reviews.ReviewsServiceClient>('ReviewsService')`.
    `createReview(actorUserId: string, dto: CreateReviewDto)`: check canary; `const result = await this.resilience.execute('reviewsGrpc', () => firstValueFrom(this.grpcService.createReview({ targetType: dto.targetType, targetId: dto.targetId, userId: actorUserId, tourBookingId: dto.tourBookingId, rating: dto.rating, comment: dto.comment ?? '' }) as any));` — `CreateReviewRequest` has NO `photos` field, so if `dto.photos?.length`, immediately do `await this.prisma.review.update({ where: { id: result.id }, data: { photos: dto.photos } });` (this is a required write-side enrichment step — without it, uploaded photo URLs are silently discarded, a real functional regression, not a display-shape cosmetic issue). Then `return this.prisma.review.findUnique({ where: { id: result.id } });` (no `include` — matches `createReview`'s current no-embedded-user REST shape exactly). Catch logs `err?.message ?? err` only, rethrows `ServiceUnavailableException`.
    `findByTarget(targetType: ReviewTargetTypeLiteral, targetId: string, opts: { page?: number; limit?: number } = {})`: check canary; `const res = await this.resilience.execute('reviewsGrpc', () => firstValueFrom(this.grpcService.listReviews({ targetType, targetId }) as any));` — `res.reviews` is up to 1000 rows (21-04's server-side cap) for this target, order not guaranteed to survive the round-trip. `const ids = res.reviews.map(r => r.id); const enriched = await this.prisma.review.findMany({ where: { id: { in: ids } }, include: { user: { select: { id: true, firstName: true, lastName: true, avatarUrl: true } } }, orderBy: { createdAt: 'desc' } });` (re-querying with `orderBy` directly, rather than manually re-sorting the gRPC response, is simpler and matches `findByTarget`'s existing sort exactly). Paginate in memory: `const page = Math.max(1, opts.page ?? 1); const limit = Math.min(50, Math.max(1, opts.limit ?? 24)); const total = enriched.length; const start = (page - 1) * limit; const rows = enriched.slice(start, start + limit);` return `{ data: rows, pagination: { page, limit, total, pages: Math.ceil(total / limit) } };`. Add a code comment documenting the D-08 volume-sizing tradeoff: this approach fetches up to 1000 rows per call (21-04's server-side cap) then paginates in memory — acceptable at current review-per-target volumes, but if any production target commonly exceeds a few hundred reviews this becomes a real latency risk against the P95 < 500ms constraint (CLAUDE.md); Task 4 below is the explicit, blocking sizing check required before this code is allowed to go live, per D-08.
    Create `reviews-client.module.ts`: `ClientsModule.registerAsync` with `package: 'reviews'`, `protoPath: .../packages/proto/reviews.proto`, `url: config.get<string>('REVIEWS_SERVICE_URL', 'localhost:5011')`; `controllers: [ReviewsController]` ONLY (NOT `ReviewsAdminController` — that stays in `reviews-admin.module.ts` from 21-04); `providers: [ReviewsClientService]`, `exports: [ReviewsClientService]`.
  </action>
  <acceptance_criteria>
    - "reviews-client.service.ts's createReview method conditionally calls prisma.review.update with data:{photos:...} only when dto.photos has entries"
    - "reviews-client.service.ts's createReview method's final return is a prisma.review.findUnique call with no include clause"
    - "reviews-client.service.ts's findByTarget method calls prisma.review.findMany with a where.id.in filter and an include.user.select clause"
    - "reviews-client.service.ts contains no resolveFlag, getFlagQueue, or getFlagById method"
    - "cd backend && npx jest src/modules/reviews-client --silent exits 0"
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx jest src/modules/reviews-client --silent</automated>
  </verify>
  <done>ReviewsClientService preserves the full REST response shape for both createReview (including photos) and findByTarget (including user embed + pagination envelope), with the D-08 volume tradeoff explicitly documented in code.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the monolith — ReviewsController swaps to ReviewsClientService, finish the reviews.module.ts split, swap app.module.ts</name>
  <files>backend/src/modules/reviews/reviews.controller.ts, backend/src/modules/reviews/reviews.module.ts, backend/src/app.module.ts</files>
  <read_first>
    backend/src/modules/reviews/reviews.controller.ts
    backend/src/modules/reviews/reviews.module.ts
    backend/src/app.module.ts
  </read_first>
  <action>
    Modify `reviews.controller.ts`: `ReviewsController`'s constructor swaps `ReviewsService` injection for `ReviewsClientService` (rename the injected field, e.g. `reviewsClient`); `create()` calls `this.reviewsClient.createReview(user.userId, dto)`; `findAll()` calls `this.reviewsClient.findByTarget(targetType, targetId, { page, limit })`. `ReviewsAdminController` in the same file is UNCHANGED — it keeps injecting `ReviewsService` and calling it directly (D-07, already isolated into `ReviewsAdminModule` by 21-04).
    Modify `reviews.module.ts`: drop `ReviewsController` from `controllers` (now `controllers: []` — `ReviewsAdminController` already left in 21-04). Keep `providers: [ReviewsService]`, `exports: [ReviewsService]` (still required by both `reviews-admin.module.ts` and `apps/reviews-service`'s wholesale import).
    Modify `backend/src/app.module.ts`: replace the plain `ReviewsModule` import/registration with `ReviewsClientModule` (import from `'./modules/reviews-client/reviews-client.module'`). Keep the `ReviewsAdminModule` import from 21-04 unchanged. The final imports array has both `ReviewsClientModule` (public controller) and `ReviewsAdminModule` (admin controller) — no bare `ReviewsModule` import remains in the monolith's `app.module.ts`.
  </action>
  <acceptance_criteria>
    - "reviews.controller.ts's ReviewsController class injects ReviewsClientService, not ReviewsService"
    - "reviews.controller.ts's ReviewsAdminController class still injects ReviewsService directly, unchanged"
    - "reviews.module.ts's controllers array is empty ([])"
    - "backend/src/app.module.ts imports ReviewsClientModule and ReviewsAdminModule, and does not import a plain ReviewsModule"
    - "cd backend && npx tsc --noEmit -p tsconfig.json exits 0"
    - "cd backend && npx jest src/modules/reviews src/modules/reviews-client --silent exits 0"
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json && npx jest src/modules/reviews src/modules/reviews-client --silent</automated>
  </verify>
  <done>The monolith compiles and both the public review-creation/listing flow and the admin flag-queue flow work exactly as before, sourced from two disjoint modules (ReviewsClientModule + ReviewsAdminModule).</done>
</task>

<task type="auto">
  <name>Task 3: reviews-client.service.spec.ts — full test coverage</name>
  <files>backend/src/modules/reviews-client/__tests__/reviews-client.service.spec.ts</files>
  <read_first>
    backend/src/modules/notifications-client/__tests__/notifications-client.service.spec.ts
    backend/src/modules/reviews-client/reviews-client.service.ts
  </read_first>
  <action>
    Mirror `notifications-client.service.spec.ts`'s mock structure (`mockGrpcService`, `mockClientGrpc`, `mockResilience`, a `makeService(canaryFlagValue?)` factory) but add a `mockPrisma` shaped with `platformConfig.findUnique`, `review.findUnique`, `review.update`, and `review.findMany` jest mocks. Write test cases covering exactly the `<behavior>` list from Task 1: (1) createReview without photos — asserts `prisma.review.update` is NOT called and the return value is whatever `prisma.review.findUnique` mock resolves; (2) createReview WITH photos — asserts `prisma.review.update` IS called with `{where:{id}, data:{photos: dto.photos}}` BEFORE `prisma.review.findUnique` is called (assert call order via `mockPrisma.review.update.mock.invocationCallOrder[0] < mockPrisma.review.findUnique.mock.invocationCallOrder[0]` or equivalent); (3) createReview gRPC failure -> ServiceUnavailableException; (4) createReview canary-off -> ServiceUnavailableException, zero gRPC/Prisma calls; (5) findByTarget success -> asserts `prisma.review.findMany` called with `where.id.in` containing the gRPC response's review ids and `include.user.select` matching `{id,firstName,lastName,avatarUrl}`, and the returned shape matches `{data, pagination:{page,limit,total,pages}}` with `total` equal to the full enriched-array length, not the sliced page length; (6) findByTarget pagination — mock a 30-row enriched result, request `page:2, limit:10`, assert `data.length === 10` and the correct slice; (7) findByTarget canary-off -> ServiceUnavailableException, zero gRPC/Prisma calls; (8) both methods call `resilience.execute` with `'reviewsGrpc'` as the first argument; (9) `expect((reviewsClientService as any).resolveFlag).toBeUndefined()` and same for `getFlagQueue`/`getFlagById` — documents D-07's admin-bypass explicitly at the test level, not just by omission.
  </action>
  <acceptance_criteria>
    - "reviews-client.service.spec.ts contains at least 9 it() blocks covering all cases listed in the action"
    - "cd backend && npx jest src/modules/reviews-client --silent exits 0 with all tests passing"
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx jest src/modules/reviews-client --silent</automated>
  </verify>
  <done>ReviewsClientService has full behavioral test coverage matching Task 1's <behavior> contract, including the photos write-back ordering assertion and the D-07 admin-bypass confirmation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: D-08 sizing gate — size the in-memory pagination approach against real review-count-per-target volumes before canary go-live</name>
  <what-built>
    `ReviewsClientService.findByTarget()` (Task 1) fetches up to 1000 rows per target via gRPC (21-04's server-side cap), re-enriches via a Prisma `findMany`, then paginates the result IN MEMORY. This preserves response *shape* exactly regardless of data volume, but CONTEXT.md D-08 requires an explicit sizing check before this is allowed to go live — the P95 < 500ms constraint (CLAUDE.md) could be at risk if any target's review count is unexpectedly high (approaching the 1000-row cap), since every page request re-fetches and re-sorts the FULL target's row set rather than doing DB-level `skip`/`take`.
  </what-built>
  <how-to-verify>
    1. Against the closest available environment to production (staging preferred; production if staging has no representative data), run: `SELECT "targetType", "targetId", COUNT(*) FROM "Review" WHERE "deletedAt" IS NULL AND flagged = false GROUP BY "targetType", "targetId" ORDER BY count(*) DESC LIMIT 20;` (via `npx prisma studio`, a direct psql session, or an ad-hoc script using `PrismaService`).
    2. Confirm the highest per-target review count is well below the 1000-row server-side cap (a count approaching 1000 means silent truncation, not just a latency risk) and low enough (order of dozens to low hundreds, not thousands) that a full re-fetch + re-sort on every paginated request comfortably completes within the P95 < 500ms budget.
    3. If any target's count is high enough to be a realistic latency or truncation risk, this is a FAIL — file a follow-up (e.g. a proto amendment adding `page`/`limit` fields to `ListReviewsRequest` for true DB-level pagination) and do NOT flip the canary flag until resolved.
    4. Record the verdict: PASS (row counts pose no realistic P95 or truncation risk — safe to flip `grpc.reviews_service.canary_enabled` per the runbook) or FAIL (follow-up filed, canary flip blocked).
  </how-to-verify>
  <resume-signal>Reply with "PASS — safe to proceed" or "FAIL — <follow-up filed/needed>"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Monolith <-> reviews-service (Railway private network gRPC) | Internal-only hop, same trust model as News/Waitlist |
| Monolith's post-gRPC Prisma enrichment reads/writes | Same DB the extracted service just wrote to — matches the established "no data-ownership boundary" pattern, not a new boundary crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-21-05-01 | Tampering | createReview's photos write-back (prisma.review.update) runs AFTER the gRPC-side eligibility guards already passed | accept | The 7 eligibility guards (ownership, booking status, target match, duplicate check) all already ran inside reviews-service's createReview() before the review row exists; the photos update only ever targets the just-created row's id, scoped to a review the actor was already authorized to create |
| T-21-05-02 | Information Disclosure | findByTarget's Prisma enrichment query | mitigate | Query re-applies the exact same where-clause fields (targetType, targetId, deletedAt: null, flagged: false is enforced by the gRPC layer's upstream query in 21-04) — no broader exposure than the existing REST endpoint |
| T-21-05-03 | Information Disclosure | reviews-client.service.ts error logging | mitigate | Catch blocks log only `err?.message ?? err`, never full error objects or review content |
| T-21-05-04 | Denial of Service | In-memory pagination degrading P95 latency under real per-target review volume | mitigate | Task 4's blocking sizing gate — canary rollout does not proceed on this service until an operator confirms real review-count-per-target volumes pose no realistic P95 or truncation risk (D-08) |
</threat_model>

<verification>
- `cd backend && npx jest src/modules/reviews-client --silent` exits 0
- `cd backend && npx jest src/modules/reviews --silent` exits 0 (regression on ReviewsAdminController + ReviewsService)
- `cd backend && npx tsc --noEmit -p tsconfig.json` exits 0
- Task 4's D-08 sizing gate recorded PASS before `grpc.reviews_service.canary_enabled` is flipped in any environment
</verification>

<success_criteria>
`POST /api/v1/reviews` and `GET /api/v1/reviews` return byte-for-byte identical shapes to before extraction, including photos and the embedded user object, now routed through `ReviewsClientService` -> `reviews-service` gRPC. `ReviewsAdminController`'s 3 endpoints remain fully in-process and untouched. Reviews is fully landed, the third of the D-05 risk-ascending 4 services, gated by an explicit, recorded D-08 volume-sizing verdict before go-live.
</success_criteria>

<output>
After completion, create `.planning/phases/21-low-risk-grpc-extraction-news-waitlist-reviews-scoped-delive/21-05-SUMMARY.md`
</output>
