---
phase: 21
plan: 07
type: execute
wave: 7
depends_on: ["21-06"]
files_modified:
  - backend/src/modules/delivery-otp-client/delivery-otp-client.constants.ts
  - backend/src/modules/delivery-otp-client/delivery-otp-client.service.ts
  - backend/src/modules/delivery-otp-client/delivery-otp-client.module.ts
  - backend/src/modules/delivery-otp-client/__tests__/delivery-otp-client.service.spec.ts
  - backend/src/modules/delivery/delivery.controller.ts
  - backend/src/modules/delivery/delivery.module.ts
autonomous: true
requirements: [GRPC-07]

must_haves:
  truths:
    - "POST /api/v1/delivery/orders/:id/verify-otp returns the identical success/failure behavior as before extraction, now routed through DeliveryOtpClientService -> delivery-otp-service gRPC"
    - "A wrong/expired/locked OTP still surfaces to the driver as HTTP 400 with the original DeliveryService.verifyOtp() message text (e.g. 'Incorrect OTP. Ask the recipient to check their SMS. 2 attempt(s) remaining.'), not a generic 503"
    - "requestDelivery, acceptOrder, declineOrder, collectParcel, completeDelivery, rateDelivery, and cancelOrder on DeliveryController are UNCHANGED — only the verifyOtp handler's body changed, confirming GRPC-07's scoped-down extraction boundary (RequestDelivery/AcceptDelivery/CompleteDelivery/DeliveryGateway stay in-process)"
    - "Setting grpc.delivery_otp_service.canary_enabled to false makes verify-otp throw 503 without any gRPC call"
  artifacts:
    - path: backend/src/modules/delivery-otp-client/delivery-otp-client.service.ts
      provides: "DeliveryOtpClientService.verifyOtp(orderId, otp) with canary+resilience wrap AND the client-side half of the business-vs-transport exception mapping (INVALID_ARGUMENT -> BadRequestException, NOT_FOUND -> NotFoundException, else -> ServiceUnavailableException)"
    - path: backend/src/modules/delivery-otp-client/__tests__/delivery-otp-client.service.spec.ts
      provides: "Unit coverage proving the client-side exception mapping round-trips correctly against 21-06's server-side RpcException codes"
  key_links:
    - from: backend/src/modules/delivery/delivery.controller.ts
      to: backend/src/modules/delivery-otp-client/delivery-otp-client.service.ts
      via: "second constructor-injected service, alongside the pre-existing DeliveryService — DeliveryController itself does NOT move to a new module"
      pattern: "DeliveryOtpClientService"
    - from: backend/src/modules/delivery/delivery.module.ts
      to: backend/src/modules/delivery-otp-client/delivery-otp-client.module.ts
      via: "imports array addition (DeliveryModule gains this import; DeliveryOtpClientModule does NOT register DeliveryController itself)"
      pattern: "DeliveryOtpClientModule"
---

<objective>
Complete the Delivery OTP extraction (position 4, final, of the D-05 risk-ascending order) with the one partial/hybrid controller swap in this entire phase: `DeliveryController` stays exactly where it is, gains a SECOND injected service (`DeliveryOtpClientService`) alongside the unchanged `DeliveryService`, and only the `verifyOtp` handler's single line of body changes. Every other `DeliveryController` handler — `requestDelivery`, `acceptOrder`, `declineOrder`, `collectParcel`, `completeDelivery`, `rateDelivery`, `cancelOrder` — is untouched, since GRPC-07 explicitly keeps those RPCs and `DeliveryGateway` in-process this milestone.

This plan builds the client-side half of Plan 21-06's business-vs-transport exception mapping: `DeliveryOtpClientService.verifyOtp()` inspects the gRPC error's `.code` (set by 21-06's `RpcException({code: GrpcStatus.INVALID_ARGUMENT | GrpcStatus.NOT_FOUND, message})`) and re-throws the matching NestJS HTTP exception with the ORIGINAL message intact — never falling through to the generic `ServiceUnavailableException` the `notifications-client`-derived facades use for every failure.

Purpose: Finish GRPC-07 with zero client-visible behavior change on the one endpoint (`verify-otp`) that legitimately needs business-rule error messages to survive a transport-layer swap.
Output: `backend/src/modules/delivery-otp-client/` (new) + updated `delivery.controller.ts`/`delivery.module.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-low-risk-grpc-extraction-news-waitlist-reviews-scoped-delive/21-CONTEXT.md
@.planning/phases/21-low-risk-grpc-extraction-news-waitlist-reviews-scoped-delive/21-RESEARCH.md
@.planning/phases/21-low-risk-grpc-extraction-news-waitlist-reviews-scoped-delive/21-PATTERNS.md
</context>

<interfaces>
```typescript
// packages/proto/generated/delivery.ts (already exists, read-only reference)
export interface VerifyDeliveryOtpRequest { orderId: string; otp: string; }
export interface VerifyDeliveryOtpResponse { success: boolean; }
export interface DeliveryServiceClient {
  verifyDeliveryOtp(request: VerifyDeliveryOtpRequest): Observable<VerifyDeliveryOtpResponse>;
  // requestDelivery/acceptDelivery/completeDelivery also exist on this interface — NOT called this phase.
}
```

Plan 21-06's `delivery-otp-grpc.controller.ts` throws `RpcException({ code: GrpcStatus.INVALID_ARGUMENT (3), message: <original BadRequestException text> })` for wrong/expired/locked OTP, and `RpcException({ code: GrpcStatus.NOT_FOUND (5), message: <original NotFoundException text> })` for a missing order. On the client side, the rejected Promise's error object carries `.code` and `.message` matching those values (confirmed via direct read of `@nestjs/microservices`'s `BaseRpcExceptionFilter`/`RpcException` source this phase — not an assumption).

Current `DeliveryController.verifyOtp` (backend/src/modules/delivery/delivery.controller.ts:163-173): `@Post('orders/:id/verify-otp') @UseGuards(JwtAuthGuard, RolesGuard) @Roles(UserRole.DRIVER) verifyOtp(@Param('id') id: string, @Body() dto: VerifyDeliveryOtpDto) { return this.deliveryService.verifyOtp(id, dto); }` — guards/decorators are UNCHANGED by this plan, only the method body's single return statement changes.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: delivery-otp-client facade — canary + resilience + client-side business/transport exception mapping</name>
  <files>
    backend/src/modules/delivery-otp-client/delivery-otp-client.constants.ts, backend/src/modules/delivery-otp-client/delivery-otp-client.service.ts,
    backend/src/modules/delivery-otp-client/delivery-otp-client.module.ts
  </files>
  <read_first>
    backend/src/modules/notifications-client/notifications-client.service.ts
    backend/src/modules/notifications-client/notifications-client.module.ts
    backend/apps/delivery-otp-service/src/delivery-otp-grpc.controller.ts
    backend/src/resilience/resilience.service.ts
  </read_first>
  <behavior>
    - verifyOtp(orderId, otp) on gRPC success: returns {verified: true}
    - verifyOtp when the gRPC error has .code === 3 (INVALID_ARGUMENT): throws BadRequestException with the original message text unchanged (e.g. "Incorrect OTP. Ask the recipient to check their SMS. 2 attempt(s) remaining.")
    - verifyOtp when the gRPC error has .code === 5 (NOT_FOUND): throws NotFoundException with the original message text unchanged ("Delivery order not found")
    - verifyOtp on any other error (transport failure, no .code, or a transport-level status like UNAVAILABLE/DEADLINE_EXCEEDED): logs err?.message ?? err only, throws ServiceUnavailableException with the standard unavailable message
    - verifyOtp when grpc.delivery_otp_service.canary_enabled === false: throws ServiceUnavailableException WITHOUT calling resilience.execute or the gRPC client
    - verifyOtp routes its network call through resilience.execute with 'deliveryOtpGrpc' as the first argument
  </behavior>
  <action>
    Create `delivery-otp-client.constants.ts`: `export const DELIVERY_OTP_PACKAGE = 'DELIVERY_OTP_PACKAGE';` (zero-import leaf file).
    Create `delivery-otp-client.service.ts`: `CANARY_FLAG_KEY = 'grpc.delivery_otp_service.canary_enabled'`, `UNAVAILABLE_MESSAGE = 'Delivery OTP service is temporarily unavailable, please try again shortly'`. Constructor injects `PrismaService`, `@Inject(DELIVERY_OTP_PACKAGE) client: ClientGrpc`, `ResilienceService`; `onModuleInit()` -> `this.grpcService = this.client.getService<delivery.DeliveryServiceClient>('DeliveryService')`.
    `verifyOtp(orderId: string, otp: string)`: check canary; wrap in try/catch: `const res = await this.resilience.execute('deliveryOtpGrpc', () => firstValueFrom(this.grpcService.verifyDeliveryOtp({ orderId, otp }) as any));` return `{ verified: res.success };`. Catch block: `if (err?.code === GrpcStatus.INVALID_ARGUMENT) throw new BadRequestException(err.message);` `if (err?.code === GrpcStatus.NOT_FOUND) throw new NotFoundException(err.message);` else `this.logger.error(\`Delivery OTP gRPC verifyOtp failed: ${err?.message ?? err}\`); throw new ServiceUnavailableException(UNAVAILABLE_MESSAGE);` (import `status as GrpcStatus` from `@grpc/grpc-js`, `BadRequestException`/`NotFoundException`/`ServiceUnavailableException` from `@nestjs/common`).
    Create `delivery-otp-client.module.ts`: `ClientsModule.registerAsync` with `package: 'delivery'`, `protoPath: .../packages/proto/delivery.proto`, `url: config.get<string>('DELIVERY_OTP_SERVICE_URL', 'localhost:5012')`; `controllers: []` — this module does NOT register `DeliveryController` (unlike every other `*-client.module.ts` in this phase); `providers: [DeliveryOtpClientService]`, `exports: [DeliveryOtpClientService]`.
  </action>
  <acceptance_criteria>
    - "delivery-otp-client.service.ts's catch block checks err?.code against GrpcStatus.INVALID_ARGUMENT and GrpcStatus.NOT_FOUND before falling back to ServiceUnavailableException"
    - "delivery-otp-client.module.ts's controllers array is empty ([])"
    - "delivery-otp-client.service.ts imports status as GrpcStatus from @grpc/grpc-js, matching resilience.service.ts's existing naming convention"
    - "cd backend && npx jest src/modules/delivery-otp-client --silent exits 0"
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx jest src/modules/delivery-otp-client --silent</automated>
  </verify>
  <done>DeliveryOtpClientService correctly distinguishes business-rule OTP failures (400/404 with preserved messages) from genuine transport failures (503), with zero exception messages silently downgraded to a generic string.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DeliveryController's partial swap + delivery.module.ts import</name>
  <files>backend/src/modules/delivery/delivery.controller.ts, backend/src/modules/delivery/delivery.module.ts</files>
  <read_first>
    backend/src/modules/delivery/delivery.controller.ts
    backend/src/modules/delivery/delivery.module.ts
  </read_first>
  <action>
    Modify `delivery.controller.ts`: add a second constructor parameter `private readonly deliveryOtpClient: DeliveryOtpClientService` (import from `'../delivery-otp-client/delivery-otp-client.service'`) alongside the existing `private readonly deliveryService: DeliveryService`. Change ONLY the `verifyOtp` handler's body: `verifyOtp(@Param('id') id: string, @Body() dto: VerifyDeliveryOtpDto) { return this.deliveryOtpClient.verifyOtp(id, dto.otp); }` — the `@Post`/`@UseGuards`/`@Roles`/`@ApiBearerAuth`/`@ApiOperation` decorators above it are UNCHANGED. Every other handler (`getFeeEstimate`, `getMyRiderProfile`, `createDeliveryRider`, `approveDeliveryRider`, `goOnline`, `goOffline`, `getRiderEarnings`, `requestDelivery`, `acceptOrder`, `declineOrder`, `collectParcel`, `completeDelivery`, `rateDelivery`, `cancelOrder`) is UNTOUCHED — still calls `this.deliveryService.*`.
    Modify `delivery.module.ts`: add `DeliveryOtpClientModule` (import from `'../delivery-otp-client/delivery-otp-client.module'`) to the `imports` array, alongside the existing `WalletModule`/`AuthModule` imports. `DeliveryController` stays registered exactly where it already is (`controllers: [DeliveryController]`, unchanged) — this is the one module in the phase that does NOT move its controller into a `*-client.module.ts`.
  </action>
  <acceptance_criteria>
    - "delivery.controller.ts's DeliveryController constructor injects both DeliveryService and DeliveryOtpClientService"
    - "delivery.controller.ts's verifyOtp method body calls this.deliveryOtpClient.verifyOtp(id, dto.otp), not this.deliveryService.verifyOtp"
    - "delivery.controller.ts's requestDelivery, acceptOrder, declineOrder, collectParcel, completeDelivery, rateDelivery, and cancelOrder methods still call this.deliveryService.*, textually unchanged from before this plan"
    - "delivery.module.ts's imports array contains DeliveryOtpClientModule alongside the pre-existing WalletModule and AuthModule"
    - "delivery.module.ts's controllers array still contains DeliveryController, unchanged"
    - "cd backend && npx tsc --noEmit -p tsconfig.json exits 0"
    - "cd backend && npx jest src/modules/delivery --silent exits 0 (existing delivery.service.spec.ts and delivery.gateway.spec.ts unaffected — DeliveryService itself is untouched)"
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json && npx jest src/modules/delivery src/modules/delivery-otp-client --silent</automated>
  </verify>
  <done>DeliveryController's verify-otp endpoint is routed through the new gRPC facade while every other Delivery endpoint and DeliveryGateway remain fully in-process, matching GRPC-07's explicit scope boundary exactly.</done>
</task>

<task type="auto">
  <name>Task 3: delivery-otp-client.service.spec.ts — full test coverage</name>
  <files>backend/src/modules/delivery-otp-client/__tests__/delivery-otp-client.service.spec.ts</files>
  <read_first>
    backend/src/modules/notifications-client/__tests__/notifications-client.service.spec.ts
    backend/src/modules/delivery-otp-client/delivery-otp-client.service.ts
  </read_first>
  <action>
    Mirror `notifications-client.service.spec.ts`'s mock structure (`mockGrpcService.verifyDeliveryOtp`, `mockClientGrpc`, `mockResilience`, `mockPrisma.platformConfig.findUnique`, `makeService(canaryFlagValue?)`). Write test cases covering exactly the Task 1 `<behavior>` list: (1) success -> `{verified: true}`; (2) `mockGrpcService.verifyDeliveryOtp` returns `throwError(() => ({code: 3, message: 'Incorrect OTP. Ask the recipient to check their SMS. 2 attempt(s) remaining.'}))` -> assert `BadRequestException` thrown with that EXACT message (use `.rejects.toThrow('Incorrect OTP. Ask the recipient to check their SMS. 2 attempt(s) remaining.')`, not just the exception class); (3) `throwError(() => ({code: 5, message: 'Delivery order not found'}))` -> assert `NotFoundException` with that exact message; (4) `throwError(() => new Error('UNAVAILABLE'))` (no `.code` property) -> assert `ServiceUnavailableException` with the standard unavailable message, NOT the raw "UNAVAILABLE" string; (5) `throwError(() => ({code: 14, message: 'connection reset'}))` (transport-level UNAVAILABLE code, distinct from INVALID_ARGUMENT/NOT_FOUND) -> assert `ServiceUnavailableException`, confirming the mapping doesn't accidentally treat every numeric `.code` as a business-rule error; (6) canary flag `false` -> `ServiceUnavailableException`, zero calls to `resilience.execute` or the gRPC client; (7) `resilience.execute` called with `'deliveryOtpGrpc'` as the first argument on the success path.
  </action>
  <acceptance_criteria>
    - "delivery-otp-client.service.spec.ts contains at least 7 it() blocks covering all cases listed in the action"
    - "at least one test asserts the exact preserved OTP-failure message text, not just the exception class"
    - "at least one test confirms a non-business-rule numeric gRPC code (e.g. 14/UNAVAILABLE) still maps to ServiceUnavailableException, not BadRequestException"
    - "cd backend && npx jest src/modules/delivery-otp-client --silent exits 0 with all tests passing"
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx jest src/modules/delivery-otp-client --silent</automated>
  </verify>
  <done>The full round-trip business-vs-transport exception mapping (21-06 server-side RpcException codes -> 21-07 client-side HTTP exceptions) is proven by tests that would fail if either side of the mapping regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Monolith <-> delivery-otp-service (Railway private network gRPC) | Internal-only hop, same trust model as News/Waitlist/Reviews |
| DeliveryController's DRIVER-only verify-otp route | Unchanged JwtAuthGuard + RolesGuard(DRIVER) protection at the REST boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-21-07-01 | Tampering | OTP brute-force via delivery-otp-service, bypassing the monolith's REST-layer throttler | mitigate | Same mitigation as T-21-06-02 — the 5-attempt Redis-counter lockout lives inside `DeliveryService.verifyOtp`, unchanged and still enforced regardless of which process calls it |
| T-21-07-02 | Information Disclosure | delivery-otp-client.service.ts error logging | mitigate | Catch block logs only `err?.message ?? err` for the transport-failure branch; the business-rule branch (BadRequestException/NotFoundException) intentionally forwards the message to the caller since it's driver-facing, non-sensitive product content, not a raw vendor/transport error |
| T-21-07-03 | Denial of Service | A malformed gRPC error missing `.code` incorrectly matching GrpcStatus.INVALID_ARGUMENT (3) or GrpcStatus.NOT_FOUND (5) by coincidence | accept | `.code` comparison is a strict `===` against numeric enum values; a `undefined === 3` or `undefined === 5` check is always false, so a malformed/codeless error always falls through to the safe ServiceUnavailableException default, not a mis-mapped 400/404 |
</threat_model>

<verification>
- `cd backend && npx jest src/modules/delivery-otp-client src/modules/delivery --silent` exits 0
- `cd backend && npx tsc --noEmit -p tsconfig.json` exits 0
- `cd backend && npm test` (full backend suite) exits 0 — final phase-gate regression sweep across all 7 plans
</verification>

<success_criteria>
`POST /api/v1/delivery/orders/:id/verify-otp` behaves identically to before extraction — success, wrong-OTP, expired-OTP, and lockout all preserve their exact original messages and status codes — now routed through `DeliveryOtpClientService` -> `delivery-otp-service` gRPC, while every other Delivery endpoint and `DeliveryGateway` remain fully in-process, exactly matching GRPC-07's scope. All 4 services in this phase (News, Waitlist, Reviews, Delivery OTP) are now extracted in the D-05 risk-ascending order, each independently health-check-gated (Phase 20) and canary-flag-gated (D-06) for staggered production rollout via `docs/blue-green-cutover-runbook.md` (Plan 21-01).
</success_criteria>

<output>
After completion, create `.planning/phases/21-low-risk-grpc-extraction-news-waitlist-reviews-scoped-delive/21-07-SUMMARY.md`
</output>
