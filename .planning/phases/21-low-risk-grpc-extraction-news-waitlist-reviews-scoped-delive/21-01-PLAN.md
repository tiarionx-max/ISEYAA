---
phase: 21
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/resilience/resilience.types.ts
  - backend/nest-cli.json
  - backend/package.json
  - .env.example
  - docker-compose.yml
  - docs/blue-green-cutover-runbook.md
autonomous: true
requirements: [GRPC-07, GRPC-08]

must_haves:
  truths:
    - "Every one of the 4 new gRPC vendor keys (newsGrpc, waitlistGrpc, reviewsGrpc, deliveryOtpGrpc) has a registered resilience policy with the same tuning as the existing notificationsGrpc entry"
    - "`npx nest build <service>` is a valid command for all 4 new service names (news-service, waitlist-service, reviews-service, delivery-otp-service) once their apps/ directories exist in later plans"
    - "`npm run build:services` (backend workspace) covers all 12 apps/*-service scaffolds, not just the original 8"
    - "docker-compose.yml can bring up all 4 new services alongside postgres/redis/backend with zero manual wiring once their Dockerfiles exist in later plans"
    - "An operator reading docs/blue-green-cutover-runbook.md can execute a staggered, risk-ascending cutover (News -> Waitlist -> Reviews -> Delivery OTP per D-05) for any of the 4 new services using only that document, substituting the per-service canary-flag key and port"
    - "D-04: the runbook and canary-flag scaffolding support shipping each of the 4 services one at a time with its own flag flip and bake period before the next starts, not all four in one wave"
    - "D-06: each of the 4 new services gets its own independent grpc.<service>_canary_enabled PlatformConfig key (opt-out semantics — absence or any value other than false means enabled), not one combined phase-wide flag"
  artifacts:
    - path: backend/src/resilience/resilience.types.ts
      provides: "Vendor union + RESILIENCE_DEFAULTS entries for newsGrpc/waitlistGrpc/reviewsGrpc/deliveryOtpGrpc"
    - path: backend/nest-cli.json
      provides: "4 new project entries: news-service, waitlist-service, reviews-service, delivery-otp-service"
    - path: backend/package.json
      provides: "build:services script includes the 4 new service names"
    - path: .env.example
      provides: "4 new *_SERVICE_URL placeholders (ports 5009-5012)"
    - path: docker-compose.yml
      provides: "4 new service blocks + backend depends_on/environment wiring"
    - path: docs/blue-green-cutover-runbook.md
      provides: "per-service canary-flag-key + port sections for the 4 new services, preserving the existing notifications-service section"
  key_links:
    - from: backend/src/resilience/resilience.service.ts
      to: backend/src/resilience/resilience.types.ts
      via: "Object.keys(RESILIENCE_DEFAULTS) iteration in onModuleInit() registers one cockatiel policy per Vendor key"
      pattern: "newsGrpc|waitlistGrpc|reviewsGrpc|deliveryOtpGrpc"
---

<objective>
Land the shared, mechanical scaffolding every one of the 4 low-risk gRPC extractions (News, Waitlist, Reviews, Delivery OTP) depends on, before any per-service extraction plan starts: resilience vendor policies, nest-cli.json project registrations, the build:services script, .env.example placeholders, docker-compose.yml service blocks, and an extension of the existing blue-green cutover runbook. This plan touches zero domain logic — it is pure configuration, matching the `notifications-service`/`notificationsGrpc` precedent exactly, four times over.

Purpose: Give every later plan (21-02 through 21-07) a stable, pre-wired foundation so each service extraction plan only has to add its own files, never touch shared config files that would otherwise create wave-ordering conflicts across the 4 staggered extractions (D-04/D-05).
Output: Updated resilience.types.ts, nest-cli.json, package.json, .env.example, docker-compose.yml, docs/blue-green-cutover-runbook.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-low-risk-grpc-extraction-news-waitlist-reviews-scoped-delive/21-CONTEXT.md
@.planning/phases/21-low-risk-grpc-extraction-news-waitlist-reviews-scoped-delive/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the 4 new gRPC vendors in resilience.types.ts + nest-cli.json + package.json build:services</name>
  <files>backend/src/resilience/resilience.types.ts, backend/nest-cli.json, backend/package.json</files>
  <read_first>
    backend/src/resilience/resilience.types.ts
    backend/src/resilience/resilience.service.ts
    backend/nest-cli.json
    backend/package.json
  </read_first>
  <action>
    In `resilience.types.ts`: add 4 new members to the `Vendor` union, immediately after `'notificationsGrpc'`: `'newsGrpc' | 'waitlistGrpc' | 'reviewsGrpc' | 'deliveryOtpGrpc'`. Add 4 matching entries to `RESILIENCE_DEFAULTS` (after the existing `notificationsGrpc: { timeoutMs: 5_000, retryCount: 1, failureThreshold: 8, halfOpenAfterMs: 20_000 }` entry), each with the identical tuning object (same-region Railway-internal gRPC hop rationale — mirror the existing inline comment above `notificationsGrpc` verbatim for each new entry, adjusted to name the new vendor): `newsGrpc`, `waitlistGrpc`, `reviewsGrpc`, `deliveryOtpGrpc` all get `{ timeoutMs: 5_000, retryCount: 1, failureThreshold: 8, halfOpenAfterMs: 20_000 }`.
    In `nest-cli.json`: add 4 new entries to the `projects` object, each shaped exactly like the existing `"notifications-service"` entry (`type: "application"`, `root: "apps/<name>"`, `entryFile: "src/main"`, `sourceRoot: "apps/<name>/src"`, `compilerOptions.tsConfigPath: "apps/<name>/tsconfig.app.json"`), for `"news-service"`, `"waitlist-service"`, `"reviews-service"`, `"delivery-otp-service"`. These entries reference `apps/<name>/tsconfig.app.json` files that do not exist yet — that is expected; later plans create them and this task's build won't be exercised on the new names until then.
    In `backend/package.json`'s `build:services` script (currently: `for s in auth-service wallet-service events-service stays-service marketplace-service admin-service ai-service notifications-service; do npx nest build $s || exit 1; done`), append ` news-service waitlist-service reviews-service delivery-otp-service` to the space-separated service list (order does not matter functionally).
  </action>
  <acceptance_criteria>
    - "resilience.types.ts's Vendor union type contains newsGrpc, waitlistGrpc, reviewsGrpc, and deliveryOtpGrpc"
    - "RESILIENCE_DEFAULTS has exactly one entry per new Vendor member, each with timeoutMs:5000, retryCount:1, failureThreshold:8, halfOpenAfterMs:20000"
    - "nest-cli.json's projects object has exactly 12 top-level keys (8 pre-existing + 4 new)"
    - "backend/package.json's build:services script string contains all 4 new service names"
    - "cd backend && npx tsc --noEmit -p tsconfig.json exits 0 (RESILIENCE_DEFAULTS: Record<Vendor, VendorThresholds> type-checks with zero missing or excess keys)"
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>resilience.types.ts, nest-cli.json, and package.json all reference the 4 new services/vendors consistently; the backend workspace still type-checks cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire .env.example, docker-compose.yml, and extend the blue-green cutover runbook for all 4 new services</name>
  <files>.env.example, docker-compose.yml, docs/blue-green-cutover-runbook.md</files>
  <read_first>
    .env.example
    docker-compose.yml
    docs/blue-green-cutover-runbook.md
    backend/apps/notifications-service/railway.toml
  </read_first>
  <action>
    In `.env.example`, immediately after the `NOTIFICATIONS_SERVICE_URL=notifications-service.railway.internal:5008` line, add 4 new placeholders in the same `<name>-service.railway.internal:<port>` shape: `NEWS_SERVICE_URL=news-service.railway.internal:5009`, `WAITLIST_SERVICE_URL=waitlist-service.railway.internal:5010`, `REVIEWS_SERVICE_URL=reviews-service.railway.internal:5011`, `DELIVERY_OTP_SERVICE_URL=delivery-otp-service.railway.internal:5012`.
    In `docker-compose.yml`'s `backend` service: add the 4 new `*_SERVICE_URL` keys to `environment` (compose-DNS shape `<name>-service:<port>`, e.g. `NEWS_SERVICE_URL: news-service:5009`) next to the existing `NOTIFICATIONS_SERVICE_URL` line, and add the 4 new service names to `depends_on` with `condition: service_started`, matching the existing `notifications-service` entry's shape exactly. Add 4 new top-level service blocks (`news-service`, `waitlist-service`, `reviews-service`, `delivery-otp-service`), each copying the `notifications-service` block's exact shape: `build.context: .`, `build.dockerfile: backend/apps/<name>/Dockerfile`, `container_name: iseyaa_<name_with_underscores>` (e.g. `iseyaa_news_service`), `restart: unless-stopped`, `env_file: .env`, `environment: { DATABASE_URL: <same value as notifications-service's>, REDIS_URL: <same value> }`, `ports: ['<port>:<port>']` (5009/5010/5011/5012 respectively), `depends_on: { postgres: {condition: service_healthy}, redis: {condition: service_healthy} }`. These blocks reference Dockerfiles that do not exist yet (later plans create them) — `docker compose config` still validates the YAML/interpolation without requiring the referenced files to exist.
    In `docs/blue-green-cutover-runbook.md`, add 4 new top-level sections (one per News/Waitlist/Reviews/Delivery OTP), each restating the SAME 6-step Procedure + Bake window + Rollback structure the existing `notifications-service` section uses, with these substitutions per service: canary flag key (`grpc.news_service.canary_enabled` / `grpc.waitlist_service.canary_enabled` / `grpc.reviews_service.canary_enabled` / `grpc.delivery_otp_service.canary_enabled`), gRPC port (5009/5010/5011/5012), resilience vendor key (`newsGrpc`/`waitlistGrpc`/`reviewsGrpc`/`deliveryOtpGrpc`) referenced in the breaker-tuning "Known manual-only checks" item. Add one explicit note near the top of the new content (not inside any individual per-service section) stating the D-05 risk-ascending rollout order — News first, then Waitlist, then Reviews, then Delivery OTP last — and that each service's full bake window must complete with no regression before the next service's Step 1 begins. Do not remove, reorder, or rewrite the existing `notifications-service` section.
  </action>
  <acceptance_criteria>
    - ".env.example contains NEWS_SERVICE_URL=, WAITLIST_SERVICE_URL=, REVIEWS_SERVICE_URL=, and DELIVERY_OTP_SERVICE_URL= lines"
    - "docker-compose.yml's backend.environment block contains all 4 new *_SERVICE_URL keys and backend.depends_on lists all 4 new service names"
    - "docker-compose.yml has exactly 4 new top-level service blocks named news-service, waitlist-service, reviews-service, delivery-otp-service, each with a distinct port in {5009,5010,5011,5012}"
    - "docker compose -f docker-compose.yml config exits 0 (or docker-compose -f docker-compose.yml config if the standalone binary is what's installed)"
    - "docs/blue-green-cutover-runbook.md contains 4 new second-level headings, one per service, each mentioning its own canary flag key and gRPC port, and a note stating the News -> Waitlist -> Reviews -> Delivery OTP rollout order"
    - "docs/blue-green-cutover-runbook.md's existing notifications-service section text is unchanged"
  </acceptance_criteria>
  <verify>
    <automated>docker compose -f docker-compose.yml config >NUL 2>&1 || docker-compose -f docker-compose.yml config >NUL 2>&1</automated>
  </verify>
  <done>.env.example, docker-compose.yml, and the runbook all reference all 4 new services consistently; docker compose config validates the modified YAML.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Monolith <-> new gRPC services (Railway private network) | Internal-only gRPC hops; no public ingress to the 4 new services in this phase |
| Config files -> deployed runtime | `.env.example`/`docker-compose.yml` are non-secret templates; real secrets live in Infisical/Railway env vars, never in these files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-21-01-01 | Information Disclosure | .env.example / docker-compose.yml new entries | accept | New placeholders reuse the existing non-secret `<name>.railway.internal:<port>` / dev DATABASE_URL/REDIS_URL shape already used for 8 other services — no new secret surface introduced, matches established precedent |
| T-21-01-02 | Tampering | docker-compose.yml new service blocks reusing shared Postgres/Redis credentials | accept | Matches the existing `notifications-service` block precedent (Phase 17/20) — no data-ownership boundary is enforced between extracted services in this codebase by design; not a new exposure introduced by this plan |
</threat_model>

<verification>
- `cd backend && npx tsc --noEmit -p tsconfig.json` exits 0
- `docker compose -f docker-compose.yml config` (or `docker-compose` fallback) exits 0
</verification>

<success_criteria>
resilience.types.ts, nest-cli.json, backend/package.json, .env.example, docker-compose.yml, and docs/blue-green-cutover-runbook.md all reference all 4 new services (news-service, waitlist-service, reviews-service, delivery-otp-service) consistently, with zero regressions to existing config for the 8 pre-existing services or notifications-service.
</success_criteria>

<output>
After completion, create `.planning/phases/21-low-risk-grpc-extraction-news-waitlist-reviews-scoped-delive/21-01-SUMMARY.md`
</output>
