---
phase: 17
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
autonomous: true
requirements: [GRPC-03]

must_haves:
  truths:
    - "docker-compose up brings up notifications-service alongside postgres/redis/backend/web, reachable at notifications-service:5008 from the backend container"
    - ".env.example documents NOTIFICATIONS_SERVICE_URL as a live, consumed config value with dev and Railway examples"
    - "Implements CONTEXT.md decision D-04 (env-var-driven gRPC target URL, documented for both dev and Railway)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "notifications-service local dev topology"
      contains: "notifications-service:"
    - path: ".env.example"
      provides: "NOTIFICATIONS_SERVICE_URL documentation"
      contains: "NOTIFICATIONS_SERVICE_URL"
  key_links:
    - from: "docker-compose.yml backend service"
      to: "docker-compose.yml notifications-service"
      via: "NOTIFICATIONS_SERVICE_URL env var + depends_on"
      pattern: "NOTIFICATIONS_SERVICE_URL: notifications-service:5008"
---

<objective>
Give `notifications-service` its own local dev-topology entry in `docker-compose.yml` and document its production config value in `.env.example`, so it genuinely runs as a separately deployed process both locally and on Railway (ROADMAP.md success criterion 2).

Purpose: Local dev parity with the eventual Railway topology — `docker-compose up` should bring up the monolith AND `notifications-service` as two independent containers communicating over gRPC, not just the monolith.
Output: A new `notifications-service:` block in `docker-compose.yml`; `.env.example` documenting `NOTIFICATIONS_SERVICE_URL` as a live, consumed config value.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-CONTEXT.md
@.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-RESEARCH.md
@.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add notifications-service to docker-compose.yml's local dev topology</name>
  <files>docker-compose.yml</files>
  <read_first>
    docker-compose.yml
  </read_first>
  <action>
    Add a new notifications-service top-level service block to docker-compose.yml (insert after the existing web block, before the volumes key), built from backend/apps/notifications-service/Dockerfile with build context "." (repo root — required so the packages/proto workspace source is in the Docker build context, matching the monolith's own Dockerfile requirement; note this differs from the existing backend/web blocks, which use context ./backend / ./web plus Dockerfile.dev). Set container_name iseyaa_notifications_service, restart unless-stopped, env_file .env, environment values DATABASE_URL=postgresql://iseyaa:iseyaa_dev_password@postgres:5432/iseyaa_dev and REDIS_URL=redis://redis:6379, ports mapping '5008:5008', depends_on postgres (condition service_healthy) and redis (condition service_healthy). Reuse the existing production backend/apps/notifications-service/Dockerfile for this compose block (not a new Dockerfile.dev) — no dev-loop live-reload Dockerfile exists for this service today, and authoring one is out of this phase's scope; accept the simpler no-live-reload tradeoff for local topology testing. Separately, add NOTIFICATIONS_SERVICE_URL=notifications-service:5008 to the existing backend service's environment block (compose DNS service name, not localhost), and add a notifications-service entry with condition service_started to the backend service's depends_on block — no HTTP healthcheck endpoint exists for this gRPC-only service today, so service_started is the only viable depends_on condition without further scaffolding work.
  </action>
  <verify>
    <automated>docker compose config >/dev/null 2>&1 && echo valid || echo "docker unavailable or invalid — verify YAML manually"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains a notifications-service top-level service key with dockerfile backend/apps/notifications-service/Dockerfile and a port mapping of '5008:5008'
    - the backend service's environment block contains NOTIFICATIONS_SERVICE_URL: notifications-service:5008
    - the backend service's depends_on block contains a notifications-service entry
    - if a Docker CLI is available, `docker compose config` (or `docker-compose config`) parses the file without YAML/schema errors
  </acceptance_criteria>
  <done>docker-compose.yml brings up notifications-service as an independent container alongside postgres/redis/backend/web, with the backend container able to reach it at notifications-service:5008.</done>
</task>

<task type="auto">
  <name>Task 2: Document NOTIFICATIONS_SERVICE_URL as a live, consumed config value in .env.example</name>
  <files>.env.example</files>
  <read_first>
    .env.example
  </read_first>
  <action>
    In the existing gRPC Microservices (Wave 3) section (currently lines 63-71), update the comment immediately above the NOTIFICATIONS_SERVICE_URL=notifications-service.railway.internal:5008 line (currently line 71) to note that as of Phase 17 this var is now actually consumed by NotificationsClientModule's ClientsModule.registerAsync factory (backend/src/modules/notifications-client/notifications-client.module.ts) — the other 7 *_SERVICE_URL vars in this same block remain unused placeholders for future extractions. Add a commented dev-mode example directly below the NOTIFICATIONS_SERVICE_URL= line, in the form: local dev (docker-compose) resolves to notifications-service:5008, bare-metal dev (nest start notifications-service) resolves to localhost:5008 — following the exact inline-comment style already used elsewhere in this file (e.g. the DATABASE_URL/DIRECT_URL block's explanatory comments). Do NOT rename the variable and do NOT introduce a new NOTIFICATIONS_GRPC_URL var — this deliberately reuses the existing placeholder (planner's resolution of RESEARCH.md's Open Question 1: zero .env.example churn, matches the convention already set for the other 7 not-yet-live services).
  </action>
  <verify>
    <automated>grep -c "NOTIFICATIONS_SERVICE_URL" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - .env.example still contains exactly one NOTIFICATIONS_SERVICE_URL= line (no duplicate NOTIFICATIONS_GRPC_URL variable introduced)
    - a comment referencing "Phase 17" or "NotificationsClientModule" appears within 2 lines above the NOTIFICATIONS_SERVICE_URL line
    - a dev-mode example comment mentioning both notifications-service:5008 and localhost:5008 appears within 2 lines below the NOTIFICATIONS_SERVICE_URL line
  </acceptance_criteria>
  <done>.env.example documents NOTIFICATIONS_SERVICE_URL as a live, consumed Phase 17 config value with dev and Railway examples, with zero new variable names introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| docker-compose local network (dev-only) | Exposes notifications-service's gRPC port to the host machine for local development |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-17-05-01 | Information Disclosure | docker-compose notifications-service port mapping (5008:5008) | accept | Dev-only, localhost-bound port mapping; not internet-facing; matches the existing backend (3001) and postgres (5432) port-mapping pattern already present in this file |
| T-17-05-02 | Configuration (V14) | NOTIFICATIONS_SERVICE_URL env var | mitigate | Documented in .env.example with dev and Railway examples, read via ConfigService in Plan 17-03's registerAsync factory — never hardcoded in application code |
</threat_model>

<verification>
- `docker compose config` parses cleanly (if Docker CLI available)
- `.env.example` documents NOTIFICATIONS_SERVICE_URL with dev/Railway examples
</verification>

<success_criteria>
- `docker-compose up` brings up notifications-service alongside the existing services, reachable from the backend container at notifications-service:5008
- `.env.example` accurately documents the config value this phase's facade consumes
</success_criteria>

<output>
After completion, create `.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-05-SUMMARY.md`
</output>
