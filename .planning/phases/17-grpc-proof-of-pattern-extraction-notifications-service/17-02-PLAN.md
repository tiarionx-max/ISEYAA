---
phase: 17
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/apps/auth-service/src/app.module.ts
  - backend/apps/admin-service/src/app.module.ts
  - backend/apps/ai-service/src/app.module.ts
  - backend/apps/events-service/src/app.module.ts
  - backend/apps/marketplace-service/src/app.module.ts
  - backend/apps/stays-service/src/app.module.ts
  - backend/apps/wallet-service/src/app.module.ts
autonomous: true
requirements: [GRPC-03]

must_haves:
  truths:
    - "All 8 backend/apps/*-service scaffolds import ResilienceModule directly in their own app.module.ts"
    - "All 8 scaffolds build cleanly with zero TypeScript errors"
  artifacts:
    - path: "backend/apps/auth-service/src/app.module.ts"
      provides: "ResilienceModule import (no CommonModule co-requisite)"
      contains: "ResilienceModule"
    - path: "backend/apps/wallet-service/src/app.module.ts"
      provides: "ResilienceModule import alongside existing CommonModule"
      contains: "ResilienceModule"
  key_links:
    - from: "backend/apps/{auth,admin,ai,events,marketplace,stays,wallet}-service/src/app.module.ts"
      to: "backend/src/resilience/resilience.module.ts"
      via: "direct NestJS module import"
      pattern: "ResilienceModule"
---

<objective>
Fold in the full INT-01 todo scope (per user's explicit choice in 17-CONTEXT.md's Folded Todos): wire `ResilienceModule` into all 7 remaining `backend/apps/*-service` gRPC scaffolds that don't yet have it (`notifications-service` was already fixed in Phase 16). This is not itself required by GRPC-03/04/05, but was folded into this phase's scope by the user, so it is planned and delivered here rather than silently dropped.

Purpose: Close the same latent `ResilienceModule` DI gap that Phase 16 found and fixed only for `notifications-service` — `@Global()` decorators do not cross separate `NestFactory` bootstrap trees, so each scaffold's own module tree needs its own explicit import.
Output: All 8 `backend/apps/*-service` scaffolds import `ResilienceModule`; all 8 build cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-CONTEXT.md
@.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-RESEARCH.md
@.planning/todos/pending/2026-07-17-wire-resiliencemodule-into-grpc-service-scaffolds.md
@.planning/phases/16-connection-pooling-infrastructure/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ResilienceModule into the 7 remaining gRPC scaffolds</name>
  <files>backend/apps/auth-service/src/app.module.ts, backend/apps/admin-service/src/app.module.ts, backend/apps/ai-service/src/app.module.ts, backend/apps/events-service/src/app.module.ts, backend/apps/marketplace-service/src/app.module.ts, backend/apps/stays-service/src/app.module.ts, backend/apps/wallet-service/src/app.module.ts</files>
  <read_first>
    backend/apps/auth-service/src/app.module.ts
    backend/apps/admin-service/src/app.module.ts
    backend/apps/ai-service/src/app.module.ts
    backend/apps/events-service/src/app.module.ts
    backend/apps/marketplace-service/src/app.module.ts
    backend/apps/stays-service/src/app.module.ts
    backend/apps/wallet-service/src/app.module.ts
    backend/apps/notifications-service/src/app.module.ts
    backend/src/resilience/resilience.module.ts
  </read_first>
  <action>
    In each of the 7 target files, add `import { ResilienceModule } from '../../../src/resilience/resilience.module';` and add `ResilienceModule` to the `@Module({ imports: [...] })` array — this exact import path and pattern matches `backend/apps/notifications-service/src/app.module.ts`'s own already-applied fix from Phase 16 (use it as the literal reference shape). For `auth-service` (which imports only `ConfigModule`, `PrismaModule`, `RedisModule`, `AuthModule` today — no `CommonModule`), add `ResilienceModule` alongside those 4 existing imports; `AuthService` itself already directly constructor-injects `ResilienceService` (confirmed via prior grep), so this fixes a real, currently-broken DI resolution for that scaffold. For the other 6 (`admin`, `ai`, `events`, `marketplace`, `stays`, `wallet` — all already import `CommonModule`), add `ResilienceModule` alongside the existing `CommonModule` import — do NOT remove `CommonModule`. `ResilienceModule`'s `@Global()` decorator does not cross separate `NestFactory` bootstrap trees, so each scaffold's own module tree needs this explicit import regardless of what `CommonModule` already provides in that same tree.
  </action>
  <verify>
    <automated>grep -l "ResilienceModule" backend/apps/*-service/src/app.module.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "ResilienceModule" backend/apps/*-service/src/app.module.ts` returns all 8 files (7 newly fixed + notifications-service already fixed)
    - each of the 7 modified files' `imports` array literal includes the identifier `ResilienceModule`
    - `auth-service/src/app.module.ts` still imports `AuthModule`, `PrismaModule`, `RedisModule`, `ConfigModule` unchanged (only `ResilienceModule` added)
    - the other 6 files still import `CommonModule` unchanged (only `ResilienceModule` added alongside it)
  </acceptance_criteria>
  <done>All 8 backend/apps/*-service scaffolds import ResilienceModule in their own bootstrap tree.</done>
</task>

<task type="auto">
  <name>Task 2: Verify all 8 gRPC scaffolds build cleanly and the full test suite is unaffected</name>
  <files>none (verification only)</files>
  <read_first>
    backend/package.json
  </read_first>
  <action>
    Run `cd backend && npm run build:services` (the existing script in backend/package.json already iterates `auth-service wallet-service events-service stays-service marketplace-service admin-service ai-service notifications-service`, running `npx nest build $s` for each and exiting non-zero on first failure) to confirm zero TypeScript errors across all 8 services. Then run `cd backend && npm test` to confirm the full existing suite (35+ suites) is still green. This task adds zero new production logic — the acceptance gate is exclusively the build + full-suite pass, no new spec file is required.
  </action>
  <verify>
    <automated>cd backend && npm run build:services</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npm run build:services` exits 0 with zero TypeScript errors across all 8 services
    - `cd backend && npm test` exits 0 (full existing suite green, no regressions)
  </acceptance_criteria>
  <done>All 8 gRPC service scaffolds compile cleanly; the existing test suite is unaffected by this wiring change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Each `backend/apps/*-service`'s own bootstrap tree | Separate `NestFactory` process from the monolith; `@Global()` modules do not cross this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-17-02-01 | Denial of Service (self-inflicted) | The 7 scaffolds' own vendor calls (Paystack via `CommonModule`, `AuthService`'s Termii/JWT calls) | mitigate | `ResilienceModule` is now importable in every scaffold's own bootstrap tree, closing the latent DI gap Phase 16 found only for `notifications-service`; these scaffolds are not yet live-wired to production traffic this phase, so current risk is latent/future-facing rather than active |
</threat_model>

<verification>
- `cd backend && npm run build:services` exits 0
- `cd backend && npm test` exits 0
</verification>

<success_criteria>
- All 8 `backend/apps/*-service` scaffolds import `ResilienceModule` in their own module tree and build with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-02-SUMMARY.md`
</output>
