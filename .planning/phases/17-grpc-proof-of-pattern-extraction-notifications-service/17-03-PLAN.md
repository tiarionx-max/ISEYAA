---
phase: 17
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - backend/src/modules/notifications-client/notifications-client.module.ts
  - backend/src/modules/notifications-client/notifications-client.service.ts
  - backend/src/modules/notifications-client/__tests__/notifications-client.service.spec.ts
autonomous: true
requirements: [GRPC-03]

must_haves:
  truths:
    - "NotificationsClientService.sendPush/registerToken convert the gRPC Observable response into the same Promise-shaped return NotificationsService always returned"
    - "A simulated gRPC transport failure results in a 503 ServiceUnavailableException, never a silent success"
    - "listForUser remains a network-free local stub"
  artifacts:
    - path: "backend/src/modules/notifications-client/notifications-client.module.ts"
      provides: "ClientsModule.registerAsync gRPC client registration"
      contains: "NOTIFICATIONS_PACKAGE"
    - path: "backend/src/modules/notifications-client/notifications-client.service.ts"
      provides: "gRPC facade over NotificationsService's 3 methods"
      contains: "resilience.execute('notificationsGrpc'"
    - path: "backend/src/modules/notifications-client/__tests__/notifications-client.service.spec.ts"
      provides: "facade test coverage"
      min_lines: 60
  key_links:
    - from: "backend/src/modules/notifications-client/notifications-client.service.ts"
      to: "backend/src/resilience/resilience.service.ts"
      via: "resilience.execute('notificationsGrpc', ...)"
      pattern: "resilience\\.execute\\('notificationsGrpc'"
    - from: "backend/src/modules/notifications-client/notifications-client.module.ts"
      to: "packages/proto/notifications.proto"
      via: "ClientsModule.registerAsync protoPath"
      pattern: "notifications\\.proto"
---

<objective>
Build the `NotificationsClientModule`/`NotificationsClientService` facade (D-01, D-02) — the first `ClientGrpc` client this codebase has ever constructed. This plan defines the contract and implementation only; wiring it into the app (moving the controller, rewiring the 2 real callers) happens in Plan 17-04's cutover wave, per the Interface-First Task Ordering principle.

Purpose: Provide a thin facade with today's exact method signatures (`sendPush`, `registerToken`, `listForUser`) backed by `ClientGrpc` internally, so both call sites can swap their import with a minimal diff — setting the copy-paste template for any future extraction (GRPC-07, deferred).
Output: A tested, standalone `NotificationsClientModule` + `NotificationsClientService`, not yet consumed by any controller/service (that's Plan 17-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-CONTEXT.md
@.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-RESEARCH.md
@.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-PATTERNS.md
@.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-VALIDATION.md
</context>

<interfaces>
<!-- Existing NotificationsService signatures this facade must match byte-for-byte at the call-site level (from backend/src/modules/notifications/notifications.service.ts) -->
async listForUser(_userId: string): Promise&lt;any[]&gt;   // returns []
async registerToken(userId: string, token: string)          // returns { registered: true }
async sendPush(userId: string, title: string, body: string, data?: Record&lt;string, string&gt;)   // returns { sent: boolean, reason?: string }

<!-- ResilienceService.execute() signature (backend/src/resilience/resilience.service.ts) -->
execute&lt;T&gt;(vendor: Vendor, fn: (context: { signal: AbortSignal }) =&gt; PromiseLike&lt;T&gt;): Promise&lt;T&gt;
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create NotificationsClientModule (D-02, D-04)</name>
  <files>backend/src/modules/notifications-client/notifications-client.module.ts</files>
  <read_first>
    backend/src/modules/auth/auth.module.ts
    backend/src/modules/notifications/notifications.module.ts
    packages/proto/generated/notifications.ts
    .env.example
  </read_first>
  <action>
    Create backend/src/modules/notifications-client/notifications-client.module.ts exporting a `NOTIFICATIONS_PACKAGE` string-constant token and a `NotificationsClientModule` class. Register `ClientsModule.registerAsync([{ name: NOTIFICATIONS_PACKAGE, imports: [ConfigModule], useFactory: (config: ConfigService) => ({ transport: Transport.GRPC, options: { package: 'notifications', protoPath: join(__dirname, '../../../../packages/proto/notifications.proto'), url: config.get<string>('NOTIFICATIONS_SERVICE_URL', 'localhost:5008') } }), inject: [ConfigService] }])`. Env var naming decision (resolves RESEARCH.md's Open Question 1): reuse the existing `.env.example` placeholder `NOTIFICATIONS_SERVICE_URL` (already committed since Phase 10, unused by any code today) rather than introducing a new `NOTIFICATIONS_GRPC_URL` name — this is zero `.env.example` churn and matches the naming convention already set for the other 7 not-yet-live `*_SERVICE_URL` vars in that file's "gRPC Microservices (Wave 3)" section. The `protoPath` join resolves via exactly 4 `../` segments from `__dirname` (backend/src/modules/notifications-client → repo root → packages/proto/notifications.proto), matching the depth already confirmed correct for this file's location in both `src` and compiled `dist` output. Do NOT declare a `controllers` array in this module yet — moving `NotificationsController`'s registration into this module happens in Plan 17-04's cutover wave. Set `providers: [NotificationsClientService]`, `exports: [NotificationsClientService]`.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - the module file exports both `NOTIFICATIONS_PACKAGE` and `NotificationsClientModule`
    - `ClientsModule.registerAsync`'s factory function reads `config.get<string>('NOTIFICATIONS_SERVICE_URL', 'localhost:5008')` — not `NOTIFICATIONS_GRPC_URL`
    - `protoPath` resolves to `packages/proto/notifications.proto` via exactly 4 `../` segments from `__dirname`
    - the module's `@Module({...})` decorator has no `controllers` key
  </acceptance_criteria>
  <done>NotificationsClientModule registers a gRPC ClientsModule client for the notifications package, configured via NOTIFICATIONS_SERVICE_URL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create NotificationsClientService (D-01, D-03, D-05, D-06)</name>
  <files>backend/src/modules/notifications-client/notifications-client.service.ts, backend/src/modules/notifications-client/__tests__/notifications-client.service.spec.ts</files>
  <read_first>
    backend/src/modules/notifications/notifications.service.ts
    backend/src/common/services/paystack.service.ts
    backend/src/resilience/resilience.service.ts
    packages/proto/generated/notifications.ts
  </read_first>
  <behavior>
    - listForUser(_userId): resolves to [] with zero network calls and zero calls to resilience.execute() (D-03 local no-op stub)
    - registerToken(userId, token): on gRPC success (mocked Observable resolves { success: true }), resolves { registered: true }
    - registerToken: on gRPC/resilience failure (mocked Observable rejects), throws ServiceUnavailableException whose message contains "Notifications service is temporarily unavailable"
    - sendPush(userId, title, body, data?): on gRPC success, resolves { sent: true }; the request sent to the gRPC client has `data: data ?? {}` (never undefined, even when the 4th arg is omitted)
    - sendPush: on gRPC/resilience failure, throws ServiceUnavailableException with the same message convention as registerToken
    - both registerToken and sendPush call `resilience.execute('notificationsGrpc', ...)` as their first argument (verified via a jest.fn() spy on the injected ResilienceService mock)
  </behavior>
  <action>
    Implement `NotificationsClientService implements OnModuleInit`, injecting `@Inject(NOTIFICATIONS_PACKAGE) private readonly client: ClientGrpc` and `private readonly resilience: ResilienceService`. In `onModuleInit()`, call `this.client.getService<notifications.NotificationsServiceClient>('NotificationsService')` and cache the result on a private field. Implement the 3 methods matching `NotificationsService`'s existing signatures exactly (`listForUser(_userId: string): Promise<any[]>`, `registerToken(userId: string, token: string)`, `sendPush(userId: string, title: string, body: string, data?: Record<string, string>)`), converting the gRPC client's Observable-returning proxy methods via `firstValueFrom()` from `rxjs`. In each catch block, log via `this.logger.error(...)` using only `err?.message ?? err` (never the full error object, request payload, or user data — matches `resilience.service.ts`'s `summarizeVendorError()` discipline), then throw `new ServiceUnavailableException('Notifications service is temporarily unavailable, please try again shortly')` (D-06 — matches `PaystackService`'s exact wording convention verbatim). In the spec file, mock `ClientGrpc.getService` to return a mock gRPC service object whose `sendPush`/`registerToken` jest.fn() mocks return `rxjs`'s `of({ success: true })` (success path) or a rejected Observable (failure path) — this is the first `ClientGrpc` mock in this codebase, no existing analog to copy verbatim; use `Test.createTestingModule` with manual `{ provide: NOTIFICATIONS_PACKAGE, useValue: mockClientGrpc }` and `{ provide: ResilienceService, useValue: mockResilience }` providers, matching the mock-provider wiring shape already used in `tour-notifications.service.spec.ts`.
  </action>
  <verify>
    <automated>cd backend && npx jest notifications-client.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npx jest notifications-client.service.spec.ts` passes with at least 5 test cases covering every scenario in the &lt;behavior&gt; block above
    - the spec asserts `resilience.execute` was called with `'notificationsGrpc'` as its first argument for both `sendPush` and `registerToken`
    - `NotificationsClientService`'s method signatures are call-compatible with `NotificationsService`'s existing signatures (same parameter count/order for `listForUser`/`registerToken`/`sendPush`)
    - no test triggers a real gRPC network dial (mock-only, no live `ClientGrpc` construction in the test module)
  </acceptance_criteria>
  <done>NotificationsClientService implements the 3-method facade, resilience-wraps its 2 network-calling methods, and throws ServiceUnavailableException on transport failure — fully covered by its own spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Monolith (`NotificationsClientService`) → `notifications-service` | New, first-of-kind live network boundary this facade will carry once wired in Plan 17-04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-17-03-01 | Information Disclosure | `notifications-client.service.ts` catch blocks | mitigate | Log only `err?.message`/`err?.code` via `Logger.error` — never the full gRPC error object, request payload, or user PII, matching `resilience.service.ts`'s `summarizeVendorError()` discipline |
| T-17-03-02 | Denial of Service | gRPC network hop to `notifications-service` | mitigate | `resilience.execute('notificationsGrpc', ...)` wraps every call in cockatiel timeout(5s)/retry(1)/circuit-breaker(8 failures) so a `notifications-service` outage degrades gracefully instead of hanging monolith requests indefinitely |
| T-17-03-03 | Information Disclosure | Plaintext gRPC channel (`Transport.GRPC`, no `credentials` option set) | accept | Railway private network (`*.railway.internal`) isolation; non-financial notification payloads; matches D-09's accepted-risk framing — MUST NOT be reused for a future money-touching extraction without revisiting |
</threat_model>

<verification>
- `cd backend && npx jest notifications-client.service.spec.ts` green
- `cd backend && npm run build` compiles with zero TypeScript errors (facade not yet wired into any consumer, so this is a compile-only check at this stage)
</verification>

<success_criteria>
- `NotificationsClientModule`/`NotificationsClientService` exist, are fully spec-tested, and are call-compatible with `NotificationsService`'s existing 3-method contract
</success_criteria>

<output>
After completion, create `.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-03-SUMMARY.md`
</output>
