---
phase: 17
plan: 06
type: execute
wave: 4
depends_on: ["17-01", "17-02", "17-03", "17-04", "17-05"]
files_modified: []
autonomous: false
requirements: [GRPC-03]

must_haves:
  truths:
    - "A human has confirmed notifications-service exists as its own Railway service, linked correctly, with the monolith's env var pointing at it"
    - "REST response shapes for /notifications/* are confirmed unchanged pre/post cutover by manual diff"
  artifacts: []
  key_links: []
---

<objective>
Final phase gate: confirm the two things no agent in this environment can verify — the live Railway dashboard service topology, and a manual before/after REST response-shape diff — before the phase is considered complete.

Purpose: RESEARCH.md's Environment Availability section confirmed no Railway CLI/dashboard access exists in this session, and no automated e2e harness exists for gRPC-backed REST endpoints today — both genuinely require human verification.
Output: Human confirmation recorded; phase gate closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-RESEARCH.md
@.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A full gRPC extraction cutover: notifications-service is called exclusively via ClientGrpc from a resilience-wrapped facade (NotificationsClientService), the GRPC-04 caller-graph audit is committed, ResilienceModule is wired into all 8 gRPC scaffolds, docker-compose and .env.example document the local/Railway deployment topology, and the full backend test suite passes with zero regressions.
  </what-built>
  <how-to-verify>
    1. In the Railway dashboard, confirm a distinct Railway service exists for notifications-service, linked to backend/apps/notifications-service/railway.toml (build config: dockerfilePath backend/apps/notifications-service/Dockerfile, watchPaths covering backend/apps/notifications-service/**, backend/src/modules/notifications/**, packages/proto/**).
    2. Confirm the monolith's own Railway service has NOTIFICATIONS_SERVICE_URL set as an environment variable, pointing at the private network hostname (e.g. notifications-service.railway.internal:5008).
    3. Locally (or on staging), boot both processes together — via `docker-compose up postgres redis backend notifications-service` or two separate `nest start` terminals sharing the same local Postgres/Redis — and call GET /api/v1/notifications, POST /api/v1/notifications/register-token, and POST /api/v1/notifications/send with a valid JWT bearer token. Confirm the response shapes and status codes match the pre-cutover behavior (only the underlying implementation changed — REST contract shape must be identical, except that a genuine notifications-service outage should now surface as HTTP 503 per D-06, which is the intended behavior change, not a regression).
    4. Confirm `cd backend && npm test` is green and `cd backend && npm run build:services` builds all 8 gRPC scaffolds with zero errors.
  </how-to-verify>
  <resume-signal>Type "approved" once all 4 steps are confirmed, or describe which step failed and its output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Railway dashboard configuration | Outside any agent's tool access this phase; genuine human-action prerequisite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-17-06-01 | Configuration (V14) | Railway env var linkage (NOTIFICATIONS_SERVICE_URL on the monolith's live Railway service) | mitigate | Human-verified directly in the Railway dashboard — no automated Railway CLI/dashboard access is available in this environment, per RESEARCH.md's Environment Availability finding |
</threat_model>

<verification>
Human confirmation of all 4 steps in how-to-verify above.
</verification>

<success_criteria>
- notifications-service is confirmed running as its own Railway service, correctly linked and configured
- REST response shapes for /notifications/* are confirmed unchanged pre/post cutover (except the intended 503-on-outage behavior)
- Full backend test suite and all-8-service build are both confirmed green
</success_criteria>

<output>
After completion, create `.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-06-SUMMARY.md`
</output>
