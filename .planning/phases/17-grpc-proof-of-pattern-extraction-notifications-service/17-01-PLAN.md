---
phase: 17
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - package-lock.json
  - backend/Dockerfile
  - backend/apps/notifications-service/Dockerfile
  - backend/src/resilience/resilience.types.ts
  - backend/src/resilience/resilience.service.ts
  - backend/src/resilience/__tests__/resilience.service.spec.ts
  - packages/proto/notifications.proto
  - packages/proto/generated/notifications.ts
  - backend/apps/notifications-service/src/notifications-grpc.controller.ts
autonomous: true
requirements: [GRPC-03]

must_haves:
  truths:
    - "docker build (or npm ci --workspace=backend --workspace=packages/proto) resolves @iseyaa/proto without TS2307"
    - "A gRPC UNAVAILABLE/DEADLINE_EXCEEDED/RESOURCE_EXHAUSTED failure is classified as transient and retried by ResilienceService"
    - "SendPushRequest carries a data map field end-to-end from proto through generated types to the server controller"
    - "Implements CONTEXT.md decision D-08 (proto data field widening); the isTransientError() fix is a hard prerequisite for D-05/D-07's resilience wrapping in Plan 17-03/17-04 to be anything but silently inert"
  artifacts:
    - path: "backend/package.json"
      provides: "@iseyaa/proto dependency declaration"
    - path: "backend/src/resilience/resilience.service.ts"
      provides: "gRPC-aware isTransientError()"
      contains: "GrpcStatus.UNAVAILABLE"
    - path: "packages/proto/notifications.proto"
      provides: "SendPushRequest.data field"
      contains: "map<string, string> data = 4;"
  key_links:
    - from: "backend/src/resilience/resilience.service.ts"
      to: "@grpc/grpc-js Status enum"
      via: "numeric .code branch"
      pattern: "GrpcStatus\\.(UNAVAILABLE|DEADLINE_EXCEEDED|RESOURCE_EXHAUSTED)"
    - from: "backend/apps/notifications-service/src/notifications-grpc.controller.ts"
      to: "packages/proto/notifications.proto"
      via: "data.data passthrough"
      pattern: "data\\.data"
---

<objective>
Fix the three pre-existing blocking prerequisites this phase's live cutover depends on: (1) the `@iseyaa/proto` Docker dependency gap that breaks BOTH the monolith's own production image and `notifications-service`'s image, (2) `ResilienceService.isTransientError()`'s inability to classify any gRPC failure as transient (which would otherwise make Wave 2/3's resilience wrapping silently inert), and (3) the `SendPushRequest` proto's missing `data` field (required for true zero-behavior-change per D-08).

Purpose: These three gaps are independent of each other but all MUST land before the facade (Plan 17-03) and cutover (Plan 17-04) can be trusted to behave correctly in production.
Output: Working Docker builds for `backend/Dockerfile` and `backend/apps/notifications-service/Dockerfile`; a gRPC-aware `isTransientError()`; a widened, regenerated `notifications.proto` contract with server-side passthrough.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-CONTEXT.md
@.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-RESEARCH.md
@.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the @iseyaa/proto Docker dependency gap (monolith AND notifications-service)</name>
  <files>backend/package.json, package-lock.json, backend/Dockerfile, backend/apps/notifications-service/Dockerfile</files>
  <read_first>
    backend/package.json
    backend/Dockerfile
    backend/apps/notifications-service/Dockerfile
    packages/proto/package.json
  </read_first>
  <action>
    Add "@iseyaa/proto": "0.1.0" to backend/package.json's "dependencies" object, placed alphabetically between "@grpc/proto-loader" and "@nestjs/common" (matching the block's existing near-alphabetical ordering; version pinned exactly to "0.1.0" per packages/proto/package.json's own declared version, no "^" — internal workspace package convention). Run `npm install` from the repo root to refresh package-lock.json (npm ci requires an in-sync lockfile; editing only package.json will make Docker's npm ci step fail). In backend/Dockerfile (the line currently reading `RUN npm ci --workspace=backend --include=workspace=shared`) and in backend/apps/notifications-service/Dockerfile (identical line), replace it with `RUN npm ci --workspace=backend --workspace=packages/proto`. The `--include=workspace=shared` flag is not a valid npm CLI flag (`npm ci --include` only accepts prod|dev|optional|peer) and nothing under backend/ imports @iseyaa/shared today, so dropping it is safe. Do NOT change either Dockerfile's COPY steps — both already stage packages/proto's source (`COPY packages/ ./packages/` in the monolith Dockerfile, `COPY packages/proto/ ./packages/proto/` in the notifications-service Dockerfile) before this npm ci line runs.
  </action>
  <verify>
    <automated>npm ci --workspace=backend --workspace=packages/proto</automated>
  </verify>
  <acceptance_criteria>
    - backend/package.json's "dependencies" object contains the exact key/value pair "@iseyaa/proto": "0.1.0"
    - Neither backend/Dockerfile nor backend/apps/notifications-service/Dockerfile contains the substring "--include=workspace=shared"
    - `npm ci --workspace=backend --workspace=packages/proto` run from the repo root exits 0
    - `node -e "console.log(require.resolve('@iseyaa/proto'))"` run from inside backend/ resolves to a real path (no MODULE_NOT_FOUND)
    - if a Docker daemon is available: `docker build -f backend/Dockerfile -t iseyaa-backend-test .` and `docker build -f backend/apps/notifications-service/Dockerfile -t iseyaa-notifications-test .` both exit 0 with no TS2307 error
  </acceptance_criteria>
  <done>Both Dockerfiles' npm ci step resolves @iseyaa/proto and the workspace dependency is correctly declared and lockfile-committed.</done>
</task>

<task type="auto">
  <name>Task 2: Add gRPC-aware transient-error classification to ResilienceService</name>
  <files>backend/src/resilience/resilience.types.ts, backend/src/resilience/resilience.service.ts, backend/src/resilience/__tests__/resilience.service.spec.ts</files>
  <read_first>
    backend/src/resilience/resilience.types.ts
    backend/src/resilience/resilience.service.ts
    backend/src/resilience/__tests__/resilience.service.spec.ts
  </read_first>
  <action>
    In resilience.types.ts: add `'notificationsGrpc'` as the 10th member of the `Vendor` union type (append after `'sendgrid'`), and add a matching `RESILIENCE_DEFAULTS` entry: `{ timeoutMs: 5_000, retryCount: 1, failureThreshold: 8, halfOpenAfterMs: 20_000 }` — this mirrors the existing `fcm` vendor's shape (same-region Railway-internal gRPC hop, best-effort/non-financial, expected to be at least as fast as the FCM HTTP round-trip fcm already wraps). Add a one-line comment above the new entry explaining the mirrored-fcm rationale, tunable later via PlatformConfig without a code change. In resilience.service.ts: import `status as GrpcStatus` from `'@grpc/grpc-js'`. Inside `isTransientError()`, insert a new branch that checks `typeof (err as any)?.code === 'number'` and returns `true` only when the code equals `GrpcStatus.UNAVAILABLE` (14), `GrpcStatus.DEADLINE_EXCEEDED` (4), or `GrpcStatus.RESOURCE_EXHAUSTED` (8) — this branch MUST be inserted BEFORE the existing `typeof code === 'string'` branch, since a numeric gRPC code would otherwise silently fall through every existing branch to the final `return false`. Do NOT add `INTERNAL`, `UNKNOWN`, or `INVALID_ARGUMENT` to the transient set — those indicate the server processed the request and hit a deterministic bug/bad-input, matching the existing exclusion rationale already applied to axios 4xx responses. In resilience.service.spec.ts: add a new test inside the `'isTransientError narrowing (WR-04)'` describe block asserting that `service.execute('notificationsGrpc', fn)` retries when `fn` rejects with `{ code: 14, details: 'no connection' }` (assert `fn.mock.calls.length` is greater than 1, matching the existing ECONNREFUSED test's shape). Update line 40's stale comment from "builds all 9 vendor policies" to "builds all 10 vendor policies" (Object.keys(RESILIENCE_DEFAULTS) iteration itself needs no change).
  </action>
  <verify>
    <automated>cd backend && npx jest resilience.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - resilience.types.ts's `Vendor` union type includes `'notificationsGrpc'` and `RESILIENCE_DEFAULTS` has a matching `notificationsGrpc` key
    - `cd backend && npx jest resilience.service.spec.ts` passes, including the new gRPC UNAVAILABLE=14 retry test
    - the numeric-code branch (`typeof grpcCode === 'number'`) appears in source BEFORE the existing `typeof code === 'string'` branch inside `isTransientError()` (verify via grep line numbers)
    - `INTERNAL`, `UNKNOWN`, `INVALID_ARGUMENT` do NOT appear anywhere in the new branch
  </acceptance_criteria>
  <done>isTransientError() classifies gRPC UNAVAILABLE/DEADLINE_EXCEEDED/RESOURCE_EXHAUSTED as transient; the notificationsGrpc vendor policy is registered and passes its dedicated test.</done>
</task>

<task type="auto">
  <name>Task 3: Widen SendPushRequest with the data field and pass it through server-side (D-08)</name>
  <files>packages/proto/notifications.proto, packages/proto/generated/notifications.ts, backend/apps/notifications-service/src/notifications-grpc.controller.ts</files>
  <read_first>
    packages/proto/notifications.proto
    packages/proto/generate.sh
    backend/apps/notifications-service/src/notifications-grpc.controller.ts
    backend/src/modules/notifications/notifications.service.ts
  </read_first>
  <action>
    Add `map<string, string> data = 4;` as the 4th field of the `SendPushRequest` message in packages/proto/notifications.proto (additive-only — confirmed zero other consumers of this proto exist yet, so widening is safe). Regenerate via `bash packages/proto/generate.sh` run from the repo root, then run `npm run build --workspace=packages/proto` to recompile the tsc dist output (Phase 16 added this compile step; both the regenerated .ts source AND the compiled dist must be current, since consumers import the compiled `@iseyaa/proto` package, not its raw generated/ source). In notifications-grpc.controller.ts's `sendPush` `@GrpcMethod` handler, change `this.notificationsService.sendPush(data.userId, data.title, data.body)` to pass a 4th argument, `data.data` — this is the ONLY line that changes in this file; the `registerToken` handler stays untouched. Do NOT add a `reason` field to `SendPushResponse` in this task — a grep of `web/src` and `mobile/app` confirms zero client consumption of a `reason` field on the push-send response, so per RESEARCH.md's Pitfall 5 recommendation this proto response-shape gap is accepted (not fixed) and will be explicitly documented in Plan 17-04's caller-graph audit, not silently dropped.
  </action>
  <verify>
    <automated>bash packages/proto/generate.sh</automated>
  </verify>
  <acceptance_criteria>
    - packages/proto/notifications.proto's `SendPushRequest` message has exactly 4 fields, the 4th being `map<string, string> data = 4;`
    - packages/proto/generated/notifications.ts's `SendPushRequest` interface includes a `data` property typed as a string-keyed record
    - backend/apps/notifications-service/src/notifications-grpc.controller.ts's `sendPush` method passes `data.data` as the 4th argument to `notificationsService.sendPush(...)`
    - `bash packages/proto/generate.sh` exits 0 and regenerates `packages/proto/generated/notifications.ts`
    - `npm run build --workspace=packages/proto` exits 0
  </acceptance_criteria>
  <done>SendPushRequest.data survives from proto through generated types to the server-side controller; SendPushResponse's missing reason field is explicitly deferred to the caller-graph audit, not silently regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Docker build supply chain | `npm ci` workspace dependency resolution inside both Dockerfiles — a broken resolution here silently produces a non-deployable image |
| Monolith ↔ notifications-service (future) | Plaintext gRPC channel that this task's resilience fix protects against real network failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-17-01-01 | Denial of Service (self-inflicted) | `backend/src/resilience/resilience.service.ts` `isTransientError()` | mitigate | Add the numeric gRPC status-code branch so circuit-breaker/retry actually engage on real `notifications-service` outages instead of silently never triggering |
| T-17-01-02 | Information Disclosure | gRPC channel (plaintext, default `Transport.GRPC`, no TLS/mTLS) | accept | Same-region Railway private network (`*.railway.internal`), not internet-routable; matches D-09's accepted-risk rationale for non-financial notification data; MUST be revisited before any future money-touching extraction reuses this pattern |
| T-17-01-03 | Tampering (supply chain) | Docker `npm ci` workspace dependency resolution | mitigate | Declare `@iseyaa/proto` as an explicit `backend` dependency with a pinned exact version (`0.1.0`, no `^`) rather than relying on implicit unscoped resolution |
</threat_model>

<verification>
- `cd backend && npx jest resilience.service.spec.ts` green
- `bash packages/proto/generate.sh` and `npm run build --workspace=packages/proto` both exit 0
- `npm ci --workspace=backend --workspace=packages/proto` exits 0 from repo root
- Full suite regression not required for this plan alone (no existing production call sites changed yet) but recommended: `cd backend && npm test`
</verification>

<success_criteria>
- Both Dockerfiles can resolve `@iseyaa/proto` during `npm ci`
- `isTransientError()` correctly classifies gRPC's numeric status codes as transient
- `SendPushRequest` carries the `data` field end-to-end through the generated types and the server-side gRPC controller
</success_criteria>

<output>
After completion, create `.planning/phases/17-grpc-proof-of-pattern-extraction-notifications-service/17-01-SUMMARY.md`
</output>
