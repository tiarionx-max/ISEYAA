---
phase: 10-documentation-correction-grpc-build-fix
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/proto/generate.sh
  - packages/proto/package.json
  - packages/proto/transport.proto
  - packages/proto/delivery.proto
  - packages/proto/tour-packages.proto
  - packages/proto/tour-guides.proto
  - packages/proto/news.proto
  - packages/proto/waitlist.proto
  - packages/proto/reviews.proto
  - packages/proto/generated/index.ts
autonomous: true
requirements: [GRPC-02]

must_haves:
  truths:
    - ".proto contracts exist under packages/proto/ for transport, delivery, tour-packages, tour-guides, news, waitlist, and reviews"
    - "packages/proto/generate.sh produces real ts-proto-generated TypeScript (NestJS gRPC client interfaces with Observable/Metadata) for all 15 modules, not hand-written stub interfaces"
    - "Running bash packages/proto/generate.sh from the repo root exits 0 with zero codegen errors"
    - "packages/proto/generated/index.ts exports all 15 generated modules, not just the original 8"
  artifacts:
    - path: "packages/proto/transport.proto"
      provides: "gRPC contract for Driver/Trip request-accept-complete flow"
      contains: "service TransportService"
    - path: "packages/proto/delivery.proto"
      provides: "gRPC contract for DeliveryRider/DeliveryOrder request-accept-otp-complete flow"
      contains: "service DeliveryService"
    - path: "packages/proto/tour-packages.proto"
      provides: "gRPC contract for TourPackage lookup/create/submit/list"
      contains: "service TourPackagesService"
    - path: "packages/proto/tour-guides.proto"
      provides: "gRPC contract for TourGuide lookup/kyc/approve/list"
      contains: "service TourGuidesService"
    - path: "packages/proto/news.proto"
      provides: "gRPC contract for NewsItem read-only list"
      contains: "service NewsService"
    - path: "packages/proto/waitlist.proto"
      provides: "gRPC contract for WaitlistEntry join + stats"
      contains: "service WaitlistService"
    - path: "packages/proto/reviews.proto"
      provides: "gRPC contract for Review create/list/moderation"
      contains: "service ReviewsService"
    - path: "packages/proto/generate.sh"
      provides: "Working protoc-based codegen invocation via grpc_tools_node_protoc"
      contains: "grpc_tools_node_protoc"
    - path: "packages/proto/package.json"
      provides: "grpc-tools devDependency"
      contains: "grpc-tools"
  key_links:
    - from: "packages/proto/generate.sh"
      to: "node_modules/.bin/protoc-gen-ts_proto"
      via: "--plugin flag passed to grpc_tools_node_protoc"
      pattern: "protoc-gen-ts_proto"
    - from: "packages/proto/generated/index.ts"
      to: "packages/proto/generated/transport.ts"
      via: "export * from './transport'"
      pattern: "export \\* from '\\./transport'"
---

<objective>
Fix `packages/proto/generate.sh`'s broken protoc invocation (it currently calls `ts-proto`'s protoc *plugin* binary directly as if it were a standalone CLI — it isn't, and produces garbled binary output, not TypeScript) and author 7 new `.proto` contracts (transport, delivery, tour-packages, tour-guides, news, waitlist, reviews) for the modules that were never proto-stubbed, so that running `generate.sh` produces real, working TypeScript for all 15 target modules.

Purpose: GRPC-02 requires proto coverage for the 7 previously-unstubbed modules and a codegen pipeline that actually works — today's checked-in `packages/proto/generated/*.ts` files are hand-written stub interfaces, not the product of any successful codegen run.
Output: 7 new `.proto` files matching the existing 8 files' narrow-RPC-surface style; a fixed `generate.sh` using `grpc-tools`' `grpc_tools_node_protoc` as the real protoc front end; regenerated `packages/proto/generated/*.ts` for all 15 modules containing genuine `ts-proto --ts_proto_opt=nestJs=true` output (Observable/Metadata-based gRPC client interfaces); an updated `generated/index.ts` barrel exporting all 15.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-documentation-correction-grpc-build-fix/10-RESEARCH.md
@.planning/phases/10-documentation-correction-grpc-build-fix/10-PATTERNS.md
@.planning/phases/10-documentation-correction-grpc-build-fix/10-VALIDATION.md
@CLAUDE.md
</context>

<interfaces>
Existing wallet.proto (mutating action-verb RPC style — 4 RPCs, snake_case fields, Request/Response suffix convention):
service WalletService { rpc Credit(CreditRequest) returns (CreditResponse); rpc Debit(DebitRequest) returns (DebitResponse); rpc GetBalance(BalanceRequest) returns (BalanceResponse); rpc GetTransactions(GetTransactionsRequest) returns (GetTransactionsResponse); }

Existing events.proto (read/availability-check RPC style — 3 RPCs):
service EventsService { rpc GetEvent(GetEventRequest) returns (GetEventResponse); rpc CheckTicketAvailability(TicketAvailabilityRequest) returns (TicketAvailabilityResponse); rpc ReserveTicket(ReserveTicketRequest) returns (ReserveTicketResponse); }

Shared conventions across all 8 existing .proto files (apply identically to all 7 new files): `syntax = "proto3";` + `package <module>;` header; one `service <PascalCaseModule>Service { ... }` block; snake_case field names (ts-proto's nestJs=true/esModuleInterop=true camelCase these automatically in generated TS); 3-4 RPCs per file covering critical-path operations only, NOT a full CRUD mirror; `Request`/`Response` message suffix per RPC; IDs always `string` (UUIDs), money fields always `double`, timestamps always `string` (ISO), status/enum fields always `string` (matches Prisma's own enum serialization convention already used by all 8 existing files).

Existing generated/wallet.ts shows the CURRENT (broken/hand-written) stub shape — plain TypeScript interfaces with camelCase fields, NO WalletServiceClient Observable-based gRPC client boilerplate beyond a bare Promise-returning interface. Once generate.sh is fixed, this file (and all others in generated/) will be OVERWRITTEN with genuine ts-proto output containing `Observable`/`Metadata` imports from `@grpc/grpc-js`/`rxjs` and full `@GrpcMethod`-compatible NestJS client/controller interfaces — this is expected and correct; do not hand-edit generated/*.ts files.

generated/index.ts (current, hand-written barrel — NOT touched by generate.sh, must be manually updated):
export * from './auth'; export * from './wallet'; export * from './events'; export * from './stays'; export * from './marketplace'; export * from './admin'; export * from './ai'; export * from './notifications';
(8 lines today — this plan adds 7 more lines for the new modules; ts-proto does not auto-generate an index barrel, so this file remains hand-maintained.)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix generate.sh's protoc invocation and add grpc-tools</name>
  <files>packages/proto/generate.sh, packages/proto/package.json</files>
  <read_first>
    packages/proto/generate.sh (current, broken)
    packages/proto/package.json (current — packages/proto IS an npm workspace per root package.json's workspaces array, so a devDependency added here installs correctly via `npm install` at the repo root)
    packages/proto/generated/wallet.ts (current hand-written stub — the "before" baseline this task's smoke test will overwrite)
  </read_first>
  <action>
    In packages/proto/package.json, add a `devDependencies` object (the file currently has none) containing `"grpc-tools": "^1.13.0"`.

    In packages/proto/generate.sh, replace the broken invocation block — currently `npx ts-proto --plugin=./node_modules/.bin/protoc-gen-ts_proto --ts_proto_out=./packages/proto/generated --ts_proto_opt=nestJs=true --ts_proto_opt=outputServices=grpc-js --ts_proto_opt=esModuleInterop=true ./packages/proto/*.proto` — with: `npx grpc_tools_node_protoc --plugin=protoc-gen-ts_proto=./node_modules/.bin/protoc-gen-ts_proto --ts_proto_out=./packages/proto/generated --ts_proto_opt=nestJs=true --ts_proto_opt=outputServices=grpc-js --ts_proto_opt=esModuleInterop=true --proto_path=./packages/proto ./packages/proto/*.proto`. The key differences: invoke `grpc_tools_node_protoc` (the real protoc-equivalent binary bundled by the `grpc-tools` npm package) instead of `ts-proto` directly, pass the plugin as `--plugin=protoc-gen-ts_proto=<path>` (named-plugin syntax, not a bare `--plugin=<path>`), and add `--proto_path=./packages/proto` so the protoc front end knows where to resolve the `.proto` files from. Keep the `set -e`, `mkdir -p packages/proto/generated`, and the trailing `echo`/`ls` lines completely unchanged — only the `npx ts-proto ...` block itself changes.

    After editing, run `npm install` from the repo root (installs grpc-tools into the workspace's node_modules), then run `bash packages/proto/generate.sh` as a smoke test against the 8 EXISTING proto files only (the 7 new files do not exist until Task 2) — confirm it exits 0 and that `packages/proto/generated/wallet.ts` now contains real ts-proto output (an `import { Observable }` line) rather than the old hand-written stub shape.
  </action>
  <verify>
    <automated>bash packages/proto/generate.sh && grep -l "Observable" packages/proto/generated/wallet.ts</automated>
  </verify>
  <acceptance_criteria>
    packages/proto/package.json contains `"grpc-tools"` under `devDependencies`.
    packages/proto/generate.sh contains the string `grpc_tools_node_protoc` and does NOT contain a line starting with `npx ts-proto ` followed by `--plugin=./node_modules/.bin/protoc-gen-ts_proto` (the old broken bare invocation).
    `bash packages/proto/generate.sh` exits 0.
    `grep -l "Observable" packages/proto/generated/wallet.ts` matches (confirms real ts-proto NestJS-style output replaced the old hand-written stub).
  </acceptance_criteria>
  <done>generate.sh invokes a real protoc front end via grpc-tools; running it against the 8 existing proto files produces genuine ts-proto output, not stub interfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Author 7 new .proto contracts for transport, delivery, tour-packages, tour-guides, news, waitlist, reviews</name>
  <files>packages/proto/transport.proto, packages/proto/delivery.proto, packages/proto/tour-packages.proto, packages/proto/tour-guides.proto, packages/proto/news.proto, packages/proto/waitlist.proto, packages/proto/reviews.proto</files>
  <read_first>
    packages/proto/wallet.proto (mutating-RPC template — read in full)
    packages/proto/events.proto (read/availability-RPC template — read in full)
    backend/prisma/schema.prisma lines 695-793 (Driver, Vehicle, Trip models)
    backend/prisma/schema.prisma lines 795-880 (DeliveryRider, DeliveryOrder, WaitlistEntry models)
    backend/prisma/schema.prisma lines 882-968 (NewsItem, TourGuide, TourPackage models)
    backend/prisma/schema.prisma lines 1017-1039 (Review model)
    backend/src/modules/transport/transport.controller.ts (endpoint inventory: fare-estimate, drivers, go-online/go-offline, trips + accept/decline/arrive/start/complete/cancel)
    backend/src/modules/delivery/delivery.controller.ts (endpoint inventory: fee-estimate, riders, go-online/go-offline, orders + accept/decline/collect/verify-otp/complete/rate/cancel)
    backend/src/modules/tour-packages/tour-packages.controller.ts (endpoint inventory: list, me, :slug, create, from-ai-suggestion, update, submit, delete)
    backend/src/modules/tour-guides/tour-guides.controller.ts (endpoint inventory: list, me, me/kyc, me/availability, create, :id, admin approve)
    backend/src/modules/news/news.controller.ts (single GET endpoint)
    backend/src/modules/waitlist/waitlist.controller.ts (POST create, GET stats)
    backend/src/modules/reviews/reviews.controller.ts (POST create, GET list, admin flags/:id/resolve)
  </read_first>
  <action>
    Author 7 new .proto files in packages/proto/, each following the exact shared-conventions block described in the interfaces section above (syntax proto3, package header, one service block, snake_case fields, Request/Response suffix, narrow 3-4 RPC surface). Do not create a full CRUD mirror of any controller — pick the critical-path subset per file below, derived from each Prisma model's actual fields (not invented):

    transport.proto — package transport, service TransportService, 4 RPCs: RequestTrip(RequestTripRequest: rider_id, vehicle_type, pickup_lat, pickup_lng, pickup_address, dropoff_lat, dropoff_lng, dropoff_address) returns RequestTripResponse(trip_id, status, fare); AcceptTrip(AcceptTripRequest: trip_id, driver_id) returns AcceptTripResponse(success, status); CompleteTrip(CompleteTripRequest: trip_id) returns CompleteTripResponse(success, fare, driver_earnings, platform_fee); GetTripStatus(GetTripStatusRequest: trip_id) returns GetTripStatusResponse(trip_id, status, driver_id, fare).

    delivery.proto — package delivery, service DeliveryService, 4 RPCs: RequestDelivery(RequestDeliveryRequest: sender_id, pickup_address, pickup_lat, pickup_lng, dropoff_address, dropoff_lat, dropoff_lng, item_description, weight_kg, recipient_phone) returns RequestDeliveryResponse(order_id, status, fee); AcceptDelivery(AcceptDeliveryRequest: order_id, rider_id) returns AcceptDeliveryResponse(success, status); VerifyDeliveryOtp(VerifyDeliveryOtpRequest: order_id, otp) returns VerifyDeliveryOtpResponse(success); CompleteDelivery(CompleteDeliveryRequest: order_id, proof_photo_url) returns CompleteDeliveryResponse(success, fee, rider_earnings, platform_fee).

    tour-packages.proto — package tour_packages, service TourPackagesService, 4 RPCs: GetTourPackage(GetTourPackageRequest: slug) returns GetTourPackageResponse(id, slug, name, category, price, duration_hours, max_group_size, status, tour_guide_id, lga_id); ListTourPackages(ListTourPackagesRequest: category, status) returns ListTourPackagesResponse(repeated TourPackageSummary packages); CreateTourPackage(CreateTourPackageRequest: name, category, price, duration_hours, tour_guide_id, lga_id) returns CreateTourPackageResponse(id, status); SubmitTourPackage(SubmitTourPackageRequest: id) returns SubmitTourPackageResponse(success, status). Include a `TourPackageSummary` message (id, slug, name, category, price, status) for the list RPC's repeated field.

    tour-guides.proto — package tour_guides, service TourGuidesService, 4 RPCs: GetTourGuide(GetTourGuideRequest: id) returns GetTourGuideResponse(id, user_id, bio, years_experience, kyc_tier, status, rating); ListTourGuides(ListTourGuidesRequest: status) returns ListTourGuidesResponse(repeated TourGuideSummary guides); SubmitTourGuideKyc(SubmitTourGuideKycRequest: user_id, nin_ciphertext, nin_hash) returns SubmitTourGuideKycResponse(success, kyc_tier); ApproveTourGuide(ApproveTourGuideRequest: id, approved_by_id) returns ApproveTourGuideResponse(success, status). Include a `TourGuideSummary` message (id, user_id, bio, rating, status) for the list RPC.

    news.proto — package news, service NewsService, 1 RPC (matches the module's single read-only REST endpoint, per the existing narrow-surface convention applied to its thinnest extreme): ListNews(ListNewsRequest: limit, category) returns ListNewsResponse(repeated NewsItemSummary items). Include a `NewsItemSummary` message (id, headline, summary, link, source, category, image_url, published_at, is_priority).

    waitlist.proto — package waitlist, service WaitlistService, 2 RPCs: JoinWaitlist(JoinWaitlistRequest: email, phone, full_name, source) returns JoinWaitlistResponse(id, success); GetWaitlistStats(GetWaitlistStatsRequest: source) returns GetWaitlistStatsResponse(total_count).

    reviews.proto — package reviews, service ReviewsService, 3 RPCs: CreateReview(CreateReviewRequest: target_type, target_id, user_id, tour_booking_id, rating, comment) returns CreateReviewResponse(id, flagged); ListReviews(ListReviewsRequest: target_type, target_id) returns ListReviewsResponse(repeated ReviewSummary reviews); ResolveReviewFlag(ResolveReviewFlagRequest: review_id, resolved_by_id) returns ResolveReviewFlagResponse(success). Include a `ReviewSummary` message (id, user_id, rating, comment, flagged, created_at).

    Field numbering starts at 1 and increments sequentially within each message, matching the existing 8 files' convention. Do not add proto `import` statements between the 7 new files or to the existing 8 — each file stays self-contained, matching the existing convention (no cross-proto imports in this repo).
  </action>
  <verify>
    <automated>ls packages/proto/transport.proto packages/proto/delivery.proto packages/proto/tour-packages.proto packages/proto/tour-guides.proto packages/proto/news.proto packages/proto/waitlist.proto packages/proto/reviews.proto</automated>
  </verify>
  <acceptance_criteria>
    All 7 files exist at the exact paths listed above.
    Each file starts with `syntax = "proto3";` followed by a `package` line matching its module name (transport, delivery, tour_packages, tour_guides, news, waitlist, reviews).
    Each file contains exactly one `service <PascalCaseModule>Service` block.
    `grep -c "^service " packages/proto/transport.proto packages/proto/delivery.proto packages/proto/tour-packages.proto packages/proto/tour-guides.proto packages/proto/news.proto packages/proto/waitlist.proto packages/proto/reviews.proto` returns `1` for every file.
    Every field in every message uses snake_case naming (no camelCase field names in any `.proto` file).
  </acceptance_criteria>
  <done>7 new .proto files exist, each matching the existing 8 files' style, with field shapes derived directly from the corresponding Prisma models and controller action verbs.</done>
</task>

<task type="auto">
  <name>Task 3: Run full codegen for all 15 modules and update the generated/index.ts barrel</name>
  <files>packages/proto/generated/index.ts</files>
  <read_first>
    packages/proto/generate.sh (post-Task-1 fixed state)
    packages/proto/generated/index.ts (current 8-line barrel)
    The 7 new .proto files authored in Task 2
  </read_first>
  <action>
    Run `bash packages/proto/generate.sh` from the repo root, now with all 15 .proto files present (8 existing + 7 new from Task 2). This regenerates every file in packages/proto/generated/, including overwriting the 8 pre-existing hand-written stubs with genuine ts-proto output.

    Add 7 new export lines to packages/proto/generated/index.ts, one per new module, in the same `export * from './<module>';` format as the existing 8 lines: `export * from './transport';`, `export * from './delivery';`, `export * from './tour-packages';`, `export * from './tour-guides';`, `export * from './news';`, `export * from './waitlist';`, `export * from './reviews';`. Preserve the existing 8 lines unchanged and in their current order; append the 7 new lines after them. This file is not touched by generate.sh (ts-proto does not emit an index barrel), so this edit is the one intentional hand-maintained exception to "generated/ is regenerated output, not hand-written."
  </action>
  <verify>
    <automated>grep -l "GrpcMethod\|Observable" packages/proto/generated/*.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    `bash packages/proto/generate.sh` exits 0 with no error output.
    `grep -l "GrpcMethod\|Observable" packages/proto/generated/*.ts` matches all 15 expected files (auth, wallet, events, marketplace, notifications, stays, admin, ai, transport, delivery, tour-packages, tour-guides, news, waitlist, reviews) — confirmed by counting the match list equals 15.
    packages/proto/generated/index.ts contains 15 `export * from` lines total (8 original + 7 new), with the original 8 unchanged.
    No file under packages/proto/generated/ still matches the old hand-written-stub shape (`grep -L "Observable" packages/proto/generated/*.ts` returns zero files, i.e. every generated file now contains Observable-based client boilerplate).
  </acceptance_criteria>
  <done>All 15 modules produce genuine ts-proto-generated TypeScript with zero codegen errors; generated/index.ts exports all 15 modules.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Build-time codegen tooling (generate.sh, grpc-tools) | Compile-time only — no runtime request path is created or modified by this plan; the 15 generated .ts files are type contracts, not live network endpoints (zero ClientGrpc/@GrpcMethod wiring is added anywhere, per explicit phase scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering (supply chain) | grpc-tools devDependency added to packages/proto/package.json | accept | Well-established, widely-used npm package used only at build-time codegen, never shipped into a deployed runtime artifact; existing repo-wide `npm audit`/dependency review process already covers new devDependencies — no additional control needed for a build-tool-only addition |
| T-10-03b | Information Disclosure | 7 new .proto contracts describe field shapes for Driver/Trip/DeliveryOrder/TourGuide/TourPackage/Review/NewsItem/WaitlistEntry | accept | Proto contracts declare only field names/types already present in the Prisma schema and existing REST DTOs — no new sensitive field (e.g. no NIN/BVN plaintext) is introduced; tour-guides.proto's KYC RPC carries only `nin_ciphertext`/`nin_hash`, matching the existing encrypted-at-rest convention (Phase 5 EncryptionService), never plaintext NIN |

</threat_model>

<verification>
Run `bash packages/proto/generate.sh` and confirm it exits 0. Run `grep -l "GrpcMethod\|Observable" packages/proto/generated/*.ts` and confirm exactly 15 files match. Confirm `packages/proto/generated/index.ts` contains 15 export lines. Confirm all 7 new `.proto` files exist with exactly one `service` block each.
</verification>

<success_criteria>
.proto contracts exist for transport, delivery, tour-packages, tour-guides, news, waitlist, and reviews; packages/proto/generate.sh produces real TypeScript for all 15 modules with zero codegen errors.
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation-correction-grpc-build-fix/10-03-SUMMARY.md`
</output>
