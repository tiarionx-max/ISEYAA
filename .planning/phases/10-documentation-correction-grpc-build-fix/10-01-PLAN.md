---
phase: 10-documentation-correction-grpc-build-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "ROADMAP.md's Phase 2 success criterion 2 no longer claims every microservice deploys as a separate Railway service — it states proto contracts existed with zero live @GrpcMethod/ClientGrpc wiring and a single monolithic NestFactory.create() process"
    - "ROADMAP.md's 02-07-PLAN.md checkbox line no longer claims ts-proto TypeScript generation succeeded — it states the codegen pipeline was broken until Phase 10 fixed it"
    - "PROJECT.md contains no overstated gRPC extraction claims anywhere in the file"
    - "ROADMAP.md lines 72-77 (02-08 through 02-11 checkboxes) are untouched — DOC-01 scopes only the Phase 2 success-criterion and 02-07 lines"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase 2 success criterion 2 and 02-07 plan description"
      contains: "zero live"
    - path: ".planning/PROJECT.md"
      provides: "Confirmed-accurate gRPC state description (already substantially corrected per research)"
      contains: "never wired into"
  key_links: []
---

<objective>
Correct the documentation record so ROADMAP.md and PROJECT.md accurately state the real gRPC starting state: proto contracts existed for 8 services under `packages/proto/`, but zero live `@GrpcMethod`/`ClientGrpc` wiring was ever implemented, and the platform ran (and still runs) as a single monolithic `NestFactory.create()` process — not "8 services extracted complete" as ROADMAP.md's Phase 2 section previously claimed.

Purpose: Before any new v2.0 work proceeds, the project's own historical record must stop overstating what Phase 2 actually delivered — future planning (Phase 16, Phase 17) depends on an accurate baseline.
Output: ROADMAP.md Phase 2 section corrected at the two concentrated drift points (success criterion 2, `02-07-PLAN.md` line); PROJECT.md confirmed accurate or corrected if review surfaces any remaining overstatement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-documentation-correction-grpc-build-fix/10-RESEARCH.md
@.planning/phases/10-documentation-correction-grpc-build-fix/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct ROADMAP.md's Phase 2 gRPC completion claims</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    .planning/ROADMAP.md (current Phase 2 section, lines 48-81 — read in full before editing)
    .planning/PROJECT.md (lines 15-49 — already-corrected phrasing template to match in tone/precision, per 10-PATTERNS.md "Documentation correction phrasing")
    .planning/phases/10-documentation-correction-grpc-build-fix/10-RESEARCH.md (State of the Art section — exact drift documented)
  </read_first>
  <action>
    In `.planning/ROADMAP.md`, edit exactly two lines inside the Phase 2 section and touch nothing else in that section:

    Line 54 (Phase 2, Success Criteria item 2) — replace the current text "Every microservice (auth, wallet, transport, events, stays, marketplace, delivery, ai, admin) deploys as a separate Railway service and auto-deploys on push to main" with: "Proto contracts (`packages/proto/*.proto`) existed for 8 services, but zero live `@GrpcMethod`/`ClientGrpc` wiring was ever implemented — the platform ran (and still runs, pending Phase 17) as a single monolithic `NestFactory.create()` process, not as separate Railway services (claim corrected 2026-07-15 — see Phase 10 for the build-fix work and Phase 17 for the first live extraction)."

    Line 71 (Wave 3 plan list, `02-07-PLAN.md` entry) — replace "gRPC proto definitions (all 8 services) + ts-proto TypeScript generation" with: "gRPC proto definitions authored for 8 services (`packages/proto/*.proto`); the `ts-proto` TypeScript generation pipeline (`generate.sh`) was broken and never produced real generated output — corrected 2026-07-15 in Phase 10 (10-03-PLAN.md fixed the codegen pipeline)". Keep the leading `- [x] 02-07-PLAN.md — ` prefix and the `[x]` checkbox state unchanged (the plan itself did ship proto file authoring — only the "TypeScript generation" half of its claim was false).

    Do NOT edit lines 72-77 (`02-08` through `02-11` checkboxes) — per 10-PATTERNS.md, research confirms those items are not part of DOC-01's concentrated drift surface. Do NOT edit any other phase section (Phase 3 onward) — DOC-01 scopes only Phase 2's gRPC claims.
  </action>
  <verify>
    <automated>grep -n "extracted\|deploys as a separate Railway service" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "deploys as a separate Railway service" .planning/ROADMAP.md` returns zero matches.
    `grep -n "zero live \`@GrpcMethod\`" .planning/ROADMAP.md` matches line 54's corrected text.
    `grep -n "ts-proto.*generation pipeline.*broken" .planning/ROADMAP.md` matches the corrected `02-07-PLAN.md` line.
    `git diff .planning/ROADMAP.md` shows changes confined to the two lines described above (no other line in the file differs from the pre-edit version).
  </acceptance_criteria>
  <done>ROADMAP.md's Phase 2 success criterion 2 and 02-07-PLAN.md description both state the corrected reality; no other line in the file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify PROJECT.md accuracy and run the cross-file DOC-01 audit gate</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    .planning/PROJECT.md (full file — already read this session; contains "already substantially corrected" language at lines 24, 49, 78, 103 per 10-RESEARCH.md)
    .planning/ROADMAP.md (post-Task-1 state, to cross-check no remaining drift after Task 1's edit)
  </read_first>
  <action>
    Run the DOC-01 audit grep across both files: `grep -n "extracted\|deploys as a separate Railway service" .planning/ROADMAP.md .planning/PROJECT.md`. Review every match in context. Confirm PROJECT.md's existing lines (the "⚠ gRPC 'microservice extraction'" line under Validated, the "Key context" paragraph under Current Milestone, the "Current state" bullet under Context, and the "Real gRPC microservice split" row under Key Decisions) each already state: proto contracts existed for 8 services, zero `@GrpcMethod`/`ClientGrpc` wiring, single monolithic `NestFactory.create()` — matching DOC-01's required wording. If any PROJECT.md line still overstates completion (e.g. implies extraction finished rather than "proto-only"), edit that specific line to match the same corrected phrasing pattern used in Task 1. If PROJECT.md requires no edit (expected outcome per research), make no changes to the file.
  </action>
  <verify>
    <automated>grep -n "extracted\|deploys as a separate Railway service" .planning/ROADMAP.md .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    Every match returned by the audit grep, when read in its surrounding sentence, states the corrected reality (proto-only, zero live wiring, monolithic `NestFactory.create()`) rather than a completion claim — confirmed by manual review of each match's context.
    `grep -n "8 services extracted complete" .planning/ROADMAP.md .planning/PROJECT.md` returns zero matches.
  </acceptance_criteria>
  <done>Both ROADMAP.md and PROJECT.md pass the DOC-01 audit grep with every match reviewed and confirmed accurate; PROJECT.md is either left unchanged (already correct) or corrected to match.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | This plan edits only `.planning/` markdown documentation — no code, no runtime configuration, no build pipeline, no request-handling surface is touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | N/A | `.planning/ROADMAP.md`, `.planning/PROJECT.md` | accept | Documentation-only prose correction; introduces zero new attack surface, zero new code path, zero new dependency. No mitigation applicable — thin-but-real analysis per phase scope note, not padded with inapplicable categories. |
</threat_model>

<verification>
Run `grep -n "extracted\|deploys as a separate Railway service" .planning/ROADMAP.md .planning/PROJECT.md` and manually confirm every match states the corrected reality. Confirm `git diff .planning/ROADMAP.md` is confined to the two lines in Task 1's action.
</verification>

<success_criteria>
ROADMAP.md's Phase 2 entry no longer claims "8 services extracted complete"; PROJECT.md confirmed (or corrected) to state the same corrected reality; DOC-01 audit grep passes across both files.
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation-correction-grpc-build-fix/10-01-SUMMARY.md`
</output>
