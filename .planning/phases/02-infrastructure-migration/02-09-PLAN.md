---
phase: 02-infrastructure-migration
plan: "09"
type: execute
wave: 3
depends_on: ["02-08"]
files_modified:
  - backend/apps/events-service/src/main.ts
  - backend/apps/events-service/src/app.module.ts
  - backend/apps/events-service/src/events-grpc.controller.ts
  - backend/apps/events-service/Dockerfile
  - backend/apps/events-service/railway.toml
  - backend/src/app.module.ts
autonomous: true
requirements:
  - INFRA-07
  - INFRA-08

must_haves:
  truths:
    - "events-service runs as a standalone gRPC server on port 5003"
    - "API gateway ClientsModule registers EVENTS_PACKAGE"
    - "All 153 existing tests still pass after creating the events-service app"
  artifacts:
    - path: "backend/apps/events-service/src/events-grpc.controller.ts"
      provides: "gRPC server for EventsService (GetEvent, CheckTicketAvailability, ReserveTicket)"
      contains: "GrpcMethod"
    - path: "backend/apps/events-service/Dockerfile"
      provides: "Standalone Docker image for events-service"
    - path: "backend/apps/events-service/railway.toml"
      provides: "Railway deployment config with watchPaths for events module"
  key_links:
    - from: "backend/src/app.module.ts (API gateway)"
      to: "backend/apps/events-service"
      via: "ClientsModule EVENTS_PACKAGE on railway.internal:5003"
      pattern: "EVENTS_PACKAGE"
---

<objective>
Extract events-service as a standalone gRPC microservice, following the exact same pattern established in Plan 08 for auth-service and wallet-service. Register EVENTS_PACKAGE in the API gateway.

Purpose: Continue the domain service decomposition. events-service reuses existing EventsService business logic via strangler-fig — only the transport layer is new. Stays-service and marketplace-service are extracted in the next plan (02-09b).
Output: events-service gRPC app in backend/apps/; API gateway updated with EVENTS_PACKAGE ClientsModule registration.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Projects/ISEYAA/.planning/PROJECT.md
@C:/Projects/ISEYAA/.planning/phases/02-infrastructure-migration/02-RESEARCH.md
@C:/Projects/ISEYAA/.planning/phases/02-infrastructure-migration/02-PATTERNS.md
@C:/Projects/ISEYAA/.planning/phases/02-infrastructure-migration/02-08-SUMMARY.md
</context>

<interfaces>
<!-- Apply the same pattern established in Plan 08 for events-service -->

Port assignment:
- events-service: 5003

Package name for ClientsModule:
- EVENTS_PACKAGE (package: 'events')

Railway internal URL:
- events-service.railway.internal:5003

Required gRPC methods (from packages/proto/events.proto created in Plan 07):
- EventsService: GetEvent, CheckTicketAvailability, ReserveTicket
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create events-service gRPC app</name>
  <files>
    backend/apps/events-service/src/main.ts,
    backend/apps/events-service/src/app.module.ts,
    backend/apps/events-service/src/events-grpc.controller.ts,
    backend/apps/events-service/Dockerfile,
    backend/apps/events-service/railway.toml
  </files>
  <read_first>
    backend/apps/auth-service/src/main.ts,
    backend/apps/auth-service/src/auth-grpc.controller.ts,
    backend/src/modules/events/events.service.ts,
    packages/proto/events.proto
  </read_first>
  <action>
    Apply the exact same pattern as auth-service (Plan 08 Task 1) to events-service.

    main.ts: NestFactory.createMicroservice with Transport.GRPC, package: 'events', port 5003, protoPath join(__dirname, '../../../../packages/proto/events.proto').

    app.module.ts: imports ConfigModule, PrismaModule, RedisModule, EventsModule (from monolith). Controllers: [EventsGrpcController].

    events-grpc.controller.ts: implements all 3 gRPC methods from events.proto using @GrpcMethod decorator:
    - @GrpcMethod('EventsService', 'GetEvent'): calls EventsService to find event by ID, returns GetEventResponse
    - @GrpcMethod('EventsService', 'CheckTicketAvailability'): checks ticket type capacity, returns TicketAvailabilityResponse
    - @GrpcMethod('EventsService', 'ReserveTicket'): calls the ticket purchase flow with reference, returns ReserveTicketResponse

    Import request/response types from @iseyaa/proto.

    Dockerfile: identical to auth-service Dockerfile; CMD starts events-service dist/main.js.

    railway.toml: watchPaths includes backend/apps/events-service/**, backend/src/modules/events/**, packages/proto/**.
  </action>
  <verify>
    <automated>test -f backend/apps/events-service/src/main.ts &amp;&amp; grep -c "5003" backend/apps/events-service/src/main.ts</automated>
    <automated>grep -c "GrpcMethod" backend/apps/events-service/src/events-grpc.controller.ts</automated>
    <automated>cd backend &amp;&amp; npm run test 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - events-service main.ts uses port 5003
    - events-grpc.controller.ts has 3 @GrpcMethod decorators (GetEvent, CheckTicketAvailability, ReserveTicket)
    - backend/apps/events-service/Dockerfile exists
    - backend/apps/events-service/railway.toml exists with watchPaths
    - All 153 existing tests still pass
    - cd backend && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>events-service gRPC app created following Plan 08 strangler-fig pattern; port 5003</done>
</task>

<task type="auto">
  <name>Task 2: Register EVENTS_PACKAGE in API gateway + add .env.example entry</name>
  <files>
    backend/src/app.module.ts,
    .env.example
  </files>
  <read_first>
    backend/src/app.module.ts,
    .env.example
  </read_first>
  <action>
    FILE 1: backend/src/app.module.ts

    Read the current app.module.ts (which already has AUTH_PACKAGE and WALLET_PACKAGE from Plan 08). Add 1 more ClientsModule registration to the same ClientsModule.register([...]) array:

      {
        name: 'EVENTS_PACKAGE',
        transport: Transport.GRPC,
        options: {
          package: 'events',
          protoPath: join(__dirname, '../../../packages/proto/events.proto'),
          url: process.env.EVENTS_SERVICE_URL || 'events-service.railway.internal:5003',
        },
      },

    FILE 2: .env.example

    Add if not already present:
      EVENTS_SERVICE_URL=events-service.railway.internal:5003
  </action>
  <verify>
    <automated>grep -c "EVENTS_PACKAGE" backend/src/app.module.ts</automated>
    <automated>grep -c "EVENTS_SERVICE_URL" .env.example</automated>
    <automated>cd backend &amp;&amp; npm run test 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/app.module.ts has EVENTS_PACKAGE registered (grep shows 1+ matches)
    - .env.example has EVENTS_SERVICE_URL entry
    - All 153 existing tests still pass
    - cd backend && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>EVENTS_PACKAGE registered in API gateway ClientsModule; events-service URL in .env.example</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API gateway → events-service | gRPC calls on Railway private network for event and ticket operations |
| events-service ReserveTicket | Ticket purchase triggers payment; SELECT FOR UPDATE must be preserved in EventsService |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-34 | Tampering | events-service ReserveTicket race condition | mitigate | EventsService.reserveTicket() already uses prisma.$transaction; gRPC controller delegates to the same method |
| T-02-36 | Information Disclosure | Event details exposed via gRPC without auth check | mitigate | GetEvent is read-only and returns only public fields; gRPC metadata carries userId for mutation methods |
</threat_model>

<verification>
After both tasks:

  cd backend && npm run test

Expected: 153+ tests passing, 0 failures.

Verify events-service app:

  ls backend/apps/events-service/src/

Expected: main.ts, app.module.ts, events-grpc.controller.ts

Verify gateway registration:

  grep "EVENTS_PACKAGE" backend/src/app.module.ts

Expected: 1+ match
</verification>

<success_criteria>
- events-service gRPC microservice app created on port 5003
- API gateway registers EVENTS_PACKAGE in ClientsModule
- Strangler-fig: events-grpc.controller.ts delegates to existing EventsService business logic
- 153+ tests pass
</success_criteria>

<output>
After completion, create C:/Projects/ISEYAA/.planning/phases/02-infrastructure-migration/02-09-SUMMARY.md
</output>
