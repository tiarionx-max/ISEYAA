---
phase: 02-infrastructure-migration
plan: "11"
type: execute
wave: 3
depends_on: ["02-10b"]
files_modified:
  - backend/src/kafka/kafka.service.ts
  - backend/src/kafka/kafka.module.ts
  - backend/src/kafka/__tests__/kafka.service.spec.ts
  - backend/src/modules/webhooks/webhooks.service.ts
  - backend/src/modules/wallet/wallet.service.ts
  - backend/src/modules/events/events.service.ts
  - backend/src/modules/stays/stays.service.ts
  - backend/src/modules/marketplace/marketplace.service.ts
  - backend/src/app.module.ts
autonomous: true
requirements:
  - INFRA-09

must_haves:
  truths:
    - "WebhooksService publishes charge.success, escrow.released, order.delivered events to Upstash Kafka topics"
    - "WalletService, EventsService, StaysService, MarketplaceService each consume from their respective Kafka topics"
    - "Kafka consumer group IDs are service-scoped (wallet-service-prod, events-service-prod, etc.)"
    - "KafkaService.emit() is a dual-write: publishes to Kafka AND still calls eventEmitter.emit() during transition"
    - "All 153 existing tests still pass (EventEmitter2 retained until Kafka consumers confirmed working)"
    - "@OnEvent() decorator usages removed from stays.service.ts, marketplace.service.ts, studio.service.ts, events.service.ts AFTER Kafka consumers are verified working"
  artifacts:
    - path: "backend/src/kafka/kafka.service.ts"
      provides: "KafkaJS service with Upstash SASL/SCRAM-SHA-256 auth; produce and consume methods"
      contains: "scram-sha-256"
    - path: "backend/src/kafka/kafka.module.ts"
      provides: "@Global() NestJS module exporting KafkaService"
    - path: "backend/src/kafka/__tests__/kafka.service.spec.ts"
      provides: "Unit tests for KafkaService with mocked kafkajs"
  key_links:
    - from: "backend/src/modules/webhooks/webhooks.service.ts"
      to: "Upstash Kafka topic payment.charge_success"
      via: "KafkaService.emit('payment.charge_success', payload)"
      pattern: "kafka.*emit.*charge_success"
    - from: "backend/src/modules/wallet/wallet.service.ts"
      to: "Upstash Kafka topic payment.charge_success"
      via: "KafkaService.consume('payment.charge_success', 'wallet-service-prod', handler)"
      pattern: "kafka.*consume.*wallet"

user_setup:
  - service: upstash-kafka
    why: "Durable event bus for cross-service payment events"
    env_vars:
      - name: KAFKA_BROKER_URL
        source: "Upstash Console → Kafka → Cluster → Details → Bootstrap Server (e.g. xxx.upstash.io:9092)"
      - name: KAFKA_USERNAME
        source: "Upstash Console → Kafka → Cluster → Details → Username"
      - name: KAFKA_PASSWORD
        source: "Upstash Console → Kafka → Cluster → Details → Password"
    dashboard_config:
      - task: "Create Kafka cluster named iseyaa-kafka in us-east-1 region"
        location: "console.upstash.com → Kafka → Create Cluster"
      - task: "Create topics: payment.charge_success, payment.escrow_released, payment.order_delivered (partition count: 1, retention: 7 days)"
        location: "Kafka Cluster → Topics → Create Topic"
---

<objective>
Replace EventEmitter2 with Upstash Kafka for cross-service payment events using a two-step approach: (1) add Kafka with dual-write alongside EventEmitter2, (2) verify Kafka consumers work, then (3) remove @OnEvent() handler usages. This completes INFRA-09.

Purpose: Payment events (charge.success, escrow.released, order.delivered) need a durable bus after microservices decomposition. EventEmitter2 is in-process only and cannot cross service boundaries. The two-step approach prevents event loss during migration.
Output: KafkaService with SASL/SCRAM-SHA-256 auth; KafkaModule; WebhooksService publishing to Kafka topics; feature services consuming from Kafka; @OnEvent() usages removed after verification; unit tests for KafkaService.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Projects/ISEYAA/.planning/PROJECT.md
@C:/Projects/ISEYAA/.planning/phases/02-infrastructure-migration/02-RESEARCH.md
@C:/Projects/ISEYAA/.planning/phases/02-infrastructure-migration/02-PATTERNS.md
</context>

<interfaces>
<!-- PATTERNS.md: KafkaService patterns (lines 357-435) -->

KafkaService constructor (SASL/SCRAM-SHA-256 for Upstash):
```typescript
this.kafka = new Kafka({
  brokers: [config.get<string>('KAFKA_BROKER_URL', '')],
  sasl: {
    mechanism: 'scram-sha-256',
    username: config.get<string>('KAFKA_USERNAME', ''),
    password: config.get<string>('KAFKA_PASSWORD', ''),
  },
  ssl: true,
});
```

Producer emit method (from PATTERNS.md lines 399-413):
```typescript
async emit(topic: string, payload: Record<string, unknown>): Promise<void> {
  await this.producer.send({ topic, messages: [{ value: JSON.stringify(payload) }] });
}
```

Consumer method (from PATTERNS.md lines 415-435):
```typescript
async consume(topic: string, groupId: string, handler: (payload) => Promise<void>): Promise<void> {
  const consumer = this.kafka.consumer({ groupId });
  await consumer.connect();
  await consumer.subscribe({ topic });
  await consumer.run({ eachMessage: async ({ message }) => {
    const payload = JSON.parse(message.value.toString());
    await handler(payload);
  }});
}
```

Kafka topics:
- payment.charge_success — published by WebhooksService on wallet top-up and ticket/stay/order payments
- payment.escrow_released — published by StaysService cron when escrow releases
- payment.order_delivered — published by MarketplaceService when order is confirmed

Consumer group IDs (service-scoped to prevent cross-service message theft):
- wallet-service-prod (consumes payment.charge_success for top-up events)
- events-service-prod (consumes payment.charge_success for ticket purchase)
- stays-service-prod (consumes payment.charge_success for booking + payment.escrow_released)
- marketplace-service-prod (consumes payment.charge_success for order payment + payment.order_delivered)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KafkaService + KafkaModule + unit tests</name>
  <files>
    backend/src/kafka/kafka.service.ts,
    backend/src/kafka/kafka.module.ts,
    backend/src/kafka/__tests__/kafka.service.spec.ts,
    backend/src/app.module.ts
  </files>
  <read_first>
    backend/src/redis/redis.service.ts,
    backend/src/redis/redis.module.ts,
    backend/src/modules/auth/__tests__/auth.service.spec.ts
  </read_first>
  <behavior>
    - Test 1: KafkaService constructor creates Kafka instance with SASL mechanism 'scram-sha-256' and ssl: true
    - Test 2: KafkaService.onModuleInit() calls producer.connect()
    - Test 3: KafkaService.emit(topic, payload) calls producer.send() with correct topic and JSON-serialized payload
    - Test 4: KafkaService.emit() logs error and rethrows on producer.send() failure
    - Test 5: KafkaService.consume(topic, groupId, handler) connects consumer, subscribes to topic, and runs handler on each message
    - Test 6: KafkaService.consume() catches handler errors, logs them, and does not crash the consumer loop
  </behavior>
  <action>
    STEP 1 — Install kafkajs:

      cd backend && npm install kafkajs

    PART A — Create backend/src/kafka/__tests__/kafka.service.spec.ts (RED phase).

    Mock kafkajs at module level:
      jest.mock('kafkajs', () => ({
        Kafka: jest.fn().mockImplementation(() => ({
          producer: jest.fn().mockReturnValue({
            connect: jest.fn().mockResolvedValue(undefined),
            disconnect: jest.fn().mockResolvedValue(undefined),
            send: jest.fn().mockResolvedValue(undefined),
          }),
          consumer: jest.fn().mockReturnValue({
            connect: jest.fn().mockResolvedValue(undefined),
            subscribe: jest.fn().mockResolvedValue(undefined),
            run: jest.fn().mockResolvedValue(undefined),
          }),
        })),
      }));

    Write 6 tests per behavior list.

    PART B — Create backend/src/kafka/kafka.service.ts per PATTERNS.md (lines 357-435).

    Class KafkaService implements OnModuleInit, OnModuleDestroy:
    - private readonly kafka: Kafka
    - private readonly producer: Producer
    - Constructor: creates Kafka with SASL/SCRAM-SHA-256 config, creates producer instance (not connected yet)
    - onModuleInit(): await producer.connect()
    - onModuleDestroy(): await producer.disconnect()
    - emit(topic, payload): sends to Kafka; logs error and rethrows on failure (Pattern A from PATTERNS.md)
    - consume(topic, groupId, handler): creates consumer, connects, subscribes, runs — handler errors caught and logged (Pattern B — does not rethrow to avoid killing consumer)

    PART C — Create backend/src/kafka/kafka.module.ts:
    @Global() @Module({ providers: [KafkaService], exports: [KafkaService] }) export class KafkaModule {}

    PART D — Add KafkaModule to backend/src/app.module.ts imports array.

    When KAFKA_BROKER_URL is empty (local dev without Upstash), KafkaService constructor should not throw — only log a warning. The connection failure (if any) happens at onModuleInit() time.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npm run test -- --testPathPattern=kafka.service --passWithNoTests 2>&amp;1 | tail -20</automated>
    <automated>grep -c "scram-sha-256" backend/src/kafka/kafka.service.ts</automated>
    <automated>grep -c "KafkaModule" backend/src/app.module.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 6 KafkaService tests pass
    - kafka.service.ts contains scram-sha-256 (literal string, not a variable)
    - kafka.service.ts ssl: true in Kafka constructor
    - app.module.ts imports KafkaModule
    - cd backend && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>KafkaService implemented with Upstash SASL/SCRAM-SHA-256; 6 tests passing; KafkaModule registered globally</done>
</task>

<task type="auto">
  <name>Task 2: Wire Kafka producers into WebhooksService + consumers into feature services (dual-write)</name>
  <files>
    backend/src/modules/webhooks/webhooks.service.ts,
    backend/src/modules/wallet/wallet.service.ts,
    backend/src/modules/events/events.service.ts,
    backend/src/modules/stays/stays.service.ts,
    backend/src/modules/marketplace/marketplace.service.ts
  </files>
  <read_first>
    backend/src/modules/webhooks/webhooks.service.ts,
    backend/src/modules/wallet/wallet.service.ts,
    backend/src/modules/events/events.service.ts,
    backend/src/modules/stays/stays.service.ts,
    backend/src/modules/marketplace/marketplace.service.ts
  </read_first>
  <action>
    STEP 1 — Update WebhooksService (dual-write pattern — add Kafka alongside existing EventEmitter2).

    Read webhooks.service.ts fully. In the constructor, inject KafkaService: constructor(...existing..., private readonly kafkaService: KafkaService).

    After EACH eventEmitter.emit() call in handlePaystack(), add a parallel kafkaService.emit() call with the matching topic:

    - After eventEmitter.emit('payment.ticket_purchase', ...) add: await this.kafkaService.emit('payment.charge_success', { type: 'ticket_purchase', reference, metadata, amount }).catch(err => this.logger.error('Kafka emit failed', err))
    - After eventEmitter.emit('payment.stay_booking', ...) add: await this.kafkaService.emit('payment.charge_success', { type: 'stay_booking', reference, metadata, amount }).catch(err => this.logger.error('Kafka emit failed', err))
    - After eventEmitter.emit('payment.order_payment', ...) add: await this.kafkaService.emit('payment.charge_success', { type: 'order_payment', reference, metadata, amount }).catch(err => this.logger.error('Kafka emit failed', err))
    - For wallet top-up (the direct WalletService call): add Kafka emit after the existing credit call: await this.kafkaService.emit('payment.charge_success', { type: 'wallet_topup', reference, amount }).catch(...)

    CRITICAL: Use .catch() on Kafka emit calls — a Kafka failure must NOT block the existing EventEmitter2 path. The dual-write allows gradual migration; either path being broken doesn't fail the webhook handler.

    STEP 2 — Add Kafka consumers to feature services via onModuleInit.

    For each service, add an onModuleInit() method (or add to existing if present) that subscribes to the relevant Kafka topic:

    WalletService: inject KafkaService; in onModuleInit(): call this.kafkaService.consume('payment.charge_success', 'wallet-service-prod', async (payload) => { if (payload.type === 'wallet_topup') { await this.handleWalletTopup(payload); } })

    EventsService: inject KafkaService; subscribe to 'payment.charge_success' with group 'events-service-prod'; if payload.type === 'ticket_purchase': call handleTicketPayment(payload).

    StaysService: inject KafkaService; subscribe to 'payment.charge_success' with group 'stays-service-prod'; if payload.type === 'stay_booking': call handleBookingPayment(payload). Also subscribe to 'payment.escrow_released' with same group.

    MarketplaceService: inject KafkaService; subscribe to 'payment.charge_success' with group 'marketplace-service-prod'; if payload.type === 'order_payment': call handleOrderPayment(payload).

    IMPORTANT: Do NOT remove the existing @OnEvent() handlers in this task. The dual-path (EventEmitter2 + Kafka) means both the @OnEvent handler and the Kafka consumer will process the same event during the transition period. This is intentional — it allows validation that Kafka is working before removing EventEmitter2. @OnEvent handlers are removed in Task 3 after Kafka consumers are confirmed working in this task.

    The Kafka consume() call in onModuleInit() should be wrapped in a try/catch: if KafkaService is not configured (KAFKA_BROKER_URL empty), log a warning and skip — do not crash the module.
  </action>
  <verify>
    <automated>grep -c "kafkaService.emit\|kafkaService\.consume" backend/src/modules/webhooks/webhooks.service.ts</automated>
    <automated>grep -rn "kafka.*consume\|kafkaService" backend/src/modules/wallet/wallet.service.ts backend/src/modules/events/events.service.ts backend/src/modules/stays/stays.service.ts backend/src/modules/marketplace/marketplace.service.ts | wc -l</automated>
    <automated>cd backend &amp;&amp; npm run test 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - webhooks.service.ts has kafkaService.emit() calls after each eventEmitter.emit() call (grep shows 4+ matches)
    - Each Kafka emit uses .catch() to prevent blocking the EventEmitter2 path
    - wallet.service.ts, events.service.ts, stays.service.ts, marketplace.service.ts each have kafkaService.consume() in onModuleInit
    - @OnEvent() decorators are NOT removed yet (dual-write maintained — Task 3 removes them)
    - Consumer group IDs are service-scoped: wallet-service-prod, events-service-prod, stays-service-prod, marketplace-service-prod
    - All 153 existing tests still pass (Kafka consume is in onModuleInit; test module doesn't trigger it)
    - cd backend && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Dual-write Kafka + EventEmitter2 active; payment events flow through both paths; @OnEvent handlers still present — removal in Task 3</done>
</task>

<task type="auto">
  <name>Task 3: Remove @OnEvent() handler usages from all feature services after Kafka verification</name>
  <files>
    backend/src/modules/stays/stays.service.ts,
    backend/src/modules/marketplace/marketplace.service.ts,
    backend/src/modules/events/events.service.ts,
    backend/src/modules/webhooks/webhooks.service.ts
  </files>
  <read_first>
    backend/src/modules/stays/stays.service.ts,
    backend/src/modules/marketplace/marketplace.service.ts,
    backend/src/modules/events/events.service.ts,
    backend/src/modules/webhooks/webhooks.service.ts
  </read_first>
  <action>
    This task runs AFTER Task 2 — the Kafka consumers in onModuleInit() must be in place before removing EventEmitter2 handlers.

    For each of the following files, read the file fully and remove @OnEvent() decorator usages:

    FILES TO MODIFY:
    - backend/src/modules/stays/stays.service.ts
    - backend/src/modules/marketplace/marketplace.service.ts
    - backend/src/modules/events/events.service.ts
    - backend/src/modules/webhooks/webhooks.service.ts

    IF a studio.service.ts file exists at backend/src/modules/studio/studio.service.ts and has @OnEvent() decorators, include it in scope — read the file and remove @OnEvent() usages there too.

    WHAT TO REMOVE:
    - Remove the @OnEvent('event.name') decorator lines above each handler method
    - Remove the OnEvent import from @nestjs/event-emitter in files where it was the only @OnEvent usage

    WHAT TO KEEP:
    - Keep the EventEmitterModule registration in app.module.ts (leave it as a no-op — removing it would require verifying no other code depends on EventEmitter2, which is out of scope for this task)
    - Keep the EventEmitter2 service injection in WebhooksService (the eventEmitter.emit() dual-write calls remain — WebhooksService still publishes to EventEmitter2 alongside Kafka)
    - Keep the @OnEvent handler method bodies — convert them to regular private methods that the Kafka consumer handler calls directly (the logic inside the methods is still needed; just the @OnEvent decorator triggering is removed)

    HOW TO HANDLE EACH FILE:
    1. Read the file to identify all @OnEvent() decorator occurrences
    2. For each @OnEvent('some.event') above a method: remove the decorator line only
    3. Remove the OnEvent import from @nestjs/event-emitter if no @OnEvent decorators remain in the file
    4. Leave the method body intact — the Kafka consumer callback (from Task 2) calls these methods directly

    VERIFICATION REQUIREMENT: After removing decorators, confirm that the Kafka consumer callbacks added in Task 2 call the same handler logic (either by calling the method directly or by inlining the logic). The event processing path must remain functional.

    SAFETY: Do NOT remove the NestJS EventEmitterModule registration from app.module.ts. This will be cleaned up in Phase 6 after a full production validation confirms no other code paths depend on EventEmitter2.
  </action>
  <verify>
    <automated>grep -rn "@OnEvent" backend/src/modules/stays/stays.service.ts backend/src/modules/marketplace/marketplace.service.ts backend/src/modules/events/events.service.ts backend/src/modules/webhooks/webhooks.service.ts 2>/dev/null | wc -l</automated>
    <automated>cd backend &amp;&amp; npm run test 2>&amp;1 | tail -5</automated>
    <automated>cd backend &amp;&amp; npx tsc --noEmit 2>&amp;1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep for @OnEvent in stays.service.ts, marketplace.service.ts, events.service.ts, webhooks.service.ts returns 0 matches
    - EventEmitterModule registration in app.module.ts is preserved (not removed)
    - eventEmitter.emit() calls in WebhooksService dual-write are preserved (still publishing to EventEmitter2)
    - All 153 existing tests still pass
    - cd backend && npx tsc --noEmit exits 0
    - The Kafka consumer handlers in onModuleInit() call the same business logic previously triggered by @OnEvent (verify by code inspection)
  </acceptance_criteria>
  <done>@OnEvent() decorator usages removed from all feature services; Kafka consumers are now the sole trigger for payment event processing; EventEmitterModule registration kept as no-op for safety; INFRA-09 complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Upstash Kafka | Kafka producer/consumer over TCP with SASL/SCRAM-SHA-256 + TLS |
| Kafka message bus | Payment event payloads cross service boundaries via Kafka topics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-41 | Tampering | Kafka message payload tampered in transit | mitigate | SASL/SCRAM-SHA-256 + SSL encrypts messages in transit; Upstash Kafka authenticates producer |
| T-02-42 | Denial of Service | Kafka consumer group ID conflict between services | mitigate | Service-scoped group IDs (wallet-service-prod, etc.) prevent cross-service message theft; documented in code comments |
| T-02-43 | Information Disclosure | Payment amount in Kafka payload | accept | Kafka is internal (not public); amount is required for wallet credit; Upstash encrypts at rest |
| T-02-44 | Repudiation | Handler methods called by Kafka consumers after @OnEvent removed | mitigate | Task 3 preserves handler method bodies; Kafka consumers call same logic directly; dual event processing is eliminated cleanly |
| T-02-45 | Denial of Service | Kafka emit failure blocks webhook handler | mitigate | Kafka emit uses .catch() — failure logs error but does not throw; EventEmitter2 dual-write path continues unchanged |
</threat_model>

<verification>
After all three tasks:

  cd backend && npm run test

Expected: 153+ tests, 0 failures.

Verify dual-write in webhooks:

  grep "kafkaService.emit" backend/src/modules/webhooks/webhooks.service.ts | wc -l

Expected: 4+ (one per payment type).

Verify @OnEvent removal:

  grep -rn "@OnEvent" backend/src/modules/

Expected: 0 matches (all @OnEvent usages removed from feature services).

Verify Kafka consumers still present:

  grep -r "kafka.*consume\|kafkaService.consume" backend/src/modules/ | wc -l

Expected: 4+ (one per feature service).

Verify EventEmitterModule preserved:

  grep "EventEmitterModule" backend/src/app.module.ts

Expected: 1 match (kept as no-op for safety).
</verification>

<success_criteria>
- KafkaService with SASL/SCRAM-SHA-256 and SSL for Upstash
- WebhooksService publishes to Kafka topics alongside EventEmitter2 (dual-write preserved for WebhooksService emit calls)
- Feature services consume from Kafka with service-scoped group IDs
- @OnEvent() decorator usages removed from stays.service.ts, marketplace.service.ts, events.service.ts, webhooks.service.ts (and studio.service.ts if applicable)
- EventEmitterModule registration in app.module.ts kept (no-op, cleaned up in Phase 6)
- 6 KafkaService unit tests passing
- 153+ total tests passing
- INFRA-09 complete: Upstash Kafka is the sole consumer-trigger for cross-service payment events; @OnEvent handlers eliminated from feature services
</success_criteria>

<output>
After completion, create C:/Projects/ISEYAA/.planning/phases/02-infrastructure-migration/02-11-SUMMARY.md
</output>
