---
phase: 02-infrastructure-migration
plan: "07"
type: execute
wave: 3
depends_on: ["02-06"]
files_modified:
  - packages/proto/auth.proto
  - packages/proto/wallet.proto
  - packages/proto/events.proto
  - packages/proto/stays.proto
  - packages/proto/marketplace.proto
  - packages/proto/admin.proto
  - packages/proto/ai.proto
  - packages/proto/notifications.proto
  - packages/proto/package.json
autonomous: true
requirements:
  - INFRA-08

must_haves:
  truths:
    - "proto files exist for all 8 service domains (auth, wallet, events, stays, marketplace, admin, ai, notifications)"
    - "ts-proto generates TypeScript types from all proto files without errors"
    - "Generated types follow NestJS gRPC conventions (nestJs=true flag)"
    - "packages/proto/ is a valid npm workspace package importable from backend/"
  artifacts:
    - path: "packages/proto/auth.proto"
      provides: "gRPC service definition for AuthService (ValidateToken, GetUser)"
      contains: "service AuthService"
    - path: "packages/proto/wallet.proto"
      provides: "gRPC service definition for WalletService (Credit, Debit, GetBalance)"
      contains: "service WalletService"
    - path: "packages/proto/package.json"
      provides: "npm workspace package definition for proto types"
      contains: "@iseyaa/proto"
  key_links:
    - from: "packages/proto/*.proto"
      to: "packages/proto/generated/*.ts"
      via: "ts-proto codegen via protoc"
      pattern: "proto"
---

<objective>
Create the proto file definitions for all 8 microservice domains and generate TypeScript types using ts-proto. This is the contract layer for Wave 3 service decomposition — all subsequent gRPC communication between services is typed against these proto definitions.

Purpose: Define the gRPC API contracts before extracting microservices. Interface-first ordering prevents contract drift between the gateway and individual services.
Output: 8 .proto files in packages/proto/; generated TypeScript types in packages/proto/generated/; packages/proto npm workspace package.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Projects/ISEYAA/.planning/PROJECT.md
@C:/Projects/ISEYAA/.planning/phases/02-infrastructure-migration/02-RESEARCH.md
@C:/Projects/ISEYAA/.planning/phases/02-infrastructure-migration/02-PATTERNS.md
</context>

<interfaces>
<!-- Proto structure from RESEARCH.md Pattern 8 and PATTERNS.md lines 462-519 -->

auth.proto template:
```protobuf
syntax = "proto3";
package auth;

service AuthService {
  rpc ValidateToken (ValidateTokenRequest) returns (ValidateTokenResponse);
  rpc GetUser (GetUserRequest) returns (GetUserResponse);
}

message ValidateTokenRequest { string token = 1; }
message ValidateTokenResponse { bool valid = 1; string user_id = 2; string role = 3; }
message GetUserRequest { string user_id = 1; }
message GetUserResponse { string id = 1; string email = 2; string role = 3; string status = 4; }
```

wallet.proto template (from RESEARCH.md Pattern 8):
```protobuf
syntax = "proto3";
package wallet;

service WalletService {
  rpc Credit (CreditRequest) returns (CreditResponse);
  rpc Debit (DebitRequest) returns (DebitResponse);
  rpc GetBalance (BalanceRequest) returns (BalanceResponse);
}

message CreditRequest { string wallet_id = 1; double amount = 2; string reference = 3; string description = 4; }
message CreditResponse { bool success = 1; double new_balance = 2; }
message DebitRequest { string wallet_id = 1; double amount = 2; string reference = 3; string description = 4; }
message DebitResponse { bool success = 1; double new_balance = 2; }
message BalanceRequest { string wallet_id = 1; }
message BalanceResponse { double balance = 1; double escrow_balance = 2; string kyc_tier = 3; }
```

ts-proto codegen command:
```bash
npx ts-proto \
  --plugin=./node_modules/.bin/protoc-gen-ts_proto \
  --ts_proto_out=packages/proto/generated \
  --ts_proto_opt=nestJs=true \
  --ts_proto_opt=outputServices=grpc-js \
  packages/proto/*.proto
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install ts-proto and write all 8 proto service definitions</name>
  <files>
    packages/proto/auth.proto,
    packages/proto/wallet.proto,
    packages/proto/events.proto,
    packages/proto/stays.proto,
    packages/proto/marketplace.proto,
    packages/proto/admin.proto,
    packages/proto/ai.proto,
    packages/proto/notifications.proto
  </files>
  <read_first>
    backend/src/modules/auth/auth.service.ts,
    backend/src/modules/wallet/wallet.service.ts,
    backend/src/modules/events/events.service.ts,
    backend/src/prisma/schema.prisma
  </read_first>
  <action>
    STEP 1 — Install proto packages in backend:

      cd backend
      npm install @nestjs/microservices@11.1.19 @grpc/grpc-js@1.14.3 @grpc/proto-loader@0.8.1 ts-proto@2.11.8

    STEP 2 — Create packages/proto/ directory if it doesn't exist.

    STEP 3 — Write all 8 proto files. Each file follows the pattern: syntax = "proto3"; package <name>; with a single service and relevant message types.

    packages/proto/auth.proto — AuthService:
    - rpc ValidateToken(ValidateTokenRequest) returns (ValidateTokenResponse)
    - rpc GetUser(GetUserRequest) returns (GetUserResponse)
    - rpc RefreshToken(RefreshTokenRequest) returns (RefreshTokenResponse)
    Messages: ValidateTokenRequest(token:string), ValidateTokenResponse(valid:bool, user_id:string, role:string), GetUserRequest(user_id:string), GetUserResponse(id:string, email:string, phone:string, role:string, status:string), RefreshTokenRequest(refresh_token:string), RefreshTokenResponse(access_token:string, refresh_token:string)

    packages/proto/wallet.proto — WalletService:
    - rpc Credit(CreditRequest) returns (CreditResponse)
    - rpc Debit(DebitRequest) returns (DebitResponse)
    - rpc GetBalance(BalanceRequest) returns (BalanceResponse)
    - rpc GetTransactions(GetTransactionsRequest) returns (GetTransactionsResponse)
    Messages per RESEARCH.md Pattern 8 (interfaces block above) plus: GetTransactionsRequest(wallet_id:string, cursor:string, limit:int32), GetTransactionsResponse with repeated Transaction message

    packages/proto/events.proto — EventsService:
    - rpc GetEvent(GetEventRequest) returns (GetEventResponse)
    - rpc CheckTicketAvailability(TicketAvailabilityRequest) returns (TicketAvailabilityResponse)
    - rpc ReserveTicket(ReserveTicketRequest) returns (ReserveTicketResponse)
    Messages: GetEventRequest(event_id:string), GetEventResponse(id:string, title:string, status:string, available_capacity:int32), TicketAvailabilityRequest(event_id:string, ticket_type_id:string, quantity:int32), TicketAvailabilityResponse(available:bool, remaining:int32), ReserveTicketRequest(event_id:string, ticket_type_id:string, user_id:string, quantity:int32, reference:string), ReserveTicketResponse(success:bool, ticket_id:string)

    packages/proto/stays.proto — StaysService:
    - rpc GetProperty(GetPropertyRequest) returns (GetPropertyResponse)
    - rpc CheckAvailability(AvailabilityRequest) returns (AvailabilityResponse)
    - rpc CreateBooking(CreateBookingRequest) returns (CreateBookingResponse)
    Messages: GetPropertyRequest(property_id:string), GetPropertyResponse(id:string, name:string, price_per_night:double, lga_id:string), AvailabilityRequest(property_id:string, check_in:string, check_out:string), AvailabilityResponse(available:bool), CreateBookingRequest(property_id:string, user_id:string, check_in:string, check_out:string, reference:string), CreateBookingResponse(success:bool, booking_id:string)

    packages/proto/marketplace.proto — MarketplaceService:
    - rpc GetProduct(GetProductRequest) returns (GetProductResponse)
    - rpc ReserveStock(ReserveStockRequest) returns (ReserveStockResponse)
    - rpc ConfirmOrder(ConfirmOrderRequest) returns (ConfirmOrderResponse)
    Messages: GetProductRequest(product_id:string), GetProductResponse(id:string, name:string, price:double, stock:int32), ReserveStockRequest(product_id:string, quantity:int32, order_id:string), ReserveStockResponse(success:bool, reserved_quantity:int32), ConfirmOrderRequest(order_id:string, reference:string), ConfirmOrderResponse(success:bool)

    packages/proto/admin.proto — AdminService:
    - rpc GetDashboard(GetDashboardRequest) returns (GetDashboardResponse)
    - rpc ApproveItem(ApproveItemRequest) returns (ApproveItemResponse)
    Messages: GetDashboardRequest(empty — use google.protobuf.Empty or just an empty message), GetDashboardResponse(total_users:int32, total_revenue:double, active_events:int32, pending_approvals:int32), ApproveItemRequest(item_type:string, item_id:string, admin_id:string), ApproveItemResponse(success:bool)

    packages/proto/ai.proto — AiService:
    - rpc GetItinerary(ItineraryRequest) returns (ItineraryResponse)
    - rpc GetLgaIntelligence(LgaIntelligenceRequest) returns (LgaIntelligenceResponse)
    Messages: ItineraryRequest(lga_slug:string, duration_days:int32, interests:string, budget_ngn:double), ItineraryResponse(content:string), LgaIntelligenceRequest(lga_id:string), LgaIntelligenceResponse(content:string)

    packages/proto/notifications.proto — NotificationsService:
    - rpc SendPush(SendPushRequest) returns (SendPushResponse)
    - rpc RegisterToken(RegisterTokenRequest) returns (RegisterTokenResponse)
    Messages: SendPushRequest(user_id:string, title:string, body:string), SendPushResponse(success:bool), RegisterTokenRequest(user_id:string, fcm_token:string), RegisterTokenResponse(success:bool)

    All proto files must use syntax = "proto3" at the top. Field numbers start at 1 and increment without gaps within each message. Do not use reserved field numbers (1-15 for frequently-used fields are efficient in protobuf wire format — use them first).
  </action>
  <verify>
    <automated>test -f packages/proto/auth.proto &amp;&amp; test -f packages/proto/wallet.proto &amp;&amp; test -f packages/proto/events.proto</automated>
    <automated>grep -l "service.*Service" packages/proto/*.proto | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 8 proto files exist in packages/proto/
    - Each proto file has exactly one service definition
    - grep "syntax = \"proto3\"" packages/proto/*.proto returns 8 matches
    - Each proto file has at least 2 rpc methods
    - No duplicate field numbers within any message type
  </acceptance_criteria>
  <done>8 proto service definitions written covering all Sprint 1 + Phase 2 domain services</done>
</task>

<task type="auto">
  <name>Task 2: Generate TypeScript types from proto files + create npm workspace package</name>
  <files>
    packages/proto/package.json,
    packages/proto/generate.sh
  </files>
  <read_first>
    package.json
  </read_first>
  <action>
    STEP 1 — Create packages/proto/package.json:

      {
        "name": "@iseyaa/proto",
        "version": "0.1.0",
        "description": "gRPC proto types for ISEYAA microservices",
        "main": "generated/index.js",
        "types": "generated/index.d.ts",
        "scripts": {
          "generate": "bash generate.sh"
        }
      }

    STEP 2 — Add @iseyaa/proto to the root package.json workspaces array (currently: ["backend","web","mobile","shared"] — add "packages/proto").

    STEP 3 — Create packages/proto/generate.sh:

      #!/bin/bash
      # Generate TypeScript types from all proto files using ts-proto
      # Run from packages/proto/ or monorepo root: bash packages/proto/generate.sh

      mkdir -p packages/proto/generated

      # ts-proto generates NestJS-compatible gRPC service interfaces
      npx ts-proto \
        --plugin=./backend/node_modules/.bin/protoc-gen-ts_proto \
        --ts_proto_out=./packages/proto/generated \
        --ts_proto_opt=nestJs=true \
        --ts_proto_opt=outputServices=grpc-js \
        --ts_proto_opt=esModuleInterop=true \
        ./packages/proto/*.proto

      echo "Proto generation complete. Generated files:"
      ls ./packages/proto/generated/

    STEP 4 — Run the generation script from the monorepo root:

      bash packages/proto/generate.sh

    This generates one .ts file per proto file in packages/proto/generated/. If ts-proto is not installed in backend/node_modules (because Plan 07 Task 1 ran in a previous session), install it first:

      cd backend && npm install ts-proto@2.11.8

    STEP 5 — Create packages/proto/generated/index.ts that re-exports all generated modules:

      export * from './auth';
      export * from './wallet';
      export * from './events';
      export * from './stays';
      export * from './marketplace';
      export * from './admin';
      export * from './ai';
      export * from './notifications';

    STEP 6 — Run npm install from monorepo root to register the @iseyaa/proto workspace:

      npm install

    This makes @iseyaa/proto importable from backend/ and other workspaces.

    Note on protoc: ts-proto includes its own protoc binary via the protoc-gen-ts_proto plugin. You do NOT need system protoc installed — ts-proto uses its bundled protoc. If the generation fails with "protoc not found", install via: npm install -g ts-proto (global) or use the local binary path.
  </action>
  <verify>
    <automated>test -f packages/proto/generated/auth.ts &amp;&amp; grep -c "AuthService" packages/proto/generated/auth.ts</automated>
    <automated>grep -c "@iseyaa/proto" package.json</automated>
    <automated>test -f packages/proto/generated/index.ts &amp;&amp; grep -c "export" packages/proto/generated/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/proto/generated/ directory exists with 8+ .ts files (one per proto)
    - generated/auth.ts contains AuthService interface (grep shows 1+ match)
    - generated/index.ts re-exports all 8 modules
    - packages/proto/package.json has name @iseyaa/proto
    - root package.json workspaces includes packages/proto
    - npm install completes without errors (workspace is valid)
    - ts-proto generated files compile cleanly: cd packages/proto && npx tsc --noEmit (if tsconfig exists) or just verify generated/*.ts files have valid TypeScript syntax
  </acceptance_criteria>
  <done>@iseyaa/proto npm workspace package created; TypeScript types generated from all 8 proto files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gRPC internal calls | Services call each other via gRPC on Railway private network |
| Proto contract | Generated types are the authoritative API contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-27 | Spoofing | gRPC service impersonation on Railway private network | accept | Railway private networking restricts traffic to same-project services; no public exposure of gRPC ports; mTLS would be the full mitigation (deferred to Phase 6) |
| T-02-28 | Tampering | Proto message field type mismatch between services | mitigate | ts-proto generates compile-time typed interfaces; type mismatches caught at build time |
| T-02-29 | Information Disclosure | gRPC metadata propagating sensitive fields | mitigate | Metadata only carries userId and role (JWT claims); not wallet balance, BVN, or NIN |
</threat_model>

<verification>
After both tasks:

  ls packages/proto/*.proto | wc -l

Expected: 8

  ls packages/proto/generated/*.ts | wc -l

Expected: 8+ (one per proto + index.ts)

  grep "AuthService\|WalletService" packages/proto/generated/auth.ts packages/proto/generated/wallet.ts

Expected: each file contains its respective service interface.
</verification>

<success_criteria>
- 8 proto files define gRPC contracts for all domain services
- ts-proto generates TypeScript types with NestJS-compatible interfaces (nestJs=true)
- @iseyaa/proto workspace package registered in root package.json
- Generated types importable from backend/ services in Wave 3 plans
</success_criteria>

<output>
After completion, create C:/Projects/ISEYAA/.planning/phases/02-infrastructure-migration/02-07-SUMMARY.md
</output>
