---
phase: 02-infrastructure-migration
plan: "06"
type: checkpoint:human-verify
wave: 2
depends_on: ["02-04", "02-05"]
files_modified: []
autonomous: false
requirements:
  - INFRA-04
  - INFRA-05
  - INFRA-06

must_haves:
  truths:
    - "Railway service auto-deploys from GitHub main branch and reports healthy"
    - "Infisical secrets are injected at container startup (no .env file in container)"
    - "Grafana Cloud Explore shows traces from the deployed Railway service"
    - "Sentry project captures at least one event (test error) from the Railway service"
    - "All 153 tests pass after monolith deployment to Railway"
  artifacts:
    - path: "backend/Dockerfile"
      provides: "Built and running in Railway"
      contains: "infisical run"
    - path: "backend/railway.toml"
      provides: "Railway service configured and deployed"
  key_links:
    - from: "Railway service"
      to: "Neon PostgreSQL"
      via: "DATABASE_URL from Infisical"
      pattern: "DATABASE_URL"
    - from: "Railway service"
      to: "Grafana Cloud"
      via: "OTel OTLP HTTP exporter in instrumentation.js"
      pattern: "OTEL_EXPORTER_OTLP_ENDPOINT"
---

<objective>
Human verification checkpoint: confirm the monolith deploys successfully to Railway with all infrastructure (Neon, Upstash Redis, Cloudflare R2, Infisical, Grafana, Sentry) working end-to-end. No code changes in this plan — purely validation.

Purpose: Gate Wave 3 (microservices decomposition) behind a confirmed working deployment. If any infrastructure is broken, it must be fixed before proceeding to complex microservices work.
Output: Developer confirms the Railway deployment is green and all external services are connected.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Projects/ISEYAA/.planning/PROJECT.md
@C:/Projects/ISEYAA/.planning/phases/02-infrastructure-migration/02-04-SUMMARY.md
@C:/Projects/ISEYAA/.planning/phases/02-infrastructure-migration/02-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 01-05 have implemented:
    - Neon PostgreSQL with Prisma baseline migration (Plan 01)
    - Upstash Redis TLS connection (Plan 01)
    - Cloudflare R2 file storage (Plan 02)
    - Typesense search service (Plan 03)
    - OpenTelemetry + Grafana Cloud + Sentry instrumentation (Plan 04)
    - Production Dockerfile with Infisical secrets injection (Plan 05)
    - Railway deployment configuration (Plan 05)
    - CI gate for .env files (Plan 05)
  </what-built>
  <how-to-verify>
    Perform these verification steps in order. Each step must pass before proceeding.

    STEP 1 — Push to GitHub main and wait for Railway auto-deploy:
      git add -A &amp;&amp; git commit -m "feat(infra): migrate to free-first cloud stack (Plans 01-05)"
      git push origin main
    Wait for Railway to detect the push and start a deployment. Expected: green deployment status within 5 minutes.

    STEP 2 — Verify Railway service health:
      curl https://<your-railway-domain>/api/v1/health
    Expected: HTTP 200 response (or 404 if health endpoint not yet implemented — the service must respond, not timeout).

    STEP 3 — Verify Neon database connection:
      curl -X POST https://<your-railway-domain>/api/v1/auth/otp/send \
        -H "Content-Type: application/json" \
        -d '{"phone": "+2348000000001"}'
    Expected: HTTP 201 with message "OTP sent" (or equivalent). This confirms DATABASE_URL is working (OTP creates a user record in Neon).

    STEP 4 — Verify Upstash Redis:
      After the OTP request above, check Upstash Console → Data Browser — look for key otp:+2348000000001 with 5-minute TTL.
    Expected: Key visible in Upstash with correct TTL.

    STEP 5 — Verify Cloudflare R2 (test file upload):
      Log into the running app admin and upload a test image (or use the API to create a tourism attraction with image upload).
    Expected: Returned URL contains r2.cloudflarestorage.com or your R2_PUBLIC_URL domain.

    STEP 6 — Verify Grafana Cloud traces:
      Visit Grafana Cloud → Explore → Select datasource "Traces" → Search last 15 minutes
    Expected: At least one trace from iseyaa-api service visible (from the STEP 2-3 HTTP requests).

    STEP 7 — Verify Sentry:
      Visit sentry.io → iseyaa-backend project → Issues
      If no issues yet, trigger a test: curl https://<railway-domain>/api/v1/nonexistent-route
    Expected: Sentry receives the 404/unhandled event within 30 seconds.

    STEP 8 — Verify no .env files in Railway:
      Railway Dashboard → Service → Variables
    Expected: Only INFISICAL_TOKEN and INFISICAL_PROJECT_ID visible (all other secrets come from Infisical).

    STEP 9 — Verify Typesense (if Typesense Railway service is deployed):
      curl https://<railway-domain>/api/v1/search?q=olumo
    Expected: HTTP 200 with results array (may be empty if not yet indexed — empty is OK, error is not).

    STEP 10 — Run full test suite locally to confirm no regression:
      cd backend &amp;&amp; npm run test
    Expected: All tests pass, 0 failures.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 10 steps pass and Railway deployment is confirmed healthy.

    If any step fails, describe which step and what error occurred. Claude will diagnose and fix before Wave 3 proceeds.

    Common issues and quick fixes:
    - Railway build fails: Check backend/Dockerfile syntax; verify npm run build exists in backend/package.json
    - Neon connection refused: Verify DATABASE_URL has correct Neon pooler hostname and ?pgbouncer=true param
    - Infisical secrets not loading: Verify INFISICAL_TOKEN and INFISICAL_PROJECT_ID are set in Railway service variables
    - OTel traces not appearing: Check OTEL_EXPORTER_OTLP_ENDPOINT format (must include /otlp path suffix)
    - Typesense connection refused: Verify Typesense Railway service is running and persistent volume is mounted
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub → Railway | Auto-deploy webhook triggers on push to main |
| Railway → All external services | Container pulls secrets from Infisical and connects to Neon/Upstash/R2/Grafana/Sentry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-25 | Spoofing | GitHub → Railway deploy webhook not verified | accept | Railway uses GitHub App integration (OAuth-based); not a raw webhook that can be spoofed |
| T-02-26 | Denial of Service | Railway service crashes on startup if Infisical unreachable | mitigate | Infisical Cloud has 99.9% SLA; if unreachable, Railway will retry with restart policy on_failure; secrets cached in Railway env as fallback option |
</threat_model>

<verification>
This plan is a checkpoint — verification is the task itself. The human confirms all 10 steps pass.
</verification>

<success_criteria>
- Railway deployment green (no failed builds)
- All Wave 1 infrastructure services (Neon, Upstash, R2, Typesense, Grafana, Sentry, Infisical) connected and responding
- No .env files in Railway service variables (only INFISICAL_TOKEN and INFISICAL_PROJECT_ID)
- Full test suite passes (153+ tests)
- Developer types "approved" to proceed to Wave 3
</success_criteria>

<output>
After approval, create C:/Projects/ISEYAA/.planning/phases/02-infrastructure-migration/02-06-SUMMARY.md
</output>
