---
phase: 02-infrastructure-migration
plan: "10"
type: execute
wave: 3
depends_on: ["02-09b"]
files_modified:
  - backend/apps/admin-service/src/main.ts
  - backend/apps/admin-service/src/app.module.ts
  - backend/apps/admin-service/src/admin-grpc.controller.ts
  - backend/apps/admin-service/Dockerfile
  - backend/apps/admin-service/railway.toml
  - backend/apps/ai-service/src/main.ts
  - backend/apps/ai-service/src/app.module.ts
  - backend/apps/ai-service/src/ai-grpc.controller.ts
  - backend/apps/ai-service/Dockerfile
  - backend/apps/ai-service/railway.toml
  - backend/src/app.module.ts
  - .env.example
autonomous: true
requirements:
  - INFRA-07
  - INFRA-08

must_haves:
  truths:
    - "admin-service runs as a standalone gRPC server on port 5006"
    - "ai-service runs as a standalone gRPC server on port 5007"
    - "API gateway ClientsModule registers ADMIN_PACKAGE and AI_PACKAGE"
    - "All 153 existing tests still pass"
  artifacts:
    - path: "backend/apps/admin-service/src/admin-grpc.controller.ts"
      provides: "gRPC server for AdminService (GetDashboard, ApproveItem)"
      contains: "GrpcMethod"
    - path: "backend/apps/ai-service/src/ai-grpc.controller.ts"
      provides: "gRPC server for AiService (GetItinerary, GetLgaIntelligence)"
      contains: "GrpcMethod"
  key_links:
    - from: "backend/src/app.module.ts (API gateway)"
      to: "backend/apps/admin-service"
      via: "ClientsModule ADMIN_PACKAGE on railway.internal:5006"
      pattern: "ADMIN_PACKAGE"
    - from: "backend/src/app.module.ts (API gateway)"
      to: "backend/apps/ai-service"
      via: "ClientsModule AI_PACKAGE on railway.internal:5007"
      pattern: "AI_PACKAGE"
---

<objective>
Extract admin-service and ai-service as standalone gRPC microservices. Register ADMIN_PACKAGE and AI_PACKAGE in the API gateway. notifications-service is extracted in the next plan (02-10b).

Purpose: Continue the service decomposition. Both services reuse existing business logic via strangler-fig — only the transport layer is new. After 02-10b, all 8 services will be decomposed.
Output: Two new gRPC service apps in backend/apps/; API gateway updated with ADMIN_PACKAGE and AI_PACKAGE ClientsModule registrations.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Projects/ISEYAA/.planning/PROJECT.md
@C:/Projects/ISEYAA/.planning/phases/02-infrastructure-migration/02-RESEARCH.md
@C:/Projects/ISEYAA/.planning/phases/02-infrastructure-migration/02-09b-SUMMARY.md
</context>

<interfaces>
<!-- Port and package assignments -->

- admin-service: port 5006, package: 'admin', proto: packages/proto/admin.proto
- ai-service: port 5007, package: 'ai', proto: packages/proto/ai.proto

Railway internal URLs:
- admin-service.railway.internal:5006
- ai-service.railway.internal:5007

gRPC methods per service (from Plan 07 proto definitions):
- AdminService: GetDashboard, ApproveItem
- AiService: GetItinerary, GetLgaIntelligence
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create admin-service gRPC app</name>
  <files>
    backend/apps/admin-service/src/main.ts,
    backend/apps/admin-service/src/app.module.ts,
    backend/apps/admin-service/src/admin-grpc.controller.ts,
    backend/apps/admin-service/Dockerfile,
    backend/apps/admin-service/railway.toml
  </files>
  <read_first>
    backend/apps/auth-service/src/main.ts,
    backend/apps/auth-service/src/auth-grpc.controller.ts,
    backend/src/modules/admin/admin.service.ts,
    packages/proto/admin.proto
  </read_first>
  <action>
    Apply the exact same pattern as auth-service (Plans 08-09) to admin-service.

    ADMIN-SERVICE (port 5006, package: 'admin'):

    main.ts: NestFactory.createMicroservice with Transport.GRPC, package: 'admin', port 5006, protoPath join(__dirname, '../../../../packages/proto/admin.proto').

    app.module.ts: imports ConfigModule, PrismaModule, RedisModule, AdminModule. Controllers: [AdminGrpcController].

    admin-grpc.controller.ts:
    - @GrpcMethod('AdminService', 'GetDashboard'): calls AdminService.getDashboard(), maps result to GetDashboardResponse (total_users, total_revenue, active_events, pending_approvals)
    - @GrpcMethod('AdminService', 'ApproveItem'): calls the appropriate approval method based on item_type field ('event', 'stay', 'vendor', 'driver') — uses a switch/case to route to the correct AdminService method

    Note: The AdminService.getRevenue() raw SQL bug (vendors.category missing) identified in CONCERNS.md is known — do NOT expose getRevenue via gRPC until the bug is fixed in Phase 6. Only expose getDashboard and approval methods.

    Dockerfile: identical to auth-service Dockerfile; CMD starts admin-service dist/main.js.

    railway.toml watchPaths: backend/apps/admin-service/**, backend/src/modules/admin/**, packages/proto/**.
  </action>
  <verify>
    <automated>test -f backend/apps/admin-service/src/main.ts &amp;&amp; grep -c "5006" backend/apps/admin-service/src/main.ts</automated>
    <automated>grep -c "GrpcMethod" backend/apps/admin-service/src/admin-grpc.controller.ts</automated>
    <automated>cd backend &amp;&amp; npm run test 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - admin-service main.ts uses port 5006
    - admin-grpc.controller.ts has 2 @GrpcMethod decorators (GetDashboard, ApproveItem — NOT getRevenue)
    - backend/apps/admin-service/Dockerfile exists
    - backend/apps/admin-service/railway.toml exists with watchPaths
    - All 153 tests still pass
    - cd backend && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>admin-service gRPC app created; AdminService revenue bug not exposed via gRPC</done>
</task>

<task type="auto">
  <name>Task 2: Create ai-service + register ADMIN_PACKAGE + AI_PACKAGE in API gateway</name>
  <files>
    backend/apps/ai-service/src/main.ts,
    backend/apps/ai-service/src/app.module.ts,
    backend/apps/ai-service/src/ai-grpc.controller.ts,
    backend/apps/ai-service/Dockerfile,
    backend/apps/ai-service/railway.toml,
    backend/src/app.module.ts,
    .env.example
  </files>
  <read_first>
    backend/apps/auth-service/src/app.module.ts,
    backend/src/modules/ai/ai.service.ts,
    packages/proto/ai.proto,
    backend/src/app.module.ts
  </read_first>
  <action>
    FILE GROUP 1: ai-service (port 5007, package: 'ai')

    main.ts: NestFactory.createMicroservice with Transport.GRPC, package: 'ai', port 5007, protoPath join(__dirname, '../../../../packages/proto/ai.proto').

    app.module.ts: imports ConfigModule, PrismaModule, RedisModule, AiModule. Controllers: [AiGrpcController].

    ai-grpc.controller.ts:
    - @GrpcMethod('AiService', 'GetItinerary'): calls AiService.generateItinerary() with lga_slug, duration_days, interests, budget_ngn from request. Returns ItineraryResponse with content string. Note: the existing AiService uses SSE streaming — the gRPC method returns the complete itinerary as a single string (not streamed). This is correct behavior for gRPC unary call.
    - @GrpcMethod('AiService', 'GetLgaIntelligence'): calls AiService.getLgaIntelligence(). Returns LgaIntelligenceResponse.

    Dockerfile: identical to auth-service Dockerfile; CMD starts ai-service dist/main.js.

    railway.toml watchPaths: backend/apps/ai-service/**, backend/src/modules/ai/**, packages/proto/**.

    FILE GROUP 2: Update backend/src/app.module.ts

    Read the current app.module.ts (which already has AUTH, WALLET, EVENTS, STAYS, MARKETPLACE packages from Plans 08-09b). Add 2 more ClientsModule registrations:

      {
        name: 'ADMIN_PACKAGE',
        transport: Transport.GRPC,
        options: {
          package: 'admin',
          protoPath: join(__dirname, '../../../packages/proto/admin.proto'),
          url: process.env.ADMIN_SERVICE_URL || 'admin-service.railway.internal:5006',
        },
      },
      {
        name: 'AI_PACKAGE',
        transport: Transport.GRPC,
        options: {
          package: 'ai',
          protoPath: join(__dirname, '../../../packages/proto/ai.proto'),
          url: process.env.AI_SERVICE_URL || 'ai-service.railway.internal:5007',
        },
      },

    FILE GROUP 3: .env.example

    Add if not already present:
      ADMIN_SERVICE_URL=admin-service.railway.internal:5006
      AI_SERVICE_URL=ai-service.railway.internal:5007
  </action>
  <verify>
    <automated>test -f backend/apps/ai-service/src/main.ts &amp;&amp; grep -c "5007" backend/apps/ai-service/src/main.ts</automated>
    <automated>grep -c "ADMIN_PACKAGE\|AI_PACKAGE" backend/src/app.module.ts</automated>
    <automated>cd backend &amp;&amp; npm run test 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ai-service main.ts uses port 5007
    - ai-grpc.controller.ts has 2 @GrpcMethod decorators (GetItinerary, GetLgaIntelligence)
    - backend/src/app.module.ts has ADMIN_PACKAGE and AI_PACKAGE registered (grep shows 2+ matches)
    - .env.example has ADMIN_SERVICE_URL and AI_SERVICE_URL
    - Total ClientsModule registrations in app.module.ts: 7 (AUTH through AI)
    - All 153 tests still pass
    - cd backend && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>ai-service created; ADMIN_PACKAGE and AI_PACKAGE registered in API gateway; 7 of 8 services in ClientsModule</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API gateway → ai-service | Claude API key is in ai-service context only (via Infisical injection) |
| admin-service | Approval operations require SUPER_ADMIN or LGA_ADMIN role check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-37 | Elevation of Privilege | admin-service ApproveItem called without role check | mitigate | ApproveItem gRPC method checks userId/role from gRPC metadata; only SUPER_ADMIN and LGA_ADMIN roles may approve |
| T-02-38 | Information Disclosure | ai-service logs containing Claude prompt details | accept | AI prompts contain public data (attraction names, LGA info); no PII in AI context |
| T-02-40 | Denial of Service | ai-service gRPC call blocks on long Claude stream | mitigate | AiService gRPC method collects full stream before returning (unary, not streaming gRPC); timeout set to 30s at ClientsModule level |
</threat_model>

<verification>
After both tasks:

  cd backend && npm run test

Expected: 153+ tests passing.

Count services so far:

  ls backend/apps/ | sort

Expected: auth-service, wallet-service, events-service, stays-service, marketplace-service, admin-service, ai-service (7 directories — notifications-service added in 02-10b)

Count ClientsModule packages:

  grep "_PACKAGE" backend/src/app.module.ts | wc -l

Expected: 7
</verification>

<success_criteria>
- admin-service on port 5006 and ai-service on port 5007 created as standalone gRPC apps
- API gateway registers ADMIN_PACKAGE and AI_PACKAGE
- INFRA-07 partially satisfied: 7 of 8 services decomposed (notifications-service in 02-10b)
- 153+ tests pass
</success_criteria>

<output>
After completion, create C:/Projects/ISEYAA/.planning/phases/02-infrastructure-migration/02-10-SUMMARY.md
</output>
