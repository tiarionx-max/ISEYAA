---
phase: 16-connection-pooling-infrastructure
plan: 04
type: execute
wave: 3
depends_on: [16-01, 16-02, 16-03]
files_modified:
  - .planning/phases/16-connection-pooling-infrastructure/16-VERIFICATION.md
  - MANUAL-ACTIONS.md
autonomous: false
requirements: [POOL-01, POOL-02]
must_haves:
  truths:
    - "A human has confirmed the real Neon plan/CU ceiling in the Neon Console and reconciled it against the conservative 104-connection baseline documented in .env.example"
    - "Grafana Cloud shows a live postgres_open_connections gauge with an alert rule saved at 80% of the confirmed ceiling — the standard early-warning threshold (D-07)"
    - "A combined-topology k6 run (monolith + notifications-service under load simultaneously) confirms total open Postgres connections stayed under the confirmed ceiling"
    - "The live production Railway DATABASE_URL for the monolith has actually been changed to the pooled -pooler format with an explicit connection_limit, redeployed, and confirmed in effect — not just documented in .env.example"
  artifacts:
    - path: ".planning/phases/16-connection-pooling-infrastructure/16-VERIFICATION.md"
      provides: "Operator's recorded sign-off — Neon Console reading, Grafana gauge/alert confirmation, combined-topology run result, production Railway DATABASE_URL change confirmation"
      contains: "Neon Console"
    - path: "MANUAL-ACTIONS.md"
      provides: "Completed Phase 16 section with a 16-approved resume signal"
      contains: "16-approved"
  key_links:
    - from: "Neon Console (external)"
      to: ".env.example connection_limit values"
      via: "human reconciliation of D-03/D-04's conservative baseline vs the live ceiling"
      pattern: "connection_limit"
    - from: "Railway dashboard (external, monolith service Variables tab)"
      to: "live production monolith Prisma client"
      via: "human-applied DATABASE_URL env var update + redeploy — the actual system this phase exists to change, not just its documentation"
      pattern: "-pooler"
---

<objective>
Final human-verification checkpoint for Phase 16. Plans 16-01 through 16-03 land every piece Claude can automate: notifications-service boots, packages/proto compiles, pooled connection strings are documented and config-tested, the open-connections gauge is wired into OTel, and the combined-topology k6 scenario is built. This plan is the human-only steps left, including the one that actually makes POOL-01 true in production (16-CONTEXT.md D-03, ROADMAP SC1/SC3): confirming the real Neon plan/CU ceiling against the Neon Console (no CLI/API credential is provisioned in this project for that), configuring/confirming the Grafana Cloud alert rule (no Grafana-as-code exists in this repo, same pattern as Phase 11's RESIL-02), and — critically — applying the documented pooled connection string to the live, already-deployed monolith's production DATABASE_URL on Railway.

This is NOT an automated plan. It is three `checkpoint:human-verify` tasks that pause for the operator. After verification, the operator marks Phase 16 complete in ROADMAP.md.

**Pre-checkpoint operator setup** (required before either task can start):
1. Ensure the monolith is running locally (`npm run dev --workspace=backend`) with a real `DATABASE_URL` pointed at the Neon dev branch (not localhost docker-compose Postgres — the pooled-connection-string behavior only manifests against a real Neon `-pooler` endpoint).
2. Ensure `notifications-service` is running locally (`cd backend && npx nest build notifications-service && node dist/apps/notifications-service/src/main.js`, per Plan 16-01 Task 2's boot command — this time let it run, do not let it be killed by a timeout).
3. Have `k6` installed (`choco install k6` on Windows, per the header comment in `load-tests/k6/main.js`) and a seeded test user (`TEST_PHONE`/`TEST_PASSWORD`) available for `main.js`'s `authFlow`.
4. Have Neon Console, Grafana Cloud, and Railway dashboard login credentials ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-connection-pooling-infrastructure/16-CONTEXT.md
@.planning/phases/16-connection-pooling-infrastructure/16-RESEARCH.md
@.planning/phases/16-connection-pooling-infrastructure/16-VALIDATION.md
@.planning/phases/16-connection-pooling-infrastructure/16-01-SUMMARY.md
@.planning/phases/16-connection-pooling-infrastructure/16-02-SUMMARY.md
@.planning/phases/16-connection-pooling-infrastructure/16-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plan 16-01 documented a conservative baseline in .env.example: monolith connection_limit=20, notifications-service connection_limit=5, both sized against Neon's smallest published tier (104 max_connections at 0.25 CU) since the real provisioned Neon plan/CU was unconfirmed at planning time (16-RESEARCH.md Assumptions Log A1/A2). This task confirms the real number and reconciles it.
  </what-built>
  <how-to-verify>
1. Log into the Neon Console for this project's database.
2. Navigate to Project -> Settings -> Compute (or the Billing page) and record the actual plan (Free / Launch / Scale) and the compute size (CU) autoscale range currently provisioned.
3. Cross-reference against Neon's published max_connections-by-CU table (16-RESEARCH.md Standard Stack / Pitfall 2) to determine the real connection ceiling.
4. Compare against the 104-connection baseline used in .env.example:
   - If the real ceiling is >= 104 (i.e., the baseline was conservative or exactly right): no changes needed, the documented connection_limit=20/5 values and the 83-connection Grafana alert threshold (80% of 104) stand as-is, just now confirmed rather than assumed.
   - If the real ceiling is LOWER than 104: this is a blocking finding — record it in 16-VERIFICATION.md and flag a gap-closure plan to reduce connection_limit and the alert threshold proportionally before Phase 17 runs a second live process against the same database.
5. Record the confirmed plan/CU/ceiling in `.planning/phases/16-connection-pooling-infrastructure/16-VERIFICATION.md` (new file — create it with the confirmed numbers and today's date).
  </how-to-verify>
  <resume-signal>Reply with the confirmed Neon plan/CU/ceiling, or "16-neon-confirmed" if the 104-connection baseline stands unchanged.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plan 16-02 wired a `postgres_open_connections` OTel gauge (30s poll against `pg_stat_activity`) into the same Grafana Cloud OTLP pipeline already receiving traces. Plan 16-03 built a combined-topology k6 scenario that drives HTTP load against the monolith and gRPC load against `notifications-service` simultaneously. This task runs the real combined load test and confirms both the connection-ceiling claim (POOL-02) and the Grafana alert (ROADMAP SC3, D-07).
  </what-built>
  <how-to-verify>
1. With the monolith and notifications-service both running locally per this plan's pre-checkpoint setup, run a smoke-scale combined run: `cd load-tests/k6 && k6 run --vus 50 --duration 60s --env BASE_URL=http://localhost:3001 --env NOTIFICATIONS_GRPC_URL=localhost:5008 --env TEST_PHONE=<seeded-phone> --env TEST_PASSWORD=<seeded-password> main.js`.
2. While the run is active, query `pg_stat_activity` directly to cross-check the gauge: `psql $DATABASE_URL -c "SELECT count(*) FROM pg_stat_activity WHERE datname = current_database();"` — confirm the count stays below the ceiling confirmed in the prior task.
3. Log into Grafana Cloud, open (or create) the dashboard panel for `postgres_open_connections`, and confirm the gauge shows live, moving values during the k6 run.
4. Create or confirm a Grafana alert rule on `postgres_open_connections` firing at 80% of the confirmed ceiling from the prior task (83 if the 104-connection baseline stands; recompute if the prior task found a different real ceiling).
5. Record the k6 run's summary output (VUs, http_req_duration p95, grpc_req_duration p95, error rate) and the Grafana alert rule's saved threshold in `.planning/phases/16-connection-pooling-infrastructure/16-VERIFICATION.md`.
6. Append the completed step-by-step record to `MANUAL-ACTIONS.md`'s "## Phase 16" section (stubbed by Plan 16-01).
  </how-to-verify>
  <resume-signal>Reply "16-load-confirmed" once the combined-topology run stays under the confirmed ceiling and the Grafana alert rule is saved, or describe any FAIL with specifics.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plans 16-01 through 16-03 and this plan's first two tasks document, wire, and locally prove the pooled connection-string pattern — but none of them touch the live, already-deployed monolith running in production on Railway. POOL-01's actual requirement is that the running system connects through a pooled connection string with an explicit connection_limit, not merely that the pattern is documented and tested. This task closes that gap: it updates the real production DATABASE_URL env var for the monolith service on Railway to the pooled format, redeploys, and confirms the new limit is in effect.
  </what-built>
  <how-to-verify>
1. Open the Railway dashboard and select this project's monolith service (the one running `backend/src/main.ts` — NOT notifications-service, which has no production Railway deployment yet; per 16-CONTEXT.md its extraction/cutover to a live gRPC caller is Phase 17 scope, out of bounds here).
2. Go to the monolith service's Variables tab and locate the current `DATABASE_URL` value.
3. Replace it with the pooled format documented in `.env.example` (Plan 16-01 Task 3): the Neon host with the `-pooler` suffix, plus query params `connection_limit=20&pool_timeout=10` — unless Task 1 above found the real Neon ceiling differs from the 104-connection baseline, in which case use the recomputed `connection_limit` value recorded in `16-VERIFICATION.md` instead of 20. Do NOT include `?pgbouncer=true` (Neon's managed pooler does not use it — 16-RESEARCH.md Pitfall 3).
4. Confirm `DIRECT_URL` (used only by `prisma migrate`, unchanged) is still set to the existing unpooled Neon connection string — do not touch it.
5. Save the variable change and let Railway redeploy the monolith service (or trigger a manual redeploy from the Railway dashboard if it does not auto-redeploy on variable change).
6. After redeploy, confirm the new connection_limit is actually in effect using at least one of: (a) `psql $DIRECT_URL -c "SELECT count(*) FROM pg_stat_activity WHERE datname = current_database();"` and observe the count stays bounded at/under the configured connection_limit under normal live traffic; (b) Railway's deploy logs for the monolith service show a clean boot with no Prisma connection errors; (c) the Grafana `postgres_open_connections` gauge (Plan 16-02) shows live values immediately after the redeploy.
7. Record the production change in `.planning/phases/16-connection-pooling-infrastructure/16-VERIFICATION.md`: the connection_limit value applied, the redeploy timestamp, and which of the three confirmation methods in step 6 was used.
8. Close `MANUAL-ACTIONS.md`'s "## Phase 16" section with the `16-approved` resume signal line (this is the final step of the phase).
  </how-to-verify>
  <resume-signal>Reply "16-approved" once the monolith's live Railway DATABASE_URL is updated to the pooled format, redeployed, and confirmed via pg_stat_activity, Railway logs, or the Grafana gauge — or describe any FAIL with specifics.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator's Neon Console / Grafana Cloud / Railway dashboard session | Human-only external dashboard access — no API credential for any of the three is provisioned in this repo's env vars, so this cannot be automated by Claude |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-16-09 | Configuration (V14) | Grafana alert threshold value | mitigate | Threshold is explicitly documented (83, or the recomputed value from the live Neon Console check) and recorded in 16-VERIFICATION.md — never left as an undocumented default |
| T-16-10 | Denial of Service | Live combined-topology run against a real Neon branch | accept | Operator runs a bounded smoke-scale test (50 VUs / 60s), not the full 10,000-VU production-scale run, against a dev/staging Neon branch — not production |
| T-16-11 | Denial of Service / Availability | Production monolith DATABASE_URL change + redeploy (Task 3) | mitigate | The applied connection_limit is the same conservative, already-documented value used throughout the phase (or a value reconciled downward per Task 1's live Neon check, never upward beyond the confirmed ceiling); the operator confirms a clean redeploy before considering the task done, catching a bad value immediately rather than silently degrading production |

</threat_model>

<verification>
All three checkpoint tasks require operator sign-off recorded in `.planning/phases/16-connection-pooling-infrastructure/16-VERIFICATION.md`. Phase 16 is not complete until all three resume signals are received, including Task 3's confirmation that the live production Railway DATABASE_URL has actually been changed to the pooled format and redeployed.
</verification>

<success_criteria>
- The real Neon plan/CU ceiling is confirmed and reconciled against the planning-time baseline
- A combined-topology k6 run proves total open Postgres connections stay under that ceiling
- Grafana Cloud shows the live gauge with a saved alert rule at 80% of the confirmed ceiling
- The live production Railway DATABASE_URL for the monolith is actually changed to the pooled -pooler format with an explicit connection_limit, redeployed, and confirmed in effect
- MANUAL-ACTIONS.md's Phase 16 section is closed with the 16-approved resume signal
</success_criteria>

<output>
After completion, create `.planning/phases/16-connection-pooling-infrastructure/16-04-SUMMARY.md`
</output>
</output>
