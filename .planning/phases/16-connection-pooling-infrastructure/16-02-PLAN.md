---
phase: 16-connection-pooling-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/common/services/db-metrics.service.ts
  - backend/src/common/services/__tests__/db-metrics.service.spec.ts
  - backend/src/common/common.module.ts
  - backend/src/instrumentation.ts
  - backend/package.json
autonomous: true
requirements: [POOL-02]
must_haves:
  truths:
    - "A scheduled query against pg_stat_activity reports the TRUE combined open-connection count across all processes, not just one process's own Prisma pool view"
    - "That count is exported as an OTel gauge over the same Grafana Cloud OTLP pipeline already receiving traces"
  artifacts:
    - path: "backend/src/common/services/db-metrics.service.ts"
      provides: "postgres_open_connections observable gauge, 30s cron poll against pg_stat_activity"
      contains: "pg_stat_activity"
    - path: "backend/src/instrumentation.ts"
      provides: "metricReader wired into the existing NodeSDK alongside traceExporter"
      contains: "PeriodicExportingMetricReader"
    - path: "backend/src/common/common.module.ts"
      provides: "DbMetricsService registered as a CommonModule provider"
      contains: "DbMetricsService"
  key_links:
    - from: "backend/src/common/services/db-metrics.service.ts"
      to: "backend/src/instrumentation.ts"
      via: "@opentelemetry/api metrics.getMeter('iseyaa-db').createObservableGauge('postgres_open_connections')"
      pattern: "postgres_open_connections"
---

<objective>
Build the open-connection observability pipeline that Plan 16-04's Grafana alert (POOL-02, ROADMAP SC3) depends on: a cron-scheduled query against Postgres's own pg_stat_activity view (the only source that reflects the TRUE combined connection count across both the monolith and notifications-service, unlike Prisma's own per-process $metrics), exported as an OTel observable gauge over the same Grafana Cloud OTLP pipeline already receiving traces.

Purpose: POOL-02's combined-topology load test (Plan 16-03) needs a live, queryable signal of total open Postgres connections to prove the platform stays under Neon's ceiling — this plan is that signal's plumbing.
Output: A working postgres_open_connections gauge, polled every 30 seconds, flowing into Grafana Cloud.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-connection-pooling-infrastructure/16-CONTEXT.md
@.planning/phases/16-connection-pooling-infrastructure/16-RESEARCH.md
@.planning/phases/16-connection-pooling-infrastructure/16-PATTERNS.md
</context>

<interfaces>
Cron pattern (backend/src/modules/stays/stays.service.ts, releaseEscrow method):
  import { Cron, CronExpression } from '@nestjs/schedule';
  @Cron(CronExpression.EVERY_HOUR) async releaseEscrow(): Promise<void> { ... }
  (this plan uses CronExpression.EVERY_30_SECONDS instead — different cadence, same decorator mechanics)

Typed $queryRaw tagged-template pattern (backend/src/modules/admin/admin.service.ts):
  constructor(private prisma: PrismaService) {}
  this.prisma.$queryRaw<{ lgaId: string; lgaName: string; total: number }[]>`SELECT ... FROM ... GROUP BY ...`
  Cast bigint/Decimal results with Number(...) before use (matches admin.service.ts's Number(revenueResult._sum.totalAmount ?? 0) convention).

Existing CommonModule shape (backend/src/common/common.module.ts, full file, 62 lines):
  @Global() @Module({ providers: [CsvExportService, DojahService, EncryptionService, ...], exports: [CsvExportService, DojahService, EncryptionService, ...] })
  Both providers and exports arrays are alphabetically ordered.

Current instrumentation.ts (full file, 21 lines) — this plan extends it additively:
  import { NodeSDK } from '@opentelemetry/sdk-node';
  import { getNodeAutoInstrumentations } from '@opentelemetry/auto-instrumentations-node';
  import { OTLPTraceExporter } from '@opentelemetry/exporter-trace-otlp-http';
  const sdk = new NodeSDK({
    traceExporter: new OTLPTraceExporter({ url: process.env.OTEL_EXPORTER_OTLP_ENDPOINT, headers: { Authorization: `Basic ${process.env.GRAFANA_CLOUD_OTLP_TOKEN ?? ''}` } }),
    instrumentations: [getNodeAutoInstrumentations()],
  });
  sdk.start();
  process.on('SIGTERM', () => { sdk.shutdown().finally(() => process.exit(0)); });

Existing admin.service.spec.ts mocking pattern (backend/src/modules/admin/__tests__/admin.service.spec.ts) — the pattern to copy for db-metrics.service.spec.ts:
  const mockPrisma = { $queryRaw: jest.fn(), ... };
  Test.createTestingModule({ providers: [AdminService, { provide: PrismaService, useValue: mockPrisma }] }).compile();

backend/package.json currently declares these @opentelemetry/* dependencies (alphabetical): @opentelemetry/api ^1.9.1, @opentelemetry/auto-instrumentations-node ^0.76.0, @opentelemetry/exporter-trace-otlp-http ^0.218.0, @opentelemetry/sdk-node ^0.218.0. Verified in node_modules but NOT yet declared: @opentelemetry/exporter-metrics-otlp-http (0.218.0 installed) and @opentelemetry/sdk-metrics (2.7.1 installed).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DbMetricsService (cron poll + OTel gauge) + CommonModule registration</name>
  <files>backend/src/common/services/db-metrics.service.ts, backend/src/common/services/__tests__/db-metrics.service.spec.ts, backend/src/common/common.module.ts</files>
  <read_first>backend/src/common/common.module.ts, backend/src/modules/stays/stays.service.ts, backend/src/modules/admin/admin.service.ts, backend/src/modules/admin/__tests__/admin.service.spec.ts, backend/src/prisma/prisma.service.ts, backend/src/resilience/resilience.service.ts</read_first>
  <behavior>
    - Test 1: pollOpenConnections() calls prisma.$queryRaw with a query selecting count(*) from pg_stat_activity filtered by datname = current_database(), and getCurrentOpenConnections() returns the Number-converted count from a resolved bigint row (e.g. [{ count: 7n }] -> 7)
    - Test 2: when $queryRaw rejects, pollOpenConnections() does not throw (resolves), logs only the error's message text (never the raw error object), and getCurrentOpenConnections() retains its prior value (0 on first failure) rather than crashing the cron
  </behavior>
  <action>
Create backend/src/common/services/db-metrics.service.ts exporting DbMetricsService, decorated @Injectable() and implementing OnModuleInit, constructor-injecting PrismaService (constructor(private prisma: PrismaService) {} — matches every other service in this codebase). Give it a private currentOpenConnections field defaulting to 0, and a public getCurrentOpenConnections(): number getter returning it (needed for testability and to keep the OTel callback's read path simple).

In onModuleInit(), acquire an OTel meter via metrics.getMeter('iseyaa-db') (import { metrics } from '@opentelemetry/api'), create an observable gauge named exactly postgres_open_connections with a description noting it reflects the combined open-connection count across all processes (monolith + notifications-service) sourced from pg_stat_activity, and register a callback via gauge.addCallback((result) => result.observe(this.currentOpenConnections)).

Add a method pollOpenConnections(): Promise<void> decorated @Cron(CronExpression.EVERY_30_SECONDS) (import { Cron, CronExpression } from '@nestjs/schedule', matching stays.service.ts's releaseEscrow cron pattern but with a 30-second cadence instead of hourly). Inside, wrap a typed tagged-template raw query in try/catch: this.prisma.$queryRaw<{ count: bigint }[]>`SELECT count(*) AS count FROM pg_stat_activity WHERE datname = current_database()` (matches admin.service.ts's typed $queryRaw convention), then this.currentOpenConnections = Number(rows[0]?.count ?? 0). On catch, log ONLY (err as Error)?.message via a class-level Logger instance's .error() call — never the raw error object and never process.env.DATABASE_URL — since a Prisma connection failure's raw error can embed the connection string with its password (V7: never log the raw connection string, only counts).

Register DbMetricsService as a new entry in both the providers and exports arrays of backend/src/common/common.module.ts, positioned alphabetically between DojahService and EncryptionService (matching the file's existing alphabetical ordering in both arrays).

Create backend/src/common/services/__tests__/db-metrics.service.spec.ts following admin.service.spec.ts's exact mocking pattern: a mockPrisma object with $queryRaw: jest.fn(), built via Test.createTestingModule({ providers: [DbMetricsService, { provide: PrismaService, useValue: mockPrisma }] }).compile(). Cover both <behavior> cases above.
  </action>
  <verify>
    <automated>cd backend && npx jest db-metrics.service.spec.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/common/services/db-metrics.service.ts exports DbMetricsService with a public getCurrentOpenConnections(): number method
    - pollOpenConnections() is decorated @Cron(CronExpression.EVERY_30_SECONDS)
    - The raw SQL query string contains "pg_stat_activity" and "current_database()"
    - onModuleInit() creates an observable gauge named exactly "postgres_open_connections" via metrics.getMeter('iseyaa-db')
    - DbMetricsService appears in both common.module.ts's providers and exports arrays
    - db-metrics.service.spec.ts covers both behavior cases and passes
  </acceptance_criteria>
  <done>DbMetricsService polls pg_stat_activity every 30s and exposes the count via an OTel observable gauge, registered globally through CommonModule.</done>
</task>

<task type="auto">
  <name>Task 2: Wire OTel metricReader into instrumentation.ts + declare explicit dependencies</name>
  <files>backend/src/instrumentation.ts, backend/package.json</files>
  <read_first>backend/src/instrumentation.ts, backend/package.json, backend/src/common/services/db-metrics.service.ts</read_first>
  <action>
In backend/package.json's dependencies object, add two new entries positioned alphabetically among the existing @opentelemetry/* entries: "@opentelemetry/exporter-metrics-otlp-http": "^0.218.0" (between @opentelemetry/auto-instrumentations-node and @opentelemetry/exporter-trace-otlp-http) and "@opentelemetry/sdk-metrics": "^2.7.1" (between @opentelemetry/exporter-trace-otlp-http and @opentelemetry/sdk-node). Both versions are already resolvable in this workspace's hoisted node_modules (verified: exporter-metrics-otlp-http 0.218.0, sdk-metrics 2.7.1) — this task makes the dependency explicit in package.json/package-lock.json rather than relying on transitive hoisting. Run `cd backend && npm install` after editing package.json to update package-lock.json.

In backend/src/instrumentation.ts, add two new imports — PeriodicExportingMetricReader from '@opentelemetry/sdk-metrics' and OTLPMetricExporter from '@opentelemetry/exporter-metrics-otlp-http' — and add a metricReader key to the existing `new NodeSDK({...})` constructor call, as a sibling to the existing traceExporter key: metricReader: new PeriodicExportingMetricReader({ exporter: new OTLPMetricExporter({ url: process.env.OTEL_EXPORTER_OTLP_ENDPOINT, headers: { Authorization: `Basic ${process.env.GRAFANA_CLOUD_OTLP_TOKEN ?? ''}` } }), exportIntervalMillis: 30000 }). This reuses the exact same url/Authorization-header shape already proven for traceExporter — do not introduce a separate env var for the metrics endpoint. Do not remove, reorder, or restructure the existing traceExporter, instrumentations, sdk.start(), or SIGTERM handler — this is an additive change only, since instrumentation.ts is loaded via --require BEFORE main.ts and must not import any NestJS modules.

Note (16-RESEARCH.md Open Question 2): whether Grafana Cloud's OTLP gateway accepts metrics at the same URL/token as traces is unverified against this project's live config — Plan 16-04's checkpoint confirms live delivery via the Grafana Cloud UI; this task only wires the code-side export path.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - backend/package.json's dependencies contains "@opentelemetry/exporter-metrics-otlp-http": "^0.218.0" and "@opentelemetry/sdk-metrics": "^2.7.1"
    - backend/package-lock.json reflects the new dependencies after `npm install`
    - backend/src/instrumentation.ts's NodeSDK constructor call includes a metricReader key using PeriodicExportingMetricReader + OTLPMetricExporter
    - The existing traceExporter, instrumentations, sdk.start(), and SIGTERM handler are unchanged
    - `npx tsc --noEmit -p tsconfig.json` exits 0 (confirms both this task's and Task 1's new/modified files compile cleanly)
  </acceptance_criteria>
  <done>instrumentation.ts exports the postgres_open_connections gauge to the same Grafana Cloud OTLP pipeline already receiving traces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| DbMetricsService.$queryRaw against pg_stat_activity -> OTel gauge -> Grafana Cloud OTLP export | Internal DB read -> external telemetry egress; the only new outbound data path this plan introduces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-16-04 | Information Disclosure | db-metrics.service.ts's catch-block error logging | mitigate | Logs only (err as Error)?.message, never the raw error object or process.env.DATABASE_URL — a Prisma connection failure's raw error object can embed the connection string with its password (V7) |
| T-16-05 | Information Disclosure | instrumentation.ts metricReader's Authorization header | mitigate | Reuses the existing GRAFANA_CLOUD_OTLP_TOKEN env var already proven for traceExporter — never hardcoded, never logged, never a separate token introduced (V14) |
| T-16-06 | Denial of Service | pg_stat_activity poll cadence (every 30s) | accept | A single lightweight count(*) query every 30 seconds is negligible additional load on Postgres — not a meaningful DoS vector, and is itself part of POOL-02's DoS mitigation (early-warning signal for pool exhaustion) |

</threat_model>

<verification>
Run `cd backend && npx jest db-metrics.service.spec.ts -x` (Task 1) and `cd backend && npx tsc --noEmit -p tsconfig.json` (Task 2). Both must pass. Confirm backend/package-lock.json was regenerated after the `npm install` in Task 2.
</verification>

<success_criteria>
- DbMetricsService polls pg_stat_activity every 30 seconds and exposes a postgres_open_connections OTel gauge
- instrumentation.ts exports that gauge alongside existing trace data to the same Grafana Cloud OTLP pipeline
- No raw connection string or raw vendor error object is ever logged
</success_criteria>

<output>
After completion, create `.planning/phases/16-connection-pooling-infrastructure/16-02-SUMMARY.md`
</output>
