---
phase: 16-connection-pooling-infrastructure
plan: 03
type: execute
wave: 2
depends_on: [16-01, 16-02]
files_modified:
  - load-tests/k6/scenarios/notifications-grpc-flow.js
  - load-tests/k6/main.js
autonomous: true
requirements: [POOL-02]
must_haves:
  truths:
    - "A single k6 run exercises both the monolith's HTTP surface AND notifications-service's gRPC surface concurrently in the same VU iteration (combined-topology, D-06)"
  artifacts:
    - path: "load-tests/k6/scenarios/notifications-grpc-flow.js"
      provides: "k6 native gRPC unary call against NotificationsService.SendPush"
      contains: "k6/net/grpc"
    - path: "load-tests/k6/main.js"
      provides: "notificationsGrpcFlow wired into the default exported VU function + grpc_req_duration threshold"
      contains: "notificationsGrpcFlow"
  key_links:
    - from: "load-tests/k6/main.js"
      to: "load-tests/k6/scenarios/notifications-grpc-flow.js"
      via: "import + call inside export default function"
      pattern: "notificationsGrpcFlow"
---

<objective>
Extend the existing k6 load test (load-tests/k6/main.js) with a native-gRPC scenario that drives notifications-service's SendPush RPC, so a single combined-topology run exercises both the monolith's HTTP surface and notifications-service's gRPC surface concurrently — proving POOL-02's total-open-connections claim against real, simultaneous load on both processes rather than the monolith alone.

Purpose: D-06 locked this as an extension of the existing k6 script (not a new dedicated script) so both processes are under load in the same run, using tooling already proven in this repo.
Output: A new k6 gRPC scenario file and main.js wired to call it from every VU iteration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-connection-pooling-infrastructure/16-CONTEXT.md
@.planning/phases/16-connection-pooling-infrastructure/16-RESEARCH.md
@.planning/phases/16-connection-pooling-infrastructure/16-PATTERNS.md
@.planning/phases/16-connection-pooling-infrastructure/16-01-SUMMARY.md
</context>

<interfaces>
Existing scenario-file naming/shape convention (load-tests/k6/scenarios/wallet-flow.js, full file, 24 lines):
  import http from 'k6/http';
  import { check, sleep } from 'k6';
  const BASE_URL = __ENV.BASE_URL || 'https://iseyaa-api.railway.app';
  export default function walletFlow() {
    const token = getToken(BASE_URL, __ENV.TEST_PHONE, __ENV.TEST_PASSWORD);
    const res = http.get(`${BASE_URL}/api/v1/wallet/balance`, { headers: { Authorization: `Bearer ${token}` }, tags: { endpoint: 'wallet' } });
    check(res, { 'wallet balance status 200': (r) => r.status === 200 });
    sleep(1);
  }
  Convention: export default function <camelCaseName>Flow(), __ENV.<VAR> || '<fallback>' pattern, check(res, { 'description': (r) => ... }), trailing sleep(1).

Verified proto contract (packages/proto/notifications.proto, full file, 27 lines):
  service NotificationsService { rpc SendPush (SendPushRequest) returns (SendPushResponse); rpc RegisterToken (RegisterTokenRequest) returns (RegisterTokenResponse); }
  message SendPushRequest { string user_id = 1; string title = 2; string body = 3; }
  message SendPushResponse { bool success = 1; }

notifications-service's local gRPC listen address (backend/apps/notifications-service/src/main.ts): url: '0.0.0.0:5008'
.env.example's existing gRPC-section naming convention (line 66): NOTIFICATIONS_SERVICE_URL=notifications-service.railway.internal:5008

Current full load-tests/k6/main.js (35 lines) — this plan's Task 2 target:
  import authFlow from './scenarios/auth-flow.js';
  import walletFlow from './scenarios/wallet-flow.js';
  import eventsFlow from './scenarios/events-flow.js';
  import transportFlow from './scenarios/transport-flow.js';
  export const options = {
    stages: [ { duration: '2m', target: 500 }, { duration: '3m', target: 10000 }, { duration: '5m', target: 10000 }, { duration: '2m', target: 0 } ],
    thresholds: {
      'http_req_duration': ['p(95)<500'],
      'http_req_failed': ['rate<0.001'],
      'http_req_duration{endpoint:wallet}': ['p(95)<500'],
      'http_req_duration{endpoint:events}': ['p(95)<500'],
      'http_req_duration{endpoint:auth}': ['p(95)<500'],
    },
  };
  const BASE_URL = __ENV.BASE_URL || 'https://iseyaa-api.railway.app';
  export default function () { authFlow(); walletFlow(); eventsFlow(); transportFlow(); }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: New k6 native-gRPC scenario for notifications-service</name>
  <files>load-tests/k6/scenarios/notifications-grpc-flow.js</files>
  <read_first>load-tests/k6/scenarios/wallet-flow.js, load-tests/k6/main.js, packages/proto/notifications.proto, backend/apps/notifications-service/src/main.ts</read_first>
  <action>
Create load-tests/k6/scenarios/notifications-grpc-flow.js using k6's native k6/net/grpc module (built into k6 core since v0.49.0 — no xk6-grpc extension needed, since NotificationsService's two RPCs are both unary). At module scope, instantiate const client = new grpc.Client(); then call client.load(['../../../packages/proto'], 'notifications.proto') (path relative to this scenario file's location, three levels up to the repo root then into packages/proto).

Export a default function notificationsGrpcFlow() (naming convention matches wallet-flow.js's export default function walletFlow()) that: connects via client.connect(__ENV.NOTIFICATIONS_GRPC_URL || 'localhost:5008', { plaintext: true }) (matches notifications-service's local 0.0.0.0:5008 listen address and the NOTIFICATIONS_GRPC_URL naming convention, parallel to .env.example's existing NOTIFICATIONS_SERVICE_URL var); invokes 'notifications.NotificationsService/SendPush' with a payload object { user_id: __ENV.TEST_USER_ID || 'k6-load-test-user', title: 'Load test', body: 'ping' } (field names verified against packages/proto/notifications.proto's SendPushRequest message: user_id, title, body — safe to copy verbatim); checks the response via check(res, { 'grpc SendPush status OK': (r) => r && r.status === grpc.StatusOK }); and calls client.close().

No FCM stubbing is required: NotificationsService.sendPush()'s Prisma query executes before any FCM call, so this load test's connection-pool behavior is unaffected whether FIREBASE_SERVICE_ACCOUNT_JSON is configured in the test environment or not (16-RESEARCH.md Open Question 3).
  </action>
  <verify>
    <automated>cd load-tests/k6 && k6 run --vus 1 --iterations 1 --env NOTIFICATIONS_GRPC_URL=localhost:5008 scenarios/notifications-grpc-flow.js</automated>
  </verify>
  <acceptance_criteria>
    - load-tests/k6/scenarios/notifications-grpc-flow.js exists, imports grpc from 'k6/net/grpc', and exports a default function named notificationsGrpcFlow
    - The gRPC invoke call targets 'notifications.NotificationsService/SendPush' with fields user_id, title, body
    - A single k6 iteration against a locally running notifications-service (started per Plan 16-01 Task 2's boot command) returns check 'grpc SendPush status OK': true
  </acceptance_criteria>
  <done>notifications-grpc-flow.js exists and successfully drives notifications-service's SendPush RPC via k6's native gRPC client.</done>
</task>

<task type="auto">
  <name>Task 2: Wire notificationsGrpcFlow into the combined-topology main.js run</name>
  <files>load-tests/k6/main.js</files>
  <read_first>load-tests/k6/main.js, load-tests/k6/scenarios/notifications-grpc-flow.js</read_first>
  <action>
In load-tests/k6/main.js, add an import of notificationsGrpcFlow from './scenarios/notifications-grpc-flow.js' alongside the four existing scenario imports (authFlow, walletFlow, eventsFlow, transportFlow), keeping the existing import order and appending this one last. Add a notificationsGrpcFlow() call inside the existing export default function () { ... } VU function, after the existing four calls — per D-06, this makes every VU iteration exercise both the monolith's HTTP surface and notifications-service's gRPC surface concurrently, which is what "combined-topology" means for this load test.

Add a new 'grpc_req_duration': ['p(95)<500'] entry to the existing thresholds object, alongside the existing http_req_duration/http_req_failed entries. k6's native gRPC module reports its own grpc_req_duration metric, distinct from http_req_duration — do not reuse an http_req_duration{endpoint:notifications} key, since gRPC calls are not tagged into the http_req_duration metric family at all.

Update the file's top comment block (currently documenting BASE_URL, TEST_PHONE, TEST_PASSWORD) to add a line documenting the new NOTIFICATIONS_GRPC_URL env var, matching the existing comment style ("// Requires: ...").
  </action>
  <verify>
    <automated>cd load-tests/k6 && k6 run --vus 1 --iterations 1 --env BASE_URL=http://localhost:3001 --env NOTIFICATIONS_GRPC_URL=localhost:5008 --env TEST_PHONE=$TEST_PHONE --env TEST_PASSWORD=$TEST_PASSWORD main.js</automated>
  </verify>
  <acceptance_criteria>
    - main.js imports notificationsGrpcFlow from './scenarios/notifications-grpc-flow.js'
    - the default exported VU function calls all five flows, including notificationsGrpcFlow()
    - thresholds includes a 'grpc_req_duration': ['p(95)<500'] entry
    - the top comment block documents NOTIFICATIONS_GRPC_URL
    - a single combined-topology k6 iteration against a fully running local stack (monolith + notifications-service, seeded TEST_PHONE/TEST_PASSWORD) completes all five flows' checks
  </acceptance_criteria>
  <done>main.js's default VU function drives combined HTTP + gRPC load against both the monolith and notifications-service in the same run, satisfying D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| k6 load-test process -> monolith HTTP + notifications-service gRPC | Controlled, operator-triggered synthetic load — not an untrusted external input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-16-07 | Information Disclosure | notifications-grpc-flow.js env vars (NOTIFICATIONS_GRPC_URL, TEST_USER_ID) | accept | Load-test-only synthetic payload ("Load test" / "ping"), no real PII touched; TEST_USER_ID defaults to a non-existent placeholder string, not a real user |
| T-16-08 | Denial of Service | Combined-topology load generation itself | accept | Intentional, operator-controlled load test against local/staging — this IS the mitigation mechanism for T-16-02 (proving connection_limit sizing holds under real concurrent load), not a threat in itself |

</threat_model>

<verification>
Run the notifications-grpc-flow.js single-iteration k6 command (Task 1) against a locally running notifications-service, then the combined main.js single-iteration run (Task 2) against the full local stack. Both must report all checks passing.
</verification>

<success_criteria>
- notifications-grpc-flow.js successfully drives notifications-service's SendPush RPC via k6 native gRPC
- main.js's default VU function exercises both the monolith (HTTP) and notifications-service (gRPC) in every iteration
- grpc_req_duration threshold is present alongside the existing http_req_duration thresholds
</success_criteria>

<output>
After completion, create `.planning/phases/16-connection-pooling-infrastructure/16-03-SUMMARY.md`
</output>
