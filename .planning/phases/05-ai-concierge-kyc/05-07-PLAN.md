---
phase: 05-ai-concierge-kyc
plan: 07
type: execute
wave: 7
depends_on: ["05-04", "05-05", "05-06"]
files_modified: []
autonomous: false
requirements: [AI-01, AI-02, AI-03, AI-04, AI-05, KYC-01, KYC-02, KYC-03, KYC-04]

must_haves:
  truths:
    - "Full backend test suite passes (≥240 tests)"
    - "All ROADMAP Phase 5 success criteria pass an end-to-end smoke test"
    - "No plaintext PII appears in any log line during the smoke run"
    - "Operator has supplied or explicitly waived (with stub-mode acceptance) every Phase 5 env var: ENCRYPTION_KEY, UPSTASH_VECTOR_REST_URL/TOKEN, DOJAH_API_KEY, SMILE_IDENTITY_*"
    - "Mobile dev build (or Expo Go for non-Smile tiers) renders /ai-chat and /kyc and exercises them successfully"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for Phase 5. No code changes; this is the sign-off gate that confirms every Wave 1–6 deliverable behaves as specified end-to-end before the phase is marked complete.

Purpose: Phase 5 ships PII handling (BVN/NIN) and a new mobile surface — both demand human eyes before /gsd-verify-work runs. The autonomous test suite cannot prove that, e.g., AsyncStorage persistence survives a real app restart, that the typing indicator animates correctly, or that the operator has actually provisioned ENCRYPTION_KEY in their environment.

Output: A signed-off checkpoint indicating Phase 5 is ready for verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-ai-concierge-kyc/05-RESEARCH.md
@.planning/phases/05-ai-concierge-kyc/05-UI-SPEC.md
@.planning/phases/05-ai-concierge-kyc/05-01-SUMMARY.md
@.planning/phases/05-ai-concierge-kyc/05-02-SUMMARY.md
@.planning/phases/05-ai-concierge-kyc/05-03-SUMMARY.md
@.planning/phases/05-ai-concierge-kyc/05-04-SUMMARY.md
@.planning/phases/05-ai-concierge-kyc/05-05-SUMMARY.md
@.planning/phases/05-ai-concierge-kyc/05-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint automated smoke</name>
  <files>(none — read-only verification)</files>
  <read_first>
    All six Phase 5 SUMMARYs
  </read_first>
  <action>
    Run the automated smoke suite that the human checkpoint will rely on. Capture output to display verbatim to the operator.

    1. Backend full test suite:
       ```
       cd backend && npm test
       ```
       Expected: ≥240 tests pass, zero failures.

    2. Backend type-check:
       ```
       cd backend && npx tsc --noEmit
       ```
       Expected: zero errors.

    3. Mobile type-check:
       ```
       cd mobile && npx tsc --noEmit
       ```
       Expected: zero errors.

    4. PII-leak grep on backend source — must return zero matches:
       ```
       grep -rn "console\.log.*\\(dto\\.bvn\\|dto\\.nin\\|user\\.bvn\\b\\|user\\.nin\\b\\)" backend/src
       grep -rn "this\\.logger\\.(log\\|warn\\|error).*\\(dto\\.bvn\\|dto\\.nin\\)" backend/src/modules/users backend/src/common
       ```
       (PowerShell equivalent: `Select-String -Path backend/src/**/*.ts -Pattern '...'`.)

    5. PlatformConfig presence check (run from a Node REPL or one-off script):
       ```
       cd backend && node -e "
         const { PrismaClient } = require('@prisma/client');
         const p = new PrismaClient();
         p.platformConfig.findMany({ where: { key: { in: ['kyc_bvn_daily_limit','kyc_nin_daily_limit','kyc_smile_daily_limit'] } } })
           .then(r => { console.log(JSON.stringify(r, null, 2)); return p.\$disconnect(); });
       "
       ```
       Expected: three rows printed with values 200000, 1000000, 5000000.

    6. Env-var inventory — confirm `.env.example` contains all six new entries (ENCRYPTION_KEY, UPSTASH_VECTOR_REST_URL, UPSTASH_VECTOR_REST_TOKEN, DOJAH_API_KEY, DOJAH_APP_ID, SMILE_IDENTITY_PARTNER_ID, SMILE_IDENTITY_API_KEY).

    If any step fails: DO NOT proceed to the human checkpoint. Surface the failure and return control to the operator to triage.
  </action>
  <verify>
    <automated>cd backend && npm test 2>&1 | tail -5 | grep -q "passing\|passed" && cd backend && npx tsc --noEmit 2>&1 | grep -c "error TS" | grep -q "^0$" && cd ../mobile && npx tsc --noEmit 2>&1 | grep -c "error TS" | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - All five automated checks pass
    - Output saved/displayed for the next checkpoint task
  </acceptance_criteria>
  <done>Automated baseline confirmed; ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 12-step end-to-end human verification</name>
  <what-built>
    Phase 5 delivered:
    - Wave 1: User schema columns (bvnHash, ninHash, kycBvnVerifiedAt, kycNinVerifiedAt, kycLivenessVerifiedAt); three PlatformConfig rows (kyc_bvn_daily_limit=200000, kyc_nin_daily_limit=1000000, kyc_smile_daily_limit=5000000); npm deps installed.
    - Wave 2: EncryptionService (AES-256-GCM), VectorService (Upstash Vector with stub), DojahService (NIN with stub), PaystackService.resolveBvn extension, KycService skeleton.
    - Wave 3: Full KycService + POST /users/kyc/{bvn,nin,smile/complete}; WalletService refactored to read tier limits from PlatformConfig; users.service.ts eraseData nulls KYC fields.
    - Wave 4: AiService.streamChatWithTools with 5 Claude tools (get_attractions, get_events, get_stays, get_ride_estimate, get_weather); POST /ai/chat upgraded to ChatDto; POST /ai/recommend added.
    - Wave 5: mobile/app/ai-chat.tsx (SSE streaming chat with tool cards + AsyncStorage history); navigation + profile menu entry.
    - Wave 6: mobile/app/kyc.tsx (three-tier progressive KYC + driver banner + polling); USER_SELECT extended; navigation + profile menu entry.

    Stub paths active when env vars are absent (logged with explicit prefixes):
    - [UPSTASH VECTOR STUB] — no personalisation
    - [DOJAH STUB] — NIN auto-verifies
    - [PAYSTACK STUB] — BVN auto-verifies
    - [SMILE STUB] — liveness auto-completes
  </what-built>
  <how-to-verify>
    **Prerequisites (operator must complete before testing):**

    A. Confirm `.env` contains a valid `ENCRYPTION_KEY` (64 hex chars). If absent, generate with `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` and set in `.env`. Backend will fail to start without it.

    B. Decide stub vs real for each external service:
       - Paystack BVN: stub OK for dev; real needs `PAYSTACK_SECRET_KEY`
       - Dojah NIN: stub OK; real needs `DOJAH_API_KEY` + `DOJAH_APP_ID`
       - Smile Identity: stub OK (admin-approves-style); real needs `SMILE_IDENTITY_*` + a Development Build (Expo Go cannot host the native SDK)
       - Upstash Vector: stub OK (no personalisation); real needs `UPSTASH_VECTOR_REST_URL/TOKEN` (BAAI/bge-m3 index)

    C. Start the stack: `npm run dev:backend` (port 3001); `cd mobile && npx expo start`. Log in as a test user via mobile or web.

    **End-to-end test (run through in order — stop on first failure and report):**

    1. **AI Chat reachable.** From the mobile Profile tab, tap "AI Concierge". The screen header reads "AI Concierge"; background is JUNGLE green; empty state shows the Bot icon + the documented copy.

    2. **AI Chat streams text.** Type "Hello" and tap send. The user bubble appears immediately on the right (GOLD-tinted). An AI bubble streams in token-by-token on the left (FOREST-tinted). Streaming completes within ~5 seconds.

    3. **AI tool use renders a card.** Send: "Show me 3 attractions in Abeokuta". The AI's bubble contains an inline tool card with a MapPin icon and a label like "Found 3 attractions". The AI then narrates the results.

    4. **AI history persists.** Close the app fully (swipe-kill on iOS / Force Stop on Android). Reopen, navigate back to AI Concierge. Prior messages are still visible.

    5. **Vector upsert reached (only if UPSTASH configured).** After step 2, check backend logs — there should be no `[UPSTASH VECTOR STUB]` line if real keys are set; if stub mode, the log line should appear.

    6. **KYC screen reachable.** From Profile tap "Verify Identity". The screen header reads "Identity Verification"; three tier cards render with Tier 1 active (GOLD border), Tier 2 + Tier 3 locked.

    7. **BVN verification (Tier 1).** Enter a test BVN `22248185000` (or any 11 digits in stub mode). Tap "Verify BVN". A pending state shows briefly; then Tier 1 transitions to green "Verified" with a date. Tier 2 unlocks (GOLD border becomes active). Backend logs include `KYC_BVN_VERIFIED` audit entry (no plaintext BVN in any log line).

    8. **NIN verification (Tier 2).** Enter an 11-digit NIN. Tap "Verify NIN". Tier 2 → verified. Tier 3 unlocks. Audit log shows `KYC_NIN_VERIFIED`.

    9. **Liveness (Tier 3).** Tap "Start Liveness Check". In stub mode, Tier 3 transitions to verified immediately. In real mode, Smile Identity SDK launches on a Development Build.

    10. **Wallet limit updates.** Navigate to the Wallet tab (or call `GET /api/v1/wallet/balance` via curl). The `daily_limit_ngn` field reflects 5,000,000 (post-Tier-3) — proving the limit reads from PlatformConfig, not the legacy hardcoded constants.

    11. **PII hygiene.** Search the backend log output for the BVN and NIN strings you submitted. They must NOT appear anywhere. Only `userId` + `verified: true` + action names are logged.

    12. **Driver banner (optional — only if a DRIVER-role test user exists).** Log in as a driver with `kycStatus !== 'VERIFIED'`. Open KYC screen. The orange "Driver KYC Pending" banner appears above the tier cards. Have an LGA_ADMIN approve the driver — the banner switches to green "Driver Approved" within 5 seconds (polling).

    **Pass criteria:** All 12 steps complete without unexpected errors. Stubbed steps (when env vars absent) are acceptable provided the backend log clearly emits the stub-mode prefix on each call.

    **Fail criteria:** Any AI streaming hang > 10s, any tier transition that never completes, any PII appearing in logs, any 500 error from the KYC endpoints, or a missing daily_limit_ngn change after Tier 3.
  </how-to-verify>
  <resume-signal>
    Reply "approved" if all 12 steps pass.
    Reply "approved with stubs: [list]" if some external services were stubbed.
    Reply "blocked: [issue]" with a description of the failure and which step failed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → live system | Operator submits real or test BVN/NIN against running backend |
| Backend logs → operator screen | PII hygiene verified by operator scanning logs during smoke |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-30 | Information Disclosure | Real BVN entered during smoke test reaches Paystack | accept | Operator chooses to use test BVN (`22248185000` widely-published demo) or stub mode |
| T-05-31 | Elevation of Privilege | Operator approves the phase while Tier 3 is in stub mode | mitigate | Resume-signal explicitly requires listing stubs so phase verification can flag for production gating in Phase 7 |
| T-05-32 | Information Disclosure | Smoke test logs contain plaintext PII unnoticed | mitigate | Step 11 is an explicit grep-the-logs check; operator must confirm before approving |
</threat_model>

<verification>
- Automated smoke (Task 1) all-green
- Human resume-signal received in the form "approved" or "approved with stubs: ..."
</verification>

<success_criteria>
1. All five ROADMAP Phase 5 success criteria are demonstrated (streaming AI, vector personalisation, three KYC tiers, driver KYC reflected, mobile chat with history)
2. No plaintext BVN/NIN appears in backend logs
3. Wallet daily limit updates dynamically based on tier (PlatformConfig-driven)
4. Stub vs real-API status is explicitly recorded for Phase 7 production gating
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-concierge-kyc/05-07-SUMMARY.md` documenting: which steps passed, which external services were stubbed vs real, any deferred concerns to log against Phase 6/7, and the operator's resume-signal verbatim.
</output>
