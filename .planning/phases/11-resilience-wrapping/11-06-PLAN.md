---
phase: 11-resilience-wrapping
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/resilience/resilience.service.ts
  - backend/src/resilience/__tests__/retry-timeout-composition.spec.ts
  - backend/src/resilience/__tests__/resilience.service.spec.ts
autonomous: true
gap_closure: true
requirements: [RESIL-01, RESIL-02]

must_haves:
  truths:
    - "Each individual vendor-call attempt is bounded by its own cfg.timeoutMs window; the retry sequence as a whole is not capped by one shared timeout, so a merely-slow (not down) vendor still gets its full configured retryCount worth of attempts"
    - "cockatiel's own per-attempt timeout cancellation (TaskCancelledError) still counts as a transient failure and triggers the next retry attempt, exactly as it must for the CR-01 fix to actually work end-to-end"
    - "A malformed PlatformConfig resilience threshold value (non-numeric, negative, NaN-producing) falls back to RESILIENCE_DEFAULTS per-key instead of silently disabling a vendor's timeout/breaker protection until next restart"
    - "An application-level bug (e.g. a TypeError thrown inside a wrapped callback) no longer counts toward a vendor's circuit-breaker consecutive-failure threshold"
  artifacts:
    - path: "backend/src/resilience/resilience.service.ts"
      provides: "Corrected wrap() composition order (retry before timeout), hardened readConfig() numeric parsing, narrowed isTransientError classification"
    - path: "backend/src/resilience/__tests__/retry-timeout-composition.spec.ts"
      provides: "Fake-timer regression proof that each retry attempt gets its own timeout window (closes CR-01, 11-VERIFICATION.md gap 1)"
  key_links:
    - from: "backend/src/resilience/resilience.service.ts's wrap(...) call"
      to: "cockatiel's retry() and timeout() policies"
      via: "argument order — retry(...) passed before timeout(...) so timeout is innermost"
      pattern: "wrap\\(\\s*breaker,\\s*retry\\("
---

<objective>
Close CR-01 (composition-order defect) from `11-VERIFICATION.md`/`11-REVIEW.md`: `ResilienceService.onModuleInit()` currently composes `wrap(breaker, timeout(...), retry(...))`, making `timeout` the outermost policy wrapping the ENTIRE retry+backoff sequence instead of each individual attempt. Reorder to `wrap(breaker, retry(...), timeout(...))` so every attempt gets its own timeout budget, and add the fake-timer regression test the phase's own review (`11-REVIEW.md` IN-01) says is required to ever detect this class of bug. While in this file, also apply two small, low-risk hardening fixes flagged adjacent to CR-01 in `11-REVIEW.md` (WR-01: validate DB-sourced config numerics; WR-04: narrow `isTransientError`'s catch-all so application bugs don't trip the breaker) — both confined to `resilience.service.ts`, both with dedicated regression tests.

Note: `RESIL-02`'s remaining gap (live Grafana/Sentry dashboard confirmation of breaker state-change spans) is explicitly human-verification-only per `11-VERIFICATION.md`'s `human_verification` section and `11-VALIDATION.md` — it is NOT addressed by any task in this plan and cannot be closed by code. It remains an outstanding manual step for the user.

Purpose: `ResilienceService` is the single choke-point every one of the phase's 7 vendor wraps depends on (Plans 01-05) — this defect and its adjacent hardening propagate identically to every vendor downstream, so fixing it here fixes it everywhere.
Output: Reordered policy composition, hardened config parsing, narrowed transient-error classification, three new/extended test files proving all three fixes — including the critical interaction proof that WR-04's narrowing does not silently break CR-01's per-attempt retry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-resilience-wrapping/11-CONTEXT.md
@.planning/phases/11-resilience-wrapping/11-VERIFICATION.md
@.planning/phases/11-resilience-wrapping/11-REVIEW.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix CR-01 — reorder retry/timeout composition + fake-timer regression test</name>
  <files>backend/src/resilience/resilience.service.ts, backend/src/resilience/__tests__/retry-timeout-composition.spec.ts</files>
  <read_first>
    backend/src/resilience/resilience.service.ts (buggy composition at lines 53-63; `VendorPolicy`/`execute()` contract at lines 19-22, 72-79; `readConfig()` at lines 81-103)
    backend/src/resilience/resilience.types.ts (`Vendor` union; `RESILIENCE_DEFAULTS.paystack` = `{ timeoutMs: 10_000, retryCount: 2, failureThreshold: 5, halfOpenAfterMs: 30_000 }`)
    backend/src/resilience/__tests__/resilience.service.spec.ts (existing `jest.mock('@sentry/nestjs', ...)` / `jest.mock('@opentelemetry/api', ...)` blocks and `PrismaService` mock shape — reuse verbatim, do not invent a new mocking style)
    .planning/phases/11-resilience-wrapping/11-REVIEW.md CR-01 section (cites `node_modules/cockatiel/dist/Policy.js:213-226` — first `wrap()` arg is outermost, last is innermost)
  </read_first>
  <behavior>
    - Test: override the mocked `PrismaService.platformConfig.findMany` to return rows for `resilience.paystack.timeout_ms` = `1000`, `resilience.paystack.retry_count` = `2`, `resilience.paystack.breaker_failure_threshold` = `5`, `resilience.paystack.half_open_after_ms` = `30000`. Call `onModuleInit()`. Enable `jest.useFakeTimers()`. Build a mock `fn` that on each invocation schedules its own settlement via an internal `setTimeout(..., 800)`: the 1st and 2nd calls reject with `{ response: { status: 500 } }`, the 3rd call resolves `'ok'`. Invoke `service.execute('paystack', fn)` without awaiting yet, then drive the fake clock forward using `await jest.advanceTimersByTimeAsync(...)` in increments — first ~800ms to let attempt 1's own timer fire, then a further ~3500ms to clear the `ExponentialBackoff` (200ms-3000ms with jitter) before attempt 2 begins, then ~800ms for attempt 2, another ~3500ms backoff window, then ~800ms for attempt 3. Await the `execute()` promise and assert it resolves to `'ok'`, and assert `fn` was called exactly 3 times. The cumulative elapsed fake-time across all 3 attempts + 2 backoff windows is several thousand ms — far more than the single `1000ms` `timeoutMs` value — which is the proof this test would have failed under the OLD composition (one shared 1000ms window for the whole sequence) but passes under the fixed composition (each 800ms attempt fits inside its own fresh 1000ms budget).
  </behavior>
  <action>
    In `resilience.service.ts`, reorder the `wrap(...)` call (currently `wrap(breaker, timeout(cfg.timeoutMs, TimeoutStrategy.Aggressive), retry(handleWhen(isTransientError), { maxAttempts: cfg.retryCount, backoff: new ExponentialBackoff({ initialDelay: 200, maxDelay: 3_000 }) }))`) so `retry(...)` is passed BEFORE `timeout(...)`: `wrap(breaker, retry(handleWhen(isTransientError), { maxAttempts: cfg.retryCount, backoff: new ExponentialBackoff({ initialDelay: 200, maxDelay: 3_000 }) }), timeout(cfg.timeoutMs, TimeoutStrategy.Aggressive))`. `timeout` is now innermost, applied to each individual attempt; `retry` is the middle policy that re-invokes the timed attempt; `breaker` remains outermost, unchanged. Update the inline comment currently attached to the retry options object to describe the corrected rationale (each attempt gets its own `cfg.timeoutMs` budget; retry orchestrates re-running the timed attempt) and cite CR-01/`11-VERIFICATION.md`. Then create `backend/src/resilience/__tests__/retry-timeout-composition.spec.ts` implementing the Behavior above as a single focused `describe('Retry/timeout composition order (CR-01)', ...)` block, reusing the exact Sentry/OTel `jest.mock(...)` setup from `resilience.service.spec.ts` so `onBreak`'s calls don't throw during the test.
  </action>
  <verify>
    <automated>cd backend && npx jest src/resilience/__tests__/retry-timeout-composition.spec.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "wrap(" -A 12 backend/src/resilience/resilience.service.ts` shows `retry(` appearing on an earlier line than `timeout(` within the same `wrap(...)` call
    - `cd backend && npx jest src/resilience/__tests__/retry-timeout-composition.spec.ts --silent` exits 0
    - The test asserts `fn` was invoked exactly 3 times AND the `execute()` promise resolves to `'ok'`, proving the full retry sequence completed despite its cumulative duration exceeding the single per-vendor `timeoutMs` value
    - `cd backend && npx jest src/resilience --silent` (whole resilience test directory) exits 0 — the existing Plan 01 `resilience.service.spec.ts` suite is unaffected by the reorder
  </acceptance_criteria>
  <done>Composition order fixed so `timeout` bounds each individual attempt, not the whole retry sequence; proven by a deterministic fake-timer regression test that exercises real (simulated) latency, closing the blind spot `11-REVIEW.md` IN-01 documented in every prior test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden readConfig() numeric parsing (WR-01)</name>
  <files>backend/src/resilience/resilience.service.ts, backend/src/resilience/__tests__/resilience.service.spec.ts</files>
  <read_first>
    backend/src/resilience/resilience.service.ts `readConfig()` (lines 81-103) — current unguarded `Number(byKey.get(...) ?? defaults.X)` coercion, no NaN/positivity check
    backend/src/resilience/resilience.types.ts `VendorThresholds` shape and `RESILIENCE_DEFAULTS.paystackRefund.retryCount: 0` (a legitimate zero value that any positivity guard MUST still allow)
    .planning/phases/11-resilience-wrapping/11-REVIEW.md WR-01 section (exact issue: a bad `PlatformConfig` row can silently produce `timeoutMs: NaN`/`0` or `failureThreshold: 0`, causing a platform-wide vendor outage until next restart)
  </read_first>
  <behavior>
    - Test: mock `prisma.platformConfig.findMany` to return a row `{ key: 'resilience.paystack.timeout_ms', value: 'not-a-number' }`. Call `onModuleInit()`. Call `service.execute('paystack', fn)` where `fn` resolves `'ok'` after a real (non-fake-timer) `setTimeout` of 50ms. Assert the promise resolves `'ok'` — this proves the malformed `timeout_ms` value fell back to the `10_000ms` default (ample room for a 50ms `fn`) rather than collapsing to a near-zero timeout that would reject before `fn` settles.
    - Test: mock `prisma.platformConfig.findMany` to return a row `{ key: 'resilience.paystack.retry_count', value: 'abc' }`. Call `onModuleInit()`. Call `service.execute('paystack', fn)` where `fn` always rejects with `{ response: { status: 500 } }`. Assert `fn.mock.calls.length` is greater than 1 — proving the malformed `retry_count` fell back to the default `retryCount: 2` (multiple attempts), not to `0`/`NaN` (which would invoke `fn` only once).
  </behavior>
  <action>
    Add two module-scope helper functions in `resilience.service.ts`, placed near `isTransientError`: `positiveInt(raw: unknown, fallback: number): number` returning `fallback` unless `Number(raw)` is finite AND greater than 0; `nonNegativeInt(raw: unknown, fallback: number): number` returning `fallback` unless `Number(raw)` is a finite integer AND greater than or equal to 0 (this one MUST allow exactly `0`, since `paystackRefund`'s legitimate default `retryCount` is `0`). In `readConfig()`'s return object, replace the four bare `Number(byKey.get(keys.X) ?? defaults.X)` expressions: use `positiveInt(byKey.get(keys.timeoutMs), defaults.timeoutMs)` for `timeoutMs`, `positiveInt(byKey.get(keys.failureThreshold), defaults.failureThreshold)` for `failureThreshold`, `positiveInt(byKey.get(keys.halfOpenAfterMs), defaults.halfOpenAfterMs)` for `halfOpenAfterMs`, and `nonNegativeInt(byKey.get(keys.retryCount), defaults.retryCount)` for `retryCount`. Do not change the `RESILIENCE_DEFAULTS` fallback values themselves.
  </action>
  <verify>
    <automated>cd backend && npx jest src/resilience/__tests__/resilience.service.spec.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `resilience.service.ts` declares `positiveInt` and `nonNegativeInt` helper functions and `readConfig()`'s return object uses them for all four `VendorThresholds` fields (`retryCount` via `nonNegativeInt`, the other three via `positiveInt`)
    - `paystackRefund`'s `retryCount: 0` default is never overridden by the new guard — `nonNegativeInt` accepts exactly `0` as valid
    - The two new test cases described above exist in `resilience.service.spec.ts` and `cd backend && npx jest src/resilience/__tests__/resilience.service.spec.ts --silent` exits 0
  </acceptance_criteria>
  <done>Malformed DB-sourced resilience config values (non-numeric, NaN-producing, negative) fall back to `RESILIENCE_DEFAULTS` per-key instead of silently producing a broken timeout/breaker/retry configuration platform-wide.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Narrow isTransientError classification (WR-04) without breaking CR-01's retry-after-timeout</name>
  <files>backend/src/resilience/resilience.service.ts, backend/src/resilience/__tests__/resilience.service.spec.ts</files>
  <read_first>
    backend/src/resilience/resilience.service.ts `isTransientError()` (lines 159-163) — current catch-all: any error with no `.response` returns `true`
    node_modules/cockatiel/dist/errors/TaskCancelledError.js and node_modules/cockatiel/dist/TimeoutPolicy.js lines 56-87 — cockatiel's aggressive `timeout()` policy races `fn(context)` against a timer and throws a `TaskCancelledError` (`isTaskCancelledError: true`, no `.response`, no `.code`) when the timer wins; this is what the retry policy's `handleWhen(isTransientError)` filter sees on every per-attempt timeout after Task 1's CR-01 fix
    .planning/phases/11-resilience-wrapping/11-REVIEW.md WR-04 section (issue: a bare `TypeError`/programming bug with no `.response` is also classified as transient today, letting application bugs trip the circuit breaker)
    Task 1 of this plan (CR-01 fix) — CRITICAL CONSTRAINT: this task's narrowing MUST NOT stop classifying `TaskCancelledError`-shaped errors as transient, or every per-attempt timeout introduced/fixed by Task 1 would silently stop triggering a retry, defeating CR-01 end-to-end
  </read_first>
  <behavior>
    - Test: `fn` rejects with a plain `new TypeError('boom')` (no `.response`, no `.code`, no `isTaskCancelledError`) for `failureThreshold + 5` sequential `execute('paystack', fn)` calls. Assert `fn.mock.calls.length` equals the number of `execute()` calls made — i.e. `fn` is invoked on every single call with no fail-fast short-circuit — mirroring the existing "breaker stays closed on 4xx business errors" test structure, proving a bare application-bug shape no longer counts toward the breaker's consecutive-failure threshold.
    - Test: `fn` rejects with an object shaped `{ code: 'ECONNREFUSED' }` (no `.response`) for a single `execute('paystack', fn)` call. Assert `fn.mock.calls.length` is greater than 1 (paystack's default `retryCount: 2` means cockatiel retries this), proving genuine network-level errors remain classified as transient.
    - Test: `fn` rejects with an object shaped `{ isTaskCancelledError: true }` (mirroring cockatiel's real `TaskCancelledError` shape) for a single `execute('paystack', fn)` call. Assert `fn.mock.calls.length` is greater than 1 — this is the critical regression guard proving Task 1's per-attempt timeout cancellation still triggers a retry after this task's narrowing is applied.
  </behavior>
  <action>
    Rewrite `isTransientError(err)` in `resilience.service.ts` with this exact decision order: (1) if `(err as any)?.response?.status` is defined, keep the existing check — return `true` only for `408`, `429`, or `>= 500`; (2) else if `(err as any)?.isTaskCancelledError === true`, return `true` — this check MUST come before any network-code check, since cockatiel's `TaskCancelledError` has neither `.response` nor `.code`; (3) else if `(err as any)?.code` is a string and is one of `ECONNREFUSED`, `ETIMEDOUT`, `ECONNRESET`, `ENOTFOUND`, `EAI_AGAIN`, `ABORT_ERR`, return `true`; (4) else if `(err as any)?.name === 'AbortError'` (fetch/undici abort shape), return `true`; (5) otherwise return `false` — a bare application error with none of the above shapes no longer counts toward retry attempts or the circuit breaker's failure accounting. Update the function's leading comment to describe this narrowed contract and cite WR-04/`11-REVIEW.md`, explicitly noting the `isTaskCancelledError` special-case exists to preserve CR-01's retry-after-per-attempt-timeout behavior.
  </action>
  <verify>
    <automated>cd backend && npx jest src/resilience/__tests__/resilience.service.spec.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `isTransientError`'s body contains an explicit `isTaskCancelledError === true` check that returns `true`, positioned before the final catch-all `return false`
    - The three new test cases above exist in `resilience.service.spec.ts` and `cd backend && npx jest src/resilience/__tests__/resilience.service.spec.ts --silent` exits 0
    - `cd backend && npx jest src/resilience --silent` (all resilience suite files, including Task 1's new `retry-timeout-composition.spec.ts`) exits 0 with zero regressions
  </acceptance_criteria>
  <done>`isTransientError` no longer classifies arbitrary application bugs as vendor-transient failures, while still correctly treating cockatiel's own per-attempt timeout cancellation and recognized network-level errors as transient — preserving CR-01's retry-after-timeout behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ResilienceService.execute()` internal policy composition | The single choke-point every vendor call site (Plans 01-05, and this gap closure's Plans 07-08) routes through — a defect here propagates identically to all 7 vendors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Denial of Service (retry amplification) | `resilience.service.ts` composition order | mitigate | Task 1's reorder makes `timeout` per-attempt (not per-sequence), so retries actually get their configured attempts instead of being silently starved by a shared timeout window — this is the correctness fix that makes `ExponentialBackoff` + circuit-breaker fail-fast (Plan 01's original T-11-02 mitigation) work as designed |
| T-11-04 | Denial of Service (false-positive circuit trip) | `resilience.service.ts` `isTransientError` | mitigate | Task 3's narrowing stops application bugs (non-network, non-cancellation shaped errors) from counting toward the breaker's failure threshold, while Task 3's dedicated `isTaskCancelledError` test proves genuine per-attempt timeouts (introduced correctly by Task 1) still count — closing WR-04 without reopening CR-01 |
</threat_model>

<verification>
- `cd backend && npx jest src/resilience --silent`
- `cd backend && npm test`
</verification>

<success_criteria>
- `resilience.service.ts`'s `wrap(...)` composition places `retry(...)` before `timeout(...)`, giving each attempt its own timeout budget
- A fake-timer regression test proves the fix (would fail under the old ordering, passes under the new one)
- DB-sourced resilience config values that are malformed (NaN, negative, non-numeric) fall back to `RESILIENCE_DEFAULTS` per-key
- `isTransientError` no longer treats arbitrary application bugs as vendor-transient, while still correctly retrying cockatiel's own per-attempt timeout cancellations
- Full backend regression suite (`npm test`) passes with zero failures
- RESIL-02's live-dashboard human-verification item remains explicitly unaddressed by this plan (out of scope — code-level wiring already exists and is unit-tested per `11-VERIFICATION.md` truth #3)
</success_criteria>

<output>
After completion, create `.planning/phases/11-resilience-wrapping/11-06-SUMMARY.md`
</output>
