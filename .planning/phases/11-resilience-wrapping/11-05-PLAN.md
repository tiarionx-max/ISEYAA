---
phase: 11-resilience-wrapping
plan: 05
type: execute
wave: 3
depends_on: ["11-02", "11-03", "11-04"]
files_modified:
  - backend/src/resilience/__tests__/vendor-outage-isolation.spec.ts
autonomous: true
requirements: [RESIL-01, RESIL-02]

must_haves:
  truths:
    - "A simulated Paystack outage opens only the Paystack circuit — the S3 circuit (a different vendor policy on the same ResilienceService instance) is completely unaffected and continues succeeding"
    - "Once the Paystack circuit is open, no further calls are dispatched to the (mock) vendor function — proving fail-fast, not another full retry+timeout cycle per request"
    - "The full backend test suite (all vendor-wrapping changes from Plans 01-04 combined) passes with zero regressions"
    - "No global axios interceptor exists that would double up retry behavior alongside cockatiel (RESEARCH.md Open Question 2, closed)"
  artifacts:
    - path: "backend/src/resilience/__tests__/vendor-outage-isolation.spec.ts"
      provides: "Deterministic, DB-free proof of ROADMAP.md Phase 11 success criterion 2 (single-vendor-outage isolation) at the ResilienceService facade level"
  key_links:
    - from: "backend/src/resilience/__tests__/vendor-outage-isolation.spec.ts"
      to: "backend/src/resilience/resilience.service.ts"
      via: "real (non-mocked) ResilienceService instance, only PrismaService mocked"
      pattern: "new ResilienceService|Test.createTestingModule"
---

<objective>
Prove, with a single deterministic and fast (DB-free) automated test, that a simulated single-vendor outage degrades only that vendor's dependent feature while a different vendor's policy remains fully operational on the same `ResilienceService` instance — the concrete expression of ROADMAP.md Phase 11 success criterion 2. Then run the full backend test suite to confirm zero regressions across all four wrapping plans (01-04) and close RESEARCH.md's remaining open question about axios interceptor compounding.

Purpose: Plans 02-04 each verify their OWN vendor's wrapping in isolation; this plan is the one place that proves CROSS-vendor isolation — the actual product guarantee this phase exists to deliver — and serves as the final regression gate before the phase is considered complete.
Output: New `vendor-outage-isolation.spec.ts`; full `npm test` green; axios-interceptor open question closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-resilience-wrapping/11-CONTEXT.md
@.planning/phases/11-resilience-wrapping/11-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-vendor circuit isolation test (success criterion 2 proof)</name>
  <files>backend/src/resilience/__tests__/vendor-outage-isolation.spec.ts</files>
  <read_first>
    backend/src/resilience/resilience.service.ts (final Plan 01 implementation — execute(vendor, fn) facade, onModuleInit construction)
    backend/src/resilience/resilience.types.ts (Vendor union, RESILIENCE_DEFAULTS — note `paystack.failureThreshold` default is 5)
    backend/src/resilience/__tests__/resilience.service.spec.ts (Plan 01 — existing PrismaService/Sentry/OTel mocking conventions to reuse verbatim, so this file needs no new mock setup beyond what Plan 01 already established)
  </read_first>
  <behavior>
    - a REAL `ResilienceService` (not mocked — only its `PrismaService` dependency is mocked) is instantiated via `Test.createTestingModule({ providers: [ResilienceService, { provide: PrismaService, useValue: { platformConfig: { findMany: jest.fn().mockResolvedValue([]) } } }] }).compile()`, then `onModuleInit()` is invoked so all 6 default vendor policies are built
    - calling `service.execute('paystack', fn)` where `fn` always rejects with a transient-shaped error (`{ response: { status: 500 } }`) exactly `RESILIENCE_DEFAULTS.paystack.failureThreshold` (5) times opens the Paystack breaker; a 6th call rejects immediately WITHOUT invoking `fn` again — assert `fn.mock.calls.length` stops growing past 5
    - immediately after the Paystack circuit is open, calling `service.execute('s3', fn2)` where `fn2` resolves `'uploaded-ok'` completes successfully and returns `'uploaded-ok'` — proving the S3 policy's state is fully independent of Paystack's, in the SAME test run and SAME `ResilienceService` instance
    - a caller pattern mirroring `PaystackService.initiatePayment`'s actual catch-and-rethrow (`try { await service.execute('paystack', fn); } catch { throw new ServiceUnavailableException('...'); }`) against the now-open Paystack circuit throws `ServiceUnavailableException` — confirming the fail-fast contract end-to-end from `ResilienceService` through to the D-05 exception shape
  </behavior>
  <action>
    Create `backend/src/resilience/__tests__/vendor-outage-isolation.spec.ts` implementing the 4 behaviors above as separate `it()` blocks within a single `describe('Vendor outage isolation (Phase 11 success criterion 2)', ...)` suite. Use the SAME `jest.mock('@sentry/nestjs', ...)` and `jest.mock('@opentelemetry/api', ...)` mock blocks established in `resilience.service.spec.ts` (Plan 01) so `onBreak`'s Sentry/OTel calls do not throw during this test. Do NOT use `test/jest-e2e.json` or a `.e2e-spec.ts` filename — this test requires no database and no HTTP server; it must run via the plain `backend/jest.config.js` config (`testRegex: '.*\\.spec\\.ts$'`) alongside `resilience.service.spec.ts` in the standard `npx jest src/resilience` sampling loop.
  </action>
  <verify>
    <automated>cd backend && npx jest src/resilience/__tests__/vendor-outage-isolation.spec.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists at the path above with exactly the 4 behaviors as separate `it()` blocks inside one `describe`
    - `npx jest src/resilience/__tests__/vendor-outage-isolation.spec.ts` exits 0
    - The Paystack-failure test explicitly asserts `fn.mock.calls.length` stabilizes at 5 (or the configured `failureThreshold`) and does not continue growing on the 6th+ call
    - The S3 success test in the same run asserts a return value of `'uploaded-ok'`, proving no cross-vendor state leakage
  </acceptance_criteria>
  <done>A single fast, deterministic test proves cross-vendor circuit isolation — the ResilienceService-level expression of "a single vendor outage degrades only the dependent feature, not the whole API."</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite regression run + axios-interceptor open-question closure</name>
  <files>backend/package.json</files>
  <read_first>
    backend/package.json (test scripts — `"test": "jest"` at line 12)
  </read_first>
  <action>
    Run `cd backend && npm test` (the full Jest suite across every `*.spec.ts` file in the project, per `backend/jest.config.js`'s `testRegex`) and confirm zero failing suites — this is the final regression gate for Plans 01-04's constructor changes to `PaystackService`, `S3Service`, `NotificationsService`, `AiService`, `AuthService`, and `DeliveryService`. If any OTHER pre-existing spec file (not touched by Plans 01-04) fails because it constructs one of these six services as a REAL provider (via `useClass` or bare class reference) rather than a `useValue` mock, add `{ provide: ResilienceService, useValue: { execute: jest.fn((vendor, fn) => fn()) } }` to that file's provider array — do not modify production source files in this task, only test provider arrays if a regression surfaces.
    Separately, close RESEARCH.md's Open Question 2 by running `grep -rn "axios.interceptors" backend/src` — confirm zero matches, meaning no global axios interceptor exists anywhere in the codebase that could compound retry behavior alongside cockatiel's own retry policies (this confirms only the Anthropic SDK needed the `maxRetries: 0` treatment from Plan 03, not any other vendor). No source change results from this grep — it is a verification-only step; record its (expected-empty) output in the plan's SUMMARY.
  </action>
  <verify>
    <automated>cd backend && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npm test` exits 0 with zero failing test suites across the entire backend
    - `grep -rn "axios.interceptors" backend/src` returns no matches
  </acceptance_criteria>
  <done>Full backend suite green; no other spec file broken by the six services' new ResilienceService constructor dependency; axios-interceptor compounding risk confirmed absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ResilienceService` (in-process, per-vendor breaker state) | Confirms no shared mutable state leaks between vendor policies — a compromised/degraded vendor cannot influence another vendor's availability decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Denial of Service (retry amplification) | Cross-vendor isolation test | mitigate (verified) | Task 1's fail-fast assertion (`fn.mock.calls.length` stabilizes) is the automated proof that the circuit breaker — not unbounded retry — governs behavior once a vendor is confirmed down, closing the loop on Plan 01's implementation |
| T-11-04 | Denial of Service (false-positive circuit trip / cross-vendor lockout) | Cross-vendor isolation test | mitigate (verified) | Task 1's S3-succeeds-while-Paystack-is-open assertion is the automated proof that no vendor's outage can degrade an unrelated vendor's availability — the core product guarantee of ROADMAP.md success criterion 2 |

This plan performs no new mitigation work — it is the verification gate confirming Plans 01-04's mitigations hold under a combined, cross-vendor scenario and across the full regression suite.
</threat_model>

<verification>
- `cd backend && npx jest src/resilience --silent`
- `cd backend && npm test`
- `grep -rn "axios.interceptors" backend/src` (expect zero matches)
</verification>

<success_criteria>
- Cross-vendor circuit isolation is proven by a dedicated, fast, deterministic automated test (RESIL-01/RESIL-02 combined proof of ROADMAP.md success criterion 2)
- Full backend test suite passes with zero regressions from the entire phase's changes
- RESEARCH.md's axios-interceptor open question is closed with an empty grep result
</success_criteria>

<output>
After completion, create `.planning/phases/11-resilience-wrapping/11-05-SUMMARY.md`
</output>
