---
phase: 11-resilience-wrapping
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/ai/ai.service.ts
  - backend/src/modules/ai/__tests__/ai.service.spec.ts
  - backend/src/modules/auth/auth.service.ts
  - backend/src/modules/auth/__tests__/auth.service.spec.ts
  - backend/src/modules/delivery/delivery.service.ts
  - backend/src/modules/delivery/__tests__/delivery.service.spec.ts
autonomous: true
gap_closure: true
requirements: [RESIL-01]

must_haves:
  truths:
    - "AiService's streamChatWithTools, streamItinerary, and getLgaIntelligence forward cockatiel's AbortSignal into the Anthropic SDK's request options, so an aggressive timeout actually cancels the in-flight stream/request instead of only abandoning the caller's promise"
    - "auth.service.ts's sendTermii forwards the AbortSignal into fetch()'s signal option"
    - "delivery.service.ts's sendTermiiDeliveryOtp forwards the AbortSignal into fetch()'s signal option"
    - "Every pre-existing test in ai.service.spec.ts, auth.service.spec.ts, and delivery.service.spec.ts still passes after their resilience mock is updated to invoke the callback with a context object instead of no argument"
  artifacts:
    - path: "backend/src/modules/ai/ai.service.ts"
      provides: "All 3 Anthropic call sites destructure { signal } and pass it as the Anthropic SDK's RequestOptions second argument"
    - path: "backend/src/modules/auth/auth.service.ts"
      provides: "sendTermii's fetch() call forwards signal"
    - path: "backend/src/modules/delivery/delivery.service.ts"
      provides: "sendTermiiDeliveryOtp's fetch() call forwards signal"
  key_links:
    - from: "backend/src/modules/ai/ai.service.ts"
      to: "this.anthropic.messages.stream/create"
      via: "RequestOptions second argument { signal }"
      pattern: "\\{ signal \\}\\)"
---

<objective>
Close CR-02 (missing AbortSignal propagation) from `11-VERIFICATION.md`/`11-REVIEW.md` for the remaining 5 call sites this gap closure batch covers: `ai.service.ts`'s 3 Anthropic calls (`streamChatWithTools`, `streamItinerary`, `getLgaIntelligence`), `auth.service.ts`'s Termii `fetch()` call (`sendTermii`), and `delivery.service.ts`'s Termii `fetch()` call (`sendTermiiDeliveryOtp`). All 3 currently discard cockatiel's `context.signal`, meaning an "aggressive" timeout stops the caller from waiting but never actually cancels the in-flight Anthropic stream or Termii SMS request. This plan threads `signal` through all 5 sites and fixes each affected spec file's resilience mock so the new `{ signal }` destructuring doesn't crash any of the 3 files' pre-existing test suites.

Purpose: Together with Plan 11-07 (paystack/s3/notifications), this closes CR-02 across all 6 files `11-VERIFICATION.md` names, using a disjoint file set so both plans run in parallel without conflict. Plan 11-06 (resilience.service.ts composition + hardening) is also file-disjoint from this plan.
Output: `signal` forwarded at 5 call sites across 3 production files; all 3 corresponding spec files' mocks fixed to pass a context object instead of calling `fn()` bare.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-resilience-wrapping/11-CONTEXT.md
@.planning/phases/11-resilience-wrapping/11-VERIFICATION.md
@.planning/phases/11-resilience-wrapping/11-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread AbortSignal into ai.service.ts (3 Anthropic call sites)</name>
  <files>backend/src/modules/ai/ai.service.ts, backend/src/modules/ai/__tests__/ai.service.spec.ts</files>
  <read_first>
    backend/src/modules/ai/ai.service.ts `streamChatWithTools` (lines 279-287: `this.resilience.execute('anthropic', async () => this.anthropic.messages.stream({...}))`), `streamItinerary` (lines 491-497, same pattern), `getLgaIntelligence` (lines 532-543: `this.resilience.execute('anthropic', () => this.anthropic.messages.create({...}))`) — all 3 discard cockatiel's context
    node_modules/@anthropic-ai/sdk/resources/messages/messages.d.ts (`stream(body: MessageStreamParams, options?: RequestOptions): MessageStream;` and `create(body: MessageCreateParamsBase, options?: RequestOptions): APIPromise<...>` — both take a SECOND argument for request options)
    node_modules/@anthropic-ai/sdk/internal/request-options.d.ts line 20 (`signal?: AbortSignal | undefined | null;` — confirms the Anthropic SDK's `RequestOptions.signal` key name)
    backend/src/resilience/resilience.service.ts `execute<T>()` signature (lines 72-79) — callback receives `{ signal }`
    backend/src/modules/ai/__tests__/ai.service.spec.ts (line 79 `execute: jest.fn((_vendor: string, fn: () => any) => fn())`, line 99 `mockImplementation` reset — both call `fn()` bare; this MUST change or the new destructuring throws)
  </read_first>
  <action>
    In `ai.service.ts`, change all 3 call sites to destructure `{ signal }` and pass it as the Anthropic SDK's second (`RequestOptions`) argument: `streamChatWithTools`'s `this.resilience.execute('anthropic', async () => this.anthropic.messages.stream({ model, max_tokens, system, tools, messages }))` becomes `this.resilience.execute('anthropic', async ({ signal }) => this.anthropic.messages.stream({ model, max_tokens, system, tools, messages }, { signal }))`; `streamItinerary` follows the identical pattern (`messages.stream({...}, { signal })`); `getLgaIntelligence`'s `this.resilience.execute('anthropic', () => this.anthropic.messages.create({...}))` becomes `this.resilience.execute('anthropic', ({ signal }) => this.anthropic.messages.create({...}, { signal }))`. Do not alter the request body objects themselves — only add the second `{ signal }` argument. In `ai.service.spec.ts`, update BOTH the top-level `mockResilience.execute` initializer and the `beforeEach`'s `mockImplementation` reset to invoke `fn({ signal: undefined })` instead of bare `fn()`.
  </action>
  <verify>
    <automated>cd backend && npx jest src/modules/ai/__tests__/ai.service.spec.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "{ signal }" backend/src/modules/ai/ai.service.ts` shows 3 matches (one per call site: `streamChatWithTools`, `streamItinerary`, `getLgaIntelligence`)
    - Each of the 3 `this.anthropic.messages.stream(...)`/`.create(...)` calls now passes a second argument object containing `signal`
    - `ai.service.spec.ts`'s default `mockResilience.execute` mock invokes `fn` with an object containing a `signal` key
    - `cd backend && npx jest src/modules/ai/__tests__/ai.service.spec.ts --silent` exits 0 with zero regressions in pre-existing test cases
  </acceptance_criteria>
  <done>All 3 Anthropic call sites forward cockatiel's real AbortSignal into the SDK's RequestOptions, so an aggressive timeout actually cancels the in-flight stream/request instead of leaving it running unobserved in the background.</done>
</task>

<task type="auto">
  <name>Task 2: Thread AbortSignal into auth.service.ts and delivery.service.ts Termii calls</name>
  <files>backend/src/modules/auth/auth.service.ts, backend/src/modules/auth/__tests__/auth.service.spec.ts, backend/src/modules/delivery/delivery.service.ts, backend/src/modules/delivery/__tests__/delivery.service.spec.ts</files>
  <read_first>
    backend/src/modules/auth/auth.service.ts `sendTermii()` (lines 302-315): `this.resilience.execute('termiiAuth', () => fetch('https://v3.api.termii.com/api/sms/send', { method: 'POST', headers, body }))` — discards context; surrounding Termii→Twilio→console-stub fallback chain (D-03) must remain untouched
    backend/src/modules/delivery/delivery.service.ts `sendTermiiDeliveryOtp()` (lines 330-343): `this.resilience.execute('termiiDelivery', () => fetch('https://v3.api.termii.com/api/sms/send', { method: 'POST', headers, body }))` — discards context; surrounding log-and-swallow fallback (D-03) must remain untouched
    backend/src/resilience/resilience.types.ts (`Vendor` union includes distinct `'termiiAuth'` and `'termiiDelivery'` keys per D-08 — do not merge these two call sites' policies)
    backend/src/modules/auth/__tests__/auth.service.spec.ts (line 46 `execute: jest.fn((vendor: string, fn: () => any) => fn())` — calls `fn()` bare; this MUST change)
    backend/src/modules/delivery/__tests__/delivery.service.spec.ts (line 150 `execute: jest.fn((vendor: string, fn: () => any) => fn())` — calls `fn()` bare; this MUST change)
  </read_first>
  <action>
    In `auth.service.ts`, change `sendTermii()`'s call site from `this.resilience.execute('termiiAuth', () => fetch(url, { method: 'POST', headers, body }))` to `this.resilience.execute('termiiAuth', ({ signal }) => fetch(url, { method: 'POST', headers, body, signal }))` — add `signal` as a new key in `fetch`'s init object, alongside `method`/`headers`/`body`. Do not touch the surrounding Termii→Twilio→console-stub fallback logic (D-03) — only the `fetch()` call itself changes. In `delivery.service.ts`, apply the identical pattern to `sendTermiiDeliveryOtp()`'s `fetch()` call, again without touching the surrounding log-and-swallow fallback. In BOTH `auth.service.spec.ts` and `delivery.service.spec.ts`, update the `execute: jest.fn((vendor: string, fn: () => any) => fn())` initializer (and any other place in the file that invokes the mock's callback bare) to invoke `fn({ signal: undefined })` instead, so the new destructuring signature does not throw on any pre-existing test.
  </action>
  <verify>
    <automated>cd backend && npx jest src/modules/auth/__tests__/auth.service.spec.ts src/modules/delivery/__tests__/delivery.service.spec.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "signal" backend/src/modules/auth/auth.service.ts` shows `signal` present in `sendTermii`'s `fetch()` init object
    - `grep -n "signal" backend/src/modules/delivery/delivery.service.ts` shows `signal` present in `sendTermiiDeliveryOtp`'s `fetch()` init object
    - `termiiAuth` and `termiiDelivery` remain distinct `resilience.execute()` vendor-key calls (D-08) — neither call site's vendor string argument changes
    - Both `auth.service.spec.ts` and `delivery.service.spec.ts`'s mock `execute` implementations invoke `fn` with an object containing a `signal` key
    - `cd backend && npx jest src/modules/auth/__tests__/auth.service.spec.ts src/modules/delivery/__tests__/delivery.service.spec.ts --silent` exits 0 with zero regressions
  </acceptance_criteria>
  <done>Both independent Termii call sites forward cockatiel's AbortSignal into fetch(), so an aggressive timeout actually cancels the in-flight SMS request instead of leaving it running unobserved; each call site's existing Twilio/log-and-swallow fallback chain (D-03/D-08) is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ai.service.ts` → Anthropic streaming API | An uncancelled timed-out stream keeps consuming Anthropic API quota/cost after the caller has given up |
| `auth.service.ts`/`delivery.service.ts` → Termii SMS API | An uncancelled timed-out OTP send request may still deliver an SMS after the caller has already fallen back to Twilio/logged a stub, wasting Termii send credits but not creating a financial/security risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Denial of Service (retry amplification / resource leak) | `ai.service.ts` Anthropic streams | mitigate | Task 1 threads `signal` into `messages.stream`/`messages.create` so an aggressive timeout actually terminates the underlying stream/request instead of leaving it consuming API resources in the background after the caller has moved on |
| T-11-03 | Information Disclosure (PII in fallback logging) | `auth.service.ts`/`delivery.service.ts` Termii fetch calls | accept (unchanged) | This plan only adds a `signal` key to existing `fetch()` init objects — it does not touch any logging or fallback-chain logic; Plan 01's PII-logging discipline (never log phone/OTP raw values beyond existing conventions) is untouched |
</threat_model>

<verification>
- `cd backend && npx jest src/modules/ai/__tests__/ai.service.spec.ts src/modules/auth/__tests__/auth.service.spec.ts src/modules/delivery/__tests__/delivery.service.spec.ts --silent`
- `cd backend && npm test`
</verification>

<success_criteria>
- All 3 Anthropic call sites in `ai.service.ts` forward `signal` as the SDK's `RequestOptions` second argument
- Both Termii call sites (`auth.service.ts`, `delivery.service.ts`) forward `signal` into `fetch()`'s init object, with their independent fallback chains (D-03/D-08) unchanged
- All 3 affected spec files' mocks updated so existing tests do not crash on the new `{ signal }` destructuring
- Full backend regression suite passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/11-resilience-wrapping/11-08-SUMMARY.md`
</output>
