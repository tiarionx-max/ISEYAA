---
phase: 14-ministry-dashboard
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/common/services/ministry-pdf.service.ts
  - backend/src/modules/ministry/ministry.controller.ts
  - backend/src/common/services/__tests__/ministry-pdf.service.spec.ts
autonomous: true
requirements: [MIN-06]

must_haves:
  truths:
    - "The Visitor Entries PDF export renders every row cleanly without overlap for real (UUID-length) LGA ID data — closing CR-02 (14-VERIFICATION.md gap 2, Roadmap SC5)"
    - "MinistryPdfService.renderTable() is generically height-aware and page-break-aware for ANY column set/row count, not a one-off fix scoped only to the Visitor Entries report"
    - "The raw `lgaId` UUID column is removed from the Visitor Entries PDF rendering path only — the CSV export still includes lgaId (lgaName already carries the human-readable identity for PDF viewers)"
  artifacts:
    - path: "backend/src/common/services/ministry-pdf.service.ts"
      provides: "renderTable() measures each cell via doc.heightOfString(), advances doc.y by the row's max cell height, and calls doc.addPage() with header re-print when content would exceed the page"
      contains: "heightOfString"
    - path: "backend/src/modules/ministry/ministry.controller.ts"
      provides: "A PDF-only column set for the Visitor Entries export that excludes the raw lgaId UUID column; the CSV export's column set (with lgaId) is unchanged"
      contains: "VISITOR_ENTRIES_PDF_COLUMNS"
    - path: "backend/src/common/services/__tests__/ministry-pdf.service.spec.ts"
      provides: "Tests proving row-height measurement is invoked for UUID-length cell content, and that doc.addPage() is called with header re-print when rows exceed one page"
      contains: "CR-02"
  key_links:
    - from: "backend/src/modules/ministry/ministry.controller.ts exportVisitorEntries()"
      to: "backend/src/common/services/ministry-pdf.service.ts renderPdf()"
      via: "PDF branch passes VISITOR_ENTRIES_PDF_COLUMNS (no lgaId), CSV branch keeps VISITOR_ENTRIES_COLUMNS (with lgaId)"
      pattern: "VISITOR_ENTRIES_PDF_COLUMNS"
    - from: "backend/src/common/services/ministry-pdf.service.ts renderTable()"
      to: "pdfkit PDFDocument"
      via: "doc.heightOfString() measurement + doc.addPage() page-break with header re-print"
      pattern: "doc\\.addPage\\(\\)"
---

<objective>
Fix CR-02: `MinistryPdfService.renderTable()` divides page width evenly across columns and advances rows by a fixed `doc.moveDown(0.4)` with no height-aware layout or page-break logic. `VISITOR_ENTRIES_COLUMNS` (shared by CSV and PDF) includes a raw `lgaId` UUID column (~36 chars, 200pt+ at 10pt Helvetica) in a 5-column table dividing ~495pt evenly (~99pt/column) — the UUID wraps onto a second line with no reserved vertical space, so the next row overlaps it. This is a gap-closure fix for a BLOCKER-severity defect confirmed unfixed by both `14-REVIEW.md` (CR-02) and `14-VERIFICATION.md` (gap 2) against the already-executed, otherwise-verified Phase 14 codebase.

Purpose: Restore MIN-06's "every report can be exported as a formatted, branded PDF" guarantee for the Visitor Entries report on real (non-fixture) production data, and make the table renderer robust for any future report shape (generic fix, not report-specific).
Output: `renderTable()` measures cell height via `doc.heightOfString()`, advances `doc.y` by the row's max cell height, and calls `doc.addPage()` (re-printing the header) when a row would exceed the page. The Visitor Entries PDF export drops the raw `lgaId` column (CSV export keeps it). New regression tests cover both the height-aware measurement and the page-break/header-reprint behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ministry-dashboard/14-CONTEXT.md
@.planning/phases/14-ministry-dashboard/14-REVIEW.md
@.planning/phases/14-ministry-dashboard/14-VERIFICATION.md

<interfaces>
<!-- Current buggy state of backend/src/common/services/ministry-pdf.service.ts's
     renderTable() (lines 125-150), included verbatim. PAGE_LEFT=50, PAGE_RIGHT=545,
     PAGE_WIDTH=495 are module-level constants already defined above this method. -->
```typescript
private renderTable(doc: PDFKit.PDFDocument, section: MinistryPdfSection): void {
  if (section.rows.length === 0) {
    doc.fontSize(11).fillColor('#666').text('No data for this period.');
    return;
  }

  const colWidth = PAGE_WIDTH / section.columns.length;

  // Header row.
  const headerY = doc.y;
  doc.fontSize(10).fillColor('#1A6B3C');
  section.columns.forEach((col, i) => {
    doc.text(col.label.toUpperCase(), PAGE_LEFT + i * colWidth, headerY, { width: colWidth });
  });
  doc.moveDown(0.5);

  // Data rows.
  for (const row of section.rows) {
    const rowY = doc.y;
    doc.fontSize(9).fillColor('#1C2B2B');
    section.columns.forEach((col, i) => {
      doc.text(String(row[col.key] ?? ''), PAGE_LEFT + i * colWidth, rowY, { width: colWidth });
    });
    doc.moveDown(0.4);
  }
}
```
`doc` is typed `PDFKit.PDFDocument` and `doc.page.height` / `doc.page.margins.bottom` are both available on this type (pdfkit's standard `PDFPage` shape) — usable directly for the page-bottom boundary check without a cast.

<!-- Current buggy state of backend/src/modules/ministry/ministry.controller.ts's
     VISITOR_ENTRIES_COLUMNS (lines 21-27) and the exportVisitorEntries() PDF
     branch (lines 97-105) that reuses it. -->
```typescript
const VISITOR_ENTRIES_COLUMNS: MinistryPdfColumn[] = [
  { key: 'lgaId', label: 'LGA ID' },
  { key: 'lgaName', label: 'LGA' },
  { key: 'month', label: 'Month' },
  { key: 'userRole', label: 'Visitor Role' },
  { key: 'count', label: 'Count' },
];
// ...
async exportVisitorEntries(@Query() query: MinistryQueryDto, @Res() res: Response) {
  const rows = await this.ministryService.getVisitorEntriesByLgaAndMonth(query.from, query.to, query.lgaId);
  const flatRows = rows as unknown as Record<string, unknown>[];
  if (query.format === 'pdf') {
    const buffer = await this.ministryPdfService.renderPdf({
      title: 'Visitor Entries',
      sections: [{ columns: VISITOR_ENTRIES_COLUMNS, rows: flatRows }],
    });
    // ...
  }
  const csv = await this.csvExportService.toCsv(flatRows, VISITOR_ENTRIES_COLUMNS.map((c) => c.key));
  // ...
}
```
`VISITOR_ENTRIES_COLUMNS` (with `lgaId`) MUST remain unchanged and continue to drive the CSV branch (`csvExportService.toCsv` call) — only the PDF branch's `sections[0].columns` value changes to a new, separate constant.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop the raw `lgaId` UUID column from the Visitor Entries PDF export path (CSV unaffected)</name>
  <files>backend/src/modules/ministry/ministry.controller.ts</files>
  <read_first>
    backend/src/modules/ministry/ministry.controller.ts (full file, current state — VISITOR_ENTRIES_COLUMNS at line 21, exportVisitorEntries() at line 90)
    .planning/phases/14-ministry-dashboard/14-REVIEW.md (CR-02 section — original finding, notes lgaName already carries the human-readable identity)
    .planning/phases/14-ministry-dashboard/14-VERIFICATION.md (gap 2 in frontmatter — confirms VISITOR_ENTRIES_COLUMNS still includes lgaId in the PDF section as of the last verification pass)
  </read_first>
  <action>
    In `backend/src/modules/ministry/ministry.controller.ts`, add a new module-level constant `VISITOR_ENTRIES_PDF_COLUMNS: MinistryPdfColumn[]` immediately after the existing `VISITOR_ENTRIES_COLUMNS` constant, containing the same 4 remaining entries in the same order but WITHOUT the `{ key: 'lgaId', label: 'LGA ID' }` entry — i.e. `[{ key: 'lgaName', label: 'LGA' }, { key: 'month', label: 'Month' }, { key: 'userRole', label: 'Visitor Role' }, { key: 'count', label: 'Count' }]`. In `exportVisitorEntries()`'s `if (query.format === 'pdf')` branch, change `sections: [{ columns: VISITOR_ENTRIES_COLUMNS, rows: flatRows }]` to `sections: [{ columns: VISITOR_ENTRIES_PDF_COLUMNS, rows: flatRows }]`. Do NOT change the CSV branch below it — it must continue to call `csvExportService.toCsv(flatRows, VISITOR_ENTRIES_COLUMNS.map((c) => c.key))` using the original 5-column `VISITOR_ENTRIES_COLUMNS` constant, unchanged. Do not touch `PURPOSE_BREAKDOWN_COLUMNS` or any of the 4 revenue-related column constants — this defect is scoped to Visitor Entries only (the only export with a raw UUID column).
  </action>
  <acceptance_criteria>
    - `ministry.controller.ts` defines `VISITOR_ENTRIES_PDF_COLUMNS` with exactly 4 entries (`lgaName`, `month`, `userRole`, `count`) and no `lgaId` entry.
    - `VISITOR_ENTRIES_COLUMNS` still has exactly 5 entries including `lgaId`, unchanged from its original definition.
    - `exportVisitorEntries()`'s PDF branch passes `VISITOR_ENTRIES_PDF_COLUMNS` to `ministryPdfService.renderPdf()`'s `sections[0].columns`; its CSV branch still passes `VISITOR_ENTRIES_COLUMNS.map((c) => c.key)` to `csvExportService.toCsv()`.
    - `cd backend && npx tsc --noEmit -p tsconfig.build.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.build.json</automated>
  </verify>
  <done>Visitor Entries PDF export no longer includes the raw lgaId UUID column; Visitor Entries CSV export is byte-for-byte unchanged (still includes lgaId).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Height-aware, page-break-aware table layout in MinistryPdfService.renderTable()</name>
  <files>backend/src/common/services/ministry-pdf.service.ts, backend/src/common/services/__tests__/ministry-pdf.service.spec.ts</files>
  <read_first>
    backend/src/common/services/ministry-pdf.service.ts (full file, current state — renderTable() at line 125, PAGE_LEFT/PAGE_RIGHT/PAGE_WIDTH constants at lines 38-40)
    backend/src/common/services/__tests__/ministry-pdf.service.spec.ts (full file, current state — existing test style: `jest.spyOn(PDFDocument.prototype, '<method>')`, `service.renderPdf({...})`, buffer/`%PDF-` assertions)
    .planning/phases/14-ministry-dashboard/14-REVIEW.md (CR-02 section — describes the required doc.heightOfString()/doc.addPage() fix in detail)
    .planning/phases/14-ministry-dashboard/14-VERIFICATION.md (gap 2 in frontmatter — confirms renderTable() still has no height-aware layout or page-break logic as of the last verification pass)
  </read_first>
  <behavior>
    - Given a table with a cell value long enough to wrap within its column width (e.g. a 36-character UUID in a ~99pt-wide column), `renderTable()` calls `doc.heightOfString(value, { width: colWidth })` for that cell before positioning the next row, and the next row's `doc.y` starts at least as far down as that cell's measured wrapped height — no fixed-height assumption.
    - Given enough rows that the cumulative row height would exceed the page's printable area (`doc.page.height - doc.page.margins.bottom`), `renderTable()` calls `doc.addPage()` before rendering the row that would overflow, and re-prints the column header row (same header-rendering logic as the initial header) on the new page before continuing with data rows.
    - Given a table where every cell fits on one line (existing short-fixture test cases, e.g. `'lga-1'`, `'Abeokuta North'`), the rendered output is visually equivalent to the pre-fix behavior — no unnecessary blank space or spurious page breaks are introduced for well-fitted content.
    - `renderTable()`'s height-aware/page-break logic works generically for ANY `section.columns`/`section.rows` shape — it is not hardcoded to the Visitor Entries 4/5-column shape.
  </behavior>
  <action>
    In `backend/src/common/services/ministry-pdf.service.ts`, refactor `renderTable()`: extract the header-row-printing logic (currently lines 133-138: capturing `headerY = doc.y`, setting `doc.fontSize(10).fillColor('#1A6B3C')`, looping `section.columns` to print `col.label.toUpperCase()` at `PAGE_LEFT + i * colWidth`, then `doc.moveDown(0.5)`) into a local function `printHeader()` declared inside `renderTable()`, so it can be invoked both before the data-row loop and again after each `doc.addPage()` call. Add a `MIN_ROW_HEIGHT` constant (e.g. `14`, matching roughly one line at 9pt Helvetica) as a floor so empty/short cells never produce a zero-height row. Compute `const pageBottom = doc.page.height - doc.page.margins.bottom;` once, before the data-row loop (page size and margins are fixed for the whole document, set once at construction in `render()`, so this value does not need recomputing after `doc.addPage()`). Replace the data-row loop: for each row, first set `doc.fontSize(9).fillColor('#1C2B2B')` (must precede height measurement so `doc.heightOfString()` measures at the same font/size actually used for rendering), then compute `const cellHeights = section.columns.map((col, i) => doc.heightOfString(String(row[col.key] ?? ''), { width: colWidth }));` and `const rowHeight = Math.max(MIN_ROW_HEIGHT, ...cellHeights);`. Before rendering, check `if (doc.y + rowHeight > pageBottom) { doc.addPage(); printHeader(); doc.fontSize(9).fillColor('#1C2B2B'); }` (font/size must be re-applied after `printHeader()`, since `printHeader()` sets `fontSize(10)` for the header text). Capture `const rowY = doc.y;` AFTER this potential page-break check. Render each cell exactly as before (`doc.text(String(row[col.key] ?? ''), PAGE_LEFT + i * colWidth, rowY, { width: colWidth })`). After rendering all cells for the row, replace `doc.moveDown(0.4)` with `doc.y = rowY + rowHeight;` (explicit height-aware advance, not a fixed line-count multiple). This logic must be generic — do not special-case any specific `section.columns` length or key name.
  </action>
  <acceptance_criteria>
    - `renderTable()` calls `doc.heightOfString()` at least once per row per column when rendering data rows.
    - `renderTable()` no longer contains `doc.moveDown(0.4)` for data-row advancement — the advance is `doc.y = rowY + rowHeight` (or equivalent height-aware assignment).
    - `renderTable()` calls `doc.addPage()` when the next row would exceed `doc.page.height - doc.page.margins.bottom`, and re-prints the header row (same label/color/fontSize as the initial header) immediately after each `doc.addPage()` call.
    - `npm test --workspace=backend -- ministry-pdf` passes, including 2 new tests: (1) a test using a ~36-character UUID-length value in a narrow (5-column) table section that asserts `doc.heightOfString` was called with that UUID value (proving row-height measurement is invoked for wrapping content); (2) a test with enough rows (e.g. 80 short rows) to force at least one page break that asserts `doc.addPage` was called AND that the column header label text appears in the `.text()` call sequence more than once (proving header re-print after the page break).
    - Both new tests include `CR-02` in their test names.
    - All pre-existing `ministry-pdf.service.spec.ts` tests (buffer starts with `%PDF-`, heading rendering, brand colors, column order, ServiceUnavailableException wrap) remain passing unmodified.
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace=backend -- ministry-pdf</automated>
  </verify>
  <done>MinistryPdfService.renderTable() measures each cell's rendered height and advances rows by that measured height (not a fixed constant), and inserts a page break with header re-print whenever content would overflow the page — verified generically (any column/row shape), not just for Visitor Entries. Regression tests prove both the height-measurement call and the page-break/header-reprint behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `MINISTRY_VIEWER` JWT → query result rows → PDF cell content | Query-result-derived values (LGA names, purposes, role strings, currency totals — all system-generated, never free-text user input) flow into `doc.text()`/`doc.heightOfString()` — no user-controlled markup or PDF-injection surface, only rendering-layout correctness is at stake here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-17 | Denial of Service | `renderTable()`'s page-break loop on pathologically large row counts | accept | Row count is bounded by the active `from`/`to`/`lgaId` filter and the underlying aggregate `GROUP BY` query shape (max realistic cardinality: LGAs × months × role buckets, low hundreds) — no unbounded/attacker-controlled row count reaches this renderer; same trust posture as the pre-fix renderer, unaffected by this change |
</threat_model>

<verification>
- `npm test --workspace=backend -- ministry-pdf` passes
- `npm test --workspace=backend -- ministry` (full module suite) passes
- `cd backend && npx tsc --noEmit -p tsconfig.build.json` exits 0
</verification>

<success_criteria>
- Visitor Entries PDF export excludes the raw `lgaId` UUID column; CSV export is unaffected.
- `renderTable()` measures each cell's height and advances rows by that measured height, generically for any column/row shape.
- `renderTable()` inserts a page break with header re-print when content would overflow the page.
- New regression tests prove both behaviors; no existing test regresses.
</success_criteria>

<output>
After completion, create `.planning/phases/14-ministry-dashboard/14-10-SUMMARY.md`
</output>
