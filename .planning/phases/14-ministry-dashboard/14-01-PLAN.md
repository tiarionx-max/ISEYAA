---
phase: 14-ministry-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/20260718000000_phase14_ministry_dashboard/migration.sql
  - backend/src/common/enums/user-role.enum.ts
  - shared/src/types/index.ts
autonomous: true
requirements: [MIN-01, MIN-07]

must_haves:
  truths:
    - "A MINISTRY_VIEWER role value exists and is usable in @Roles() decorators without a TypeScript compile error"
    - "D-07: A VisitorLog table exists with zero PII columns (no BVN/NIN/phone/name/email), limited to lgaId, purpose, sourceType, sourceId, visitedAt, userRole — MIN-07's structural isolation guarantee, satisfying the requirement structurally rather than via query-time filtering"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "MINISTRY_VIEWER UserRole value + VisitorLog model + VisitorSourceType enum"
      contains: "model VisitorLog"
    - path: "backend/prisma/migrations/20260718000000_phase14_ministry_dashboard/migration.sql"
      provides: "Hand-authored additive migration: ALTER TYPE UserRole, CREATE TYPE VisitorSourceType, CREATE TABLE visitor_logs"
      contains: "ALTER TYPE \"UserRole\" ADD VALUE IF NOT EXISTS 'MINISTRY_VIEWER'"
    - path: "backend/src/common/enums/user-role.enum.ts"
      provides: "MINISTRY_VIEWER member on the TypeScript UserRole enum actually consumed by @Roles()/RolesGuard"
      contains: "MINISTRY_VIEWER = 'MINISTRY_VIEWER'"
  key_links:
    - from: "backend/prisma/schema.prisma"
      to: "backend/src/common/enums/user-role.enum.ts"
      via: "both enums must list MINISTRY_VIEWER identically or @Roles(UserRole.MINISTRY_VIEWER) either fails to compile or never matches a real DB row (Pitfall 4)"
      pattern: "MINISTRY_VIEWER"
---

<objective>
Lay the schema foundation every later Phase 14 plan depends on: add `MINISTRY_VIEWER` to the `UserRole` enum (Prisma schema + the hand-maintained TypeScript enum `@Roles()` actually reads + the `shared` package's client-facing enum), and create the net-new `VisitorLog` table (D-07) whose column list deliberately excludes every PII field — this is MIN-07's structural half of the "zero row-level PII" guarantee, verified in Plan 14-06's automated allowlist test.

Purpose: Nothing else in this phase can compile or run until `MINISTRY_VIEWER` exists in all three enum locations and `prisma.visitorLog` is a real Prisma Client delegate. This is Wave 1 — every other plan in this phase transitively depends on it.
Output: Migrated `visitor_logs` table + `VisitorSourceType` enum, `MINISTRY_VIEWER` role value synced across `schema.prisma`, `backend/src/common/enums/user-role.enum.ts`, and `shared/src/types/index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ministry-dashboard/14-CONTEXT.md
@.planning/phases/14-ministry-dashboard/14-RESEARCH.md
@.planning/phases/14-ministry-dashboard/14-PATTERNS.md

<interfaces>
<!-- Exact VisitorLog model spec (14-PATTERNS.md, D-07 columns only — no BVN/NIN/phone/name). -->
```prisma
enum VisitorSourceType {
  EVENT
  STAY
  TOUR
}

model VisitorLog {
  id         String            @id @default(uuid())
  lgaId      String?
  lga        LGA?              @relation(fields: [lgaId], references: [id])
  purpose    String            // free-text taxonomy value (D-05) — not an enum, per "not hardcoded" decision
  sourceType VisitorSourceType
  sourceId   String            // Ticket.id | Booking.id | TourBooking.id — polymorphic, no FK enforced
  visitedAt  DateTime
  userRole   UserRole
  createdAt  DateTime          @default(now())

  @@index([lgaId])
  @@index([visitedAt])
  @@index([sourceType, sourceId])
  @@map("visitor_logs")
}
```

<!-- Current UserRole enum (backend/prisma/schema.prisma:13-25) — MINISTRY_VIEWER is the new last member. -->
```prisma
enum UserRole {
  CITIZEN
  TOURIST
  VENDOR
  ORGANISER
  HOST
  DRIVER
  CREATIVE
  LGA_ADMIN
  STATE_ADMIN
  SUPER_ADMIN
  TOUR_GUIDE // Phase 9 — additively appended via PG ALTER TYPE ADD VALUE
}
```

<!-- Phase 9's exact precedent for hand-authored additive enum migrations
     (backend/prisma/migrations/20260623120000_phase9_tour_packages/migration.sql:1-15) —
     copy this header/ordering convention verbatim. -->
```sql
-- ============================================================================
-- Phase 9 — Tour Packages & Tour Guides (additive migration)
-- ----------------------------------------------------------------------------
-- This migration is hand-authored to use ALTER TYPE ... ADD VALUE for the
-- UserRole enum extension (Prisma's diff would otherwise drop & recreate the
-- enum, breaking every FK referencing users.role).
-- ============================================================================
ALTER TYPE "UserRole" ADD VALUE IF NOT EXISTS 'TOUR_GUIDE';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend schema.prisma — MINISTRY_VIEWER enum value + VisitorLog model + LGA back-relation</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>
    backend/prisma/schema.prisma (lines 13-25 UserRole enum, lines 273-296 LGA model, lines 663-675 ShadowSettlementComparison as the polymorphic sourceId/no-FK precedent)
    .planning/phases/14-ministry-dashboard/14-PATTERNS.md (VisitorLog model spec, lines 57-82)
  </read_first>
  <action>
    In `backend/prisma/schema.prisma`, append `MINISTRY_VIEWER` as the new last member of the `UserRole` enum (after `TOUR_GUIDE` at line 24, before the closing brace), with the inline comment `// Phase 14 — additively appended via PG ALTER TYPE ADD VALUE`. Do NOT reorder existing values. Insert a new `enum VisitorSourceType { EVENT STAY TOUR }` and `model VisitorLog { ... }` immediately after the `LGA` model closes (after line 296, before `model Attraction {`), using the exact field list from `<interfaces>` above — `id`, `lgaId` (nullable, `String?`), `lga` (nullable relation to `LGA`), `purpose` (`String`, required, free-text), `sourceType` (`VisitorSourceType`), `sourceId` (`String`, no FK — polymorphic across `Ticket`/`Booking`/`TourBooking`, mirrors `ShadowSettlementComparison.sourceId`'s no-FK convention), `visitedAt` (`DateTime`), `userRole` (`UserRole`), `createdAt` (`DateTime @default(now())`). Add `@@index([lgaId])`, `@@index([visitedAt])`, `@@index([sourceType, sourceId])`, and `@@map("visitor_logs")`. Add `visitorLogs VisitorLog[]` to the `LGA` model's relations block (alongside the existing `events`, `properties`, `tourPackages` lines at 289-293), before its `@@map("lgas")` line.
  </action>
  <acceptance_criteria>
    - `backend/prisma/schema.prisma` contains the literal string `MINISTRY_VIEWER` inside the `UserRole` enum block, appended after `TOUR_GUIDE` with no reordering of prior values.
    - `backend/prisma/schema.prisma` contains `model VisitorLog` with fields `lgaId String?`, `purpose String`, `sourceType VisitorSourceType`, `sourceId String`, `visitedAt DateTime`, `userRole UserRole`, and `@@map("visitor_logs")`.
    - `backend/prisma/schema.prisma`'s `LGA` model contains `visitorLogs VisitorLog[]`.
    - `cd backend && npx prisma validate` exits 0.
    - `cd backend && npx prisma format` produces no diff (schema already correctly formatted).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx prisma validate</automated>
  </verify>
  <done>Schema file is syntactically valid and contains the new enum value, new enum type, new model, and LGA back-relation exactly as specified.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Hand-author and apply the additive migration</name>
  <files>backend/prisma/migrations/20260718000000_phase14_ministry_dashboard/migration.sql</files>
  <read_first>
    backend/prisma/schema.prisma (post Task 1 edit)
    backend/prisma/migrations/20260623120000_phase9_tour_packages/migration.sql (full file — exact hand-authored additive-enum + CREATE TABLE convention, including the `ALTER TYPE` ordering constraint)
    backend/prisma/migrations/20260717231213_add_shadow_settlement_comparison (most recent migration — column-type conventions: TEXT for String, TIMESTAMP(3) for DateTime, no DB-level UUID default since Prisma generates `id` client-side)
  </read_first>
  <action>
    This project uses tracked Prisma migrations (11 existing folders under `backend/prisma/migrations/`), not `db push` — follow that established pattern, not the generic `prisma db push` fallback. Create directory `backend/prisma/migrations/20260718000000_phase14_ministry_dashboard/` and hand-author `migration.sql` (do NOT run `prisma migrate dev` first without the file in place — Prisma's diff algorithm would try to drop and recreate the `UserRole` enum, breaking every FK typed as `UserRole`). The SQL must contain, in this exact order (Postgres requires `ALTER TYPE ADD VALUE` to precede any DDL using the new value): (1) `ALTER TYPE "UserRole" ADD VALUE IF NOT EXISTS 'MINISTRY_VIEWER';`, (2) `CREATE TYPE "VisitorSourceType" AS ENUM ('EVENT', 'STAY', 'TOUR');`, (3) `CREATE TABLE "visitor_logs"` with columns `id TEXT NOT NULL`, `lgaId TEXT`, `purpose TEXT NOT NULL`, `sourceType "VisitorSourceType" NOT NULL`, `sourceId TEXT NOT NULL`, `visitedAt TIMESTAMP(3) NOT NULL`, `userRole "UserRole" NOT NULL`, `createdAt TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP`, plus `CONSTRAINT "visitor_logs_pkey" PRIMARY KEY ("id")`, (4) three `CREATE INDEX` statements matching the `@@index` directives in schema.prisma (`lgaId`, `visitedAt`, `(sourceType, sourceId)`), (5) `ALTER TABLE "visitor_logs" ADD CONSTRAINT "visitor_logs_lgaId_fkey" FOREIGN KEY ("lgaId") REFERENCES "lgas"("id") ON DELETE SET NULL ON UPDATE CASCADE;`. After writing the file, run `cd backend && npx prisma migrate dev --name phase14_ministry_dashboard` — since the migration directory already exists with hand-authored SQL, Prisma applies it as-is without regenerating a diff. If Prisma reports drift instead of applying cleanly, run `npx prisma migrate resolve --applied 20260718000000_phase14_ministry_dashboard` followed by `npx prisma generate`.
  </action>
  <acceptance_criteria>
    - `backend/prisma/migrations/20260718000000_phase14_ministry_dashboard/migration.sql` exists and contains, in order, `ALTER TYPE "UserRole" ADD VALUE IF NOT EXISTS 'MINISTRY_VIEWER'` before any `CREATE TABLE "visitor_logs"` statement.
    - The file does NOT contain `DROP TYPE "UserRole"` anywhere.
    - `cd backend && npx prisma migrate status` reports the database schema is up to date (no pending migrations).
    - `cd backend && npx tsc --noEmit -p tsconfig.build.json` reports zero "Property 'visitorLog' does not exist" errors — confirms the generated Prisma Client exposes `prisma.visitorLog`.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx prisma migrate status</automated>
  </verify>
  <done>Migration applied to the dev database; `visitorLog` delegate available on the generated Prisma Client; `MINISTRY_VIEWER` is a valid value for the `users.role` column.</done>
</task>

<task type="auto">
  <name>Task 3: Sync MINISTRY_VIEWER across the TypeScript UserRole enum and the shared client-facing enum</name>
  <files>backend/src/common/enums/user-role.enum.ts, shared/src/types/index.ts</files>
  <read_first>
    backend/src/common/enums/user-role.enum.ts (full file — 21 lines, confirm TOUR_GUIDE precedent and REGISTERABLE_ROLES exclusion pattern)
    shared/src/types/index.ts (lines 1-9 — the client-facing UserRole enum, currently missing TOUR_GUIDE too; this file is behind the backend enum and this task does not need to backfill TOUR_GUIDE, only add MINISTRY_VIEWER)
  </read_first>
  <action>
    In `backend/src/common/enums/user-role.enum.ts`, add `MINISTRY_VIEWER = 'MINISTRY_VIEWER',` as a new member of the `UserRole` enum (this is the enum `@Roles()`/`RolesGuard` actually consume at runtime — per Pitfall 4, drift between this file and `schema.prisma`'s enum breaks either compilation or runtime role matching). Do NOT add `UserRole.MINISTRY_VIEWER` to the `REGISTERABLE_ROLES` array (lines 15-21) — Ministry accounts are provisioned by an admin, not self-registered, matching how `LGA_ADMIN`/`STATE_ADMIN`/`SUPER_ADMIN`/`TOUR_GUIDE` are already excluded. In `shared/src/types/index.ts`, add `MINISTRY_VIEWER = 'MINISTRY_VIEWER',` to the exported `UserRole` enum (used by `web/` and `mobile/` for client-side role-gating, e.g. Plan 14-08's `ALLOWED_ROLES` check) — add it as the new last member, no other changes to that enum.
  </action>
  <acceptance_criteria>
    - `backend/src/common/enums/user-role.enum.ts` contains `MINISTRY_VIEWER = 'MINISTRY_VIEWER'` and `REGISTERABLE_ROLES` does NOT include `UserRole.MINISTRY_VIEWER`.
    - `shared/src/types/index.ts`'s `UserRole` enum contains `MINISTRY_VIEWER = 'MINISTRY_VIEWER'`.
    - `cd backend && npx tsc --noEmit -p tsconfig.build.json` exits 0.
    - `cd shared && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.build.json && cd ../shared && npx tsc --noEmit</automated>
  </verify>
  <done>All three UserRole enum definitions (Prisma schema, backend TS enum, shared TS enum) contain MINISTRY_VIEWER with identical string values; backend and shared workspaces type-check cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration script → Postgres | Trusted, operator-run, offline (not an HTTP-reachable surface) |
| `VisitorLog` schema shape → all future Ministry query code | The table's column list is the first line of defense for MIN-07 — no PII column exists to accidentally `select`/`include` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | `VisitorLog` table design | mitigate | D-07's column list (lgaId, purpose, sourceType, sourceId, visitedAt, userRole) structurally excludes BVN/NIN/phone/name/email — there is nothing to leak from this table regardless of future query mistakes; verified by Plan 14-06's automated allowlist test against live query output |
| T-14-02 | Elevation of Privilege | `MINISTRY_VIEWER` enum sync across 3 files | mitigate | Task 3's acceptance criteria require identical string values in `schema.prisma`, `user-role.enum.ts`, and `shared/src/types/index.ts` — a mismatch here would either break compilation (safe failure) or silently make `@Roles(UserRole.MINISTRY_VIEWER)` never match a real user (safe failure, not a privilege leak) |
</threat_model>

<verification>
- `cd backend && npx prisma validate` passes after Task 1
- `cd backend && npx prisma migrate status` reports up-to-date after Task 2
- `cd backend && npx tsc --noEmit -p tsconfig.build.json` and `cd shared && npx tsc --noEmit` both exit 0 after Task 3
</verification>

<success_criteria>
- `MINISTRY_VIEWER` exists in `schema.prisma`'s `UserRole` enum, `backend/src/common/enums/user-role.enum.ts`, and `shared/src/types/index.ts`, all three with identical string value.
- `visitor_logs` table exists in the dev database, queryable via `prisma.visitorLog`, with zero PII columns.
- Legacy `UserRole` values are unchanged and unreordered.
</success_criteria>

<output>
After completion, create `.planning/phases/14-ministry-dashboard/14-01-SUMMARY.md`
</output>
