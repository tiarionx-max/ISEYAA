---
phase: 14-ministry-dashboard
plan: 06
type: execute
wave: 3
depends_on: ["14-03"]
files_modified:
  - backend/src/modules/ministry/ministry.service.ts
  - backend/src/modules/ministry/ministry.controller.ts
  - backend/src/modules/ministry/__tests__/ministry.service.spec.ts
  - backend/src/modules/ministry/__tests__/ministry-pii-allowlist.spec.ts
autonomous: true
requirements: [MIN-04, MIN-07]

must_haves:
  truths:
    - "GET /ministry/revenue returns revenue grouped by settled module and by month, sourced from the standing Ministry wallet's Transaction ledger, spanning all historical settlement data (D-10 — not scoped to post-Phase-14)"
    - "A JSON response from any /ministry/* endpoint, scanned recursively for both key names AND string values, never contains a key or value matching a BVN/NIN/phone/name/email pattern — proven by an automated test, not manual review"
  artifacts:
    - path: "backend/src/modules/ministry/ministry.service.ts"
      provides: "getRevenueToGovernment() — module+month totals for all 7 settled modules, plus an LGA sub-breakdown for Stays, Marketplace, and Tour (per D-09)"
      contains: "getRevenueToGovernment"
    - path: "backend/src/modules/ministry/__tests__/ministry-pii-allowlist.spec.ts"
      provides: "MIN-07's literal 'automated field-allowlist/schema-shape test' requirement — dual key-denylist + value-canary scanner, where the value-canary half genuinely inspects string VALUES (not just key names)"
      contains: "PII_FIELD_DENYLIST"
  key_links:
    - from: "backend/src/modules/ministry/ministry.service.ts"
      to: "backend/src/common/services/settlement.service.ts"
      via: "resolveMinistryWallet() resolves the standing Ministry wallet fresh on every call — never cached (Pitfall 2 in settlement.service.ts)"
      pattern: "resolveMinistryWallet"
---

<objective>
Complete `MinistryService`/`MinistryController` with MIN-04's revenue-to-government-share query, reading the standing Ministry wallet's `Transaction` ledger built in Phase 12/13 (no new settlement plumbing — this phase only adds read/aggregation). Then satisfy MIN-07's hardest, most literal constraint: an automated field-allowlist/schema-shape test (not ad hoc review) proving no `MINISTRY_VIEWER` response ever carries BVN/NIN/phone/name/email — as both a key-name AND a value match — using the dual key-denylist + value-canary pattern from RESEARCH.md (no existing precedent in this codebase — this is net-new).

Purpose: This is the last backend read-query work before the export layer (Plan 14-07) and web UI (Plan 14-08). D-09's guaranteed dimensions (by module, by month) apply to all 7 settled modules. D-09 names three modules for the optional LGA sub-breakdown — Stays (`Property.lgaId`), Marketplace (`Vendor.lgaId`), and Tour (package LGA) — each reached via a direct, reliable join from the Ministry-credited `Transaction` row. Transport/Delivery/Studio/Events are excluded from the LGA sub-breakdown (still fully counted in the by-module/by-month totals) because no such join path exists for them, consistent with D-09's "added only where the underlying source record naturally carries one already" clause. Tour's join path is NOT `Transaction.metadata` directly (unlike Stays/Marketplace) — it goes through `Transaction.gatewayRef` -> `TourBooking.paymentReference` -> `TourBooking.tourPackageId` -> `TourPackage.lgaId`, with a documented split-bill undercount caveat (see `<interfaces>` below and Task 1's action).
Output: `GET /ministry/revenue` route; `ministry-pii-allowlist.spec.ts` running against all 3 live Ministry endpoints with a seeded PII-canary fixture, checking both key names and string values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ministry-dashboard/14-CONTEXT.md
@.planning/phases/14-ministry-dashboard/14-RESEARCH.md
@.planning/phases/14-ministry-dashboard/14-PATTERNS.md

<interfaces>
<!-- Confirmed Transaction.metadata.module values written by SettlementService.settle()
     across all 7 call sites (grepped directly against backend/src) —
     use this exact list, NOT the confusable 'tour' value that lives on
     TourBooking.metadata (a different field on a different model). -->
```
'events', 'marketplace', 'stays', 'studio', 'transport', 'delivery', 'tour_booking'
```

<!-- Ministry-credited recipient metadata per module (confirmed by direct
     source read) — the exact join path for the LGA sub-breakdown: -->
```
Stays:        metadata.bookingId  -> bookings.propertyId     -> properties.lgaId
Marketplace:  metadata.orderId    -> orders.vendorId          -> vendors.lgaId
Tour:         t."gatewayRef"      -> tour_bookings."paymentReference"
                                   -> tour_bookings."tourPackageId" -> tour_packages."lgaId"
(Transport/Delivery/Studio/Events: no reliable direct LGA join from the
 Ministry-credited row's metadata — excluded from the LGA sub-breakdown,
 per D-09's "naturally carries one" clause; still included in byModule/byMonth.)
```

<!-- Tour's join is structurally different from Stays/Marketplace: settlement.service.ts's
     settle() (verbatim, lines 177-196) writes `gatewayRef: input.reference` on every
     recipient row it creates (including the ATTRACTION-tagged row credited to the
     Ministry wallet), where `input.reference` is the Paystack reference for THAT
     specific settle() call. tour-settlement.service.ts sets
     `TourBooking.paymentReference` to `payload.reference` for a non-split-bill
     booking (line ~253) — which matches `gatewayRef` exactly, so the join is exact.

     SPLIT-BILL CAVEAT: for a split-bill booking, each passenger's share is its own
     settle() call with its own distinct payload.reference, so each share's Ministry
     credit row has a DIFFERENT gatewayRef. Only once ALL shares are paid does
     `paymentReference` get set to `payload.metadata?.parentReference ?? payload.reference`
     from the LAST share processed (tour-settlement.service.ts lines ~298-320) — this
     means a `gatewayRef = paymentReference` equi-join only recovers ONE share's
     Ministry-credited row per split-bill booking (the row whose gatewayRef happens to
     equal the stored paymentReference), undercounting the LGA sub-breakdown total for
     split-bill tour bookings relative to the byModule/byMonth totals (which sum ALL
     shares correctly, since they don't rely on this join). This is a known, accepted
     limitation — document it in the query method's doc comment, do not attempt to fix
     it in this plan (would require walking TourBooking.metadata.shares, out of scope
     for a read/aggregation-only phase). -->

<!-- resolveMinistryWallet() — settlement.service.ts:321-328, verbatim. -->
```typescript
async resolveMinistryWallet(): Promise<{ id: string } | null> {
  const cfg = await this.prisma.platformConfig.findUnique({
    where: { key: 'tour.government_wallet_user_id' },
  });
  const userId = (cfg?.value as string | null | undefined) ?? null;
  if (!userId) return null;
  return this.prisma.wallet.findUnique({ where: { userId }, select: { id: true } });
}
```

<!-- RESEARCH.md's fully-specified PII allowlist test pattern, EXTENDED with a genuine
     value-canary scanner (blocker fix — the key-only scanner below does NOT catch an
     aliased leak, e.g. `user.firstName` renamed to `guestName` in a `select`). Both
     functions are required; the value scanner is a second, independent check, not a
     restatement of the key scanner. -->
```typescript
const PII_FIELD_DENYLIST = ['bvn', 'nin', 'bvnHash', 'ninHash', 'phone', 'firstName', 'lastName', 'email'];

function assertNoPiiKeys(obj: unknown, path = ''): void {
  if (obj === null || typeof obj !== 'object') return;
  if (Array.isArray(obj)) {
    obj.forEach((item, i) => assertNoPiiKeys(item, `${path}[${i}]`));
    return;
  }
  for (const [key, value] of Object.entries(obj as Record<string, unknown>)) {
    const lowerKey = key.toLowerCase();
    if (PII_FIELD_DENYLIST.some((f) => lowerKey === f.toLowerCase())) {
      throw new Error(`PII field "${key}" found at response path "${path}.${key}" — MIN-07 violation`);
    }
    assertNoPiiKeys(value, `${path}.${key}`);
  }
}

// Value-canary half of the pattern: walks every string VALUE in the response tree
// (regardless of the key name it's stored under) and asserts none of them equals a
// seeded canary value drawn from a real fixture user's BVN/NIN/phone/firstName/
// lastName/email. This is what catches a field renamed to dodge the key denylist.
function assertNoPiiValues(obj: unknown, canaryValues: string[], path = ''): void {
  if (obj === null || obj === undefined) return;
  if (Array.isArray(obj)) {
    obj.forEach((item, i) => assertNoPiiValues(item, canaryValues, `${path}[${i}]`));
    return;
  }
  if (typeof obj === 'string') {
    if (canaryValues.some((canary) => obj === canary)) {
      throw new Error(`PII canary value found at response path "${path}" — MIN-07 violation`);
    }
    return;
  }
  if (typeof obj === 'object') {
    for (const [key, value] of Object.entries(obj as Record<string, unknown>)) {
      assertNoPiiValues(value, canaryValues, `${path}.${key}`);
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getRevenueToGovernment() query + GET /ministry/revenue route</name>
  <files>backend/src/modules/ministry/ministry.service.ts, backend/src/modules/ministry/ministry.controller.ts</files>
  <read_first>
    backend/src/modules/ministry/ministry.service.ts, backend/src/modules/ministry/ministry.controller.ts (post Plan 14-03 — existing 2 query methods + 2 GET routes, mirror their exact style)
    backend/src/common/services/settlement.service.ts (lines 180-230 — Transaction.metadata/gatewayRef shape written by settle(); lines 300-328 — getStatement()/resolveMinistryWallet())
    backend/src/modules/tour-bookings/tour-settlement.service.ts (lines 249-256, 298-326 — where `TourBooking.paymentReference` is set on the non-split-bill vs. split-bill-final CONFIRMED transition, the basis for the join + its documented caveat)
    backend/src/modules/admin/admin.service.ts (lines 59-67 — the JOIN vendors/JOIN lgas admin.getRevenue() pattern to mirror for the Marketplace LGA sub-breakdown)
  </read_first>
  <behavior>
    - `getRevenueToGovernment(from?, to?)` returns `{ byModule: {module, total}[], byMonth: {month, total}[], byModuleLga: {module, lgaId, lgaName, total}[] }` where `byModule`/`byMonth` cover ALL 7 module strings that have ever credited the Ministry wallet, and `byModuleLga` only ever contains rows where `module` is `'stays'`, `'marketplace'`, or `'tour_booking'`.
    - If `resolveMinistryWallet()` returns `null` (unconfigured `tour.government_wallet_user_id`), the method returns `{ byModule: [], byMonth: [], byModuleLga: [] }` without throwing.
    - `from`/`to` filters, when supplied, bound `Transaction.createdAt` inclusively; omitted, the query covers all historical data (D-10 — no phase-14-ship-date floor).
    - All returned aggregate values are JS `number`, never `Decimal`/`bigint`-shaped.
    - Tour's row(s) in `byModuleLga` are sourced via `Transaction.gatewayRef -> TourBooking.paymentReference -> TourBooking.tourPackageId -> TourPackage.lgaId` (NOT `metadata.bookingId`/`metadata.orderId` like Stays/Marketplace) — a split-bill tour booking may undercount in this specific sub-breakdown (documented caveat, does not affect byModule/byMonth totals).
  </behavior>
  <action>
    In `backend/src/modules/ministry/ministry.service.ts`, inject `SettlementService` into the constructor (CommonModule global). Add `async getRevenueToGovernment(from?: string, to?: string)`: (1) call `this.settlementService.resolveMinistryWallet()`; if `null`, return the empty-shape object above; (2) run a `$queryRaw` grouping `transactions` by `metadata->>'module'` filtered on `walletId = ministryWallet.id AND type = 'CREDIT' AND status = 'SUCCESS'`, with optional `from`/`to` bounds via `Prisma.sql`/`Prisma.empty` conditional fragments (never string concatenation), summing `amount`; (3) run a second `$queryRaw` with the same filter, grouped by `TO_CHAR("createdAt", 'YYYY-MM')`, summing `amount`; (4) run a third `$queryRaw` restricted to `metadata->>'module' IN ('stays', 'marketplace', 'tour_booking')`, using `CASE WHEN metadata->>'module' = 'stays' THEN (SELECT p."lgaId" FROM bookings b JOIN properties p ON b."propertyId" = p.id WHERE b.id = (t.metadata->>'bookingId')::text) WHEN metadata->>'module' = 'marketplace' THEN (SELECT v."lgaId" FROM orders o JOIN vendors v ON o."vendorId" = v.id WHERE o.id = (t.metadata->>'orderId')::text) WHEN metadata->>'module' = 'tour_booking' THEN (SELECT tp."lgaId" FROM tour_bookings tb JOIN tour_packages tp ON tb."tourPackageId" = tp.id WHERE tb."paymentReference" = t."gatewayRef") END` (or an equivalent per-module `LEFT JOIN LATERAL`/subquery approach — either is acceptable as long as it produces one row per (module, lgaId, month-independent) grouping) joined to `lgas` for the name, grouped by module + lgaId + lgaName, summing `amount`. Add a code comment directly above the `tour_booking` `CASE WHEN` branch documenting the split-bill undercount caveat verbatim from `<interfaces>` above, so a future maintainer understands why Tour's LGA sub-breakdown total can be less than its byModule total. Coerce every aggregate with `Number()` before returning. In `backend/src/modules/ministry/ministry.controller.ts`, add `@Get('revenue')` calling `this.ministryService.getRevenueToGovernment(query.from, query.to)` with `@Query() query: MinistryQueryDto`, same pattern as the existing 2 routes.
  </action>
  <acceptance_criteria>
    - `GET /ministry/revenue` exists on `MinistryController` with the same class-level `@Roles()` guard as the other 2 routes (no per-route override).
    - `getRevenueToGovernment()`'s `byModule` query groups on all 7 confirmed module strings (`'events'`, `'marketplace'`, `'stays'`, `'studio'`, `'transport'`, `'delivery'`, `'tour_booking'`) with no hardcoded module allowlist filtering them out.
    - `getRevenueToGovernment()`'s `byModuleLga` query is restricted to `'stays'`/`'marketplace'`/`'tour_booking'` only, and Tour's row is joined via `gatewayRef -> TourBooking.paymentReference -> tourPackageId -> TourPackage.lgaId`, not via `metadata.bookingId`/`metadata.orderId`.
    - The split-bill undercount caveat for Tour's LGA sub-breakdown is documented as a code comment directly above the relevant `CASE WHEN`/join clause.
    - `getRevenueToGovernment()` returns the empty-shape object (not a throw) when `resolveMinistryWallet()` resolves `null`.
    - `cd backend && npx tsc --noEmit -p tsconfig.build.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.build.json</automated>
  </verify>
  <done>MIN-04's revenue-to-government-share query is live at GET /ministry/revenue, covers all historical Ministry wallet transactions, and provides an LGA sub-breakdown covering all three modules named in D-09 (Stays, Marketplace, Tour) via each module's actual direct/reliable join path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Revenue query tests + MIN-07 PII allowlist spec (key-denylist AND value-canary)</name>
  <files>backend/src/modules/ministry/__tests__/ministry.service.spec.ts, backend/src/modules/ministry/__tests__/ministry-pii-allowlist.spec.ts</files>
  <read_first>
    backend/src/modules/ministry/__tests__/ministry.service.spec.ts (post Plan 14-03 — existing test file structure to extend, not replace)
    backend/src/modules/ministry/ministry.service.ts (post Task 1)
    backend/prisma/schema.prisma (User model field list, lines ~224-264 approx — the exact column names PII_FIELD_DENYLIST must cover: bvn, nin, bvnHash, ninHash, phone, firstName, lastName, email)
  </read_first>
  <behavior>
    - `getRevenueToGovernment()`: given a mocked `resolveMinistryWallet()` returning `null`, the method returns the empty-shape object without calling `$queryRaw`.
    - `getRevenueToGovernment()`: given mocked `$queryRaw` results including `Decimal`-shaped totals, the returned `total` fields are plain JS numbers.
    - `getRevenueToGovernment()`: the `byModuleLga` `$queryRaw` template's SQL string (asserted via `mockPrisma.$queryRaw.mock.calls`) includes the `tour_bookings`/`tour_packages` join alongside the existing `bookings`/`properties` and `orders`/`vendors` joins.
    - `assertNoPiiKeys()` throws when given a fixture object containing a `firstName` key at any nesting depth, including inside an array.
    - `assertNoPiiKeys()` does NOT throw for the real shape returned by `getVisitorEntriesByLgaAndMonth()`, `getPurposeBreakdown()`, and `getRevenueToGovernment()` when called against a seeded fixture user whose `firstName` is a canary value (e.g. `'PII_CANARY_FIRSTNAME'`) — proving the actual query methods never surface it, not just that the scanner works in isolation.
    - `assertNoPiiValues()` throws when a fixture object contains the seeded canary string as a VALUE under a non-denylisted key name (e.g. `{ guestName: 'PII_CANARY_FIRSTNAME' }`) — proving the value-canary half of the pattern catches an aliased-field leak that `assertNoPiiKeys()` alone would miss.
    - `assertNoPiiValues()` does NOT throw for the real shape returned by all 3 query methods against the same seeded fixture, run independently of the key-denylist check (both checks execute against every method's output, neither is skipped).
  </behavior>
  <action>
    Extend `backend/src/modules/ministry/__tests__/ministry.service.spec.ts` with a `describe('getRevenueToGovernment', ...)` block covering: the null-wallet early return, `Decimal`/`bigint` coercion to `Number`, that the `byModule` `$queryRaw` template does not filter on a hardcoded module list, and that the `byModuleLga` `$queryRaw` template's SQL includes the Tour join (`tour_bookings`/`tour_packages`/`paymentReference`/`gatewayRef`). Create `backend/src/modules/ministry/__tests__/ministry-pii-allowlist.spec.ts` using the exact `PII_FIELD_DENYLIST` array, the recursive `assertNoPiiKeys()` key-scanner, AND the recursive `assertNoPiiValues()` value-scanner from `<interfaces>` above (verify the denylist strings match `schema.prisma`'s actual `User` column names — `bvn`, `nin`, `bvnHash`, `ninHash`, `phone`, `firstName`, `lastName`, `email`). Seed a fixture user with real canary values for `firstName`, `phone`, and `email` (e.g. `'PII_CANARY_FIRSTNAME'`, `'PII_CANARY_PHONE'`, `'PII_CANARY_EMAIL'`) and collect them into a `canaryValues: string[]` array. Instantiate `MinistryService` with a mocked `PrismaService` whose `$queryRaw` mock returns realistic fixture rows (LGA names, months, counts, purpose strings, module names, currency totals — zero PII-shaped fields, zero canary values) for all 3 query methods, and run BOTH `assertNoPiiKeys()` AND `assertNoPiiValues(result, canaryValues)` against each method's resolved output — two independent assertions per method, not one. Add a negative-control test proving `assertNoPiiKeys()` throws on a deliberately PII-key-containing fixture, AND a separate negative-control test proving `assertNoPiiValues()` throws on a deliberately canary-VALUE-containing fixture stored under an innocuous key name (e.g. `{ guestName: canaryValues[0] }`) — this second negative control is what proves the value-canary half of the pattern is not a no-op.
  </action>
  <acceptance_criteria>
    - `ministry-pii-allowlist.spec.ts` exists, exports/uses a `PII_FIELD_DENYLIST` containing at minimum `bvn`, `nin`, `bvnHash`, `ninHash`, `phone`, `firstName`, `lastName`, `email`.
    - `ministry-pii-allowlist.spec.ts` implements and exercises BOTH `assertNoPiiKeys()` (key-name scan) and `assertNoPiiValues()` (string-value scan against seeded canary values) — the value scan is a genuine independent check, not documentation-only.
    - The spec includes at least one negative-control test proving `assertNoPiiKeys()` throws on a deliberately PII-key fixture, AND at least one negative-control test proving `assertNoPiiValues()` throws on a deliberately canary-value fixture stored under a non-denylisted key (aliased-leak regression class).
    - `npm test --workspace=backend -- ministry.service` and `npm test --workspace=backend -- ministry-pii-allowlist` both pass.
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace=backend -- ministry</automated>
  </verify>
  <done>MIN-07 is proven by an automated test that both confirms current Ministry responses are free of PII keys AND PII-shaped values, AND proves both scanners would independently catch a regression (including an aliased-field leak), not a self-fulfilling no-op check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `MINISTRY_VIEWER` JWT → `GET /ministry/revenue` | Same authenticated, role-gated boundary as the other 2 routes |
| Every `/ministry/*` JSON response → the `MINISTRY_VIEWER` caller | The literal MIN-07 boundary — this is where a future `include: { user: true }` regression, OR a field renamed to dodge the key denylist (e.g. `firstName` -> `guestName`), would leak PII to an external government stakeholder if unguarded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-11 | Information Disclosure | Response-shape drift (a future code change adds `include: { user: true }` to a Ministry query, OR renames a PII field to dodge the key denylist) | mitigate | `ministry-pii-allowlist.spec.ts` runs on every CI build (not just at phase-ship time) with BOTH a key-denylist scan AND a value-canary scan — the value scan specifically catches the aliased-field-rename regression class the key scan alone would miss, per MIN-07's literal "automated... test, not by ad hoc review" requirement |
| T-14-12 | Tampering | SQL injection via `from`/`to` in the revenue query | mitigate | Same `Prisma.sql`/`Prisma.empty` tagged-template convention as Plan 14-03's queries — never string-concatenated |
</threat_model>

<verification>
- `npm test --workspace=backend -- ministry` passes (all 3 query methods + PII allowlist, both key and value checks)
- `cd backend && npx tsc --noEmit -p tsconfig.build.json` exits 0
- `npm test --workspace=backend` (full suite) still green
</verification>

<success_criteria>
- `GET /ministry/revenue` returns module+month totals spanning all historical Ministry wallet transactions, with an LGA sub-breakdown for Stays, Marketplace, and Tour (per D-09), including a documented split-bill caveat for Tour.
- An automated test proves zero PII field names AND zero PII-shaped values ever appear in any Ministry endpoint's response, and proves both scanners are not no-ops.
</success_criteria>

<output>
After completion, create `.planning/phases/14-ministry-dashboard/14-06-SUMMARY.md`
</output>
