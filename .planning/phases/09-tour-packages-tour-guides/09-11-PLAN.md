---
phase: 09-tour-packages-tour-guides
plan: 11
type: execute
wave: 7
depends_on: [09-04, 09-05, 09-07, 09-08]
files_modified:
  - mobile/lib/category-config.ts
  - mobile/app/(tabs)/book.tsx
  - mobile/app/tours/[id].tsx
  - mobile/app/trips/index.tsx
  - mobile/app/_layout.tsx
  - mobile/components/tours/TourBookingSheet.tsx
  - mobile/components/tours/TourCard.tsx
  - mobile/components/tours/ItineraryTimeline.tsx
  - mobile/components/tours/RatingModal.tsx
  - mobile/components/tours/SplitBillShareSheet.tsx
autonomous: true
requirements: [TOUR-03, TOUR-05, TOUR-06, TOUR-07]
must_haves:
  truths:
    - "Book hub adds Tours as the 5th sub-section alongside Events/Stays/Studio/Marketplace — category-tabbed grid of approved tour packages"
    - "TOUR_CATEGORIES (10 entries: All + 9 enum values) added to mobile/lib/category-config.ts as the single source of truth"
    - "mobile/app/tours/[id].tsx renders cover gallery + ItineraryTimeline + guide profile card + sticky TourBookingSheet (date picker + passenger stepper + split-bill toggle)"
    - "mobile/app/trips/index.tsx lists user's past + upcoming TourBookings from GET /tour-bookings/me with itinerary + guide info"
    - "TourBookingSheet POSTs to /api/v1/tour-bookings; on payment.authorizationUrl opens Paystack via expo-web-browser; on splitBillJoinLink opens SplitBillShareSheet with copy + native Share"
    - "RatingModal opens after T+1h push (tap notification deep links to /tours/rate/:bookingId) — 3-tab modal: rate guide / rate package / rate venue, POSTs to /api/v1/reviews"
    - "_layout.tsx adds 3 new Stack routes: tours/[id] (card), trips/index (card), tours/rate/[bookingId] (modal)"
    - "No inline hex — use existing mobile/lib/tokens.ts FOREST/GOLD/JUNGLE tokens (Phase 8 pattern)"
  artifacts:
    - path: "mobile/app/(tabs)/book.tsx"
      provides: "Book hub 5-sub-section layout (Events/Stays/Studio/Marketplace/TOURS NEW)"
      contains: "Tours"
    - path: "mobile/app/tours/[id].tsx"
      provides: "Tour package detail screen — gallery + itinerary + guide card + sticky booking sheet"
      contains: "TourBookingSheet"
    - path: "mobile/app/trips/index.tsx"
      provides: "User's tour bookings list (past + upcoming)"
      contains: "tour-bookings/me"
    - path: "mobile/components/tours/TourBookingSheet.tsx"
      provides: "Mode-aware mobile booking sheet — date picker + passenger stepper + split-bill toggle + email"
      exports: ["TourBookingSheet"]
  key_links:
    - from: "mobile/components/tours/TourBookingSheet.tsx"
      to: "POST /api/v1/tour-bookings"
      via: "useMutation; on response branches to Paystack (expo-web-browser) or SplitBillShareSheet"
      pattern: "/tour-bookings"
    - from: "mobile/app/_layout.tsx"
      to: "Stack route tours/[id] + trips/index + tours/rate/[bookingId]"
      via: "Stack.Screen registrations"
      pattern: "tours/\\[id\\]|trips/index|tours/rate"
---

<objective>
Mobile slice of Phase 9. Closes TOUR-03/TOUR-05/TOUR-06/TOUR-07 mobile-side.

Phase 8 already lays the Book hub architecture (5 sub-sections per the redesign). This plan adds "Tours" as that 5th sub-section, following Phase 8's patterns byte-for-byte:
- `mobile/lib/category-config.ts` extended with `TOUR_CATEGORIES`.
- Book hub gets a 5th tab pointing to a tour grid backed by GET /tour-packages.
- `mobile/app/tours/[id].tsx` mirrors `mobile/app/stays/[id].tsx` shape (gallery + sticky sheet) but with a single booking sheet, not 4 mode-aware ones.
- `mobile/app/trips/index.tsx` is a new screen for past + upcoming tours (different from existing /bookings if any — verify and prefer /trips/ to avoid collision).
- `RatingModal.tsx` is the post-tour rating UI (3 tabs: guide / package / venue).
- `SplitBillShareSheet.tsx` renders the split-bill join link with a Share native sheet + clipboard copy + WhatsApp deep link.

All UI uses existing tokens from `mobile/lib/tokens.ts` (Phase 8 established). `expo-image` for all photos. `@react-native-community/datetimepicker` for the date picker (Phase 8 installed). `expo-web-browser` for Paystack handoff. No new deps required.

Output: 1 new tab category + 3 new screens + 4 new components + 1 _layout.tsx route registration edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-tour-packages-tour-guides/09-CONTEXT.md
@CLAUDE.md
@mobile/lib/category-config.ts
@mobile/lib/tokens.ts
@mobile/lib/api.ts
@mobile/app/_layout.tsx
@mobile/app/(tabs)/book.tsx
@mobile/app/stays/[id].tsx
@mobile/components/stays/NightlyBookingSheet.tsx

<interfaces>
TOUR_CATEGORIES addition to category-config.ts:
  export type TourCategory = {
    id: 'all' | 'heritage' | 'cultural' | 'adire' | 'festival' | 'food' | 'family' | 'faith' | 'school' | 'corporate';
    label: string;
    icon: LucideIcon;
    category?: string;          // backend enum value (HERITAGE/CULTURAL/...)
  };
  export const TOUR_CATEGORIES: TourCategory[] = [
    { id: 'all',       label: 'All',       icon: Sparkles },
    { id: 'heritage',  label: 'Heritage',  icon: Landmark,    category: 'HERITAGE' },
    { id: 'cultural',  label: 'Cultural',  icon: Drum,        category: 'CULTURAL' },
    { id: 'adire',     label: 'Adire',     icon: Palette,     category: 'ADIRE' },
    { id: 'festival',  label: 'Festival',  icon: PartyPopper, category: 'FESTIVAL' },
    { id: 'food',      label: 'Food',      icon: Utensils,    category: 'FOOD' },
    { id: 'family',    label: 'Family',    icon: Heart,       category: 'FAMILY' },
    { id: 'faith',     label: 'Faith',     icon: Cross,       category: 'FAITH' },
    { id: 'school',    label: 'School',    icon: BookOpen,    category: 'SCHOOL' },
    { id: 'corporate', label: 'Corporate', icon: Briefcase,   category: 'CORPORATE' },
  ];
  export function buildTourQuery(c: TourCategory): string {
    const p = new URLSearchParams({ limit: '48' });
    if (c.category) p.set('category', c.category);
    return p.toString();
  }

TourBookingSheet props:
  type TourBookingArgs = {
    tourPackageId: string;
    tourDate: string;          // ISO YYYY-MM-DD
    passengerCount: number;
    splitBillEnabled: boolean;
    email: string;
  };
  type TourBookingSheetProps = {
    pkg: TourPackage;
    pending: boolean;
    signedIn: boolean;
    defaultEmail?: string | null;
    onSubmit: (args: TourBookingArgs) => void;
  };

Stack route additions (mobile/app/_layout.tsx — insert after existing tour-related screens or near stays/[id] registration):
  <Stack.Screen name="tours/[id]" options={{ title: 'Tour' }} />
  <Stack.Screen name="trips/index" options={{ title: 'My trips', presentation: 'card' }} />
  <Stack.Screen name="tours/rate/[bookingId]" options={{ headerShown: false, presentation: 'transparentModal', animation: 'fade' }} />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend category-config.ts with TOUR_CATEGORIES + buildTourQuery; register 3 Stack routes in _layout.tsx</name>
  <files>mobile/lib/category-config.ts, mobile/app/_layout.tsx</files>
  <action>**category-config.ts** — open the file (already has STAY_CATEGORIES + MARKETPLACE_CATEGORIES from Phase 8). Append `TOUR_CATEGORIES` (10 entries per <interfaces>) + `TourCategory` type + `buildTourQuery(c)` helper. Import additional Lucide icons (`Landmark`, `Drum`, `PartyPopper`, `Utensils`, `Heart`, `Cross`, `BookOpen`, `Briefcase`) from `lucide-react-native`. Do NOT modify existing exports.

  **_layout.tsx** — open `mobile/app/_layout.tsx`. Locate the existing Stack block. Insert 3 new `<Stack.Screen>` lines near the existing stays/[id] route (likely around line 47 per the 08-04b plan). Insertions:
  ```tsx
  <Stack.Screen name="tours/[id]" options={{ title: 'Tour' }} />
  <Stack.Screen name="trips/index" options={{ title: 'My trips', presentation: 'card' }} />
  <Stack.Screen name="tours/rate/[bookingId]" options={{ headerShown: false, presentation: 'transparentModal', animation: 'fade' }} />
  ```
  Do NOT touch any other Stack.Screen, the QueryClient init, or the auth redirect useEffect.</action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>category-config.ts exports TOUR_CATEGORIES (10) + buildTourQuery + TourCategory type. _layout.tsx has the 3 new Stack.Screen entries. Existing Phase 8 exports + routes untouched. tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add Tours sub-section to Book hub + create TourCard + ItineraryTimeline + tours/[id] screen</name>
  <files>mobile/app/(tabs)/book.tsx, mobile/components/tours/TourCard.tsx, mobile/components/tours/ItineraryTimeline.tsx, mobile/app/tours/[id].tsx</files>
  <action>**book.tsx** — Phase 8 established the 4-sub-section pattern. Add a 5th button to the top sub-section selector: "Tours" (Tag icon or Map icon). When selected, render the tours grid:
   - Category strip (horizontal CategoryStrip from Phase 8) mapped to TOUR_CATEGORIES.
   - useQuery: `useQuery({ queryKey: ['tour-packages', selectedTourCategory], queryFn: () => fetcher('/tour-packages?' + buildTourQuery(selectedTourCategory)) })`.
   - 2-column grid of TourCard. On tap: router.push('/tours/' + pkg.slug) (NOT id — backend findBySlug uses slug; verify against 09-04 contract).

   Wait — backend exposes both `/tour-packages` (list returns id) and `/tour-packages/:slug` (detail). The Stack route is `tours/[id]` which is the FILE-BASED ROUTING segment — it accepts either id or slug as the param value. Decision: use slug for navigation (canonical URL). The screen reads the param and calls the slug endpoint. The Stack file is named `[id]` purely because expo-router named the segment that way historically — rename to `[slug]` if cleaner, but mind that breaks _layout.tsx registration. Choose: keep `[id]` filename, pass the SLUG as the param value, screen uses slug for the API call. Add a code comment explaining the misleading filename.

  **TourCard.tsx** — `'use client'` not needed (RN doesn't use the directive). Props: `{ pkg }`. Renders coverImageUrl via expo-image (contentFit cover, 4:3), name, category pill (from TOUR_CATEGORIES label lookup), lga.name, price formatted with tabular-nums in gold, rating + reviewCount. PressableScale animation (Phase 8 primitive). Touch target >= 44pt.

  **ItineraryTimeline.tsx** — vertical timeline. For each item sorted by hour ascending: `+{hour}h` gold pill on left, title bold ink, description in INK_SECONDARY, location with MapPin icon if present. Left vertical line (1px BORDER_SUBTLE).

  **tours/[id].tsx** — full screen:
   - Read `slug` from useLocalSearchParams (param name `id` but value is slug — see above comment).
   - useQuery for /tour-packages/:slug.
   - Loading skeleton + 404 fallback.
   - Top: cover gallery — paged horizontal FlatList of pkg.imageUrls (or 4 cells if imageUrls.length>=4). Back button + share button overlay.
   - Main content (scrollable):
     - Name + LGA + rating row.
     - Description.
     - ItineraryTimeline.
     - Attractions strip (horizontal of pkg.attractions cards with name + image).
     - Optional property card (if pkg.property).
     - Optional events list (if pkg.events.length).
     - Guide profile card: avatar + name + yearsExp + languages + rating + (tappable for future guide profile screen — for v1 inert).
     - Spacer 140pt so sticky sheet doesn't cover the last line.
   - Sticky TourBookingSheet at bottom (absolute, bottom inset via useSafeAreaInsets).
   - Mutation: useMutation POST /tour-bookings; on success branches:
     * response.payment?.authorizationUrl -> WebBrowser.openAuthSessionAsync(url, 'iseyaa://booking-callback').
     * response.splitBillJoinLink -> setShowSplitBillSheet(true) with the link.

  No inline hex. expo-image only. Touch targets >= 44pt.</action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Book hub has 5th sub-section. TourCard + ItineraryTimeline + tours/[id] all render. Slug navigation works. Sticky sheet branches between Paystack + split-bill share. tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Create TourBookingSheet + SplitBillShareSheet + trips/index + RatingModal + tours/rate/[bookingId]</name>
  <files>mobile/components/tours/TourBookingSheet.tsx, mobile/components/tours/SplitBillShareSheet.tsx, mobile/app/trips/index.tsx, mobile/components/tours/RatingModal.tsx, mobile/app/tours/rate/[bookingId].tsx</files>
  <action>**TourBookingSheet.tsx** — mirror Phase 8's NightlyBookingSheet shape (rounded top, SURFACE_RAISED bg, BORDER_GLASS top border, 24px inner padding). Fields:
   - Date picker via @react-native-community/datetimepicker (mode='date', minimumDate=today).
   - Passenger stepper (1..pkg.maxGroupSize, default 2). Show "Bulk discount: 10% off" caption when count >= 10; "Bulk discount: 20% off" at >= 25.
   - Split-bill toggle (Switch).
   - Email TextInput (defaultEmail prefill, email validation, autoCapitalize='none').
   - Total row: pkg.price * passengerCount (client-side; server is source of truth; show "Tax + fees on next page" caption since bulk discount applies server-side).
   - Primary "Reserve tour" button -> onSubmit({ tourPackageId, tourDate, passengerCount, splitBillEnabled, email }).
   - "Sign in to book" button when !signedIn that routes to /auth/phone.

  **SplitBillShareSheet.tsx** — Modal triggered after split-bill response. Renders the splitBillJoinLink with: a "Copy link" button (Clipboard.setStringAsync), a "Share via WhatsApp" button (Linking.openURL('https://wa.me/?text=' + encodedLink)), a "Share native" button (Share.share({ message: link })). Close button. Use existing tokens.

  **trips/index.tsx** — useQuery for /tour-bookings/me. Sectioned list: "Upcoming" (tourDate >= today, status in {PENDING, CONFIRMED}) + "Past" (tourDate < today OR status in {CHECKED_OUT, CANCELLED, REFUNDED}). Each row: package coverImageUrl thumbnail, name, tourDate, status pill (color per status: PENDING=amber, CONFIRMED=gold, CHECKED_OUT=forest, CANCELLED/REFUNDED=jungle/grey). Tap routes to /tours/<slug-from-snapshot>. Empty state: "No tours yet — explore packages" linking to Book hub Tours.

  **RatingModal.tsx** — 3-tab (or 3-section) modal:
   - Tab 1: Rate guide — 5-star picker, optional comment, optional photos (file picker stub for v1 — just URL inputs).
   - Tab 2: Rate package.
   - Tab 3: Rate venue (radio of attractionIds + propertyId names).
   - Each submit POSTs to /reviews with the right targetType + targetId.
   - All 3 can be submitted in one session or skipped; show progress (e.g. "2 of 3 submitted").
   - Close button.

  **tours/rate/[bookingId].tsx** — minimal screen that fetches /tour-bookings/:id and renders RatingModal with the booking context. This is the deep-link target for the T+1h "Rate your tour" push notification.

  No inline hex throughout. All use mobile/lib/tokens.ts.</action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>5 files + 1 screen file exist. Booking sheet wires submit + branches response. Split-bill share sheet renders. Trips list sections correctly. RatingModal POSTs 3 reviews independently.</done>
</task>

</tasks>

<verification>
- `cd mobile && npx tsc --noEmit` passes.
- `grep -c "TOUR_CATEGORIES" mobile/lib/category-config.ts` returns >= 1.
- `grep -E 'name="(tours/\\[id\\]|trips/index|tours/rate)' mobile/app/_layout.tsx` returns 3 matches.
- `grep -rEn "#[0-9a-fA-F]{6}" mobile/components/tours/ mobile/app/tours/ mobile/app/trips/` returns 0 (no inline hex).
- Manual smoke (on emulator/device):
   - Book hub shows 5 sub-sections (Events/Stays/Studio/Marketplace/Tours).
   - Tours sub-section shows the APPROVED tour grid filtered by selected category.
   - Tapping a tour opens /tours/<slug>.
   - Booking sheet submit with split-bill OFF opens Paystack in expo-web-browser.
   - Booking sheet submit with split-bill ON opens SplitBillShareSheet with copy + WhatsApp.
   - /trips/index lists user's bookings sectioned.
   - /tours/rate/<bookingId> renders the 3-tab rating modal and POSTs reviews.
- All existing 282+ mobile tests still pass: `cd mobile && npm test` (if there are any; otherwise verify build still succeeds via tsc).
</verification>

<success_criteria>
- TOUR-03 mobile closed: Book hub Tours sub-section with category-tabbed grid.
- TOUR-05 mobile closed: tours/[id] renders structured itinerary timeline + booking sheet; trips/index shows user's tours with itinerary.
- TOUR-06 mobile closed: split-bill toggle + share sheet with native + WhatsApp share.
- TOUR-07 mobile closed: RatingModal with 3 target types.
- No inline hex; all from tokens.
- Reuses Phase 8 primitives (PressableScale, CategoryStrip, Chip).
</success_criteria>

<output>
Create `.planning/phases/09-tour-packages-tour-guides/09-11-SUMMARY.md` with: 3 new screens, 5 new components, 1 category-config extension, 3 new Stack routes, the slug-vs-id Stack route file-naming note, and confirmation that no new mobile deps were added.
</output>
</content>
</invoke>
