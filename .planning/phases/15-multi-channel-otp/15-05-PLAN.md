---
phase: 15-multi-channel-otp
plan: 05
type: execute
wave: 3
depends_on: ["15-03"]
files_modified:
  - mobile/app/auth/phone.tsx
  - mobile/app/auth/otp.tsx
autonomous: true
requirements: [OTP-01, OTP-02]

must_haves:
  truths:
    - "At registration, the user sees three selectable channel options (SMS/WhatsApp/Email) with SMS pre-selected by default, and can submit their choice with the phone number (OTP-01)"
    - "When Email is selected, the user must supply an email address before the send CTA activates — there is no other source for this address in the phone-only registration flow"
    - "If the backend reports a fallback occurred, the verification screen visibly tells the user their code arrived via SMS instead, using the exact locked copy from the UI design contract (OTP-02, D-10)"
  artifacts:
    - path: "mobile/app/auth/phone.tsx"
      provides: "3-card channel picker + conditional email input, submitted on POST /auth/otp/send"
      contains: "channel"
    - path: "mobile/app/auth/otp.tsx"
      provides: "Fallback Notice Banner shown when fallbackUsed is true"
      contains: "We sent your code via SMS instead"
  key_links:
    - from: "mobile/app/auth/phone.tsx"
      to: "POST /auth/otp/send"
      via: "api.post('/auth/otp/send', { phone, channel, email? })"
      pattern: "channel"
    - from: "mobile/app/auth/phone.tsx"
      to: "mobile/app/auth/otp.tsx"
      via: "router.push params: { phone, fallbackUsed }"
      pattern: "fallbackUsed"
---

<objective>
Wire the registration channel picker (UI-SPEC Component 1) into `phone.tsx` and the fallback-notice banner (UI-SPEC Component 2) into `otp.tsx` — the two screens that make up the mobile app's actual live OTP registration flow (confirmed by RESEARCH.md Pitfall 2: `phone.tsx` → `POST /auth/otp/send` → `otp.tsx` → `POST /auth/phone-auth`, not the unused `RegisterDto` flow). Both screens are additive insertions into the existing gradient/kicker/title layout — no redesign.

Purpose: this is the literal, user-facing half of OTP-01 (channel selection at registration) and OTP-02 (visible fallback notice, D-10's locked copy). Depends on Plan 15-03 because the request/response shapes (`channel`/`email` on send, `fallbackUsed` in the response) are defined there.
Output: a working 3-option channel picker with conditional email capture on `phone.tsx`, and a fallback banner on `otp.tsx` driven by the backend's `fallbackUsed` flag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-multi-channel-otp/15-CONTEXT.md
@.planning/phases/15-multi-channel-otp/15-UI-SPEC.md
@.planning/phases/15-multi-channel-otp/15-PATTERNS.md

<interfaces>
<!-- Backend contract this plan calls against (Plan 15-03, already landed). -->
POST /auth/otp/send
  request:  { phone: string, channel?: 'SMS' | 'WHATSAPP' | 'EMAIL', email?: string }
            (email required only when channel === 'EMAIL')
  response: { message: string, fallbackUsed: boolean }

POST /auth/phone-auth  — unchanged request shape { phone, otp }; response unaffected by this plan.

<!-- mobile/lib/tokens.ts values this plan uses (already exported, read directly this session): -->
GOLD = '#D4A843'; GOLD_DIM = 'rgba(212,168,67,0.15)'; GOLD_LINE = 'rgba(212,168,67,0.40)'
SUCCESS_DIM = 'rgba(46,204,113,0.14)'; SUCCESS_TEXT = '#7BE7A2'
INK = '#FFFFFF'; INK_DIM = 'rgba(255,255,255,0.45)'; BORDER = 'rgba(255,255,255,0.06)'
SPACE_2 = 8; SPACE_3 = 12; SPACE_4 = 16; SPACE_5 = 20
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: phone.tsx — channel picker + conditional email input</name>
  <files>mobile/app/auth/phone.tsx</files>
  <read_first>
    mobile/app/auth/phone.tsx (full file, 240 lines — current state: handleContinue() lines 56-71, inputWrapper/inputWrapperActive styles lines 185-199, cta/ctaDisabled styles lines 222-230)
    mobile/lib/tokens.ts (full file — confirm exact export names/values used below)
    .planning/phases/15-multi-channel-otp/15-UI-SPEC.md (Component 1 "Channel Picker" — exact dimensions, colors, copy, icon mapping)
  </read_first>
  <action>
    In `mobile/app/auth/phone.tsx`, add `MessageSquare, MessageCircle, Mail` to the existing `lucide-react-native` import (the file currently has no lucide import — add a new `import { MessageSquare, MessageCircle, Mail } from 'lucide-react-native';` line), and add `GOLD_DIM` to the existing `from '../../lib/tokens'` import list. Add local state `const [channel, setChannel] = useState<'SMS' | 'WHATSAPP' | 'EMAIL'>('SMS');` and `const [email, setEmail] = useState('');` near the existing `phone`/`loading` state. Insert a new section between the existing phone `inputWrapper` (ends line 120) and the `cta` TouchableOpacity (starts line 122): a label `Text` reading `How should we reach you?` (Caption style: fontSize 12, fontWeight '700', color `INK_MID`, letterSpacing 0.3, marginBottom 8 — matches `mobile/lib/tokens.ts`'s existing `INK_MID` export, no new token needed), then a `View` with `flexDirection: 'row', gap: 8, marginBottom: 16` containing three pressable cards (SMS → `MessageSquare` icon + label `SMS`; WhatsApp → `MessageCircle` icon + label `WhatsApp`; Email → `Mail` icon + label `Email`), each `flex: 1, height: 76, borderRadius: 14, borderWidth: 1.5`, unselected state `backgroundColor: SURFACE_MID, borderColor: 'rgba(255,255,255,0.06)'` with the icon/label colored `INK_MID`, selected state `backgroundColor: GOLD_DIM, borderColor: GOLD_LINE` with the icon/label colored `GOLD` — per UI-SPEC Component 1 exactly. Each card's `onPress` sets `channel` to its value; add `accessibilityRole="radio"`, `accessibilityState={{ selected: channel === 'SMS' }}` (etc. per card), and `accessibilityLabel={\`${label} verification channel\`}`. When `channel === 'EMAIL'`, render an additional `TextInput` directly below the picker row, reusing the exact `inputWrapper`/`inputWrapperActive` style pair (lines 185-199) with `keyboardType="email-address"`, `autoCapitalize="none"`, `placeholder="you@example.com"`, bound to `email`/`setEmail` — this field does not exist in the UI design contract because the contract only specifies the 3-card picker, but it is required: the phone-only registration flow has no other source for an email address when Email is selected (RESEARCH.md's own `sendOtpEmail(to, ...)` signature requires one), so this is a necessary functional addition using the screen's established input styling, not a redesign. Update `handleContinue()`: change the `api.post('/auth/otp/send', { phone: formattedPhone })` call to `api.post('/auth/otp/send', { phone: formattedPhone, channel, ...(channel === 'EMAIL' ? { email } : {}) })`; unwrap the response the same way `otp.tsx`'s `verify()` does (`const payload = res.data?.data ?? res.data ?? {};`) and read `const fallbackUsed = payload.fallbackUsed === true;`; change the subsequent `router.push({ pathname: '/auth/otp', params: { phone: formattedPhone } })` call to also include `fallbackUsed: String(fallbackUsed)` in `params`. Update the `isReady` derivation (currently `phone.replace(/\s/g, '').length >= 10`) to additionally require, when `channel === 'EMAIL'`, that `email` matches a minimal `/\S+@\S+\.\S+/` pattern — i.e. `const isReady = phone.replace(/\s/g, '').length >= 10 && (channel !== 'EMAIL' || /\S+@\S+\.\S+/.test(email));` — so the CTA stays disabled until a plausible email is entered for the Email channel.
  </action>
  <acceptance_criteria>
    - `mobile/app/auth/phone.tsx` renders exactly 3 channel option cards with labels `SMS`, `WhatsApp`, `Email`, defaulting to `SMS` selected on mount.
    - `mobile/app/auth/phone.tsx` conditionally renders an email `TextInput` only when `channel === 'EMAIL'`.
    - `mobile/app/auth/phone.tsx`'s `handleContinue()` posts `channel` (and `email` only when `channel === 'EMAIL'`) to `/auth/otp/send`, and forwards `fallbackUsed` as a route param to `/auth/otp`.
    - `cd mobile && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Registration screen lets the user pick SMS/WhatsApp/Email (SMS default), captures an email address only when needed, and forwards the channel choice plus the backend's fallback flag onward to the verification screen.</done>
</task>

<task type="auto">
  <name>Task 2: otp.tsx — Fallback Notice Banner</name>
  <files>mobile/app/auth/otp.tsx</files>
  <read_first>
    mobile/app/auth/otp.tsx (full file, 265 lines — current state: useLocalSearchParams destructuring line 35, resend() lines 71-78, content layout around the "Sent to {maskedPhone}" subtitle line 115)
    .planning/phases/15-multi-channel-otp/15-UI-SPEC.md (Component 2 "Fallback Notice Banner" — exact container/text spec and LOCKED copy, do not vary wording)
  </read_first>
  <action>
    In `mobile/app/auth/otp.tsx`, add `Info` to a new `import { Info } from 'lucide-react-native';` line, and add `SUCCESS_DIM, SUCCESS_TEXT, INK_DIM` to the existing `from '../../lib/tokens'` import list. Change `const { phone } = useLocalSearchParams<{ phone: string }>();` to `const { phone, fallbackUsed: fallbackUsedParam } = useLocalSearchParams<{ phone: string; fallbackUsed?: string }>();` and add `const [fallbackUsed, setFallbackUsed] = useState(fallbackUsedParam === 'true');`. Insert the Fallback Notice Banner directly below the existing `<Text style={styles.sub}>Sent to {maskedPhone}</Text>` line and above the OTP-boxes `TouchableOpacity`, rendered only `{fallbackUsed && ( ... )}`: a `View` with `flexDirection: 'row', alignItems: 'flex-start', gap: 8, backgroundColor: SUCCESS_DIM, borderWidth: 1, borderColor: 'rgba(46,204,113,0.30)', borderRadius: 12, padding: 12, paddingHorizontal: 16, marginBottom: 20`, containing an `Info` icon (`size={16}`, `color={SUCCESS_TEXT}`, `style={{ marginTop: 1 }}`) and a text block (`flex: 1`) with two `Text` lines: primary `We sent your code via SMS instead` (fontSize 14, fontWeight '700', color `INK`) and secondary `Your original channel didn't respond in time.` (fontSize 12, fontWeight '400', color `INK_DIM`, marginTop 2) — this copy is LOCKED per CONTEXT.md D-10, do not reword. Add `accessibilityLiveRegion="polite"` and `accessibilityRole="text"` on the outer `View` so screen readers announce it without requiring focus. In the `resend()` function, update the `api.post('/auth/otp/send', { phone })` call to `api.post('/auth/otp/send', { phone })` unchanged in request shape (resend always reuses whatever channel is already on file server-side — no channel re-selection per D-05), but unwrap its response the same way `handleContinue()` does in `phone.tsx` and call `setFallbackUsed(payload.fallbackUsed === true)` so a resend's own fallback outcome updates the banner.
  </action>
  <acceptance_criteria>
    - `mobile/app/auth/otp.tsx` contains the exact literal string `We sent your code via SMS instead`.
    - `mobile/app/auth/otp.tsx` contains the exact literal string `Your original channel didn't respond in time.`
    - The banner `View` renders only when local `fallbackUsed` state is `true`, and that state is seeded from the `fallbackUsed` route param and updated again after `resend()`.
    - `cd mobile && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Verification screen visibly and accessibly informs the user when their code arrived via SMS fallback, both on initial send and after a manual resend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Mobile client → `POST /auth/otp/send` | User-entered `channel`/`email` cross here; server-side validation (Plan 15-01's DTOs) is the actual enforcement point, this plan is presentation-layer only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Information Disclosure | Email address typed into `phone.tsx`'s conditional input | accept | Transmitted over the existing HTTPS API client (`mobile/lib/api.ts`), same trust level as the phone number already sent on this screen; no new storage introduced client-side |
</threat_model>

<verification>
- `cd mobile && npx tsc --noEmit` exits 0 after both tasks
</verification>

<success_criteria>
- OTP-01: user can select SMS/WhatsApp/Email at registration, SMS pre-selected; Email selection requires (and captures) an email address before the CTA activates.
- OTP-02/D-10: a fallback-to-SMS is visibly communicated on the verification screen with the exact locked copy, on both initial send and resend.
</success_criteria>

<output>
After completion, create `.planning/phases/15-multi-channel-otp/15-05-SUMMARY.md`
</output>
