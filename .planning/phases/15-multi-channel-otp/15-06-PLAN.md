---
phase: 15-multi-channel-otp
plan: 06
type: execute
wave: 3
depends_on: ["15-04"]
files_modified:
  - mobile/app/otp-channel-settings.tsx
  - mobile/app/(tabs)/profile.tsx
  - mobile/app/_layout.tsx
autonomous: true
requirements: [OTP-01]

must_haves:
  truths:
    - "A signed-in user can navigate from their Profile tab to a dedicated screen showing their current OTP channel, select a different one, and see it take effect immediately (D-07)"
    - "The Profile tab's new menu entry shows the user's current channel as its subtitle, sourced from a live query, not a hardcoded string"
  artifacts:
    - path: "mobile/app/otp-channel-settings.tsx"
      provides: "Instant-apply 3-row channel settings screen, PATCH /users/me/otp-channel on selection"
      contains: "otp-channel"
    - path: "mobile/app/(tabs)/profile.tsx"
      provides: "New Verification Channel MenuRow entry navigating to the settings screen"
      contains: "Verification Channel"
  key_links:
    - from: "mobile/app/(tabs)/profile.tsx"
      to: "mobile/app/otp-channel-settings.tsx"
      via: "router.push('/otp-channel-settings')"
      pattern: "otp-channel-settings"
    - from: "mobile/app/otp-channel-settings.tsx"
      to: "PATCH /users/me/otp-channel"
      via: "api.patch('/users/me/otp-channel', { channel })"
      pattern: "otp-channel"
---

<objective>
Build the post-registration settings surface for D-07: a new flat-file screen `mobile/app/otp-channel-settings.tsx` (UI-SPEC Component 3) showing the three channel options as instant-apply rows, reachable from a new Profile-tab menu entry (UI-SPEC Component 4). Registers the new route in `_layout.tsx` following the existing `kyc` flat-screen precedent — this project has no nested `settings/` route group, every standalone screen (`kyc.tsx`, `host.tsx`, `topup.tsx`, `notifications.tsx`) is a flat file explicitly registered as a `Stack.Screen`.

Purpose: D-07 requires this as net-new UI/endpoint work this phase, not deferred. Depends on Plan 15-04 because the screen calls the settings endpoint and reads `otpChannel` from `GET /users/me` defined there.
Output: a working, accessible settings screen with optimistic-update + revert-on-failure semantics, and a discoverable entry point from the Profile tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-multi-channel-otp/15-CONTEXT.md
@.planning/phases/15-multi-channel-otp/15-UI-SPEC.md
@.planning/phases/15-multi-channel-otp/15-PATTERNS.md

<interfaces>
<!-- Backend contract this plan calls against (Plan 15-04, already landed). -->
GET /users/me            -> { ..., otpChannel: 'SMS' | 'WHATSAPP' | 'EMAIL', ... }
PATCH /users/me/otp-channel  body: { channel: 'SMS' | 'WHATSAPP' | 'EMAIL' }  -> updated user object

<!-- mobile/lib/tokens.ts values this plan uses (already exported, read directly this session): -->
SURFACE_MID = '#0D1F1F'; SURFACE_RAISED = '#162B2B'; SURFACE_HIGH = '#1E3535'
GOLD = '#D4A843'; GOLD_DIM = 'rgba(212,168,67,0.15)'
INK = '#FFFFFF'; INK_SECONDARY = 'rgba(255,255,255,0.60)'; INK_FAINT = 'rgba(255,255,255,0.25)'
BORDER_SUBTLE = 'rgba(255,255,255,0.07)'; ERROR_TEXT = '#F08F8F'
SPACE_2 = 8; SPACE_4 = 16; SPACE_6 = 24; RADIUS_LG = 20

<!-- mobile/app/(tabs)/profile.tsx's existing UserProfile interface (lines 70-83) — does NOT yet
     declare otpChannel. Task 2 must extend it before referencing user?.otpChannel, or
     `npx tsc --noEmit` fails under strict mode: -->
interface UserProfile {
  id: string;
  name?: string;
  phone?: string;
  role: string;
  kyc_tier?: number;
  kyc_verified?: boolean;
  created_at?: string;
  trips_count?: number;
  stays_count?: number;
  registeredRoles?: string[];
  isHost?: boolean;
  // otpChannel is added by Task 2 below
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: otp-channel-settings.tsx screen + Stack.Screen registration</name>
  <files>mobile/app/otp-channel-settings.tsx, mobile/app/_layout.tsx</files>
  <read_first>
    mobile/app/kyc.tsx (lines 1-120 — SafeAreaView/ScrollView/useQuery/useQueryClient scaffolding and TierCard selectable-state visual pattern to mirror structurally)
    mobile/app/(tabs)/profile.tsx (lines 1-70 and 262-334 — MenuRow component contract, icon imports, tokens.ts import list)
    mobile/lib/tokens.ts (full file — confirm exact export names used below)
    mobile/app/_layout.tsx (lines 42-65 — existing Stack.Screen registrations, kyc precedent at line 45)
    .planning/phases/15-multi-channel-otp/15-UI-SPEC.md (Component 3 "Settings Screen — Verification Channel" — exact layout, copy, instant-apply behavior, accessibility roles)
  </read_first>
  <action>
    Create `mobile/app/otp-channel-settings.tsx` (new flat file — this project has no `settings/` route group; every standalone screen is a flat file under `mobile/app/`, matching `kyc.tsx`/`host.tsx`/`topup.tsx`). Import `SafeAreaView` from `'react-native-safe-area-context'`, `useQuery, useQueryClient` from `'@tanstack/react-query'`, `api, fetcher` from `'../lib/api'`, `router` from `'expo-router'`, and `ChevronLeft, MessageSquare, MessageCircle, Mail, CheckCircle2` from `'lucide-react-native'`; import `SURFACE_MID, SURFACE_RAISED, SURFACE_HIGH, GOLD, GOLD_DIM, INK, INK_SECONDARY, INK_FAINT, BORDER_SUBTLE, ERROR_TEXT` from `'../lib/tokens'`. Fetch the current user via `useQuery(['me'], () => fetcher('/users/me'))` (matches `profile.tsx`'s existing query key exactly, so both screens share the same React Query cache entry). Render a `SafeAreaView` (`edges={['top']}`, background `SURFACE_MID`) containing: a 56px header row with a 44×44 `ChevronLeft` back button (`onPress={() => router.back()}`, `accessibilityLabel="Go back"`) and the title `Verification Channel` (22px, weight 700, color `INK`); a subheading `Choose how you'd like to receive your verification codes.` (14px, weight 400, color `INK_SECONDARY`, lineHeight 21, marginBottom 24); an option group `View` (`backgroundColor: SURFACE_RAISED, borderRadius: 20, borderWidth: 1, borderColor: BORDER_SUBTLE, overflow: 'hidden'`) containing exactly 3 rows in this order — SMS (`MessageSquare` icon, label `SMS`, sub `Delivered via text message`), WhatsApp (`MessageCircle` icon, label `WhatsApp`, sub `Delivered via WhatsApp`), Email (`Mail` icon, label `Email`, sub `Delivered via email`) — each row `flexDirection: 'row', alignItems: 'center', paddingHorizontal: 16, paddingVertical: 16`, all but the last row bottom-bordered with `BORDER_SUBTLE`, a 32×32 icon box (`borderRadius: 9`, selected `backgroundColor: GOLD_DIM` + icon `color: GOLD`, unselected `backgroundColor: SURFACE_HIGH` + icon `color: INK_SECONDARY`), a label block (title 14px weight 700 `INK`, sub 12px weight 400 `INK_FAINT`), and a trailing slot showing `CheckCircle2` (`size={20}`, `color={GOLD}`) when selected, an `ActivityIndicator` (`size="small"`, `color={GOLD}`) while that specific row is saving, or a 20px empty spacer otherwise. Track local state `const [channel, setChannel] = useState(user?.otpChannel);` synced from the query result via a `useEffect`, plus `const [saving, setSaving] = useState<string | null>(null);` and `const [error, setError] = useState(false);`. On row press: optimistically `setChannel(newChannel)`, `setSaving(newChannel)`, `setError(false)`, then `await api.patch('/users/me/otp-channel', { channel: newChannel })` — on success, `queryClient.invalidateQueries(['me'])` (so `profile.tsx`'s query re-fetches too) and clear `saving`; on failure, revert `setChannel(previousChannel)`, clear `saving`, and `setError(true)`. Below the option group, conditionally render inline error text `Could not update your verification channel. Please try again.` (12px, weight 400, color `ERROR_TEXT`, marginTop 8) when `error` is true. Add `accessibilityRole="radiogroup"` + `accessibilityLabel="Verification channel"` on the option-group container, and `accessibilityRole="radio"` + `accessibilityState={{ selected }}` on each row. In `mobile/app/_layout.tsx`, add `<Stack.Screen name="otp-channel-settings" options={{ headerShown: false, presentation: 'card' }} />` directly after the existing `<Stack.Screen name="kyc" ... />` line (line 45) — `headerShown: false` because this screen renders its own custom header row, matching the UI-SPEC's Component 3 spec rather than `kyc`'s native-header convention.
  </action>
  <acceptance_criteria>
    - `mobile/app/otp-channel-settings.tsx` renders exactly 3 rows in the order SMS, WhatsApp, Email, each with the exact sub-label copy specified above.
    - Tapping a row calls `api.patch('/users/me/otp-channel', { channel: <value> })` and optimistically updates the selected row before the request resolves.
    - A failed PATCH reverts the selection and shows the exact literal string `Could not update your verification channel. Please try again.`
    - `mobile/app/_layout.tsx` contains `<Stack.Screen name="otp-channel-settings"`.
    - `cd mobile && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>otp-channel-settings.tsx is a fully functional, accessible, instant-apply settings screen registered as a Stack route.</done>
</task>

<task type="auto">
  <name>Task 2: Profile tab — Verification Channel menu entry</name>
  <files>mobile/app/(tabs)/profile.tsx</files>
  <read_first>
    mobile/app/(tabs)/profile.tsx (lines 1-83 imports + UserProfile interface, lines 262-334 MenuRow component + menuStyles, lines 397-429 menuRows array, lines 594-608 render call — full context needed to insert correctly without disturbing the existing Security & ID row)
    .planning/phases/15-multi-channel-otp/15-UI-SPEC.md (Component 4 "Profile (You tab) — new Menu Row entry")
  </read_first>
  <action>
    First, extend `mobile/app/(tabs)/profile.tsx`'s existing `UserProfile` interface (lines 70-83) by adding a new optional property `otpChannel?: 'SMS' | 'WHATSAPP' | 'EMAIL';` (place it after the existing `isHost?: boolean;` line, matching the backend's `GET /users/me` contract from Plan 15-04). This interface does not declare `otpChannel` today — referencing `user?.otpChannel` without this addition fails `mobile/tsconfig.json`'s `strict: true` check with `Property 'otpChannel' does not exist on type 'UserProfile'`.

    Then, add `MessageSquare` to the existing `lucide-react-native` import list (alongside `Check, CheckCircle, ChevronRight, Car, Shield, Ticket, ShoppingBag, Heart, Clock, Home`). In the `menuRows: MenuRowItem[]` array (currently lines 397-429), insert a new entry immediately before the existing `Security & ID` entry (which currently has `isLast: true`): `{ icon: MessageSquare, label: 'Verification Channel', sub: <dynamic, see below>, onPress: () => router.push('/otp-channel-settings' as any) }`. For the dynamic `sub` value, derive a title-cased label from the already-fetched `user` query result (the same `useQuery(['me'], ...)` call already in this file, lines 345-348): map `user?.otpChannel` (`'SMS' | 'WHATSAPP' | 'EMAIL'`) to `'SMS'`, `'WhatsApp'`, `'Email'` respectively, defaulting to `'SMS'` when the field is not yet loaded — e.g. a small local helper `function otpChannelLabel(channel?: string) { if (channel === 'WHATSAPP') return 'WhatsApp'; if (channel === 'EMAIL') return 'Email'; return 'SMS'; }` placed near the file's other small helpers (`getInitials`, `getHandle`, `getMemberSince`). Do not add `isLast: true` to the new entry — leave that flag on the existing `Security & ID` entry as the array's final item, unchanged.
  </action>
  <acceptance_criteria>
    - `mobile/app/(tabs)/profile.tsx`'s `UserProfile` interface declares `otpChannel?: 'SMS' | 'WHATSAPP' | 'EMAIL';`.
    - `mobile/app/(tabs)/profile.tsx`'s `menuRows` array contains an entry with `label: 'Verification Channel'` positioned immediately before the `Security & ID` entry.
    - That entry's `onPress` calls `router.push('/otp-channel-settings' as any)`.
    - That entry's `sub` value is derived from `user?.otpChannel` via a small mapping helper, not a hardcoded string.
    - `cd mobile && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Profile tab surfaces a discoverable, live-data-driven entry point into the new OTP channel settings screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Mobile client → `PATCH /users/me/otp-channel` | Authenticated request; the actual access-control enforcement lives server-side in Plan 15-04 (`@CurrentUser()`-derived target), this plan is presentation-layer only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Denial of Service (self) | Optimistic UI update without server confirmation | mitigate | On PATCH failure, the screen reverts the optimistic selection and surfaces an inline error — a user is never left believing a channel change succeeded when it did not |
</threat_model>

<verification>
- `cd mobile && npx tsc --noEmit` exits 0 after both tasks
</verification>

<success_criteria>
- D-07: a user can reach a dedicated settings screen from the Profile tab and change their OTP channel post-registration, with the change taking effect immediately and surviving a failed-save revert correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/15-multi-channel-otp/15-06-SUMMARY.md`
</output>
