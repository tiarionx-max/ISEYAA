---
phase: 15-multi-channel-otp
plan: 04
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - backend/src/modules/users/dto/change-otp-channel.dto.ts
  - backend/src/modules/users/users.controller.ts
  - backend/src/modules/users/users.service.ts
  - backend/src/modules/users/__tests__/users.service.spec.ts
autonomous: true
requirements: [OTP-01]

must_haves:
  truths:
    - "An authenticated user can change their OTP channel preference after registration via a dedicated endpoint, and GET /users/me reflects the new value immediately (D-07)"
    - "Only the requesting user's own otpChannel can be changed — the target user is always derived from the JWT (@CurrentUser()), never from a client-supplied id"
  artifacts:
    - path: "backend/src/modules/users/dto/change-otp-channel.dto.ts"
      provides: "ChangeOtpChannelDto — single required channel: OtpChannel field"
      contains: "ChangeOtpChannelDto"
    - path: "backend/src/modules/users/users.controller.ts"
      provides: "PATCH /users/me/otp-channel route"
      contains: "otp-channel"
  key_links:
    - from: "backend/src/modules/users/users.controller.ts"
      to: "backend/src/modules/users/users.service.ts"
      via: "changeOtpChannel(@CurrentUser() user, @Body() dto) -> usersService.updateOtpChannel(user.userId, dto.channel)"
      pattern: "updateOtpChannel"
---

<objective>
Add the settings-screen backend surface (D-07): a dedicated `PATCH /users/me/otp-channel` route, its `ChangeOtpChannelDto`, and `UsersService.updateOtpChannel()`, plus expose `otpChannel` on `GET /users/me` so the mobile settings screen (Plan 15-06) can read the current value. This plan is independent of Plan 15-03 (different files) — both consume Plan 15-01's `OtpChannel` enum in parallel.

Purpose: D-07 explicitly requires letting a user change their OTP channel after registration, net-new this phase, not deferred. Mirrors the existing `PATCH /users/me/role` (`SwitchRoleDto`) precedent already established in this controller.
Output: a working, auth-guarded channel-change endpoint and a `USER_SELECT` projection that surfaces `otpChannel` on every user-profile read.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-multi-channel-otp/15-CONTEXT.md
@.planning/phases/15-multi-channel-otp/15-RESEARCH.md
@.planning/phases/15-multi-channel-otp/15-PATTERNS.md

<interfaces>
<!-- Contract this plan creates, consumed by Plan 15-06's mobile settings screen. -->
PATCH /users/me/otp-channel
  body: { channel: 'SMS' | 'WHATSAPP' | 'EMAIL' }
  auth: JwtAuthGuard (class-level, already applied)
  response: full user object (USER_SELECT shape) including otpChannel

GET /users/me
  response now additionally includes: otpChannel: 'SMS' | 'WHATSAPP' | 'EMAIL'

From backend/src/common/enums/otp-channel.enum.ts (Plan 15-01, already landed):
export enum OtpChannel { SMS = 'SMS', WHATSAPP = 'WHATSAPP', EMAIL = 'EMAIL' }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChangeOtpChannelDto + PATCH /users/me/otp-channel + UsersService.updateOtpChannel()</name>
  <files>backend/src/modules/users/dto/change-otp-channel.dto.ts, backend/src/modules/users/users.controller.ts, backend/src/modules/users/users.service.ts</files>
  <read_first>
    backend/src/modules/users/users.controller.ts (full file — switchRole handler lines 59-64, class-level @UseGuards(JwtAuthGuard) at line 37, inline SwitchRoleDto lines 30-33)
    backend/src/modules/users/users.service.ts (full file — switchRole() lines 46-62, USER_SELECT constant lines 11-29)
    backend/src/modules/users/dto/verify-bvn.dto.ts (full file — dto/ subfolder single-field DTO convention to mirror, since this module already has a dto/ directory unlike the inline SwitchRoleDto exception)
    backend/src/common/enums/otp-channel.enum.ts (from Plan 15-01)
  </read_first>
  <behavior>
    - PATCH /users/me/otp-channel with { channel: 'WHATSAPP' } for an authenticated user updates that user's otpChannel and returns the updated USER_SELECT-shaped user object with otpChannel: 'WHATSAPP'.
    - PATCH /users/me/otp-channel with an invalid channel value (not SMS/WHATSAPP/EMAIL) is rejected by class-validator with a 400 before reaching the service.
    - GET /users/me's response object contains an otpChannel field.
  </behavior>
  <action>
    Create `backend/src/modules/users/dto/change-otp-channel.dto.ts` exporting `export class ChangeOtpChannelDto { @IsEnum(OtpChannel, { message: 'channel must be one of: ' + Object.values(OtpChannel).join(', ') }) channel: OtpChannel; }` — a single required field (unlike `OtpSendDto`'s optional `channel`, this endpoint's whole purpose is changing it, so it is not `@IsOptional()`). Import `OtpChannel` from `'../../../common/enums/otp-channel.enum'`. In `backend/src/modules/users/users.controller.ts`, import `ChangeOtpChannelDto` from `'./dto/change-otp-channel.dto'`, and add a new handler directly after the existing `switchRole` handler (lines 59-64): `@Patch('me/otp-channel')` + `@HttpCode(HttpStatus.OK)` + `@ApiOperation({ summary: 'Change OTP delivery channel preference' })`, method `changeOtpChannel(@CurrentUser() user: { userId: string }, @Body() dto: ChangeOtpChannelDto)` returning `this.usersService.updateOtpChannel(user.userId, dto.channel)` — mirrors `switchRole`'s exact shape, deriving the target user exclusively from the JWT-backed `@CurrentUser()` decorator, never from a client-supplied id. In `backend/src/modules/users/users.service.ts`, import `OtpChannel` from `'../../common/enums/otp-channel.enum'`, add `otpChannel: true` to the `USER_SELECT` constant's field list (lines 11-29, alongside the existing `kycStatus: true` line) so every method using `USER_SELECT` (`getMe`, `switchRole`, `becomeHost`, `becomeGuide`, `findById`) now surfaces the field, and add a new method `async updateOtpChannel(userId: string, channel: OtpChannel) { return this.prisma.user.update({ where: { id: userId }, data: { otpChannel: channel }, select: USER_SELECT }); }` directly after `switchRole()` — simpler than `switchRole()` since no registered-roles membership check applies (any of the three enum values is always valid for any user).
  </action>
  <acceptance_criteria>
    - `backend/src/modules/users/dto/change-otp-channel.dto.ts` exports `ChangeOtpChannelDto` with a required (non-optional) `channel: OtpChannel` field decorated with `@IsEnum(OtpChannel, ...)`.
    - `backend/src/modules/users/users.controller.ts` contains `@Patch('me/otp-channel')` and a `changeOtpChannel` handler that reads the target user exclusively from `@CurrentUser()`.
    - `backend/src/modules/users/users.service.ts`'s `USER_SELECT` constant contains `otpChannel: true`, and the file contains `async updateOtpChannel(userId: string, channel: OtpChannel)`.
    - `cd backend && npx tsc --noEmit -p tsconfig.build.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.build.json</automated>
  </verify>
  <done>PATCH /users/me/otp-channel exists, guarded by the class-level JwtAuthGuard, updates only the caller's own row, and every USER_SELECT-shaped response (including GET /users/me) now includes otpChannel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: updateOtpChannel() unit tests</name>
  <files>backend/src/modules/users/__tests__/users.service.spec.ts</files>
  <read_first>
    backend/src/modules/users/__tests__/users.service.spec.ts (full file — switchRole describe block lines 44-66 is the direct structural template)
    backend/src/modules/users/users.service.ts (post Task 1 edit)
  </read_first>
  <behavior>
    - updateOtpChannel(userId, 'WHATSAPP') calls prisma.user.update with data: { otpChannel: 'WHATSAPP' } and the USER_SELECT projection, returning the updated user.
    - No NotFoundException/membership check exists for this method — any of the three enum values succeeds unconditionally for any userId (class-validator already rejected malformed values before the service is reached).
  </behavior>
  <action>
    In `backend/src/modules/users/__tests__/users.service.spec.ts`, add a new `describe('updateOtpChannel', ...)` block (after the existing `switchRole` block) with a single test: mock `mockPrisma.user.update.mockResolvedValue({ id: 'u1', otpChannel: 'WHATSAPP' });`, call `await service.updateOtpChannel('u1', OtpChannel.WHATSAPP)`, and assert both `result.otpChannel === 'WHATSAPP'` and that `mockPrisma.user.update` was called with `expect.objectContaining({ where: { id: 'u1' }, data: { otpChannel: OtpChannel.WHATSAPP } })`. Import `OtpChannel` from `'../../../common/enums/otp-channel.enum'` at the top of the file alongside the existing `UserRole` import.
  </action>
  <acceptance_criteria>
    - `cd backend && npx jest --testPathPattern users.service.spec --no-coverage` passes with the new `updateOtpChannel` test green.
    - The new test asserts on both the returned value and the exact `prisma.user.update` call arguments.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx jest --testPathPattern users.service.spec --no-coverage</automated>
  </verify>
  <done>updateOtpChannel() is unit-tested and the full users.service.spec.ts suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client → `PATCH /users/me/otp-channel` | Untrusted `channel` input; authenticated caller identity crosses via JWT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Elevation of Privilege / IDOR | `PATCH /users/me/otp-channel` | mitigate | Target user id is derived exclusively from `@CurrentUser()` (JWT payload), never from the request body or a route param — matches every other `/users/me/*` route in this controller; there is no `:id`-parameterized variant of this endpoint |
| T-15-08 | Tampering | `ChangeOtpChannelDto.channel` | mitigate | `@IsEnum(OtpChannel)` rejects any value outside `SMS`/`WHATSAPP`/`EMAIL` at the HTTP boundary via the global `ValidationPipe` |
</threat_model>

<verification>
- `cd backend && npx jest --testPathPattern users.service.spec --no-coverage` passes after Task 2
- `cd backend && npx tsc --noEmit -p tsconfig.build.json` exits 0 after Task 1
</verification>

<success_criteria>
- A user can PATCH their own `otpChannel` via a dedicated, JWT-guarded, non-IDOR-able endpoint.
- `GET /users/me` and every other `USER_SELECT`-shaped response surface the current `otpChannel` value.
</success_criteria>

<output>
After completion, create `.planning/phases/15-multi-channel-otp/15-04-SUMMARY.md`
</output>
