---
phase: 15-multi-channel-otp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/20260718020000_phase15_multi_channel_otp/migration.sql
  - backend/src/common/enums/otp-channel.enum.ts
  - backend/src/modules/auth/dto/otp-send.dto.ts
  - backend/src/modules/auth/dto/phone-auth.dto.ts
  - backend/src/modules/auth/dto/register.dto.ts
  - backend/src/resilience/resilience.types.ts
  - .env.example
  - MANUAL-ACTIONS.md
autonomous: true
requirements: [OTP-01, OTP-04]

must_haves:
  truths:
    - "A client can submit an optional channel value (SMS/WHATSAPP/EMAIL) — and, when EMAIL, an email address — on the OTP-send request, and omitting channel resolves to SMS server-side in later plans"
    - "The resilience layer has distinct, independently-tunable circuit-breaker/timeout/retry policy slots reserved for the new metaWhatsapp and sendgrid vendors, separate from the existing termiiAuth policy"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "OtpChannel enum + User.otpChannel field, defaulting to SMS"
      contains: "enum OtpChannel"
    - path: "backend/src/common/enums/otp-channel.enum.ts"
      provides: "TypeScript OtpChannel enum consumed by DTOs and services"
      contains: "export enum OtpChannel"
    - path: "backend/src/resilience/resilience.types.ts"
      provides: "metaWhatsapp + sendgrid vendor keys and default thresholds"
      contains: "metaWhatsapp"
  key_links:
    - from: "backend/src/modules/auth/dto/otp-send.dto.ts"
      to: "backend/src/common/enums/otp-channel.enum.ts"
      via: "@IsEnum(OtpChannel) decorator import"
      pattern: "OtpChannel"
---

<objective>
Lay every contract this phase's downstream plans depend on: the `OtpChannel` enum (Prisma + TypeScript), the optional `channel`/`email` fields on the three auth DTOs, two new resilience vendor slots (`metaWhatsapp`, `sendgrid`), and the env-var + stakeholder-documentation scaffolding for the direct Meta WhatsApp Business Cloud API integration (D-01). This is Wave 1 — Plans 15-03, 15-04, 15-05, and 15-06 all read types/fields this plan creates.

Purpose: nothing else in this phase can compile against `OtpChannel`, the new DTO fields, or the new resilience vendor keys until this plan lands. It also produces D-03's required deliverable — the drafted Meta Authentication-category template text — as a stakeholder-facing document, not blocking code completion.
Output: migrated `otpChannel` column on `users`, `OtpChannel` TS enum, extended `OtpSendDto`/`PhoneAuthDto`/`RegisterDto`, extended `resilience.types.ts`, updated `.env.example`, and a new `MANUAL-ACTIONS.md` section for the Meta WhatsApp setup + template submission.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-multi-channel-otp/15-CONTEXT.md
@.planning/phases/15-multi-channel-otp/15-RESEARCH.md
@.planning/phases/15-multi-channel-otp/15-PATTERNS.md

<interfaces>
<!-- Contracts this plan creates, consumed verbatim by Plans 15-02 through 15-06. -->

OtpChannel (Prisma, backend/prisma/schema.prisma):
enum OtpChannel { SMS WHATSAPP EMAIL }
User.otpChannel OtpChannel @default(SMS)

OtpChannel (TypeScript, backend/src/common/enums/otp-channel.enum.ts):
export enum OtpChannel { SMS = 'SMS', WHATSAPP = 'WHATSAPP', EMAIL = 'EMAIL' }

OtpSendDto (backend/src/modules/auth/dto/otp-send.dto.ts) — final shape after this plan:
phone: string (existing, @IsMobilePhone('en-NG'))
channel?: OtpChannel (@IsEnum(OtpChannel) + @IsOptional())
email?: string (@ValidateIf((o) => o.channel === OtpChannel.EMAIL) + @IsEmail())

resilience.types.ts Vendor union gains: 'metaWhatsapp' | 'sendgrid'
RESILIENCE_DEFAULTS gains:
metaWhatsapp: { timeoutMs: 8_000, retryCount: 1, failureThreshold: 5, halfOpenAfterMs: 30_000 }
sendgrid: { timeoutMs: 8_000, retryCount: 1, failureThreshold: 5, halfOpenAfterMs: 30_000 }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema — OtpChannel enum + User.otpChannel field + migration</name>
  <files>backend/prisma/schema.prisma, backend/prisma/migrations/20260718020000_phase15_multi_channel_otp/migration.sql</files>
  <read_first>
    backend/prisma/schema.prisma (lines 28-40 UserStatus/KYCStatus enum block, lines 225-272 User model)
    .planning/phases/15-multi-channel-otp/15-RESEARCH.md (Code Examples section — Prisma schema additions)
    .planning/phases/14-ministry-dashboard/14-01-PLAN.md (Task 2 — migration convention precedent; this phase's migration does NOT need hand-authored ALTER TYPE ADD VALUE SQL, because OtpChannel is a brand-new enum type, not an extension of an existing enum already referenced by a live column — a plain prisma migrate dev diff is safe)
  </read_first>
  <action>
    In `backend/prisma/schema.prisma`, add a new enum block immediately after the existing `KYCStatus` enum (currently lines 35-40): `enum OtpChannel { SMS WHATSAPP EMAIL }` — bare identifiers, no `= 'STRING'` literals, matching the `UserStatus`/`KYCStatus` style exactly. In the `User` model, add `otpChannel OtpChannel @default(SMS)` directly after the existing `kycStatus KYCStatus @default(NONE)` field (currently line 235), before `lgaId`. Do not reorder or otherwise modify any other field or enum in the file. Then run `cd backend && npx prisma migrate dev --name phase15_multi_channel_otp` from the repo root — since `OtpChannel` is a brand-new enum type (not extending `UserRole` or any enum already referenced by a live column with existing rows), Prisma's diff engine generates a plain `CREATE TYPE "OtpChannel" AS ENUM (...)` plus `ALTER TABLE "users" ADD COLUMN "otpChannel" "OtpChannel" NOT NULL DEFAULT 'SMS'` migration safely, with zero risk of dropping an existing enum — hand-authoring the SQL (the pattern `14-01-PLAN.md` used for `UserRole`, which required `ALTER TYPE ... ADD VALUE`) is unnecessary here. Confirm the auto-generated migration folder appears under `backend/prisma/migrations/<timestamp>_phase15_multi_channel_otp/` (the exact timestamp is assigned by Prisma at run time; `20260718020000_phase15_multi_channel_otp` in this plan's frontmatter is indicative).
  </action>
  <acceptance_criteria>
    - `backend/prisma/schema.prisma` contains `enum OtpChannel {` with exactly the three members `SMS`, `WHATSAPP`, `EMAIL`.
    - `backend/prisma/schema.prisma`'s `User` model contains the literal line `otpChannel OtpChannel @default(SMS)`.
    - `cd backend && npx prisma migrate status` reports the database schema is up to date (no pending migrations).
    - `cd backend && npx tsc --noEmit -p tsconfig.build.json` reports zero errors referencing `otpChannel` (confirms the generated Prisma Client exposes the field).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx prisma migrate status</automated>
  </verify>
  <done>OtpChannel enum and User.otpChannel column exist in the live dev database; Prisma Client regenerated with the new field.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: OtpChannel TS enum + DTO field additions</name>
  <files>backend/src/common/enums/otp-channel.enum.ts, backend/src/modules/auth/dto/otp-send.dto.ts, backend/src/modules/auth/dto/phone-auth.dto.ts, backend/src/modules/auth/dto/register.dto.ts</files>
  <read_first>
    backend/src/common/enums/user-role.enum.ts (full file — style template for a bare string enum)
    backend/src/modules/auth/dto/otp-send.dto.ts (full file, 6 lines — current state)
    backend/src/modules/auth/dto/phone-auth.dto.ts (full file, 10 lines — current state)
    backend/src/modules/auth/dto/register.dto.ts (full file, 27 lines — role?: UserRole optional-enum precedent)
    backend/src/modules/waitlist/dto/join-waitlist.dto.ts (full file — @ValidateIf conditional-email-required precedent, lines 12-15)
  </read_first>
  <action>
    Create `backend/src/common/enums/otp-channel.enum.ts` exporting `export enum OtpChannel { SMS = 'SMS', WHATSAPP = 'WHATSAPP', EMAIL = 'EMAIL' }` — no `REGISTERABLE_ROLES`-style const export needed. In `backend/src/modules/auth/dto/otp-send.dto.ts`, add two optional fields after the existing `phone` field: `channel?: OtpChannel` decorated with `@IsEnum(OtpChannel, { message: 'channel must be one of: ' + Object.values(OtpChannel).join(', ') })` plus `@IsOptional()`; and `email?: string` decorated with `@ValidateIf((o) => o.channel === OtpChannel.EMAIL)` plus `@IsEmail({}, { message: 'A valid email is required when channel is EMAIL' })`. This `email` field exists because the phone-only OTP flow has no other source for an email address when the user picks the EMAIL channel — there is no email+password step in this flow (confirmed by RESEARCH.md Pitfall 2). In `backend/src/modules/auth/dto/phone-auth.dto.ts`, add the same optional `channel?: OtpChannel` field (`@IsEnum` + `@IsOptional()`) for API-shape consistency only — Plan 15-03's service layer will NOT read this field at verify time (channel is resolved server-side from the value persisted during `otp/send`, per RESEARCH.md Pitfall 3), so add no validation logic beyond the decorator pair and do not add an `email` field to this DTO. In `backend/src/modules/auth/dto/register.dto.ts`, add `channel?: OtpChannel` (`@IsEnum` + `@IsOptional()`) after the existing `role?: UserRole` field, for schema consistency only — `AuthService.register()`'s email+password flow never calls `sendOtp()` (RESEARCH.md Pitfall 2), so this field is not exercised by any live code path this phase. Import `OtpChannel` from `'../../../common/enums/otp-channel.enum'` in all three DTO files, matching the existing relative-import convention (no `@/` alias in `backend/`).
  </action>
  <acceptance_criteria>
    - `backend/src/common/enums/otp-channel.enum.ts` exports `OtpChannel` with exactly members `SMS`, `WHATSAPP`, `EMAIL`, each self-mapped to its own string value.
    - `backend/src/modules/auth/dto/otp-send.dto.ts` contains both `channel?: OtpChannel` and `email?: string`, the latter decorated with `@ValidateIf((o) => o.channel === OtpChannel.EMAIL)`.
    - `backend/src/modules/auth/dto/phone-auth.dto.ts` and `backend/src/modules/auth/dto/register.dto.ts` each contain `channel?: OtpChannel` and neither gains an `email` field.
    - `cd backend && npx tsc --noEmit -p tsconfig.build.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.build.json</automated>
  </verify>
  <done>OtpChannel TS enum created; all three auth DTOs carry the optional channel field, otp-send.dto.ts additionally carries the conditionally-required email field; backend type-checks cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Resilience vendor registration + env var scaffolding + WhatsApp template deliverable</name>
  <files>backend/src/resilience/resilience.types.ts, .env.example, MANUAL-ACTIONS.md</files>
  <read_first>
    backend/src/resilience/resilience.types.ts (full file, 41 lines)
    .env.example (lines 24-29 — Messaging section being modified)
    MANUAL-ACTIONS.md (lines 1-45 Environment Setup table — row format convention; lines 250-313 Phase 5 section — dated-section-with-Resume-Signal convention to mirror)
  </read_first>
  <action>
    In `backend/src/resilience/resilience.types.ts`, add `| 'metaWhatsapp' | 'sendgrid'` to the `Vendor` union type, and add two entries to `RESILIENCE_DEFAULTS`: `metaWhatsapp: { timeoutMs: 8_000, retryCount: 1, failureThreshold: 5, halfOpenAfterMs: 30_000 }` and `sendgrid: { timeoutMs: 8_000, retryCount: 1, failureThreshold: 5, halfOpenAfterMs: 30_000 }` — do not modify any existing vendor entry. In `.env.example`, remove the line `TERMII_WHATSAPP_SENDER_ID=       # optional — set to your approved WhatsApp sender ID to bypass GSM/DND; Termii always tried first` (D-01/D-02: WhatsApp no longer routes through Termii), and add a new section immediately after the existing `# ─── Messaging ───` block: a header comment `# ─── Messaging — WhatsApp (Meta Business Cloud API, direct integration — Phase 15) ──` followed by four new lines: `META_WHATSAPP_ACCESS_TOKEN=`, `META_WHATSAPP_PHONE_NUMBER_ID=`, `META_WHATSAPP_TEMPLATE_NAME=`, `META_WHATSAPP_TEMPLATE_LANG=en_US`. In `MANUAL-ACTIONS.md`, add the four new env vars as new rows to the top "Environment Setup" table (same `| Variable | Service | Where to get it |` row format as existing entries — e.g. `| META_WHATSAPP_ACCESS_TOKEN | Meta Business Cloud API | Meta Business Manager → WhatsApp → API Setup → System User permanent token |`), and append a new dated section titled `## Phase 15 — Meta WhatsApp Business Cloud API Setup + Template Submission` at the end of the file (after the existing LAUNCH-06 section) containing: (a) account setup steps — create/confirm a Meta Business Account and WhatsApp Business Account, generate a permanent system-user access token and note the phone-number ID; (b) a drafted Authentication-category template for the stakeholder to submit verbatim in Meta Business Manager — name `iseyaa_otp_verification`, category `AUTHENTICATION`, language `en_US`, body `{{1}} is your Iṣẹ́yáá verification code. For your security, do not share this code.`, footer `This code expires in 5 minutes.`, one-tap copy-code button enabled (`otp_type: copy_code`) — with a note that Meta's template composer presents "add security recommendation" and "add expiration warning" as toggles rather than freeform text fields, so the stakeholder should enable both toggles rather than retyping this sentence verbatim if Meta's UI supplies its own preset phrasing; (c) a note that until Meta approves the template and all three `META_WHATSAPP_*` secrets are set, every WhatsApp-channel OTP send fails and automatically falls back to SMS (D-04/D-08 — expected behavior, not a bug); (d) an informational Resume Signal `15-meta-approved` the stakeholder can reply with once the template is APPROVED and credentials are live — this signal is informational only, this phase's automated plans do not block on it (per D-03).
  </action>
  <acceptance_criteria>
    - `backend/src/resilience/resilience.types.ts`'s `Vendor` union includes `'metaWhatsapp'` and `'sendgrid'`, and `RESILIENCE_DEFAULTS` has entries for both with the exact threshold values specified above.
    - `.env.example` no longer contains the string `TERMII_WHATSAPP_SENDER_ID`.
    - `.env.example` contains `META_WHATSAPP_ACCESS_TOKEN=`, `META_WHATSAPP_PHONE_NUMBER_ID=`, `META_WHATSAPP_TEMPLATE_NAME=`, and `META_WHATSAPP_TEMPLATE_LANG=en_US`.
    - `MANUAL-ACTIONS.md` contains the literal string `iseyaa_otp_verification` and the section heading text `Meta WhatsApp Business Cloud API Setup`.
    - `cd backend && npx tsc --noEmit -p tsconfig.build.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.build.json</automated>
  </verify>
  <done>metaWhatsapp and sendgrid are registered resilience vendors with sane defaults; .env.example reflects the Meta integration and drops the removed Termii WhatsApp var; MANUAL-ACTIONS.md documents the stakeholder's Meta setup + template submission deliverable (D-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client → `POST /auth/otp/send` | Untrusted `channel`/`email` input crosses here (validated by DTOs added in Task 2) |
| `.env` / `MANUAL-ACTIONS.md` → operator | Trusted, offline, operator-run documentation — not an HTTP-reachable surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `OtpSendDto.channel` / `OtpSendDto.email` | mitigate | `@IsEnum`/`@ValidateIf` + `@IsEmail` reject malformed channel/email at the HTTP boundary (NestJS global `ValidationPipe`, `whitelist: true`) before any service logic runs |
| T-15-02 | Information Disclosure | `META_WHATSAPP_ACCESS_TOKEN` handling | accept | The value only ever lives in `.env`/Infisical; no code path added by this plan reads, logs, or transmits it — Plan 15-03 is responsible for the actual vendor call and its logging discipline |
</threat_model>

<verification>
- `cd backend && npx prisma migrate status` reports no pending migrations after Task 1
- `cd backend && npx tsc --noEmit -p tsconfig.build.json` exits 0 after Tasks 2 and 3
</verification>

<success_criteria>
- `OtpChannel` exists identically in `schema.prisma` and `otp-channel.enum.ts`.
- `OtpSendDto`, `PhoneAuthDto`, `RegisterDto` all carry an optional `channel` field; `OtpSendDto` additionally carries a conditionally-required `email` field.
- `resilience.types.ts` has `metaWhatsapp` and `sendgrid` vendor slots ready for Plan 15-03 to call `resilience.execute()` against.
- `.env.example` and `MANUAL-ACTIONS.md` reflect the Meta WhatsApp integration, including the drafted Authentication template text (D-03).
</success_criteria>

<output>
After completion, create `.planning/phases/15-multi-channel-otp/15-01-SUMMARY.md`
</output>
