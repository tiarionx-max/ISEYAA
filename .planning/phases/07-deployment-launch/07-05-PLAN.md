---
phase: 07-deployment-launch
plan: 05
type: execute
wave: 4
depends_on:
  - "07-02"
  - "07-03"
files_modified: []
autonomous: false
requirements:
  - LAUNCH-01
  - LAUNCH-02
  - LAUNCH-03
  - LAUNCH-06
  - LAUNCH-07
  - LAUNCH-08

must_haves:
  truths:
    - "All Railway production services are healthy (GET /api/v1/health returns 200)"
    - "Paystack LIVE keys are active and the real-money E2E test passes"
    - "Rollback procedure tested and completed under 5 minutes"
    - "Grafana dashboards showing live KPIs"
    - "Sentry error alerts configured and verified with a test error"
    - "48-hour soft launch completes with error rate below 0.5%"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for Phase 7. No code changes — this is the sign-off gate that confirms every LAUNCH requirement is met before the project is marked complete.

Purpose: LAUNCH-03 requires a real-money test that only a human can perform with live Paystack keys. LAUNCH-07 requires a timed rollback test. LAUNCH-08 requires a 48-hour window that only passes with time elapsed. These cannot be automated.

Output: Signed-off checkpoint indicating Phase 7 (and the full project) is complete.
</objective>

<execution_context>
@c:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@c:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@c:/Developer/work/ISEYAA/.planning/ROADMAP.md
@c:/Developer/work/ISEYAA/MANUAL-ACTIONS.md
@c:/Developer/work/ISEYAA/.planning/phases/07-deployment-launch/07-01-SUMMARY.md
@c:/Developer/work/ISEYAA/.planning/phases/07-deployment-launch/07-02-SUMMARY.md
@c:/Developer/work/ISEYAA/.planning/phases/07-deployment-launch/07-03-SUMMARY.md
@c:/Developer/work/ISEYAA/.planning/phases/07-deployment-launch/07-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:auto">
  <name>Task 1: Pre-launch automated checks</name>
  <files>(none — verification only)</files>
  <action>
    Run the following automated checks and display results:

    1. **Backend health check (production):**
       ```
       curl -s https://iseyaa-api.up.railway.app/api/v1/health | python3 -m json.tool
       ```
       Expected: { "status": "ok", services all green }

    2. **Swagger inaccessible in production:**
       ```
       curl -s -o /dev/null -w "%{http_code}" https://iseyaa-api.up.railway.app/api/docs
       ```
       Expected: 404

    3. **CORS check — reject unlisted origin:**
       ```
       curl -s -H "Origin: https://evil.com" -H "Access-Control-Request-Method: GET" -X OPTIONS https://iseyaa-api.up.railway.app/api/v1/health -v 2>&1 | grep -i "access-control-allow-origin"
       ```
       Expected: no Access-Control-Allow-Origin header (rejected)

    4. **Backend type-check:**
       ```
       cd backend && npx tsc --noEmit
       ```
       Expected: zero errors

    5. **Full test suite:**
       ```
       cd backend && npm test
       ```
       Expected: all tests pass

    If any check fails, do NOT proceed. Surface the failure and return control to the operator.
  </action>
  <verify>
    <automated>cd backend && npm test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All 5 automated checks pass
    - Production health endpoint returns 200
    - Swagger returns 404 in production
  </acceptance_criteria>
  <done>Automated pre-launch checks all green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 10-step production launch verification</name>
  <what-built>
    Phase 7 delivered:
    - Plan 07-01: EAS build configuration (eas.json, expo-dev-client, build scripts)
    - Plan 07-02: Production CORS hardening, Swagger gate, Railway env var checklist
    - Plan 07-03: Grafana dashboard JSON, Sentry alert runbook, Railway rollback procedure
    - Plan 07-04: App store metadata (buildNumber, versionCode), submission checklist

    Stub vs real status inherited from earlier phases:
    - Paystack: may still be on test keys — must switch to LIVE for Step 3
    - Dojah NIN: stub OK for launch if DOJAH_API_KEY not yet provisioned
    - Smile Identity: stub OK if real liveness SDK not yet configured
    - Upstash Vector: stub OK for launch (affects personalization quality only)
  </what-built>
  <how-to-verify>
    **Prerequisites:**
    A. Complete the Railway env var checklist from MANUAL-ACTIONS.md Phase 7 section.
    B. Switch Paystack to LIVE mode: Dashboard → Settings → API Keys → copy sk_live_* key → update Railway env var.
    C. Run a development build: `cd mobile && npm run build:dev:android` (or iOS). Install on device.

    **Verification steps (run in order — stop on first failure):**

    1. **Production health.** `curl https://iseyaa-api.up.railway.app/api/v1/health` returns 200 with all services green.

    2. **Mobile app installs.** Scan EAS build QR code and install on Android device (or TestFlight for iOS). App opens to onboarding without crash. Sentry should NOT show an error for the launch.

    3. **Real-money E2E test (LAUNCH-03).** 
       - Create a test user account in production.
       - Top up wallet with ₦100 using your real Paystack-linked card (or bank transfer).
       - Confirm ₦100 appears in wallet balance (minus Paystack fee).
       - Purchase a ₦50 event ticket (use a test event if available).
       - Confirm wallet deducted ₦50 and ticket QR appears.
       - Request a ₦50 escrow refund if the stay module allows it.
       - Total cost: ~₦200 including fees. This confirms LAUNCH-03.

    4. **Paystack webhook fires.** After the topup, check backend logs for `payment.topup` event dispatched. No 500 error should appear.

    5. **Grafana dashboard live.** Import monitoring/grafana-dashboard.json into Grafana Cloud. Confirm at least one panel shows live data (RPS, error rate).

    6. **Sentry test alert.** In Sentry, use "Send Test Alert" on the "High error rate" rule. Confirm email arrives within 5 minutes.

    7. **Rollback test (LAUNCH-07).** 
       - Follow the rollback procedure in MANUAL-ACTIONS.md.
       - Time from clicking "Redeploy" to health check passing.
       - Must complete in under 5 minutes.

    8. **Swagger inaccessible.** Open https://iseyaa-api.up.railway.app/api/docs in browser. Must show 404 or error, not the Swagger UI.

    9. **App size check.** Download the production build from EAS build page. Check iOS IPA size < 40MB and Android APK size < 30MB.

    10. **Soft launch monitoring (48-hour window — LAUNCH-08).**
        - Share development build link with 10-20 testers in Abeokuta.
        - Monitor error rate in Grafana for 48 hours.
        - Pass criteria: error rate stays below 0.5% over the 48-hour window.
        - Note: this step runs asynchronously — you do not need to wait for it before approving steps 1-9.

    **Pass criteria:** Steps 1–9 complete without failures. Step 10 is noted as "in progress" or "completed" with error rate.

    **Fail criteria:** Any production 500 error on core flows, real-money test failure, rollback exceeding 5 minutes, app size exceeding limits.
  </how-to-verify>
  <resume-signal>
    Reply "approved" if all 10 steps pass.
    Reply "approved, step 10 in progress" if the 48-hour window is still running.
    Reply "blocked: [issue]" with description of which step failed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real-money E2E test | ₦100 real transaction — operator accepts this cost as LAUNCH-03 validation |
| Production environment | All verification done against live Railway deployment, not staging |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Financial Loss | Real-money test uses operator's personal card | accept | ₦200 test cost is minimal and required for LAUNCH-03 confirmation |
| T-07-11 | Availability | Rollback test causes brief downtime | mitigate | Schedule rollback test during low-traffic window (overnight) |
| T-07-12 | Repudiation | Soft launch error rate not monitored | mitigate | Grafana dashboard + Sentry alerts cover 48-hour window even if operator is not watching |
</threat_model>

<verification>
- Automated pre-launch checks all green
- Human resume-signal received as "approved" or "approved, step 10 in progress"
</verification>

<success_criteria>
1. All LAUNCH requirements (01-08) are confirmed met by operator
2. Real-money transaction completes successfully
3. Rollback tested under 5 minutes
4. Mobile app installed and working on operator's physical device
5. 48-hour soft launch error rate below 0.5%
</success_criteria>

<output>
After checkpoint approval, create `.planning/phases/07-deployment-launch/07-05-SUMMARY.md` documenting:
- Which LAUNCH requirements were confirmed
- Real-money test result
- Rollback test timing
- App sizes (iOS/Android)
- Soft launch result (or "in progress" with start time)
- Phase 7 completion confirmation
- Project v1.0 completion status
</output>
