---
phase: 07-deployment-launch
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - backend/src/main.ts
  - .env.example
autonomous: true
requirements:
  - LAUNCH-01
  - LAUNCH-02
  - LAUNCH-03

must_haves:
  truths:
    - "backend/src/main.ts CORS origin accepts both iseyaa.ng domain and the Railway production URL"
    - "Swagger UI is gated behind authentication (not publicly accessible in production)"
    - "Health endpoint GET /api/v1/health returns 200 with all service statuses"
    - ".env.example documents every production env var needed for Railway deployment"
    - "MANUAL-ACTIONS.md documents the exact Railway environment variable checklist"
  artifacts:
    - path: "backend/src/main.ts"
      provides: "CORS production config + Swagger auth gate"
      contains: "iseyaa.ng"
    - path: ".env.example"
      provides: "Complete production env var list"
      contains: "PAYSTACK_SECRET_KEY"
  key_links:
    - from: "backend/src/main.ts CORS"
      to: "Railway production domain"
      via: "origin: [process.env.ALLOWED_ORIGINS]"
      pattern: "ALLOWED_ORIGINS"
---

<objective>
Harden the backend for production: update CORS to accept the production domain, gate Swagger behind admin auth, verify the health endpoint works, and document the complete Railway environment variable checklist. Update MANUAL-ACTIONS.md with the production deployment steps.

Purpose: The backend is currently deployed on Railway (Phase 2 checkpoint passed) but with dev-mode CORS and an exposed Swagger UI. STATE.md explicitly flags "Swagger UI exposed without auth in production". This plan closes that gap and ensures the app build created in Plan 07-01 can reach the production API.

Output: Updated main.ts (CORS + Swagger gate), updated .env.example (ALLOWED_ORIGINS), MANUAL-ACTIONS.md production deployment section.
</objective>

<execution_context>
@c:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@c:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@c:/Developer/work/ISEYAA/.planning/PROJECT.md
@c:/Developer/work/ISEYAA/.planning/ROADMAP.md
@c:/Developer/work/ISEYAA/MANUAL-ACTIONS.md
</context>

<interfaces>
<!-- backend/src/main.ts current CORS config -->
backend/src/main.ts current CORS:
- app.enableCors() — currently accepts ALL origins (wildcard)
- This must be restricted to production domain in NODE_ENV=production

Swagger config:
- Currently: SwaggerModule.setup('api/docs', app, document) — no auth gate
- Must add: only show in !production, or require basic auth header

Health endpoint:
- GET /api/v1/health — implemented in Phase 2 (02-04-PLAN.md)
- Returns { status: 'ok', services: { database, redis, ... } }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update CORS and gate Swagger in backend/src/main.ts</name>
  <files>backend/src/main.ts</files>
  <read_first>
    backend/src/main.ts — read full file to understand current CORS and Swagger setup
  </read_first>
  <action>
    1. Replace the current `app.enableCors()` call with environment-aware config:
       ```typescript
       const allowedOrigins = process.env.ALLOWED_ORIGINS
         ? process.env.ALLOWED_ORIGINS.split(',').map(o => o.trim())
         : ['http://localhost:3000', 'http://localhost:19006'];

       app.enableCors({
         origin: allowedOrigins,
         credentials: true,
       });
       ```

    2. Gate Swagger UI behind production check:
       ```typescript
       if (process.env.NODE_ENV !== 'production') {
         const config = new DocumentBuilder()
           .setTitle('ISEYAA API')
           .setVersion('1.0')
           .addBearerAuth()
           .build();
         const document = SwaggerModule.createDocument(app, config);
         SwaggerModule.setup('api/docs', app, document);
       }
       ```
       This ensures `/api/docs` is only accessible in development/staging — never in production.

    Both changes are ENV-driven and do not affect dev workflow.
  </action>
  <verify>
    <automated>grep -q "ALLOWED_ORIGINS" /c/Developer/work/ISEYAA/backend/src/main.ts && grep -q "NODE_ENV.*production" /c/Developer/work/ISEYAA/backend/src/main.ts</automated>
  </verify>
  <acceptance_criteria>
    - CORS reads from ALLOWED_ORIGINS env var (split by comma), falls back to localhost in dev
    - Swagger setup wrapped in NODE_ENV !== 'production' check
    - backend TypeScript compiles clean: npx tsc --noEmit -p backend/tsconfig.json
  </acceptance_criteria>
  <done>main.ts updated with production CORS and Swagger gate.</done>
</task>

<task type="auto">
  <name>Task 2: Add ALLOWED_ORIGINS and production vars to .env.example</name>
  <files>.env.example</files>
  <read_first>
    .env.example — read current contents
  </read_first>
  <action>
    Append a Production Deployment section to .env.example:

    ```
    # ── Production Deployment ──────────────────────────────────────────────
    # Set in Railway environment variables (NOT in .env file)
    NODE_ENV=production
    # Comma-separated list of allowed CORS origins
    ALLOWED_ORIGINS=https://iseyaa.ng,https://www.iseyaa.ng,https://admin.iseyaa.ng

    # Paystack LIVE keys (replace test keys for production)
    # PAYSTACK_SECRET_KEY=sk_live_REPLACE_WITH_LIVE_KEY
    # PAYSTACK_WEBHOOK_SECRET=REPLACE_WITH_LIVE_WEBHOOK_SECRET

    # Neon production database branch
    # DATABASE_URL=postgresql://...@...neon.tech/iseyaa?sslmode=require
    ```

    Note: Lines starting with `#` are comments — they document what the operator must set without committing live keys.
  </action>
  <verify>
    <automated>grep -q "ALLOWED_ORIGINS" /c/Developer/work/ISEYAA/.env.example && grep -q "NODE_ENV=production" /c/Developer/work/ISEYAA/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - ALLOWED_ORIGINS documented with production domain example
    - NODE_ENV=production documented
    - Paystack LIVE key names documented as comments (no actual values)
  </acceptance_criteria>
  <done>.env.example updated with production deployment section.</done>
</task>

<task type="auto">
  <name>Task 3: Append Railway production deployment checklist to MANUAL-ACTIONS.md</name>
  <files>MANUAL-ACTIONS.md</files>
  <read_first>
    MANUAL-ACTIONS.md — read current file to find the right section to append to
  </read_first>
  <action>
    Append a new section "## Phase 7: Production Deployment (LAUNCH-01 → LAUNCH-03)" to MANUAL-ACTIONS.md:

    Include:
    1. Railway environment variable checklist (table of all required vars with current vs production values)
    2. Steps to switch from Neon dev branch to Neon production branch
    3. Steps to activate Paystack LIVE keys (toggle in dashboard, copy sk_live_* key)
    4. CORS verification step (curl to confirm /api/v1/health returns 200 from production domain)
    5. Swagger inaccessibility check (curl /api/docs returns 404 in production)
    6. Cloudflare WAF setup: add site iseyaa.ng, enable "Managed Rules" ruleset, create WAF rule blocking suspicious country codes if needed

    Railway env var checklist table:
    | Variable | Dev Value | Production Value | Notes |
    |----------|-----------|-----------------|-------|
    | DATABASE_URL | Neon dev branch | Neon prod branch | Change branch in Neon console |
    | REDIS_URL | Upstash dev | Upstash prod | Create prod env in Upstash |
    | PAYSTACK_SECRET_KEY | sk_test_* | sk_live_* | Toggle in Paystack dashboard |
    | PAYSTACK_WEBHOOK_SECRET | test secret | live secret | Must match live webhook endpoint |
    | NODE_ENV | development | production | Disables Swagger, tightens CORS |
    | ALLOWED_ORIGINS | (not set) | https://iseyaa.ng,... | Comma-separated production origins |
    | JWT_SECRET | any strong string | Rotate to new 64-char hex | Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))" |
    | ENCRYPTION_KEY | dev key | New 64-char hex key | NOTE: Rotating this breaks existing encrypted BVN/NIN — migrate before rotating |
    | ANTHROPIC_API_KEY | same | same | Already production key |
    | AWS_ACCESS_KEY_ID | R2 creds | R2 creds | Same (Cloudflare R2, not regional) |
  </action>
  <verify>
    <automated>grep -q "Phase 7" /c/Developer/work/ISEYAA/MANUAL-ACTIONS.md && grep -q "LAUNCH-01" /c/Developer/work/ISEYAA/MANUAL-ACTIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - MANUAL-ACTIONS.md has Phase 7 section with Railway env var checklist
    - Paystack LIVE key switch documented
    - ENCRYPTION_KEY rotation warning present
  </acceptance_criteria>
  <done>MANUAL-ACTIONS.md updated with production deployment checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → backend API | CORS restricts browser-origin requests; Swagger gated to prevent API schema disclosure |
| Admin → Railway dashboard | Env var changes require Railway dashboard access (separate from code commits) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | Swagger UI exposed in production | mitigate | NODE_ENV=production check hides /api/docs entirely |
| T-07-04 | Elevation of Privilege | CORS wildcard allows any origin to make credentialed requests | mitigate | ALLOWED_ORIGINS env var restricts to explicit domain list |
| T-07-05 | Information Disclosure | ENCRYPTION_KEY rotation breaks existing KYC data | accept | Warning documented in MANUAL-ACTIONS.md; rotation only safe after migrating ciphertext |
</threat_model>

<verification>
- backend/src/main.ts uses ALLOWED_ORIGINS env var for CORS
- Swagger setup wrapped in NODE_ENV !== 'production'
- .env.example has production deployment section
- MANUAL-ACTIONS.md has Railway env var checklist
- npx tsc --noEmit -p backend/tsconfig.json exits 0
</verification>

<success_criteria>
1. In production (NODE_ENV=production), GET /api/docs returns 404
2. CORS rejects requests from unlisted origins
3. Health endpoint returns 200 with all services up
4. Railway env var checklist documented for operator
</success_criteria>

<output>
After completion, create `.planning/phases/07-deployment-launch/07-02-SUMMARY.md` documenting:
- CORS change description
- Swagger gate confirmation
- MANUAL-ACTIONS.md Railway checklist summary
</output>
