---
phase: 13-settlement-cutover-transport-delivery
plan: 04
type: execute
wave: 3
depends_on: ["13-02", "13-03"]
files_modified:
  - backend/scripts/shadow-settlement-verify.ts
autonomous: true
requirements: [SETTLE-09]

must_haves:
  truths:
    - "A read-only Stage 1 batch script proves the new split formula produces zero discrepancies against historical completed trips/deliveries before any live bake period begins (D-05, D-06)"
    - "The full backend test suite has zero regressions from the settlement-engine convergence, including the shared SettlementService contract used by Tour/Marketplace/Events/Studio/Stays"
    - "No direct wallet-crediting call survives outside SettlementService.settle() in either module's cutover-enabled path"
  artifacts:
    - path: "backend/scripts/shadow-settlement-verify.ts"
      provides: "Stage 1 historical batch re-verification for both Transport and Delivery"
      contains: "verifyTransportShadow"
  key_links:
    - from: "backend/scripts/shadow-settlement-verify.ts"
      to: "Trip / DeliveryOrder tables (read-only)"
      via: "raw PrismaClient query, never SettlementService.settle()"
      pattern: "prisma\\.trip\\.findMany|prisma\\.deliveryOrder\\.findMany"
---

<objective>
Build the Stage 1 shadow-mode batch verification script (SETTLE-09's first gate) and close out the phase with a full-suite regression run plus a source-level audit confirming Plans 13-02/13-03 fully delegated wallet-crediting to `SettlementService` with no surviving direct writes.

Purpose: Stage 1 is the cheapest, zero-wallet-risk verification pass — proving the new split formula reproduces every historical driver/rider payout exactly (D-06's exact-match bar) before Stage 2's live dual-run (already wired in Plans 13-02/13-03) or any cutover-flag flip is considered. This plan is the phase's exit gate.
Output: `backend/scripts/shadow-settlement-verify.ts`; full green backend test suite; a confirmed-clean settlement-delegation audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-settlement-cutover-transport-delivery/13-CONTEXT.md
@.planning/phases/13-settlement-cutover-transport-delivery/13-RESEARCH.md
@.planning/phases/13-settlement-cutover-transport-delivery/13-02-SUMMARY.md
@.planning/phases/13-settlement-cutover-transport-delivery/13-03-SUMMARY.md

<interfaces>
<!-- Raw-PrismaClient standalone-script convention — the ONLY pattern to follow for this script.
     Do NOT use NestFactory.createApplicationContext (zero precedent in this codebase per RESEARCH.md). -->
From backend/prisma/seed.ts:1-4 and backend/package.json's `"prisma": { "seed": "ts-node --compiler-options {\"module\":\"CommonJS\"} prisma/seed.ts" }`:
```typescript
import { PrismaClient } from '@prisma/client';
const prisma = new PrismaClient();
```

<!-- Full worked Stage 1 example — copy this shape, do not reinvent. -->
See .planning/phases/13-settlement-cutover-transport-delivery/13-RESEARCH.md lines 247-288 (Pattern 2, Transport version, fully worked).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Stage 1 shadow-settlement-verify script</name>
  <files>backend/scripts/shadow-settlement-verify.ts</files>
  <read_first>
    backend/prisma/seed.ts (lines 1-10 — raw PrismaClient bootstrap convention)
    backend/package.json (scripts block + `prisma.seed` entry — ts-node invocation convention)
    .planning/phases/13-settlement-cutover-transport-delivery/13-RESEARCH.md (Pattern 2, lines 241-288 — full worked Transport example; Anti-Patterns section, lines 344-348, "Calling SettlementService.settle() against historical rows...would create real, duplicate wallet credits")
    backend/src/modules/transport/transport.service.ts (post-cutover `completeTrip()`, for the exact new-path formula to reproduce)
    backend/src/modules/delivery/delivery.service.ts (post-cutover `completeDelivery()`, for the exact new-path formula to reproduce)
  </read_first>
  <action>
    Create `backend/scripts/shadow-settlement-verify.ts` using `import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient();` (raw client, no NestJS DI — matches `seed.ts:1-4`). Export two async functions:
    - `verifyTransportShadow(sampleSize = 200): Promise<boolean>` — query `prisma.trip.findMany({ where: { status: 'COMPLETED', fare: { not: null } }, orderBy: { completedAt: 'desc' }, take: sampleSize })`; read `transport.govt_levy_pct`/`transport.platform_fee_pct` PlatformConfig rows (fallback 5/10); for each trip, recompute `driverEarnings` via the SAME subtract-first formula as `transport.service.ts`'s cutover-enabled path (`totalCommission = round(fare*(totalCommissionPct/100)*100)/100`, `driverEarnings = round((fare-totalCommission)*100)/100`); compare with EXACT equality (`===`, no ±0.02 tolerance — D-06) against `trip.driverEarnings`; accumulate `mismatches` count and an itemized `report[]` of `{ tripId, storedDriverEarnings, recomputedDriverEarnings }` for any mismatch.
    - `verifyDeliveryShadow(sampleSize = 200): Promise<boolean>` — mirror using `prisma.deliveryOrder.findMany({ where: { status: 'DELIVERED', fee: { not: null } }, orderBy: { completedAt: 'desc' }, take: sampleSize })`, `delivery.govt_levy_pct`/`delivery.platform_fee_pct` (fallback 5/15), the MULTIPLY-FIRST formula (`riderEarnings = round(fee*(1-totalCommissionPct/100)*100)/100`), comparing against `order.riderEarnings`.
    Each function must `console.log` a one-line summary (`` `Transport Stage 1: ${sampled} sampled, ${mismatches} mismatches` `` / Delivery equivalent), write a JSON report via `require('fs').writeFileSync(\`shadow-report-transport-${Date.now()}.json\`, JSON.stringify({ sampled, mismatches, report }, null, 2))` (Delivery: `shadow-report-delivery-*.json`), and `return mismatches === 0`. Add a bottom-of-file runner: `if (require.main === module) { Promise.all([verifyTransportShadow(), verifyDeliveryShadow()]).then(([t, d]) => { process.exitCode = t && d ? 0 : 1; }).finally(() => prisma.$disconnect()); }` so the script is directly `ts-node`-runnable. CRITICAL: this script must contain ZERO wallet-mutating calls of any kind (`settlementService.settle()`, `wallet.update()`, `wallet.create()`) — it is a pure read-only diff; calling `settle()` against already-`COMPLETED`/`DELIVERED` rows would double-credit drivers/riders already paid (RESEARCH.md Anti-Pattern).
  </action>
  <acceptance_criteria>
    - File `backend/scripts/shadow-settlement-verify.ts` exists, imports `PrismaClient` directly from `'@prisma/client'` (not `PrismaService`, not `NestFactory`).
    - `grep -c '\.settle(\|wallet\.update(\|wallet\.create(' backend/scripts/shadow-settlement-verify.ts` returns `0`.
    - `cd backend && npx ts-node --compiler-options {\"module\":\"CommonJS\"} scripts/shadow-settlement-verify.ts` exits with code `0` or `1` (never throws an unhandled exception/stack trace) against the current dev database, and prints both a `Transport Stage 1:` and a `Delivery Stage 1:` summary line.
    - Running against a database state with zero `COMPLETED` trips / `DELIVERED` orders reports `0 sampled, 0 mismatches` (not an error) for the affected module.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx ts-node --compiler-options {\"module\":\"CommonJS\"} scripts/shadow-settlement-verify.ts</automated>
  </verify>
  <done>Stage 1 batch script exists, is provably read-only, runs cleanly against the dev DB, and produces a queryable JSON report per D-06.</done>
</task>

<task type="auto">
  <name>Task 2: Full backend regression suite + source-level settlement-delegation audit</name>
  <files></files>
  <read_first>
    backend/src/modules/transport/transport.service.ts (post-cutover, full file)
    backend/src/modules/delivery/delivery.service.ts (post-cutover, full file)
    backend/src/common/services/__tests__/settlement.service.spec.ts (lines 1-90 — confirm the shared `SettlementService` contract's own test suite is unaffected)
    backend/jest.config.js
  </read_first>
  <action>
    Run `cd backend && npm test` (full Jest suite, including `tour-settlement.service.spec.ts`, `settlement.service.spec.ts`, `marketplace.service.spec.ts`, `studio.service.spec.ts`, `stays.service.spec.ts` — confirming zero regression to any other existing `SettlementService` caller from Phase 12). Then run a source-level grep audit confirming no surviving direct wallet-crediting call inside the cutover-ENABLED branch of either service: `grep -n "tx.wallet.update" backend/src/modules/transport/transport.service.ts backend/src/modules/delivery/delivery.service.ts`. The ONLY match allowed per file is the one inside the legacy `cutoverEnabled === false` branch's untouched `$transaction` block. Verify this by CONTAINMENT, not by line-order relative to `settlementService.settle(`: per 13-02/13-03's Task 1 action, the `cutoverEnabled === true` branch (which contains `settlementService.settle(`) is written FIRST in the file, with the `cutoverEnabled === false` branch (which contains `tx.wallet.update`) written SECOND — so `settlementService.settle(` will always appear textually BEFORE `tx.wallet.update(`, even in a correct implementation. A before/after line-order check would therefore pass regardless of correctness. Instead, open the file and confirm the single `tx.wallet.update` match's line number falls between the `} else {` block's opening brace and that block's matching closing `}` — i.e., it is lexically INSIDE the `cutoverEnabled === false` branch, not merely positioned after the `true` branch's code. If the match instead falls inside the `true` branch (or anywhere outside the `else` block), that is a defect; fix it by removing the direct wallet write, confirming the credit is fully delegated to `SettlementService.settle()`, and re-running the affected file's Jest spec to confirm it stays green.
  </action>
  <acceptance_criteria>
    - `cd backend && npm test` (full backend suite) exits 0.
    - `grep -n "tx.wallet.update" backend/src/modules/transport/transport.service.ts` returns exactly 1 line; manually inspect the file and confirm that line's number falls strictly between the `} else {` opening brace (of the `if (cutoverEnabled) { ... } else { ... }` block in `completeTrip()`) and that same `else` block's matching closing `}` — i.e., the match is lexically contained WITHIN the `cutoverEnabled === false` branch, not merely after `settlementService.settle(` in file order (see action note: a before/after check is insufficient because the `true` branch is written first).
    - `grep -n "tx.wallet.update" backend/src/modules/delivery/delivery.service.ts` returns exactly 1 line; same lexical-containment check against `completeDelivery()`'s `else` block.
    - `grep -c "uuidv4" backend/src/modules/transport/transport.service.ts` returns at least `1` (legacy `ref` generation preserved in the false-branch, confirming the legacy code path was branched around, not deleted).
    - `grep -c "uuidv4" backend/src/modules/delivery/delivery.service.ts` returns at least `1` (same confirmation for Delivery).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npm test</automated>
  </verify>
  <done>Full backend suite green; source audit confirms zero direct wallet writes escaped `SettlementService` delegation in the cutover-enabled path for either module, and the legacy pre-cutover code paths are intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `shadow-settlement-verify.ts` (offline script) → historical `Trip`/`DeliveryOrder` rows | Read-only; operator-run, not HTTP-reachable |
| Phase-13 exit gate → production cutover-flag flip | This plan's audit is a precondition, not a guarantee — D-08's live bake-period gate (3 elapsed days OR 100 completed trips/deliveries, zero discrepancies) remains a manual, post-deployment verification outside this plan's automated scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Repudiation | Stage 1 script silently skipped or its failure ignored before a flag flip | mitigate | Task 1's acceptance criteria require the script to run to completion and produce a queryable JSON report; the report's `mismatches` count is the objective pass/fail signal, not a subjective read of console output |
| T-13-10 | Tampering | A direct `tx.wallet.update()` call surviving inside the cutover-enabled branch of either module (re-introducing an uncoordinated wallet write alongside `SettlementService`'s canonical lock order) | mitigate | Task 2's grep audit is a hard gate — any match inside the `true` branch is treated as a defect requiring an immediate fix before this plan can be marked done |
| T-13-11 | Denial of Service | D-08's live bake-period gate not yet satisfied when a flag is flipped in production | accept (documented, not code-enforced) | Explicitly out of this phase's automated scope per 13-VALIDATION.md's "Manual-Only Verifications" — requires real multi-day live traffic; the operator must query `ShadowSettlementComparison` (module, matched, comparedAt) manually before flipping either `*.settlement_engine_enabled` flag to `true` |
</threat_model>

<verification>
- `cd backend && npx ts-node --compiler-options {\"module\":\"CommonJS\"} scripts/shadow-settlement-verify.ts` after Task 1 (exits 0 or 1, never throws)
- `cd backend && npm test` after Task 2 (full suite green)
- `grep -n "tx.wallet.update" backend/src/modules/transport/transport.service.ts backend/src/modules/delivery/delivery.service.ts` — exactly 1 match each, both lexically contained within the legacy `else` branch (see Task 2's action for the correct containment check)
</verification>

<success_criteria>
- Stage 1 batch script exists, is read-only, and reports zero mismatches against the current historical `COMPLETED`/`DELIVERED` sample (or an empty sample if none exist yet in this environment).
- Full backend test suite (Phase 12's settlement callers + Phase 13's new coverage) is green with zero regressions.
- No direct wallet-crediting call survives outside `SettlementService.settle()` in either module's cutover-enabled path.
- Phase 13's three roadmap requirements (SETTLE-03, SETTLE-04, SETTLE-09) are all code-complete; SETTLE-09's live bake-period gate (D-08) remains an explicit manual precondition before either flag flips to `true` in production.
</success_criteria>

<output>
After completion, create `.planning/phases/13-settlement-cutover-transport-delivery/13-04-SUMMARY.md`
</output>
</output>
