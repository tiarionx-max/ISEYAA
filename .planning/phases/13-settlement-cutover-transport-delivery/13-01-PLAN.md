---
phase: 13-settlement-cutover-transport-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/seed.ts
autonomous: true
requirements: [SETTLE-09]

must_haves:
  truths:
    - "A ShadowSettlementComparison table exists so Stage 2's live dual-run comparisons are durably persisted, not just Logger output"
    - "Six new PlatformConfig rows exist so Transport/Delivery's three-way split and independent cutover flags are 100% DB-driven, never hardcoded (CLAUDE.md constraint)"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "ShadowSettlementComparison model"
      contains: "model ShadowSettlementComparison"
    - path: "backend/prisma/seed.ts"
      provides: "6 new PlatformConfig upserts (govt_levy_pct/platform_fee_pct/settlement_engine_enabled x2 modules)"
      contains: "transport.settlement_engine_enabled"
  key_links:
    - from: "backend/prisma/seed.ts"
      to: "platform_configs table"
      via: "prisma.platformConfig.upsert()"
      pattern: "platformConfig\\.upsert"
---

<objective>
Lay the schema and configuration foundation Phase 13's cutover needs: a new `ShadowSettlementComparison` table for Stage 2's live dual-run bake-period tracking (D-05/D-06/D-08), and six new `PlatformConfig` rows — `transport.govt_levy_pct`, `transport.platform_fee_pct`, `delivery.govt_levy_pct`, `delivery.platform_fee_pct`, `transport.settlement_engine_enabled`, `delivery.settlement_engine_enabled` — that the Transport/Delivery cutover (Plans 13-02, 13-03) and the shadow-verify script (Plan 13-04) both depend on.

Purpose: Nothing else in this phase can run until this migration is applied and these config rows exist — Plans 13-02/13-03 read the cutover flags and new percentage keys on every `completeTrip()`/`completeDelivery()` call, and Plan 13-02/13-03's Stage 2 shadow-write needs the new table.
Output: Migrated `shadow_settlement_comparisons` table + 6 seeded `PlatformConfig` rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-settlement-cutover-transport-delivery/13-CONTEXT.md
@.planning/phases/13-settlement-cutover-transport-delivery/13-RESEARCH.md
@.planning/phases/13-settlement-cutover-transport-delivery/13-PATTERNS.md

<interfaces>
<!-- ShadowSettlementComparison model spec — Plans 13-02/13-03 write to this table, Plan 13-04 does NOT read it (Stage 1 is separate). -->
From 13-RESEARCH.md Pattern 3 (lines 296-312):
```prisma
model ShadowSettlementComparison {
  id              String   @id @default(uuid())
  module          String   // 'transport' | 'delivery'
  sourceId        String   // tripId or orderId
  oldEarnerAmount Decimal
  newEarnerAmount Decimal
  matched         Boolean
  comparedAt      DateTime @default(now())

  @@index([module, comparedAt])
  @@index([module, matched])
  @@map("shadow_settlement_comparisons")
}
```

<!-- PlatformConfig value SCALE — CRITICAL: Transport/Delivery use WHOLE-PERCENT scale (15, 5, 10),
     NOT Studio's fraction scale (0.10, 0.05). Confirmed in 13-PATTERNS.md lines 256-260 against the
     existing seeded values at seed.ts:1269 (transport_platform_fee_pct=15) and seed.ts:1299
     (delivery_platform_fee_pct=20), which transport.service.ts:520 / delivery.service.ts:552 read
     as `feePct / 100`. The two new keys per module MUST sum to the existing total. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ShadowSettlementComparison Prisma model</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>
    backend/prisma/schema.prisma (lines 627-661 — Transaction and PlatformConfig models, for field-style and `@@map` convention)
    .planning/phases/13-settlement-cutover-transport-delivery/13-RESEARCH.md (lines 296-312 — exact model spec, Pattern 3)
  </read_first>
  <action>
    Insert a new `model ShadowSettlementComparison` block into `backend/prisma/schema.prisma` immediately after the `PlatformConfig` model closes (after line 661, before `model AuditLog` at line 663). Fields: `id String @id @default(uuid())`; `module String` (values `'transport'` | `'delivery'`); `sourceId String` (the `Trip.id` or `DeliveryOrder.id` this comparison is for); `oldEarnerAmount Decimal`; `newEarnerAmount Decimal`; `matched Boolean`; `comparedAt DateTime @default(now())`. Add `@@index([module, comparedAt])` and `@@index([module, matched])` (needed for Plan 13-04's/D-08's bake-period gate aggregate queries). Map with `@@map("shadow_settlement_comparisons")`. Do not add a foreign-key relation to `Trip`/`DeliveryOrder` — `sourceId` is a plain string, since this table must survive independently of trip/order lifecycle and spans two unrelated parent tables.
  </action>
  <acceptance_criteria>
    - `backend/prisma/schema.prisma` contains the literal string `model ShadowSettlementComparison`.
    - The model has a `matched Boolean` field and `@@map("shadow_settlement_comparisons")`.
    - `cd backend && npx prisma validate` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx prisma validate</automated>
  </verify>
  <done>Schema file contains a syntactically valid `ShadowSettlementComparison` model per the spec above.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Apply the schema migration</name>
  <files>backend/prisma/migrations/**</files>
  <read_first>
    backend/prisma/schema.prisma (post Task 1 edit)
    backend/prisma/migrations/20260717170330_settle_02_booking_govt_levy_pct (most recent migration, as the additive-migration naming/shape convention to follow)
  </read_first>
  <action>
    From the `backend/` workspace, run `npx prisma migrate dev --name add_shadow_settlement_comparison` to generate and apply a real migration file (this is a production financial system — prefer a tracked migration over `db push`, per the schema-push requirement). This is a purely additive change (one new table, no existing columns touched), so it should apply non-interactively with no data-loss prompt. If the CLI cannot complete non-interactively for any reason, fall back to `npx prisma db push --accept-data-loss` only as a last resort and note the deviation in the plan SUMMARY. After the migration applies, confirm Prisma Client was regenerated (migrate dev runs `prisma generate` automatically) so `PrismaClient` exposes `prisma.shadowSettlementComparison`.
  </action>
  <acceptance_criteria>
    - A new folder under `backend/prisma/migrations/` timestamped after `20260717170330_settle_02_booking_govt_levy_pct` exists, and its `migration.sql` contains `CREATE TABLE "shadow_settlement_comparisons"`.
    - `cd backend && npx prisma migrate status` reports the database schema is up to date (no pending migrations).
    - The generated `@prisma/client` type/delegate `prisma.shadowSettlementComparison` is available (verify via `cd backend && npx tsc --noEmit -p tsconfig.build.json` reporting no "Property 'shadowSettlementComparison' does not exist" error anywhere).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx prisma migrate status</automated>
  </verify>
  <done>Migration applied to the dev database; `shadowSettlementComparison` delegate available on the generated Prisma Client.</done>
</task>

<task type="auto">
  <name>Task 3: Seed the 6 new PlatformConfig keys</name>
  <files>backend/prisma/seed.ts</files>
  <read_first>
    backend/prisma/seed.ts (lines 1265-1320 — legacy Transport/Delivery `transport_platform_fee_pct`/`delivery_platform_fee_pct` block, DO NOT touch)
    backend/prisma/seed.ts (lines 1440-1515 — Events/Studio/Stays dot-convention upsert block, the exact shape to copy)
    .planning/phases/13-settlement-cutover-transport-delivery/13-PATTERNS.md (lines 226-260 — seed key naming/scale guidance)
  </read_first>
  <action>
    Immediately after the existing dot-convention block ends (`stays.govt_levy_pct` upsert, `seed.ts:1504-1514`), add six new `prisma.platformConfig.upsert()` calls following that exact block's shape (`where: { key }, update: {}, create: { key, value, isPublic: false, metadata: { module } }`, followed by a `process.stdout.write` confirmation line). Use WHOLE-PERCENT numeric scale (matching the legacy `transport_platform_fee_pct: 15` / `delivery_platform_fee_pct: 20` keys at `seed.ts:1269`/`1299` — NOT Studio's 0-1 fraction scale) for the four percentage keys, and a literal boolean for the two flag keys:
    - `transport.govt_levy_pct` = `5`, metadata `{ module: 'transport' }`
    - `transport.platform_fee_pct` = `10`, metadata `{ module: 'transport' }` (10 + 5 = 15, matches today's single `transport_platform_fee_pct` total)
    - `delivery.govt_levy_pct` = `5`, metadata `{ module: 'delivery' }`
    - `delivery.platform_fee_pct` = `15`, metadata `{ module: 'delivery' }` (15 + 5 = 20, matches today's single `delivery_platform_fee_pct` total)
    - `transport.settlement_engine_enabled` = `false`, metadata `{ module: 'transport' }`
    - `delivery.settlement_engine_enabled` = `false`, metadata `{ module: 'delivery' }`
    Do NOT modify or delete the legacy `transport_platform_fee_pct` (`seed.ts:1269`) / `delivery_platform_fee_pct` (`seed.ts:1299`) keys — they stay orphaned-but-present per D-02 and RESEARCH.md's State of the Art note (still read by the pre-cutover code path in Plans 13-02/13-03).
  </action>
  <acceptance_criteria>
    - `seed.ts` contains upsert calls for all 6 exact key strings listed above.
    - `transport.platform_fee_pct`'s seeded `value` is the number `10` (not `0.10`); `transport.settlement_engine_enabled`'s seeded `value` is the boolean `false`.
    - The legacy keys `transport_platform_fee_pct` and `delivery_platform_fee_pct` are unchanged (still present, still value `15`/`20`).
    - Running the seed script exits 0 and prints all 6 new confirmation lines.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx prisma db seed</automated>
  </verify>
  <done>All 6 new keys present in the `platform_configs` table with the correct values and scale; legacy keys untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration/seed script → Postgres | Trusted, operator-run, offline (not an HTTP-reachable surface) |
| PlatformConfig rows → runtime read by Transport/Delivery services (Plans 13-02/13-03) | These rows become the sole source of truth for the split percentages and the cutover kill switch — no code-level defaults may silently diverge from what's seeded here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01a | Tampering | `seed.ts` new PlatformConfig values | mitigate | Task 3's acceptance criteria pin the exact numeric scale (whole-percent, not fraction) and exact sum-to-legacy-total constraint, preventing a silent 100x scale mismatch that would corrupt every subsequent split calculation in Plans 13-02/13-03 |
| T-13-02a | Elevation of Privilege | `transport.settlement_engine_enabled` / `delivery.settlement_engine_enabled` seeded as `false` | accept | No public mutation endpoint exists for `PlatformConfig` in this codebase (confirmed in RESEARCH.md) — flags can only be flipped via direct DB/seed access, consistent with every other `PlatformConfig` value in the project; adding an admin endpoint is out of this phase's scope |
</threat_model>

<verification>
- `cd backend && npx prisma validate` passes after Task 1
- `cd backend && npx prisma migrate status` reports up-to-date after Task 2
- `cd backend && npx prisma db seed` exits 0 and prints all 6 new key confirmations after Task 3
</verification>

<success_criteria>
- `ShadowSettlementComparison` table exists in the dev database and is queryable via the generated Prisma Client.
- All 6 new `PlatformConfig` rows exist with correct keys, values, and scale.
- Legacy `transport_platform_fee_pct` / `delivery_platform_fee_pct` keys are unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/13-settlement-cutover-transport-delivery/13-01-SUMMARY.md`
</output>
