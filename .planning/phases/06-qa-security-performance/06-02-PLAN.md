---
phase: 06-qa-security-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/src/common/services/image.service.ts
  - backend/src/modules/stays/stays.service.ts
  - backend/src/modules/events/events.service.ts
autonomous: true
requirements:
  - QA-05
  - QA-06

must_haves:
  truths:
    - "9 missing FK indexes are declared in schema.prisma across Ticket, Booking, Order, OrderItem, Transaction, AuditLog, and Trip models"
    - "Prisma migration add_fk_indexes is applied to the database"
    - "ImageService.resizeEventCover converts images to WebP at quality 85 and returns { buffer, contentType: 'image/webp' }"
    - "stays.service.ts uploadPropertyImage and events.service.ts uploadImage use the new { buffer, contentType } return shape and store .webp files"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "9 new @@index directives on FK columns"
      contains: "@@index([walletId])"
    - path: "backend/src/common/services/image.service.ts"
      provides: "WebP conversion pipeline"
      contains: "image/webp"
    - path: "backend/src/modules/stays/stays.service.ts"
      provides: "Updated uploadPropertyImage using WebP key extension"
      contains: ".webp"
    - path: "backend/src/modules/events/events.service.ts"
      provides: "Updated uploadImage using WebP key extension"
      contains: ".webp"
  key_links:
    - from: "schema.prisma @@index([walletId]) on Transaction"
      to: "Neon Postgres transactions table"
      via: "prisma migrate dev --name add_fk_indexes"
      pattern: "@@index\\(\\[walletId\\]\\)"
    - from: "ImageService.resizeEventCover"
      to: "S3Service.upload"
      via: "{ buffer, contentType } destructure in callers"
      pattern: "contentType.*image/webp"
---

<objective>
Add the 9 missing FK indexes to schema.prisma and run the migration so EXPLAIN ANALYZE shows index scans. Convert ImageService.resizeEventCover to produce WebP output and update the two callers (stays, events) so all images are stored as .webp in R2/S3.

Purpose: The index migration eliminates sequential scans on the hottest queries (wallet history, ticket lookups, order lists, trip history). The WebP pipeline reduces image sizes ~30% vs JPEG at same quality, enabling LCP < 2.5s on 3G (QA-05 + QA-06).

Output: Updated schema.prisma, migration applied, updated image.service.ts with new return signature, updated callers in stays.service.ts and events.service.ts.
</objective>

<execution_context>
@c:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@c:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@c:/Developer/work/ISEYAA/.planning/PROJECT.md
@c:/Developer/work/ISEYAA/.planning/ROADMAP.md
@c:/Developer/work/ISEYAA/.planning/phases/06-qa-security-performance/06-RESEARCH.md
@c:/Developer/work/ISEYAA/.planning/phases/06-qa-security-performance/06-PATTERNS.md
</context>

<interfaces>
<!-- Key interfaces extracted from codebase -->

From backend/prisma/schema.prisma — existing @@index patterns to replicate:
  model Booking: `@@index([propertyId, status, escrowReleasedAt])` before `@@map("bookings")`
  model Trip: `@@index([status, requestedAt])` and `@@index([driverId, status])` before `@@map("trips")`
  Pattern: @@index directives go AFTER all field declarations, BEFORE @@map, one per line.

9 missing indexes to add (verified from schema.prisma line numbers):
  model Ticket (line 317, @@map at line 333): add `@@index([userId])` and `@@index([ticketTypeId])`
  model Booking (line 365, @@map at line 387): add `@@index([userId])` after existing @@index
  model Order (line 434, @@map at line 452): add `@@index([userId])` and `@@index([vendorId])`
  model OrderItem (line 455, @@map at line 469): add `@@index([orderId])` and `@@index([productId])`
  model Transaction (line 551, @@map at line 570): add `@@index([walletId])`
  model AuditLog (line 586, @@map at line 602): add `@@index([userId])` and `@@index([createdAt])`
  model Trip (line 664, @@map at line 702): add `@@index([riderId])` after existing @@index directives

From backend/src/common/services/image.service.ts (full file — 24 lines):
  Current signature: `async resizeEventCover(buffer: Buffer): Promise<Buffer>`
  New signature: `async resizeEventCover(buffer: Buffer): Promise<{ buffer: Buffer; contentType: string }>`
  Change: `.jpeg({ quality: 85 })` → `.webp({ quality: 85 })`
  Return: `return { buffer: optimized, contentType: 'image/webp' }`
  Note: method must be `async` with `await sharp(...).toBuffer()` explicitly assigned before return

From backend/src/modules/stays/stays.service.ts (lines 121–137):
  Current: `const resized = await this.imageService.resizeEventCover(file.buffer);`
           `const key = properties/${id}/${uuidv4()}.jpg;`
           `const url = await this.s3.upload(key, resized, 'image/jpeg');`
  New: `const { buffer: resized, contentType } = await this.imageService.resizeEventCover(file.buffer);`
       `const key = properties/${id}/${uuidv4()}.webp;`
       `const url = await this.s3.upload(key, resized, contentType);`

From backend/src/modules/events/events.service.ts (lines 136–152):
  Same pattern as stays: resized variable, key with .jpg extension, 'image/jpeg' content-type.
  Apply same three-line update pattern as stays.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add 9 missing FK indexes to schema.prisma and run migration</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>
    backend/prisma/schema.prisma
  </read_first>
  <action>
    Read schema.prisma. Find each of the 7 affected models and add the missing @@index directives immediately before each model's @@map directive, following the existing @@index pattern already used in Booking and Trip models.

    Models and indexes to add (9 total):
    1. model Ticket — add `@@index([userId])` and `@@index([ticketTypeId])` before `@@map("tickets")`
    2. model Booking — add `@@index([userId])` after the existing `@@index([propertyId, status, escrowReleasedAt])`, before `@@map("bookings")`
    3. model Order — add `@@index([userId])` and `@@index([vendorId])` before `@@map("orders")`
    4. model OrderItem — add `@@index([orderId])` and `@@index([productId])` before `@@map("order_items")`
    5. model Transaction — add `@@index([walletId])` before `@@map("transactions")`
    6. model AuditLog — add `@@index([userId])` and `@@index([createdAt])` before `@@map("audit_logs")`
    7. model Trip — add `@@index([riderId])` after the existing two @@index directives, before `@@map("trips")`

    After editing schema.prisma, run the migration:
    cd c:/Developer/work/ISEYAA/backend && npx prisma migrate dev --name add_fk_indexes

    If the non-interactive environment causes migrate dev to hang, use:
    cd c:/Developer/work/ISEYAA/backend && npx prisma migrate dev --name add_fk_indexes --skip-generate

    After migration succeeds, run prisma generate to update the client:
    cd c:/Developer/work/ISEYAA/backend && npx prisma generate
  </action>
  <verify>
    <automated>cd c:/Developer/work/ISEYAA/backend && grep -c "@@index" prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@@index" backend/prisma/schema.prisma` returns a value greater than the pre-existing count (was 5: deletedAt, status on User + propertyId composite on Booking + 2 on Trip + 2 on DeliveryOrder = 8; new count must be 8+9 = 17 or more)
    - backend/prisma/schema.prisma contains `@@index([walletId])` (CRITICAL index)
    - backend/prisma/schema.prisma contains `@@index([riderId])` in the Trip model
    - backend/prisma/schema.prisma contains `@@index([userId])` in the Transaction model area (AuditLog)
    - `ls backend/prisma/migrations/` shows a directory named with add_fk_indexes suffix
    - `cd backend && npx prisma migrate status` exits 0 with "Database schema is up to date"
  </acceptance_criteria>
  <done>9 FK indexes declared in schema and applied to the Neon database via migration; prisma client regenerated</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WebP image pipeline — ImageService + callers</name>
  <files>
    backend/src/common/services/image.service.ts
    backend/src/modules/stays/stays.service.ts
    backend/src/modules/events/events.service.ts
  </files>
  <read_first>
    backend/src/common/services/image.service.ts
    backend/src/modules/stays/stays.service.ts
    backend/src/modules/events/events.service.ts
    backend/src/modules/stays/__tests__/stays.service.spec.ts
    backend/src/modules/events/__tests__/events.service.spec.ts
  </read_first>
  <behavior>
    - Test 1: resizeEventCover returns an object with `buffer` (Buffer) and `contentType: 'image/webp'`
    - Test 2: stays.service.ts uploadPropertyImage stores key with `.webp` extension and calls s3.upload with `image/webp` content type
    - Test 3: events.service.ts uploadImage stores key with `.webp` extension and calls s3.upload with `image/webp` content type
  </behavior>
  <action>
    STEP 1 — image.service.ts:
    Change the return type of resizeEventCover from `Promise<Buffer>` to `Promise<{ buffer: Buffer; contentType: string }>`.
    Replace `.jpeg({ quality: 85 })` with `.webp({ quality: 85 })`.
    Assign the toBuffer() result to a `const optimized` variable then return `{ buffer: optimized, contentType: 'image/webp' }`.
    The method must remain async.

    STEP 2 — stays.service.ts uploadPropertyImage (around line 127):
    Change `const resized = await this.imageService.resizeEventCover(file.buffer)` to
    `const { buffer: resized, contentType } = await this.imageService.resizeEventCover(file.buffer)`.
    Change the key from `properties/${id}/${uuidv4()}.jpg` to `properties/${id}/${uuidv4()}.webp`.
    Change the s3.upload third arg from `'image/jpeg'` to `contentType`.

    STEP 3 — events.service.ts uploadImage (around line 142):
    Apply the identical three-line change pattern:
    - Destructure `{ buffer: resized, contentType }` from resizeEventCover
    - Key: `events/${id}/${uuidv4()}.webp`
    - s3.upload third arg: `contentType`

    STEP 4 — Update test mocks:
    In stays.service.spec.ts and events.service.spec.ts, the mockImage.resizeEventCover mock currently resolves to a Buffer. Update it to resolve to `{ buffer: Buffer.from('test'), contentType: 'image/webp' }` so existing tests continue to pass with the new return shape.
  </action>
  <verify>
    <automated>cd c:/Developer/work/ISEYAA/backend && npx jest --testPathPattern "image|stays|events" --passWithNoTests --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/common/services/image.service.ts contains `.webp({ quality: 85 })` and does NOT contain `.jpeg({ quality: 85 })`
    - backend/src/common/services/image.service.ts return type includes `contentType: string`
    - backend/src/modules/stays/stays.service.ts contains `.webp` in the key string for uploadPropertyImage
    - backend/src/modules/events/events.service.ts contains `.webp` in the key string for uploadImage
    - Neither stays.service.ts nor events.service.ts contains `'image/jpeg'` in the upload call (was replaced by contentType variable)
    - Jest image/stays/events tests exit 0 (no regression from mock shape update)
  </acceptance_criteria>
  <done>
    ImageService produces WebP buffers with correct content type; both image upload callers store .webp files in R2; mock shapes updated so all related tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upload endpoint → ImageService → S3Service | Untrusted file content crosses into image processing; MIME validation already in place |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | ImageService — upload pipeline | accept | ALLOWED_MIME already validates incoming MIME type at line 4 of image.service.ts; WebP input is already in ALLOWED_MIME |
| T-06-02-02 | Denial of Service | schema migration — long-running index creation | accept | Indexes created via prisma migrate dev which runs concurrently in Postgres; no downtime for existing reads; Neon handles it without locking |
| T-06-02-03 | Information Disclosure | WebP key extension in R2 public URLs | accept | Extension change from .jpg to .webp does not expose new data; Content-Type header takes precedence in browsers |
</threat_model>

<verification>
Run full backend test suite after both tasks complete:
cd c:/Developer/work/ISEYAA/backend && npx jest --coverage 2>&1 | tail -15

Confirm: 270+ tests pass. Migration status clean. schema.prisma contains 17+ @@index directives. image.service.ts contains .webp.
</verification>

<success_criteria>
- schema.prisma has 9 new @@index directives; migration applied to Neon
- `@@index([walletId])` on Transaction model eliminates the critical seq scan on wallet history
- ImageService.resizeEventCover returns { buffer, contentType: 'image/webp' }
- stays.service.ts and events.service.ts upload .webp files
- 270+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-qa-security-performance/06-02-SUMMARY.md`
</output>
