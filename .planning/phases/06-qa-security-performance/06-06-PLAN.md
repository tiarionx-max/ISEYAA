---
phase: 06-qa-security-performance
plan: 06
type: execute
wave: 6
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
  - "06-04"
  - "06-05"
autonomous: false
requirements:
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
  - QA-07

must_haves:
  truths:
    - "k6 smoke test at 500 VUs completes with P95 < 500ms against staging"
    - "Artillery GPS stress test shows 500+ connections open for 10+ minutes with zero drops"
    - "OWASP ZAP passive scan returns zero CRITICAL findings on /api/docs-json endpoints"
    - "EXPLAIN ANALYZE script output shows Index Scan (not Seq Scan) for walletId, userId on transactions"
    - "Mobile bundle Atlas export completes; total initial bundle size documented"
    - "Mobile cold start measured (Chrome DevTools or physical device) and recorded"
    - "All backend tests pass (270+ count confirmed)"
  artifacts:
    - path: "load-tests/k6/main.js"
      provides: "k6 scripts available for manual run"
      contains: "p(95)<500"
    - path: "load-tests/artillery/socketio-gps.yml"
      provides: "Artillery GPS test available for manual run"
      contains: "driver:location"
  key_links:
    - from: "k6 run results"
      to: "QA-01 threshold gate"
      via: "P95 < 500ms + error rate < 0.1%"
      pattern: "p\\(95\\)<500"
    - from: "OWASP ZAP report"
      to: "QA-04 zero critical findings"
      via: "passive-report.html CRITICAL count = 0"
      pattern: "CRITICAL.*0"
---

<objective>
Human-verified checkpoint: run all load tests, security scan, database audit, and mobile performance measurement against the staging environment. Record results and confirm all 7 QA acceptance criteria are met before proceeding to Phase 7.

Purpose: This is the quality gate. Phases 1–5 built the platform; Phase 6 Plans 01–05 hardened it. This plan verifies the hardening delivered measurable results against quantitative thresholds.

Output: Human-confirmed pass/fail verdict on QA-01 through QA-07. Any failures block Phase 7.
</objective>

<execution_context>
@c:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@c:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@c:/Developer/work/ISEYAA/.planning/PROJECT.md
@c:/Developer/work/ISEYAA/.planning/ROADMAP.md
@c:/Developer/work/ISEYAA/.planning/phases/06-qa-security-performance/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-flight — full test suite + EXPLAIN ANALYZE</name>
  <files></files>
  <read_first>
    load-tests/db-audit/explain-analyze.ts
  </read_first>
  <action>
    Run the full backend test suite to confirm all tests pass before entering the manual verification steps:

    cd c:/Developer/work/ISEYAA/backend && npx jest --coverage 2>&1 | tail -20

    If tests fail, DO NOT proceed to manual steps — identify the failure and fix it (or create a gap closure plan).

    Run the isolation tests specifically to confirm QA-03 coverage:
    cd c:/Developer/work/ISEYAA/backend && npx jest --testPathPattern "isolation" --no-coverage 2>&1 | tail -15

    Confirm the migration status is clean:
    cd c:/Developer/work/ISEYAA/backend && npx prisma migrate status

    Print a pre-flight summary as output so the human checkpoint has a baseline:
    - Total tests passing: N
    - Isolation tests passing: N (across 3 spec files)
    - Migration status: clean / pending
    - Schema @@index count: (grep -c "@@index" backend/prisma/schema.prisma)
    - WebP callers updated: (grep -c ".webp" backend/src/modules/stays/stays.service.ts)
    - Admin v.category bug fixed: (grep -c "by_vendor_status" backend/src/modules/admin/admin.service.ts)
  </action>
  <verify>
    <automated>cd c:/Developer/work/ISEYAA/backend && npx jest --coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npx jest exits 0 (all tests pass)
    - `npx jest --testPathPattern isolation` shows 6+ passing tests
    - `npx prisma migrate status` exits 0 with "up to date" message
    - `grep -c "@@index" backend/prisma/schema.prisma` returns 17 or more
    - `grep -c "by_vendor_status" backend/src/modules/admin/admin.service.ts` returns 1
  </acceptance_criteria>
  <done>Pre-flight automated checks all green; ready for human verification steps</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 01–05 delivered: 4 bug fixes, 9 FK index migrations, WebP image pipeline, 3 isolation test suites (6+ tests), k6 load scripts, Artillery Socket.IO stress scripts, Sentry mobile SDK, Hermes engine config, EXPLAIN ANALYZE audit script. All 270+ backend tests pass.
  </what-built>
  <how-to-verify>
    Work through each QA criterion in order. Record PASS or FAIL for each.

    --- QA-03: Data Isolation (automated — run this first) ---
    cd c:/Developer/work/ISEYAA/backend && npx jest --testPathPattern isolation --no-coverage
    Expected: 6+ tests pass, 0 failures.

    --- QA-05: Database Index Audit ---
    Prerequisites: Ensure DATABASE_URL in your .env points to Neon dev branch.
    cd c:/Developer/work/ISEYAA && npx ts-node --project load-tests/db-audit/tsconfig.json load-tests/db-audit/explain-analyze.ts
    Look for: each query output should say "Index Scan" not "Seq Scan".
    Key check: the first query (transactions WHERE walletId) MUST show "Index Scan using transactions_walletId_idx".

    --- QA-04: OWASP ZAP Passive Scan ---
    Prerequisites: Docker installed and running. Staging deployed with APP_ENV=staging.
    Confirm spec URL accessible: curl -s https://iseyaa-staging.railway.app/api/docs-json | head -5
    Run ZAP:
      docker run --rm -v $(pwd)/zap-reports:/zap/reports ghcr.io/zaproxy/zaproxy:stable zap-api-scan.py -t https://iseyaa-staging.railway.app/api/docs-json -f openapi -r zap-reports/passive-report.html -S
    Open zap-reports/passive-report.html in a browser.
    Count CRITICAL (red) findings. Target: 0.
    Note any HIGH (orange) findings for Phase 7 remediation.

    --- QA-01: k6 HTTP Load Test (smoke at 500 VUs first) ---
    Prerequisites: k6 installed (choco install k6). Staging URL accessible.
    Install k6 if not present: winget install k6 --source winget
    Smoke test (500 VUs, 2 min):
      k6 run --vus 500 --duration 120s --env BASE_URL=https://iseyaa-staging.railway.app --env TEST_PHONE=+2349000000001 --env TEST_PASSWORD=testpass123 load-tests/k6/main.js
    Expected: P95 < 500ms, error rate < 0.1%.
    Full 10K VU run: Only if smoke passes. Use k6 Cloud, a GitHub Actions runner with 16GB RAM, or a dedicated VM. The 10K run requires the Neon PgBouncer pooling to be enabled (check Neon dashboard → Connection pooling → enable).

    --- QA-02: Artillery Socket.IO Stress Test ---
    Prerequisites: npm install -g artillery@2.0.31. A TEST_DRIVER account with an active trip ID on staging.
    TEST_DRIVER_PHONE=+2349000000002 TEST_DRIVER_PASSWORD=testpass123 TEST_TRIP_ID=<active-trip-id> BASE_URL=https://iseyaa-staging.railway.app artillery run load-tests/artillery/socketio-gps.yml
    Watch NestJS logs on Railway for "Client connected:" messages.
    Expected: 500 connections open simultaneously, 0 disconnect events in the 10-minute window.
    If connections fail silently: check if artillery-engine-socketio-v3 is needed (see processor.js comments).

    --- QA-06: WebP Image Pipeline ---
    Upload a test image to any event or property via API:
      curl -X POST https://iseyaa-staging.railway.app/api/v1/events/{id}/image -H "Authorization: Bearer TOKEN" -F "file=@test.jpg"
    Check the returned URL. Open the URL in a browser. In DevTools → Network: confirm Content-Type is image/webp.
    For LCP: open the platform homepage in Chrome → DevTools → Performance → simulate Fast 3G → record page load → check LCP timestamp. Target: < 2.5s.

    --- QA-07: Mobile Cold Start + Crash Rate ---
    Bundle analysis:
      cd c:/Developer/work/ISEYAA/mobile && npm run atlas
    This runs: EXPO_UNSTABLE_ATLAS=true expo export --platform ios
    Then: npx expo-atlas .expo/atlas.jsonl
    Note the total initial bundle size from the Atlas web UI. Target: < 2MB for 3G < 3s.

    Cold start measurement (physical device or emulator):
    On an Android emulator with throttled network (3G): adb shell am force-stop ng.gov.ogun.iseyaa && time adb shell am start -n ng.gov.ogun.iseyaa/.MainActivity
    OR: Use Chrome DevTools Remote Debugging on an Android device with 3G network throttle. Record from "App start" to "First Contentful Paint".
    Target: < 3 seconds.

    Crash rate: After confirming Sentry is initialized, run the app for 48 hours (or use Sentry's simulated error API to validate the integration). Check Sentry → Performance → Crash Free Sessions. Target: > 99.5%.
  </how-to-verify>
  <resume-signal>
    For each QA-01 through QA-07, type "PASS" or "FAIL: [brief description]".
    Example: "QA-01: PASS, QA-02: FAIL: connections drop at 400, QA-03: PASS, QA-04: PASS (0 critical), QA-05: PASS (index scan confirmed), QA-06: PASS, QA-07: PASS (cold start 2.3s)"

    If ANY criterion FAILS:
    - Type the failures and a brief description
    - The orchestrator will create gap closure plans to address each failure before Phase 7

    If ALL criteria PASS: Type "ALL PASS — Phase 6 complete"
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ZAP Docker → staging API | ZAP sends probe requests including active scan mutations — only against staging, never production |
| k6 VUs → staging API | Load generated against staging Neon branch — not production branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Denial of Service | k6 10K VU run against production | mitigate | Scripts default to staging URL; human review step confirms target before full run |
| T-06-06-02 | Tampering | ZAP active scan mutations on staging data | accept | Staging data is non-production; ZAP active scan mutates test data only; production branch unaffected |
| T-06-06-03 | Information Disclosure | ZAP passive report HTML contains API endpoint list | accept | Report stored locally; not committed to repo; contains no user PII |
</threat_model>

<verification>
This plan IS the verification step. Task 1 auto-verifies the automated test suite.
Task 2 is the human checkpoint — the developer must confirm all 7 QA criteria.

Pre-Phase 7 gate: ALL of QA-01 through QA-07 must be PASS before Phase 7 begins.
</verification>

<success_criteria>
- QA-01: k6 P95 < 500ms at 10,000 concurrent VUs; error rate < 0.1%
- QA-02: 500 concurrent Socket.IO connections sustained 10 minutes with zero drops
- QA-03: Isolation test suite passes — 0 cross-user data access possible
- QA-04: OWASP ZAP passive scan — 0 CRITICAL findings
- QA-05: EXPLAIN ANALYZE shows Index Scan on all 8 hot queries
- QA-06: Images served as image/webp; LCP < 2.5s on 3G
- QA-07: Mobile cold start < 3s on 3G; crash-free rate > 99.5% over 48h
</success_criteria>

<output>
After completion, create `.planning/phases/06-qa-security-performance/06-06-SUMMARY.md`
</output>
