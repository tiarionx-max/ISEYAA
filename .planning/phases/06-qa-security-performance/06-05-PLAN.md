---
phase: 06-qa-security-performance
plan: 05
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - mobile/app.json
  - mobile/package.json
autonomous: true
requirements:
  - QA-06
  - QA-07

must_haves:
  truths:
    - "app.json has jsEngine: 'hermes' configured to ensure bytecode compilation"
    - "@sentry/react-native is installed in mobile/package.json"
    - "Sentry is initialized in mobile/app/_layout.tsx with the DSN from SENTRY_DSN env"
    - "Expo Atlas export command is documented in package.json scripts for bundle analysis"
    - "Mobile screens load atlas-compatible lazy imports for the two heaviest screens (ai-chat, kyc)"
  artifacts:
    - path: "mobile/app.json"
      provides: "jsEngine: 'hermes' + Sentry plugin entry"
      contains: "hermes"
    - path: "mobile/package.json"
      provides: "@sentry/react-native dependency"
      contains: "@sentry/react-native"
  key_links:
    - from: "mobile/app.json plugins"
      to: "@sentry/react-native/expo"
      via: "expo-sentry plugin in plugins array"
      pattern: "sentry.*expo"
    - from: "mobile/app/_layout.tsx"
      to: "Sentry.init"
      via: "import * as Sentry from @sentry/react-native + init in _layout.tsx"
      pattern: "Sentry\\.init"
---

<objective>
Configure the mobile Expo app for QA-07 compliance: add Hermes JS engine for bytecode compilation (cold-start reduction), install and initialize Sentry React Native for crash-free rate tracking, and add the Atlas bundle analysis command to package.json scripts.

Purpose: QA-07 requires cold start < 3s on 3G and crash-free rate > 99.5%. Hermes bytecode parsing is ~40% faster than JIT. Sentry mobile crash reporting enables the 48-hour crash-free measurement needed before Phase 7 launch.

Output: Updated mobile/app.json (jsEngine + Sentry plugin), updated mobile/package.json (@sentry/react-native + atlas script), Sentry init in mobile/app/_layout.tsx.
</objective>

<execution_context>
@c:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@c:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@c:/Developer/work/ISEYAA/.planning/PROJECT.md
@c:/Developer/work/ISEYAA/.planning/phases/06-qa-security-performance/06-RESEARCH.md
@c:/Developer/work/ISEYAA/.planning/phases/06-qa-security-performance/06-PATTERNS.md
</context>

<interfaces>
<!-- Current mobile app.json and _layout.tsx structures -->

mobile/app.json current structure (relevant excerpt):
- No jsEngine field present (Hermes is SDK 51 default but explicit config is best practice)
- plugins array: ["expo-router", "expo-secure-store", ["expo-image-picker", {...}]]
- No Sentry plugin entry
- experiments.typedRoutes: true

mobile/app/_layout.tsx current structure:
- Root layout wrapping Stack navigator with GestureHandlerRootView + QueryClientProvider
- Import pattern: relative imports for lib, no Sentry import

mobile/package.json current structure:
- expo: ~51.0.0
- No @sentry/react-native entry
- No atlas-related script in scripts

Sentry React Native SDK conventions (Expo):
- Package: @sentry/react-native (latest compatible with Expo SDK 51 = ^6.x)
- Expo plugin entry in app.json plugins: ["@sentry/react-native/expo", { "organization": "iseyaa", "project": "iseyaa-mobile" }]
- Init call: Sentry.init({ dsn: process.env.EXPO_PUBLIC_SENTRY_DSN || '', tracesSampleRate: 0.1, environment: __DEV__ ? 'development' : 'production' })
- Init must be called before the root component renders (in _layout.tsx, before the Stack return)
- Wrap root with Sentry.wrap() HOC OR use Sentry.init standalone — either is acceptable

Expo Atlas command (Expo SDK 51+, no install needed):
- EXPO_UNSTABLE_ATLAS=true npx expo export --platform ios
- Then: npx expo-atlas .expo/atlas.jsonl
- Add as package.json script: "atlas": "EXPO_UNSTABLE_ATLAS=true expo export --platform ios"

Hermes configuration in app.json:
- Add "jsEngine": "hermes" at top level of expo config object
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add jsEngine hermes + Sentry plugin to app.json; update package.json</name>
  <files>
    mobile/app.json
    mobile/package.json
  </files>
  <read_first>
    mobile/app.json
    mobile/package.json
  </read_first>
  <action>
    APP.JSON CHANGES:
    1. Add `"jsEngine": "hermes"` as a top-level property inside the `expo` config object (at the same level as "name", "slug", "version").
    2. Append `["@sentry/react-native/expo", { "organization": "iseyaa", "project": "iseyaa-mobile" }]` to the `plugins` array. The array currently has 3 entries; it will have 4 after this change.

    PACKAGE.JSON CHANGES:
    1. Add `"@sentry/react-native": "^6.5.0"` to the `dependencies` object. (Use ^6.x — latest minor compatible with Expo SDK 51).
    2. Add an `"atlas"` entry to the `scripts` section: `"atlas": "EXPO_UNSTABLE_ATLAS=true expo export --platform ios"`.

    After editing package.json, run:
    cd c:/Developer/work/ISEYAA/mobile && npm install

    This installs @sentry/react-native. If npm install triggers a lockfile update for the workspace root, that is expected — commit both mobile/package.json and the root package-lock.json.
  </action>
  <verify>
    <automated>node -e "const a = require('c:/Developer/work/ISEYAA/mobile/app.json'); console.log(a.expo.jsEngine);"</automated>
  </verify>
  <acceptance_criteria>
    - mobile/app.json expo.jsEngine === "hermes"
    - mobile/app.json expo.plugins array contains an entry matching `@sentry/react-native/expo`
    - mobile/package.json dependencies contains `@sentry/react-native`
    - mobile/package.json scripts contains `"atlas"` key with `EXPO_UNSTABLE_ATLAS` in the value
    - `node -e "const a = require('./mobile/app.json'); console.log(a.expo.jsEngine);"` prints `hermes`
  </acceptance_criteria>
  <done>app.json configured for Hermes bytecode and Sentry plugin; @sentry/react-native installed in mobile workspace</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Sentry in mobile root layout</name>
  <files>
    mobile/app/_layout.tsx
  </files>
  <read_first>
    mobile/app/_layout.tsx
  </read_first>
  <action>
    Read mobile/app/_layout.tsx in full to understand the current root layout structure.

    Add Sentry initialization to the root layout:

    1. At the top of the file, add:
       import * as Sentry from '@sentry/react-native';

    2. Before the root component function or at the module level (not inside the render function), call:
       Sentry.init({
         dsn: process.env.EXPO_PUBLIC_SENTRY_DSN || '',
         tracesSampleRate: 0.1,
         environment: __DEV__ ? 'development' : 'production',
         enabled: !__DEV__,
       });

    The Sentry.init call must be at module level (executed on import) or immediately before the root component function body, not inside useEffect or any hook. This matches Sentry's documented initialization pattern for React Native.

    Do not modify any other functionality in _layout.tsx (Stack navigator, GestureHandlerRootView, QueryClientProvider structure must remain unchanged).

    Note: EXPO_PUBLIC_SENTRY_DSN will be configured as an environment variable in .env.example and in the Railway/Infisical secrets vault. The DSN is not hardcoded. If the env var is absent, Sentry.init still runs but captures nothing (dsn: '' effectively disables it).

    Add `EXPO_PUBLIC_SENTRY_DSN` to backend/.env.example (or the root .env.example if it covers mobile) with a placeholder comment: `# Sentry DSN for mobile crash reporting (from sentry.io project settings)`.
  </action>
  <verify>
    <automated>grep -c "Sentry.init" c:/Developer/work/ISEYAA/mobile/app/_layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - mobile/app/_layout.tsx contains `import * as Sentry from '@sentry/react-native'`
    - mobile/app/_layout.tsx contains `Sentry.init({` call
    - Sentry.init includes `dsn: process.env.EXPO_PUBLIC_SENTRY_DSN || ''`
    - Sentry.init includes `tracesSampleRate` and `environment` properties
    - Sentry.init is NOT inside a useEffect, useState hook, or component render function
    - The root Stack navigator and GestureHandlerRootView structure is unchanged
    - `grep -c "Sentry.init" mobile/app/_layout.tsx` returns 1
    - .env.example contains `EXPO_PUBLIC_SENTRY_DSN` key
  </acceptance_criteria>
  <done>
    Sentry React Native initialized at app startup; crash events reported to Sentry project for crash-free rate tracking required by QA-07; EXPO_PUBLIC_SENTRY_DSN documented in .env.example.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile app → Sentry cloud | Crash reports + stack traces sent to Sentry; may include device metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Information Disclosure | Sentry crash reports — potential PII in stack traces | mitigate | tracesSampleRate: 0.1 (10% of transactions sampled); enabled: !__DEV__ (off in dev); Sentry DSN not hardcoded; Sentry can be configured with PII scrubbing rules in the Sentry project dashboard before Phase 7 |
| T-06-05-02 | Information Disclosure | EXPO_PUBLIC_SENTRY_DSN in app bundle | accept | Sentry DSNs are intentionally public (they identify a project, not a secret key); this is standard Sentry practice |
| T-06-05-03 | Tampering | Hermes bytecode with disabled source maps | accept | Source maps uploaded to Sentry via Sentry plugin in app.json; debugging is not impaired |
</threat_model>

<verification>
node -e "const a = require('c:/Developer/work/ISEYAA/mobile/app.json'); console.log('jsEngine:', a.expo.jsEngine); console.log('plugins:', JSON.stringify(a.expo.plugins));"
node -e "const p = require('c:/Developer/work/ISEYAA/mobile/package.json'); console.log('@sentry/react-native:', p.dependencies['@sentry/react-native']); console.log('atlas script:', p.scripts?.atlas);"
grep "Sentry.init" c:/Developer/work/ISEYAA/mobile/app/_layout.tsx
</verification>

<success_criteria>
- app.json: jsEngine = 'hermes', Sentry plugin in plugins array
- package.json: @sentry/react-native installed, atlas script present
- _layout.tsx: Sentry.init called at module level with dsn from EXPO_PUBLIC_SENTRY_DSN
- .env.example documents EXPO_PUBLIC_SENTRY_DSN
- Existing mobile navigation structure unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/06-qa-security-performance/06-05-SUMMARY.md`
</output>
