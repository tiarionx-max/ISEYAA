---
phase: 06-qa-security-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/admin/admin.service.ts
  - backend/src/modules/stays/stays.service.ts
  - backend/src/modules/marketplace/marketplace.service.ts
autonomous: true
requirements:
  - QA-03
  - QA-04

must_haves:
  truths:
    - "GET /api/v1/admin/revenue returns 200 with by_vendor_status array (no 500 from missing column)"
    - "Escrow cron releases funds 24 h after checkOut, not checkIn"
    - "Marketplace order payment decrements stock for each purchased product"
    - "Paystack webhook signature verification confirmed wired (rawBody passed from controller)"
  artifacts:
    - path: "backend/src/modules/admin/admin.service.ts"
      provides: "getRevenue — by_vendor_status grouping replacing broken by_category"
      contains: "by_vendor_status"
    - path: "backend/src/modules/stays/stays.service.ts"
      provides: "releaseEscrow — checkOut cutoff, not checkIn"
      contains: "checkOut: { lt: cutoff }"
    - path: "backend/src/modules/marketplace/marketplace.service.ts"
      provides: "handleOrderPayment — stock decrement loop after status update"
      contains: "stock: { decrement: item.quantity }"
  key_links:
    - from: "admin.service.ts getRevenue"
      to: "vendors table — status column"
      via: "GROUP BY v.status"
      pattern: "GROUP BY v\\.status"
    - from: "stays.service.ts releaseEscrow"
      to: "bookings.checkOut"
      via: "Prisma where clause"
      pattern: "checkOut.*lt.*cutoff"
    - from: "marketplace.service.ts handleOrderPayment"
      to: "products.stock"
      via: "prisma.product.update"
      pattern: "decrement.*quantity"
---

<objective>
Fix the four known bugs flagged in STATE.md that will cause test failures and a 500 error during the OWASP ZAP security scan in Wave 3. Three bugs require code changes; one (Paystack webhook) is confirmed already correct and requires only verification.

Purpose: The security scan must complete without 500 errors. The isolation tests must exercise the actual guarded code paths. The escrow bug causes hosts to receive funds prematurely; the stock bug allows overselling.

Output: Three modified service files. All 270+ existing tests continue to pass.
</objective>

<execution_context>
@c:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@c:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@c:/Developer/work/ISEYAA/.planning/PROJECT.md
@c:/Developer/work/ISEYAA/.planning/ROADMAP.md
@c:/Developer/work/ISEYAA/.planning/STATE.md
@c:/Developer/work/ISEYAA/.planning/phases/06-qa-security-performance/06-RESEARCH.md
@c:/Developer/work/ISEYAA/.planning/phases/06-qa-security-performance/06-PATTERNS.md
</context>

<interfaces>
<!-- Key interfaces the executor needs. Extracted from codebase. -->

From backend/src/modules/admin/admin.service.ts (lines 50–98):
getRevenue() returns: { govt_levy_total, by_lga, by_category, by_month }
Bug: line 70–77 — `SELECT v.category` references a non-existent column on `vendors`.
Fix: replace `by_category` query with GROUP BY `v.status` (VendorStatus enum: PENDING | ACTIVE | SUSPENDED).
Return key rename: `by_category` → `by_vendor_status`.

From backend/src/modules/stays/stays.service.ts (lines 279–292):
releaseEscrow() Cron — where clause at line 285: `checkIn: { lt: cutoff }`.
Bug: must be `checkOut: { lt: cutoff }`.
Comment to add: "// Escrow releases 24 h after checkOut — not checkIn — to give host time to report issues"

From backend/src/modules/marketplace/marketplace.service.ts (lines 225–246):
handleOrderPayment() — after `prisma.order.update({ status: 'PROCESSING' })` at line 237–240, no stock decrement.
Fix: after `order.update`, add a for-loop over `order.orderItems` calling `prisma.product.update({ where: { id: item.productId }, data: { stock: { decrement: item.quantity } } })`.
Note: `item.productId` is directly on the OrderItem model — no extra select needed.

From backend/src/modules/webhooks/webhooks.controller.ts:
handlePaystack uses `@Req() req: RawBodyRequest<Request>` and passes `req.rawBody` to service.
Confirmed correct — no code change needed; only verification.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix admin.service.ts getRevenue broken SQL</name>
  <files>backend/src/modules/admin/admin.service.ts</files>
  <read_first>
    backend/src/modules/admin/admin.service.ts
  </read_first>
  <action>
    In getRevenue() at lines 69–77, replace the broken `by_category` query that references the non-existent `v.category` column.

    Replace the entire by_category query (the third argument to Promise.all) with a query that groups by `v.status` instead:

    The new raw query must SELECT `v.status` and COALESCE(SUM(o."govtLevy"), 0) AS total, join orders o with vendors v on o."vendorId" = v.id, WHERE o."deletedAt" IS NULL AND o.status != 'CANCELLED', GROUP BY v.status, ORDER BY total DESC.

    The TypeScript type for the result changes from `{ category: string; total: number }[]` to `{ status: string; total: number }[]`.

    In the return object at line 92–97: rename `by_category` key to `by_vendor_status`. The map call stays identical in shape: `byVendorStatus.map(r => ({ ...r, total: Number(r.total) }))`.

    Update the variable name from `byCategory` to `byVendorStatus` throughout (destructuring at line 51 and the return at line 95).

    Do not modify any other method in the file.
  </action>
  <verify>
    <automated>cd c:/Developer/work/ISEYAA/backend && npx jest --testPathPattern admin --passWithNoTests --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/modules/admin/admin.service.ts contains `by_vendor_status` (not `by_category`) in the return object
    - backend/src/modules/admin/admin.service.ts contains `GROUP BY v.status` in the raw SQL (not `GROUP BY v.category`)
    - backend/src/modules/admin/admin.service.ts does NOT contain `v.category` anywhere
    - Jest admin tests exit 0 or with passWithNoTests (no new failures introduced)
  </acceptance_criteria>
  <done>getRevenue() executes without referencing the non-existent category column; returns by_vendor_status grouping</done>
</task>

<task type="auto">
  <name>Task 2: Fix stays escrow cutoff (checkIn → checkOut) and marketplace stock decrement</name>
  <files>
    backend/src/modules/stays/stays.service.ts
    backend/src/modules/marketplace/marketplace.service.ts
  </files>
  <read_first>
    backend/src/modules/stays/stays.service.ts
    backend/src/modules/marketplace/marketplace.service.ts
  </read_first>
  <action>
    STAYS BUG FIX — stays.service.ts releaseEscrow():
    At the Cron handler (around line 279), find the Prisma where clause in the dueBookings findMany call. Change `checkIn: { lt: cutoff }` to `checkOut: { lt: cutoff }`. Add or update the comment above the cutoff line to read: "// Escrow releases 24 h after checkOut — not checkIn — to give host time to report issues".

    MARKETPLACE BUG FIX — marketplace.service.ts handleOrderPayment():
    In the order findUnique include at line ~230, the orderItems include already has `product: { select: { name: true } }`. No change needed to the select — `item.productId` is a direct field on OrderItem.

    After the prisma.order.update call that sets status: 'PROCESSING' (around line 237–240), and BEFORE the `await this.notifyOrderUpdate(order.id, 'PROCESSING')` call, insert a for-loop:

    for (const item of order.orderItems) {
      await this.prisma.product.update({
        where: { id: item.productId },
        data: { stock: { decrement: item.quantity } },
      });
    }

    This loop must sit between the order status update and the notification call. Keep the existing logger.error catch block unchanged.

    WEBHOOK VERIFICATION (no code change):
    Read backend/src/modules/webhooks/webhooks.controller.ts and confirm the handlePaystack method uses `@Req() req: RawBodyRequest<Request>` and passes `req.rawBody` as the third argument to webhooksService.handlePaystack. If correct, no change needed — document the confirmation in the task's SUMMARY note. If rawBody is NOT passed, add `@Req() req: RawBodyRequest<Request>` and pass `req.rawBody`.
  </action>
  <verify>
    <automated>cd c:/Developer/work/ISEYAA/backend && npx jest --testPathPattern "stays|marketplace" --passWithNoTests --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/modules/stays/stays.service.ts contains `checkOut: { lt: cutoff }` in the releaseEscrow where clause
    - backend/src/modules/stays/stays.service.ts does NOT contain `checkIn: { lt: cutoff }` in releaseEscrow
    - backend/src/modules/marketplace/marketplace.service.ts contains `stock: { decrement: item.quantity }` in handleOrderPayment
    - backend/src/modules/marketplace/marketplace.service.ts contains the stock decrement loop between the order.update and notifyOrderUpdate calls
    - backend/src/modules/webhooks/webhooks.controller.ts passes `req.rawBody` as third arg to handlePaystack
    - Jest stays and marketplace tests pass (or passWithNoTests exit 0)
  </acceptance_criteria>
  <done>
    Escrow releases after guest checkOut (not checkIn); marketplace orders decrement product stock on payment confirmation; Paystack webhook rawBody wiring verified correct.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Paystack webhook → WebhooksController | External HTTP call with x-paystack-signature header; rawBody must be verified before crediting wallet |
| Admin API → getRevenue | Authenticated admin endpoint; broken SQL caused unhandled 500 exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | webhooks.service.ts handlePaystack | mitigate | HMAC-SHA512 verification already implemented; rawBody passed via RawBodyRequest confirmed in controller |
| T-06-01-02 | Denial of Service | marketplace stock decrement | mitigate | Add stock decrement in handleOrderPayment; prevents overselling (resource exhaustion via payment bypass) |
| T-06-01-03 | Information Disclosure | admin.service.ts getRevenue 500 | mitigate | Fix v.category SQL bug so endpoint returns 200; ZAP scan will not flag unhandled 500 errors |
| T-06-01-04 | Tampering | stays escrow premature release | mitigate | Fix checkIn→checkOut; hosts cannot receive funds before checkout date |
</threat_model>

<verification>
Run full backend test suite after both tasks:
cd c:/Developer/work/ISEYAA/backend && npx jest --coverage 2>&1 | tail -15

Confirm: all 270+ tests pass. No regressions from the three file changes.
</verification>

<success_criteria>
- admin.service.ts getRevenue returns by_vendor_status (no v.category reference)
- stays.service.ts releaseEscrow uses checkOut cutoff
- marketplace.service.ts handleOrderPayment decrements stock
- webhooks.controller.ts confirmed passing rawBody
- 270+ tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-qa-security-performance/06-01-SUMMARY.md`
</output>
