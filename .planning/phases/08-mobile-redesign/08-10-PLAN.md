---
phase: 08-mobile-redesign
plan: 10
type: execute
wave: 6
depends_on: [08-09]
files_modified:
  - .planning/phases/08-mobile-redesign/08-VERIFICATION.md
autonomous: false
requirements: [MOB-RD-01, MOB-RD-02, MOB-RD-03, MOB-RD-04, MOB-RD-05, MOB-RD-06, MOB-RD-07, MOB-RD-08]
must_haves:
  truths:
    - "Every MOB-RD-* success criterion (1-8) has been verified by a human and recorded with a checkbox"
    - "Any regression observed is logged with a reproducible repro"
    - "08-VALIDATION.md is also populated by this checkpoint (Status: Draft → final)"
    - "Phase is either marked Complete in ROADMAP.md or a gap-closure plan is queued"
  artifacts:
    - path: ".planning/phases/08-mobile-redesign/08-VERIFICATION.md"
      provides: "Human verification record for all 8 MOB-RD criteria"
      contains: "MOB-RD-01"
    - path: ".planning/phases/08-mobile-redesign/08-VALIDATION.md"
      provides: "Filled-in validation doc (was Draft, now finalized by this checkpoint)"
      contains: "Status:"
  key_links: []
---

<objective>
Human verification gate. The operator installs the APK from 08-09 on a real Android device and walks through each MOB-RD success criterion, ticking it off or filing a regression. No code changes — this plan exists to produce the auditable verification record AND finalize the validation doc that 08-VALIDATION.md is scaffolded with.

Closes MOB-RD-08 (success criterion #8: all existing tests still pass, no backend changes, web unchanged) and finalises the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-mobile-redesign/08-CONTEXT.md
@.planning/phases/08-mobile-redesign/08-VALIDATION.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Phase 8 mobile redesign is shipped to an Android device via the EAS preview APK from 08-09. The operator now has a build to verify against.</what-built>
  <how-to-verify>
    Open `.planning/phases/08-mobile-redesign/08-VERIFICATION.md` (create the file if it doesn't exist) and paste in the template below. Also open `.planning/phases/08-mobile-redesign/08-VALIDATION.md` (created with `Status: Draft` earlier in this phase) and flip its status to `Final` after every acceptance test in it has been ticked. For each criterion, install the APK on an Android device, perform the listed action, and tick the box only if the criterion passes. If any criterion fails, log a concrete repro under "Regressions" at the bottom of the file.

    ---

    # Phase 8 — Mobile Redesign Verification

    **Build URL:** <from 08-09>
    **APK install URL:** <from 08-09>
    **Device:** <model + Android version>
    **Verifier:** <name>
    **Date:** <yyyy-mm-dd>

    ## MOB-RD-01 — 5-tab navigation
    - [ ] mobile/app/(tabs)/ contains exactly 5 .tsx tab screens (index, book, wallet, concierge, profile) + _layout.tsx — verify by running `ls "mobile/app/(tabs)/"` on the dev machine.
    - [ ] App's tab bar shows exactly 5 tabs (Discover, Book, Wallet, Concierge, You) and no others.
    - [ ] Transport / Delivery / Driver / Rider functionality still reachable via Concierge tab and You tab → mode cards.

    ## MOB-RD-02 — Discover news ticker
    - [ ] Discover tab top shows the news ticker scrolling horizontally with the LIVE red pulsing dot + Iṣẹ́yáá Pulse label.
    - [ ] Headlines load from `GET /api/v1/news?limit=20` (verify by tailing backend logs or by force-disabling network and confirming the inline FALLBACK array still shows).
    - [ ] Tapping a headline with a `link` field opens the URL in the OS browser (NOT `url` — the model field is `link`).
    - [ ] Below the ticker, the rest of the Discover content (hero greeting, search, curated feed) renders unchanged.

    ## MOB-RD-03 — Stays category browse
    - [ ] Book tab → Stays sub-section shows exactly 10 category chips in this order: All / Stays / Lounges / Clubs / Beach / Tours / Experiences / Memberships / Attractions / Featured.
    - [ ] Tapping a category triggers a refetch — verify by tailing backend logs for the `/properties?types=...` or `?bookingMode=...` or `?featured=true` query.
    - [ ] Grid is photo-first 2-column with real `coverImageUrl` images (not gradient placeholders unless image is null).
    - [ ] Book hub also shows Events / Studio / Marketplace sub-section tabs (all 4 selectable — H-1).

    ## MOB-RD-04 — Stay detail mode-aware booking
    - [ ] Open a NIGHTLY property → booking sheet shows check-in + check-out (datetimepicker) + guests + email field + nights × pricePerNight total.
    - [ ] Open a HOURLY property → booking sheet shows date + start time (datetimepicker) + hours + email + total.
    - [ ] Open a TIMED_EVENT property → booking sheet shows date + slot picker + email + `pricePerHour × slot length` total (NOT people × pricePerNight — H-3).
    - [ ] Open a MEMBERSHIP property → booking sheet shows benefits list + duration chips (1/3/6/12mo) + email + monthly total.
    - [ ] 4-image gallery scrolls horizontally with paging dots indicator.
    - [ ] Highlights render with check icons; amenities render as chips.
    - [ ] Email field defaults from session `me.email` in all 4 sheets.
    - [ ] Confirm in any sheet POSTs to `/properties/:id/bookings` (NOT `/stays/bookings`) and opens the Paystack URL in `expo-web-browser`.

    ## MOB-RD-05 — Marketplace cart + checkout
    - [ ] Book tab → Marketplace sub-section shows 8 category chips: All / Fashion / Crafts / Food / Art / Tech / Agriculture / Featured.
    - [ ] Product grid shows discount badges on products with `compareAtPrice > price` (only if Task 0 of 08-05 GREEN-LIT the field).
    - [ ] Wishlist heart toggles per-product.
    - [ ] Tapping a product opens detail screen with gallery + qty stepper + Add to Cart + Buy Now.
    - [ ] Add to Cart increments the bag icon badge in the Book tab header (badge driven by `useCartStore.getState().totalCount()`).
    - [ ] Tapping the bag icon opens the cart drawer (right-slide modal) — triggered by `useCartDrawerStore.getState().openDrawer()`.
    - [ ] Cart drawer shows items + qty stepper + remove + subtotal (from `useCartStore.getState().totalPrice()`).
    - [ ] Checkout flow shows order summary + single email field (defaulted from session) + Pay button. **NO delivery address field** (H-2 — backend rejects extra fields).
    - [ ] Checkout POSTs to `POST /api/v1/orders` (NOT `/cart/checkout`) with `{ items, email }` and opens Paystack URL.
    - [ ] After Paystack closes, cart is cleared and user returns to Book tab.
    - [ ] **AsyncStorage cart persistence smoke check (L-4):** before final commit, the executor temporarily added `console.log('CART_KEY iseyaa-cart-v1', items)` inside `useCartStore`'s `persist` rehydration callback (or equivalent log site) and ran the app in dev mode. The Metro console shows the items array being loaded under the key. Confirm the storage key is `iseyaa-cart-v1` (mirrors web). After confirmation, **remove the console.log before the final phase commit**.

    ## MOB-RD-06 — Host onboarding
    - [ ] You tab shows a "Become a host" card (only when `me.registeredRoles` does not include `HOST`).
    - [ ] Tapping the card opens `/host` with hero (using `CARD_GRADIENTS.goldHero` — no inline 4-stop hex) + 3 benefit cards + Q&A accordion.
    - [ ] Gold CTA fires `POST /api/v1/users/me/become-host` (verify via backend log).
    - [ ] On success, returns to profile with a toast/alert and the "Become a host" card disappears (refetch of `me`).

    ## MOB-RD-07 — EAS preview build
    - [ ] Build URL captured at top of this file.
    - [ ] APK install URL captured at top of this file.
    - [ ] APK installs on real Android device.
    - [ ] App launches without crash on first screen.

    ## MOB-RD-08 — No regressions
    - [ ] `cd backend && npm test` passes (all 282+ tests).
    - [ ] `cd web && npm run typecheck` passes (no web changes).
    - [ ] `git diff main -- 'backend/**' | wc -l` returns 0 (L-1: web/backend are contracts, not targets).
    - [ ] `git diff main -- 'web/**' | wc -l` returns 0 (L-1: no web changes this phase).

    ## Regressions
    (empty if none — otherwise list each regression with repro steps and severity)

    ---

    If ANY box is unticked, do NOT mark the phase complete. Instead:
    1. List the failing criteria.
    2. The orchestrator should re-run `/gsd-plan-phase 08 --gaps` to plan a closure for the failed items.

    After all boxes pass, open `.planning/phases/08-mobile-redesign/08-VALIDATION.md` and update its status block from `**Status: Draft — to be filled by 08-10 verifier**` to `**Status: Final — verified yyyy-mm-dd by <name>**`. Tick the Acceptance Tests inside that doc to match.
  </how-to-verify>
  <resume-signal>Reply with one of: (a) "ALL PASS — mark Phase 8 complete in ROADMAP.md and finalize 08-VALIDATION.md", (b) "REGRESSIONS: <list>" and the orchestrator will queue a gap-closure plan.</resume-signal>
</task>

</tasks>

<verification>
- `.planning/phases/08-mobile-redesign/08-VERIFICATION.md` exists and is filled in.
- Every checkbox in MOB-RD-01 through MOB-RD-08 is either ticked or has a regression filed.
- The cart-storage smoke check `console.log` is added during verification and removed before the final phase commit (L-4).
- `git diff main -- 'web/**' | wc -l` returns 0.
- `08-VALIDATION.md` status updated to Final once all boxes pass.
- ROADMAP.md Phase 8 status updated to Complete (or partial with the open gap plans listed).
</verification>

<success_criteria>
- Phase 8 verification record exists.
- All 8 success criteria from ROADMAP.md are evidenced.
- 08-VALIDATION.md transitions Draft → Final.
- Phase 8 is closed or has a clear continuation plan.
</success_criteria>

<output>
Create `.planning/phases/08-mobile-redesign/08-10-SUMMARY.md` with: which criteria passed, any regressions filed with repros, confirmation that the temporary cart `console.log` was removed, and the recommended next action (close phase, or queue gap-closure).
</output>
