---
phase: 08-mobile-redesign
plan: 03
type: execute
wave: 2
depends_on: [08-02]
files_modified:
  - mobile/app/(tabs)/index.tsx
autonomous: true
requirements: [MOB-RD-02]
must_haves:
  truths:
    - "Discover tab renders the NewsTicker at the top of the scroll, below safe area, above the hero greeting"
    - "Existing Discover content (hero greeting, search, curated feed, bookmarks) still works"
  artifacts:
    - path: "mobile/app/(tabs)/index.tsx"
      provides: "Discover screen with NewsTicker integrated"
      contains: "<NewsTicker"
  key_links:
    - from: "mobile/app/(tabs)/index.tsx"
      to: "mobile/components/NewsTicker.tsx"
      via: "import { NewsTicker } from '../../components/NewsTicker'"
      pattern: "import.*NewsTicker"
---

<objective>
Insert the `NewsTicker` component into the Discover tab (`mobile/app/(tabs)/index.tsx`). This is an additive edit only — the existing 1101-line screen is preserved as-is, and the ticker is wedged into the JSX tree at the top of the scrolling content, below the SafeAreaView top edge and above the greeting/hero block.

**Scope deferral (per M-1):** Full Discover hero redesign per UI-SPEC §TAB 0 (rotating billboard hero, sticky search bar, Quick Action Pills, curated sections) is DEFERRED to a follow-up phase. This plan inserts the news ticker only — no other Discover edits. The existing 1101-line screen is left intentionally untouched apart from the single `<NewsTicker />` insertion. Do NOT expand scope here.

Purpose: Closes MOB-RD-02 success criterion #2 (Discover renders a working news ticker fed by `/api/v1/news?limit=20`). Keeping this as a tiny additive edit minimizes the diff and avoids touching the rest of the screen.

Output: One file with a single import line added and a single JSX element added near the top of the render tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-mobile-redesign/08-CONTEXT.md
@.planning/mobile-redesign-UI-SPEC.md
@mobile/app/(tabs)/index.tsx
@mobile/components/NewsTicker.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert NewsTicker into Discover scroll</name>
  <files>mobile/app/(tabs)/index.tsx</files>
  <action>Open `mobile/app/(tabs)/index.tsx`. Add `import { NewsTicker } from '../../components/NewsTicker';` to the existing import block (alphabetical placement near other component/lib imports is fine — match the existing import style). Locate the topmost JSX inside the rendered SafeAreaView/ScrollView (the greeting/hero block — currently the first child of the ScrollView, look for the "Good morning" or location/bell header). Insert `<NewsTicker />` as a sibling element directly BEFORE that header block (so it appears at the very top of the scrollable content, but inside the SafeAreaView so it respects the notch). Do NOT wrap NewsTicker in any extra View — it brings its own height/border styling. Do NOT reorder or modify any other JSX. Do NOT add any new state, hooks, or refs. Do NOT redesign the hero, search, or feed sections (deferred per M-1 — record in SUMMARY). Verify the import-block ordering stays consistent with existing conventions (relative path `../../components/NewsTicker`).</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('mobile/app/(tabs)/index.tsx','utf8'); if(!c.includes('NewsTicker')) process.exit(1); if(!c.includes('<NewsTicker')) process.exit(2); if(c.match(/<NewsTicker/g).length !== 1) process.exit(3); console.log('OK');"</automated>
  </verify>
  <done>Discover renders a single `<NewsTicker />` element at the top of the scroll. `cd mobile && npx tsc --noEmit` passes. The diff against HEAD touches only one file with ≤ 6 lines added.</done>
</task>

</tasks>

<verification>
- `git diff --stat mobile/app/(tabs)/index.tsx` shows ≤ 6 lines changed.
- `grep -c "<NewsTicker" mobile/app/(tabs)/index.tsx` returns exactly `1`.
- TypeScript compiles cleanly.
- All existing 282+ tests still pass.
</verification>

<success_criteria>
- Discover scrolls with NewsTicker visible at the top.
- No other content removed or reordered.
- News headlines load from `/api/v1/news?limit=20` and fall back to the inline FALLBACK array on cold start.
- Full Discover redesign (billboard hero, sticky search, Quick Action Pills) remains deferred — DO NOT expand scope.
</success_criteria>

<output>
Create `.planning/phases/08-mobile-redesign/08-03-SUMMARY.md` with: line number where NewsTicker was inserted, exact import statement added, confirmation no other JSX changed, and explicit note that the full Discover hero redesign is deferred to a follow-up phase.
</output>
