---
phase: 08-mobile-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/components/NewsTicker.tsx
  - mobile/components/ui/PressableScale.tsx
  - mobile/components/ui/CategoryStrip.tsx
  - mobile/components/ui/Chip.tsx
autonomous: true
requirements: [MOB-RD-02, MOB-RD-08]
must_haves:
  truths:
    - "NewsTicker renders the LIVE pulse + Iṣẹ́yáá Pulse label + scrolling headlines fed by GET /api/v1/news?limit=20"
    - "Pressable scale-0.97 spring animation is reusable across Stays/Marketplace/Host cards via PressableScale"
    - "CategoryStrip renders horizontal lucide-icon + label tabs with active state matching the UI-SPEC Chip token, with minHeight 44pt per tab"
    - "Chip renders an inline pill with default/active states from the design tokens"
  artifacts:
    - path: "mobile/components/NewsTicker.tsx"
      provides: "Animated.loop translateX marquee + LIVE pulse + fallback headlines"
      exports: ["NewsTicker"]
    - path: "mobile/components/ui/PressableScale.tsx"
      provides: "Reanimated scale-0.97 spring on press"
      exports: ["PressableScale"]
    - path: "mobile/components/ui/CategoryStrip.tsx"
      provides: "Horizontal lucide-icon category tabs (drives Stays + Marketplace), 44pt min touch target"
      exports: ["CategoryStrip"]
    - path: "mobile/components/ui/Chip.tsx"
      provides: "Inline pill (default + active state) from UI-SPEC §Chips/Pills"
      exports: ["Chip"]
  key_links:
    - from: "mobile/components/NewsTicker.tsx"
      to: "/api/v1/news?limit=20"
      via: "useQuery via fetcher from mobile/lib/api"
      pattern: "useQuery.*news"
    - from: "mobile/components/ui/PressableScale.tsx"
      to: "react-native-reanimated"
      via: "useSharedValue + useAnimatedStyle + withSpring"
      pattern: "withSpring"
---

<objective>
Build the four small shared UI primitives every downstream Wave 3/4 plan will import. None of these primitives wire into any screen yet — that's the consumer plans' job. Keeping them in their own plan lets Wave 1 run fully parallel: this plan owns `mobile/components/**`, plan 08-01 owns `mobile/lib/**`, plan 08-03 owns `mobile/app/(tabs)/index.tsx`.

Purpose: The redesign needs (1) a news ticker with a marquee animation, (2) a press-scale animation contract used by every card, (3) a horizontal icon-tab category strip used by Stays AND Marketplace AND eventually any future browse screen, (4) a generic pill chip. Extracting these now prevents three duplicate implementations of the same pattern.

Output: 4 new component files under `mobile/components/`. No screen edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-mobile-redesign/08-CONTEXT.md
@.planning/mobile-redesign-UI-SPEC.md
@CLAUDE.md
@mobile/lib/tokens.ts
@mobile/lib/api.ts
@web/src/components/landing/NewsTicker.tsx
@backend/prisma/schema.prisma

<interfaces>
<!-- Component contracts other plans will rely on. Define exactly these props. -->

NewsTicker (no props — self-contained, uses useQuery + fallback):

  export function NewsTicker(): JSX.Element;

PressableScale (drop-in Pressable replacement):

  export type PressableScaleProps = {
    onPress?: () => void;
    onLongPress?: () => void;
    style?: StyleProp<ViewStyle>;
    children: React.ReactNode;
    disabled?: boolean;
    hapticStyle?: 'light' | 'medium' | null;  // null = no haptic
  };
  export function PressableScale(props: PressableScaleProps): JSX.Element;

CategoryStrip (generic — works for stays AND marketplace):

  export type CategoryStripItem = {
    id: string;
    label: string;
    icon: LucideIcon;
  };
  export type CategoryStripProps = {
    items: CategoryStripItem[];
    activeId: string;
    onChange: (id: string) => void;
  };
  export function CategoryStrip(props: CategoryStripProps): JSX.Element;

Chip (inline pill):

  export type ChipProps = {
    label: string;
    active?: boolean;
    onPress?: () => void;
    icon?: LucideIcon;
    style?: StyleProp<ViewStyle>;
  };
  export function Chip(props: ChipProps): JSX.Element;

News API model (from backend/prisma/schema.prisma News model — verified):
  - News has `headline`, `summary?`, `source?`, `link?` (NOT `url`), `category?`, `imageUrl?`, `publishedAt`, `isLive`.
  - When opening external news, use `Linking.openURL(item.link)` — the field is `link`. Do NOT reference `item.url` (does not exist on the model).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PressableScale + Chip primitives</name>
  <files>mobile/components/ui/PressableScale.tsx, mobile/components/ui/Chip.tsx</files>
  <action>Create `mobile/components/ui/PressableScale.tsx`: wraps `Pressable` with `react-native-reanimated` (already a dep — see `mobile/package.json`). Use `useSharedValue(1)`, `useAnimatedStyle({ transform: [{ scale: scale.value }] })`, and `withSpring(0.97, { stiffness: 400, damping: 25 })` on `onPressIn`, `withSpring(1, { stiffness: 300, damping: 20 })` on `onPressOut` — matches UI-SPEC §Cards Standard press state. Optionally fire `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` from `expo-haptics` on pressIn when `hapticStyle === 'light'` — load expo-haptics dynamically with try/require so a missing package is a silent no-op, not a crash. The reference pattern lives in `mobile/app/(tabs)/profile.tsx` (the dynamic `try { Haptics = require('expo-haptics'); } catch (_) {}` block); verify the line range is still accurate when executing (currently lines 18-26) by `grep -n "expo-haptics" mobile/app/(tabs)/profile.tsx` before copying. Wrap children in `Animated.View` so the scale transform applies.

Create `mobile/components/ui/Chip.tsx`: implement the UI-SPEC §4 Chips/Pills contract — minHeight 32px (touch target floor satisfied by surrounding paddingVertical 8), paddingHorizontal 16px, borderRadius 100 (RADIUS_PILL). Default state uses `SURFACE_RAISED` bg + `BORDER_SUBTLE` border + `INK_SECONDARY` text. Active state uses `GOLD_DIM` bg + `GOLD_BORDER` border + `GOLD` text (weight 700). If `icon` prop provided, render the lucide icon at size 14 with the same color as text, 6px to the left of the label. If `onPress` is provided, wrap the chip in `PressableScale` (cross-component import). All colors from `mobile/lib/tokens.ts` — no inline hex.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const ps=fs.readFileSync('mobile/components/ui/PressableScale.tsx','utf8'); const ch=fs.readFileSync('mobile/components/ui/Chip.tsx','utf8'); if(!ps.includes('withSpring')) process.exit(1); if(!ps.includes('useSharedValue')) process.exit(2); if(!ch.includes('GOLD_DIM')) process.exit(3); const noComments = ch.split('\n').filter(l=>!l.trim().startsWith('//')).join('\n'); if(/#[0-9A-Fa-f]{6}/.test(noComments)) { console.error('Inline hex in Chip'); process.exit(4); } console.log('OK');"</automated>
  </verify>
  <done>Both files exist, export the named components, use Reanimated for press animation, and contain zero inline hex strings outside imports/comments.</done>
</task>

<task type="auto">
  <name>Task 2: Create CategoryStrip component (44pt min touch target per tab)</name>
  <files>mobile/components/ui/CategoryStrip.tsx</files>
  <action>Create a horizontal FlatList (not ScrollView — better perf with 10 items) with `horizontal`, `showsHorizontalScrollIndicator={false}`, `pagingEnabled={false}`, `contentContainerStyle={{ paddingHorizontal: 16, gap: 4 }}`. Render each item as a Pressable column with EXPLICIT `minHeight: 44` (per M-2 touch-target floor — non-negotiable). Inside the column: lucide icon on top at size 20, label below at 11px weight 600. Active item: white icon + white label + a 2px gold underline absolute-positioned at the bottom of the column (translate the underline 6px below the label baseline). Inactive: `INK_FAINT` icon + `INK_FAINT` label. Each tab is a `Pressable` (NOT PressableScale — these don't need scale animation, just color flip — matches web behavior). Min width 64px per tab AND minHeight 44px. Call `onChange(item.id)` on press. The container has `backgroundColor: 'rgba(13,31,31,0.92)'` (mid-surface with blur substitute since BlurView would cost a dep), `borderBottomColor: BORDER_SUBTLE`, `borderBottomWidth: 1`. The component is stateless — `activeId` is owned by the parent screen. All tokens via `mobile/lib/tokens.ts`.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('mobile/components/ui/CategoryStrip.tsx','utf8'); if(!c.includes('FlatList')) process.exit(1); if(!c.includes('horizontal')) process.exit(2); if(!c.includes('activeId')) process.exit(3); if(!c.includes('minHeight: 44') && !c.includes('minHeight:44')) {console.error('Missing minHeight 44 on tab');process.exit(4);} console.log('OK');"</automated>
  </verify>
  <done>File exists, exports `CategoryStrip` with the props shape from the interfaces block, uses a horizontal FlatList, every tab has `minHeight: 44`, and passes `tsc --noEmit` from the mobile workspace.</done>
</task>

<task type="auto">
  <name>Task 3: Create NewsTicker component (Animated.loop marquee, link field)</name>
  <files>mobile/components/NewsTicker.tsx</files>
  <action>Create the React Native counterpart to `web/src/components/landing/NewsTicker.tsx`. Use `useQuery({ queryKey: ['news-ticker'], queryFn: () => fetcher('/news?limit=20'), staleTime: 5 * 60 * 1000 })` from `mobile/lib/api`. Embed the same FALLBACK array of 4 items inline (copy from the web file — keeps strings identical). Render a single horizontal container `height: 36, backgroundColor: SURFACE_DEEP, borderTopColor: BORDER_SUBTLE, borderBottomColor: BORDER_SUBTLE, borderTopWidth: 1, borderBottomWidth: 1`. Inside: a left "LIVE" pill (red dot + label + Iṣẹ́yáá Pulse text with a Radio icon), then an `Animated.View` that holds two copies of the headline list rendered side-by-side. On mount, measure the inner content width with `onLayout`, then start an `Animated.loop(Animated.timing(translateX, { toValue: -contentWidth, duration: 60000, easing: Easing.linear, useNativeDriver: true }))` per CONTEXT.md `<specifics>`. For the LIVE dot, start a parallel `Animated.loop` that toggles opacity 0.4 → 1 → 0.4 over 1200ms (no `Animated.sequence` shortcut — write it explicitly). Render each headline as `<source · headline>` text — source in `GOLD` weight 700 size 10 uppercase letterSpacing 1.2, separator `·` in `INK_FAINT`, headline in `INK_SECONDARY` size 13. Tapping a headline that has a `link` field calls `Linking.openURL(item.link)` from `react-native` — the News model field is `link` (verified in `backend/prisma/schema.prisma:848`), NOT `url`. NO `react-native-marquee` dependency (CONTEXT cross-cutting constraint). All colors from tokens — no inline hex except `'#EF4444'` for the LIVE dot which is acceptable as a one-off (or add a `LIVE_RED` constant if cleaner).</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('mobile/components/NewsTicker.tsx','utf8'); if(!c.includes('Animated.loop')) process.exit(1); if(!c.includes('useNativeDriver: true')) process.exit(2); if(!c.includes('/news?limit=20')) process.exit(3); if(c.includes('react-native-marquee')) process.exit(4); if(c.includes('item.url')||c.includes('.url)')) {console.error('Used item.url — News model uses item.link');process.exit(5);} if(!c.includes('item.link')&&!c.includes('.link')) {console.error('Missing item.link reference');process.exit(6);} console.log('OK');"</automated>
  </verify>
  <done>File exists, queries `/news?limit=20`, uses `Animated.loop` + `translateX` + `useNativeDriver: true`, has a LIVE pulsing dot, renders 20 items max, falls back to the inline FALLBACK array on empty/error, and opens headline URLs via `Linking.openURL(item.link)` (NOT item.url). `tsc --noEmit` passes from the mobile workspace.</done>
</task>

</tasks>

<verification>
- `cd mobile && npx tsc --noEmit` passes with no new errors from these 4 files.
- `grep -E '#[0-9A-Fa-f]{6}' mobile/components/NewsTicker.tsx mobile/components/ui/Chip.tsx mobile/components/ui/CategoryStrip.tsx | grep -v '//' | grep -v EF4444` returns no matches (only acceptable inline hex is the LIVE_RED constant if used).
- `grep -c 'minHeight: 44' mobile/components/ui/CategoryStrip.tsx` returns >= 1.
- `grep "item.link\|\.link" mobile/components/NewsTicker.tsx` returns a match; `grep "item.url" mobile/components/NewsTicker.tsx` returns nothing.
- No new deps added to `mobile/package.json` (this plan uses only what 08-01 already installed + existing deps).
</verification>

<success_criteria>
- 4 files exist under `mobile/components/`.
- NewsTicker queries `/news?limit=20` via TanStack Query, opens links via `Linking.openURL(item.link)`, and renders an `Animated.loop` translateX marquee with `useNativeDriver: true`.
- PressableScale uses `react-native-reanimated` `withSpring` (not Animated.spring).
- CategoryStrip is generic (drives both Stays and Marketplace lists in Wave 3), with each tab honoring minHeight: 44.
- Chip matches UI-SPEC §4 (32px height, pill radius, gold-dim active state).
- All existing 282+ tests still pass.
</success_criteria>

<output>
Create `.planning/phases/08-mobile-redesign/08-02-SUMMARY.md` with: files created, animation parameters chosen (stiffness/damping), Reanimated version used, and any decisions on the LIVE-red constant location.
</output>
