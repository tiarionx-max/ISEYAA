---
phase: 08-mobile-redesign
plan: 06
type: execute
wave: 3
depends_on: [08-01, 08-02, 08-04, 08-04b]
files_modified:
  - mobile/app/marketplace/[id].tsx
  - mobile/app/cart.tsx
  - mobile/app/checkout.tsx
autonomous: true
requirements: [MOB-RD-05]
must_haves:
  truths:
    - "A product detail screen at /marketplace/[id] renders gallery + qty stepper + Add to Cart + Buy Now"
    - "A cart drawer modal at /cart shows current items, allows qty edits + remove, displays subtotal, and routes to /checkout"
    - "A checkout screen at /checkout shows order summary + single email field (defaulted from session) + Pay button — NO delivery address (backend forbids it via forbidNonWhitelisted)"
    - "Checkout submits POST /api/v1/orders with { items, email } and opens response.payment.authorizationUrl in expo-web-browser"
    - "Routes are already registered by 08-04b — this plan does NOT touch mobile/app/_layout.tsx (H-4 disjoint ownership)"
  artifacts:
    - path: "mobile/app/marketplace/[id].tsx"
      provides: "Product detail screen"
      contains: "useCartStore"
    - path: "mobile/app/cart.tsx"
      provides: "Cart drawer modal (transparentModal with right-slide)"
      contains: "useCartStore"
    - path: "mobile/app/checkout.tsx"
      provides: "Checkout form + Paystack handoff to POST /api/v1/orders"
      contains: "/orders"
  key_links:
    - from: "mobile/app/cart.tsx"
      to: "useCartStore"
      via: "items selector + removeItem/updateQty/totalPrice actions"
      pattern: "useCartStore"
    - from: "mobile/app/checkout.tsx"
      to: "POST /api/v1/orders"
      via: "api.post('/orders', { items, email })"
      pattern: "/orders"
    - from: "mobile/app/checkout.tsx"
      to: "expo-web-browser"
      via: "WebBrowser.openAuthSessionAsync for Paystack URL"
      pattern: "expo-web-browser"
---

<objective>
Build the three Marketplace flow screens that 08-05 leaves stubbed: (1) product detail at `/marketplace/[id]`, (2) cart drawer modal at `/cart`, (3) checkout at `/checkout` with Paystack handoff to **`POST /api/v1/orders`** (H-2 fix — NOT the non-existent `/cart/checkout`).

Closes MOB-RD-05 success criterion #5 in full (the browse portion was done by 08-05; this finishes detail + cart + checkout flow into Paystack via `POST /api/v1/orders`).

**Out of scope (H-4 disjoint ownership):** This plan does NOT touch `mobile/app/_layout.tsx`. The 4 Stack routes (`marketplace/[id]`, `cart`, `checkout`, `host`) are already registered by 08-04b (Wave 2). Wave 3 plans run truly parallel.

**Out of scope (H-2 contract):** Delivery address capture is deferred. The backend `CreateOrderDto` (verified `backend/src/modules/marketplace/dto/create-order.dto.ts`) accepts ONLY `{ items, email }`. The global `ValidationPipe` runs with `forbidNonWhitelisted: true` — any extra field (e.g. `deliveryAddress`) will return HTTP 400. Mobile UI must NOT collect a delivery address this phase. Email-only checkout matches the actual backend contract.

Output: 3 new screens. No `_layout.tsx` edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-mobile-redesign/08-CONTEXT.md
@.planning/mobile-redesign-UI-SPEC.md
@CLAUDE.md
@mobile/lib/tokens.ts
@mobile/lib/api.ts
@mobile/lib/cart-store.ts
@mobile/components/ui/PressableScale.tsx
@mobile/components/ui/Chip.tsx
@mobile/app/topup.tsx
@web/src/app/marketplace/page.tsx
@web/src/app/checkout/page.tsx
@backend/src/modules/marketplace/marketplace.controller.ts
@backend/src/modules/marketplace/dto/create-order.dto.ts

<interfaces>
<!-- CANONICAL request/response shapes — verified against backend code on 2026-06-09. -->

POST /api/v1/orders request (matches CreateOrderDto in backend/src/modules/marketplace/dto/create-order.dto.ts):

  type OrderItem = { productId: string; quantity: number };  // class-validator: @IsUUID, @IsInt @Min(1)

  type CreateOrderRequest = {
    items: OrderItem[];   // class-validator: @IsArray @ValidateNested({ each: true })
    email: string;        // class-validator: @IsEmail — REQUIRED
  };

  -- NO `deliveryAddress` field. NO `phone` field. Backend's global ValidationPipe runs with
     forbidNonWhitelisted: true (backend/src/main.ts). Any extra field returns HTTP 400.

POST /api/v1/orders response (matches MarketplaceService.createOrder shape — mirror web's
checkout flow at web/src/app/checkout/page.tsx for how to read it defensively):

  type OrderResponse = {
    orderId: string;
    payment?: {
      authorizationUrl: string;   // Paystack hosted page URL
      reference: string;
    };
    authorizationUrl?: string;    // legacy fallback path — web reads this defensively
  };

  // Read defensively (mirror web pattern):
  //   const url = data.payment?.authorizationUrl ?? data.authorizationUrl;
  //   if (!url) throw new Error('No payment URL in order response');

Cart store API (from mobile/lib/cart-store.ts — 08-01, mirrors web/src/lib/cart.ts):
  useCartStore: items, addItem(product, qty?), removeItem(productId), updateQty(productId, qty), clear(), totalCount(), totalPrice()
  useCartDrawerStore: open: boolean, openDrawer(), closeDrawer(), toggleDrawer()  -- USE THE DRAWER-SUFFIXED NAMES

  -- addItem takes (MinimalProduct, qty?). Pass the Product object (or { id, name, price, imageUrls, vendor }).
  -- DO NOT build a CartItem manually and pass it — the store derives imageUrl/vendorName internally.

Session/user shape (read from existing auth — typically via SecureStore access_token + useQuery on /users/me):
  -- user.email is the default value for the checkout email field.
  -- Reference: see how `mobile/app/(tabs)/profile.tsx` fetches /users/me.

Reference: see how an existing screen does Paystack handoff (`mobile/app/topup.tsx`) for the exact
`WebBrowser.openAuthSessionAsync` call signature.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Product detail screen at mobile/app/marketplace/[id].tsx</name>
  <files>mobile/app/marketplace/[id].tsx</files>
  <action>Create the route folder `mobile/app/marketplace/` if needed, then create `[id].tsx`. The Stack route is already registered by 08-04b — no `_layout.tsx` edit needed here. Use `useLocalSearchParams<{ id: string }>()` to read the product ID. Fetch: `useQuery({ queryKey: ['product', id], queryFn: () => fetcher('/products/' + id) })`.

Layout: full-screen with `useSafeAreaInsets` for the back button area. Top: image gallery — if `product.imageUrls` exists with ≥ 2 images, use a horizontal paged `FlatList` with `pagingEnabled`, dots indicator at the bottom; if only one image, single hero. All images via `expo-image` with `contentFit="cover"` and `aspectRatio: 1`. Overlay back chevron (top-left) and wishlist heart (top-right), both with `rgba(0,0,0,0.4)` circular backgrounds (those are the only acceptable inline overlays — use a const if cleaner).

Below gallery: padded section with product name (TYPE.heading), vendor name (Caption, `vendor.businessName`), then the price row: gold `₦{price}` (large, weight 700, tabular-nums), strikethrough `compareAtPrice` if present, and the discount % pill if applicable (only if Task 0 of 08-05 GREEN-LIT the field).

Qty stepper: row with `-` and `+` buttons (44pt min) flanking a center quantity number (Body emphasis), bounded by `[1, product.inventory ?? 99]`. State: `const [qty, setQty] = useState(1)`.

Two action buttons stacked: (a) "Add to cart" — secondary gold-outline button per UI-SPEC §6, calls `useCartStore.getState().addItem(product, qty)` (pass the Product directly — the store handles derivation per H-5) and shows a toast or `Alert.alert('Added', '...')`. (b) "Buy now" — primary gold-fill button per UI-SPEC §5, calls `addItem(product, qty)` AND `router.push('/checkout')`.

Below buttons: tabbed description / shipping / reviews section using a simple horizontal tab row with three Chips; only "Description" tab needs real content (render `product.description ?? 'No description'`); Shipping and Reviews tabs can show "Coming soon" placeholders (CONTEXT.md `<deferred>` keeps reviews as separate phase).

All tokens from `mobile/lib/tokens.ts`. Touch targets ≥ 44pt.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('mobile/app/marketplace/[id].tsx','utf8'); if(!c.includes('useCartStore')) process.exit(1); if(!c.includes('expo-image')) process.exit(2); if(!c.includes('router.push') || !c.includes('/checkout')) process.exit(3); if(!c.includes('addItem(product') && !c.includes('addItem(\n')) {console.error('addItem should receive the Product, not a constructed CartItem');process.exit(4);} console.log('OK');"</automated>
  </verify>
  <done>`mobile/app/marketplace/[id].tsx` exists, renders product gallery + qty stepper + Add to Cart + Buy Now, both buttons call `useCartStore.getState().addItem(product, qty)` (passing the MinimalProduct shape, not a constructed CartItem), Buy Now navigates to `/checkout`. `tsc --noEmit` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Cart drawer modal at mobile/app/cart.tsx</name>
  <files>mobile/app/cart.tsx</files>
  <action>Create `mobile/app/cart.tsx`. The Stack route is already registered by 08-04b as `presentation: 'transparentModal', animation: 'none'`. Layout: right-aligned ~85% width drawer card, dark backdrop on the left covering the remaining ~15%. Tapping the backdrop calls `router.back()`. The drawer slides in from the right on mount using `react-native-reanimated` `useSharedValue(SCREEN_WIDTH)` → `withTiming(0, { duration: 280 })` translateX; reverse on unmount/back via `useFocusEffect` cleanup.

Drawer contents: header with title "Cart" + close X (calls `router.back()`). Body: `<FlatList data={items}>` from `useCartStore(s => s.items)` — each row shows thumbnail (60×60 expo-image of `item.imageUrl`), name (Body), vendor (Caption, `item.vendorName`), price × qty (mono tabular nums), small qty stepper (- / + buttons calling `updateQty(productId, n)`), and a Remove (X) button calling `removeItem(productId)`. Empty state: centered "Your cart is empty" with `INK_FAINT` text and a "Browse products" button that calls `router.back()`.

Footer (sticky bottom): subtotal row "Subtotal" + `₦{useCartStore.getState().totalPrice().toLocaleString()}` — use the store method (H-5), do NOT re-implement the sum inline. Then a full-width primary gold button "Checkout (${useCartStore.getState().totalCount()} items)" that calls `router.replace('/checkout')` (replace not push — drawer should not stay in stack when checkout opens). Disable button when `items.length === 0`.

All colors from tokens.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('mobile/app/cart.tsx','utf8'); const need=['useCartStore','removeItem','updateQty','/checkout','totalPrice','totalCount']; for(const t of need){if(!c.includes(t)){console.error('Missing token',t);process.exit(1);}} console.log('OK');"</automated>
  </verify>
  <done>`cart.tsx` exists, renders a right-slide drawer with items + qty stepper + remove + subtotal (using `totalPrice()`) + checkout CTA (using `totalCount()`). Empty cart shows the empty state. `tsc --noEmit` passes.</done>
</task>

<task type="auto">
  <name>Task 3: Checkout screen at mobile/app/checkout.tsx — POST /api/v1/orders with { items, email } only</name>
  <files>mobile/app/checkout.tsx</files>
  <action>Create `mobile/app/checkout.tsx`. The Stack route is already registered by 08-04b as `presentation: 'card'`. Layout: header with back chevron + title "Checkout". Body in a `ScrollView`:

**Section 1 — "Order Summary":** list of cart items (compact rows from `useCartStore(s => s.items)`), each showing thumbnail + name + qty + line total (`price × quantity`). Below: "Subtotal" row with `useCartStore.getState().totalPrice().toLocaleString()` in gold tabular-nums.

**Section 2 — "Confirmation email":** a single `TextInput` for email. State: `const [email, setEmail] = useState<string>('')`. On mount, default the value from the session: `useQuery({ queryKey: ['me'], queryFn: () => fetcher('/users/me') })` — when `me?.email` arrives, set `email` to `me.email` if the field is currently empty. UI-SPEC §8 Text Input styles, height 52, borderRadius 14, `SURFACE_RAISED` bg, `BORDER_SUBTLE` border, `BORDER_FOCUS` on focus, `keyboardType="email-address"`, `autoCapitalize="none"`, label "Email for receipt", placeholder "you@example.com".

**Explicit deferral note (per H-2):** Delivery address capture is deferred. The backend `CreateOrderDto` accepts only `{ items, email }`; any extra field returns HTTP 400 (global `forbidNonWhitelisted: true`). Do NOT add a delivery-address form section. Future phase will add address handling when the backend DTO is extended.

**Submit button** (full-width primary gold, sticky at bottom): "Pay ₦{useCartStore.getState().totalPrice().toLocaleString()}". On press:

1. Validate the email — if invalid (failing a simple `/^[^\s@]+@[^\s@]+\.[^\s@]+$/` test), show inline error caption "Enter a valid email" under the input and bail.
2. `useMutation({ mutationFn: () => api.post('/orders', { items: useCartStore.getState().items.map(i => ({ productId: i.productId, quantity: i.quantity })), email }).then(r => r.data) })`.
3. On success, read the Paystack URL DEFENSIVELY (mirror web pattern at `web/src/app/checkout/page.tsx`): `const url = response.payment?.authorizationUrl ?? response.authorizationUrl`. If `url` is falsy, surface "Order created but no payment URL" alert.
4. Call `WebBrowser.openAuthSessionAsync(url, 'iseyaa://checkout-callback')` from `expo-web-browser`.
5. After the browser closes (any result), clear the cart via `useCartStore.getState().clear()` and `router.replace('/(tabs)/book')` with a toast or `Alert.alert('Order submitted', 'Check your wallet for confirmation.')`.
6. On mutation error, surface `err?.response?.data?.message` via `Alert.alert`.

Disable submit while mutation is pending and show ActivityIndicator in button per UI-SPEC §5 loading state. All colors from tokens.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('mobile/app/checkout.tsx','utf8'); const need=['/orders','expo-web-browser','openAuthSessionAsync','clear()','payment?.authorizationUrl','/users/me']; for(const t of need){if(!c.includes(t)){console.error('Missing',t);process.exit(1);}} if(c.includes('/cart/checkout')){console.error('Stale endpoint /cart/checkout — must be /orders');process.exit(2);} if(c.includes('deliveryAddress')||c.includes('delivery_address')){console.error('Forbidden field deliveryAddress — backend rejects via forbidNonWhitelisted');process.exit(3);} console.log('OK');"</automated>
  </verify>
  <done>`checkout.tsx` exists, posts to `/orders` (NOT `/cart/checkout`) with `{ items, email }` (NO deliveryAddress), email defaults from `/users/me`, reads payment URL defensively from `response.payment?.authorizationUrl ?? response.authorizationUrl`, opens Paystack via `expo-web-browser`, clears the cart on success. `tsc --noEmit` passes.</done>
</task>

</tasks>

<verification>
- `cd mobile && npx tsc --noEmit` passes.
- `grep -c "/orders" mobile/app/checkout.tsx` returns >= 1; `grep -c "/cart/checkout" mobile/app/checkout.tsx` returns 0.
- `grep -c "deliveryAddress" mobile/app/checkout.tsx` returns 0 (backend rejects).
- `grep "payment?.authorizationUrl" mobile/app/checkout.tsx` returns a match (defensive read).
- `git diff --stat mobile/app/_layout.tsx` returns no changes (08-04b owns _layout edits).
- Add-to-cart from Book → Marketplace → product detail increments header bag badge.
- Bag tap opens the cart drawer (slides in from right), shows items, qty stepper works, remove works.
- "Checkout (N items)" routes to checkout, email field defaults from session, submit posts to `/orders`, response opens Paystack URL via `expo-web-browser`.
- After Paystack closes, cart is cleared and user returns to Book tab.
- All existing 282+ tests still pass.
</verification>

<success_criteria>
- MOB-RD-05 fully closed: 8 category tabs (08-05) + product grid (08-05) + wishlist (08-05) + cart drawer (this plan) + checkout flow (this plan) handing off to Paystack via existing `POST /api/v1/orders`.
- AsyncStorage key `iseyaa-cart-v1` mirrors web exactly (from 08-01 — verified by integration here).
- No `_layout.tsx` edits (08-04b owns those).
- No delivery-address collection (deferred per backend contract).
</success_criteria>

<output>
Create `.planning/phases/08-mobile-redesign/08-06-SUMMARY.md` with: 3 screens created, Paystack callback URL scheme used (`iseyaa://checkout-callback` or otherwise), email default-from-session pattern used, and explicit confirmation that no `_layout.tsx` edits were made and no `deliveryAddress` field was collected.
</output>
