---
phase: 08-mobile-redesign
plan: 05
type: execute
wave: 3
depends_on: [08-01, 08-02, 08-04, 08-04b]
files_modified:
  - mobile/app/(tabs)/book.tsx
  - mobile/components/book/EventsSubsection.tsx
autonomous: true
requirements: [MOB-RD-03, MOB-RD-05]
must_haves:
  truths:
    - "Book hub has a top-level sub-section switcher with ALL 4 sub-sections: Events, Stays, Studio, Marketplace (CONTEXT §Navigation Architecture — none may be dropped)"
    - "Events sub-section renders the migrated EventsFeed + QR scan-in FAB previously in book.tsx (and the deleted events.tsx)"
    - "Stays sub-section renders the 10-category strip from STAY_CATEGORIES and a photo-first 2-column grid"
    - "Selecting a category triggers a refetch with the correct query string (types/bookingMode/featured)"
    - "Marketplace sub-section renders the 8-category strip and a 2-column product grid with wishlist hearts and Add button (44pt min touch target)"
    - "Discount badge renders ONLY if Product.compareAtPrice is confirmed present in the schema (Task 0 audit gates this)"
    - "Book hub header shows a bag/cart icon with the live count from useCartStore.getState().totalCount() that opens the cart drawer via useCartDrawerStore.getState().openDrawer()"
    - "Studio sub-section keeps its legacy look this phase (per CONTEXT §Navigation Architecture — full redesign deferred)"
  artifacts:
    - path: "mobile/app/(tabs)/book.tsx"
      provides: "Four-pane Book hub: Events + Stays + Studio + Marketplace + cart-icon header"
      contains: "STAY_CATEGORIES"
    - path: "mobile/components/book/EventsSubsection.tsx"
      provides: "Migrated EventsFeed list + QR scan-in FAB"
      exports: ["EventsSubsection"]
  key_links:
    - from: "mobile/app/(tabs)/book.tsx"
      to: "/api/v1/properties"
      via: "useQuery with buildStayQuery output"
      pattern: "useQuery.*properties"
    - from: "mobile/app/(tabs)/book.tsx"
      to: "/api/v1/products"
      via: "useQuery with buildMarketplaceQuery output"
      pattern: "useQuery.*products"
    - from: "mobile/app/(tabs)/book.tsx"
      to: "mobile/lib/cart-store.ts"
      via: "useCartStore.totalCount() for badge, useCartDrawerStore.openDrawer() on bag tap"
      pattern: "useCartStore|useCartDrawerStore|openDrawer"
    - from: "mobile/components/book/EventsSubsection.tsx"
      to: "/api/v1/events"
      via: "useQuery (migrated from current book.tsx EventsFeed)"
      pattern: "useQuery.*events"
---

<objective>
Rewrite `mobile/app/(tabs)/book.tsx` as the new Book hub with FOUR top-level sub-sections (per locked CONTEXT decision — none may be dropped):

1. **Events** — migrated EventsFeed + QR scan-in FAB (H-1 fix). Functionality preserved from current `book.tsx:179-217` (EventsFeed) and from the now-deleted `events.tsx:57` (QR FAB).
2. **Stays** — Airbnb-style 10-category strip + photo-first 2-column grid driven by `GET /api/v1/properties` filtered by category, mirroring `web/src/app/stays/page.tsx`.
3. **Studio** — keeps the existing legacy fetch + list rendering from current `book.tsx` (the Studio code that already exists in the file). Full Studio redesign is DEFERRED to a follow-up phase per CONTEXT §Navigation Architecture. Do NOT redesign Studio.
4. **Marketplace** — Temu-style 8-category strip + 2-column product grid with wishlist hearts, an Add button (44pt min touch target — M-2 fix), and a cart icon in the header that opens the cart drawer (drawer route built in 08-06).

Closes MOB-RD-03 success criterion #3 (Stays sub-section) and the browse-portion of MOB-RD-05 (Marketplace sub-section grid + cart trigger). Product detail + cart drawer modal + checkout screen are 08-06's scope.

Purpose: This is the most context-heavy plan in the phase because it owns the only shared file the Events/Stays/Studio/Marketplace work touches (`book.tsx`). Keeping all 4 sub-sections in one plan ensures one executor owns the file's structure end-to-end (no merge conflicts) and lets us reuse the same hook patterns, grid card, and header layout across sub-sections.

Output: A fully rewritten `mobile/app/(tabs)/book.tsx` (~700-900 lines) + one new component `mobile/components/book/EventsSubsection.tsx` for the migrated events functionality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-mobile-redesign/08-CONTEXT.md
@.planning/mobile-redesign-UI-SPEC.md
@CLAUDE.md
@mobile/app/(tabs)/book.tsx
@mobile/lib/tokens.ts
@mobile/lib/api.ts
@mobile/lib/category-config.ts
@mobile/lib/cart-store.ts
@mobile/components/NewsTicker.tsx
@mobile/components/ui/CategoryStrip.tsx
@mobile/components/ui/Chip.tsx
@mobile/components/ui/PressableScale.tsx
@backend/prisma/schema.prisma
@web/src/app/stays/page.tsx
@web/src/app/marketplace/page.tsx

<interfaces>
<!-- Backend property/product response shapes (read-only — do not modify backend) -->

Property (from backend/src/modules/stays/stays.controller.ts GET /properties):

  type Property = {
    id: string;
    name: string;
    city: string;
    propertyType: 'HOTEL' | 'GUESTHOUSE' | 'APARTMENT' | 'VILLA' | 'RESORT' | 'LOUNGE' | 'CLUB' | 'BEACH' | 'TOUR' | 'EXPERIENCE' | 'ATTRACTION';
    bookingMode: 'NIGHTLY' | 'HOURLY' | 'TIMED_EVENT' | 'MEMBERSHIP';
    pricePerNight?: number | null;
    pricePerHour?: number | null;
    membershipMonthlyPrice?: number | null;
    membershipBenefits?: string[];
    coverImageUrl?: string | null;
    galleryImages?: string[];
    isFeatured?: boolean;
    maxGuests?: number;
    averageRating?: number | null;
    highlights?: string[];
    amenities?: string[];
    lga?: { name: string };
  };

Product (from backend/src/modules/marketplace/marketplace.controller.ts GET /products):
  Confirmed schema (backend/prisma/schema.prisma:446-450):
    - id, name, slug, description?
    - imageUrls: String[]   -- array, first entry is the canonical thumbnail
    - price: Decimal
    - compareAtPrice?: Decimal  -- CONFIRMED present on Product model (schema line 450)
    - category, vendor.businessName (relation), inventory?

  type Product = {
    id: string;
    name: string;
    price: number;
    compareAtPrice?: number | null;
    imageUrls?: string[];
    category: string;
    isFeatured?: boolean;
    vendor?: { id: string; businessName?: string | null };
    inventory?: number;
    description?: string | null;
  };

Cart store API (from mobile/lib/cart-store.ts — 08-01, mirrors web/src/lib/cart.ts):
  useCartStore: { items: CartItem[]; addItem(product, qty?); removeItem; updateQty; clear; totalCount(): number; totalPrice(): number }
  useCartDrawerStore: { open: boolean; openDrawer(); closeDrawer(); toggleDrawer() }

  -- addItem signature: addItem(product: MinimalProduct, qty?: number)
  -- MinimalProduct = { id, name, price: number|string, imageUrls?: string[], vendor?: { businessName?: string|null } | null }
  -- DO NOT pass a CartItem directly. Pass the Product (or a shape compatible with MinimalProduct).
  -- The store derives imageUrl from imageUrls[0] and vendorName from vendor.businessName internally.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Audit Product.compareAtPrice in Prisma schema (gates discount-badge sub-task)</name>
  <files>(no file changes — read-only audit)</files>
  <action>Open `backend/prisma/schema.prisma` and grep for the Product model's `compareAtPrice` field. Per M-4: the plan assumes this field exists, but planner verified it at schema line 450 (Decimal? with a `// "was ₦X" — for sale strikethrough` comment). Re-confirm during execution:

```bash
grep -n 'compareAtPrice' backend/prisma/schema.prisma
```

If the grep returns a line with `compareAtPrice` in the Product model section (between `model Product {` and the next `model `), the discount badge sub-task in Task 2 is GREEN-LIT — proceed. If the grep returns nothing or the field has been removed since planning, DROP the discount-badge sub-task in Task 2 and document the omission in the plan SUMMARY. Do not invent the field.</action>
  <verify>
    <automated>bash -c 'grep -n "compareAtPrice" backend/prisma/schema.prisma | head -3; grep -q "compareAtPrice" backend/prisma/schema.prisma && echo OK || (echo MISSING; exit 1)'</automated>
  </verify>
  <done>Field presence confirmed (or absence documented). Decision recorded in plan SUMMARY for Task 2 to honor.</done>
</task>

<task type="auto">
  <name>Task 1: Create mobile/components/book/EventsSubsection.tsx (migrate EventsFeed + QR FAB)</name>
  <files>mobile/components/book/EventsSubsection.tsx</files>
  <action>Create `mobile/components/book/EventsSubsection.tsx`. Migrate the EventsFeed implementation from the current `mobile/app/(tabs)/book.tsx` (currently lines 179-217 per planner grep — verify line range still accurate when executing). The EventsFeed currently:
  - Fetches via `useQuery({ queryKey: ['events'], queryFn: () => fetcher('/events?limit=10') })` (currently in book.tsx around line 410-413).
  - Renders the events list with `router.push('/events/${item.id}')` press handler.

Additionally, migrate the QR scan-in FAB from the deleted `events.tsx` (planner preserved the JSX in `08-04-SUMMARY.md`). The FAB is a floating Pressable in the bottom-right corner that calls `router.push('/qr-checkin')`. Use the design tokens (FOREST + GOLD per UI-SPEC) and `PressableScale` for press animation. Place it absolute-positioned, `bottom: 24, right: 24`, with a small `QrCode` lucide icon and "Scan ticket" label.

Component signature:

  export function EventsSubsection(): JSX.Element;

Self-contained — no props. Owns its own useQuery + FAB. Touch targets ≥ 44pt. No inline hex.

Acceptance check (per H-1): after executing this task, `grep -rE "EventsSubsection|events.*sub" mobile/app/(tabs)/book.tsx` MUST return at least one match once Task 2 wires it in. This task only creates the component file; the wiring happens in Task 2.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('mobile/components/book/EventsSubsection.tsx','utf8'); if(!c.includes('EventsSubsection')) process.exit(1); if(!c.includes('/events')) process.exit(2); if(!c.includes('/qr-checkin')) {console.error('Missing QR scan-in FAB');process.exit(3);} console.log('OK');"</automated>
  </verify>
  <done>`mobile/components/book/EventsSubsection.tsx` exists, exports `EventsSubsection`, contains the migrated events fetch + list rendering + QR scan-in FAB. `tsc --noEmit` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite book.tsx with 4-sub-section switcher (Events default first OR Stays default — pick one) + Stays + Marketplace sub-sections + cart-icon header</name>
  <files>mobile/app/(tabs)/book.tsx</files>
  <action>Rewrite `mobile/app/(tabs)/book.tsx` from scratch. Top-level structure: `<SafeAreaView edges={['top']}>` → header row → sub-section switcher chips → conditional render of active sub-section content.

**Header row:** title "Book" using TYPE.heading from tokens; right-aligned bag/cart icon (lucide `ShoppingBag`) with a small gold circular badge showing `useCartStore.getState().totalCount()` (hidden when 0) — H-5 method name. Tapping the bag calls `useCartDrawerStore.getState().openDrawer()` (H-5 method name — NOT `open()`).

**Sub-section switcher:** FOUR `Chip` components from `mobile/components/ui/Chip` for **Events / Stays / Studio / Marketplace** (per locked CONTEXT decision — all 4 required). Controls local state `activeSection: 'events' | 'stays' | 'studio' | 'marketplace'` (default `'stays'` — Stays is the showcase redesign this phase, but Events MUST be selectable). Each chip has `minHeight: 44` via wrapper padding.

**Events sub-section:** render `<EventsSubsection />` (imported from `mobile/components/book/EventsSubsection`). No further work — the component is self-contained.

**Stays sub-section:** render `<CategoryStrip items={STAY_CATEGORIES_AS_STRIP_ITEMS} activeId={activeStayCat} onChange={setActiveStayCat} />` (transform STAY_CATEGORIES to CategoryStripItem[] inline — drop the `types`/`bookingMode`/`featured` fields). Local state `activeStayCat` default `'all'`. Below: `useQuery({ queryKey: ['stays-browse', activeStayCat], queryFn: () => fetcher('/properties?' + buildStayQuery(STAY_CATEGORIES.find(c=>c.id===activeStayCat)!)), staleTime: 30_000 })`. Render results as a 2-column grid using FlatList with `numColumns={2}` and `columnWrapperStyle={{ gap: 12, paddingHorizontal: 16 }}` and a `StayCard` component (define inline) that uses `expo-image` for `property.coverImageUrl`, falls back to a `CARD_COLORS` gradient when null, overlays a price chip (Chip with `label={formatPrice(p)}` and `active` for gold styling), and a category badge in the top-left corner. Wrap each card in `PressableScale` (`onPress={() => router.push('/stays/' + item.id)}`). Show a skeleton grid while `isLoading`. Empty state: simple "No stays in this category yet" text in `INK_FAINT`.

`formatPrice(p)`: mirror the web helper — switch on `bookingMode` to produce `{primary, suffix}` (e.g. `₦18,000` + `/ night`). Use existing locale formatting `Number(n).toLocaleString()`.

**Studio sub-section:** preserve the existing Studio rendering from the current `book.tsx` file (Studio code already in the file — keep it intact). Per CONTEXT §Navigation Architecture: "Studio sub-section keeps legacy look this phase — full redesign deferred." Do NOT redesign Studio. If the current Studio block is small enough, inline it; otherwise extract to a sibling `StudioSubsection` component and call from book.tsx. Document the decision in SUMMARY.

**Marketplace sub-section:** add local state `activeMktCat` default `'all'`. Render `<CategoryStrip>` with items from `MARKETPLACE_CATEGORIES`. Below: `useQuery({ queryKey: ['products-browse', activeMktCat], queryFn: () => fetcher('/products?' + buildMarketplaceQuery(MARKETPLACE_CATEGORIES.find(c=>c.id===activeMktCat)!)), staleTime: 30_000 })`. Render as 2-column grid (same FlatList pattern as Stays). Each `ProductCard` (inline component): square thumbnail at top (1:1 aspect ratio via `style={{ aspectRatio: 1 }}`) using `expo-image` for `product.imageUrls?.[0]`, fallback to a single-color `SURFACE_RAISED` block.

  **Discount badge** (GATED by Task 0): if Task 0 confirmed `compareAtPrice` is present on Product, render a small red pill in the top-left corner reading `-{Math.round((compareAt-price)/compareAt*100)}%` (text 10px weight 700 white, bg `'#EF4444'`) when `compareAtPrice && compareAtPrice > price`. If Task 0 found the field missing, OMIT this sub-task entirely.

  Wishlist heart: top-right, tappable, stores wishlist IDs in component state (local — persistence is deferred per CONTEXT).

  Tapping the card opens product detail at `/marketplace/[id]` — this route is registered by 08-04b (Wave 2) and built by 08-06 (Wave 3). Use `router.push('/marketplace/' + product.id)`.

  Below the thumbnail: 2-line product name (Body, weight 700, ink), then price row: large gold price `₦{price}` with strikethrough `compareAtPrice` if present in `INK_FAINT`.

  Below price: an **"Add" button with EXPLICIT `minHeight: 44`** (M-2 fix — touch target floor, NOT 40pt). On press call `useCartStore.getState().addItem(product, 1)` — pass the WHOLE product (or at minimum a `MinimalProduct` shape `{ id, name, price, imageUrls, vendor }`) — NOT a pre-built CartItem (per H-5: the store derives imageUrl/vendorName internally from MinimalProduct). Fire a `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` if available using the dynamic try/require pattern from `profile.tsx` (verify line range 18-26 still accurate when executing).

Skeleton + empty state mirror the Stays approach.

All colors from `mobile/lib/tokens.ts` — no inline hex (only acceptable: discount-pill red `#EF4444` if Task 0 GREEN-LIT). Use `expo-image` for ALL photo loads (CONTEXT cross-cutting). Touch targets ≥ 44pt throughout.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('mobile/app/(tabs)/book.tsx','utf8'); const need=['STAY_CATEGORIES','MARKETPLACE_CATEGORIES','/properties','/products','useCartStore','CategoryStrip','expo-image','EventsSubsection','openDrawer','totalCount','minHeight: 44']; for(const t of need){if(!c.includes(t)){console.error('Missing token',t);process.exit(1);}} if(c.includes('useCartDrawerStore.getState().open()')){console.error('Wrong method name — must be openDrawer()');process.exit(2);} const subSections=(c.match(/events|stays|studio|marketplace/gi)||[]).length; console.log('OK, sub-section mentions:',subSections);"</automated>
  </verify>
  <done>book.tsx renders 4-section switcher (Stays default), all 4 sub-sections wired: Events via `<EventsSubsection />`, Stays via 10-category strip + 2-col grid, Studio preserves legacy rendering, Marketplace via 8-category strip + product grid with 44pt Add button. Header has working bag icon with `totalCount()` badge bound to `useCartStore`, tap calls `useCartDrawerStore.getState().openDrawer()`. `tsc --noEmit` passes.</done>
</task>

</tasks>

<verification>
- `cd mobile && npx tsc --noEmit` passes.
- `grep -rE "EventsSubsection|events.*sub" mobile/app/(tabs)/book.tsx` returns >= 1 match (H-1 acceptance check).
- All 4 sub-section literals present: `grep -E "'(events|stays|studio|marketplace)'" mobile/app/(tabs)/book.tsx | wc -l` returns >= 4.
- Switching between sub-sections preserves their selected category (separate state vars per CONTEXT specifics).
- Switching categories triggers a refetch (TanStack queryKey includes activeCat).
- Bag badge reflects `useCartStore.getState().totalCount()` and increments by 1 every time "Add" is tapped.
- Bag tap calls `useCartDrawerStore.getState().openDrawer()` (the drawer modal itself is built in 08-06; this plan only wires the trigger).
- `grep -c "minHeight: 44" mobile/app/(tabs)/book.tsx` returns >= 1 (Add button + sub-section chips).
- All existing 282+ tests still pass.
- No inline hex strings in component bodies (only acceptable: discount-pill red `#EF4444` if Task 0 GREEN-LIT).
</verification>

<success_criteria>
- All 4 sub-sections (Events / Stays / Studio / Marketplace) selectable from the Book hub switcher. None dropped (CONTEXT locked decision).
- MOB-RD-03 criterion #3: Stays sub-section with 10-category horizontal scroll + photo-first 2-col grid hitting `/api/v1/properties`.
- MOB-RD-05 criterion #5 (partial — browse portion only): Marketplace sub-section with 8 category tabs, product grid, wishlist + 44pt Add button that increments the header badge.
- Studio sub-section preserved with legacy look (redesign deferred per CONTEXT).
- Web parity for category IDs (10 stay IDs + 8 marketplace IDs match web exactly).
- Discount-badge sub-task gated by Task 0 schema audit.
</success_criteria>

<output>
Create `.planning/phases/08-mobile-redesign/08-05-SUMMARY.md` with: final book.tsx line count, Task 0 audit result (compareAtPrice present or missing — and what was done with the discount-badge sub-task), Studio sub-section handling decision, the exact `formatPrice` helper signature, EventsSubsection component summary, and a list of any wishlist persistence decisions deferred.
</output>
