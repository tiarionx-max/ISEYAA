---
phase: 08-mobile-redesign
plan: 09
type: execute
wave: 5
depends_on: [08-03, 08-05, 08-06, 08-07, 08-08]
files_modified:
  - mobile/app.json
autonomous: true
requirements: [MOB-RD-07]
must_haves:
  truths:
    - "mobile/app.json android.versionCode is incremented by +1 from the last shipped build"
    - "mobile/app.json android.package is 'ng.gov.ogun.iseyaa' (unchanged)"
    - "mobile/eas.json has a 'preview' profile with internal distribution + apk buildType"
    - "An Android EAS preview build is submitted (eas build --platform android --profile preview --non-interactive) and the install URL is captured"
  artifacts:
    - path: "mobile/app.json"
      provides: "Bumped versionCode for the preview build"
      contains: "ng.gov.ogun.iseyaa"
  key_links:
    - from: "mobile/eas.json"
      to: "EAS Build cloud"
      via: "preview profile"
      pattern: "preview"
---

<objective>
Bump `mobile/app.json` `android.versionCode` by +1, commit the bump, then run the EAS Android preview build, and capture the install URL into a phase verification record.

Closes MOB-RD-07 (success criterion #7: fresh Android EAS preview build completes and install URL is captured).

Note: This is an operational plan. The build itself runs on EAS cloud and typically takes 12-20 minutes. The executor should kick it off with `--non-interactive` and capture the resulting URL.

**Operation order (L-2 fix):** versionCode bump → commit → git verification (compares HEAD to working tree post-commit) → EAS build. The plan-check pointed out that `git show HEAD:mobile/app.json` requires the bump to already be committed; running the verification BEFORE committing returned stale data. The task ordering below honors that constraint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-mobile-redesign/08-CONTEXT.md
@mobile/app.json
@mobile/eas.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight checks (versionCode, bundle ID, preview profile, EAS login)</name>
  <files>(read-only audit, no file changes yet)</files>
  <action>Run three checks and report findings:

1. Read `mobile/app.json` and record the current `android.versionCode` and `android.package`. Confirm `android.package === 'ng.gov.ogun.iseyaa'` (CONTEXT cross-cutting constraint — must not change).
2. Read `mobile/eas.json` and confirm the `build.preview` profile exists with `distribution: 'internal'` and `android.buildType: 'apk'`. If missing, STOP and write `.planning/phases/08-mobile-redesign/08-BLOCKERS.md`. (Verified by planner: profile exists at `mobile/eas.json` lines 20-32.)
3. Run `cd mobile && npx eas whoami` to confirm the executor is logged in to EAS. If not logged in, this becomes a checkpoint — but if logged in, proceed.

Output a short audit log:

  versionCode (current): N
  bundle/package: ng.gov.ogun.iseyaa  ✓
  eas.json preview profile: ✓
  eas whoami: <expo-username></action>
  <verify>
    <automated>node -e "const a=require('./mobile/app.json'); const e=require('./mobile/eas.json'); if(a.expo.android.package!=='ng.gov.ogun.iseyaa') process.exit(1); if(!e.build.preview) process.exit(2); if(e.build.preview.android.buildType!=='apk') process.exit(3); console.log('OK versionCode:', a.expo.android.versionCode);"</automated>
  </verify>
  <done>Pre-flight passes. Current versionCode logged. eas.json preview profile confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Bump android.versionCode by +1 in mobile/app.json (compare against working tree, not HEAD)</name>
  <files>mobile/app.json</files>
  <action>Increment `expo.android.versionCode` by exactly 1. Do NOT change `expo.version` (semantic user-facing version), `expo.ios.buildNumber`, `expo.ios.bundleIdentifier`, or `expo.android.package`. Per CONTEXT.md discretion call: bumping manually here (rather than via an EAS hook) keeps the diff visible in git history. iOS build is not in scope this phase (CONTEXT `<deferred>` — Android-only preview build).

**Verification at this stage compares against `git show HEAD:mobile/app.json`** because the bump has NOT yet been committed. After Task 3 commits the bump, that comparison would show no diff; the verification baseline must be captured BEFORE commit.</action>
  <verify>
    <automated>node -e "const a=require('./mobile/app.json'); const orig=JSON.parse(require('child_process').execSync('git show HEAD:mobile/app.json').toString()); if(a.expo.android.versionCode !== orig.expo.android.versionCode + 1) {console.error('Expected',orig.expo.android.versionCode+1,'got',a.expo.android.versionCode); process.exit(1);} if(a.expo.android.package!==orig.expo.android.package) process.exit(2); if(a.expo.ios.buildNumber!==orig.expo.ios.buildNumber) process.exit(3); console.log('OK bumped to', a.expo.android.versionCode);"</automated>
  </verify>
  <done>`mobile/app.json` `android.versionCode` is exactly one higher than the git-HEAD value. No other fields changed. Working tree is dirty (the commit happens in Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: Commit the versionCode bump BEFORE running the build</name>
  <files>mobile/app.json</files>
  <action>Stage `mobile/app.json` and commit with a clear message. This commit MUST happen before the build is kicked off so the cloud build receives the bumped versionCode. It also makes Task 4's post-commit verification possible (per L-2 reorder).

  git add mobile/app.json
  git commit -m "chore(mobile): bump android.versionCode for Phase 8 preview build"

Do NOT push (the orchestrator handles pushes). Just commit locally.</action>
  <verify>
    <automated>bash -c 'git log -1 --pretty=%s | grep -q "versionCode" && echo OK || (echo "Last commit subject must reference versionCode"; git log -1 --pretty=%s; exit 1)'</automated>
  </verify>
  <done>The HEAD commit contains the versionCode bump. `git status mobile/app.json` shows clean working tree for that file.</done>
</task>

<task type="auto">
  <name>Task 4: Verify the commit landed (post-commit sanity)</name>
  <files>(read-only verification)</files>
  <action>Now that Task 3 committed the bump, run a post-commit sanity check: compare HEAD's `mobile/app.json` versionCode against HEAD~1's. They should differ by exactly +1.

  node -e "
    const cp=require('child_process');
    const head=JSON.parse(cp.execSync('git show HEAD:mobile/app.json').toString());
    const prev=JSON.parse(cp.execSync('git show HEAD~1:mobile/app.json').toString());
    if(head.expo.android.versionCode !== prev.expo.android.versionCode + 1) {
      console.error('Expected', prev.expo.android.versionCode + 1, 'got', head.expo.android.versionCode);
      process.exit(1);
    }
    console.log('OK HEAD versionCode:', head.expo.android.versionCode, 'prev:', prev.expo.android.versionCode);
  "</action>
  <verify>
    <automated>node -e "const cp=require('child_process'); const head=JSON.parse(cp.execSync('git show HEAD:mobile/app.json').toString()); const prev=JSON.parse(cp.execSync('git show HEAD~1:mobile/app.json').toString()); if(head.expo.android.versionCode !== prev.expo.android.versionCode + 1) {process.exit(1);} console.log('OK');"</automated>
  </verify>
  <done>HEAD's versionCode is exactly +1 vs HEAD~1's. Ready to submit the build.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>versionCode bumped, committed, post-commit verified. Executor is ready to submit the build.</what-built>
  <how-to-verify>
    Run from the `mobile/` directory:

      cd mobile
      npx eas build --platform android --profile preview --non-interactive

    The command will print a build URL like `https://expo.dev/accounts/.../projects/iseyaa/builds/<uuid>`. The build runs on EAS cloud servers and takes typically 12-20 minutes.

    While the build is queued/running, the human operator should:
    1. Open the build URL in a browser and watch for progress.
    2. When status flips to "finished", copy the resulting APK install URL (under "Install" tab).
    3. Paste the URL into `.planning/phases/08-mobile-redesign/08-VERIFICATION.md` under a new heading `## EAS Preview Build`.
    4. Install the APK on a real Android device and smoke-check that the app launches without a crash.

    Why this is a checkpoint and not pure automation: while `eas build --non-interactive` itself runs autonomously, the install-and-smoke-check step requires a physical device that only the human operator has access to. The CI cannot perform the install.
  </how-to-verify>
  <resume-signal>Once the APK is installed and the app launches: reply "build URL: <url>, app launches: yes" (or list any crashes).</resume-signal>
</task>

</tasks>

<verification>
- `mobile/app.json android.versionCode` is exactly `previous + 1`.
- HEAD commit references "versionCode" in its message.
- HEAD's app.json versionCode = HEAD~1's versionCode + 1.
- EAS build status URL is recorded in `08-VERIFICATION.md`.
- APK install URL is recorded in `08-VERIFICATION.md`.
- App launches on a real Android device without a crash on the first screen.
</verification>

<success_criteria>
- MOB-RD-07 closed: fresh Android EAS preview build completes successfully and the install URL is captured.
- No iOS build attempted (deferred per CONTEXT).
- All other constraints unchanged (bundle ID `ng.gov.ogun.iseyaa`, hermes engine, sentry init).
- VersionCode bump committed BEFORE build submission (L-2 fix).
</success_criteria>

<output>
Create `.planning/phases/08-mobile-redesign/08-09-SUMMARY.md` with: previous + new versionCode, commit SHA of the bump, eas build command output, build URL, install URL, device used for smoke test, and result.
</output>
