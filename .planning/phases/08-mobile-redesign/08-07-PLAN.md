---
phase: 08-mobile-redesign
plan: 07
type: execute
wave: 3
depends_on: [08-01, 08-02, 08-04, 08-04b]
files_modified:
  - mobile/app/host.tsx
  - mobile/app/(tabs)/profile.tsx
autonomous: true
requirements: [MOB-RD-06]
must_haves:
  truths:
    - "A new screen at /host renders hero (using CARD_GRADIENTS.goldHero) + 3 benefit cards + Q&A accordion + gold CTA"
    - "The You tab profile shows a 'Become a host' card unless user.registeredRoles already includes 'HOST'"
    - "Tapping the card navigates to /host; tapping the CTA fires POST /api/v1/users/me/become-host and refetches /users/me"
    - "Routes are already registered by 08-04b — this plan does NOT touch mobile/app/_layout.tsx (H-4 disjoint ownership)"
  artifacts:
    - path: "mobile/app/host.tsx"
      provides: "Host onboarding screen with CARD_GRADIENTS.goldHero hero"
      contains: "/users/me/become-host"
    - path: "mobile/app/(tabs)/profile.tsx"
      provides: "Become-a-host CTA card wiring"
      contains: "/host"
  key_links:
    - from: "mobile/app/host.tsx"
      to: "POST /api/v1/users/me/become-host"
      via: "useMutation"
      pattern: "/users/me/become-host"
    - from: "mobile/app/(tabs)/profile.tsx"
      to: "mobile/app/host.tsx"
      via: "router.push('/host')"
      pattern: "router.push.*host"
    - from: "mobile/app/host.tsx"
      to: "mobile/lib/tokens.ts"
      via: "import { CARD_GRADIENTS } and use CARD_GRADIENTS.goldHero"
      pattern: "CARD_GRADIENTS.goldHero"
---

<objective>
Build the mobile equivalent of `web/src/app/host/page.tsx`: a host onboarding screen reachable from the You tab profile. Closes MOB-RD-06 (Host onboarding screen + `POST /api/v1/users/me/become-host` wiring).

**Out of scope (H-4 disjoint ownership):** This plan does NOT touch `mobile/app/_layout.tsx`. The `host` Stack route is already registered by 08-04b (Wave 2) as `presentation: 'card'`. Wave 3 plans run truly parallel.

Output: One new screen file (`mobile/app/host.tsx`) and one small additive edit to `(tabs)/profile.tsx` that inserts a "Become a host" card linking to `/host` (conditional on user not already being a host).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-mobile-redesign/08-CONTEXT.md
@.planning/mobile-redesign-UI-SPEC.md
@CLAUDE.md
@mobile/lib/tokens.ts
@mobile/lib/api.ts
@mobile/components/ui/PressableScale.tsx
@mobile/components/ui/Chip.tsx
@mobile/app/(tabs)/profile.tsx
@web/src/app/host/page.tsx

<interfaces>
<!-- Backend response (from backend/src/modules/users/users.controller.ts) -->

GET /users/me response (relevant fields only):

  type Me = {
    id: string;
    name?: string;
    phone?: string;
    email?: string;
    registeredRoles: string[];   // e.g. ["CITIZEN", "HOST"] — check includes "HOST"
    isHost?: boolean;             // older alias; check both for safety
    // ...
  };

POST /users/me/become-host — no body, returns updated Me.

Token import (from mobile/lib/tokens.ts after 08-01 Task 2):
  import { CARD_GRADIENTS } from '../lib/tokens';
  // CARD_GRADIENTS.goldHero === ['#3a2e15', '#6a4a14', '#C8962A', '#4a3208']  (4-stop)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mobile/app/host.tsx (use CARD_GRADIENTS.goldHero for hero gradient)</name>
  <files>mobile/app/host.tsx</files>
  <action>Create the host onboarding screen mirroring `web/src/app/host/page.tsx` adapted for mobile. The Stack route is already registered by 08-04b — no `_layout.tsx` edit needed here. Structure:

1. **Hero section** (full-bleed top): gold-gradient background using `expo-linear-gradient` with colors from `CARD_GRADIENTS.goldHero` (4-stop tuple from `mobile/lib/tokens.ts`, added by 08-01 Task 2 — per M-5 fix, NO inline hex stops). Pass `colors={CARD_GRADIENTS.goldHero}` to the LinearGradient. Add a dark scrim overlay (`rgba(0,0,0,0.4)`). Inside: a small gold-tinted pill "Hosting on Iṣẹ́yáá" with `Sparkles` lucide icon, then a CREAM display heading "Become an Iṣẹ́yáá Host" (use `TYPE.display` from tokens, color `CREAM`, with the word "Iṣẹ́yáá" in gold), then subtitle "Earn from your space, club, beach, or experience. Reach 7M+ citizens across 20 LGAs." in `INK_SECONDARY`. Vertical padding 64px top, 48px bottom. Back chevron in top-left for navigation.

2. **Benefits section**: heading "Built for serious operators" + 3 vertically-stacked cards. Card content (matches web `BENEFITS` array):
   - `{ icon: Wallet, title: 'Earn money', desc: 'Set your price, accept bookings, get paid via wallet within 24h of guest checkout.' }`
   - `{ icon: Users, title: 'Reach the right guests', desc: 'Verified Ogun citizens, tourists, and members find your listing through curated categories.' }`
   - `{ icon: ShieldCheck, title: 'We handle payments', desc: 'Paystack-integrated escrow, automatic platform fee, full ledger you can export.' }`

   Each card: `SURFACE_RAISED` bg, `BORDER_SUBTLE` border, 24px padding, 20px borderRadius, icon box (44×44, `FOREST_DIM` bg, `FOREST` border) with gold lucide icon, title in Body emphasis, desc in Body `INK_SECONDARY`.

3. **What you can host** section: heading "What you can host" + a wrapped flex of 7 Chips (using `Chip` from `mobile/components/ui/Chip`) with labels: Stay, Lounge, Club, Beach, Tour, Experience, Social Club. Each chip has a `Check` icon prefix.

4. **Q&A section**: heading "Quick answers" + 3 collapsible accordion items. Question text in Body emphasis with a ChevronDown icon (rotates 180° when open). Answer text below in Body `INK_SECONDARY`, hidden until tapped. Use simple `useState<number | null>(null)` for the open index. Content from web `FAQS`:
   - `{ q: 'How does payout work?', a: 'Funds are held in Paystack escrow until 24 hours after guest checkout, then automatically credited to your in-app wallet. From there, withdraw to any Nigerian bank account.' }`
   - `{ q: 'How much do I keep?', a: '85% — we charge a 15% platform fee on the first 100 transactions, then 12% thereafter.' }`
   - `{ q: 'What about social clubs?', a: 'We process your member dues monthly and remit to your account. Members get auto-renewing access via wallet auto-debit.' }`

5. **Sticky CTA footer**: fetch `useQuery({ queryKey: ['me'], queryFn: () => fetcher('/users/me'), staleTime: 30_000 })`. Compute `alreadyHost = (me?.registeredRoles ?? []).includes('HOST') || me?.isHost === true`. If `alreadyHost`, show "You're already a host" + a secondary "Go to dashboard" outline button (no destination for now — just `Alert.alert('Coming soon')` or pop back). Otherwise show primary gold button "I want to host" (UI-SPEC §5 fill). On press: `useMutation({ mutationFn: () => api.post('/users/me/become-host').then(r => r.data), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ['me'] }); Alert.alert('Welcome aboard!', 'You can now list your space.'); router.replace('/(tabs)/profile'); }, onError: (err) => Alert.alert('Error', err?.response?.data?.message ?? 'Try again.') })`. Disable button + show ActivityIndicator while pending.

All colors from tokens — NO inline hex stops in the hero gradient (M-5: use `CARD_GRADIENTS.goldHero`). Touch targets ≥ 44pt.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('mobile/app/host.tsx','utf8'); if(!c.includes('/users/me/become-host')) process.exit(1); if(!c.includes('registeredRoles')) process.exit(2); if(!c.includes('expo-linear-gradient')) process.exit(3); if(!c.includes('CARD_GRADIENTS.goldHero')) {console.error('Must use CARD_GRADIENTS.goldHero (M-5)');process.exit(4);} if(c.includes(\"'#3a2e15'\")||c.includes(\"'#6a4a14'\")||c.includes(\"'#C8962A'\")||c.includes(\"'#4a3208'\")) {console.error('Forbidden inline hex stops — must reference CARD_GRADIENTS.goldHero');process.exit(5);} const faqs=(c.match(/How does payout|How much do I keep|What about social clubs/g)||[]).length; if(faqs<3) process.exit(6); console.log('OK faqs:',faqs);"</automated>
  </verify>
  <done>`host.tsx` exists, hero uses `CARD_GRADIENTS.goldHero` (no inline 4-stop hex), renders 3 benefit cards + 7 hostable chips + 3 Q&A accordion items + sticky CTA, posts to `/users/me/become-host`, handles both already-host and not-yet-host states. `tsc --noEmit` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Insert Become-a-host card in profile.tsx</name>
  <files>mobile/app/(tabs)/profile.tsx</files>
  <action>**Important:** This plan does NOT edit `mobile/app/_layout.tsx`. The `host` route is registered by 08-04b. Only `(tabs)/profile.tsx` is touched here.

Read the current `mobile/app/(tabs)/profile.tsx` to find the section where action cards (Driver Mode, Rider Mode, KYC, etc.) are listed. Add a new card "Become a host" using the existing card layout pattern (match the visual style of the Driver/Rider cards). Card props: icon `Home` from lucide-react-native, title "Become a host", subtitle "List your stay, club, or experience". On press: `router.push('/host')`. Conditional render: fetch `useQuery({ queryKey: ['me'], queryFn: () => fetcher('/users/me') })` (or reuse existing me-query if already present in this file — read the file and check), compute `alreadyHost = (me?.registeredRoles ?? []).includes('HOST') || me?.isHost === true`, and only render the card when `!alreadyHost`. If `alreadyHost`, optionally render a "Host dashboard" card in its place — but if simpler, just hide the card entirely. Do NOT change any other section of profile.tsx — keep the diff small (≤ 30 lines added).</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p=fs.readFileSync('mobile/app/(tabs)/profile.tsx','utf8'); if(!p.includes('/host')) process.exit(1); if(!p.includes('registeredRoles') && !p.includes('isHost')) process.exit(2); console.log('OK');"</automated>
  </verify>
  <done>Profile tab shows a "Become a host" card when user is not yet a host, hides it when they are. Tapping it routes to `/host`. `tsc --noEmit` passes. No `_layout.tsx` edit.</done>
</task>

</tasks>

<verification>
- `cd mobile && npx tsc --noEmit` passes.
- `git diff --stat mobile/app/_layout.tsx` returns no changes (08-04b owns _layout edits).
- Host route resolves: `router.push('/host')` opens the new screen.
- Profile tab shows "Become a host" card only when `me.registeredRoles` does not include "HOST".
- CTA on host screen fires `POST /api/v1/users/me/become-host` (verify with backend log when wired) and on success refetches `me` + navigates back to profile.
- `grep "CARD_GRADIENTS.goldHero" mobile/app/host.tsx` returns >= 1 match; `grep -E "#(3a2e15|6a4a14|C8962A|4a3208)" mobile/app/host.tsx` returns 0 matches.
- All existing 282+ tests still pass.
</verification>

<success_criteria>
- MOB-RD-06 fully closed: hero (using CARD_GRADIENTS.goldHero), 3 benefit cards, Q&A, gold CTA, `POST /api/v1/users/me/become-host` wiring, profile-tab entry point.
- No `_layout.tsx` edits (08-04b owns those).
- No inline hex stops in the hero gradient.
- Reuses the existing `/dashboard`-style follow-up only via toast (no host dashboard screen — that's deferred per CONTEXT.md `<deferred>` §8.2).
</success_criteria>

<output>
Create `.planning/phases/08-mobile-redesign/08-07-SUMMARY.md` with: host screen line count, accordion implementation choice, where in profile.tsx the card was inserted, confirmation that CARD_GRADIENTS.goldHero is used (no inline hex), confirmation that no _layout.tsx edits were made, and any deviations from web parity.
</output>
