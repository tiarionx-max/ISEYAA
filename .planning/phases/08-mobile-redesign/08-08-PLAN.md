---
phase: 08-mobile-redesign
plan: 08
type: execute
wave: 4
depends_on: [08-01, 08-02, 08-04, 08-04b]
files_modified:
  - mobile/app/stays/[id].tsx
  - mobile/components/stays/NightlyBookingSheet.tsx
  - mobile/components/stays/HourlyBookingSheet.tsx
  - mobile/components/stays/TimedEventBookingSheet.tsx
  - mobile/components/stays/MembershipBookingSheet.tsx
autonomous: true
requirements: [MOB-RD-04]
must_haves:
  truths:
    - "Stay detail renders a 4-image gallery (horizontal paged FlatList) using property.galleryImages with coverImageUrl fallback"
    - "Stay detail renders property.highlights as a vertical list with CheckCircle2 icons"
    - "Stay detail renders property.amenities as wrapped Chip pills"
    - "Sticky booking sheet at bottom switches between 4 distinct UIs based on property.bookingMode: NIGHTLY/HOURLY/TIMED_EVENT/MEMBERSHIP"
    - "TIMED_EVENT total uses pricePerHour × slot length (NOT people × pricePerNight) — H-3 + CONTEXT §Stay Detail"
    - "All 4 sheets POST to /api/v1/properties/:id/bookings (NOT /stays/bookings) with { checkIn, checkOut, guests, email } — email REQUIRED, defaulted from session"
    - "On success, opens response Paystack URL via expo-web-browser"
  artifacts:
    - path: "mobile/app/stays/[id].tsx"
      provides: "Stay detail screen — gallery + highlights + amenities + mode-aware sticky sheet"
      contains: "property.bookingMode"
    - path: "mobile/components/stays/NightlyBookingSheet.tsx"
      provides: "Check-in/check-out date pickers + guest count + email field + nights × pricePerNight total"
      exports: ["NightlyBookingSheet"]
    - path: "mobile/components/stays/HourlyBookingSheet.tsx"
      provides: "Date + start time + duration hours + email field + hours × pricePerHour total"
      exports: ["HourlyBookingSheet"]
    - path: "mobile/components/stays/TimedEventBookingSheet.tsx"
      provides: "Date + time slot picker + email field + pricePerHour × slot length total"
      exports: ["TimedEventBookingSheet"]
    - path: "mobile/components/stays/MembershipBookingSheet.tsx"
      provides: "Duration selector (1/3/6/12mo) + email field + months × membershipMonthlyPrice + benefits list"
      exports: ["MembershipBookingSheet"]
  key_links:
    - from: "mobile/app/stays/[id].tsx"
      to: "property.bookingMode"
      via: "switch statement rendering one of 4 booking sheet components"
      pattern: "bookingMode.*NIGHTLY|HOURLY|TIMED_EVENT|MEMBERSHIP"
    - from: "mobile/app/stays/[id].tsx"
      to: "POST /api/v1/properties/:id/bookings"
      via: "useMutation, called from booking-sheet onSubmit prop"
      pattern: "/properties/.*bookings"
    - from: "mobile/app/stays/[id].tsx"
      to: "expo-web-browser"
      via: "WebBrowser.openAuthSessionAsync for Paystack URL"
      pattern: "expo-web-browser"
---

<objective>
Rewrite `mobile/app/stays/[id].tsx` to match the web stay-detail experience: 4-image gallery, highlights with check icons, amenity chips, and a sticky booking sheet at the bottom whose UI switches on `property.bookingMode`. The 4 booking-sheet variants are extracted into separate components per CONTEXT.md `<decisions>` ("4 components — better testability") so each variant can be reasoned about independently.

Closes MOB-RD-04 (success criterion #4: 4-image gallery + highlights + amenity chips + 4-mode booking sheet matching `web/src/app/stays/[id]/page.tsx`).

**H-3 endpoint correction:** The booking endpoint is `POST /api/v1/properties/:id/bookings` (verified in `backend/src/modules/stays/stays.controller.ts:94`). NOT `/stays/bookings` (that path does not exist; handoff doc was wrong). All 4 sheets POST to this path with `{ checkIn, checkOut, guests, email }` matching `CreateBookingDto` (`backend/src/modules/stays/dto/create-booking.dto.ts`). The `email` field is REQUIRED (`@IsEmail()`) — defaults from `user.email` from session; if user is not logged in, fail gracefully via a "Sign in to book" button that routes to `/auth/phone`.

**H-3 TIMED_EVENT formula correction:** Per CONTEXT §Stay Detail, TIMED_EVENT total uses `pricePerHour × slot length` (or flat `pricePerNight` fallback if no slot data). NOT `people × pricePerNight` (the iteration-1 plan had the formula wrong).

**M-7 date picker decision:** Use `@react-native-community/datetimepicker` (installed by 08-01 Task 1). Do NOT ship a TextInput-with-parse fallback — the dep is small, well-supported, and makes the booking sheets actually usable.

**H-4 disjoint ownership:** This plan does NOT touch `mobile/app/_layout.tsx`. The `stays/[id]` route was already registered before this phase.

Output: 1 screen rewrite + 4 booking-sheet components. All four sheets share the same `onSubmit` contract so the screen treats them uniformly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-mobile-redesign/08-CONTEXT.md
@.planning/mobile-redesign-UI-SPEC.md
@CLAUDE.md
@mobile/lib/tokens.ts
@mobile/lib/api.ts
@mobile/components/ui/Chip.tsx
@mobile/components/ui/PressableScale.tsx
@mobile/app/stays/[id].tsx
@web/src/app/stays/[id]/page.tsx
@backend/src/modules/stays/stays.controller.ts
@backend/src/modules/stays/dto/create-booking.dto.ts

<interfaces>
<!-- Booking-sheet contract (all 4 sheets implement this same prop shape) -->

  export type BookingArgs = {
    checkIn: string;          // ISO date string (YYYY-MM-DD) or full ISO timestamp
    checkOut: string;         // ISO date string (YYYY-MM-DD) or full ISO timestamp
    guests: number;           // class-validator: @IsInt @Min(1)
    email: string;            // class-validator: @IsEmail — REQUIRED (CreateBookingDto)
  };

  export type BookingSheetProps = {
    property: Property;
    pending: boolean;
    signedIn: boolean;
    defaultEmail?: string | null;   // me?.email from session — sheet pre-fills the email TextInput
    onSubmit: (args: BookingArgs) => void;
  };

POST /api/v1/properties/:id/bookings request (CreateBookingDto — verified backend):

  type CreateBookingRequest = {
    checkIn: string;     // @IsDateString
    checkOut: string;    // @IsDateString
    guests: number;      // @IsInt @Min(1)
    email: string;       // @IsEmail — REQUIRED
  };

POST /api/v1/properties/:id/bookings response (mirror existing stay-booking handoff pattern):

  type BookingResponse = {
    bookingId: string;
    reference: string;
    authorizationUrl: string;  // Paystack hosted page URL
    // Or possibly nested under `payment:` — read defensively like checkout (see 08-06):
    // const url = resp.payment?.authorizationUrl ?? resp.authorizationUrl;
  };

Property shape (subset relevant here):

  type Property = {
    id: string;
    name: string;
    bookingMode: 'NIGHTLY' | 'HOURLY' | 'TIMED_EVENT' | 'MEMBERSHIP';
    pricePerNight?: number | null;
    pricePerHour?: number | null;
    membershipMonthlyPrice?: number | null;
    membershipBenefits?: string[];
    galleryImages?: string[];
    coverImageUrl?: string | null;
    highlights?: string[];
    amenities?: string[];
    maxGuests?: number;
    city?: string;
    lga?: { name: string };
  };

Date picker (installed by 08-01 Task 1):
  import DateTimePicker from '@react-native-community/datetimepicker';
  // Standard usage: <DateTimePicker value={date} mode="date" onChange={(_, d) => d && setDate(d)} />
  // Time mode: mode="time"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the 4 booking-sheet components (all 4 collect email, all 4 share POST payload shape)</name>
  <files>mobile/components/stays/NightlyBookingSheet.tsx, mobile/components/stays/HourlyBookingSheet.tsx, mobile/components/stays/TimedEventBookingSheet.tsx, mobile/components/stays/MembershipBookingSheet.tsx</files>
  <action>Create the `mobile/components/stays/` directory and four sheet components. Each is a self-contained component that owns its own form state (dates, hours, slot, duration, etc.) and calls the `onSubmit(args)` prop on confirm. All four sheets share the SAME props shape from the `<interfaces>` block above. All four sheets MUST include an email TextInput (UI-SPEC §8 styles, `keyboardType="email-address"`, `autoCapitalize="none"`, label "Confirmation email") that defaults to `defaultEmail` on mount (if provided) and is REQUIRED before submit. If the email field is empty or fails a simple `/^[^\s@]+@[^\s@]+\.[^\s@]+$/` test, show an inline error caption and disable submit.

What DIFFERS across the 4 sheets is how `checkIn` / `checkOut` / `guests` are derived from the mode-specific UI. The POST payload shape is IDENTICAL across all 4: `{ checkIn, checkOut, guests, email }`.

Use `@react-native-community/datetimepicker` (installed by 08-01) for ALL date and time inputs. NO TextInput-parse fallback (M-7).

**NightlyBookingSheet** — mirrors web `NightlyForm` (web stays/[id]/page.tsx around line 130 — verify line range still accurate when executing). Inputs: check-in date (DateTimePicker mode="date"), check-out date (DateTimePicker mode="date"), guests stepper (1..maxGuests), email TextInput. Compute `nights = Math.max(1, Math.round((checkOut.getTime() - checkIn.getTime()) / 86_400_000))` and `total = nights * pricePerNight`. Display "Total" row at bottom in gold. Primary "Reserve" button calls `onSubmit({ checkIn: checkIn.toISOString().slice(0,10), checkOut: checkOut.toISOString().slice(0,10), guests, email })`. If `!signedIn`, render a single "Sign in to book" button that pushes to `/auth/phone` instead.

**HourlyBookingSheet** — mirrors web `HourlyForm` (around line 200 — verify). Inputs: date (DateTimePicker mode="date"), start time (DateTimePicker mode="time"), duration hours (1..12 stepper), guests (1..maxGuests), email. Compute `total = hours * (pricePerHour ?? pricePerNight ?? 0)`. On submit, build `start = new Date(date + 'T' + time + ':00')`, `end = new Date(start.getTime() + hours * 3_600_000)`. Pass `{ checkIn: start.toISOString(), checkOut: end.toISOString(), guests, email }`. Button label "Reserve table".

**TimedEventBookingSheet** — mirrors web `TimedEventForm` (around line 298 — verify). Inputs: date (DateTimePicker mode="date"), time-slot picker (a row of Chip components for the property's available slots — if no slot data, default to a 09:00-12:00 window selectable as a single slot), guests count (1..maxGuests, default 2), email.

  **TIMED_EVENT formula (H-3 — per CONTEXT §Stay Detail):**
  `slotLengthHours = (slotEnd - slotStart) / 3_600_000`  (the picked slot's duration in hours)
  `total = pricePerHour × slotLengthHours`
  Fallback: if `pricePerHour` is null/undefined, use flat `pricePerNight` as the total (single value, no multiplier).

  Do NOT use `people × pricePerNight` (that was the iteration-1 mistake — the formula is per-slot, not per-person).

  On submit, build `start` and `end` ISO timestamps from the date + picked slot, pass `{ checkIn: start.toISOString(), checkOut: end.toISOString(), guests, email }`. Button label "Book experience".

**MembershipBookingSheet** — mirrors web `MembershipForm` (around line 373 — verify). No date inputs from the user; render `membershipBenefits[]` as a bullet list with `Check` icons. Duration selector: 4 chips (1mo / 3mo / 6mo / 12mo), default `1`. Guests stepper (1, since memberships are individual; can default to 1 and hide UI if `maxGuests === 1`). Email TextInput.

  Compute `total = months * (membershipMonthlyPrice ?? pricePerNight ?? 0)`.

  On submit, build `start = new Date()`, `end = new Date(start.getTime() + months * 30 * 86_400_000)`, pass `{ checkIn: start.toISOString(), checkOut: end.toISOString(), guests: 1, email }`. Button label "Become a member". Footer caption "Cancel anytime · Charges renew monthly via wallet auto-debit" in `INK_FAINT`.

Shared visual contract for all four: rounded top corners (24px), `SURFACE_RAISED` bg, `BORDER_GLASS` top border, inner padding 24px, fields use UI-SPEC §8 Text Input styles, totals row uses gold tabular-nums for the amount. All colors from `mobile/lib/tokens.ts`. Use `Chip` from `mobile/components/ui/Chip` for the membership duration selector AND the TIMED_EVENT slot picker. No inline hex.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['mobile/components/stays/NightlyBookingSheet.tsx','mobile/components/stays/HourlyBookingSheet.tsx','mobile/components/stays/TimedEventBookingSheet.tsx','mobile/components/stays/MembershipBookingSheet.tsx']; for(const f of files){const c=fs.readFileSync(f,'utf8'); if(!c.includes('onSubmit')) {console.error('Missing onSubmit in',f);process.exit(1);} if(!c.includes('email')) {console.error('Missing email field in',f);process.exit(2);} if(!c.includes('@react-native-community/datetimepicker') && !c.includes('DateTimePicker')) {if(f.includes('Membership')) {/* membership has no date picker — OK */} else {console.error('Missing datetimepicker in',f);process.exit(3);}}} const te=fs.readFileSync('mobile/components/stays/TimedEventBookingSheet.tsx','utf8'); if(te.includes('people * pricePerNight')||te.includes('people*pricePerNight')) {console.error('TIMED_EVENT formula must NOT be people × pricePerNight (H-3)');process.exit(4);} if(!te.includes('pricePerHour')) {console.error('TIMED_EVENT must use pricePerHour × slot length (H-3)');process.exit(5);} console.log('OK 4 sheets');"</automated>
  </verify>
  <done>All four files exist, each exports its named component with the `BookingSheetProps` contract (including `defaultEmail` and `email` in BookingArgs). All sheets collect an email field. Non-Membership sheets use `@react-native-community/datetimepicker`. TIMED_EVENT formula is `pricePerHour × slot length` (NOT people × pricePerNight). `tsc --noEmit` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite mobile/app/stays/[id].tsx with gallery + highlights + amenities + mode-aware sheet — POST to /properties/:id/bookings</name>
  <files>mobile/app/stays/[id].tsx</files>
  <action>Rewrite `mobile/app/stays/[id].tsx`. Layout:

1. **Top: 4-image gallery** — horizontal paged `FlatList` with `pagingEnabled`, dots indicator at the bottom (small white circles, active one gold). Images source: `property.galleryImages?.length ? property.galleryImages.slice(0, 4) : Array(4).fill(property.coverImageUrl ?? null)`. When the source is null, render a `CARD_COLORS[0]` LinearGradient as fallback. Use `expo-image` with `contentFit="cover"`, aspect ratio 4:3. Overlay top-left: back chevron (`ChevronLeft`) with `rgba(0,0,0,0.4)` round bg, calls `router.back()`. Overlay top-right: share button (`Share2`) + wishlist heart (`Heart`).

2. **Main content** (scrollable, padded 16px horizontal):
   - Property name (TYPE.heading, ink).
   - Location row: `MapPin` icon + `{property.city ?? property.lga?.name ?? 'Ogun State'}` in `INK_SECONDARY`.
   - Rating row (if `averageRating`): star icon + rating in gold.
   - Divider (1px `BORDER_SUBTLE`).
   - **Highlights** (if `property.highlights?.length`): heading "Highlights" + vertical list, each row = `CheckCircle2` lucide icon (size 18, color `FOREST_LIGHT`) + highlight text (Body, ink). 16px gap between rows.
   - Divider.
   - **Amenities** (if `property.amenities?.length`): heading "Amenities" + wrapped flex of `Chip` components, each chip showing the amenity name. No icons on amenity chips (raw text is fine).
   - Divider.
   - **Description** (if `property.description`): Body text in `INK_SECONDARY`.
   - Spacer at bottom (140px) so the sticky sheet doesn't cover the last line of content.

3. **Fetch session + property:**
   - `const { data: property } = useQuery({ queryKey: ['property', id], queryFn: () => fetcher('/properties/' + id) });`
   - `const { data: me } = useQuery({ queryKey: ['me'], queryFn: () => fetcher('/users/me'), staleTime: 60_000 });`
   - `const accessToken` from `SecureStore.getItemAsync('access_token')` cached in a `useEffect` (matches existing pattern).
   - `const signedIn = !!accessToken;`
   - `const defaultEmail = me?.email ?? null;`

4. **Sticky booking sheet** (absolute, bottom: 0, left: 0, right: 0, insetBottom from useSafeAreaInsets): switch on `property.bookingMode`:

  switch (property.bookingMode) {
    case 'HOURLY':       return <HourlyBookingSheet {...sheetProps} />;
    case 'TIMED_EVENT':  return <TimedEventBookingSheet {...sheetProps} />;
    case 'MEMBERSHIP':   return <MembershipBookingSheet {...sheetProps} />;
    case 'NIGHTLY':
    default:             return <NightlyBookingSheet {...sheetProps} />;
  }

  `sheetProps`: `{ property, pending: bookingMutation.isPending, signedIn, defaultEmail, onSubmit: handleSubmit }`.

5. **handleSubmit** — `useMutation({ mutationFn: (args: BookingArgs) => api.post('/properties/' + property.id + '/bookings', args).then(r => r.data), onSuccess: async (resp) => { const url = resp.payment?.authorizationUrl ?? resp.authorizationUrl; if (!url) { Alert.alert('Booking created', 'No payment URL returned.'); return; } await WebBrowser.openAuthSessionAsync(url, 'iseyaa://booking-callback'); /* refresh availability after browser closes */ }, onError: (err) => Alert.alert('Booking failed', err?.response?.data?.message ?? 'Try again.') })`.

   The endpoint is `/properties/:id/bookings` (H-3 — verified backend). NOT `/stays/bookings`.

All colors from tokens. Touch targets ≥ 44pt. Use `expo-image` for ALL photos. Use `useSafeAreaInsets` to inset the bottom of the sheet by `insets.bottom`.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('mobile/app/stays/[id].tsx','utf8'); const modes=['NIGHTLY','HOURLY','TIMED_EVENT','MEMBERSHIP']; for(const m of modes) if(!c.includes(m)) {console.error('Missing mode',m);process.exit(1);} const sheets=['NightlyBookingSheet','HourlyBookingSheet','TimedEventBookingSheet','MembershipBookingSheet']; for(const s of sheets) if(!c.includes(s)) {console.error('Missing import',s);process.exit(2);} if(!c.includes('/properties/') || !c.includes('/bookings')) {console.error('Endpoint must be /properties/:id/bookings (H-3)');process.exit(3);} if(c.includes('/stays/bookings')) {console.error('Stale endpoint /stays/bookings — must be /properties/:id/bookings');process.exit(4);} if(!c.includes('expo-web-browser')) process.exit(5); if(!c.includes('defaultEmail')) {console.error('Must pass defaultEmail prop to sheets');process.exit(6);} console.log('OK');"</automated>
  </verify>
  <done>Stay detail renders 4-image gallery, highlights, amenity chips, and a sticky booking sheet whose UI switches across all 4 modes. Each sheet receives `defaultEmail` from session. Confirm action posts to `/properties/:id/bookings` (NOT `/stays/bookings`) and opens Paystack via `expo-web-browser`. `tsc --noEmit` passes.</done>
</task>

</tasks>

<verification>
- `cd mobile && npx tsc --noEmit` passes.
- `grep "/properties/.*bookings" mobile/app/stays/[id].tsx` returns >= 1 match; `grep "/stays/bookings" mobile/app/stays/[id].tsx` returns 0.
- `grep "email" mobile/components/stays/*.tsx | wc -l` shows email fields across all 4 sheets.
- TimedEventBookingSheet does NOT contain `people * pricePerNight` and DOES contain `pricePerHour`.
- Opening a NIGHTLY property shows date + guests + email + nights × price total.
- Opening a HOURLY property shows date + start time + hours + email + total.
- Opening a TIMED_EVENT property shows date + slot picker + email + pricePerHour×slot total.
- Opening a MEMBERSHIP property shows benefits list + duration chips + email + monthly total.
- Tapping Confirm posts to `/properties/:id/bookings` with `{ checkIn, checkOut, guests, email }` and opens Paystack URL.
- `git diff --stat mobile/app/_layout.tsx` returns no changes.
- All existing 282+ tests still pass.
- No inline hex strings outside the small acceptable overlay color (`rgba(0,0,0,0.4)`).
</verification>

<success_criteria>
- MOB-RD-04 fully closed: 4-image gallery, highlights with check icons, amenity chips, sticky booking sheet with 4 distinct UIs matching `web/src/app/stays/[id]/page.tsx`.
- Booking sheets are 4 separate components (testable independently per CONTEXT.md discretion call).
- All 4 sheets POST to `POST /api/v1/properties/:id/bookings` (correct endpoint per H-3) with `{ checkIn, checkOut, guests, email }` (email REQUIRED per backend DTO).
- TIMED_EVENT total is `pricePerHour × slot length` (per CONTEXT, NOT `people × pricePerNight`).
- Date/time inputs use `@react-native-community/datetimepicker` (no TextInput parse fallback).
- Email defaults from session `me.email`.
- Paystack handoff via `expo-web-browser` — no backend changes.
</success_criteria>

<output>
Create `.planning/phases/08-mobile-redesign/08-08-SUMMARY.md` with: list of the 4 sheet components + line counts, decision on the date picker library (`@react-native-community/datetimepicker`), confirmation that the endpoint is `/properties/:id/bookings` (NOT `/stays/bookings`), TIMED_EVENT formula used (`pricePerHour × slot length`), email-default-from-session pattern used, and any deviations from web parity.
</output>
