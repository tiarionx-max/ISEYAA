---
phase: 08-mobile-redesign
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - mobile/app/(tabs)/_layout.tsx
  - mobile/app/(tabs)/events.tsx
  - mobile/app/(tabs)/stays.tsx
  - mobile/app/(tabs)/studio.tsx
  - mobile/app/(tabs)/transport.tsx
  - mobile/app/(tabs)/delivery.tsx
  - mobile/app/(tabs)/driver.tsx
  - mobile/app/(tabs)/rider.tsx
autonomous: true
requirements: [MOB-RD-01]
must_haves:
  truths:
    - "mobile/app/(tabs)/ contains exactly 5 .tsx files: index, book, wallet, concierge, profile (plus _layout.tsx)"
    - "_layout.tsx registers exactly 5 Tabs.Screen entries — no hidden legacy tab declarations remain"
    - "Functionality previously reached via legacy tabs is still reachable via modal/stack routes (transport-flow, delivery-flow, driver-dashboard, rider-dashboard) or via Book hub (events, stays, studio handled in Wave 3) — note: events sub-section migration is owned by 08-05 (H-1)"
  artifacts:
    - path: "mobile/app/(tabs)/_layout.tsx"
      provides: "5-tab registration only — no hidden legacy entries"
      contains: "Tabs.Screen"
  key_links:
    - from: "mobile/app/(tabs)/_layout.tsx"
      to: "expo-router Tabs"
      via: "Tabs.Screen registrations"
      pattern: "Tabs.Screen.*name"
---

<objective>
Delete the 7 legacy tab files and strip their hidden `<Tabs.Screen href={null}>` registrations from `_layout.tsx`. After this plan, `mobile/app/(tabs)/` contains exactly 6 files: `_layout.tsx` + the 5 visible tab screens (`index.tsx`, `book.tsx`, `wallet.tsx`, `concierge.tsx`, `profile.tsx`). Closes MOB-RD-01 success criterion #1.

**Important:** before deleting `events.tsx`, plan 08-05 (Wave 3) MUST migrate the EventsFeed component + QR scan-in FAB out of it (or out of the current book.tsx) into a `mobile/components/book/EventsSubsection.tsx`. The destination migration is owned by 08-05 (per H-1); this plan only removes the old tab file. Wave 2 plans run in parallel — 08-04 deletes the file, 08-05 (Wave 3) re-creates the functionality in the Book hub.

Purpose: Today `_layout.tsx` has 5 visible + 7 hidden Tabs.Screen entries. The hidden ones still register the file as a tab route, which (a) bloats the bundle, (b) prevents `expo-router` from cleanly resolving the 5-tab structure, and (c) makes "is the migration done?" impossible to assert. Every destination for legacy tab functionality already exists as a modal/stack screen (per CONTEXT.md `<decisions>`): `transport-flow`, `delivery-flow`, `driver-dashboard`, `rider-dashboard` are present in `mobile/app/` and registered in the root `Stack`. Events/Stays/Studio sub-sections live inside `book.tsx` (08-05).

Output: 7 files deleted, 1 file (`_layout.tsx`) trimmed to ~95 lines (down from 165).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-mobile-redesign/08-CONTEXT.md
@mobile/app/(tabs)/_layout.tsx
@mobile/app/_layout.tsx
@mobile/app/(tabs)/events.tsx
@mobile/app/(tabs)/book.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit cross-references before deletion (run in bash — Windows PowerShell incompatible)</name>
  <files>(no file changes — read-only audit)</files>
  <action>**IMPORTANT (per M-6):** This audit grep uses POSIX regex and must run in bash (Git Bash, WSL, or macOS/Linux). It will NOT work in Windows PowerShell. If on Windows, open Git Bash and run the heredoc below from the repo root. Do NOT translate the regex to PowerShell — the parentheses and backslash semantics differ.

```bash
# Run from repo root in bash (NOT PowerShell)
bash -c '
set -e
echo "Scanning for legacy-tab navigation targets..."
grep -rnE "router\.(push|replace).*\((tabs)/(events|stays|studio|transport|delivery|driver|rider)" mobile/app mobile/components mobile/lib 2>/dev/null || echo "  No router push/replace hits"
echo "Scanning for relative imports of legacy tab files..."
grep -rnE "from .*(tabs)/(events|stays|studio|transport|delivery|driver|rider)" mobile/app mobile/components mobile/lib 2>/dev/null || echo "  No relative imports"
'
```

For each hit found, rewrite the navigation target to its modal/stack equivalent per CONTEXT.md decisions: `events` → migrate-destination is the Book hub events sub-section (built by 08-05) — top-level events tab callers should target `/(tabs)/book` (08-05 will default the active sub-section to whatever makes sense, but a generic Book-hub redirect is correct); `stays` → `/(tabs)/book`; `studio` → `/(tabs)/book`; `transport` → `/transport-flow`; `delivery` → `/delivery-flow`; `driver` → `/driver-dashboard`; `rider` → `/rider-dashboard`. Report the list of rewritten files in this task's done note. If a hit cannot be safely rewritten (e.g., depends on tab-scoped state), STOP and write `.planning/phases/08-mobile-redesign/08-BLOCKERS.md` instead of proceeding to Task 2.</action>
  <verify>
    <automated>bash -c 'count=$(grep -rnE "router\.(push|replace).*\((tabs)/(events|stays|studio|transport|delivery|driver|rider)" mobile/app mobile/components mobile/lib 2>/dev/null | wc -l); echo "Remaining hits: $count"; test "$count" -eq 0'</automated>
  </verify>
  <done>All cross-references rewritten or zero hits found. Audit summary recorded in the task summary. No `BLOCKERS.md` written (or if written, this plan halts and is re-planned).</done>
</task>

<task type="auto">
  <name>Task 2: Delete the 7 legacy tab files</name>
  <files>mobile/app/(tabs)/events.tsx, mobile/app/(tabs)/stays.tsx, mobile/app/(tabs)/studio.tsx, mobile/app/(tabs)/transport.tsx, mobile/app/(tabs)/delivery.tsx, mobile/app/(tabs)/driver.tsx, mobile/app/(tabs)/rider.tsx</files>
  <action>**Before deleting `events.tsx`**, confirm that 08-05's plan now includes the EventsSubsection migration (it does — per H-1 fix). If 08-05 has not yet run, the deletion of `events.tsx` is still safe because the functionality is being re-implemented in `mobile/components/book/EventsSubsection.tsx` during Wave 3 — but the EventsFeed code itself currently lives in `mobile/app/(tabs)/book.tsx` (verified — see book.tsx:179-217), so deleting `events.tsx` does NOT lose source. The QR scan-in FAB lives at `events.tsx:57` (a `router.push('/qr-checkin')` call). Before deleting, copy the QR-FAB JSX block (the surrounding Pressable/FloatingActionButton wrapper, ~10-20 lines around line 57) into a comment block at the top of `.planning/phases/08-mobile-redesign/08-04-SUMMARY.md` so 08-05 can re-use it when building EventsSubsection.

Then delete all 7 files using `git rm` (so the deletion is staged for the next commit and tracked properly). Do NOT delete `book.tsx`, `index.tsx`, `wallet.tsx`, `concierge.tsx`, `profile.tsx`, or `_layout.tsx` — those are the 5-tab survivors. Do NOT touch any modal/stack screens under `mobile/app/` (e.g. `transport-flow.tsx`, `delivery-flow.tsx`, `driver-dashboard.tsx`, `rider-dashboard.tsx`) — those are the migration destinations and stay. After deletion, confirm the tabs directory has exactly 6 entries (the 5 visible tabs + `_layout.tsx`).</action>
  <verify>
    <automated>bash -c 'count=$(ls "mobile/app/(tabs)/" | wc -l); echo "Files: $count"; ls "mobile/app/(tabs)/"; test "$count" -eq 6'</automated>
  </verify>
  <done>`mobile/app/(tabs)/` contains exactly: `_layout.tsx`, `book.tsx`, `concierge.tsx`, `index.tsx`, `profile.tsx`, `wallet.tsx`. The 7 legacy tab files are git-removed. QR-FAB JSX preserved in 08-04-SUMMARY.md for 08-05 to consume.</done>
</task>

<task type="auto">
  <name>Task 3: Strip hidden Tabs.Screen registrations from _layout.tsx</name>
  <files>mobile/app/(tabs)/_layout.tsx</files>
  <action>Edit `mobile/app/(tabs)/_layout.tsx` and delete the 7 hidden `<Tabs.Screen name="events" options={{ href: null }} />` lines (events/stays/studio/transport/delivery/driver/rider) — currently lines 99-105 per the current file (verify the line range is still accurate when executing). Also delete the `{/* ── Hidden legacy tabs ── */}` comment on line 98 (verify range). Leave the 5 visible Tabs.Screen registrations (index/book/wallet/concierge/profile) untouched. Leave all styling, custom TabBarBackground, TabIcon, TabLabel, and styles unchanged. Result: the Tabs component should contain exactly 5 Tabs.Screen children.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('mobile/app/(tabs)/_layout.tsx','utf8'); const screens=(c.match(/<Tabs\.Screen/g)||[]).length; if(screens!==5){console.error('Expected 5 Tabs.Screen, got',screens);process.exit(1);} if(c.includes('href: null')){console.error('href: null still present');process.exit(2);} console.log('OK 5 tabs');"</automated>
  </verify>
  <done>`_layout.tsx` contains exactly 5 `<Tabs.Screen` lines, no `href: null` remains, no reference to events/stays/studio/transport/delivery/driver/rider tab names remains.</done>
</task>

</tasks>

<verification>
- `ls mobile/app/(tabs)/` returns exactly 6 entries (5 tabs + `_layout.tsx`).
- `grep -c "Tabs.Screen" mobile/app/(tabs)/_layout.tsx` returns `5`.
- `bash -c 'grep -rnE "router\.(push|replace).*\((tabs)/(events|stays|studio|transport|delivery|driver|rider)" mobile/'` returns zero hits.
- `cd mobile && npx tsc --noEmit` passes.
- App boots without "route not found" warnings.
</verification>

<success_criteria>
- 5-tab layout is final and asserted.
- All legacy tab functionality reachable via modal/stack routes already registered in root `mobile/app/_layout.tsx`.
- Events/Stays/Studio/Marketplace migration destinations are owned by 08-05 (Wave 3).
- All existing 282+ tests still pass.
</success_criteria>

<output>
Create `.planning/phases/08-mobile-redesign/08-04-SUMMARY.md` with: the audit hit list and rewrites performed, the 7 deleted file paths, the preserved QR-FAB JSX block (for 08-05 to consume), and the final 5-tab layout source after edits.
</output>
