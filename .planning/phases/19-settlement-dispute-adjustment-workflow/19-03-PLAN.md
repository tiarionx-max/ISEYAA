---
phase: 19-settlement-dispute-adjustment-workflow
plan: 03
type: tdd
wave: 2
depends_on: ["19-01", "19-02"]
files_modified:
  - backend/src/modules/settlement-disputes/dto/raise-dispute.dto.ts
  - backend/src/modules/settlement-disputes/dto/resolve-dispute.dto.ts
  - backend/src/modules/settlement-disputes/settlement-disputes.service.ts
  - backend/src/modules/settlement-disputes/__tests__/settlement-disputes.service.spec.ts
autonomous: true
requirements: [SETTLE-10a, SETTLE-10b, SETTLE-10c, SETTLE-10d, SETTLE-10e]

must_haves:
  truths:
    - "A SUPER_ADMIN-identified actor can raise a dispute against a completed settlement, capturing reason and disputed amount, only if the settlement exists and has no other active (non-terminal) dispute already open against it (SETTLE-10a)"
    - "A dispute moves OPEN → IN_REVIEW with a reviewer recorded, and resolve()/dismiss() are callable from OPEN, IN_REVIEW, or BLOCKED — only RESOLVED/DISMISSED are terminal (SETTLE-10b, D-04, D-05)"
    - "Resolving a dispute is 100% system-computed: SettlementService.resolveSplit() is diffed against the original settlement's actual Transaction rows to derive adjustment lines automatically — no caller-supplied override amount exists anywhere in the resolve() path (D-01)"
    - "An adjustment that would drive a wallet negative is caught as a typed error and moves the dispute to BLOCKED (not DISMISSED, not left IN_REVIEW) — and a later resolve() call on a BLOCKED dispute re-attempts the same computation (SETTLE-10d, D-05)"
    - "Every dispute action (raise, review, resolve success, resolve→BLOCKED, resolve no-op, dismiss) writes an AuditLog row with who/when/why/amount, and an AuditLog write failure never blocks the primary state change (SETTLE-10e)"
  artifacts:
    - path: "backend/src/modules/settlement-disputes/settlement-disputes.service.ts"
      provides: "raise/findQueue/findById/moveToReview/resolve/dismiss/computeAdjustmentLines"
      contains: "async computeAdjustmentLines"
    - path: "backend/src/modules/settlement-disputes/dto/raise-dispute.dto.ts"
      provides: "RaiseDisputeDto"
      contains: "class RaiseDisputeDto"
    - path: "backend/src/modules/settlement-disputes/dto/resolve-dispute.dto.ts"
      provides: "ResolveDisputeDto"
      contains: "class ResolveDisputeDto"
  key_links:
    - from: "backend/src/modules/settlement-disputes/settlement-disputes.service.ts (resolve())"
      to: "backend/src/common/services/settlement.service.ts (resolveSplit(), adjust())"
      via: "this.settlementService.resolveSplit(module, chargeAmountNgn) then this.settlementService.adjust({...lines})"
      pattern: "settlementService\\.(resolveSplit|adjust)"
    - from: "backend/src/modules/settlement-disputes/settlement-disputes.service.ts (writeAudit())"
      to: "audit_logs table"
      via: "prisma.auditLog.create, wrapped in try/catch, never rethrows"
      pattern: "auditLog\\.create"
---

<objective>
Build `SettlementDisputesService` — the state-machine + financial-diffing core of this phase. Implements `raise()`, `findQueue()`, `findById()`, `moveToReview()` (Task 1), and the complex `computeAdjustmentLines()` + `resolve()` + `dismiss()` (Task 2), all wired to the `SettlementService.resolveSplit()`/`adjust()` primitives built in `19-01-PLAN.md`/`19-02-PLAN.md`.

This plan does NOT build the controller or register the module in `AppModule` — that is `19-04-PLAN.md`, which depends on this plan's service existing first (interface-first ordering: service+DTOs before the HTTP surface that calls them).

Purpose: this is where D-01 ("system computes the adjustment, not the reviewer"), D-04 (5-status BLOCKED state machine), D-05 (BLOCKED is retryable), and SETTLE-10e (every action audited) all get implemented together, since they are tightly coupled inside `resolve()`.

Output: `RaiseDisputeDto`, `ResolveDisputeDto`, and a fully tested `SettlementDisputesService`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-settlement-dispute-adjustment-workflow/19-CONTEXT.md
@.planning/phases/19-settlement-dispute-adjustment-workflow/19-PATTERNS.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- SettlementDispute model, from 19-01-PLAN.md — this plan's service reads/writes this shape directly. -->

```prisma
model SettlementDispute {
  id                     String    @id @default(uuid())
  settlementReference    String
  module                 String
  raisedByUserId         String
  raisedBy               User      @relation(fields: [raisedByUserId], references: [id])
  reason                 String
  status                 String    @default("OPEN") // OPEN | IN_REVIEW | RESOLVED | DISMISSED | BLOCKED
  requestedAdjustmentNgn Decimal?
  assignedTo             String?
  resolution             String?
  resolvedAt             DateTime?
  adjustmentReference    String?
  metadata               Json?
  createdAt              DateTime  @default(now())
  updatedAt              DateTime  @updatedAt
}
```

<!-- adjust()/resolveSplit() signatures, from 19-02-PLAN.md (adjust) and Phase 18 (resolveSplit, already shipped) — this plan's resolve() calls both directly. -->

```typescript
// backend/src/common/services/settlement.service.ts
async resolveSplit(module: string, amountNgn: number): Promise<{
  earnerPct: number; ministryPct: number; platformPct: number | null;
}>

export class InsufficientAdjustmentBalanceError extends Error {
  constructor(public readonly walletId: string, public readonly shortfallNgn: number) { /* ... */ }
}
export interface SettlementAdjustmentLine { walletId: string; deltaNgn: number; }
export interface SettlementAdjustmentInput {
  originalReference: string; module: string; lines: SettlementAdjustmentLine[];
  reason: string; metadata?: Record<string, unknown>;
}
async adjust(input: SettlementAdjustmentInput): Promise<SettlementResult>
```

<!-- Transaction row shape written by settle() (backend/src/common/services/settlement.service.ts settle(), lines 184-234) —
     computeAdjustmentLines() greps EXACTLY these fields back out of already-persisted rows: -->

```typescript
// Each recipient row: reference = `${settleReference}-${refSuffix}`, metadata.recipientType = the caller's `tag`
//   (e.g. 'DRIVER', 'VENDOR', 'MINISTRY' — see transport.service.ts lines 572-587 for the canonical
//   "one earner tag + one 'MINISTRY' tag" 2-recipient shape every non-Tour module's settle() call follows).
// Platform row: reference = `${settleReference}-PLAT`, metadata has NO recipientType field.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DTOs + raise()/findQueue()/findById()/moveToReview() + AuditLog helper</name>
  <files>backend/src/modules/settlement-disputes/dto/raise-dispute.dto.ts, backend/src/modules/settlement-disputes/dto/resolve-dispute.dto.ts, backend/src/modules/settlement-disputes/settlement-disputes.service.ts, backend/src/modules/settlement-disputes/__tests__/settlement-disputes.service.spec.ts</files>
  <read_first>
    backend/src/modules/reviews/reviews.service.ts (full file — `findFlagQueue()`/`findFlagById()` lines 259-312 near-verbatim structural precedent; constructor + imports lines 1-52)
    backend/src/modules/reviews/dto/create-review.dto.ts (full file — class-validator + `@nestjs/swagger` decorator style, exported literal-union + const-array pattern for enum-like string fields)
    backend/src/modules/reviews/dto/resolve-flag.dto.ts (full file — optional `resolution` field style)
    backend/src/modules/users/kyc.service.ts (lines 106-122 — the exact AuditLog write try/catch silent-fallback shape: `this.prisma.auditLog.create({ data: { userId, action, entity, entityId } })` wrapped in try/catch, `this.logger.error(...)` on failure, never rethrows)
    backend/prisma/schema.prisma (`SettlementDispute` model, as added by `19-01-PLAN.md` — exact field list, see `<interfaces>` above)
  </read_first>
  <behavior>
    - Test: `raise()` throws `NotFoundException` when no `Transaction` row matches `{ reference: { startsWith: \`${dto.settlementReference}-\` } }`
    - Test: `raise()` throws `ConflictException` when an existing `SettlementDispute` row for the same `settlementReference` has `status` in `['OPEN','IN_REVIEW','BLOCKED']`
    - Test: `raise()` creates a `SettlementDispute` row with `status: 'OPEN'`, `raisedByUserId: actorUserId`, and writes exactly one `AuditLog` row with `action: 'SETTLEMENT_DISPUTE_RAISED'`
    - Test: `findQueue({})` defaults to `status: 'OPEN'`, paginates via `skip`/`take` mirroring `findFlagQueue()`'s shape, returns `{ data, pagination }`
    - Test: `findById()` throws `NotFoundException` when the row doesn't exist
    - Test: `moveToReview()` throws `ConflictException` when `dispute.status !== 'OPEN'`; on `OPEN`, transitions to `'IN_REVIEW'`, sets `assignedTo: actorUserId`, writes `AuditLog` `'SETTLEMENT_DISPUTE_MOVED_TO_REVIEW'`
    - Test: an `AuditLog` write failure (mocked `auditLog.create` rejecting) does NOT throw out of `raise()`/`moveToReview()` — the primary state change already committed; only `this.logger.error` is invoked
  </behavior>
  <action>
    Create `backend/src/modules/settlement-disputes/dto/raise-dispute.dto.ts` exporting `type SettlementDisputeModuleLiteral = 'transport' | 'delivery' | 'events' | 'marketplace' | 'stays' | 'studio' | 'tour'`, `const SETTLEMENT_DISPUTE_MODULES: SettlementDisputeModuleLiteral[]` (same 7 literals), and `class RaiseDisputeDto` with `settlementReference: string` (`@IsString()`), `module: SettlementDisputeModuleLiteral` (`@IsEnum(SETTLEMENT_DISPUTE_MODULES)`), `reason: string` (`@IsString() @MaxLength(1000)`), `requestedAdjustmentNgn?: number` (`@IsOptional() @IsNumber()`, `@ApiPropertyOptional` description: `'Informational only (D-01) — the system computes the actual adjustment via resolveSplit(), this value is not used to derive it.'`) — mirror `create-review.dto.ts`'s decorator style exactly.

    Create `backend/src/modules/settlement-disputes/dto/resolve-dispute.dto.ts` exporting `class ResolveDisputeDto` with a single optional `resolution?: string` field (`@IsOptional() @IsString() @MaxLength(500)`) — no decision/amount field belongs here; D-01 means the ROUTE (`/resolve` vs `/dismiss`, wired in `19-04-PLAN.md`) determines the outcome, and the system (not the caller) computes the amount. Mirror `resolve-flag.dto.ts`'s structure, minus the required `decision` enum field.

    Create `backend/src/modules/settlement-disputes/settlement-disputes.service.ts`. `@Injectable()` class `SettlementDisputesService` with a `private readonly logger = new Logger(SettlementDisputesService.name)` and constructor injecting `private readonly prisma: PrismaService` and `private readonly settlementService: SettlementService` (both already globally available — no module-level `imports` array needed downstream). Import `BadRequestException, ConflictException, Injectable, Logger, NotFoundException` from `@nestjs/common` (Task 2 will need `ConflictException` too — already imported here) plus `PrismaService` from `'../../prisma/prisma.service'`, `SettlementService` from `'../../common/services/settlement.service'`, `RaiseDisputeDto` from `'./dto/raise-dispute.dto'`, `ResolveDisputeDto` from `'./dto/resolve-dispute.dto'`.

    Add a `private async writeAudit(actorUserId: string, action: string, disputeId: string, extra: Record<string, unknown>): Promise<void>` helper mirroring `kyc.service.ts` lines 106-122 exactly: on success, `await this.prisma.auditLog.create({ data: { userId: actorUserId, action, entity: 'SettlementDispute', entityId: disputeId, newValue: extra as any } });`; on catch, `this.logger.error(\`Dispute audit log failed for disputeId=${disputeId}\`, err);` — never rethrows, never blocks the caller.

    Implement `async raise(actorUserId: string, dto: RaiseDisputeDto)` — three steps: (1) `const original = await this.prisma.transaction.findFirst({ where: { reference: { startsWith: \`${dto.settlementReference}-\` } }, select: { id: true } }); if (!original) throw new NotFoundException('No settlement found for this reference');` (2) `const activeExisting = await this.prisma.settlementDispute.findFirst({ where: { settlementReference: dto.settlementReference, status: { in: ['OPEN', 'IN_REVIEW', 'BLOCKED'] } }, select: { id: true } }); if (activeExisting) throw new ConflictException('An active dispute already exists for this settlement');` — this guard exists because `adjust()`'s idempotency key is per-`originalReference`, not per-dispute; without it, two concurrent disputes against the same settlement could race to post two DIFFERENT adjustments, with the second silently REPLAYing onto the first's numbers. (3) `const created = await this.prisma.settlementDispute.create({ data: { settlementReference: dto.settlementReference, module: dto.module, raisedByUserId: actorUserId, reason: dto.reason, requestedAdjustmentNgn: dto.requestedAdjustmentNgn ?? null, status: 'OPEN' } });` then `await this.writeAudit(actorUserId, 'SETTLEMENT_DISPUTE_RAISED', created.id, { module: dto.module, settlementReference: dto.settlementReference, reason: dto.reason, requestedAdjustmentNgn: dto.requestedAdjustmentNgn ?? null });` and `return created;`.

    Implement `async findQueue(opts: { status?: string; page?: number; limit?: number } = {})` and `async findById(disputeId: string)` as near-verbatim copies of `reviews.service.ts`'s `findFlagQueue()`/`findFlagById()` (lines 263-312), substituting `prisma.adminReviewFlag` → `prisma.settlementDispute`, default `status: 'OPEN'`, `include: { raisedBy: { select: { id: true, firstName: true, lastName: true } } }` in place of the review include block, and `NotFoundException('Dispute not found')` for `findById`.

    Implement `async moveToReview(disputeId: string, actorUserId: string)` — load `{ id: true, status: true }`; `NotFoundException` if missing; `if (dispute.status !== 'OPEN') throw new ConflictException(\`Dispute is already ${dispute.status}\`);` (only `OPEN` can enter `IN_REVIEW` — a `BLOCKED` dispute re-resolves directly via `resolve()` per D-05, it does not re-enter `IN_REVIEW` first); `await this.prisma.settlementDispute.update({ where: { id: disputeId }, data: { status: 'IN_REVIEW', assignedTo: actorUserId } });` then `await this.writeAudit(actorUserId, 'SETTLEMENT_DISPUTE_MOVED_TO_REVIEW', disputeId, {});` and return the updated row.

    Create `backend/src/modules/settlement-disputes/__tests__/settlement-disputes.service.spec.ts`. Build a `MockPrisma` interface covering `settlementDispute: { findFirst, findMany, findUnique, create, update, count }`, `transaction: { findFirst }`, `auditLog: { create }`, and a mock `SettlementService` object `{ resolveSplit: jest.fn(), adjust: jest.fn() }` provided via `{ provide: SettlementService, useValue: mockSettlementService }` in a `Test.createTestingModule`. Implement the 7 behavior scenarios listed above.
  </action>
  <verify>
    <automated>cd backend && npm run test -- settlement-disputes.service</automated>
  </verify>
  <done>settlement-disputes.service.spec.ts has 7 passing scenarios covering raise()'s two guard clauses + audit write, findQueue/findById defaults, moveToReview's 409 + transition + audit, and the silent-audit-failure fallback.</done>
  <acceptance_criteria>
    - `backend/src/modules/settlement-disputes/dto/raise-dispute.dto.ts` exists and exports `RaiseDisputeDto` + `SETTLEMENT_DISPUTE_MODULES` with exactly 7 literal values
    - `grep -c "ConflictException" backend/src/modules/settlement-disputes/settlement-disputes.service.ts` returns at least 2 (raise's active-dispute guard + moveToReview's terminal-state guard)
    - `grep -c "async writeAudit" backend/src/modules/settlement-disputes/settlement-disputes.service.ts` returns 1
    - `grep -c "catch (err)" backend/src/modules/settlement-disputes/settlement-disputes.service.ts` returns at least 1, inside `writeAudit` (silent fallback, never rethrows)
    - `cd backend && npm run test -- settlement-disputes.service` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: computeAdjustmentLines() + resolve()/dismiss() (D-01, D-04, D-05, SETTLE-10c/10d/10e)</name>
  <files>backend/src/modules/settlement-disputes/settlement-disputes.service.ts, backend/src/modules/settlement-disputes/__tests__/settlement-disputes.service.spec.ts</files>
  <read_first>
    backend/src/common/services/settlement.service.ts (`adjust()`'s exported `SettlementAdjustmentLine`/`SettlementAdjustmentInput`/`InsufficientAdjustmentBalanceError` and `resolveSplit()`'s signature, both as added by `19-02-PLAN.md` — see `<interfaces>` above)
    backend/src/modules/transport/transport.service.ts (lines 554-600 — confirms the `settle()` recipients convention every non-Tour module's call site follows: exactly one earner-tagged row via `metadata.recipientType` plus one `'MINISTRY'`-tagged row plus an implicit `-PLAT` platform row — the shape `computeAdjustmentLines()` must reverse-engineer from already-persisted `Transaction` rows)
    backend/src/common/services/settlement.service.ts (`settle()` lines 184-234 — confirms `Transaction.metadata.recipientType = r.tag` and the platform row's reference suffix `-PLAT`, the exact fields `computeAdjustmentLines()` greps for)
    backend/src/modules/settlement-disputes/settlement-disputes.service.ts (Task 1's output — `raise`/`findQueue`/`findById`/`moveToReview`/`writeAudit` to extend, not duplicate)
  </read_first>
  <behavior>
    - Test: `computeAdjustmentLines()` for a single-earner settlement (2 original rows: 1 earner tagged `'DRIVER'` amount=8500, 1 `'MINISTRY'` amount=500) with a mocked `resolveSplit()` returning a DIFFERENT split than what was originally paid returns exactly 2 lines: one for the earner's wallet with `deltaNgn = correctEarnerTotal - 8500`, one for the ministry wallet with `deltaNgn = correctMinistryTotal - 500`
    - Test: `computeAdjustmentLines()` returns an EMPTY `lines` array when the mocked `resolveSplit()` returns the SAME split that was originally applied (both deltas round to < ₦0.01)
    - Test: `computeAdjustmentLines()` for a multi-earner (Tour-style) settlement with 3 non-ministry, non-platform rows of amounts 3000/2000/1000 (`actualEarnerTotal=6000`) distributes a computed `earnerDeltaTotal` proportionally across all 3 wallets by original share (3000/6000, 2000/6000, 1000/6000), with the LAST row absorbing the rounding remainder so the 3 deltas sum exactly to `earnerDeltaTotal`
    - Test: `computeAdjustmentLines()` skips the ministry line entirely when the original settlement had no `'MINISTRY'`-tagged row
    - Test: `computeAdjustmentLines()` throws `NotFoundException` when no `Transaction` rows at all match the `settlementReference` prefix
    - Test: `resolve()` on a dispute in status `'BLOCKED'` recomputes and retries `adjust()` (D-05) — allowed, not a 409
    - Test: `resolve()` on a dispute in status `'RESOLVED'` or `'DISMISSED'` throws `ConflictException` (terminal states)
    - Test: `resolve()` happy path — `computeAdjustmentLines()` returns non-empty lines, `settlementService.adjust()` resolves `{ status: 'SETTLED' }`, dispute updates to `status: 'RESOLVED'` with `adjustmentReference` set, `AuditLog` `'SETTLEMENT_DISPUTE_RESOLVED'` written
    - Test: `resolve()` no-op path — `computeAdjustmentLines()` returns empty lines, dispute updates to `status: 'RESOLVED'` with `adjustmentReference: null` and a resolution noting no adjustment was required, `settlementService.adjust()` is NEVER called
    - Test: `resolve()` BLOCKED path (SETTLE-10d) — `settlementService.adjust()` rejects with `InsufficientAdjustmentBalanceError`, dispute updates to `status: 'BLOCKED'` (not `DISMISSED`, not left `IN_REVIEW`), `AuditLog` `'SETTLEMENT_DISPUTE_BLOCKED'` written with `walletId`+`shortfallNgn` in its `newValue`, and the error is swallowed (not rethrown to the controller — the caller receives the updated `BLOCKED` dispute row, not a 500)
    - Test: `dismiss()` throws `ConflictException` on `RESOLVED`/`DISMISSED`; on `OPEN`/`IN_REVIEW`/`BLOCKED` transitions to `'DISMISSED'`, writes `AuditLog` `'SETTLEMENT_DISPUTE_DISMISSED'`, never calls `settlementService.adjust()` or `computeAdjustmentLines()`
  </behavior>
  <action>
    Add `async computeAdjustmentLines(module: string, settlementReference: string): Promise<{ lines: SettlementAdjustmentLine[]; chargeAmountNgn: number }>` to `SettlementDisputesService`, under a `// INTERNAL — exposed for the spec and for resolve()` comment (mirrors `reviews.service.ts`'s `recomputeTargetRating()` comment convention). Import `SettlementAdjustmentLine`, `InsufficientAdjustmentBalanceError` from `'../../common/services/settlement.service'` (both added by `19-02-PLAN.md`).

    Steps inside `computeAdjustmentLines()`: (1) `const rows = await this.prisma.transaction.findMany({ where: { reference: { startsWith: \`${settlementReference}-\` }, NOT: { reference: { contains: '-ADJ-' } }, status: 'SUCCESS' }, select: { id: true, reference: true, amount: true, walletId: true, metadata: true } });` (2) if `rows.length === 0`, throw `new NotFoundException('No settled Transaction rows found for this settlement reference')`. (3) `const platformRow = rows.find((r) => r.reference.endsWith('-PLAT'));` `const ministryRow = rows.find((r) => (r.metadata as any)?.recipientType === 'MINISTRY');` `const earnerRows = rows.filter((r) => r.id !== platformRow?.id && r.id !== ministryRow?.id);`. (4) `const chargeAmountNgn = rows.reduce((s, r) => s + Number(r.amount), 0);` `const actualEarnerTotal = earnerRows.reduce((s, r) => s + Number(r.amount), 0);` `const actualMinistryTotal = ministryRow ? Number(ministryRow.amount) : 0;`. (5) `const { earnerPct, ministryPct } = await this.settlementService.resolveSplit(module, chargeAmountNgn);` (6) `const correctEarnerTotal = Math.round(chargeAmountNgn * earnerPct * 100) / 100;` `const correctMinistryTotal = Math.round(chargeAmountNgn * ministryPct * 100) / 100;` `const earnerDeltaTotal = Math.round((correctEarnerTotal - actualEarnerTotal) * 100) / 100;` `const ministryDelta = Math.round((correctMinistryTotal - actualMinistryTotal) * 100) / 100;`. (7) build `const lines: SettlementAdjustmentLine[] = [];` — ministry: `if (ministryRow?.walletId && Math.abs(ministryDelta) >= 0.01) lines.push({ walletId: ministryRow.walletId, deltaNgn: ministryDelta });`. Earner distribution: `const earnerRowsWithWallet = earnerRows.filter((r) => r.walletId);` if `earnerRowsWithWallet.length > 0 && actualEarnerTotal > 0 && Math.abs(earnerDeltaTotal) >= 0.01`, iterate with a running `remaining = earnerDeltaTotal`: for every row EXCEPT the last, `const share = Number(row.amount) / actualEarnerTotal; const deltaNgn = Math.round(earnerDeltaTotal * share * 100) / 100; remaining = Math.round((remaining - deltaNgn) * 100) / 100; if (Math.abs(deltaNgn) >= 0.01) lines.push({ walletId: row.walletId!, deltaNgn });`; for the LAST row in `earnerRowsWithWallet`, push `{ walletId: lastRow.walletId!, deltaNgn: remaining }` if `Math.abs(remaining) >= 0.01` — this absorbs the full rounding remainder so the earner-side lines always sum exactly to `earnerDeltaTotal`. A single-earner module (`earnerRowsWithWallet.length === 1`) trivially collapses to one line with `deltaNgn = earnerDeltaTotal` (the loop body for "every row except the last" never executes; the sole row IS the last row). (8) `return { lines, chargeAmountNgn };`.

    Add `async resolve(disputeId: string, actorUserId: string, dto: ResolveDisputeDto)`: (1) `const dispute = await this.prisma.settlementDispute.findUnique({ where: { id: disputeId } }); if (!dispute) throw new NotFoundException('Dispute not found');` (2) `if (dispute.status === 'RESOLVED' || dispute.status === 'DISMISSED') throw new ConflictException(\`Dispute is already ${dispute.status}\`);` — `OPEN`, `IN_REVIEW`, and `BLOCKED` are all valid entry states (D-05). (3) `const { lines } = await this.computeAdjustmentLines(dispute.module, dispute.settlementReference);` (4) no-op path: `if (lines.length === 0) { const updated = await this.prisma.settlementDispute.update({ where: { id: disputeId }, data: { status: 'RESOLVED', assignedTo: actorUserId, resolvedAt: new Date(), resolution: dto.resolution ?? 'No adjustment required — current resolveSplit() output matches the original settlement', adjustmentReference: null } }); await this.writeAudit(actorUserId, 'SETTLEMENT_DISPUTE_RESOLVED_NOOP', disputeId, { module: dispute.module, settlementReference: dispute.settlementReference }); return updated; }` (5) apply path, wrapped in `try`/`catch`: call `this.settlementService.adjust({ originalReference: dispute.settlementReference, module: dispute.module, lines, reason: dto.resolution ?? dispute.reason, metadata: { disputeId: dispute.id, adjustmentReason: dto.resolution ?? dispute.reason } })`; on success, `const adjustmentReference = \`${dispute.settlementReference}-ADJ\`;` (the prefix, individual lines carry the `-ADJ-${n}` suffix internally) then update the dispute to `status: 'RESOLVED'`, `assignedTo: actorUserId`, `resolvedAt: new Date()`, `resolution: dto.resolution ?? null`, `adjustmentReference`, write `AuditLog` `'SETTLEMENT_DISPUTE_RESOLVED'` with `{ module, settlementReference, adjustmentReference, lines, adjustResultStatus: adjResult.status }`, return the updated row. In the `catch` block: `if (err instanceof InsufficientAdjustmentBalanceError) { const updated = await this.prisma.settlementDispute.update({ where: { id: disputeId }, data: { status: 'BLOCKED', assignedTo: actorUserId, resolution: \`Blocked — insufficient balance on wallet ${err.walletId} (shortfall ₦${err.shortfallNgn})\` } }); await this.writeAudit(actorUserId, 'SETTLEMENT_DISPUTE_BLOCKED', disputeId, { module: dispute.module, settlementReference: dispute.settlementReference, walletId: err.walletId, shortfallNgn: err.shortfallNgn }); return updated; } throw err;` — note the `BLOCKED` update does NOT set `resolvedAt` (BLOCKED is not terminal, D-05) and always re-sets `assignedTo` to the retrying actor.

    Add `async dismiss(disputeId: string, actorUserId: string, dto: ResolveDisputeDto)`: load dispute (`NotFoundException` if missing); `if (dispute.status === 'RESOLVED' || dispute.status === 'DISMISSED') throw new ConflictException(\`Dispute is already ${dispute.status}\`);` else `await this.prisma.settlementDispute.update({ where: { id: disputeId }, data: { status: 'DISMISSED', assignedTo: actorUserId, resolvedAt: new Date(), resolution: dto.resolution ?? null } });` then `await this.writeAudit(actorUserId, 'SETTLEMENT_DISPUTE_DISMISSED', disputeId, { module: dispute.module, settlementReference: dispute.settlementReference });` and return the updated row. `dismiss()` MUST NOT call `settlementService.adjust()` or `computeAdjustmentLines()` under any circumstance.

    Extend the Task 1 spec file's `MockPrisma` with `transaction: { findFirst, findMany }` (add `findMany`) and mock `mockSettlementService.resolveSplit`/`mockSettlementService.adjust` per scenario via `.mockResolvedValueOnce`/`.mockRejectedValueOnce`. Add the 10 `<behavior>`-listed scenarios as new `describe('computeAdjustmentLines()', ...)` and `describe('resolve()', ...)`/`describe('dismiss()', ...)` blocks in the same spec file.
  </action>
  <verify>
    <automated>cd backend && npm run test -- settlement-disputes.service</automated>
  </verify>
  <done>Full settlement-disputes.service.spec.ts is green with 17+ scenarios across Task 1 and Task 2; computeAdjustmentLines()/resolve()/dismiss() implement D-01/D-04/D-05/SETTLE-10c/10d/10e exactly.</done>
  <acceptance_criteria>
    - `grep -c "async computeAdjustmentLines" backend/src/modules/settlement-disputes/settlement-disputes.service.ts` returns 1
    - `grep -c "InsufficientAdjustmentBalanceError" backend/src/modules/settlement-disputes/settlement-disputes.service.ts` returns at least 2 (import + `instanceof` check)
    - `grep -c "status: 'BLOCKED'" backend/src/modules/settlement-disputes/settlement-disputes.service.ts` returns at least 1
    - `grep -c "settlementService.adjust(" backend/src/modules/settlement-disputes/settlement-disputes.service.ts` returns exactly 1 (only `resolve()`'s apply path calls it — `dismiss()`/`moveToReview()` never do)
    - `grep -c "settlementService.adjust\|computeAdjustmentLines" backend/src/modules/settlement-disputes/settlement-disputes.service.ts` between the `async dismiss(` line and its matching closing brace returns 0 (proves `dismiss()` never touches the financial computation path)
    - `cd backend && npm run test -- settlement-disputes.service` exits 0 with all `computeAdjustmentLines`/`resolve`/`dismiss` scenarios passing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Controller (wired in `19-04-PLAN.md`, `SUPER_ADMIN`-only) → `SettlementDisputesService` | The service takes `actorUserId` as a plain trusted parameter — authorization happens at the HTTP/controller layer, not duplicated in the service (matches `ReviewsService`/`AdminService` precedent) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-19-05 | Elevation of Privilege | `SettlementDisputesService` (all methods) | mitigate | role enforcement lives at the controller (`@Roles(SUPER_ADMIN)`, wired in `19-04-PLAN.md`) — the service itself performs no additional authorization check, matching the existing `ReviewsService`/`AdminService` defense-boundary placement (HTTP layer, not duplicated per-service-method) |
| T-19-06 | Tampering | `resolve()`'s adjustment computation | mitigate | the caller (`SUPER_ADMIN`) supplies only free-text `reason`/`resolution` — the actual adjustment amount is 100% system-computed via `resolveSplit()` + `computeAdjustmentLines()`, with no DTO field or code path allowing a caller-supplied override amount (D-01) — closes the "reviewer types in whatever number they want" tampering vector entirely |
| T-19-07 | Repudiation / Replay | `raise()`'s active-dispute guard | mitigate | rejecting a second `raise()` call while a non-terminal (`OPEN`/`IN_REVIEW`/`BLOCKED`) dispute already exists for the same `settlementReference` prevents two disputes from racing to post two DIFFERENT `adjust()` calls against the same idempotency key, where the second would silently `REPLAY` onto the first's numbers |
| T-19-08 | Denial of Service | `computeAdjustmentLines()` | accept | reads are bounded by a single `settlementReference`'s `Transaction` rows (small, fixed cardinality — at most a handful of recipients per settlement); no unbounded query risk |
</threat_model>

<verification>
- `cd backend && npm run test -- settlement-disputes.service` passes (17+ scenarios)
- `cd backend && npm run test -- settlement.service` still passes (no regression on the `19-02` primitive this plan consumes)
- Full backend suite still green: `cd backend && npm run test`
</verification>

<success_criteria>
- `SettlementDisputesService` implements `raise`/`findQueue`/`findById`/`moveToReview`/`resolve`/`dismiss`/`computeAdjustmentLines`
- `resolve()` is 100% system-computed (D-01), handles the no-op/happy-path/BLOCKED branches distinctly (D-04)
- A `BLOCKED` dispute is retryable via a second `resolve()` call (D-05)
- Every state transition writes an `AuditLog` row, with silent-fallback-on-failure (SETTLE-10e)
</success_criteria>

<output>
After completion, create `.planning/phases/19-settlement-dispute-adjustment-workflow/19-03-SUMMARY.md`
</output>
