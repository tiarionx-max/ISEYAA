---
phase: 19-settlement-dispute-adjustment-workflow
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/settlement-disputes/settlement-disputes.service.ts
  - backend/src/modules/settlement-disputes/__tests__/settlement-disputes.service.spec.ts
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [SETTLE-10c, SETTLE-10e]

must_haves:
  truths:
    - "computeAdjustmentLines() produces a zero-sum lines array for every settlement shape, including ones where the corrected split assigns a nonzero share to a category (ministry or earner) that has NO wallet-bearing row in the original settlement (SETTLE-10c)"
    - "When a category has no deliverable wallet row, its corrected share is retained on the platform wallet (folded into correctPlatformTotal via the category's actual, not corrected, total) rather than silently discarded — mirrors settle()'s own unresolved-recipient roll-up-to-platform semantics"
    - "3 new regression tests (missing ministryRow, missing earnerRowsWithWallet, earnerRowsWithWallet present but actualEarnerTotal===0) each assert BOTH sum(lines.map(l => l.deltaNgn)) === 0 AND the exact individual line values for their scenario"
    - "All pre-existing settlement-disputes.service.spec.ts scenarios (raise/moveToReview/computeAdjustmentLines/resolve/dismiss) still pass unmodified — the guard refactor is behavior-preserving for every case where a category already has a deliverable wallet row"
    - "REQUIREMENTS.md's SETTLE-10a through SETTLE-10e checkboxes are corrected to [x] once this plan's tests confirm the fix"
  artifacts:
    - path: "backend/src/modules/settlement-disputes/settlement-disputes.service.ts"
      provides: "Money-conserving correctPlatformTotal formula guarded by earnerDelivered/ministryDelivered flags that mirror the existing line-push guards"
      contains: "earnerDelivered"
    - path: "backend/src/modules/settlement-disputes/__tests__/settlement-disputes.service.spec.ts"
      provides: "3 new regression tests for the residual CR-01 trigger paths (missing ministry wallet, missing earner wallet, zero-amount earner wallet)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected checkbox state for SETTLE-10a through SETTLE-10e reflecting the true post-fix status"
  key_links:
    - from: "settlement-disputes.service.ts computeAdjustmentLines() earnerDelivered/ministryDelivered flags"
      to: "correctPlatformTotal formula"
      via: "conditional subtraction — a category's CORRECTED total is only subtracted from the platform total when that category will actually receive a deliverable line; otherwise its ACTUAL (original) total is subtracted instead, retaining the difference on the platform wallet"
      pattern: "earnerDelivered \\? correctEarnerTotal : actualEarnerTotal"
---

<objective>
Close the residual CR-01 money-conservation defect confirmed by `19-VERIFICATION.md`'s 2026-07-20 re-verification (score 4/5, Truth #3 FAILED, SETTLE-10c NOT SATISFIED). Prior gap-closure plan 19-05 fixed the "platform line never emitted at all" variant of CR-01. This plan fixes a NEW variant of the same bug class that survived 19-05's fix: `correctPlatformTotal` unconditionally subtracts `correctEarnerTotal`/`correctMinistryTotal` even when the corresponding ministry/earner line is never actually pushed (because no wallet-bearing row exists to receive it), silently destroying or manufacturing money for exactly the scenarios dispute resolution exists to correct (a settlement made under an incomplete or wrong original split).

This is a single, localized, formula-precision fix inside `computeAdjustmentLines()` — the state machine, `raise()`, `dismiss()`, `SettlementService.adjust()`, and the already-closed CR-02/CR-03 fixes are all out of scope and must not be touched.

Purpose: make SETTLE-10c's "money-conserving for every input shape" guarantee actually hold for every input shape, not just the common one.

Output: a `computeAdjustmentLines()` that only "claims" a category's corrected total into the platform-balancing subtraction when that category has a deliverable line; 3 new regression tests exercising the previously-untested trigger paths; corrected `REQUIREMENTS.md` checkbox state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-settlement-dispute-adjustment-workflow/19-CONTEXT.md
@.planning/phases/19-settlement-dispute-adjustment-workflow/19-VERIFICATION.md

<interfaces>
<!-- Exact shapes this plan's code reads/writes. No exploration needed. -->

From backend/src/common/services/settlement.service.ts:
```
export interface SettlementAdjustmentLine {
  walletId: string;
  deltaNgn: number; // positive = credit, negative = debit
}
async resolveSplit(module: string, amountNgn: number): Promise<{ earnerPct: number; ministryPct: number; platformPct: number | null }>
async adjust(input: SettlementAdjustmentInput): Promise<SettlementResult>  // no defensive balance check — every line here is caller-supplied
```

Current `computeAdjustmentLines()` state (backend/src/modules/settlement-disputes/settlement-disputes.service.ts, lines 243-317, post-19-05):
```
const rows = await this.prisma.transaction.findMany({ ... });
const platformRow = rows.find((r) => r.reference.endsWith('-PLAT'));
const ministryRow = rows.find((r) => (r.metadata as any)?.recipientType === 'MINISTRY');
const earnerRows = rows.filter((r) => r.id !== platformRow?.id && r.id !== ministryRow?.id);

const chargeAmountNgn = rows.reduce((s, r) => s + Number(r.amount), 0);
const actualEarnerTotal = earnerRows.reduce((s, r) => s + Number(r.amount), 0);
const actualMinistryTotal = ministryRow ? Number(ministryRow.amount) : 0;

const { earnerPct, ministryPct } = await this.settlementService.resolveSplit(module, chargeAmountNgn);

const correctEarnerTotal = Math.round(chargeAmountNgn * earnerPct * 100) / 100;
const correctMinistryTotal = Math.round(chargeAmountNgn * ministryPct * 100) / 100;
const earnerDeltaTotal = Math.round((correctEarnerTotal - actualEarnerTotal) * 100) / 100;
const ministryDelta = Math.round((correctMinistryTotal - actualMinistryTotal) * 100) / 100;

const lines: SettlementAdjustmentLine[] = [];

if (ministryRow?.walletId && Math.abs(ministryDelta) >= 0.01) {
  lines.push({ walletId: ministryRow.walletId, deltaNgn: ministryDelta });
}

const earnerRowsWithWallet = earnerRows.filter((r) => r.walletId);
if (earnerRowsWithWallet.length > 0 && actualEarnerTotal > 0 && Math.abs(earnerDeltaTotal) >= 0.01) {
  // proportional distribution loop, last row absorbs remainder — UNCHANGED, do not touch
}

// BUGGY: unconditional subtraction
const correctPlatformTotal = Math.round((chargeAmountNgn - correctEarnerTotal - correctMinistryTotal) * 100) / 100;
const actualPlatformTotal = platformRow ? Number(platformRow.amount) : 0;
const platformDelta = Math.round((correctPlatformTotal - actualPlatformTotal) * 100) / 100;
if (platformRow?.walletId && Math.abs(platformDelta) >= 0.01) {
  lines.push({ walletId: platformRow.walletId, deltaNgn: platformDelta });
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix residual CR-01 — gate correctPlatformTotal's subtraction on deliverability, add 3 regression tests</name>
  <files>backend/src/modules/settlement-disputes/settlement-disputes.service.ts, backend/src/modules/settlement-disputes/__tests__/settlement-disputes.service.spec.ts</files>
  <read_first>
    backend/src/modules/settlement-disputes/settlement-disputes.service.ts (lines 222-317 — `computeAdjustmentLines()` in full, its JSDoc, and the exact guards `ministryRow?.walletId` / `earnerRowsWithWallet.length > 0 && actualEarnerTotal > 0` this fix must mirror, not replace)
    backend/src/modules/settlement-disputes/__tests__/settlement-disputes.service.spec.ts (lines 302-452 — the full `describe('computeAdjustmentLines()', ...)` block including the `WAL_DRIVER`/`WAL_MINISTRY`/`WAL_PLATFORM` constants at lines 302-304, and the existing "skips the ministry line entirely" fixture at lines 411-443 which is the closest existing analog to the new tests)
    .planning/phases/19-settlement-dispute-adjustment-workflow/19-05-PLAN.md (Task 1 — the pattern already used to fix the platform-line-never-emitted variant of this exact bug class; this task fixes the next variant of the same class using the same self-balancing-formula philosophy)
    .planning/phases/19-settlement-dispute-adjustment-workflow/19-VERIFICATION.md (lines 14, 19, 22-25, 48, 97 — the exact defect description, the exact formula the verifier specified, and the exact 3 trigger scenarios required)
  </read_first>
  <behavior>
    - Test A ("retains a missing MINISTRY category's corrected share on the platform wallet when the corrected split assigns it a nonzero ministryPct but no MINISTRY-tagged row exists in the original settlement"): rows = `{ DRV row, amount 8000, WAL_DRIVER, recipientType DRIVER }` + `{ PLAT row, amount 2000, WAL_PLATFORM }`, no MINISTRY row. `resolveSplit()` returns `{ earnerPct: 0.85, ministryPct: 0.05, platformPct: 0.10 }`. `chargeAmountNgn` is 10000. Expected `lines` is exactly `[{ walletId: WAL_DRIVER, deltaNgn: 500 }, { walletId: WAL_PLATFORM, deltaNgn: -500 }]` (order: driver/earner line first, platform line last, matching the function's existing push order) and `sum(lines.map(l => l.deltaNgn))` is `0`.
    - Test B ("retains a missing earner category's corrected share on the platform wallet when no earnerRowsWithWallet exist at all but the corrected split assigns a nonzero earnerPct"): rows = `{ MINISTRY row, amount 1000, WAL_MINISTRY, recipientType MINISTRY }` + `{ PLAT row, amount 9000, WAL_PLATFORM }`, no earner/DRV rows at all. `resolveSplit()` returns `{ earnerPct: 0.80, ministryPct: 0.05, platformPct: 0.15 }`. `chargeAmountNgn` is 10000. Expected `lines` is exactly `[{ walletId: WAL_MINISTRY, deltaNgn: -500 }, { walletId: WAL_PLATFORM, deltaNgn: 500 }]` and `sum(lines.map(l => l.deltaNgn))` is `0`.
    - Test C ("retains a zero-amount earner category's corrected share on the platform wallet when earnerRowsWithWallet exist but actualEarnerTotal is exactly 0"): rows = `{ DRV row, amount 0, WAL_DRIVER, recipientType DRIVER }` + `{ MINISTRY row, amount 1000, WAL_MINISTRY, recipientType MINISTRY }` + `{ PLAT row, amount 9000, WAL_PLATFORM }`. `resolveSplit()` returns `{ earnerPct: 0.10, ministryPct: 0.05, platformPct: 0.85 }`. `chargeAmountNgn` is 10000. Expected `lines` is exactly `[{ walletId: WAL_MINISTRY, deltaNgn: -500 }, { walletId: WAL_PLATFORM, deltaNgn: 500 }]` (no DRIVER line — `actualEarnerTotal === 0` keeps the earner push guarded out exactly as it is today) and `sum(lines.map(l => l.deltaNgn))` is `0`.
    - All 3 new tests MUST FAIL against the current (unfixed) formula before the fix is applied — verify this by running the test file once with only the tests added (RED), confirming Test A/B/C fail with a nonzero nonzero sum, THEN apply the fix (GREEN).
    - After the fix, every pre-existing test in the file (raise/moveToReview/computeAdjustmentLines/resolve/dismiss describe blocks) must still pass unmodified — the guard refactor changes behavior ONLY when `ministryDelivered`/`earnerDelivered` is `false`, and every pre-existing fixture has both `true`.
  </behavior>
  <action>
    RED: Add the 3 new tests described above to the `describe('computeAdjustmentLines()', ...)` block in `settlement-disputes.service.spec.ts` (place after the existing "skips the ministry line entirely..." test, before the "throws NotFoundException when no Transaction rows..." test). Run `cd backend && npm run test -- settlement-disputes.service` and confirm exactly these 3 new tests fail (their `lines` arrays will contain an incorrect platform delta and/or the sum-to-zero assertion will fail) while all pre-existing tests still pass.

    GREEN: In `computeAdjustmentLines()` (settlement-disputes.service.ts), restructure exactly as follows — do not touch the ministry-delta/earner-delta computation, the proportional earner-distribution loop, or the `-PLAT` platform-row lookup:
    1. Move the `const earnerRowsWithWallet = earnerRows.filter((r) => r.walletId);` declaration to immediately BEFORE the `const lines: SettlementAdjustmentLine[] = [];` line (currently it is declared after the ministry-line push, inside the earner block — hoist it up so both flags below can reference it).
    2. Immediately after hoisting `earnerRowsWithWallet`, add two new `const` declarations: `const ministryDelivered = !!ministryRow?.walletId;` and `const earnerDelivered = earnerRowsWithWallet.length > 0 && actualEarnerTotal > 0;` — these names and expressions MUST exactly mirror the two existing guard conditions (`ministryRow?.walletId` truthy-check and `earnerRowsWithWallet.length > 0 && actualEarnerTotal > 0`), just extracted into named variables so `correctPlatformTotal` can reuse them.
    3. Replace the ministry-line push guard `if (ministryRow?.walletId && Math.abs(ministryDelta) >= 0.01)` with `if (ministryDelivered && Math.abs(ministryDelta) >= 0.01)` — same condition, using the new variable, zero behavior change.
    4. Replace the earner-line push guard `if (earnerRowsWithWallet.length > 0 && actualEarnerTotal > 0 && Math.abs(earnerDeltaTotal) >= 0.01)` with `if (earnerDelivered && Math.abs(earnerDeltaTotal) >= 0.01)` — same condition, using the new variable, zero behavior change.
    5. Replace the `correctPlatformTotal` formula. Current (buggy): `Math.round((chargeAmountNgn - correctEarnerTotal - correctMinistryTotal) * 100) / 100`. Fixed: `Math.round((chargeAmountNgn - (earnerDelivered ? correctEarnerTotal : actualEarnerTotal) - (ministryDelivered ? correctMinistryTotal : actualMinistryTotal)) * 100) / 100`. This is the exact formula from `19-VERIFICATION.md`'s gap description: when a category is NOT delivered, its ACTUAL (original) total — not its corrected total — is what gets subtracted, so the platform wallet retains the difference between the corrected and actual totals for that undeliverable category instead of that difference silently vanishing.
    6. Update the JSDoc comment above the platform-balancing block (the `// Platform-wallet balancing line (CR-01)` comment, currently just above `correctPlatformTotal`) to add one sentence noting: when a category has no deliverable wallet row, its ACTUAL total (not corrected total) is folded into the platform subtraction, so the platform wallet retains that category's corrected share rather than the correction being silently discarded — mirrors `settle()`'s own unresolved-recipient roll-up-to-platform semantics.
    Re-run `cd backend && npm run test -- settlement-disputes.service` and confirm all tests (pre-existing + 3 new) pass.
  </action>
  <verify>
    <automated>cd backend && npm run test -- settlement-disputes.service</automated>
  </verify>
  <done>computeAdjustmentLines() only subtracts a category's CORRECTED total from correctPlatformTotal when that category has a deliverable wallet line (mirroring the exact guards already used to push those lines); when not deliverable, the category's ACTUAL total is subtracted instead, retaining the difference on the platform wallet. All 3 new regression tests (missing ministryRow, missing earnerRowsWithWallet, zero-amount earnerRowsWithWallet) pass with exact expected line values and a sum-to-zero assertion. Every pre-existing test in the file still passes unmodified.</done>
  <acceptance_criteria>
    - `grep -c "earnerDelivered" backend/src/modules/settlement-disputes/settlement-disputes.service.ts` returns at least 3 (declaration + earner-line guard + correctPlatformTotal formula)
    - `grep -c "ministryDelivered" backend/src/modules/settlement-disputes/settlement-disputes.service.ts` returns at least 3 (declaration + ministry-line guard + correctPlatformTotal formula)
    - The `correctPlatformTotal` assignment line in `settlement-disputes.service.ts` contains the literal substring `earnerDelivered ? correctEarnerTotal : actualEarnerTotal` and the literal substring `ministryDelivered ? correctMinistryTotal : actualMinistryTotal`
    - `grep -c "ministryRow?.walletId &&" backend/src/modules/settlement-disputes/settlement-disputes.service.ts` returns 0 (the raw guard expression has been replaced by the named `ministryDelivered` variable in the line-push condition; `ministryRow?.walletId` may still appear once in the `ministryDelivered` declaration itself)
    - `cd backend && npm run test -- settlement-disputes.service` exits 0 with 3 new tests present (test count in the `computeAdjustmentLines()` describe block increases from 5 to 8) and zero pre-existing test failures
    - `cd backend && npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Correct REQUIREMENTS.md checkbox state for SETTLE-10a through SETTLE-10e</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md (lines 18-28 — the "Settlement Flexibility" section containing the SETTLE-10a/b/c/d/e checkboxes currently in a stale state per 19-VERIFICATION.md's staleness note)
    .planning/phases/19-settlement-dispute-adjustment-workflow/19-VERIFICATION.md (lines 95-99, 101 — the requirements coverage table and the explicit staleness note explaining what the corrected state should be)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`, under the "Settlement Flexibility" section, change the 5 SETTLE-10 checkbox lines from their current state to the corrected post-fix state: `SETTLE-10a` from `[ ]` to `[x]` (raise() fully satisfied per 19-VERIFICATION.md's Truth #1 VERIFIED, CR-02/CR-03 closed by 19-05), `SETTLE-10b` from `[ ]` to `[x]` (state machine, unchanged and verified), `SETTLE-10c` from `[x]` to `[x]` (this plan's Task 1 fix closes the residual CR-01 gap that made it NOT SATISFIED — this is the line this plan is directly responsible for; only mark it `[x]` because Task 1's tests in this same plan confirm the fix, not preemptively), `SETTLE-10d` from `[x]` to `[x]` (unchanged, already correct and verified), `SETTLE-10e` from `[ ]` to `[x]` (audit-writing mechanism is sound; the data-quality caveat noted in 19-VERIFICATION.md was a downstream consequence of the now-fixed SETTLE-10c defect, not a separate audit-logging defect). Do not alter any other requirement lines (GRPC-*, SETTLE-11*, MIN-*) in this file.
  </action>
  <verify>
    <automated>grep -E "SETTLE-10[a-e]" "C:/Developer/work/ISEYAA/.planning/REQUIREMENTS.md"</automated>
  </verify>
  <done>All 5 SETTLE-10 lines in REQUIREMENTS.md read `[x]`, accurately reflecting the post-fix state confirmed by Task 1's passing tests.</done>
  <acceptance_criteria>
    - `grep -c "\[x\] \*\*SETTLE-10a\*\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "\[x\] \*\*SETTLE-10b\*\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "\[x\] \*\*SETTLE-10c\*\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "\[x\] \*\*SETTLE-10d\*\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "\[x\] \*\*SETTLE-10e\*\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "\[ \] \*\*SETTLE-10" .planning/REQUIREMENTS.md` returns 0 (no remaining unchecked SETTLE-10 lines)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `SettlementDisputesController` (`SUPER_ADMIN`-only) → `resolve()` → `SettlementService.adjust()` | Same money-sensitive write surface identified in 19-04/19-05 — this plan closes the last known gap in how the data flowing across it is computed, not the boundary itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-19-15 | Tampering (accounting integrity, residual CR-01) | `computeAdjustmentLines()` | mitigate | `correctPlatformTotal` now only subtracts a category's CORRECTED total when that category has a deliverable wallet line (`earnerDelivered`/`ministryDelivered` guards mirror the exact conditions used to push those lines); when not deliverable, the category's ACTUAL total is subtracted instead, so the platform wallet retains the undelivered correction rather than it vanishing. Every non-empty `lines` result sums to 0 by construction for every input shape, not just the common one. |
</threat_model>

<verification>
- `cd backend && npm run test -- settlement-disputes.service` passes, including the 3 new regression tests, with zero pre-existing test regressions
- `cd backend && npx tsc --noEmit -p tsconfig.json` compiles cleanly
- Reading the updated `correctPlatformTotal` assignment confirms it uses `earnerDelivered ? correctEarnerTotal : actualEarnerTotal` and `ministryDelivered ? correctMinistryTotal : actualMinistryTotal`
- `.planning/REQUIREMENTS.md`'s SETTLE-10a through SETTLE-10e are all `[x]`
- **Not in scope for this plan:** re-running `cd backend && npm run test -- settlement-disputes.e2e` is a useful sanity check but no e2e fixture edits are required — both existing e2e scenarios already include a wallet-bearing MINISTRY row, so `ministryDelivered`/`earnerDelivered` are `true` in both and the formula is unchanged for those paths (confirmed by inspection, not requiring a fixture change).
</verification>

<success_criteria>
- `computeAdjustmentLines()` is money-conserving (`sum(lines.map(l => l.deltaNgn)) === 0`) for every input shape, including settlements missing a wallet-bearing ministry or earner row that the corrected split assigns a nonzero share to (SETTLE-10c, closes the residual CR-01 finding from `19-VERIFICATION.md`'s 2026-07-20 re-verification)
- 3 new regression tests lock in the previously-untested trigger paths with exact expected line values, not just a sum-to-zero check
- Zero regressions: every pre-existing test in `settlement-disputes.service.spec.ts` still passes
- `.planning/REQUIREMENTS.md` accurately reflects SETTLE-10a through SETTLE-10e as satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/19-settlement-dispute-adjustment-workflow/19-06-SUMMARY.md`
</output>
