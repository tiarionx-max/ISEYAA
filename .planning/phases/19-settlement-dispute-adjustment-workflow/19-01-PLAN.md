---
phase: 19-settlement-dispute-adjustment-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/
autonomous: true
requirements: [SETTLE-10a, SETTLE-10b]

must_haves:
  truths:
    - "A SettlementDispute table exists capturing which settlement/module is disputed, who raised it, why, and the raiser's believed adjustment amount (SETTLE-10a)"
    - "SettlementDispute.status supports the full 5-value state machine OPEN | IN_REVIEW | RESOLVED | DISMISSED | BLOCKED (D-04), extending the AdminReviewFlag 4-value precedent"
    - "A dispute's raiser is a real FK to User; its reviewer (assignedTo) is a plain userId string, matching the existing AdminReviewFlag convention exactly"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "SettlementDispute model"
      contains: "model SettlementDispute"
  key_links:
    - from: "backend/prisma/schema.prisma (SettlementDispute.raisedByUserId)"
      to: "User table"
      via: "raisedBy User @relation(fields: [raisedByUserId], references: [id])"
      pattern: "raisedBy\\s+User\\s+@relation"
---

<objective>
Add the `SettlementDispute` Prisma model — the data-model foundation every other plan in this phase depends on — and apply it via a real migration. This plan does NOT add any service/controller code; it only establishes the schema and the live database table.

Purpose: `19-02-PLAN.md` (SettlementService.adjust()) does not need this model, but `19-03-PLAN.md` (SettlementDisputesService) references `prisma.settlementDispute.*` directly and cannot type-check or run until the Prisma Client has been regenerated against this model — this is the load-bearing schema plan for the phase.

Output: `SettlementDispute` model in `schema.prisma`, a new migration under `backend/prisma/migrations/`, and a regenerated Prisma Client exposing `prisma.settlementDispute`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-settlement-dispute-adjustment-workflow/19-CONTEXT.md
@.planning/phases/19-settlement-dispute-adjustment-workflow/19-PATTERNS.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- Exact model this plan creates — 19-03-PLAN.md consumes this shape directly via prisma.settlementDispute.*, no further schema exploration needed. -->

```prisma
model SettlementDispute {
  id                     String    @id @default(uuid())
  settlementReference    String    // the original Transaction.reference prefix this dispute targets
  module                 String    // 'transport' | 'delivery' | 'events' | 'marketplace' | 'stays' | 'studio' | 'tour'
  raisedByUserId         String
  raisedBy               User      @relation(fields: [raisedByUserId], references: [id])
  reason                 String
  status                 String    @default("OPEN") // OPEN | IN_REVIEW | RESOLVED | DISMISSED | BLOCKED
  requestedAdjustmentNgn Decimal?
  assignedTo             String?   // SUPER_ADMIN userId (D-02: no STATE_ADMIN this phase)
  resolution             String?
  resolvedAt             DateTime?
  adjustmentReference    String?   // set once resolved+applied — the `${originalReference}-ADJ-*` prefix actually written
  metadata               Json?
  createdAt              DateTime  @default(now())
  updatedAt              DateTime  @updatedAt

  @@index([status])
  @@index([settlementReference])
  @@map("settlement_disputes")
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SettlementDispute Prisma model + User relation</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>
    backend/prisma/schema.prisma (lines 1115-1128: `AdminReviewFlag` — the state-machine precedent this model extends, same string-based `status` field, same `assignedTo`/`resolution`/`resolvedAt` shape, same `@@map(snake_case)` convention; lines 231-278: `User` model — relation array to append to, e.g. `reviews Review[]` at line 274; lines 659-680: `Transaction`/`Wallet` — confirms `Transaction.reference` is `@unique` and `walletId` is required, the invariant `SettlementDispute.settlementReference` (a bare prefix, not a full reference) and the later adjust()/compute logic depend on)
  </read_first>
  <action>
    Append a new `SettlementDispute` model to the END of `backend/prisma/schema.prisma` (the file currently ends at line 1128 with `AdminReviewFlag`'s closing brace — colocate this model immediately after it). Use the exact field list, types, and attributes shown in the `<interfaces>` block above: `id String @id @default(uuid())`; `settlementReference String`; `module String`; `raisedByUserId String` + `raisedBy User @relation(fields: [raisedByUserId], references: [id])`; `reason String`; `status String @default("OPEN")`; `requestedAdjustmentNgn Decimal?`; `assignedTo String?`; `resolution String?`; `resolvedAt DateTime?`; `adjustmentReference String?`; `metadata Json?`; `createdAt DateTime @default(now())`; `updatedAt DateTime @updatedAt`. Add `@@index([status])`, `@@index([settlementReference])`, `@@map("settlement_disputes")`. Do NOT add a `@@unique` on `settlementReference` — a settlement legitimately has zero or one non-terminal dispute at a time, but historical (terminal) disputes may accumulate against the same reference over time; that uniqueness is enforced in application code in `19-03-PLAN.md`, not the schema (mirrors `SettlementSplitTier`'s own comment explaining why its analogous constraint is NOT a bare `@@unique`). Do NOT use a Prisma `enum` for `status` — match `AdminReviewFlag`'s plain-`String`-with-comment convention exactly (the codebase's stated preference for flexibility on this specific field). Use `Decimal` (not `Float`) for `requestedAdjustmentNgn`, matching `SettlementSplitTier`'s percentage-column convention for money-adjacent values.

    In the `User` model, add `settlementDisputesRaised SettlementDispute[]` to the relation array, directly after `reviews Review[]` (line 274). No `@relation` name is needed on either side — `SettlementDispute` has only one FK field pointing at `User` (`raisedByUserId`), so Prisma infers the relation unambiguously (same as `AuditLog.user`/`User.auditLogs`).

    From `backend/`, run `npx prisma migrate dev --name add_settlement_dispute` to generate and apply the migration against the dev database — do not hand-write migration SQL. Run `npx prisma generate` if it does not run automatically as part of `migrate dev`.
  </action>
  <verify>
    <automated>cd backend && npx prisma validate</automated>
  </verify>
  <done>SettlementDispute model exists in schema.prisma with the exact fields/constraints above; User.settlementDisputesRaised relation array exists; a new migration directory exists under backend/prisma/migrations/ named *_add_settlement_dispute; the generated PrismaClient exposes a settlementDispute delegate.</done>
  <acceptance_criteria>
    - `grep -c "model SettlementDispute" backend/prisma/schema.prisma` returns 1
    - `grep -c "status.*String.*@default(\"OPEN\")" backend/prisma/schema.prisma` returns at least 2 (AdminReviewFlag's existing row + the new SettlementDispute row)
    - `grep -c "settlementDisputesRaised" backend/prisma/schema.prisma` returns 1, inside the `User` model's relation block
    - A directory matching `backend/prisma/migrations/*_add_settlement_dispute` exists and its `migration.sql` contains `CREATE TABLE` and `"settlement_disputes"`
    - `grep -c "settlementDispute" backend/node_modules/.prisma/client/index.d.ts` (or the equivalent generated client path) is greater than 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Verify migration applied cleanly and Prisma Client is live</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>
    backend/prisma/migrations/ (directory listing — confirms the naming convention `<timestamp>_<name>` other recent migrations follow, e.g. `20260719152059_add_settlement_split_tier`)
  </read_first>
  <action>
    This is the mandatory schema-push gate: confirm the migration created by Task 1 is fully applied to the dev database and the Prisma Client reflects it BEFORE any downstream plan (19-03, which references `prisma.settlementDispute.*` directly) can be started. Run `cd backend && npx prisma migrate status` — it must report no pending migrations. Run `cd backend && npx prisma generate` once more to guarantee the client is fresh (idempotent, safe to rerun). Do not proceed to author any application code against `prisma.settlementDispute` in this plan — that is out of scope here and belongs to `19-03-PLAN.md`.
  </action>
  <verify>
    <automated>cd backend && npx prisma migrate status</automated>
  </verify>
  <done>`npx prisma migrate status` reports "Database schema is up to date" with zero pending migrations; `settlement_disputes` table exists in the dev database.</done>
  <acceptance_criteria>
    - `cd backend && npx prisma migrate status` exits 0 and its output contains no "following migration(s) have not yet been applied" text
    - A raw check confirms the table exists, e.g. `cd backend && npx prisma db execute --stdin <<< "SELECT 1 FROM settlement_disputes LIMIT 1;"` (or equivalent `psql`/Prisma Studio check) does not error with "relation does not exist"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator → `prisma migrate dev` | One-off migration run manually by a developer/operator with direct DB/shell access, not exposed via HTTP — no runtime code is introduced by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-19-00a | Tampering | `SettlementDispute.status` (plain String, no DB-level CHECK constraint) | accept | matches the existing `AdminReviewFlag.status` precedent — application-layer state-machine guards (added in `19-03-PLAN.md`) are the enforcement point, not a DB constraint; consistent with every other status-machine model in this schema |
| T-19-00b | Repudiation | `settlement_disputes` table, no runtime write path yet | accept | this plan only creates the schema; no code path can write to this table until `19-03-PLAN.md` ships, so there is no exposure introduced by this plan alone |
</threat_model>

<verification>
- `cd backend && npx prisma validate` passes
- `cd backend && npx prisma migrate status` shows no pending migrations
- `settlement_disputes` table exists in the dev database with the exact columns listed in `<interfaces>`
- Full backend suite still green: `cd backend && npm run test`
</verification>

<success_criteria>
- `SettlementDispute` model exists in `schema.prisma`, migration applied to the dev database
- `User.settlementDisputesRaised` relation exists
- Prisma Client regenerated and exposes `prisma.settlementDispute`
</success_criteria>

<output>
After completion, create `.planning/phases/19-settlement-dispute-adjustment-workflow/19-01-SUMMARY.md`
</output>
