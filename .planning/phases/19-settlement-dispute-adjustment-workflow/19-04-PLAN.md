---
phase: 19-settlement-dispute-adjustment-workflow
plan: 04
type: execute
wave: 3
depends_on: ["19-03"]
files_modified:
  - backend/src/modules/settlement-disputes/settlement-disputes.controller.ts
  - backend/src/modules/settlement-disputes/settlement-disputes.module.ts
  - backend/src/app.module.ts
  - backend/src/modules/settlement-disputes/__tests__/settlement-disputes.e2e-spec.ts
autonomous: true
requirements: [SETTLE-10a, SETTLE-10b, SETTLE-10c, SETTLE-10d, SETTLE-10e]

must_haves:
  truths:
    - "SUPER_ADMIN-authenticated API clients can raise / list+queue / get / review / resolve / dismiss disputes over HTTP — no other role can reach any of these routes (D-02, D-06)"
    - "The full dispute lifecycle (raise → review → resolve, and BLOCKED → retry → resolved) works end-to-end through the REAL SettlementService + SettlementDisputesService pair, not just via mocked units"
    - "No new web admin UI page ships this phase — backend REST only (D-06)"
  artifacts:
    - path: "backend/src/modules/settlement-disputes/settlement-disputes.controller.ts"
      provides: "admin/settlement-disputes routes, SUPER_ADMIN-only"
      contains: "@Controller('admin/settlement-disputes')"
    - path: "backend/src/modules/settlement-disputes/settlement-disputes.module.ts"
      provides: "SettlementDisputesModule"
      contains: "class SettlementDisputesModule"
    - path: "backend/src/app.module.ts"
      provides: "SettlementDisputesModule registered in the root imports array"
      contains: "SettlementDisputesModule"
  key_links:
    - from: "backend/src/modules/settlement-disputes/settlement-disputes.controller.ts"
      to: "backend/src/modules/settlement-disputes/settlement-disputes.service.ts"
      via: "constructor-injected SettlementDisputesService, one method call per route"
      pattern: "this\\.service\\.(raise|findQueue|findById|moveToReview|resolve|dismiss)"
    - from: "backend/src/app.module.ts"
      to: "backend/src/modules/settlement-disputes/settlement-disputes.module.ts"
      via: "SettlementDisputesModule in the root @Module imports array"
      pattern: "SettlementDisputesModule"
---

<objective>
Expose `SettlementDisputesService` (built in `19-03-PLAN.md`) over HTTP: a single `SettlementDisputesController` under `admin/settlement-disputes`, gated `SUPER_ADMIN`-only at the class level (D-02 — no `STATE_ADMIN` this phase, unlike `ReviewsAdminController`'s broader role list), a thin `SettlementDisputesModule`, and registration in the root `AppModule`. Then prove the full stack wired correctly with an end-to-end regression test running the REAL `SettlementService` + REAL `SettlementDisputesService` together (not mocked units in isolation).

Purpose: this is the plan that actually makes SETTLE-10a-e reachable by a `SUPER_ADMIN` operator — everything built in `19-01`/`19-02`/`19-03` is inert until this plan wires it up. D-06 confirms this phase ships backend API only — no new web admin page.

Output: `SettlementDisputesController`, `SettlementDisputesModule`, `AppModule` registration, and a passing end-to-end regression spec covering the full dispute lifecycle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-settlement-dispute-adjustment-workflow/19-CONTEXT.md
@.planning/phases/19-settlement-dispute-adjustment-workflow/19-PATTERNS.md

<interfaces>
<!-- SettlementDisputesService methods, from 19-03-PLAN.md — this controller wires each route to exactly one call. -->

```typescript
// backend/src/modules/settlement-disputes/settlement-disputes.service.ts
raise(actorUserId: string, dto: RaiseDisputeDto): Promise<SettlementDispute>
findQueue(opts: { status?: string; page?: number; limit?: number }): Promise<{ data: any[]; pagination: any }>
findById(disputeId: string): Promise<SettlementDispute>
moveToReview(disputeId: string, actorUserId: string): Promise<SettlementDispute>
resolve(disputeId: string, actorUserId: string, dto: ResolveDisputeDto): Promise<SettlementDispute>
dismiss(disputeId: string, actorUserId: string, dto: ResolveDisputeDto): Promise<SettlementDispute>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettlementDisputesController + SettlementDisputesModule + AppModule registration</name>
  <files>backend/src/modules/settlement-disputes/settlement-disputes.controller.ts, backend/src/modules/settlement-disputes/settlement-disputes.module.ts, backend/src/app.module.ts</files>
  <read_first>
    backend/src/modules/reviews/reviews.controller.ts (lines 86-143 — `ReviewsAdminController`'s route/guard shape to mirror; lines 1-28 — import block to mirror, adjusted for this module's relative path depth)
    backend/src/modules/admin/admin.controller.ts (lines 1-20 — `SUPER_ADMIN`-only class-level `@Roles` precedent from Phase 18, the tighter match than Reviews' broader `LGA_ADMIN, STATE_ADMIN, SUPER_ADMIN` list)
    backend/src/modules/reviews/reviews.module.ts (full file — module shape: no `imports` array needed, `PrismaModule`/`CommonModule` are both `@Global()`)
    backend/src/app.module.ts (full file — import list lines 1-36, `imports` array lines 38-72; `AdminModule` at line 62 is the insertion anchor)
    backend/src/modules/settlement-disputes/settlement-disputes.service.ts (Task output from `19-03-PLAN.md` — exact method signatures to wire, see `<interfaces>` above)
  </read_first>
  <action>
    Create `backend/src/modules/settlement-disputes/settlement-disputes.controller.ts`. Imports: `Body, Controller, Get, HttpCode, HttpStatus, Param, Post, Query, UseGuards, DefaultValuePipe, ParseIntPipe` from `@nestjs/common`; `ApiBearerAuth, ApiOperation, ApiQuery, ApiResponse, ApiTags` from `@nestjs/swagger`; `SettlementDisputesService` from `'./settlement-disputes.service'`; `RaiseDisputeDto` from `'./dto/raise-dispute.dto'`; `ResolveDisputeDto` from `'./dto/resolve-dispute.dto'`; `JwtAuthGuard` from `'../auth/guards/jwt-auth.guard'`; `RolesGuard` from `'../../common/guards/roles.guard'`; `Roles` from `'../../common/decorators/roles.decorator'`; `CurrentUser` from `'../../common/decorators/current-user.decorator'`; `UserRole` from `'../../common/enums/user-role.enum'`.

    Class-level decorators, ONE controller (unlike Reviews' two-controller split — D-02/D-06 mean every route here is `SUPER_ADMIN`-only, no separate citizen-facing surface): `@ApiTags('admin')`, `@ApiBearerAuth()`, `@UseGuards(JwtAuthGuard, RolesGuard)`, `@Roles(UserRole.SUPER_ADMIN)`, `@Controller('admin/settlement-disputes')`. No per-route `@Roles` overrides needed — every route in this controller is `SUPER_ADMIN`-only.

    Routes: `@Post() @HttpCode(HttpStatus.CREATED) raise(@CurrentUser() user: { userId: string }, @Body() dto: RaiseDisputeDto) { return this.service.raise(user.userId, dto); }`; `@Get('queue') getQueue(@Query('status') status?: string, @Query('page', new DefaultValuePipe(1), ParseIntPipe) page?: number, @Query('limit', new DefaultValuePipe(24), ParseIntPipe) limit?: number) { return this.service.findQueue({ status, page, limit }); }`; `@Get(':id') getById(@Param('id') id: string) { return this.service.findById(id); }`; `@Post(':id/review') @HttpCode(HttpStatus.OK) review(@Param('id') id: string, @CurrentUser() user: { userId: string }) { return this.service.moveToReview(id, user.userId); }`; `@Post(':id/resolve') @HttpCode(HttpStatus.OK) resolve(@Param('id') id: string, @CurrentUser() user: { userId: string }, @Body() dto: ResolveDisputeDto) { return this.service.resolve(id, user.userId, dto); }`; `@Post(':id/dismiss') @HttpCode(HttpStatus.OK) dismiss(@Param('id') id: string, @CurrentUser() user: { userId: string }, @Body() dto: ResolveDisputeDto) { return this.service.dismiss(id, user.userId, dto); }`. Add `@ApiOperation`/`@ApiResponse` decorators on each route matching `reviews.controller.ts`'s documentation density (summary + 200/404/409 response descriptions where applicable).

    Create `backend/src/modules/settlement-disputes/settlement-disputes.module.ts` mirroring `reviews.module.ts`'s shape exactly: `@Module({ controllers: [SettlementDisputesController], providers: [SettlementDisputesService], exports: [SettlementDisputesService] }) export class SettlementDisputesModule {}` — no `imports` array (`PrismaModule` and `CommonModule`, which exports `SettlementService`, are both `@Global()`).

    In `backend/src/app.module.ts`, add `import { SettlementDisputesModule } from './modules/settlement-disputes/settlement-disputes.module';` to the import block (grouped near the other `modules/*` imports, e.g. directly after the `AdminModule` import line), and insert `SettlementDisputesModule,` into the root `@Module({ imports: [...] })` array immediately after `AdminModule,` (before `MinistryModule,`) — matching the logical grouping of settlement-adjacent admin modules.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>SettlementDisputesController exposes 6 routes under admin/settlement-disputes, all SUPER_ADMIN-gated at the class level; SettlementDisputesModule registers cleanly; AppModule imports it; project compiles with zero TypeScript errors.</done>
  <acceptance_criteria>
    - `grep -c "@Roles(UserRole.SUPER_ADMIN)" backend/src/modules/settlement-disputes/settlement-disputes.controller.ts` returns exactly 1 (class-level only — no per-route overrides needed since D-02 makes every route SUPER_ADMIN-only)
    - `grep -c "@Controller('admin/settlement-disputes')" backend/src/modules/settlement-disputes/settlement-disputes.controller.ts` returns 1
    - `grep -c "@Post\|@Get" backend/src/modules/settlement-disputes/settlement-disputes.controller.ts` returns at least 6 (raise, queue, getById, review, resolve, dismiss)
    - `grep -c "SettlementDisputesModule" backend/src/app.module.ts` returns 2 (one import statement + one entry in the `imports` array)
    - `cd backend && npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: End-to-end regression — full dispute lifecycle through the real SettlementService + SettlementDisputesService pair</name>
  <files>backend/src/modules/settlement-disputes/__tests__/settlement-disputes.e2e-spec.ts</files>
  <read_first>
    backend/src/modules/tour-bookings/__tests__/wallet-invariant.e2e-spec.ts (full file — the exact "real service classes wired via `Test.createTestingModule`, only `PrismaService`/`RefundService`/`PaystackService` mocked at the boundary" pattern to replicate for this spec)
    backend/src/common/services/__tests__/settlement.service.spec.ts (`makeService()` lines 131-157 — `SettlementService.onModuleInit()` must be invoked once before any `settle()`/`adjust()` call to resolve the system wallet; `wireTransaction()` lines 85-127 — the `$transaction` mock-capture pattern to reuse so sequential `settle()` + `adjust()` calls in the SAME test see consistent wallet state)
    backend/src/modules/settlement-disputes/settlement-disputes.service.ts (real class this spec wires, from `19-03-PLAN.md`)
  </read_first>
  <behavior>
    - Test: happy path — `settle()` a fixture single-earner settlement (2 recipients: `'DRIVER'` + `'MINISTRY'`) via the REAL `SettlementService.settle()`, then `raise()` a dispute via the REAL `SettlementDisputesService` against a mocked `settlementSplitTier.findFirst` resolving a DIFFERENT split than what was paid, `moveToReview()`, then `resolve()` — asserts the dispute ends `RESOLVED` with `adjustmentReference` set, exactly 2 new `-ADJ-` `Transaction` rows exist with the expected deltas, and `AuditLog.create` was called 3 times (`RAISED`, `MOVED_TO_REVIEW`, `RESOLVED`)
    - Test: BLOCKED → retry → RESOLVED (D-05) — `raise()` then `resolve()` directly from `OPEN` (skipping `moveToReview()`) where the computed earner debit exceeds the mocked wallet's current balance — asserts the dispute ends `BLOCKED`, NO `-ADJ-` `Transaction` row was committed, `AuditLog.create` was called for `RAISED` + `BLOCKED`. Then top up the mocked wallet's balance and call `resolve()` again on the same (now `BLOCKED`) dispute — asserts it now ends `RESOLVED` with the `-ADJ-` row present
    - Test: dismiss (SETTLE-10e completeness) — `raise()` then `dismiss()` an `OPEN` dispute — asserts `status: 'DISMISSED'`, `settlementService.adjust` (via `jest.spyOn` on the REAL instance) was never invoked, and `AuditLog.create` was called for `RAISED` + `DISMISSED`
  </behavior>
  <action>
    Create `backend/src/modules/settlement-disputes/__tests__/settlement-disputes.e2e-spec.ts` following `wallet-invariant.e2e-spec.ts`'s exact structure. Build a `MockPrisma` covering everything both real services touch: `transaction: { findFirst, findMany, create }`, `wallet: { findUnique, update, upsert }`, `user: { upsert }`, `settlementSplitTier: { findFirst }`, `settlementDispute: { findFirst, findUnique, create, update }`, `auditLog: { create }`, `$transaction`, `$executeRaw`. Mock `RefundService` as `{ refund: jest.fn() }` (only invoked on a `settle()` failure path, not expected to fire in these happy/BLOCKED/dismiss scenarios). Wire `$transaction` via a `wireTransaction()` helper adapted from `settlement.service.spec.ts`/`wallet-invariant.e2e-spec.ts` — capture wallet balances in a shared `Record<string, number>` so a `settle()` call followed by a LATER `adjust()` call in the SAME test sees the wallet balance `settle()` actually left behind (not a fresh reset).

    Build the `TestingModule` with `providers: [{ provide: PrismaService, useValue: mockPrisma }, { provide: RefundService, useValue: mockRefund }, SettlementService, SettlementDisputesService]` — `SettlementService` and `SettlementDisputesService` are provided as their REAL classes (no `useValue`/`useClass` override needed beyond NestJS's default class-token registration), so Nest's DI container wires the real `SettlementDisputesService` to the real `SettlementService` instance, which in turn receives the mocked `PrismaService`/`RefundService`. After `moduleRef.compile()`, call `await settlementService.onModuleInit()` once (resolves + caches the system wallet id) before running any test.

    Implement the 3 scenarios listed in `<behavior>` above. For the happy-path scenario, use `jest.spyOn(settlementService, 'resolveSplit')`-free approach — instead mock the underlying `mockPrisma.settlementSplitTier.findFirst` to return a specific tier row (the REAL `resolveSplit()` implementation runs against it), proving the full stack including `resolveSplit()`'s own logic, not just a stubbed return value.
  </action>
  <verify>
    <automated>cd backend && npm run test -- settlement-disputes.e2e</automated>
  </verify>
  <done>3 end-to-end scenarios pass, proving raise→review→resolve, BLOCKED→retry→RESOLVED, and dismiss all wire correctly through the REAL SettlementService + SettlementDisputesService pair (not mocked in isolation).</done>
  <acceptance_criteria>
    - `backend/src/modules/settlement-disputes/__tests__/settlement-disputes.e2e-spec.ts` exists and its `TestingModule` providers array includes `SettlementService` and `SettlementDisputesService` as real class tokens (not `useValue` mocks for either)
    - `grep -ci "BLOCKED" backend/src/modules/settlement-disputes/__tests__/settlement-disputes.e2e-spec.ts` returns at least 1 (the retry scenario)
    - `grep -c "onModuleInit" backend/src/modules/settlement-disputes/__tests__/settlement-disputes.e2e-spec.ts` returns at least 1
    - `cd backend && npm run test -- settlement-disputes.e2e` exits 0
    - Full backend suite green: `cd backend && npm run test`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin API client → `SettlementDisputesController` (`admin/settlement-disputes/*`) | Authenticated `SUPER_ADMIN` JWT boundary — the most money-sensitive write surface this phase introduces (it can trigger a real wallet debit via `resolve()` → `adjust()`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-19-09 | Elevation of Privilege | `SettlementDisputesController` (all 6 routes) | mitigate | `@Roles(UserRole.SUPER_ADMIN)` at the class level, no `STATE_ADMIN`/`LGA_ADMIN` inclusion anywhere (D-02) — tighter than `ReviewsAdminController`'s broader role list, matching `AdminController`'s Phase-18 money-adjacent precedent |
| T-19-10 | Information Disclosure | `GET admin/settlement-disputes/queue` and `GET admin/settlement-disputes/:id` | accept | responses include `raisedBy` user identity (`id`/`firstName`/`lastName` only, no PII beyond what `ReviewsService`'s existing include projection already exposes to the same `SUPER_ADMIN` role) — no NIN/BVN/wallet-balance field is ever included in a dispute response |
| T-19-11 | Repudiation | end-to-end lifecycle (`raise`/`review`/`resolve`/`dismiss`) | mitigate | proven in Task 2's e2e regression that every transition writes its `AuditLog` row through the REAL wiring, not just in isolated unit mocks — closes the gap between "the service claims to audit" and "the full stack actually does" |
</threat_model>

<verification>
- `cd backend && npx tsc --noEmit -p tsconfig.json` compiles cleanly
- `cd backend && npm run test -- settlement-disputes.e2e` passes (3 scenarios)
- `cd backend && npm run test -- settlement-disputes.service` still passes (no regression from Task 1's wiring)
- Full backend suite green: `cd backend && npm run test`
- **Manual-only (matches Phase 18's `18-04` precedent):** call the new endpoints with a `SUPER_ADMIN` token (expect 200/201) and a non-`SUPER_ADMIN` token, e.g. `STATE_ADMIN` (expect 403), against a running dev server — role-gating correctness at the live-request level is not fully provable by unit-level guard mocks alone
</verification>

<success_criteria>
- `POST/GET admin/settlement-disputes*` — 6 routes, all `SUPER_ADMIN`-only, wired to `SettlementDisputesService`
- `SettlementDisputesModule` registered in `AppModule`
- End-to-end regression proves the full lifecycle (raise/review/resolve, BLOCKED retry, dismiss) works through the real service pair
- No new web admin UI page ships this phase (D-06)
</success_criteria>

<output>
After completion, create `.planning/phases/19-settlement-dispute-adjustment-workflow/19-04-SUMMARY.md`
</output>
