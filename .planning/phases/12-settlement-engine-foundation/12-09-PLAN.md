---
phase: 12-settlement-engine-foundation
plan: 09
type: execute
wave: 3
depends_on: ["12-03", "12-04", "12-05", "12-06", "12-07", "12-08"]
files_modified:
  - backend/src/common/services/settlement.service.ts
  - backend/src/common/controllers/settlement.controller.ts
  - backend/src/modules/tour-bookings/tour-settlement.service.ts
  - backend/src/modules/marketplace/marketplace.service.ts
  - backend/src/modules/events/events.service.ts
  - backend/src/modules/studio/studio.service.ts
  - backend/src/modules/stays/stays.service.ts
autonomous: true
requirements: [SETTLE-01, SETTLE-08]

must_haves:
  truths:
    - "The full backend test suite passes after all five settlement callers (Tour, Marketplace, Events, Studio, Stays) and the statement controller are wired"
    - "Every settlement caller resolves recipient wallet ids exclusively from server-side domain data (Vendor.userId, Event.organizerId, Property.hostId, PlatformConfig) — never from Paystack webhook metadata"
    - "The P2002 benign-replay guard and the never-cached Ministry wallet resolution are present in the shipped SettlementService"
  artifacts: []
  key_links: []
---

<objective>
Close the phase with a full-suite regression run and a source-level security audit confirming the four blocking threats called out for this phase (idempotency/replay, race conditions, IDOR, drift/rounding) are actually mitigated in the shipped code, not just described in earlier plans' threat models. This wave exists because Plan 12-03 refactored a component with 12 pre-existing tested scenarios, and Plans 12-04 through 12-08 each independently wired a new caller onto the same shared service — a full-suite run is the only way to catch a cross-plan interaction regression before the phase is marked complete.

Purpose: SETTLE-08's "automated test asserts N-way split calculations sum exactly... across a wide range of non-round amounts" is proven per-caller in Plans 12-01/12-04 through 12-07's specs; this plan is the cross-cutting confirmation that nothing broke when all five callers coexist, plus a grep-based audit that closes out the phase's mandatory threat model requirement with evidence, not assertion.

Output: a green full backend test suite; a written confirmation (in the SUMMARY) of each audit finding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-settlement-engine-foundation/12-CONTEXT.md
@.planning/phases/12-settlement-engine-foundation/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full backend regression suite — fix any cross-plan interaction failures</name>
  <files>
    backend/src/common/services/settlement.service.ts
    backend/src/modules/tour-bookings/tour-settlement.service.ts
    backend/src/modules/marketplace/marketplace.service.ts
    backend/src/modules/events/events.service.ts
    backend/src/modules/studio/studio.service.ts
    backend/src/modules/stays/stays.service.ts
  </files>
  <read_first>
    backend/src/common/services/settlement.service.ts
  </read_first>
  <action>
    Run `cd backend && npm test` (the full Jest suite, not a scoped pattern). If every suite
    passes, no code changes are needed for this task — proceed directly to acceptance criteria
    verification. If any suite fails, diagnose whether the failure originates from this phase's
    changes (most likely: a fixture/mock shape drift between two plans that both touched the same
    spec file's shared constants, or a genuine logic bug surfaced only when all five settlement
    callers' specs run in the same Jest process). Fix the failure in the specific service or spec
    file where it originates — do not weaken an assertion to make a test pass; fix the
    implementation if the implementation is wrong, or fix a genuinely stale fixture if the fixture
    is wrong. Re-run `npm test` until the full suite is green. If a failure cannot be resolved
    without revisiting a design decision locked in `12-CONTEXT.md`, stop and report it rather than
    silently deviating from a locked decision.
  </action>
  <verify>
    <automated>cd backend && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with zero failing suites
    - `tour-settlement.service.spec.ts` still reports its full original scenario count passing
    - `settlement.service.spec.ts`, `marketplace.service.spec.ts`, `events.service.spec.ts`, `studio.service.spec.ts`, `stays.service.spec.ts`, `settlement.controller.spec.ts` all pass
  </acceptance_criteria>
  <done>Full backend test suite is green with all five settlement callers and the statement controller coexisting.</done>
</task>

<task type="auto">
  <name>Task 2: Source-level security audit of the four blocking threats</name>
  <files>audit-only, no files modified — read-only grep/review, findings recorded in 12-09-SUMMARY.md</files>
  <read_first>
    backend/src/common/services/settlement.service.ts
    backend/src/common/controllers/settlement.controller.ts
    backend/src/modules/tour-bookings/tour-settlement.service.ts
    backend/src/modules/marketplace/marketplace.service.ts
    backend/src/modules/events/events.service.ts
    backend/src/modules/studio/studio.service.ts
    backend/src/modules/stays/stays.service.ts
  </read_first>
  <action>
    Perform and record (in the plan's SUMMARY) the result of each of these four grep-based checks,
    matching the four threats explicitly flagged as blocking for this phase:

    1. **Idempotency/replay (duplicate webhook double-crediting):** confirm
       `Prisma.PrismaClientKnownRequestError` and `'P2002'` both appear in
       `backend/src/common/services/settlement.service.ts`'s `settle()` catch block — run `grep -n
       "P2002" backend/src/common/services/settlement.service.ts` and confirm at least one match
       sits inside a `catch` block (read the surrounding lines, not just the grep hit).

    2. **Race conditions on concurrent settlement of the same payment:** confirm every recipient
       wallet write in `settlement.service.ts` is preceded by a raw `SELECT ... FOR UPDATE` —
       `grep -c "FOR UPDATE" backend/src/common/services/settlement.service.ts` should return 2
       (one for the recipient loop, one for the platform wallet row).

    3. **IDOR on the settlement statement endpoint:** confirm
       `backend/src/common/controllers/settlement.controller.ts` never passes a client-supplied
       `walletId` to `settlementService.getStatement()` for a non-admin role — re-read the
       `isAdmin && walletId` conditional and confirm the `else` branch always re-resolves via
       `prisma.wallet.findUnique({ where: { userId: user.userId } })`.

    4. **Drift/rounding exploitation:** confirm the `Math.abs(drift) > 0.02` assertion in
       `settlement.service.ts` runs BEFORE the `$transaction` call (not inside it, where a
       mid-transaction throw could leave partial writes) — read the method body top-to-bottom and
       confirm the ordering.

    Also confirm, across all five caller files (`tour-settlement.service.ts`,
    `marketplace.service.ts`, `events.service.ts`, `studio.service.ts`, `stays.service.ts`), that
    no `recipients` array entry's `walletId` is ever sourced from `payload.metadata` — every
    `walletId` must trace back to a `prisma.wallet.findUnique` call keyed by a domain-model FK
    (`Vendor.userId`, `Event.organizerId`, `Property.hostId`, `TourGuide.userId`,
    `tour.government_wallet_user_id`). `grep -n "payload.metadata" backend/src/modules/*/*.service.ts`
    and manually confirm none of the matches feed into a `walletId` field.

    Report each of the five findings explicitly in the plan's SUMMARY.md — this is the phase's
    closing evidence for its mandatory `<threat_model>` requirement, not a rubber-stamp.
  </action>
  <verify>
    <automated>cd backend && grep -n "P2002" src/common/services/settlement.service.ts && grep -c "FOR UPDATE" src/common/services/settlement.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - All five audit findings (P2002 catch, SELECT FOR UPDATE count = 2, IDOR self-resolution, drift-check ordering, no webhook-metadata-sourced walletId) are confirmed present and documented in the SUMMARY
    - Any finding that is NOT confirmed present is fixed in the relevant file before this task is marked done
  </acceptance_criteria>
  <done>All four blocking threats from the phase's security requirement are confirmed mitigated in the shipped code, with grep-verifiable evidence recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| This plan is a verification pass, not a new code surface | No new trust boundary is introduced; this plan confirms boundaries established in Plans 12-01 through 12-08 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-20 | Repudiation | Cross-plan regression (5 callers sharing one engine, refactor of a 12-scenario-tested component) | mitigate | Full-suite run (Task 1) is the closing gate — any interaction bug between plans is caught here before the phase is marked verified |
| T-12-21 | (Audit, not a new threat) | All four blocking threats from earlier plans | mitigate | Task 2 re-confirms each mitigation is actually present in the shipped code via grep-verifiable evidence, closing the phase's mandatory threat-model requirement with proof rather than description |
</threat_model>

<verification>
- `cd backend && npm test` — full suite green
- Five audit findings from Task 2 documented in SUMMARY.md
</verification>

<success_criteria>
- Full backend test suite passes with all Phase 12 changes present simultaneously
- All four blocking security threats (replay, race condition, IDOR, drift) are confirmed mitigated with grep-verifiable evidence, not just design-time claims
</success_criteria>

<output>
After completion, create `.planning/phases/12-settlement-engine-foundation/12-09-SUMMARY.md`
</output>
