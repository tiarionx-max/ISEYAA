---
phase: 04-delivery-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/package.json
  - backend/prisma/schema.prisma
autonomous: true
requirements:
  - DELIVERY-01
  - DELIVERY-02
  - DELIVERY-03
  - DELIVERY-04
  - DELIVERY-05
  - DELIVERY-06

must_haves:
  truths:
    - "Prisma schema declares DeliveryOrderStatus enum"
    - "Prisma schema declares DeliveryRider, DeliveryOrder, DeliveryEvent models"
    - "User model carries deliveryRiderProfile and senderOrders back-relations"
    - "mobile/package.json declares expo-image-picker ~15.0.7"
  artifacts:
    - path: backend/prisma/schema.prisma
      provides: "DeliveryRider, DeliveryOrder, DeliveryEvent models + DeliveryOrderStatus enum"
      contains: "model DeliveryRider"
    - path: mobile/package.json
      provides: "expo-image-picker dependency for proof-of-delivery photo capture"
      contains: "expo-image-picker"
  key_links:
    - from: backend/src/modules/delivery/delivery.service.ts (future)
      to: backend/prisma/schema.prisma
      via: "prisma.deliveryOrder / prisma.deliveryRider / prisma.deliveryEvent"
      pattern: "prisma\\.(deliveryOrder|deliveryRider|deliveryEvent)"
    - from: mobile/app/(tabs)/rider.tsx (future)
      to: expo-image-picker
      via: "ImagePicker.launchImageLibraryAsync / launchCameraAsync"
      pattern: "ImagePicker\\.(launchImageLibraryAsync|launchCameraAsync)"
---

<objective>
Establish the two foundational prerequisites for the Delivery Module: (1) extend the Prisma schema with the three new delivery models and one new enum; (2) add expo-image-picker to mobile dependencies. No backend code or DB push is performed here — those belong to Plans 02 and 03 respectively.

Purpose: Every subsequent plan in this phase depends on Prisma having the delivery models defined (so Plan 03 can push them) and the mobile app having expo-image-picker declared (so Plan 07 can use it). These are pure file changes — no database or runtime effects.

Output: Schema file with DeliveryRider + DeliveryOrder + DeliveryEvent models and DeliveryOrderStatus enum. mobile/package.json with expo-image-picker ~15.0.7.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-delivery-module/04-RESEARCH.md
@.planning/phases/04-delivery-module/04-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing DriverStatus enum in schema.prisma (lines 148-153) — DeliveryRider.status reuses this enum: -->
enum DriverStatus {
  PENDING_REVIEW
  APPROVED
  SUSPENDED
  REJECTED
}

<!-- Existing User model back-relations already added (Phase 3): -->
driverProfile   Driver?
riderTrips      Trip[]   @relation("RiderTrips")

<!-- New relations to add to User model: -->
deliveryRiderProfile  DeliveryRider?
senderOrders         DeliveryOrder[] @relation("SenderOrders")

<!-- DeliveryOrderStatus enum (new): -->
enum DeliveryOrderStatus {
  SEARCHING
  MATCHED
  COLLECTING
  IN_TRANSIT
  DELIVERED
  CANCELLED
  EXPIRED
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add expo-image-picker to mobile/package.json</name>
  <files>mobile/package.json</files>
  <read_first>
    - mobile/package.json (current dependencies — verify expo-image-picker is absent, confirm Expo SDK version ~51)
    - .planning/phases/04-delivery-module/04-RESEARCH.md (Standard Stack section — confirms ~15.0.7 for SDK 51)
  </read_first>
  <action>
    Run from repo root: `npm install expo-image-picker@~15.0.7 --workspace=mobile --save`.

    expo-image-picker 15.x is the correct range for Expo SDK 51. Version 16.x requires SDK 52+ and uses a different MediaTypeOptions API (array syntax vs enum). Do NOT use `npx expo install` here — use npm workspace install for consistency with the project toolchain.

    After install, verify `mobile/package.json` contains `"expo-image-picker": "~15.0.7"` (or the resolved compatible version) in the `dependencies` block.
  </action>
  <verify>
    <automated>node -e "const p=require('./mobile/package.json'); if(!p.dependencies['expo-image-picker']){console.error('MISSING expo-image-picker');process.exit(1)} console.log('OK:', p.dependencies['expo-image-picker'])"</automated>
  </verify>
  <acceptance_criteria>
    - `mobile/package.json` `dependencies` contains key `expo-image-picker` with value matching `~15.0.7` or compatible 15.x range
    - `npm ls expo-image-picker --workspace=mobile` exits 0 with no UNMET PEER warnings
    - No other dependencies in mobile/package.json are modified
  </acceptance_criteria>
  <done>
    expo-image-picker declared in mobile/package.json at SDK 51-compatible version. Existing deps unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend Prisma schema with DeliveryRider, DeliveryOrder, DeliveryEvent models + DeliveryOrderStatus enum</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>
    - backend/prisma/schema.prisma (read lines 148-220 for DriverStatus enum + User model back-relations; read from line 595 onward for Driver/Vehicle/Trip/TripEvent model patterns and @@map convention)
    - .planning/phases/04-delivery-module/04-PATTERNS.md (schema section — exact model definitions with all fields)
    - .planning/phases/04-delivery-module/04-RESEARCH.md (Prisma Schema Additions section — exact field names and types)
  </read_first>
  <action>
    Edit `backend/prisma/schema.prisma` and apply these changes:

    1. In `model User` (existing), add two new relation back-references inside the body immediately after the existing `riderTrips` relation field (line ~199), before the `@@map` line:
       - `deliveryRiderProfile  DeliveryRider?`
       - `senderOrders         DeliveryOrder[] @relation("SenderOrders")`

    2. After the last existing enum (locate by searching for the last `}` that closes an enum block), append one new enum:
       ```
       enum DeliveryOrderStatus {
         SEARCHING
         MATCHED
         COLLECTING
         IN_TRANSIT
         DELIVERED
         CANCELLED
         EXPIRED
       }
       ```

    3. After the last existing model in the file (after `TripEvent` model and its closing `}`), append three new models in this exact order:

       **model DeliveryRider:**
       - id String @id @default(uuid())
       - userId String @unique
       - user User @relation(fields: [userId], references: [id])
       - status DriverStatus @default(PENDING_REVIEW)   ← reuses existing DriverStatus enum
       - approvedById String?
       - approvedAt DateTime?
       - isOnline Boolean @default(false)
       - lastSeenAt DateTime?
       - avgRating Decimal @default(0)
       - totalDeliveries Int @default(0)
       - acceptanceRate Decimal @default(0)
       - metadata Json?
       - createdAt DateTime @default(now())
       - updatedAt DateTime @updatedAt
       - deletedAt DateTime?
       - orders DeliveryOrder[] @relation("RiderOrders")
       - @@map("delivery_riders")

       **model DeliveryOrder:**
       - id String @id @default(uuid())
       - senderId String
       - sender User @relation("SenderOrders", fields: [senderId], references: [id])
       - riderId String?
       - rider DeliveryRider? @relation("RiderOrders", fields: [riderId], references: [id])
       - pickupAddress String
       - pickupLat Decimal
       - pickupLng Decimal
       - dropoffAddress String
       - dropoffLat Decimal
       - dropoffLng Decimal
       - itemDescription String
       - weightKg Decimal
       - recipientPhone String   ← OTP is sent to this phone, NOT the sender's phone
       - fee Decimal?
       - platformFee Decimal?
       - riderEarnings Decimal?
       - status DeliveryOrderStatus @default(SEARCHING)
       - proofPhotoUrl String?
       - otpVerifiedAt DateTime?  ← set when POST /verify-otp succeeds; required before complete
       - requestedAt DateTime @default(now())
       - matchedAt DateTime?
       - collectedAt DateTime?
       - completedAt DateTime?
       - cancelReason String?
       - senderRating Int?
       - metadata Json?
       - createdAt DateTime @default(now())
       - updatedAt DateTime @updatedAt
       - deletedAt DateTime?
       - events DeliveryEvent[]
       - @@map("delivery_orders")

       **model DeliveryEvent:**
       - id String @id @default(uuid())
       - orderId String
       - order DeliveryOrder @relation(fields: [orderId], references: [id])
       - event String
       - metadata Json?
       - createdAt DateTime @default(now())
       - @@map("delivery_events")

    DO NOT run `prisma migrate` or `prisma db push` — that is the [BLOCKING] task in Plan 02.
  </action>
  <verify>
    <automated>cd backend && npx prisma format --schema=prisma/schema.prisma && npx prisma validate --schema=prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^model DeliveryRider " backend/prisma/schema.prisma` returns 1
    - `grep -c "^model DeliveryOrder " backend/prisma/schema.prisma` returns 1
    - `grep -c "^model DeliveryEvent " backend/prisma/schema.prisma` returns 1
    - `grep -c "^enum DeliveryOrderStatus " backend/prisma/schema.prisma` returns 1
    - `grep -c "deliveryRiderProfile" backend/prisma/schema.prisma` returns 1
    - `grep -c "senderOrders" backend/prisma/schema.prisma` returns 1
    - `grep -c "recipientPhone" backend/prisma/schema.prisma` returns 1
    - `grep -c "otpVerifiedAt" backend/prisma/schema.prisma` returns 1
    - `grep -c "@@map..\"delivery_riders\".." backend/prisma/schema.prisma` returns 1
    - `grep -c "@@map..\"delivery_orders\".." backend/prisma/schema.prisma` returns 1
    - `grep -c "@@map..\"delivery_events\".." backend/prisma/schema.prisma` returns 1
    - `npx prisma validate --schema=backend/prisma/schema.prisma` exits 0
  </acceptance_criteria>
  <done>
    Schema file contains 3 new models + 1 new enum + 2 new User relation fields. `prisma validate` exits 0. No DB push performed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema file → DB (future push) | Schema changes are file-only in this plan; no DB interaction occurs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | DeliveryOrder.recipientPhone field | mitigate | Field carries the recipient's phone for OTP dispatch; `@IsMobilePhone('en-NG')` validation enforced in RequestDeliveryDto (Plan 03) prevents invalid numbers reaching the DB |
| T-04-02 | Tampering | DeliveryOrder.status field | mitigate | `DeliveryOrderStatus` enum enforced at DB column level; service-layer state machine transitions validated in DeliveryService (Plan 05) |
| T-04-03 | Information Disclosure | expo-image-picker iOS permissions | mitigate | Permission strings added in Plan 07 (app.json); no camera/photo access is granted at package install time |
</threat_model>

<verification>
- `npx prisma validate --schema=backend/prisma/schema.prisma` passes
- `node -e "require('./mobile/package.json').dependencies['expo-image-picker']"` prints non-null version
- Schema contains all 3 new models and 1 new enum — `grep` commands above all return ≥ 1
</verification>

<success_criteria>
- mobile/package.json declares expo-image-picker at ~15.0.7 (SDK 51-compatible)
- Prisma schema declares DeliveryOrderStatus enum with 7 members (SEARCHING → EXPIRED)
- Prisma schema declares DeliveryRider model with DriverStatus relation and all required fields
- Prisma schema declares DeliveryOrder model including recipientPhone, otpVerifiedAt, proofPhotoUrl
- Prisma schema declares DeliveryEvent model with orderId FK
- User model includes deliveryRiderProfile and senderOrders back-relations
- `prisma validate` exits 0 — no relation or syntax errors
- No DB push attempted (Plan 02 owns the [BLOCKING] push)
</success_criteria>

<output>
After completion, create `.planning/phases/04-delivery-module/04-01-SUMMARY.md` documenting:
- expo-image-picker resolved version installed
- Models and enums added to schema (all field names)
- Confirmation that no DB push was performed
- Any deviations from plan (e.g. prisma format auto-corrected whitespace)
</output>
