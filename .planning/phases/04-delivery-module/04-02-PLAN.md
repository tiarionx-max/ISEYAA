---
phase: 04-delivery-module
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/seed.ts
autonomous: true
requirements:
  - DELIVERY-01
  - DELIVERY-05

must_haves:
  truths:
    - "delivery_riders, delivery_orders, delivery_events tables exist in PostgreSQL"
    - "Prisma Client TypeScript types include DeliveryRider, DeliveryOrder, DeliveryEvent accessors"
    - "PlatformConfig table contains 5 delivery_* keys with correct default values"
    - "delivery_platform_fee_pct is 20 (not hardcoded in service code)"
  artifacts:
    - path: backend/prisma/seed.ts
      provides: "5 delivery PlatformConfig rows seeded idempotently"
      contains: "delivery_platform_fee_pct"
  key_links:
    - from: backend/src/modules/delivery/delivery.service.ts (future)
      to: backend/prisma/seed.ts
      via: "delivery_platform_fee_pct read at runtime from platformConfig table"
      pattern: "platformConfig.*delivery_platform_fee_pct"
---

<objective>
[BLOCKING] Push the delivery schema changes to the database and regenerate the Prisma Client, then seed the 5 delivery PlatformConfig rows. No subsequent plan can use delivery Prisma types until both steps complete.

Purpose: Plan 03 writes test specs that import DeliveryService which imports Prisma types. Plans 05 onward write DeliveryService code that calls prisma.deliveryOrder, prisma.deliveryRider, prisma.deliveryEvent. None of this compiles until Prisma Client has the delivery models. The seed ensures delivery fee calculations never use hardcoded values — per CLAUDE.md "Platform fee source: Always from DB (platformConfig table), never hardcoded".

Output: 3 new tables in PostgreSQL. Prisma Client with delivery TypeScript types. 5 delivery PlatformConfig rows in DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-delivery-module/04-RESEARCH.md
@.planning/phases/03-transport-module/03-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Phase 3 seed pattern (backend/prisma/seed.ts lines 1268-1297) — copy this exact loop pattern: -->
const transportConfigs = [
  { key: 'transport_platform_fee_pct', value: 15 },
  // ... 10 more entries
];
for (const config of transportConfigs) {
  try {
    await prisma.platformConfig.upsert({
      where:  { key: config.key },
      update: { value: config.value },
      create: { key: config.key, value: config.value, isPublic: false },
    });
  } catch (err) {
    console.error(`  ✗ Failed to upsert ${config.key}:`, (err as Error).message);
  }
}

<!-- Delivery PlatformConfig keys to seed: -->
delivery_platform_fee_pct  = 20    (platform retains 20%; rider gets 80%)
delivery_base_fee          = 300   (₦300 base fee)
delivery_per_kg_rate       = 50    (₦50/kg above 2 kg free allowance)
delivery_match_radius_km   = 5     (initial GEOSEARCH radius)
delivery_otp_ttl_seconds   = 300   (5 minutes; matches auth OTP TTL)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING] Push delivery schema to database and regenerate Prisma Client</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>
    - backend/prisma/schema.prisma (confirm DeliveryRider, DeliveryOrder, DeliveryEvent models from Plan 01 are present)
    - .planning/phases/03-transport-module/03-02-SUMMARY.md (Windows EPERM workaround — use --no-engine flag if DLL lock occurs)
  </read_first>
  <action>
    Run these two commands in sequence from the `backend/` directory:

    1. `npx prisma db push` — syncs the three new delivery tables to the local PostgreSQL database. This is a non-destructive push (adding new tables only). Do NOT use `--accept-data-loss` unless prisma reports it is required.

    2. `npx prisma generate` — regenerates the Prisma Client TypeScript types. If this fails on Windows with EPERM (DLL file lock), use `npx prisma generate --no-engine` instead (see Phase 3 SUMMARY deviation — this is a known Windows issue).

    Verify the tables exist after push by running a quick count check:
    `node -e "const {PrismaClient}=require('@prisma/client'); const p=new PrismaClient(); Promise.all([p.deliveryRider.count(),p.deliveryOrder.count(),p.deliveryEvent.count()]).then(r=>console.log('counts',r)).finally(()=>p.$disconnect())"`

    If the DB push requires `DATABASE_URL` or `DIRECT_URL` environment variables, ensure they are set in `backend/.env` matching the local PostgreSQL connection (same pattern used in Phase 3 — see 03-02-SUMMARY.md).
  </action>
  <verify>
    <automated>cd backend && node -e "const {PrismaClient}=require('@prisma/client'); const p=new PrismaClient(); Promise.all([p.deliveryRider.count(),p.deliveryOrder.count(),p.deliveryEvent.count()]).then(r=>{console.log('OK',r);p.$disconnect()}).catch(e=>{console.error('FAIL',e.message);p.$disconnect();process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db push` exits 0 with message "Your database is now in sync with your Prisma schema"
    - `npx prisma generate` (or `--no-engine`) exits 0
    - Node count query returns array of three 0-counts (tables exist, empty) without error
    - `npx tsc --noEmit -p backend/tsconfig.json` exits 0 (Prisma Client types compile without error)
  </acceptance_criteria>
  <done>
    Tables delivery_riders, delivery_orders, delivery_events created in PostgreSQL. Prisma Client exports DeliveryRider, DeliveryOrder, DeliveryEvent TypeScript types. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Seed delivery PlatformConfig rows</name>
  <files>backend/prisma/seed.ts</files>
  <read_first>
    - backend/prisma/seed.ts (read lines 1260-1305 — transport PlatformConfig seed block to copy the loop pattern exactly)
  </read_first>
  <action>
    Edit `backend/prisma/seed.ts` — locate the transport PlatformConfig seed block (the `transportConfigs` array + loop) and immediately after its closing brace, add a new delivery seed block using the identical loop pattern:

    ```
    const deliveryConfigs = [
      { key: 'delivery_platform_fee_pct', value: 20 },
      { key: 'delivery_base_fee',         value: 300 },
      { key: 'delivery_per_kg_rate',      value: 50 },
      { key: 'delivery_match_radius_km',  value: 5 },
      { key: 'delivery_otp_ttl_seconds',  value: 300 },
    ];
    console.log('Seeding delivery PlatformConfig...');
    for (const config of deliveryConfigs) {
      try {
        await prisma.platformConfig.upsert({
          where:  { key: config.key },
          update: { value: config.value },
          create: { key: config.key, value: config.value, isPublic: false },
        });
        console.log(`  ✓ ${config.key} = ${config.value}`);
      } catch (err) {
        console.error(`  ✗ Failed to upsert ${config.key}:`, (err as Error).message);
      }
    }
    ```

    All 5 rows must have `isPublic: false` — these are server-side config values, never exposed via API directly (per CLAUDE.md "Platform fee source: Always from DB, never hardcoded").

    After editing the file, run the seed from the `backend/` directory:
    `npx ts-node prisma/seed.ts`

    The seed must be idempotent — running it twice should produce the same 5 rows without error.
  </action>
  <verify>
    <automated>cd backend && node -e "const {PrismaClient}=require('@prisma/client'); const p=new PrismaClient(); const keys=['delivery_platform_fee_pct','delivery_base_fee','delivery_per_kg_rate','delivery_match_radius_km','delivery_otp_ttl_seconds']; Promise.all(keys.map(k=>p.platformConfig.findUnique({where:{key:k}}))).then(rows=>{const missing=keys.filter((_,i)=>!rows[i]); if(missing.length){console.error('MISSING',missing);process.exit(1)} console.log('OK - all 5 delivery config rows present'); rows.forEach((r,i)=>console.log(' ',keys[i],r.value));p.$disconnect()}).catch(e=>{console.error('FAIL',e.message);p.$disconnect();process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/prisma/seed.ts` contains string `delivery_platform_fee_pct`
    - `backend/prisma/seed.ts` contains string `delivery_base_fee`
    - `backend/prisma/seed.ts` contains string `delivery_per_kg_rate`
    - All 5 delivery config keys exist in the `platform_configs` table with correct values
    - `delivery_platform_fee_pct` value is 20 (not 15, not 0.20 — integer percentage)
    - `delivery_base_fee` value is 300 (₦300)
    - `delivery_per_kg_rate` value is 50 (₦50/kg)
    - All 5 rows have `isPublic: false`
    - Running seed a second time exits 0 with no errors (idempotency confirmed)
  </acceptance_criteria>
  <done>
    5 delivery PlatformConfig rows exist in database. Seed is idempotent. delivery_platform_fee_pct = 20 confirmed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB push → local PostgreSQL only | Only `localhost:5432/iseyaa_dev` is modified; production Neon DB is not touched |
| PlatformConfig.isPublic = false | Fee config values are server-internal; not surfaced in GET /api/v1/delivery/fee-estimate response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | delivery_platform_fee_pct DB row | mitigate | Row is seeded with `isPublic: false`; only `LGA_ADMIN` can update via `PATCH /api/v1/admin/config/:key` (existing AdminController guard); DeliveryService reads at runtime — no hardcoded fallback in service code (20 is the seed value, not a code literal) |
| T-04-05 | Elevation of Privilege | DB push against production | accept | DATABASE_URL in backend/.env targets localhost only; Neon production branch requires separate DIRECT_URL (not set in local env); risk of accidental production push is negligible in dev context |
</threat_model>

<verification>
- `npx prisma db push` exits 0 — no schema drift
- Prisma Client count queries for deliveryRider/deliveryOrder/deliveryEvent return 0 without error
- All 5 delivery PlatformConfig keys present in DB with correct values
- `npx tsc --noEmit -p backend/tsconfig.json` exits 0
- Seed is idempotent (run twice, no errors)
</verification>

<success_criteria>
- Three new tables in local PostgreSQL: delivery_riders, delivery_orders, delivery_events
- Prisma Client TypeScript types include DeliveryRider, DeliveryOrder, DeliveryEvent
- PlatformConfig table has 5 delivery rows: delivery_platform_fee_pct=20, delivery_base_fee=300, delivery_per_kg_rate=50, delivery_match_radius_km=5, delivery_otp_ttl_seconds=300
- All 5 rows have isPublic=false
- TypeScript compilation passes after Prisma Client regeneration
</success_criteria>

<output>
After completion, create `.planning/phases/04-delivery-module/04-02-SUMMARY.md` documenting:
- db push output (tables created)
- prisma generate outcome (or --no-engine workaround if used)
- All 5 PlatformConfig keys and values confirmed
- Whether DIRECT_URL workaround was needed
</output>
