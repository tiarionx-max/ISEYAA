---
phase: 04-delivery-module
plan: 03
type: tdd
wave: 3
depends_on:
  - 04-02
files_modified:
  - backend/src/modules/delivery/dto/create-delivery-rider.dto.ts
  - backend/src/modules/delivery/dto/approve-delivery-rider.dto.ts
  - backend/src/modules/delivery/dto/rider-go-online.dto.ts
  - backend/src/modules/delivery/dto/request-delivery.dto.ts
  - backend/src/modules/delivery/dto/verify-delivery-otp.dto.ts
  - backend/src/modules/delivery/dto/complete-delivery.dto.ts
  - backend/src/modules/delivery/__tests__/delivery.service.spec.ts
  - backend/src/modules/delivery/__tests__/delivery.gateway.spec.ts
autonomous: true
requirements:
  - DELIVERY-01
  - DELIVERY-02
  - DELIVERY-03
  - DELIVERY-04
  - DELIVERY-05

must_haves:
  truths:
    - "All 6 delivery DTOs exist with correct class-validator decorators"
    - "RequestDeliveryDto includes recipientPhone validated with @IsMobilePhone('en-NG')"
    - "delivery.service.spec.ts has failing tests for all 5 service behaviors (DELIVERY-01 through DELIVERY-05)"
    - "delivery.gateway.spec.ts has a failing test for rider:location relay (DELIVERY-03)"
  artifacts:
    - path: backend/src/modules/delivery/dto/request-delivery.dto.ts
      provides: "Request body DTO for POST /delivery/orders"
      contains: "recipientPhone"
    - path: backend/src/modules/delivery/dto/complete-delivery.dto.ts
      provides: "Request body DTO for PATCH /delivery/orders/:id/complete"
      contains: "proofPhotoBase64"
    - path: backend/src/modules/delivery/__tests__/delivery.service.spec.ts
      provides: "Failing unit tests covering all 5 DELIVERY requirements"
      contains: "delivery.service"
    - path: backend/src/modules/delivery/__tests__/delivery.gateway.spec.ts
      provides: "Failing unit test for WebSocket location relay"
      contains: "delivery.gateway"
  key_links:
    - from: backend/src/modules/delivery/__tests__/delivery.service.spec.ts
      to: backend/src/modules/delivery/delivery.service.ts (future Plan 04)
      via: "import DeliveryService"
      pattern: "DeliveryService"
---

<objective>
TDD red step: create all 6 delivery DTOs and write failing test specs for DeliveryService and DeliveryGateway. Tests will fail because the implementation files do not exist yet — that is the expected state after this plan. Plan 04 (TDD green) writes the implementation.

Purpose: DTOs define the contract for all delivery endpoints. Test specs define the behavioral contract for DeliveryService. Written in this order — contracts before implementation — the specs force the implementation to be verifiable rather than retrofitted.

Output: 6 DTO files. 2 spec files with failing tests. `npx jest --testPathPattern=delivery --no-coverage` must fail with "Cannot find module" or similar import errors (red).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-delivery-module/04-PATTERNS.md
@.planning/phases/04-delivery-module/04-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Analog DTOs — read these before writing delivery DTOs: -->
<!-- backend/src/modules/transport/dto/request-ride.dto.ts -->
<!-- backend/src/modules/transport/dto/complete-trip.dto.ts -->
<!-- backend/src/modules/transport/dto/go-online.dto.ts -->
<!-- backend/src/modules/transport/dto/approve-driver.dto.ts -->
<!-- backend/src/modules/transport/dto/create-driver.dto.ts -->

<!-- Analog test specs — read these for mock structure: -->
<!-- backend/src/modules/transport/__tests__/transport.service.spec.ts (entire file) -->
<!-- backend/src/modules/transport/__tests__/transport.gateway.spec.ts (entire file) -->

<!-- WalletService.creditWallet signature (already updated in Phase 3): -->
async creditWallet(walletId: string, amount: number, reference: string, description: string, module = 'wallet', gateway: 'PAYSTACK' | 'FLUTTERWAVE' | 'INTERNAL' = 'PAYSTACK'): Promise<void>

<!-- S3Service.upload signature (confirmed in PATTERNS.md): -->
async upload(key: string, body: Buffer, contentType: string): Promise<string>

<!-- RedisService geo methods (Phase 3 additions): -->
async geoadd(key: string, lng: number, lat: number, member: string): Promise<void>
async geosearch(key: string, lng: number, lat: number, radiusKm: number): Promise<string[]>
async zrem(key: string, member: string): Promise<void>
</interfaces>
</context>

<feature>
  <name>Delivery Module — DTOs and TDD Red Specs</name>
  <files>
    backend/src/modules/delivery/dto/create-delivery-rider.dto.ts,
    backend/src/modules/delivery/dto/approve-delivery-rider.dto.ts,
    backend/src/modules/delivery/dto/rider-go-online.dto.ts,
    backend/src/modules/delivery/dto/request-delivery.dto.ts,
    backend/src/modules/delivery/dto/verify-delivery-otp.dto.ts,
    backend/src/modules/delivery/dto/complete-delivery.dto.ts,
    backend/src/modules/delivery/__tests__/delivery.service.spec.ts,
    backend/src/modules/delivery/__tests__/delivery.gateway.spec.ts
  </files>
  <behavior>
    DTOs (validated by class-validator):
    - RequestDeliveryDto: pickupLat/Lng (number, ±90/±180), pickupAddress (string), dropoffLat/Lng (number, ±90/±180), dropoffAddress (string), itemDescription (string, not empty), weightKg (number, min 0.1, max 500), recipientPhone (Nigerian mobile, @IsMobilePhone('en-NG'))
    - VerifyDeliveryOtpDto: otp (string, exactly 6 chars via @Length(6,6))
    - CompleteDeliveryDto: proofPhotoBase64 (string, optional — service rejects if absent), senderRating (int, 1-5, optional)
    - CreateDeliveryRiderDto: metadata (string, optional)
    - ApproveDeliveryRiderDto: approved (boolean) — copy from approve-driver.dto.ts
    - RiderGoOnlineDto: lat/lng (numbers) — copy from go-online.dto.ts

    DeliveryService behaviors (tested via mocks — all tests RED before Plan 04):
    - Test 1 (DELIVERY-01): requestDelivery calls redis.geosearch('riders:online', ...) with pickupLng, pickupLat, radiusKm; creates a DeliveryOrder in DB with SEARCHING status; sends OTP via Termii (or stub)
    - Test 2 (DELIVERY-02): When geosearch returns a riderId, emits 'delivery:request' to gateway.server.to('rider:{riderId}'); schedules 60s match timeout via SchedulerRegistry
    - Test 3 (DELIVERY-04): verifyOtp: redis.get('delivery:otp:{orderId}') matches → updates deliveryOrder.otpVerifiedAt; mismatches → throws BadRequestException
    - Test 4 (DELIVERY-04 + DELIVERY-05): completeDelivery: throws BadRequestException when otpVerifiedAt is null; throws BadRequestException when proofPhotoBase64 absent; when both present → calls s3Service.upload('delivery-proof/...', buffer, 'image/jpeg') → calls walletService.creditWallet with ISY-RDR- prefix and 'INTERNAL' gateway
    - Test 5 (DELIVERY-05): creditWallet is called with riderEarnings = fee × (1 - feePct/100) where feePct comes from platformConfig key 'delivery_platform_fee_pct'

    DeliveryGateway behaviors:
    - Test 6 (DELIVERY-03): handleRiderLocation relays { lat, lng } to server.to('delivery:{deliveryId}').emit('rider:location', ...) — NOT server.emit() globally
  </behavior>
  <implementation>
    RED step only — create stub files (DTOs with correct decorators; spec files with test bodies that import DeliveryService/DeliveryGateway which do not exist yet). Tests will fail with "Cannot find module '../../delivery.service'" or similar. That is the correct red state.

    For each spec file:
    1. Create the __tests__/ directory if absent
    2. Write the full mock structure (mockPrisma, mockRedis, mockWallet, mockScheduler, mockGateway, mockS3, mockConfig) — copy from transport.service.spec.ts mock structure, adapt model names to delivery
    3. Write describe blocks with it() tests whose bodies reference DeliveryService methods that do not exist yet
    4. Confirm tests fail: `npx jest --testPathPattern=delivery --no-coverage` exits non-zero

    GREEN step is Plan 04.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request body → RequestDeliveryDto | recipientPhone is the only PII field; validated as Nigerian mobile number before entering service layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Spoofing | RequestDeliveryDto.recipientPhone | mitigate | @IsMobilePhone('en-NG') rejects non-Nigerian numbers; OTP sent only to validated phone; NestJS ValidationPipe strips all unknown fields (whitelist:true) |
| T-04-07 | Tampering | CompleteDeliveryDto.proofPhotoBase64 | mitigate | Backend validates presence before upload; S3 key scoped to 'delivery-proof/' prefix; contentType set to 'image/jpeg' explicitly (not from client header) |
| T-04-08 | Tampering | VerifyDeliveryOtpDto.otp | mitigate | @Length(6,6) rejects non-6-digit values at DTO layer; brute-force noted as acceptable risk per RESEARCH.md A3 (delivery context; no financial data at this endpoint) |
</threat_model>

<verification>
- `npx jest --testPathPattern=delivery --no-coverage` exits non-zero with "Cannot find module" errors (red state confirmed)
- All 6 DTO files exist in backend/src/modules/delivery/dto/
- `grep -c "recipientPhone" backend/src/modules/delivery/dto/request-delivery.dto.ts` returns ≥ 1
- `grep -c "proofPhotoBase64" backend/src/modules/delivery/dto/complete-delivery.dto.ts` returns ≥ 1
- `grep -c "delivery_platform_fee_pct" backend/src/modules/delivery/__tests__/delivery.service.spec.ts` returns ≥ 1
- `grep -c "ISY-RDR-" backend/src/modules/delivery/__tests__/delivery.service.spec.ts` returns ≥ 1
</verification>

<success_criteria>
- 6 DTO files exist with correct class-validator decorators matching the behavior spec above
- RequestDeliveryDto has recipientPhone with @IsMobilePhone('en-NG') — NOT pickupPhone or senderPhone
- CompleteDeliveryDto has proofPhotoBase64 field (string, optional at DTO level)
- delivery.service.spec.ts has at least 5 it() blocks covering DELIVERY-01 through DELIVERY-05
- delivery.gateway.spec.ts has at least 1 it() block for rider:location relay
- All spec tests fail (red) — `npx jest --testPathPattern=delivery` exits non-zero
- `npx tsc --noEmit -p backend/tsconfig.json` may fail on "Cannot find module delivery.service" — this is acceptable red state for tsc; DTOs themselves must compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-delivery-module/04-03-SUMMARY.md` documenting:
- All 6 DTO field lists
- Number of failing tests in delivery.service.spec.ts and delivery.gateway.spec.ts
- Exact jest failure messages (red confirmation)
- Any deviations from the analog transport DTOs
</output>
