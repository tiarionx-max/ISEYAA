---
phase: 04-delivery-module
plan: 07
type: execute
wave: 7
depends_on:
  - 04-06
files_modified:
  - mobile/app/(tabs)/rider.tsx
autonomous: true
requirements:
  - DELIVERY-03
  - DELIVERY-04
  - DELIVERY-05
  - DELIVERY-06

must_haves:
  truths:
    - "mobile/app/(tabs)/rider.tsx exists with 5 screens: home, incoming, pickup, active, earnings"
    - "R-4 Active screen has 6-cell OTP input with auto-advance focus"
    - "R-4 Active screen has photo upload button using expo-image-picker"
    - "Confirm Delivery CTA is disabled until both OTP server-verified AND photoUri non-null"
    - "completeDelivery converts photoUri to base64 before PATCH /delivery/orders/:id/complete"
    - "Rider GPS emitted to socket every 2 seconds via expo-location watchPositionAsync"
    - "Credit banner shows riderEarnings amount; auto-navigates to earnings after 2s"
  artifacts:
    - path: mobile/app/(tabs)/rider.tsx
      provides: "5-screen delivery rider flow with OTP + photo proof"
      contains: "verify-otp"
  key_links:
    - from: mobile/app/(tabs)/rider.tsx
      to: backend/src/modules/delivery/delivery.gateway.ts
      via: "socket.emit('join:rider') → rider:{userId} room"
      pattern: "join:rider"
    - from: mobile/app/(tabs)/rider.tsx
      to: backend/src/modules/delivery/delivery.controller.ts
      via: "api.post('/delivery/orders/:id/verify-otp', { otp })"
      pattern: "verify-otp"
    - from: mobile/app/(tabs)/rider.tsx
      to: backend/src/modules/delivery/delivery.controller.ts
      via: "api.patch('/delivery/orders/:id/complete', { proofPhotoBase64, senderRating })"
      pattern: "proofPhotoBase64"
---

<objective>
Build the mobile Rider tab (delivery rider flow) covering screens R-1 through R-5: rider home/online toggle, incoming request with 15s timer, active pickup navigation, active delivery with OTP entry + photo proof, and earnings dashboard. This is the most complex mobile screen in Phase 4 — the R-4 dual-gate (OTP + photo) is the key differentiator.

Purpose: DELIVERY-04 and DELIVERY-05 require the rider to enter OTP and upload photo before the delivery can complete. This plan implements that exact flow in native mobile.

Output: rider.tsx with 5 screens. `npx jest --testPathPattern=delivery --no-coverage` continues to pass (no regressions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-delivery-module/04-UI-SPEC.md
@.planning/phases/04-delivery-module/04-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Primary analog: mobile/app/(tabs)/driver.tsx (read full file, 800 lines) -->
<!-- Rider tab follows driver.tsx exactly except: -->
<!-- 1. Screen type: 'home'|'incoming'|'pickup'|'active'|'earnings' (not 'home'|'incoming'|'pickup'|'active'|'earnings') -->
<!-- 2. API endpoints use /delivery/* (not /transport/*) -->
<!-- 3. WS events: 'join:rider', 'delivery:request' (not 'join:driver', 'ride:request') -->
<!-- 4. Location emit: { deliveryId, lat, lng } (not { tripId, lat, lng }) -->
<!-- 5. R-4 adds: 6-cell OTP input + photo upload button (new — no driver.tsx analog) -->

<!-- expo-image-picker API for SDK 51: -->
import * as ImagePicker from 'expo-image-picker';
// Use MediaTypeOptions.Images (NOT array syntax — v15.x only)
const result = await ImagePicker.launchImageLibraryAsync({
  mediaTypes: ImagePicker.MediaTypeOptions.Images,
  allowsEditing: true, aspect: [4,3], quality: 0.7,
});

<!-- base64 conversion from URI (no expo-file-system needed): -->
const response = await fetch(photoUri);
const arrayBuffer = await response.arrayBuffer();
const base64 = Buffer.from(arrayBuffer).toString('base64');

<!-- haversineKm (copy verbatim from driver.tsx lines 61-69): -->
function haversineKm(lat1, lng1, lat2, lng2): number
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create mobile/app/(tabs)/rider.tsx — 5-screen rider flow with OTP + photo proof</name>
  <files>mobile/app/(tabs)/rider.tsx</files>
  <read_first>
    - mobile/app/(tabs)/driver.tsx (full file — primary analog for all patterns)
    - .planning/phases/04-delivery-module/04-UI-SPEC.md (screens R-1 through R-5, OTP sub-state specs, photo upload specs)
    - .planning/phases/04-delivery-module/04-PATTERNS.md (rider.tsx section with all code patterns)
  </read_first>
  <action>
    Create `mobile/app/(tabs)/rider.tsx`. Base it on driver.tsx with these changes:

    **Imports:** Add `import * as ImagePicker from 'expo-image-picker'` (not in driver.tsx). Add `Image` from react-native. Change icons: keep MapPin, AlertCircle, Navigation, CheckCircle; replace Driver-specific icons with Package, Camera, Upload from lucide-react-native.

    **haversineKm** — copy verbatim from driver.tsx lines 61-69.

    **Type declarations:**
    ```
    type Screen = 'home' | 'incoming' | 'pickup' | 'active' | 'earnings';
    interface DeliveryRequest { id: string; pickupAddress: string; dropoffAddress: string; itemDescription: string; weightKg: number; fee: number; pickupLat: number; pickupLng: number; dropoffLat: number; dropoffLng: number; }
    ```

    **State additions (beyond driver.tsx state):**
    ```
    const [otpCells, setOtpCells] = useState<string[]>(['','','','','','']);
    const [otpVerified, setOtpVerified] = useState(false);
    const [otpError, setOtpError] = useState(false);
    const [photoUri, setPhotoUri] = useState<string | null>(null);
    const otpInputRefs = useRef<Array<any>>([]);  // 6 refs for auto-focus
    // Rename: arrived → collected (parcel collection, not rider arrival)
    // Rename: currentTrip → currentOrder
    ```

    **WebSocket setup:** Async IIFE from driver.tsx. Events:
    - `'connect'` → emit `'join:rider'` (not 'join:driver')
    - `'delivery:request'` → setCurrentOrder(req); startRespondCountdown(); setScreen('incoming')
    Location emit: `socket.emit('rider:location', { deliveryId: currentOrder.id, lat, lng })` (not tripId)

    **Go-online/offline:** Same Alert.alert pattern as driver.tsx. API endpoints: `/delivery/go-online` and `/delivery/go-offline`.

    **handleAccept:** `PATCH /delivery/orders/${currentOrder.id}/accept`
    **handleDecline:** `PATCH /delivery/orders/${currentOrder.id}/decline`

    **R-3 Pickup screen:** Same 200m proximity gate pattern as driver.tsx. CTA text: "I've Collected" (not "I've Arrived"). API: `PATCH /delivery/orders/${currentOrder.id}/collect`. Transition to 'active' screen on success.

    **R-4 Active screen — two sub-states based on distance to dropoff:**

    Sub-state A (distance > 200m from dropoff): map + delivery info card + "Approach recipient to enter OTP and upload proof" banner. Confirm Delivery CTA disabled.

    Sub-state B (distance <= 200m from dropoff): map shrinks to height:180. Show OTP section + photo section + active Confirm Delivery CTA (enabled only when otpVerified AND photoUri non-null).

    **OTP entry (R-4 Sub-state B):**
    - 6 `TextInput` cells in a flex row, each `width:48, height:56, borderRadius:10, borderWidth:2, textAlign:'center', fontSize:24, fontWeight:'bold', color:'white', keyboardType:'number-pad', maxLength:1`
    - Border colors: default `rgba(255,255,255,0.15)`, verified `#22C55E`, error `#DC2626`
    - Auto-advance: `handleOtpChange(index, value)` — on digit entry, `otpInputRefs.current[index+1]?.focus()`. On backspace in empty cell, `otpInputRefs.current[index-1]?.focus()`.
    - Auto-verify when all 6 cells filled: call `verifyOtp(cells.join(''))`
    - `verifyOtp(otp)`: `POST /delivery/orders/${currentOrder.id}/verify-otp` with `{ otp }`. On success: `setOtpVerified(true); setOtpError(false)`. On error: `setOtpError(true); setOtpCells(['','','','','','']); otpInputRefs.current[0]?.focus()`
    - Error message (only on otpError=true): "Incorrect OTP. Ask recipient to check their SMS." in #DC2626

    **Photo upload (R-4 Sub-state B):**
    - Button: `height:80, backgroundColor: rgba(26,107,60,0.2), borderRadius:12, borderWidth:2, borderStyle:'dashed', borderColor: photoUri ? '#22C55E' : 'rgba(255,255,255,0.15)'`
    - No photo state: Camera icon + "Tap to take or upload photo" text
    - Photo selected state: Image thumbnail with CheckCircle overlay
    - Tap: show Alert with options "Take Photo" → `ImagePicker.launchCameraAsync(...)` and "Choose from Library" → `ImagePicker.launchImageLibraryAsync(...)`. Both use `{ mediaTypes: ImagePicker.MediaTypeOptions.Images, allowsEditing: true, aspect: [4,3], quality: 0.7 }`. Store `result.assets[0].uri` in `photoUri`.
    - Request permissions before each: `requestCameraPermissionsAsync()` for camera; `requestMediaLibraryPermissionsAsync()` for library.

    **handleConfirmDelivery:**
    - UI gates (also enforced server-side): `if (!otpVerified || !photoUri) return`
    - Show Alert.alert: "Confirm delivery?" / "₦{riderEarnings} will be credited..." / "Not Yet" + "Yes, Confirm"
    - On confirm: convert photo to base64: `const r=await fetch(photoUri); const ab=await r.arrayBuffer(); const b64=Buffer.from(ab).toString('base64')`
    - Call `PATCH /delivery/orders/${currentOrder.id}/complete` with `{ proofPhotoBase64: b64, senderRating: rating || undefined }`
    - Show loading indicator during upload
    - On success: `setCreditBanner('₦${data.riderEarnings?.toFixed(0)} credited to your wallet.')`. After 2s: navigate to 'earnings'; reset state.
    - On error: show Alert with error message; do not reset state.

    **R-5 Earnings screen:** Copy driver.tsx earnings screen verbatim. Change API endpoint to `/delivery/riders/earnings?period=${earningsPeriod}`. History rows show "Your share: ₦{riderEarnings}" subtext (not total fare).

    **StyleSheet:** Copy driver.tsx full StyleSheet. Add new styles:
    - `otpCell`: `width:48, height:56, borderRadius:10, borderWidth:2, textAlign:'center', fontSize:24, fontWeight:'bold', color:'white'`
    - `otpCellVerified`: `borderColor:'#22C55E', backgroundColor:'rgba(34,197,94,0.15)'`
    - `otpCellError`: `borderColor:'#DC2626', backgroundColor:'rgba(220,38,38,0.15)'`
    - `photoUploadButton`: `height:80, backgroundColor:'rgba(26,107,60,0.2)', borderRadius:12, borderWidth:2, alignItems:'center', justifyContent:'center', flexDirection:'row', gap:8`
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('mobile/app/(tabs)/rider.tsx','utf8'); ['join:rider','delivery:request','verify-otp','proofPhotoBase64','otpVerified','photoUri','ImagePicker','I\'ve Collected','riders/earnings'].forEach(t=>{if(!src.includes(t)){console.error('MISSING:',t);process.exit(1)}}); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at mobile/app/(tabs)/rider.tsx
    - Contains `'join:rider'` (WebSocket room join)
    - Contains `'delivery:request'` (incoming assignment WS event)
    - Contains `'verify-otp'` (API path for OTP verification)
    - Contains `proofPhotoBase64` (body field in complete request)
    - Contains `otpVerified` (state variable for dual-gate)
    - Contains `photoUri` (state variable for photo)
    - Contains `ImagePicker` (expo-image-picker import used)
    - Contains `"I've Collected"` (R-3 CTA copy per UI-SPEC)
    - Contains `'riders/earnings'` (R-5 earnings API endpoint)
    - Contains `MediaTypeOptions.Images` (correct SDK 51 API — NOT array syntax)
    - Does NOT contain `MediaTypeOptions.Videos` or `['images']` (wrong API)
    - Does NOT contain `'drivers:online'` or `'join:driver'` (wrong WS events)
    - Does NOT contain `'uploadBuffer'` (wrong S3 method — not applicable but sanity check)
  </acceptance_criteria>
  <done>
    rider.tsx exists with all 5 screens. OTP 6-cell input with auto-advance implemented. Photo upload with expo-image-picker implemented. Dual-gate (otpVerified + photoUri) enforced before confirm. Credit banner and auto-navigate present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile rider → Backend complete endpoint | proofPhotoBase64 is large; base64 encoding at quality:0.7 keeps payload under NestJS rawBody limit |
| Mobile → expo-image-picker | Runtime permissions requested before picker launch; user must explicitly grant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-21 | Tampering | OTP bypass via direct API call | mitigate | Mobile UI gates confirm button; backend DeliveryService.completeDelivery also checks otpVerifiedAt is non-null (server-side enforcement in Plan 04) |
| T-04-22 | Tampering | Photo bypass (complete without photo) | mitigate | Mobile UI gates confirm button on photoUri !== null; backend checks dto.proofPhotoBase64 presence — both layers enforce |
| T-04-23 | Information Disclosure | GPS location emitted while off-duty | mitigate | GPS watch starts only after acceptOrder succeeds; stops in handleConfirmDelivery after completion and after declineOrder; useEffect cleanup disconnects socket on unmount |
| T-04-24 | Tampering | Malicious photo upload size | mitigate | expo-image-picker quality:0.7 + aspect:[4,3] constrains payload; NestJS main.ts rawBody:true applies 5MB limit inherited from express; 70% quality JPEG at 800x600 is approximately 150-250KB base64 |
</threat_model>

<verification>
- node -e verification command exits 0
- Manual check: rider tab visible in Expo Go, R-1 go-online toggle renders
- Manual check: R-4 OTP cells render with correct sizing
- `npx jest --testPathPattern=delivery --no-coverage` exits 0 (no regressions)
</verification>

<success_criteria>
- rider.tsx has all 5 screens matching UI-SPEC
- R-4 Sub-state A shows proximity prompt (disabled CTA) when > 200m from dropoff
- R-4 Sub-state B shows 6-cell OTP input + photo upload button when <= 200m
- OTP cells auto-advance focus and auto-verify on 6th digit
- Photo picker uses MediaTypeOptions.Images (SDK 51 compatible)
- proofPhotoBase64 is base64 string derived from photoUri via fetch + arrayBuffer
- Confirm CTA disabled until both otpVerified=true AND photoUri!=null
- Earnings screen queries /delivery/riders/earnings and shows "Your share:" subtext
- Credit banner shows amount; auto-navigates to earnings after 2 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/04-delivery-module/04-07-SUMMARY.md` documenting:
- All 5 screen types implemented
- OTP input implementation details (auto-advance, verify, error reset)
- Photo upload method used (library vs camera)
- base64 conversion approach
- Any deviations from driver.tsx analog
</output>
