---
phase: quick
plan: 260802-8x8
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/apps/auth-service/src/main.ts
  - backend/apps/auth-service/src/health.controller.ts
  - backend/apps/auth-service/src/app.module.ts
  - backend/apps/auth-service/Dockerfile
  - backend/apps/auth-service/railway.toml
  - backend/apps/wallet-service/src/main.ts
  - backend/apps/wallet-service/src/health.controller.ts
  - backend/apps/wallet-service/src/app.module.ts
  - backend/apps/wallet-service/Dockerfile
  - backend/apps/wallet-service/railway.toml
  - backend/apps/events-service/src/main.ts
  - backend/apps/events-service/src/health.controller.ts
  - backend/apps/events-service/src/app.module.ts
  - backend/apps/events-service/Dockerfile
  - backend/apps/events-service/railway.toml
  - backend/apps/stays-service/src/main.ts
  - backend/apps/stays-service/src/health.controller.ts
  - backend/apps/stays-service/src/app.module.ts
  - backend/apps/stays-service/Dockerfile
  - backend/apps/stays-service/railway.toml
  - backend/apps/marketplace-service/src/main.ts
  - backend/apps/marketplace-service/src/health.controller.ts
  - backend/apps/marketplace-service/src/app.module.ts
  - backend/apps/marketplace-service/Dockerfile
  - backend/apps/marketplace-service/railway.toml
  - backend/apps/admin-service/src/main.ts
  - backend/apps/admin-service/src/health.controller.ts
  - backend/apps/admin-service/src/app.module.ts
  - backend/apps/admin-service/Dockerfile
  - backend/apps/admin-service/railway.toml
  - backend/apps/ai-service/src/main.ts
  - backend/apps/ai-service/src/health.controller.ts
  - backend/apps/ai-service/src/app.module.ts
  - backend/apps/ai-service/Dockerfile
  - backend/apps/ai-service/railway.toml
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 7 services (auth, wallet, events, stays, marketplace, admin, ai) boot via NestFactory.create(AppModule) + app.connectMicroservice + app.startAllMicroservices + app.listen(process.env.PORT ?? 8080), exposing a real HTTP server alongside their gRPC transport, matching the already-converted notifications-service pattern"
    - "Each service's gRPC transport still binds to the IPv6-safe [::]:<port> address (preserved from prior IPv6 fix, never reverted to 0.0.0.0)"
    - "Each service responds on GET /healthz via a Terminus health check, matching the 5 already-converted Phase-21 services"
    - "Each service's Docker image resolves @nestjs/platform-express at runtime via the symlink hoist step, so the hybrid HTTP driver does not crash on boot inside the Alpine container"
    - "Each service's railway.toml declares healthcheckPath=/healthz so Railway's deploy healthcheck targets the new HTTP endpoint instead of failing/defaulting"
    - "No merge to main and no Railway deploy is triggered by this task - all changes stay committed on the current branch"
  artifacts:
    - path: "backend/apps/auth-service/src/main.ts"
      provides: "Hybrid gRPC+HTTP bootstrap, IPv6 bind preserved"
      contains: "[::]:5001"
    - path: "backend/apps/wallet-service/src/main.ts"
      provides: "Hybrid gRPC+HTTP bootstrap, IPv6 bind preserved"
      contains: "[::]:5002"
    - path: "backend/apps/events-service/src/main.ts"
      provides: "Hybrid gRPC+HTTP bootstrap, IPv6 bind preserved"
      contains: "[::]:5003"
    - path: "backend/apps/stays-service/src/main.ts"
      provides: "Hybrid gRPC+HTTP bootstrap, IPv6 bind preserved"
      contains: "[::]:5004"
    - path: "backend/apps/marketplace-service/src/main.ts"
      provides: "Hybrid gRPC+HTTP bootstrap, IPv6 bind preserved"
      contains: "[::]:5005"
    - path: "backend/apps/admin-service/src/main.ts"
      provides: "Hybrid gRPC+HTTP bootstrap, IPv6 bind preserved"
      contains: "[::]:5006"
    - path: "backend/apps/ai-service/src/main.ts"
      provides: "Hybrid gRPC+HTTP bootstrap, IPv6 bind preserved"
      contains: "[::]:5007"
    - path: "backend/apps/auth-service/src/health.controller.ts"
      provides: "GET /healthz Terminus check controller (byte-identical to notifications-service)"
      contains: "HealthCheck"
    - path: "backend/apps/wallet-service/src/health.controller.ts"
      provides: "GET /healthz Terminus check controller"
      contains: "HealthCheck"
    - path: "backend/apps/events-service/src/health.controller.ts"
      provides: "GET /healthz Terminus check controller"
      contains: "HealthCheck"
    - path: "backend/apps/stays-service/src/health.controller.ts"
      provides: "GET /healthz Terminus check controller"
      contains: "HealthCheck"
    - path: "backend/apps/marketplace-service/src/health.controller.ts"
      provides: "GET /healthz Terminus check controller"
      contains: "HealthCheck"
    - path: "backend/apps/admin-service/src/health.controller.ts"
      provides: "GET /healthz Terminus check controller"
      contains: "HealthCheck"
    - path: "backend/apps/ai-service/src/health.controller.ts"
      provides: "GET /healthz Terminus check controller"
      contains: "HealthCheck"
    - path: "backend/apps/auth-service/src/app.module.ts"
      provides: "TerminusModule imported, HealthController registered"
      contains: "TerminusModule"
    - path: "backend/apps/wallet-service/src/app.module.ts"
      provides: "TerminusModule imported, HealthController registered"
      contains: "TerminusModule"
    - path: "backend/apps/events-service/src/app.module.ts"
      provides: "TerminusModule imported, HealthController registered"
      contains: "TerminusModule"
    - path: "backend/apps/stays-service/src/app.module.ts"
      provides: "TerminusModule imported, HealthController registered"
      contains: "TerminusModule"
    - path: "backend/apps/marketplace-service/src/app.module.ts"
      provides: "TerminusModule imported, HealthController registered"
      contains: "TerminusModule"
    - path: "backend/apps/admin-service/src/app.module.ts"
      provides: "TerminusModule imported, HealthController registered"
      contains: "TerminusModule"
    - path: "backend/apps/ai-service/src/app.module.ts"
      provides: "TerminusModule imported, HealthController registered"
      contains: "TerminusModule"
    - path: "backend/apps/auth-service/Dockerfile"
      provides: "platform-express symlink hoist + EXPOSE 8080"
      contains: "ln -s /app/backend/node_modules/@nestjs/platform-express"
    - path: "backend/apps/wallet-service/Dockerfile"
      provides: "platform-express symlink hoist + EXPOSE 8080"
      contains: "ln -s /app/backend/node_modules/@nestjs/platform-express"
    - path: "backend/apps/events-service/Dockerfile"
      provides: "platform-express symlink hoist + EXPOSE 8080"
      contains: "ln -s /app/backend/node_modules/@nestjs/platform-express"
    - path: "backend/apps/stays-service/Dockerfile"
      provides: "platform-express symlink hoist + EXPOSE 8080"
      contains: "ln -s /app/backend/node_modules/@nestjs/platform-express"
    - path: "backend/apps/marketplace-service/Dockerfile"
      provides: "platform-express symlink hoist + EXPOSE 8080"
      contains: "ln -s /app/backend/node_modules/@nestjs/platform-express"
    - path: "backend/apps/admin-service/Dockerfile"
      provides: "platform-express symlink hoist + EXPOSE 8080"
      contains: "ln -s /app/backend/node_modules/@nestjs/platform-express"
    - path: "backend/apps/ai-service/Dockerfile"
      provides: "platform-express symlink hoist + EXPOSE 8080"
      contains: "ln -s /app/backend/node_modules/@nestjs/platform-express"
    - path: "backend/apps/auth-service/railway.toml"
      provides: "healthcheckPath /healthz for Railway deploy healthcheck"
      contains: "healthcheckPath = \"/healthz\""
    - path: "backend/apps/wallet-service/railway.toml"
      provides: "healthcheckPath /healthz for Railway deploy healthcheck"
      contains: "healthcheckPath = \"/healthz\""
    - path: "backend/apps/events-service/railway.toml"
      provides: "healthcheckPath /healthz for Railway deploy healthcheck"
      contains: "healthcheckPath = \"/healthz\""
    - path: "backend/apps/stays-service/railway.toml"
      provides: "healthcheckPath /healthz for Railway deploy healthcheck"
      contains: "healthcheckPath = \"/healthz\""
    - path: "backend/apps/marketplace-service/railway.toml"
      provides: "healthcheckPath /healthz for Railway deploy healthcheck"
      contains: "healthcheckPath = \"/healthz\""
    - path: "backend/apps/admin-service/railway.toml"
      provides: "healthcheckPath /healthz for Railway deploy healthcheck"
      contains: "healthcheckPath = \"/healthz\""
    - path: "backend/apps/ai-service/railway.toml"
      provides: "healthcheckPath /healthz for Railway deploy healthcheck"
      contains: "healthcheckPath = \"/healthz\""
  key_links:
    - from: "backend/apps/auth-service/src/app.module.ts"
      to: "backend/apps/auth-service/src/health.controller.ts"
      via: "controllers array registration"
      pattern: "controllers:.*HealthController"
    - from: "backend/apps/wallet-service/src/app.module.ts"
      to: "backend/apps/wallet-service/src/health.controller.ts"
      via: "controllers array registration"
      pattern: "controllers:.*HealthController"
    - from: "backend/apps/events-service/src/app.module.ts"
      to: "backend/apps/events-service/src/health.controller.ts"
      via: "controllers array registration"
      pattern: "controllers:.*HealthController"
    - from: "backend/apps/stays-service/src/app.module.ts"
      to: "backend/apps/stays-service/src/health.controller.ts"
      via: "controllers array registration"
      pattern: "controllers:.*HealthController"
    - from: "backend/apps/marketplace-service/src/app.module.ts"
      to: "backend/apps/marketplace-service/src/health.controller.ts"
      via: "controllers array registration"
      pattern: "controllers:.*HealthController"
    - from: "backend/apps/admin-service/src/app.module.ts"
      to: "backend/apps/admin-service/src/health.controller.ts"
      via: "controllers array registration"
      pattern: "controllers:.*HealthController"
    - from: "backend/apps/ai-service/src/app.module.ts"
      to: "backend/apps/ai-service/src/health.controller.ts"
      via: "controllers array registration"
      pattern: "controllers:.*HealthController"
    - from: "backend/apps/auth-service/Dockerfile"
      to: "backend/apps/auth-service/railway.toml"
      via: "container EXPOSE 8080 matched by healthcheckPath /healthz target port"
      pattern: "EXPOSE 8080"
---

<objective>
Convert the 7 remaining pure-gRPC microservices - auth-service (5001), wallet-service (5002), events-service (5003), stays-service (5004), marketplace-service (5005), admin-service (5006), ai-service (5007) - to the hybrid gRPC+HTTP bootstrap already proven on notifications-service and the other 4 Phase-21 services (news, waitlist, reviews, delivery-otp). Each service currently uses `NestFactory.createMicroservice(...)` + bare `app.listen()`, exposing gRPC only - no HTTP server, no `/healthz`, no Railway healthcheck target. This blocks Railway from ever reporting these 7 services as healthy once deployed, the same class of gap already fixed for the other 5.

Purpose: give all 12 microservices a uniform HTTP `/healthz` surface so Railway's deploy healthcheck can gate promotion consistently, matching the Phase-20 blue-green healthcheck retrofit pattern and closing the last 7 stragglers.
Output: 7 rewritten `main.ts` (hybrid bootstrap, IPv6 gRPC bind preserved), 7 new `health.controller.ts`, 7 `app.module.ts` registering `TerminusModule`/`HealthController`, 7 `Dockerfile`s with the platform-express symlink hoist + `EXPOSE 8080`, 7 `railway.toml`s with `healthcheckPath = "/healthz"`. No RPC method bodies, proto files, business logic, or existing module imports touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

This is a quick task, not a full phase - there is no ROADMAP requirement ID to satisfy (`requirements: []` is correct here, not an omission).

**Hard constraints for the executor:**
- Do NOT merge this branch into `main` and do NOT trigger any Railway deploy/redeploy. Commits stay on the current branch (`claude/upbeat-montalcini-d49312`). STATE.md's git-workflow entry (2026-07-28) documents `main` as Railway's actual deploy source - untouched by this task.
- Do NOT run `npm ci` / `npm install`. There is no `node_modules` in this worktree, so `nest build` verification is not possible - rely on grep/structural checks only. The Railway Docker build is the real gate for this change, run separately by the user later.
- Do NOT touch `packages/proto/*.proto`, any RPC method body, any existing feature-module import (`AuthModule`, `WalletModule`, `EventsModule`, `KafkaModule`, etc.), or the IPv6 gRPC bind string beyond preserving it verbatim (`[::]:<port>` - never revert to `0.0.0.0`).
- Each Dockerfile's `CMD` line stays unchanged (do NOT add an `instrumentation.js --require` flag - that is notifications-service-specific and out of scope here).

<interfaces>
**Canonical `main.ts` template** - mirror `backend/apps/notifications-service/src/main.ts` exactly, substituting `<PKG>`, `<PORT>`, `<SERVICE-NAME>` per the table below. This is the full target file content for each of the 7 services (only the substituted values differ):

Imports: `NestFactory` from `@nestjs/core`; `MicroserviceOptions, Transport` from `@nestjs/microservices`; `DocumentBuilder, SwaggerModule` from `@nestjs/swagger` (NEW - not in the current pure-gRPC file); `HealthImplementation, protoPath as healthCheckProtoPath` from `grpc-health-check` (NEW); `join` from `path`; `AppModule` from `./app.module`.

`bootstrap()` body, in order:
1. `const app = await NestFactory.create(AppModule);` (replaces `NestFactory.createMicroservice`)
2. `app.connectMicroservice<MicroserviceOptions>({ transport: Transport.GRPC, options: { package: '<PKG>', protoPath: [join(__dirname, '../../../../../packages/proto/<PKG>.proto'), healthCheckProtoPath], url: '[::]:<PORT>', onLoadPackageDefinition: (...) } })` - the `onLoadPackageDefinition` IIFE registers the grpc-health-check `HealthImplementation` on the server exactly as in notifications-service (a `registered` guard flag, `new HealthImplementation({ '': 'UNKNOWN' })`, `healthImpl.addToServer(server)`, `healthImpl.setStatus('', 'SERVING')`). Preserve the existing IPv6 comment above the `url:` line verbatim from each service's current file.
3. `await app.startAllMicroservices();`
4. Swagger block: `new DocumentBuilder().setTitle('<SERVICE-NAME>').setDescription('gRPC-internal microservice - HTTP surface is health-only (/healthz). Real API contract is the gRPC "<PKG>" package in packages/proto/<PKG>.proto.').setVersion('1.0').build()`, then `SwaggerModule.createDocument(app, config)`, then `SwaggerModule.setup('docs', app, document)`.
5. `await app.listen(process.env.PORT ?? 8080);`
6. `console.log('<SERVICE-NAME> gRPC :<PORT>, HTTP healthz :8080');`

Per-service substitution table:

| Service dir | `<PKG>` | `<PORT>` | `<SERVICE-NAME>` |
|---|---|---|---|
| auth-service | auth | 5001 | auth-service |
| wallet-service | wallet | 5002 | wallet-service |
| events-service | events | 5003 | events-service |
| stays-service | stays | 5004 | stays-service |
| marketplace-service | marketplace | 5005 | marketplace-service |
| admin-service | admin | 5006 | admin-service |
| ai-service | ai | 5007 | ai-service |

**`health.controller.ts`** - create byte-identical to `backend/apps/notifications-service/src/health.controller.ts` in all 7 services (generic, no per-service diff): `@Controller()` class `HealthController` injecting `HealthCheckService` from `@nestjs/terminus`, one `@Get('healthz')` + `@HealthCheck()` method `check()` returning `this.health.check([])`.

**`app.module.ts` edits** - model: `backend/apps/notifications-service/src/app.module.ts` (import `TerminusModule` from `@nestjs/terminus`, import `HealthController` from `./health.controller`, add `TerminusModule` to the `imports` array, add `HealthController` to the `controllers` array alongside the existing gRPC controller). Do NOT add `ScheduleModule` - none of these 7 currently have it, and it is notifications-service-specific.

Current per-service `app.module.ts` content (full files already read - apply the two additions listed above, changing nothing else):
- `auth-service`: imports `ConfigModule, PrismaModule, RedisModule, CommonModule, AuthModule, ResilienceModule`; controllers `[AuthGrpcController]`.
- `wallet-service`: imports `ConfigModule, PrismaModule, RedisModule, CommonModule, WalletModule, ResilienceModule`; controllers `[WalletGrpcController]`.
- `events-service`: imports `ConfigModule, PrismaModule, RedisModule, CommonModule, ResilienceModule, EventsModule, KafkaModule`; controllers `[EventsGrpcController]`.
- `stays-service`: imports `ConfigModule, PrismaModule, RedisModule, CommonModule, ResilienceModule, StaysModule, KafkaModule`; controllers `[StaysGrpcController]`.
- `marketplace-service`: imports `ConfigModule, PrismaModule, RedisModule, CommonModule, ResilienceModule` (single-line array, no feature module); controllers `[MarketplaceGrpcController]`.
- `admin-service`: imports `ConfigModule, PrismaModule, RedisModule, CommonModule, AdminModule, ResilienceModule` (single-line array); controllers `[AdminGrpcController]`.
- `ai-service`: imports `ConfigModule, PrismaModule, RedisModule, CommonModule, AiModule, ResilienceModule` (single-line array); controllers `[AiGrpcController]`.

**Dockerfile edit** - every one of the 7 Dockerfiles has this exact line present (verbatim, confirmed via read): `RUN cd packages/proto && npx tsc -p tsconfig.json` immediately followed by `COPY backend/ ./backend/`. Insert this block between those two lines, verbatim from `backend/apps/notifications-service/Dockerfile`:

Comment (4 lines, verbatim): "# Lockfile nests @nestjs/platform-express under backend/node_modules while" / "# @nestjs/core is hoisted to root - NestJS's internal require() for the HTTP" / "# driver walks up from @nestjs/core's own location and never reaches the" / "# nested copy. Hoist it explicitly so the hybrid gRPC+HTTP bootstrap resolves."
Then the RUN line: `RUN mkdir -p node_modules/@nestjs && ln -s /app/backend/node_modules/@nestjs/platform-express node_modules/@nestjs/platform-express`

Also add `EXPOSE 8080` on its own line directly after each Dockerfile's existing `EXPOSE <port>` line (`EXPOSE 5001` for auth-service, `EXPOSE 5002` for wallet-service, `EXPOSE 5003` for events-service, `EXPOSE 5004` for stays-service, `EXPOSE 5005` for marketplace-service, `EXPOSE 5006` for admin-service, `EXPOSE 5007` for ai-service - one per service, already present in each file). Leave `CMD` and every other line unchanged.

**railway.toml edit** - every one of the 7 `railway.toml` files currently has, under `[deploy]`: a `watchPaths = [...]` line immediately followed by `restartPolicyType = "on_failure"`. Insert these two lines between them, verbatim from `backend/apps/notifications-service/railway.toml`:

`healthcheckPath = "/healthz"`
`healthcheckTimeout = 60`

Leave `[build]`, `watchPaths`, `restartPolicyType`, `restartPolicyMaxRetries` unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite main.ts, create health.controller.ts, and register TerminusModule/HealthController in app.module.ts across all 7 services</name>
  <files>
    backend/apps/auth-service/src/main.ts
    backend/apps/auth-service/src/health.controller.ts
    backend/apps/auth-service/src/app.module.ts
    backend/apps/wallet-service/src/main.ts
    backend/apps/wallet-service/src/health.controller.ts
    backend/apps/wallet-service/src/app.module.ts
    backend/apps/events-service/src/main.ts
    backend/apps/events-service/src/health.controller.ts
    backend/apps/events-service/src/app.module.ts
    backend/apps/stays-service/src/main.ts
    backend/apps/stays-service/src/health.controller.ts
    backend/apps/stays-service/src/app.module.ts
    backend/apps/marketplace-service/src/main.ts
    backend/apps/marketplace-service/src/health.controller.ts
    backend/apps/marketplace-service/src/app.module.ts
    backend/apps/admin-service/src/main.ts
    backend/apps/admin-service/src/health.controller.ts
    backend/apps/admin-service/src/app.module.ts
    backend/apps/ai-service/src/main.ts
    backend/apps/ai-service/src/health.controller.ts
    backend/apps/ai-service/src/app.module.ts
  </files>
  <action>
For each of the 7 services (auth, wallet, events, stays, marketplace, admin, ai), apply the three changes described in the `interfaces` block above, substituting that service's PKG/PORT/SERVICE-NAME from the table:

1. Rewrite `src/main.ts` in full to the hybrid bootstrap template - replace `NestFactory.createMicroservice` + bare `app.listen()` with `NestFactory.create(AppModule)` + `app.connectMicroservice` + `app.startAllMicroservices()` + Swagger setup + `app.listen(process.env.PORT ?? 8080)`. Preserve the existing IPv6 bind comment and the `url: '[::]:PORT'` value exactly as currently written in each file - do not change the port number or revert to `0.0.0.0`. Do not alter `package` or the proto filename beyond wrapping `protoPath` in the two-element array with `healthCheckProtoPath`, matching the template.
2. Create `src/health.controller.ts`, byte-identical to `backend/apps/notifications-service/src/health.controller.ts` - same imports, same `HealthController` class, same `@Get('healthz')` + `@HealthCheck()` method.
3. Edit `src/app.module.ts`: add the `TerminusModule` import from `@nestjs/terminus` and the `HealthController` import from `./health.controller`; add `TerminusModule` to the `imports` array and `HealthController` to the `controllers` array, alongside the existing entries. Do not remove, reorder, or otherwise modify any existing import, module, or controller in the file.
  </action>
  <verify>
    <automated>cd backend/apps && for s in auth wallet events stays marketplace admin ai; do grep -q "NestFactory.create(AppModule)" $s-service/src/main.ts && grep -q "app.listen(process.env.PORT" $s-service/src/main.ts && test -f $s-service/src/health.controller.ts && grep -q "TerminusModule" $s-service/src/app.module.ts && grep -q "HealthController" $s-service/src/app.module.ts && echo "OK: $s-service" || echo "FAIL: $s-service"; done</automated>
  </verify>
  <done>All 7 main.ts files use the hybrid NestFactory.create + connectMicroservice + startAllMicroservices + app.listen(process.env.PORT ?? 8080) bootstrap with their IPv6 bind preserved; all 7 health.controller.ts files exist and match notifications-service's content; all 7 app.module.ts files import and register both TerminusModule and HealthController without disturbing existing imports/controllers.</done>
</task>

<task type="auto">
  <name>Task 2: Add platform-express symlink hoist + EXPOSE 8080 to Dockerfiles, and healthcheckPath to railway.toml, across all 7 services</name>
  <files>
    backend/apps/auth-service/Dockerfile
    backend/apps/auth-service/railway.toml
    backend/apps/wallet-service/Dockerfile
    backend/apps/wallet-service/railway.toml
    backend/apps/events-service/Dockerfile
    backend/apps/events-service/railway.toml
    backend/apps/stays-service/Dockerfile
    backend/apps/stays-service/railway.toml
    backend/apps/marketplace-service/Dockerfile
    backend/apps/marketplace-service/railway.toml
    backend/apps/admin-service/Dockerfile
    backend/apps/admin-service/railway.toml
    backend/apps/ai-service/Dockerfile
    backend/apps/ai-service/railway.toml
  </files>
  <action>
For each of the 7 services (auth, wallet, events, stays, marketplace, admin, ai), apply the two changes described in the `interfaces` block above:

1. In `Dockerfile`, insert the platform-express symlink hoist comment block + `RUN mkdir -p node_modules/@nestjs && ln -s /app/backend/node_modules/@nestjs/platform-express node_modules/@nestjs/platform-express` line immediately after the existing `RUN cd packages/proto && npx tsc -p tsconfig.json` line and immediately before the existing `COPY backend/ ./backend/` line - copy the comment and RUN line verbatim from `backend/apps/notifications-service/Dockerfile`. Then add a new `EXPOSE 8080` line directly after the file's existing `EXPOSE <port>` line (the service's own gRPC port - 5001/5002/5003/5004/5005/5006/5007 respectively). Do not modify `CMD`, the `npm ci` line, the sharp install line, the prisma generate line, or the `nest build <service>` line.
2. In `railway.toml`, insert `healthcheckPath = "/healthz"` and `healthcheckTimeout = 60` as two new lines under `[deploy]`, positioned directly after the existing `watchPaths = [...]` line and directly before the existing `restartPolicyType = "on_failure"` line. Do not modify `[build]`, `watchPaths`, `restartPolicyType`, or `restartPolicyMaxRetries`.
  </action>
  <verify>
    <automated>cd backend/apps && for s in auth wallet events stays marketplace admin ai; do grep -q "ln -s /app/backend/node_modules/@nestjs/platform-express" $s-service/Dockerfile && grep -c "^EXPOSE 8080$" $s-service/Dockerfile | grep -q "^1$" && grep -q 'healthcheckPath = "/healthz"' $s-service/railway.toml && grep -q 'healthcheckTimeout = 60' $s-service/railway.toml && echo "OK: $s-service" || echo "FAIL: $s-service"; done</automated>
  </verify>
  <done>All 7 Dockerfiles contain the platform-express symlink hoist RUN step (before COPY backend/) and exactly one new EXPOSE 8080 line; all 7 railway.toml files declare healthcheckPath = "/healthz" and healthcheckTimeout = 60 under [deploy], with watchPaths and restartPolicy untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Railway private network -> gRPC service | Internal-only gRPC calls between already-authenticated backend processes; not internet-exposed |
| Internet -> Swagger /docs and /healthz | New public HTTP surface added by this task on 7 more services - currently unauthenticated, same pattern already accepted on the 5 already-converted services and the monolith's /api/docs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260802-8x8-01 | Information Disclosure | New `/docs` Swagger UI + `/healthz` on all 7 microservices | accept | Documents/exposes only the health-only HTTP surface - no request/response schemas, no business data, no auth secrets. Matches the identical, already-accepted pattern on notifications/news/waitlist/reviews/delivery-otp services from quick task 260802-7xs. |
| T-260802-8x8-02 | Denial of Service | New HTTP listener (`app.listen`) added alongside existing gRPC transport | accept | `/healthz` performs an empty Terminus check (`this.health.check([])`) with no downstream dependency probing - constant-time, no amplification vector. Matches the reference services. |
| T-260802-8x8-03 | Tampering | Dockerfile symlink hoist step | mitigate | Symlink target (`/app/backend/node_modules/@nestjs/platform-express`) is a build-time-only, same-image path copied verbatim from the already-shipped notifications-service Dockerfile - no external or user-controlled input. |
</threat_model>

<verification>
1. `grep -rn "url: '\[::\]:500[1-7]'" backend/apps/{auth,wallet,events,stays,marketplace,admin,ai}-service/src/main.ts` returns exactly 7 matches, one per file, each with the correct distinct port (5001-5007) and none reverted to `0.0.0.0`.
2. `grep -l "NestFactory.create(AppModule)" backend/apps/{auth,wallet,events,stays,marketplace,admin,ai}-service/src/main.ts | wc -l` equals 7.
3. `ls backend/apps/{auth,wallet,events,stays,marketplace,admin,ai}-service/src/health.controller.ts | wc -l` equals 7.
4. `grep -l "TerminusModule" backend/apps/{auth,wallet,events,stays,marketplace,admin,ai}-service/src/app.module.ts | wc -l` equals 7, and `grep -l "HealthController" ...` (same glob) also equals 7.
5. `grep -l "ln -s /app/backend/node_modules/@nestjs/platform-express" backend/apps/{auth,wallet,events,stays,marketplace,admin,ai}-service/Dockerfile | wc -l` equals 7.
6. `grep -c "^EXPOSE 8080$" backend/apps/{auth,wallet,events,stays,marketplace,admin,ai}-service/Dockerfile` shows exactly 1 per file (no duplicate EXPOSE 8080 lines).
7. `grep -l 'healthcheckPath = "/healthz"' backend/apps/{auth,wallet,events,stays,marketplace,admin,ai}-service/railway.toml | wc -l` equals 7.
8. `git status` confirms only the 35 listed files changed (21 modified/new source files + 7 Dockerfiles + 7 railway.toml), `git branch --show-current` confirms still on the working branch (not `main`), and no deploy/push command was run.
</verification>

<success_criteria>
- All 7 microservices (auth, wallet, events, stays, marketplace, admin, ai) run the hybrid gRPC+HTTP bootstrap, matching notifications-service's pattern exactly.
- Each service's gRPC transport IPv6 bind (`[::]:<port>`) is preserved unchanged from the prior fix.
- Each service exposes `GET /healthz` via a new `HealthController` registered through `TerminusModule`.
- Each service's Dockerfile hoists `@nestjs/platform-express` before `COPY backend/` and exposes port 8080 for the HTTP healthcheck.
- Each service's `railway.toml` declares `healthcheckPath = "/healthz"` under `[deploy]`.
- No RPC method body, proto file, or existing feature-module import was touched.
- No merge to `main`. No Railway deploy triggered. All changes committed only on the current branch.
</success_criteria>

<output>
After completion, create `.planning/quick/260802-8x8-convert-7-grpc-microservices-to-hybrid-b/260802-8x8-SUMMARY.md`
</output>
