---
phase: quick
plan: 260728-9ha
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/app/(tabs)/book.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "The Book screen's 5-item section switcher (Events, Stays, Studio, Market, Tours) is horizontally scrollable, so all 5 sections are reachable even when their combined width exceeds the screen"
    - "The bottom border under the switcher still spans the full screen width, not just the width of its scrollable content"
  artifacts:
    - path: "mobile/app/(tabs)/book.tsx"
      provides: "Section switcher wrapped in a horizontal ScrollView instead of a plain non-scrolling row View"
      contains: "switcherScroll"
---

<objective>
User reported being unable to "slide through" the Events/Stays/Studio/Market/Tours row on the Book screen. Confirmed via direct code read: the switcher (`mobile/app/(tabs)/book.tsx` lines ~761-771) was a plain `<View style={{flexDirection:'row'}}>` — not wrapped in any scroll container. With 5 chip labels, this row can exceed screen width on many devices, and anything past the visible edge was completely unreachable (no scroll affordance existed at all).

Purpose: Every one of the 5 top-level Book sections must be reachable regardless of screen width.
Output: The switcher row is now a horizontal `ScrollView` (`showsHorizontalScrollIndicator={false}`), with the bottom border moved to the ScrollView container itself (so it still spans the full screen width even when the scrollable content is narrower, e.g. on wide tablets).
</objective>

<context>
Made directly (not through the full plan → worktree-executor cycle) — a small, well-understood, standard React Native fix (wrap an overflowing row in a horizontal ScrollView), verified via `tsc --noEmit`. Live emulator re-verification was not possible because the Android emulator had disconnected after this session's very long test run; the user will confirm on the next APK install.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the Book screen section switcher horizontally scrollable</name>
  <files>mobile/app/(tabs)/book.tsx</files>
  <action>
    Added `ScrollView` to the `react-native` import list. Wrapped the section-switcher `<View style={styles.switcherWrap}>...</View>` block in `<ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.switcherWrap} style={styles.switcherScroll}>...</ScrollView>`. Split the original `switcherWrap` style: the row-layout properties (`flexDirection`, `paddingHorizontal`, `paddingVertical`, `gap`) stayed on `switcherWrap` (now the ScrollView's `contentContainerStyle`), while `borderBottomWidth`/`borderBottomColor` moved to a new `switcherScroll` style (`{ flexGrow: 0, borderBottomWidth: 1, borderBottomColor: BORDER_SUBTLE }`) applied to the ScrollView itself, so the border always spans the full screen width regardless of how wide the scrollable content is.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>The 5-item switcher scrolls horizontally when it overflows, the bottom border spans full width, tsc --noEmit passes clean.</done>
</task>

</tasks>

<success_criteria>
- All 5 Book sections (Events, Stays, Studio, Market, Tours) are reachable by swiping the switcher row on any screen width.
- No visual regression to the border/padding when the row already fit on screen.
- `cd mobile && npx tsc --noEmit` passes clean.
</success_criteria>

<output>
After completion, create `.planning/quick/260728-9ha-fix-book-screen-s-5-item-section-switche/260728-9ha-SUMMARY.md`
</output>
