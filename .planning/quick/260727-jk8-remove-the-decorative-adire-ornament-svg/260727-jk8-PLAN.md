---
phase: quick
plan: 260727-jk8
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/app/onboarding.tsx
  - mobile/app/auth/forgot-password.tsx
  - mobile/app/auth/register.tsx
  - mobile/app/auth/email.tsx
  - mobile/app/auth/phone.tsx
  - mobile/app/events/[id].tsx
  - mobile/app/search.tsx
  - mobile/app/(tabs)/index.tsx
  - mobile/app/(tabs)/profile.tsx
  - mobile/app/(tabs)/wallet.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "No screen in the app renders the decorative Adire ornament SVG pattern anywhere — it is fully removed, not repositioned or resized"
    - "The KYC-tier-limit card on the Profile tab ('you') no longer has any element overlapping or obscuring the TIER 2/TIER 3 daily-limit text, because the ornament that was rendered inside that card is gone"
    - "No other decorative element (gradients, gold color scheme, wordmark, icons) was touched on any of the 10 screens — only the Adire ornament SVG"
    - "mobile passes `npx tsc --noEmit` clean with no new unused-import or undefined-symbol errors"
  artifacts:
    - path: "mobile/app/onboarding.tsx"
      provides: "No AdireOrnament function, render call, adireWrapper style, or dangling react-native-svg import remains"
    - path: "mobile/app/auth/forgot-password.tsx"
      provides: "No AdireOrnament function, render call, adireWrapper style, or dangling react-native-svg import remains"
    - path: "mobile/app/auth/register.tsx"
      provides: "No AdireOrnament function, render call, adireWrapper style, or dangling react-native-svg import remains"
    - path: "mobile/app/auth/email.tsx"
      provides: "No AdireOrnament function, render call, adireWrapper style, or dangling react-native-svg import remains"
    - path: "mobile/app/auth/phone.tsx"
      provides: "No AdireOrnament function, render call, adireWrapper style, or dangling react-native-svg import remains"
    - path: "mobile/app/events/[id].tsx"
      provides: "No AdireOrnament function, render call, ornamentWrap style, or dangling react-native-svg import remains"
    - path: "mobile/app/search.tsx"
      provides: "No AdireOrnament function, render call, heroAdire style, or dangling react-native-svg import remains"
    - path: "mobile/app/(tabs)/index.tsx"
      provides: "No AdireOrnament function, render call, adireOrnamentPos style, or dangling react-native-svg import remains"
    - path: "mobile/app/(tabs)/profile.tsx"
      provides: "No AdireOrnament function, render call, adireContainer style, or dangling react-native-svg import remains — the KYC card no longer overlaps its limit text"
    - path: "mobile/app/(tabs)/wallet.tsx"
      provides: "No AdireOrnament function, render call, adireOrnament style, or dangling react-native-svg import remains"
  key_links: []
---

<objective>
Delete the decorative "Adire ornament" SVG pattern (a locally-duplicated `AdireOrnament` component rendering a Yoruba-textile-inspired square/circle motif) from all 10 mobile screens that currently render it. The user pointed at this element live on the Profile tab, where it visually overlaps and obscures the KYC-tier-limit text ("TIER 2 / ₦1M daily limit"), and confirmed the fix is full removal everywhere, not repositioning.

This is a purely subtractive change: for each file, delete (1) the JSX render block (a `<View>` wrapper whose sole purpose is positioning the ornament, plus the `<AdireOrnament .../>` self-close inside it, plus any comment line directly above it), (2) the local `function AdireOrnament(...) {...}` definition, (3) the now-dead positioning style key from that file's `StyleSheet.create({...})` call, and (4) the `react-native-svg` import line — confirmed via this session's grep that `Svg`/`Rect`/`Line`/`Circle`/`G`/`Path` are used ONLY inside each file's `AdireOrnament` function and nowhere else, so the whole import line is removable in all 10 files.

Purpose: Remove clutter that actively obscures real UI content (KYC limits) and was confirmed by the user as unnecessary everywhere.
Output: 10 mobile screen files with zero `AdireOrnament` references, zero dead styles, zero dangling imports; `npx tsc --noEmit` clean; no other visual/behavioral change.

This plan has 2 tasks, split 5 files / 5 files with zero file overlap — safe to execute in either order, and safe to parallelize if the executor supports concurrent task execution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<verified_facts>
Confirmed by direct file reads and grep this session (do not re-derive from scratch — re-open each file only to confirm these exact line numbers haven't drifted since this read, then edit). Two visually distinct `AdireOrnament` variants exist across the 10 files — both are locally duplicated per-file, no shared import:

- **Variant A** (`Rect`/`Line`/`Circle`, size-prop driven, ~14-line function): `onboarding.tsx`, `events/[id].tsx`, `auth/forgot-password.tsx`, `search.tsx`, `auth/register.tsx`, `auth/email.tsx`, `auth/phone.tsx`
- **Variant B** (`G`/`Rect`/`Path`/`Circle`, fixed 80x80 viewBox, ~12-line function): `(tabs)/index.tsx`, `(tabs)/profile.tsx`, `(tabs)/wallet.tsx`

Confirmed via grep across the whole `mobile/app/` tree: none of `Svg`, `G`, `Rect`, `Path`, `Line`, `Circle` appear anywhere in these 10 files outside the `AdireOrnament` function body and its `import ... from 'react-native-svg'` line — safe to delete the entire import line in every file.

| # | File | Import line | Function def | Render block (incl. comment if present) | Dead style key + location |
|---|------|-------------|---------------|-------------------------------------------|----------------------------|
| 1 | `mobile/app/onboarding.tsx` | line 10 | lines 23-69 (incl. `// ── Adire ornament ──` header comment at 23 and trailing blank at 69 — leave the `// ── Screen ──` comment at line 70 intact) | lines 111-114 (`{/* ── Adire ornament — centered at top ── */}` comment + `<View style={styles.adireWrapper} pointerEvents="none"><AdireOrnament size={260} opacity={0.18} /></View>`) | `adireWrapper` (lines 190-197) |
| 2 | `mobile/app/auth/forgot-password.tsx` | line 16 | lines 29-42 (no header comment above it) | lines 98-100 (`<View style={styles.adireWrapper} pointerEvents="none"><AdireOrnament /></View>`, no comment above) | `adireWrapper` (lines 146-153) |
| 3 | `mobile/app/auth/register.tsx` | line 18 | lines 45-58 (no header comment above it) | lines 189-191 (`<View style={styles.adireWrapper} pointerEvents="none"><AdireOrnament /></View>`, no comment above) | `adireWrapper` (lines 411-418) |
| 4 | `mobile/app/auth/email.tsx` | line 17 | lines 32-45 (no header comment above it) | lines 99-101 (`<View style={styles.adireWrapper} pointerEvents="none"><AdireOrnament /></View>`, no comment above) | `adireWrapper` (lines 189-196) |
| 5 | `mobile/app/auth/phone.tsx` | line 16 | lines 38-51 (no header comment above it) | lines 115-117 (`<View style={styles.adireWrapper} pointerEvents="none"><AdireOrnament /></View>`, no comment above) | `adireWrapper` (lines 184-193) |
| 6 | `mobile/app/events/[id].tsx` | line 14 | lines 25-39 (incl. `// ── AdireOrnament ──` header comment at 25) | lines 196-199 (`{/* Adire ornament */}` comment + `<View style={s.ornamentWrap} pointerEvents="none"><AdireOrnament size={200} opacity={0.32} /></View>`) | `ornamentWrap` (lines 395-399, in the `s` StyleSheet) |
| 7 | `mobile/app/search.tsx` | line 18 | lines 44-89 (incl. `// ── Adire ornament ──` header comment at 44) | lines 336-339 (`{/* Adire ornament top-right */}` comment + `<View style={styles.heroAdire} pointerEvents="none"><AdireOrnament size={80} opacity={0.22} /></View>`) | `heroAdire` (lines 563-567) |
| 8 | `mobile/app/(tabs)/index.tsx` | line 36 | lines 76-99 (incl. `// ── Adire ornament SVG ──` header comment at 76) | lines 501-503 (`<View style={styles.adireOrnamentPos}><AdireOrnament size={120} color={GOLD} opacity={0.22} /></View>` — NOTE: this one has no `pointerEvents="none"` and no comment above it, confirmed by direct read; delete the 3-line block as-is) | `adireOrnamentPos` (lines 682-686) |
| 9 | `mobile/app/(tabs)/profile.tsx` | line 20 | lines 150-165 (incl. `// ── Adire Ornament ──` header comment at 150) | lines 690-693 (`{/* Adire ornament top-right */}` comment + `<View style={styles.adireContainer} pointerEvents="none"><AdireOrnament size={120} opacity={0.4} /></View>` — this is the exact instance the user screenshotted, inside the KYC-progress gradient card) | `adireContainer` (lines 1215-1219) |
| 10 | `mobile/app/(tabs)/wallet.tsx` | line 18 | lines 124-139 (incl. `// ── Adire Ornament ──` header comment at 124) | lines 340-343 (`{/* Adire ornament top-right */}` comment + `<View style={styles.adireOrnament} pointerEvents="none"><AdireOrnament size={110} color={GOLD} opacity={0.25} /></View>`) | `adireOrnament` (lines 661-665) |

For every file, after deleting the function and its header comment, confirm the blank-line spacing around the deletion point stays consistent with the surrounding code (a single blank line between sections, matching the file's existing `// ── Section ──` divider convention) — do not leave double-blank gaps or orphaned divider comments.
</verified_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Adire ornament from the 5 auth-flow screens (Variant A)</name>
  <files>mobile/app/onboarding.tsx, mobile/app/auth/forgot-password.tsx, mobile/app/auth/register.tsx, mobile/app/auth/email.tsx, mobile/app/auth/phone.tsx</files>
  <action>
    For each of these 5 files, read it first to reconfirm current line numbers against the `<verified_facts>` table (row #1-5), then apply the same 4-part deletion:
    1. Delete the JSX render block — the `<View style={styles.adireWrapper} pointerEvents="none">...<AdireOrnament .../>...</View>` wrapper (and, in `onboarding.tsx` only, the `{/* ── Adire ornament — centered at top ── */}` comment line directly above it — the other 4 files have no comment there).
    2. Delete the local `function AdireOrnament({ size, opacity }) {...}` definition in full, including its `// ── Adire ornament ──` header comment where present (`onboarding.tsx` only — the other 4 files have no header comment on this function).
    3. Delete the `adireWrapper` key from that file's `StyleSheet.create({...})` call.
    4. Delete the `import Svg, { Rect, Line, Circle } from 'react-native-svg';` line entirely (confirmed unused elsewhere in each of these 5 files).
    Do not touch any other gradient, wordmark, form field, or CTA in these files — this is pure deletion of ornament-only code.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>None of onboarding.tsx, auth/forgot-password.tsx, auth/register.tsx, auth/email.tsx, auth/phone.tsx contain the string "AdireOrnament", "adireWrapper", or "react-native-svg" anymore. tsc --noEmit passes clean with no new errors in these 5 files.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Adire ornament from the 5 tab/detail screens (mixed Variant A/B), including the profile.tsx KYC-card instance the user flagged</name>
  <files>mobile/app/events/[id].tsx, mobile/app/search.tsx, mobile/app/(tabs)/index.tsx, mobile/app/(tabs)/profile.tsx, mobile/app/(tabs)/wallet.tsx</files>
  <action>
    For each of these 5 files, read it first to reconfirm current line numbers against the `<verified_facts>` table (row #6-10), then apply the same 4-part deletion:
    1. Delete the JSX render block — the `<View style={styles.XXX} pointerEvents="none">...<AdireOrnament .../>...</View>` wrapper (`(tabs)/index.tsx` has no `pointerEvents="none"` on its wrapper — delete it as-is regardless), including the `{/* Adire ornament ... */}` comment line directly above it in `events/[id].tsx`, `search.tsx`, `(tabs)/profile.tsx`, and `(tabs)/wallet.tsx` (`(tabs)/index.tsx` has no comment there).
    2. Delete the local `function AdireOrnament(...) {...}` definition in full, including its `// ── Adire ornament ──`/`// ── AdireOrnament ──`/`// ── Adire Ornament ──` header comment (present in all 5 of these files, unlike task 1's files).
    3. Delete the dead style key from that file's `StyleSheet.create({...})` call: `ornamentWrap` (events/[id].tsx), `heroAdire` (search.tsx), `adireOrnamentPos` ((tabs)/index.tsx), `adireContainer` ((tabs)/profile.tsx), `adireOrnament` ((tabs)/wallet.tsx).
    4. Delete the `react-native-svg` import line entirely — `import Svg, { Rect, Line, Circle } from 'react-native-svg';` in events/[id].tsx and search.tsx, `import Svg, { G, Rect, Path, Circle } from 'react-native-svg';` in (tabs)/index.tsx, (tabs)/profile.tsx, (tabs)/wallet.tsx (confirmed unused elsewhere in each).
    In `(tabs)/profile.tsx` specifically: after deletion, the KYC-progress gradient card (`kycGradient` style) must render with no ornament element inside it at all — confirm by reading the resulting JSX that the card's children are just the kicker/headline/progress content, nothing else added or repositioned.
    Do not touch any other gradient, icon, chip, CTA, or unrelated logic in these files — this is pure deletion of ornament-only code.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>None of events/[id].tsx, search.tsx, (tabs)/index.tsx, (tabs)/profile.tsx, (tabs)/wallet.tsx contain the string "AdireOrnament" or "react-native-svg" anymore, and none contain "ornamentWrap"/"heroAdire"/"adireOrnamentPos"/"adireContainer"/"adireOrnament" as style keys. The KYC-progress card on the Profile tab has no overlapping ornament element. tsc --noEmit passes clean with no new errors in these 5 files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Pure client-side UI deletion — no new data flow, no new user input, no server-side change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | N/A | mobile/app/*.tsx (10 files) | accept | No trust boundary is touched — this change only deletes a decorative SVG component, its wrapper View, a dead style key, and an unused import across 10 screens. No API calls, no auth logic, no data handling changed. |

</threat_model>

<verification>
1. `cd mobile && npx tsc --noEmit` — no new type errors across all 10 files.
2. Grep confirmation across the 10 files: zero matches for `AdireOrnament` and zero matches for `from 'react-native-svg'`.
3. Manual read-through of `mobile/app/(tabs)/profile.tsx`'s KYC-progress card JSX — confirm no ornament element remains and the TIER 2/TIER 3 limit text has no overlapping sibling.
4. Manual read-through of each file's `StyleSheet.create({...})` call — confirm the dead positioning style key (`adireWrapper`/`ornamentWrap`/`heroAdire`/`adireOrnamentPos`/`adireContainer`/`adireOrnament`) is gone and no other style key was touched.
</verification>

<success_criteria>
- All 10 mobile screens render with zero trace of the Adire ornament SVG pattern — no component, no wrapper, no dead style, no dangling import.
- The KYC-tier-limit text on the Profile tab is fully unobstructed.
- No other visual element (gradients, wordmark, gold color scheme, icons, form fields, CTAs) was altered on any of the 10 screens.
- `cd mobile && npx tsc --noEmit` passes clean.
</success_criteria>

<output>
After completion, create `.planning/quick/260727-jk8-remove-the-decorative-adire-ornament-svg/260727-jk8-SUMMARY.md`
</output>
