---
phase: quick
plan: 260727-c41
subsystem: api
tags: [nestjs, prisma, marketplace, events, users, self-service, vendor, organiser]

# Dependency graph
requires:
  - phase: quick-260727-8v6
    provides: "Existing marketplace image-upload endpoints and controller/service structure this plan extends"
provides:
  - "GET /vendors/me — current user's own vendor profile"
  - "GET /products/mine — current vendor's own products, including inactive/paused/out-of-stock"
  - "GET /orders/vendor — orders placed against the current user's vendor account, for fulfillment"
  - "POST /users/me/become-organiser — self-service ORGANISER role promotion"
  - "GET /events/mine — current organiser's own events regardless of status"
affects: [mobile-vendor-product-management, mobile-organiser-event-management]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Self-scoped GET /mine and /me routes resolve caller identity exclusively from @CurrentUser() JWT — never from a client-supplied id"
    - "New /mine or /me routes are declared in file order before their sibling GET /:id route to avoid Express/NestJS route-shadowing"

key-files:
  created: []
  modified:
    - backend/src/modules/marketplace/marketplace.controller.ts
    - backend/src/modules/marketplace/marketplace.service.ts
    - backend/src/modules/marketplace/__tests__/marketplace.service.spec.ts
    - backend/src/modules/users/users.controller.ts
    - backend/src/modules/users/users.service.ts
    - backend/src/modules/users/__tests__/users.service.spec.ts
    - backend/src/modules/events/events.controller.ts
    - backend/src/modules/events/events.service.ts
    - backend/src/modules/events/__tests__/events.service.spec.ts

key-decisions:
  - "findMyVendor/findMyProducts/findVendorOrders added as new sibling methods rather than modifying createVendor/findProducts/updateOrderStatus, preserving existing public-facing behavior unchanged"
  - "becomeOrganiser mirrors becomeHost's simple (non-transactional) pattern verbatim, since ORGANISER has no separate profile model unlike TourGuide"
  - "findMine (events) deliberately omits the status: 'PUBLISHED' filter used by findAll, so organisers can see DRAFT/PENDING_APPROVAL/APPROVED/PUBLISHED/CANCELLED events"

patterns-established:
  - "Vendor/organiser self-service scope resolution always goes through a caller-derived lookup (findMyVendor(user.userId), user.userId directly) — no route in this plan accepts a client-supplied vendor/product/order/organizer id"

requirements-completed: []

# Metrics
duration: 13min
completed: 2026-07-27
---

# Quick Task 260727-c41: Backend Self-Service Vendor/Organiser Endpoints Summary

**Five additive GET/POST endpoints across marketplace, users, and events modules — vendor/product/order self-service scoping and self-service ORGANISER role promotion — unblocking the next two mobile management-suite quick tasks.**

## Performance

- **Duration:** 13 min (09:01:50 to 09:14:38, git commit timestamps)
- **Started:** 2026-07-27T09:01:50-05:00
- **Completed:** 2026-07-27T09:14:38-05:00
- **Tasks:** 3/3 completed
- **Files modified:** 9 (3 per task: controller, service, spec)

## Accomplishments
- Vendors can now fetch their own profile (`GET /vendors/me`), all their own products including inactive/paused/out-of-stock ones (`GET /products/mine`), and all orders placed against their vendor account for fulfillment (`GET /orders/vendor`)
- Any authenticated user can self-promote to ORGANISER (`POST /users/me/become-organiser`), mirroring the existing become-host/become-guide/become-driver pattern
- Organisers can fetch all their own events regardless of status (`GET /events/mine`) — DRAFT, PENDING_APPROVAL, APPROVED, PUBLISHED, CANCELLED — unlike the public `findAll` which is PUBLISHED-only

## Task Commits

Each task was committed atomically:

1. **Task 1: Marketplace self-service endpoints — GET /vendors/me, GET /products/mine, GET /orders/vendor** - `76eaf64` (feat)
2. **Task 2: POST /users/me/become-organiser self-service role promotion** - `928009f` (feat)
3. **Task 3: GET /events/mine — organiser's own events regardless of status** - `ff0eb1f` (feat)

**Plan metadata:** committed separately by orchestrator (docs commit not made by this executor per constraints)

## Files Created/Modified
- `backend/src/modules/marketplace/marketplace.service.ts` - Added `findMyVendor(userId)`, `findMyProducts(vendorId)`, `findVendorOrders(vendorId)`
- `backend/src/modules/marketplace/marketplace.controller.ts` - Added `GET /vendors/me` (VendorsController), `GET /products/mine` (ProductsController, declared before `GET /products/:id`), `GET /orders/vendor` (OrdersController)
- `backend/src/modules/marketplace/__tests__/marketplace.service.spec.ts` - New describe blocks for all three methods; added `order.findMany` to mockPrisma
- `backend/src/modules/users/users.service.ts` - Added `becomeOrganiser(userId)`, inserted between `becomeGuide` and `eraseData`
- `backend/src/modules/users/users.controller.ts` - Added `POST /users/me/become-organiser`, grouped with become-host/become-guide/become-driver
- `backend/src/modules/users/__tests__/users.service.spec.ts` - New `becomeOrganiser` describe block mirroring `becomeDriver`'s test shape
- `backend/src/modules/events/events.service.ts` - Added `findMine(organizerId)` with no status filter
- `backend/src/modules/events/events.controller.ts` - Added `GET /events/mine`, declared after `tickets/mine` and before `GET /events/:id`
- `backend/src/modules/events/__tests__/events.service.spec.ts` - New `findMine` describe block verifying no `status` key in the where clause

## Decisions Made
- Reused `findMyVendor` across all three marketplace controllers (VendorsController, ProductsController, OrdersController) as the single source of vendor-scope resolution — no duplicate lookup logic
- Did not touch `createVendor`, `findProducts`, `updateOrderStatus`, or `findAll` (events) — all five new endpoints are additive, existing public-facing behavior is byte-for-byte unchanged
- `becomeOrganiser` inserted anchored relative to `becomeGuide`/`eraseData` rather than at a fixed line number, since a concurrent sibling task (260727-bph) had already added `becomeDriver` to the same two files — this merged cleanly with no conflict

## Deviations from Plan

None - plan executed exactly as written. All five endpoints, three new service methods, and one mirrored service method match the plan's verified_facts and task actions exactly.

## Issues Encountered

- **Worktree had no `node_modules`** (fresh worktree checkout). Resolved by running `npm install --prefer-offline --no-audit --no-fund` from the worktree root (~4 min, using local npm cache).
- **Post-install `npx prisma generate` was required** — the freshly installed `@prisma/client` package didn't match the schema-generated types (`Prisma.PrismaClientKnownRequestError` type error in `settlement.service.ts`, an unrelated pre-existing file). Running `npx prisma generate` in `backend/` regenerated the client and resolved the type error with no code changes needed.
- Attempted to speed up node_modules setup via an NTFS junction (`mklink /J`) pointing at the main worktree's `node_modules` before falling back to `npm install` — the `cmd.exe` invocations via the Bash tool did not execute the mklink command (likely a quoting/shell interaction issue specific to this environment) and no junction was created; abandoned in favor of the full `npm install`, which succeeded.

## User Setup Required

None - no external service configuration required. All five endpoints are pure backend additions using existing Prisma models and existing guard/decorator infrastructure.

## Next Phase Readiness

- Backend foundation is complete for the two planned mobile follow-up quick tasks: mobile vendor product management (product CRUD, image upload, order fulfillment UI) and mobile organiser event management (event CRUD, ticket types, analytics UI) — both can now call `GET /vendors/me`, `GET /products/mine`, `GET /orders/vendor`, `POST /users/me/become-organiser`, and `GET /events/mine`.
- `cd backend && npx tsc --noEmit` passes with no new type errors.
- Full backend jest suite passes: 76 suites, 905 tests, 0 failures.
- No blockers.

---
*Phase: quick*
*Completed: 2026-07-27*

## Self-Check: PASSED

All commits (76eaf64, 928009f, ff0eb1f) verified present in git log. All modified files (marketplace.service.ts, users.service.ts, events.service.ts) verified present on disk. New methods (findMyVendor, becomeOrganiser, findMine) verified present in their respective source files. SUMMARY.md verified present at the expected path.
