---
phase: quick
plan: 260727-8v6
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/marketplace/marketplace.controller.ts
  - backend/src/modules/marketplace/marketplace.service.ts
  - backend/src/modules/marketplace/__tests__/marketplace.service.spec.ts
  - backend/src/modules/admin/admin.controller.ts
  - backend/src/modules/admin/admin.service.ts
  - backend/src/modules/admin/__tests__/admin.service.spec.ts
  - mobile/app/events/[id].tsx
  - mobile/components/book/EventsSubsection.tsx
  - mobile/app/(tabs)/index.tsx
  - mobile/app.json
  - mobile/assets/notification-icon.png
autonomous: true
requirements: []

must_haves:
  truths:
    - "An approved VENDOR can POST a jpg/png/webp (≤5MB) to /products/:id/images for their own product and get back a live S3 url appended to that product's imageUrls; they cannot do this for another vendor's product"
    - "A SUPER_ADMIN or LGA_ADMIN can POST a jpg/png/webp (≤5MB) to /admin/attractions/:id/images for any attraction (no ownership concept exists for attractions) and get back a live S3 url appended to that attraction's imageUrls"
    - "The mobile events detail screen shows the event's real cover photo (imageUrls[0]) as its hero background when one exists, falling back to the existing gradient when it does not"
    - "The mobile events list card (Book tab) shows each event's real cover photo when present, falling back to its existing per-card gradient when not"
    - "The mobile Discover tab's attraction cards show the attraction's real photo when present, falling back to the existing per-card gradient when not, with the bookmark heart button still rendering on top in both cases"
    - "Building the mobile app does not fail on a missing Android notification icon asset — a real (if placeholder) notification-icon.png exists and app.json's expo-notifications plugin config points to it"
    - "Full backend test suite and tsc --noEmit remain clean; mobile tsc --noEmit remains clean"
  artifacts:
    - path: "backend/src/modules/marketplace/marketplace.service.ts"
      provides: "uploadImage(id, userId, file) — vendor-ownership-gated product image upload using ImageService.resizeProduct + S3Service.upload"
      contains: "resizeProduct"
    - path: "backend/src/modules/marketplace/marketplace.controller.ts"
      provides: "POST /products/:id/images on ProductsController, guarded VENDOR-only"
      contains: "products/:id/images"
    - path: "backend/src/modules/admin/admin.service.ts"
      provides: "uploadAttractionImage(id, file) — admin-only attraction image upload, no ownership check"
      contains: "async uploadAttractionImage"
    - path: "backend/src/modules/admin/admin.controller.ts"
      provides: "POST admin/attractions/:id/images route, inheriting class-level SUPER_ADMIN/LGA_ADMIN guard"
      contains: "attractions/:id/images"
    - path: "mobile/app/events/[id].tsx"
      provides: "Hero background renders event.imageUrls[0] via expo-image when present"
      contains: "imageUrls"
    - path: "mobile/components/book/EventsSubsection.tsx"
      provides: "Event card hero renders item.imageUrls[0] via expo-image when present"
      contains: "imageUrls"
    - path: "mobile/app/(tabs)/index.tsx"
      provides: "Attraction card photo renders item.imageUrls[0] via expo-image when present, heart button still on top"
      contains: "imageUrls"
    - path: "mobile/assets/notification-icon.png"
      provides: "96x96 transparent placeholder PNG for Android notification icon"
    - path: "mobile/app.json"
      provides: "expo-notifications plugin configured with icon + color tuple"
      contains: "notification-icon.png"
  key_links:
    - from: "backend/src/modules/marketplace/marketplace.controller.ts uploadImage route"
      to: "backend/src/modules/marketplace/marketplace.service.ts uploadImage"
      via: "direct method call, mirroring EventsController/EventsService's uploadImage pattern"
      pattern: "marketplaceService\\.uploadImage"
    - from: "backend/src/modules/admin/admin.controller.ts uploadAttractionImage route"
      to: "backend/src/modules/admin/admin.service.ts uploadAttractionImage"
      via: "direct method call"
      pattern: "adminService\\.uploadAttractionImage"
    - from: "mobile/app/events/[id].tsx hero"
      to: "GET /events/:id response's imageUrls field"
      via: "event.imageUrls?.[0] read from the existing useQuery(['event', id]) result"
      pattern: "event\\.imageUrls"
    - from: "mobile/app.json expo-notifications plugin"
      to: "mobile/assets/notification-icon.png"
      via: "plugin config icon path"
      pattern: "notification-icon\\.png"
---

<objective>
Followup to an image/motion asset brief prepared for the design team. Backend already correctly handles Events and Stays photo upload+display end-to-end. This plan closes the four remaining gaps: Marketplace has no upload endpoint (mobile already displays product photos fine), Tourism/Attractions have neither upload nor display, Events has upload but mobile never displays the photo, and the Android notification icon isn't configured.

Purpose: Give vendors and admins a real write path for product/attraction photos, make the mobile app actually show the photos that already flow through the pipeline for Events, and stop the Android build from shipping with an unconfigured/default notification icon.
Output: POST /products/:id/images, POST /admin/attractions/:id/images, mobile hero/card image display for events and attractions, and a configured (placeholder-backed) Android notification icon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<verified_facts>
Confirmed by direct inspection (2026-07-27, branch microservices-redesign):

Backend / image upload (Tasks 1-2):

- EventsController.uploadImage (backend/src/modules/events/events.controller.ts:98-116) and EventsService.uploadImage (backend/src/modules/events/events.service.ts:167-183) are the exact template: @Post(':id/images') + @UseGuards(JwtAuthGuard, RolesGuard) + @Roles(...) + @ApiBearerAuth() + @ApiConsumes('multipart/form-data') + @UseInterceptors(FileInterceptor('file', { storage: memoryStorage(), limits: { fileSize: 5 * 1024 * 1024 } })); the service method validates, resizes, uploads to S3 under a {domain}/{id}/{uuid}.webp key, then prisma.{model}.update({ where: { id }, data: { imageUrls: { push: url } } }) and returns { url }.
- ImageService (backend/src/common/services/image.service.ts) already exports resizeProduct(buffer) (1200x1200 webp q86, currently unused anywhere) and a generic validateImage(file) (identical body to validateEventImage) — reuse both directly, no new methods. resizeEventCover(buffer) (1200x630 webp q85) is the closer fit for Attraction's wide card aspect — reuse it for Task 2 rather than adding a third near-duplicate resize method.
- ImageService, S3Service are provided AND exported by the @Global() CommonModule (backend/src/common/common.module.ts) — injectable in MarketplaceService/AdminService constructors with zero module-import changes. Confirmed: neither MarketplaceModule nor AdminModule needs any new imports entry.
- Product model (schema.prisma:549-573) has imageUrls String[] — identical field shape to Event.imageUrls. Product has no userId/organizerId — ownership goes through Vendor.userId, exactly as MarketplaceService.updateProduct (marketplace.service.ts:144-163) already checks: const vendor = await this.prisma.vendor.findUnique({ where: { userId } }); if (!vendor || product.vendorId !== vendor.id) throw new ForbiddenException('Not your product'). Mirror this exact shape for the new uploadImage.
- Attraction model (schema.prisma:333-355) has NO ownership/creator field at all — only lgaId (FK to LGA, not a user). TourismController/TourismService today expose only public GETs + an authenticated bookmark toggle — intentionally no mutation routes; do NOT add one there. Instead, add to AdminController (backend/src/modules/admin/admin.controller.ts), which already carries a class-level @UseGuards(JwtAuthGuard, RolesGuard) + @Roles(UserRole.SUPER_ADMIN, UserRole.LGA_ADMIN) guard covering the whole controller (confirmed lines 15-19) — no per-route @Roles override needed, matching sibling updateVendorStatus/updateEventStatus routes.
- AdminController/AdminService already have an updateEventStatus route/method and UpdateEventStatusDto (backend/src/modules/admin/dto/update-event-status.dto.ts) from a prior quick task — pre-existing and out of scope; do not touch beyond leaving as-is. AdminController already imports Post from @nestjs/common — confirm before adding, do not duplicate.
- AdminService's constructor currently injects only PrismaService, SettlementService (admin.service.ts:16-21) — this plan adds ImageService, S3Service. MarketplaceService's constructor currently injects prisma, paystack, sendgrid, kafka, settlementService (marketplace.service.ts:31-37) — this plan adds ImageService, S3Service. uuidv4 is already imported in marketplace.service.ts; admin.service.ts needs a new import { v4 as uuidv4 } from 'uuid';.
- S3Service.upload(key: string, body: Buffer, contentType: string): Promise<string> (backend/src/common/services/s3.service.ts:69) — same signature already used elsewhere.
- backend/src/modules/marketplace/__tests__/marketplace.service.spec.ts already declares mockPrisma.product.findFirst/update and mockPrisma.vendor.findUnique (lines 66-77), plus mockVendor/mockProduct fixtures — only need to add ImageService/S3Service mocks to the TestingModule providers array (lines 87-96) and a new describe('uploadImage', ...) block. backend/src/modules/admin/__tests__/admin.service.spec.ts has mockPrisma (lines 7-19) with no attraction key yet — add attraction: { findFirst: jest.fn(), update: jest.fn() }, plus ImageService/S3Service mocks to the TestingModule providers (lines 30-36).

Mobile / image display (Task 3):

- mobile/app/events/[id].tsx:177-202 hero: first LinearGradient uses hardcoded colors={['#3D2A05', '#1A1200']} as the base layer; event comes from useQuery({ queryKey: ['event', id], queryFn: () => fetcher(`/events/${id}`) }) then const event = data?.data ?? data; (lines 84-99) — event.imageUrls is available but currently unread. s.hero style: { height: 420, position: 'relative', overflow: 'hidden', justifyContent: 'flex-end' }. No Image import currently in this file (imports LinearGradient, no RN Image or expo-image import) — safe to add import { Image } from 'expo-image'; without a name collision.
- mobile/components/book/EventsSubsection.tsx:132-156 cardHero: first LinearGradient uses colors={gradientForIndex(gradientIndex)} (deterministic per-card gradient, keep the function as fallback). styles.cardHero: { height: 180, width: '100%', position: 'relative' }. Data source: useQuery({ queryKey: ['events'], queryFn: () => fetcher('/events?limit=10') }) (lines 187-190) — backend's EventsService.findAll has no select projection, so imageUrls is present unfiltered on each row. EventItem type (lines 95-104) has no imageUrls field — needs to be added. No Image import currently in this file — safe to add.
- mobile/app/(tabs)/index.tsx:298-318 attraction card: <LinearGradient colors={[c0, c1]} ... style={styles.attractionPhoto}> wraps a <TouchableOpacity style={styles.heartBtn}> as a child. styles.attractionPhoto (line 1012-1018): { height: 96, padding: SPACE_2, flexDirection: 'row', justifyContent: 'flex-end', alignItems: 'flex-start' } — no overflow: 'hidden' currently (the parent attractionCard at line 1000-1011 already has overflow: 'hidden', and attractionPhoto's 96px height exactly matches the intended image height, so an absoluteFillObject image will not visually overflow, but add overflow: 'hidden' to attractionPhoto itself for safety per the brief). heartBtn (line 1019-1028) has no position: 'absolute' — it is positioned purely via the parent's flex layout (flexDirection: 'row', justifyContent: 'flex-end', alignItems: 'flex-start'), so converting the outer element to a plain View with the same attractionPhoto style preserves heartBtn's current top-right position automatically as long as heartBtn remains a flex (non-absolute) child alongside an absoluteFillObject-positioned image/gradient. Data source: useQuery({ queryKey: ['attractions'], queryFn: () => fetcher('/attractions?limit=50') }) (lines 431-433) — TourismService.findAll has no select projection, so imageUrls is present on each row; item: any (no interface to update, confirmed no AttractionItem type exists in this file). No RN Image or expo-image import currently in this file (imports LinearGradient, Svg, icons only) — safe to add import { Image } from 'expo-image';.
- mobile/app/stays/[id].tsx imports { Image as ExpoImage } from 'expo-image'; mobile/app/marketplace/[id].tsx imports { Image } from 'expo-image' (no RN Image collision in either file) — both confirm expo-image is already a project dependency and the plain-Image import name is safe to use in files that don't already import RN's Image.

Mobile / notification icon (Task 4):

- mobile/app.json:75 currently has the bare string "expo-notifications" in the plugins array (confirmed, line 75, alongside "expo-router", "expo-secure-store", the expo-image-picker tuple, and the @sentry/react-native/expo tuple already at lines 58-76).
- No mobile/assets/notification-icon.png exists yet. sharp is available via backend/node_modules (already a backend dependency) — generation script must run with cwd: backend/ (e.g. cd backend && node <script>.js) so module resolution finds it, then write the output PNG directly into mobile/assets/notification-icon.png.
- App's documented brand primary color (per CLAUDE.md) is FOREST = '#1A6B3C' — use this for the notification plugin's color field, not '#006400' (that's the existing splash/adaptive-icon background in app.json, a different, deliberately distinct value).
</verified_facts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Marketplace product image upload endpoint</name>
  <files>backend/src/modules/marketplace/marketplace.controller.ts, backend/src/modules/marketplace/marketplace.service.ts, backend/src/modules/marketplace/__tests__/marketplace.service.spec.ts</files>
  <behavior>
    New tests in a describe('uploadImage', ...) block within marketplace.service.spec.ts:
    - Happy path: mockPrisma.product.findFirst resolves mockProduct (vendorId = VENDOR_ID), mockPrisma.vendor.findUnique resolves mockVendor (id = VENDOR_ID, userId = USER_ID); mock ImageService.validateImage/resizeProduct and mock S3Service.upload resolve successfully; assert prisma.product.update is called with { where: { id: PRODUCT_ID }, data: { imageUrls: { push: url } } } and the method returns { url }.
    - Not-found: mockPrisma.product.findFirst resolves null — expect rejection with NotFoundException.
    - Not-owner: mockPrisma.product.findFirst resolves a product with a vendorId different from the requesting user's vendor row (or mockPrisma.vendor.findUnique resolves null) — expect rejection with ForbiddenException.
  </behavior>
  <action>
    In backend/src/modules/marketplace/marketplace.service.ts: import ImageService from ../../common/services/image.service and S3Service from ../../common/services/s3.service; add both as constructor-injected private readonly fields alongside the existing prisma, paystack, sendgrid, kafka, settlementService. Add async uploadImage(id: string, userId: string, file: Express.Multer.File) in the Products section (after removeProduct): look up the product via this.prisma.product.findFirst({ where: { id, deletedAt: null } }), throw NotFoundException('Product not found') if missing; look up the vendor via this.prisma.vendor.findUnique({ where: { userId } }), throw ForbiddenException('Not your product') if !vendor or product.vendorId !== vendor.id (identical shape to updateProduct's ownership check); call this.imageService.validateImage(file), then destructure { buffer: resized, contentType } from this.imageService.resizeProduct(file.buffer); build a key of the form products/{id}/{uuidv4()}.webp; call this.s3.upload(key, resized, contentType) to get url; call this.prisma.product.update({ where: { id }, data: { imageUrls: { push: url } } }); return { url }.

    In backend/src/modules/marketplace/marketplace.controller.ts: add imports for UseInterceptors, UploadedFile from @nestjs/common, FileInterceptor from @nestjs/platform-express, memoryStorage from multer, and ApiConsumes from @nestjs/swagger (alongside the existing ApiTags, ApiBearerAuth, ApiOperation import). In ProductsController, add a new route below remove(): POST :id/images guarded JwtAuthGuard + RolesGuard, @Roles(UserRole.VENDOR), @ApiBearerAuth(), @ApiOperation summary "Upload product image (jpg/png/webp <=5 MB, resized to 1200x1200)", @ApiConsumes('multipart/form-data'), and @UseInterceptors(FileInterceptor('file', { storage: memoryStorage(), limits: { fileSize: 5 * 1024 * 1024 } })), with a handler uploadImage(@Param('id') id: string, @CurrentUser() user: any, @UploadedFile() file: Express.Multer.File) calling this.marketplaceService.uploadImage(id, user.userId, file) — mirror EventsController.uploadImage's exact decorator stack and parameter shape.

    Update marketplace.service.spec.ts: add { provide: ImageService, useValue: mockImageService } and { provide: S3Service, useValue: mockS3 } to the TestingModule providers array, with corresponding imports for ImageService/S3Service and new fixtures mockImageService = { validateImage: jest.fn(), resizeProduct: jest.fn().mockResolvedValue({ buffer: Buffer.from('x'), contentType: 'image/webp' }) } and mockS3 = { upload: jest.fn().mockResolvedValue('https://cdn.example.com/products/x.webp') } declared near the existing mockPaystack/mockSendgrid, then add the describe('uploadImage', ...) block per the behavior above.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && npx jest src/modules/marketplace/__tests__/marketplace.service.spec.ts</automated>
  </verify>
  <done>POST /products/:id/images exists on ProductsController, guarded VENDOR-only; MarketplaceService.uploadImage validates+resizes via ImageService.resizeProduct, uploads via S3Service.upload, and appends the resulting URL to Product.imageUrls, rejecting with NotFoundException/ForbiddenException on missing/not-owned products. New tests (happy path + not-found + not-owner) pass; tsc --noEmit is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tourism/Attraction admin image upload endpoint</name>
  <files>backend/src/modules/admin/admin.controller.ts, backend/src/modules/admin/admin.service.ts, backend/src/modules/admin/__tests__/admin.service.spec.ts</files>
  <behavior>
    New tests in a describe('uploadAttractionImage', ...) block within admin.service.spec.ts:
    - Happy path: mockPrisma.attraction.findFirst resolves an attraction row; mock ImageService.validateImage/resizeEventCover and mock S3Service.upload resolve successfully; assert prisma.attraction.update is called with { where: { id }, data: { imageUrls: { push: url } } } and the method returns { url }.
    - Not-found: mockPrisma.attraction.findFirst resolves null — expect rejection with NotFoundException. (No ownership case — attractions have no creator field; the role guard is the only access gate.)
  </behavior>
  <action>
    In backend/src/modules/admin/admin.service.ts: import ImageService from ../../common/services/image.service, S3Service from ../../common/services/s3.service, and v4 as uuidv4 from uuid; add both services as constructor-injected private readonly fields alongside the existing prisma, settlementService. Add async uploadAttractionImage(id: string, file: Express.Multer.File) in a new "Attractions" section-divider comment block (following this file's existing section-divider convention): look up the attraction via this.prisma.attraction.findFirst({ where: { id, deletedAt: null } }), throw NotFoundException('Attraction not found') if missing (no ownership check — attractions have no creator field, only lgaId); call this.imageService.validateImage(file), then destructure { buffer: resized, contentType } from this.imageService.resizeEventCover(file.buffer) (1200x630 landscape — closer fit for the attraction card's wide aspect than the square resizeProduct); build a key of the form attractions/{id}/{uuidv4()}.webp; call this.s3.upload(key, resized, contentType) to get url; call this.prisma.attraction.update({ where: { id }, data: { imageUrls: { push: url } } }); return { url }.

    In backend/src/modules/admin/admin.controller.ts: add imports for UseInterceptors, UploadedFile from @nestjs/common, FileInterceptor from @nestjs/platform-express, memoryStorage from multer, and ApiConsumes from @nestjs/swagger (alongside the existing ApiTags, ApiBearerAuth, ApiOperation). Add a new route, placed near updateEventStatus: POST attractions/:id/images with @ApiOperation summary "Upload attraction cover image (jpg/png/webp <=5 MB, resized to 1200x630)", @ApiConsumes('multipart/form-data'), and @UseInterceptors(FileInterceptor('file', { storage: memoryStorage(), limits: { fileSize: 5 * 1024 * 1024 } })) — no route-level @Roles override needed (inherits the class-level SUPER_ADMIN/LGA_ADMIN guard). Handler: uploadAttractionImage(@Param('id') id: string, @UploadedFile() file: Express.Multer.File) calling this.adminService.uploadAttractionImage(id, file). Post is already imported in this controller's existing import list — do not add a duplicate.

    Update admin.service.spec.ts: add attraction: { findFirst: jest.fn(), update: jest.fn() } to mockPrisma; add { provide: ImageService, useValue: mockImageService } and { provide: S3Service, useValue: mockS3 } to the TestingModule providers array (with imports and mockImageService/mockS3 fixtures matching Task 1's shape), then add the describe('uploadAttractionImage', ...) block per the behavior above.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && npx jest src/modules/admin/__tests__/admin.service.spec.ts</automated>
  </verify>
  <done>POST admin/attractions/:id/images exists, inheriting the class-level SUPER_ADMIN/LGA_ADMIN guard; AdminService.uploadAttractionImage validates+resizes via ImageService.resizeEventCover, uploads via S3Service.upload, and appends the URL to Attraction.imageUrls, rejecting with NotFoundException when the attraction doesn't exist (no ownership check, by design). New tests (happy path + not-found) pass; tsc --noEmit is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Mobile display wiring — Events hero, Events list card, Attraction cards</name>
  <files>mobile/app/events/[id].tsx, mobile/components/book/EventsSubsection.tsx, mobile/app/(tabs)/index.tsx</files>
  <action>
    mobile/app/events/[id].tsx: add import { Image } from 'expo-image'; near the other imports. In the HERO block (lines 177-202), replace ONLY the first LinearGradient (the colors={['#3D2A05', '#1A1200']} base layer) with: if event.imageUrls?.[0] is truthy, render an expo-image Image with source={{ uri: event.imageUrls[0] }}, style={StyleSheet.absoluteFillObject}, contentFit="cover", transition={200}; else render the existing LinearGradient unchanged as fallback. Leave the Adire ornament View and the second scrim LinearGradient (rgba(5,14,14,0.50) -> transparent -> SURFACE_DEEP) unconditional and untouched in both branches — they are legibility/decoration layers stacked on top, not the placeholder itself.

    mobile/components/book/EventsSubsection.tsx: add import { Image } from 'expo-image'; near the other imports. Add imageUrls?: string[] | null; to the EventItem type (lines 95-104). In EventCard's styles.cardHero block (lines 132-156), replace ONLY the first LinearGradient (the colors={colors} layer, where colors = gradientForIndex(gradientIndex)) with: if item.imageUrls?.[0] is truthy, render an expo-image Image with source={{ uri: item.imageUrls[0] }}, style={StyleSheet.absoluteFillObject}, contentFit="cover", transition={200}; else keep the existing gradient (still used as the deterministic per-card fallback — do not delete gradientForIndex). Leave the second scrim gradient, the EVENT tag chip, and the rating badge unconditional and untouched.

    mobile/app/(tabs)/index.tsx: add import { Image } from 'expo-image'; near the other imports. In AttractionCard (lines 293-334), change the outer LinearGradient element (colors={[c0, c1]}, style={styles.attractionPhoto}) to a plain View with style={styles.attractionPhoto}. Inside it, render — as the first child — either an expo-image Image with source={{ uri: item.imageUrls[0] }}, style={StyleSheet.absoluteFillObject}, contentFit="cover", transition={200} when item.imageUrls?.[0] is truthy, or the original LinearGradient with colors={[c0, c1]}, start={{ x: 0, y: 0 }}, end={{ x: 1, y: 1 }}, style={StyleSheet.absoluteFillObject} (fallback; no longer needs its own styles.attractionPhoto, since the wrapping View now carries that) when it is not. Keep the existing TouchableOpacity with style={styles.heartBtn} as the next sibling, unmodified, so it continues to render on top via the parent's flex layout (flexDirection: 'row', justifyContent: 'flex-end', alignItems: 'flex-start', already on attractionPhoto) regardless of which background renders. Add overflow: 'hidden' to the attractionPhoto style object (line ~1012-1018) if not already present, so the image respects the card's border radius.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Events detail hero, Events list cards, and Discover-tab attraction cards all render the real photo (via expo-image) when imageUrls[0] is present on the underlying API data, and fall back to their existing gradient placeholders when it is not. The attraction card's bookmark heart button still renders on top in both branches. npx tsc --noEmit in mobile/ is clean.</done>
</task>

<task type="auto">
  <name>Task 4: Configure Android notification icon (placeholder asset + app.json)</name>
  <files>mobile/app.json, mobile/assets/notification-icon.png</files>
  <action>
    Generate a placeholder mobile/assets/notification-icon.png: write and run a short Node script (from backend/, e.g. cd backend && node script-name.js, so require('sharp') resolves against backend/node_modules) that rasterizes an inline SVG string (a 96x96 canvas containing a centered solid white circle of about 64px diameter, no gradients or anti-aliasing artifacts, respecting Android's safe-zone convention) to a 96x96 transparent-background PNG with alpha preserved via sharp(Buffer.from(svg)).png().toBuffer(), then writes the buffer to ../mobile/assets/notification-icon.png (path relative to backend/, resolving to mobile/assets/notification-icon.png at the repo root). Delete the temporary script after use (or place it in the scratchpad, not committed). This is explicitly a stand-in for the design team's real deliverable (already documented in the delivered asset brief) — functional and correctly shaped/sized only, no polish effort.

    In mobile/app.json, change the bare "expo-notifications" plugin entry (line 75) to the tuple form: an array containing the string "expo-notifications" followed by an object with icon set to "./assets/notification-icon.png" and color set to "#1A6B3C" — #1A6B3C is the app's documented FOREST brand primary color (per CLAUDE.md), not #006400 (the existing splash/adaptive-icon background — a deliberately different value).
  </action>
  <verify>
    <automated>cd mobile && node -e "require('fs').accessSync('./assets/notification-icon.png'); const j=require('./app.json'); const p=j.expo.plugins.find(x=>Array.isArray(x)&&x[0]==='expo-notifications'); if(!p||p[1].icon!=='./assets/notification-icon.png'){console.error('FAIL'); process.exit(1);} console.log('OK')"</automated>
  </verify>
  <done>mobile/assets/notification-icon.png exists as a valid 96x96 transparent PNG; mobile/app.json's expo-notifications plugin entry is the tuple form pointing to it with color #1A6B3C.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VENDOR client to POST /products/:id/images | Authenticated, role-gated file upload; server validates mime/size and re-derives ownership from Vendor.userId, never trusts a client-supplied vendor/product association |
| SUPER_ADMIN/LGA_ADMIN client to POST admin/attractions/:id/images | Authenticated, role-gated file upload with no ownership concept — the role guard is the sole authorization boundary |
| Mobile client rendering imageUrls from backend responses | Backend-controlled S3/CDN URLs rendered via expo-image — not raw user-supplied strings, since imageUrls is only ever populated by the server-side upload endpoints above |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | Uploaded file content (both backend endpoints) | mitigate | ImageService.validateImage enforces an allow-list of image/jpeg, image/png, image/webp mime types and a 5MB size cap before any processing; sharp re-encodes to webp server-side rather than trusting/passing through the raw uploaded bytes, stripping embedded metadata/exploits in the original file |
| T-quick-02 | Elevation of Privilege | POST /products/:id/images | mitigate | @Roles(UserRole.VENDOR) + explicit vendor.userId === userId && product.vendorId === vendor.id ownership re-derivation inside uploadImage, identical to the existing updateProduct/removeProduct pattern — a vendor cannot attach images to another vendor's product |
| T-quick-03 | Elevation of Privilege | POST admin/attractions/:id/images | accept | No per-attraction ownership exists by design (attractions are LGA/admin-curated content with only an lgaId FK, not a user-owned entity) — the AdminController class-level @Roles(SUPER_ADMIN, LGA_ADMIN) guard is the only and intentionally sufficient gate, matching the existing updateEventStatus/updateVendorStatus sibling routes on the same controller |
| T-quick-04 | Denial of Service | Large/malicious file uploads (both backend endpoints) | mitigate | FileInterceptor enforces limits: { fileSize: 5 * 1024 * 1024 } at the multer layer before the handler body ever runs, identical to the existing EventsController.uploadImage configuration |

</threat_model>

<verification>
1. cd backend && npx tsc --noEmit — clean, no new type errors.
2. cd backend && npx jest src/modules/marketplace/__tests__/marketplace.service.spec.ts src/modules/admin/__tests__/admin.service.spec.ts — new upload tests pass alongside all pre-existing tests in both files.
3. cd backend && npx jest --silent — full backend suite remains green.
4. cd mobile && npx tsc --noEmit — clean, no new type errors from the three mobile display-wiring changes.
5. Manual read-through confirms neither MarketplaceModule nor AdminModule needed an explicit imports change.
6. cd mobile && node -e (asset/plugin-config check in Task 4's verify) confirms the notification icon asset exists and app.json is wired correctly.
</verification>

<success_criteria>
- A VENDOR can upload a product image via POST /products/:id/images for their own product only; the resulting URL is appended to Product.imageUrls.
- A SUPER_ADMIN/LGA_ADMIN can upload an attraction image via POST admin/attractions/:id/images for any attraction; the resulting URL is appended to Attraction.imageUrls.
- Events detail hero, Events list cards, and Discover-tab attraction cards all display real photos when available, with unchanged gradient fallbacks otherwise.
- mobile/assets/notification-icon.png exists and app.json's expo-notifications plugin is correctly configured with it.
- Full backend jest suite and tsc --noEmit remain clean; mobile tsc --noEmit remains clean.
</success_criteria>

<output>
After completion, create `.planning/quick/260727-8v6-build-marketplace-tourism-image-upload-e/260727-8v6-SUMMARY.md`
</output>
