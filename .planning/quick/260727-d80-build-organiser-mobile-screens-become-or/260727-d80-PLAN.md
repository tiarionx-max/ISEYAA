---
phase: quick
plan: 260727-d80
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/app/organiser-dashboard.tsx
  - mobile/app/(tabs)/profile.tsx
  - mobile/app/event-create.tsx
  - mobile/app/event-edit/[id].tsx
  - mobile/app/event-analytics/[id].tsx
  - mobile/app/_layout.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "A user can reach an organiser entry point from Profile ('Organiser Tools'); if not yet an organiser, tapping 'Become an organiser' instantly grants the role (no pending-approval state for the role itself) and reveals the dashboard"
    - "An organiser can see ALL of their own events regardless of status (DRAFT/PENDING_APPROVAL/APPROVED/PUBLISHED/CANCELLED/COMPLETED) on organiser-dashboard.tsx, each visually distinguished by a status badge"
    - "An organiser can create a new event (title/description/lgaId/venue/address/startDate/endDate) via event-create.tsx, which POSTs to /events and lands as DRAFT — never auto-submitted for approval — with an optional immediate cover-photo upload step"
    - "An organiser can edit an existing event's details, add more photos, and delete it (with a confirmation prompt) via event-edit/[id].tsx"
    - "An organiser can submit a DRAFT event for approval, and that action is only ever shown while the event's status is DRAFT (both on the dashboard list and the edit screen)"
    - "An organiser can view ticket-sales analytics for an event: tickets sold, revenue (in Naira), check_in_rate shown as a percentage, and an hourly sales breakdown via event-analytics/[id].tsx"
  artifacts:
    - path: "mobile/app/organiser-dashboard.tsx"
      provides: "Organiser entry point (become-organiser gate) + events list (GET /events/mine) with status badges, Add event CTA, per-event Edit/Submit-for-approval/View analytics actions"
      min_lines: 150
    - path: "mobile/app/event-create.tsx"
      provides: "Event creation form matching CreateEventDto (title/description/lgaId/venue/address/startDate/endDate) posting to POST /events, then an optional photo-upload step"
      min_lines: 120
    - path: "mobile/app/event-edit/[id].tsx"
      provides: "Event edit form pre-filled from GET /events/:id, submitting via PATCH /events/:id, with delete (DELETE /events/:id), add-more-photos, and conditional submit-for-approval actions"
      min_lines: 120
    - path: "mobile/app/event-analytics/[id].tsx"
      provides: "Analytics screen fetching GET /events/:id/analytics and rendering tickets_sold/revenue/check_in_rate/hourly_sales_chart"
      min_lines: 60
  key_links:
    - from: "mobile/app/(tabs)/profile.tsx"
      to: "mobile/app/organiser-dashboard.tsx"
      via: "new 'Organiser Tools' MenuRow entry: router.push('/organiser-dashboard')"
      pattern: "organiser-dashboard"
    - from: "mobile/app/organiser-dashboard.tsx"
      to: "backend POST /api/v1/users/me/become-organiser"
      via: "api.post('/users/me/become-organiser') when 'ORGANISER' not in registeredRoles"
      pattern: "become-organiser"
    - from: "mobile/app/organiser-dashboard.tsx"
      to: "backend GET /api/v1/events/mine"
      via: "fetcher('/events/mine') via useQuery, enabled once ORGANISER confirmed"
      pattern: "events/mine"
    - from: "mobile/app/event-create.tsx"
      to: "backend POST /api/v1/events"
      via: "api.post('/events', body) with CreateEventDto-shaped fields"
      pattern: "api\\.post\\(.\\/events.\\s*,"
    - from: "mobile/app/event-edit/[id].tsx"
      to: "backend PATCH /api/v1/events/:id/submit-for-approval"
      via: "api.patch(`/events/${id}/submit-for-approval`) shown only when event.status === 'DRAFT'"
      pattern: "submit-for-approval"
    - from: "mobile/app/event-analytics/[id].tsx"
      to: "backend GET /api/v1/events/:id/analytics"
      via: "fetcher(`/events/${id}/analytics`) via useQuery"
      pattern: "events/\\$\\{.*\\}/analytics"
---

<objective>
Seventh of a sequence of quick tasks closing gaps found in a mobile completeness audit (see STATE.md Blockers/Concerns, 2026-07-27 entry, item 5: vendor/organiser onboarding). Backend for this gap is already 100% complete (`POST /users/me/become-organiser`, `GET /events/mine` landed this session in quick task 260727-c41; event CRUD, `submit-for-approval`, and image upload already existed) — this plan is pure mobile UI and does not touch any backend file. Today zero organiser-facing UI exists anywhere in the app: an organiser cannot become one via self-service, create/edit/submit/delete an event, upload a cover photo, or view ticket-sales analytics.

**Known pre-existing backend limitation (not in scope, not to be fixed here):** there is no backend endpoint anywhere to create/update `TicketType` rows (confirmed via full-codebase grep — `POST /events` and `PATCH /events/:id` only ever touch `Event` scalar fields; `TicketType` rows can only be read, never written, through any current API). This means an organiser-created event has no way to acquire sellable ticket types through the app today, so `event-analytics/[id].tsx`'s `tickets_sold`/`revenue`/`check_in_rate` will legitimately read as zero for any event created through this new UI until a future backend quick task adds ticket-type management. This plan still builds the full analytics screen against the real endpoint (correct behavior once that gap closes) — it is called out here so it is not mistaken for a mobile bug.

Purpose: Give organisers a complete, reachable, self-service event-management workflow on mobile.
Output: `mobile/app/organiser-dashboard.tsx`, `mobile/app/event-create.tsx`, `mobile/app/event-edit/[id].tsx`, `mobile/app/event-analytics/[id].tsx` (all new); `mobile/app/(tabs)/profile.tsx` (new reachability entry point); `mobile/app/_layout.tsx` (route registration).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<verified_facts>
Confirmed by direct inspection this session — use directly, do not re-investigate.

**CreateEventDto** (`backend/src/modules/events/dto/create-event.dto.ts`, full file, 37 lines): `title` (string, required, `@MinLength(3)`), `description?` (string, optional), `lgaId` (string, `@IsUUID()`, required), `venue` (string, required), `address?` (string, optional), `startDate` (string, `@IsDateString()`, required, e.g. `'2026-08-15T09:00:00Z'`), `endDate` (string, `@IsDateString()`, required). `UpdateEventDto` is `PartialType(CreateEventDto)` — no additional fields, all optional on edit.

**EventsController route table** (`backend/src/modules/events/events.controller.ts`, full file, 147 lines, all confirmed live): `GET /events` (public, PUBLISHED-only), `GET /events/tickets/mine` (JWT), `GET /events/mine` (JWT+`@Roles(ORGANISER)`, returns ALL statuses), `GET /events/:id` (public, single event by id — same endpoint the buyer-facing `events/[id].tsx` uses, returns the raw `Event` row including scalar `lgaId` alongside the included `lga` relation object, plus `ticketTypes`), `POST /events` (`@Roles(ORGANISER)`, creates `status: 'DRAFT'`), `PATCH /events/:id` (`@Roles(ORGANISER)`, ownership-checked — `event.organizerId !== organizerId` throws `ForbiddenException`), `DELETE /events/:id` (`@Roles(ORGANISER)`, ownership-checked, `@HttpCode(HttpStatus.OK)`, soft-delete via `deletedAt`), `PATCH /events/:id/submit-for-approval` (`@Roles(ORGANISER)`, ownership-checked, only valid when `event.status === 'DRAFT'` — throws `BadRequestException` otherwise per `EventsService.submitForApproval` lines 159-170), `POST /events/:id/images` (`@Roles(ORGANISER)`, ownership-checked, multipart field name `'file'`, 5MB limit, resizes to 1200×630 webp, appends to `Event.imageUrls`), `GET /events/:id/analytics` (`@Roles(ORGANISER)`, ownership-checked).

**EventStatus enum** (`backend/prisma/schema.prisma` line 56): `DRAFT`, `PENDING_APPROVAL`, `APPROVED`, `PUBLISHED`, `CANCELLED`, `COMPLETED` — exactly these six values, no others.

**GET /events/:id/analytics response shape** (`EventsService.getAnalytics`, `events.service.ts` lines 429-466, confirmed by direct read): `{ tickets_sold: number, revenue: number, check_in_rate: number (0-1 fraction, NOT a percentage), hourly_sales_chart: { hour: string (ISO, hour-truncated, e.g. '2026-07-27T14:00:00Z'), count: number }[] }` — exact snake_case field names, do not rename or reshape.

**POST /users/me/become-organiser** (`backend/src/modules/users/users.controller.ts`, confirmed live): `JwtAuthGuard` only, self-service, no approval gate. Mirrors `become-host` exactly. Adds `'ORGANISER'` to `registeredRoles` and sets it as the active `role` in one call — unlike `HOST`/`VENDOR`'s active-role-drift concern, there is no separate approval step for the role itself, only per-event DRAFT→PENDING_APPROVAL→PUBLISHED transitions matter downstream.

**Role-drift reconciliation pattern** (mirrored from `mobile/app/property-create.tsx` lines 76-84 and `property-edit/[id].tsx` lines 72-77, both confirmed via direct read): `RolesGuard` checks the single active `user.role`, not `registeredRoles[]`. A user could gain ORGANISER via `become-organiser` then later switch active role away via `PATCH /users/me/role`, which would 403 any subsequent `@Roles(ORGANISER)` call. Duplicate this exact helper into every file that performs an event mutation (`event-create.tsx`, `event-edit/[id].tsx`; `organiser-dashboard.tsx` also needs it for its `submit-for-approval` action):
```ts
async function ensureOrganiserRole(currentRole: string | undefined): Promise<void> {
  if (currentRole !== 'ORGANISER') {
    await api.patch('/users/me/role', { role: 'ORGANISER' });
  }
}
```
Source `currentRole` from the existing `GET /users/me` query (`useQuery(['me'], () => fetcher('/users/me'))`) already used throughout the app.

**Multipart image-upload pattern** (verbatim from `mobile/app/property-create.tsx` lines 59-84, 155-185 and `property-edit/[id].tsx` lines 57-70, 181-211, both confirmed via direct read): `ImagePicker.requestMediaLibraryPermissionsAsync()` → if denied, `Alert.alert` and stop → `ImagePicker.launchImageLibraryAsync({ mediaTypes: ImagePicker.MediaTypeOptions.Images, allowsEditing: true, quality: 0.8 })` → if canceled or no asset, stop → build `FormData`, `formData.append('file', { uri: asset.uri, name: filename, type: mimeType } as any)` (multer field name is `'file'`, confirmed in `events.controller.ts` line 114) → `api.post(`/events/${id}/images`, formData, { headers: { 'Content-Type': 'multipart/form-data' } })`. Duplicate the local `inferMimeType(uri, assetMimeType?)` and `deriveFilename(uri)` helpers verbatim (small pure functions, this codebase's established per-screen duplication convention — do not attempt to extract a shared module).

**Date/time picker pattern** (verbatim from `mobile/components/stays/TimedEventBookingSheet.tsx` lines 183-208 and `mobile/app/driver-application.tsx`, both confirmed installed and in use — `@react-native-community/datetimepicker` v8.0.1 is already a mobile dependency): a `Pressable` field showing the current formatted value, toggling a boolean `show*` state; the picker itself renders conditionally (`{showX && <DateTimePicker ... />}`), calling `setShowX(false)` on non-iOS platforms inside `onChange` (iOS keeps its inline spinner open until dismissed by the surrounding UI). `mode="date"` and `mode="time"` are the only cross-platform-safe modes (`mode="datetime"` is Android-only in this library) — this plan uses one `Date` state per event date field (`startDateObj`, `endDateObj`) with two Pressable rows each (Date, Time), each toggling its own `mode="date"`/`mode="time"` picker instance and merging into the same `Date` object via `setHours`/`setMinutes`.

**No LGA picker component exists anywhere in mobile** (confirmed via whole-`mobile/` grep for `lgaId|LGA_NAMES|lgas` — zero picker matches). Use a plain `TextInput` for `lgaId`, matching the exact accepted simplification already used in `property-create.tsx` (same code comment convention: "Known simplification: no LGA picker component exists anywhere in mobile yet").

**No charting library installed beyond `react-native-svg` 15.2.0** (confirmed via `mobile/package.json` grep — no `victory-native`/similar). Per constraints, render `hourly_sales_chart` as a simple horizontal bar-list using plain `View`s sized by `count / maxCount` — do not add a new charting dependency.

**Currency/percentage formatting conventions** (`mobile/app/events/[id].tsx` line 42-44, confirmed via direct read): `function formatCurrency(amount: number) { return \`₦${amount.toLocaleString('en-NG')}\`; }` — duplicate this exact convention for `revenue` in the analytics screen (NOT `host-dashboard.tsx`'s per-unit-suffix `formatPrice`). `check_in_rate` is a 0-1 fraction — display as `${(check_in_rate * 100).toFixed(1)}%`, never the raw fraction.

**`mobile/app/(tabs)/profile.tsx` reachability precedent and risk** (confirmed via direct read, lines 1-51, 296-306, 547-597, 832-854): `menuRows` (a `MenuRowItem[]` array, `interface MenuRowItem { icon: React.ComponentType<LucideProps>; label: string; sub: string; onPress: () => void; isLast?: boolean; }`) is rendered unconditionally via `MenuRow` components — adding one new entry here is a low-conflict-risk change (a single array literal addition), unlike the existing "Become a host" conditional CTA card (`{!alreadyHost && (...)}` around line 833) which — confirmed by grep — has NO corresponding "already a host → go to dashboard" link anywhere in `profile.tsx` (a latent reachability gap in a prior quick task's dashboard, out of scope here). To avoid repeating that gap and to avoid the repeated real merge-conflict history this file has had this session (`profile.tsx`/`_layout.tsx` conflicted across three consecutive sibling quick tasks per STATE.md), add exactly ONE new `menuRows` entry — `{ icon: Megaphone, label: 'Organiser Tools', sub: 'Manage your events', onPress: () => router.push('/organiser-dashboard' as never) }` — always visible regardless of current role; `organiser-dashboard.tsx` itself handles both the become-organiser gate and the real dashboard internally, so no conditional CTA card is needed in `profile.tsx`. `Megaphone` is not currently imported from `lucide-react-native` in this file — add it to the existing import list (line 33-51).

**`mobile/app/_layout.tsx` registration conventions** (confirmed via direct read, 87 lines): flat screens use `<Stack.Screen name="x" options={{ title: 'Y', presentation: 'card' }} />` (e.g. `host-dashboard`, `property-create`); dynamic-segment folders use the folder path verbatim (e.g. `property-edit/[id]`, `property-bookings/[id]`). This plan adds: `organiser-dashboard` (flat, title `'My Events'`), `event-create` (flat, title `'Create Event'`), `event-edit/[id]` (dynamic, title `'Edit Event'`), `event-analytics/[id]` (dynamic, title `'Event Analytics'`) — insert immediately after the existing `<Stack.Screen name="events/[id]" .../>` line for organizational proximity to the sibling buyer-facing event screen.

**`mobile/lib/api.ts` exports** (confirmed): `api` (configured axios instance, auth header + refresh-token interceptor attached), `fetcher` (GET helper for `useQuery`), `getErrorMessage(err, fallback)` (always route API errors through this before `Alert.alert` — NestJS `ValidationPipe` 400s return `message` as a string array that crashes the native alert bridge if passed raw).

**`mobile/components/ui/Chip.tsx`** (`ChipProps: { label, active?, onPress?, icon?, style? }`) — reusable for a status-filter-free display badge (render with `active` toggled per status color, or render a plain colored pill directly using tokens — this plan uses a small local `StatusBadge` component with per-status text/background colors from `mobile/lib/tokens.ts`, since `Chip`'s single gold/neutral palette does not distinguish six different statuses).
</verified_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Organiser entry point + dashboard + profile reachability</name>
  <files>mobile/app/organiser-dashboard.tsx, mobile/app/(tabs)/profile.tsx</files>
  <action>
    Create `mobile/app/organiser-dashboard.tsx`. Fetch `GET /users/me` via `useQuery(['me'], () => fetcher('/users/me'))` to read `registeredRoles: string[]` and `role: string`. Define the local `ensureOrganiserRole(currentRole)` helper exactly as specified in the verified facts above.

    **Gate state** (when `'ORGANISER'` is NOT in `registeredRoles`): render a centered prompt — heading ("Become an organiser"), one line of supporting copy ("Create and manage your own events on Iṣẹ́yáá — no approval needed to get started."), and a primary button "Become an organiser" wired to a `useMutation` that calls `api.post('/users/me/become-organiser')`, and on success invalidates `['me']` (`queryClient.invalidateQueries`) so the gate flips to the dashboard state without navigating away. Surface errors via `Alert.alert('Error', getErrorMessage(err, 'Something went wrong. Please try again.'))`.

    **Dashboard state** (when `'ORGANISER'` IS in `registeredRoles`): `useQuery(['my-events'], () => fetcher('/events/mine'), { enabled: <is-organiser> })` fetches the organiser's events. Render a header with an "Add event" CTA (`router.push('/event-create' as never)`) and a `FlatList`/mapped list of event cards. Each card shows: `title`, `venue`, formatted `startDate` (`toLocaleDateString('en-NG', { day: 'numeric', month: 'short', year: 'numeric' })`), a small local `StatusBadge` component rendering `status` with per-status colors (`DRAFT`: `INK_MID` text on `SURFACE_RAISED`; `PENDING_APPROVAL`: `WARNING` text on its dim variant; `APPROVED`: `SUCCESS` text on `SUCCESS_DIM`; `PUBLISHED`: `GOLD` text on `GOLD_DIM`; `CANCELLED`: `DESTRUCTIVE` text on `DESTRUCTIVE_DIM`; `COMPLETED`: `INK_DIM` text on `SURFACE_RAISED`), and three actions: "Edit" (`router.push(\`/event-edit/${event.id}\`)`), "Submit for approval" (rendered ONLY when `event.status === 'DRAFT'`; calls `ensureOrganiserRole(me?.role)` then `api.patch(\`/events/${event.id}/submit-for-approval\`)` inside a `useMutation`, invalidating `['my-events']` on success, surfacing errors via `getErrorMessage`), and "View analytics" (`router.push(\`/event-analytics/${event.id}\`)`, rendered for every status — zero-value analytics on an unpublished/no-sales event are an acceptable, correctly-zero result, not an error state to hide). Handle loading (`ActivityIndicator`) and empty-list ("No events yet" + the same "Add event" CTA) states.

    In `mobile/app/(tabs)/profile.tsx`: add `Megaphone` to the existing `lucide-react-native` import list (line 33-51). Add exactly one new entry to the `menuRows` array (insert before the existing `Change Password` entry so `Change Password` keeps its `isLast: true`): `{ icon: Megaphone, label: 'Organiser Tools', sub: 'Manage your events', onPress: () => router.push('/organiser-dashboard' as never) }`. Do not add a conditional CTA card (unlike the host/driver sections above the menu) — this single always-visible menu row is the sole reachability path, per the verified-facts rationale (avoids repeating the pre-existing host reachability gap and avoids further merge-conflict risk in this frequently-contested file).
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>organiser-dashboard.tsx renders a "Become an organiser" gate (POST /users/me/become-organiser) when the user lacks the role, and — once granted — a real dashboard listing GET /events/mine with per-event status badges (all six EventStatus values distinguishable), an Add event CTA, and conditional Edit/Submit-for-approval/View-analytics actions. profile.tsx exposes a single always-visible "Organiser Tools" menu row reaching /organiser-dashboard. `cd mobile && npx tsc --noEmit` passes with no new type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Event create/edit/delete/submit-for-approval + analytics + route registration</name>
  <files>mobile/app/event-create.tsx, mobile/app/event-edit/[id].tsx, mobile/app/event-analytics/[id].tsx, mobile/app/_layout.tsx</files>
  <action>
    Create `mobile/app/event-create.tsx`. Fetch `GET /users/me` for role reconciliation. Duplicate `ensureOrganiserRole`, `inferMimeType`, `deriveFilename` locally (per this codebase's per-screen-duplication convention). Form fields, matching `CreateEventDto` exactly: `title` (TextInput, required, min 3 chars enforced client-side before enabling submit), `description` (multiline TextInput, optional), `venue` (TextInput, required), `address` (TextInput, optional), `lgaId` (plain TextInput, required — known simplification, same code comment as `property-create.tsx`), `startDate`/`endDate` (each a `Date` state with Date+Time Pressable rows toggling `@react-native-community/datetimepicker` per the verified-facts pattern, defaulting to a reasonable near-future value e.g. tomorrow 09:00 for start / tomorrow 21:00 for end). On submit: `ensureOrganiserRole(me?.role)` first (await, surface errors via `getErrorMessage`), then `api.post('/events', { title, description: description.trim() || undefined, lgaId: lgaId.trim(), venue, address: address.trim() || undefined, startDate: startDateObj.toISOString(), endDate: endDateObj.toISOString() })`. On success, store the created event id and render a Step 2 "Add a cover photo" screen reusing the exact multipart pattern (`POST /events/${id}/images`), skippable, then `router.replace('/organiser-dashboard' as never)` invalidating `['my-events']` first via `queryClient`. Do NOT call `submit-for-approval` automatically — the event must remain DRAFT after creation.

    Create `mobile/app/event-edit/[id].tsx`. `useLocalSearchParams<{ id: string }>()` for the event id. `useQuery(['event', id], () => fetcher(`/events/${id}`))` to pre-fill the same field set as `event-create.tsx` (`title`, `description`, `venue`, `address`, `lgaId` from the response's scalar `lgaId` field, `startDate`/`endDate` parsed via `new Date(...)`). Render a `StatusBadge` (reuse/duplicate the same small component from `organiser-dashboard.tsx`) showing the event's current status read-only. On submit: `ensureOrganiserRole(me?.role)` then `api.patch(\`/events/${id}\`, body)` with the collected fields, then invalidate `['event', id]` and `['my-events']` and `router.back()`. Add an "Add more photos" action reusing the same multipart pattern against `POST /events/${id}/images`, invalidating `['event', id]` on success. Add a "Submit for approval" action — rendered ONLY when `event.status === 'DRAFT'` — calling `ensureOrganiserRole` then `api.patch(\`/events/${id}/submit-for-approval\`)`, invalidating `['event', id]` and `['my-events']` on success. Add a "Delete event" action: `Alert.alert('Delete event?', 'This cannot be undone.', [{ text: 'Cancel', style: 'cancel' }, { text: 'Delete', style: 'destructive', onPress: () => deleteMutation.mutate() }])` before calling `ensureOrganiserRole` then `api.delete(\`/events/${id}\`)`, invalidating `['my-events']` and `router.replace('/organiser-dashboard' as never)` on success. Surface all mutation errors via `getErrorMessage` in `Alert.alert`.

    Create `mobile/app/event-analytics/[id].tsx`. `useLocalSearchParams<{ id: string }>()`. `useQuery(['event-analytics', id], () => fetcher(`/events/${id}/analytics`))` fetching `{ tickets_sold, revenue, check_in_rate, hourly_sales_chart }` exactly as returned (do not rename fields). Duplicate the local `formatCurrency(amount: number)` helper from `events/[id].tsx` (`\`₦${amount.toLocaleString('en-NG')}\``) for `revenue`. Render three stat cards/tiles: "Tickets sold" (`tickets_sold`), "Revenue" (`formatCurrency(revenue)`), "Check-in rate" (`${(check_in_rate * 100).toFixed(1)}%` — multiply the 0-1 fraction, never show the raw fraction). Render `hourly_sales_chart` as a simple bar-list: for each `{ hour, count }` entry, one row with a label (`new Date(hour).toLocaleString('en-NG', { month: 'short', day: 'numeric', hour: '2-digit' })`) and a `View` bar whose `width` is `${(count / maxCount) * 100}%` (`maxCount = Math.max(1, ...hourly_sales_chart.map(h => h.count))`) plus the raw `count` printed alongside. Handle the empty-chart case (no sales yet) with a plain "No ticket sales yet" message instead of an empty bar list. Handle loading state.

    In `mobile/app/_layout.tsx`, add four new `Stack.Screen` entries immediately after the existing `<Stack.Screen name="events/[id]" .../>` line: `<Stack.Screen name="organiser-dashboard" options={{ title: 'My Events', presentation: 'card' }} />`, `<Stack.Screen name="event-create" options={{ title: 'Create Event', presentation: 'card' }} />`, `<Stack.Screen name="event-edit/[id]" options={{ title: 'Edit Event', presentation: 'card' }} />`, `<Stack.Screen name="event-analytics/[id]" options={{ title: 'Event Analytics', presentation: 'card' }} />`.
  </action>
  <verify>
    <automated>cd mobile && npx tsc --noEmit</automated>
  </verify>
  <done>event-create.tsx posts a CreateEventDto-shaped body to POST /events (landing as DRAFT), then offers an optional cover-photo upload before returning to the dashboard. event-edit/[id].tsx pre-fills from GET /events/:id, saves via PATCH /events/:id, supports add-more-photos, conditional (DRAFT-only) submit-for-approval, and a confirmed delete. event-analytics/[id].tsx renders tickets_sold/revenue/check_in_rate (as a %)/hourly_sales_chart (as a bar list) from the exact response shape. All four new routes are registered in _layout.tsx. `cd mobile && npx tsc --noEmit` passes with no new type errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Mobile app → `POST`/`PATCH`/`DELETE /events*` | Organiser-supplied event data (title, description, venue, address, lgaId, dates) crosses into Prisma writes; ownership already enforced server-side |
| Mobile app → `GET /events/:id/analytics` | Must never expose another organiser's event analytics |
| Mobile app → `PATCH /users/me/role` | Client-triggered active-role change, silently invoked by `ensureOrganiserRole` before every mutation |
| Mobile app → `POST /events/:id/images` | User-selected image file crosses into S3 storage via existing server-side validation/resize |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Elevation of Privilege (client-triggered role switch) | `ensureOrganiserRole` → `PATCH /users/me/role` | accept | No backend change in this plan; `UsersService.switchRole` already validates server-side that the requested role is present in the caller's `registeredRoles` (`ForbiddenException` otherwise, per existing code) — the mobile-side silent call cannot grant ORGANISER to a user who never called `become-organiser`, it only re-selects an already-granted role. Same accepted pattern as the shipped host/driver dashboards. |
| T-quick-02 | Elevation of Privilege (self-service role grant) | `POST /users/me/become-organiser` | accept | Already-shipped, already-accepted backend endpoint (260727-c41) mirroring `become-host`/`become-guide` — out of scope to re-litigate here; ORGANISER only grants the ability to manage one's own events, still gated by ownership checks on every mutating route |
| T-quick-03 | Information Disclosure (cross-tenant analytics leak) | `GET /events/:id/analytics` | mitigate | No mobile change needed — `EventsService.getAnalytics` already throws `ForbiddenException` when `event.organizerId !== organizerId`; this plan's screen surfaces that error via `getErrorMessage` rather than assuming success |
| T-quick-04 | Tampering (unauthorized event mutation) | `event-edit/[id].tsx`'s PATCH/DELETE/submit-for-approval calls | accept | No mobile-side ownership check is added (or needed) — the backend independently re-verifies `event.organizerId !== userId` on every one of these routes; a non-owner reaching this screen (e.g., via a guessed id) would still be rejected server-side with `ForbiddenException`, surfaced to the user as an error rather than a silent no-op |
| T-quick-05 | Denial of Service / data loss (accidental delete) | `event-edit/[id].tsx`'s Delete action | mitigate | A confirming `Alert.alert` with a destructive-styled "Delete" option is required before `DELETE /events/:id` fires — no single-tap delete path exists |

</threat_model>

<verification>
1. `cd mobile && npx tsc --noEmit` passes with no new type errors.
2. Manual read-through: `event-create.tsx` never calls `submit-for-approval` automatically after `POST /events` — the event remains DRAFT until an explicit, separate user action.
3. Manual read-through: the "Submit for approval" action is conditionally rendered (only when `status === 'DRAFT'`) in both `organiser-dashboard.tsx` and `event-edit/[id].tsx`.
4. Manual read-through: `ensureOrganiserRole` is called before every `POST`/`PATCH`/`DELETE /events*` mutation across `event-create.tsx`, `event-edit/[id].tsx`, and the dashboard's submit-for-approval action.
5. Manual read-through: `event-analytics/[id].tsx` reads `tickets_sold`, `revenue`, `check_in_rate`, `hourly_sales_chart` exactly as named in the backend response, with `check_in_rate` multiplied by 100 for display.
6. Manual read-through: all four new routes (`organiser-dashboard`, `event-create`, `event-edit/[id]`, `event-analytics/[id]`) are registered in `_layout.tsx`, and `profile.tsx` exposes exactly one new, always-visible entry point to `/organiser-dashboard`.
</verification>

<success_criteria>
- A user can reach an organiser entry point from Profile, become an organiser with one tap (no approval wait), and immediately see a real dashboard of their own events across all statuses.
- An organiser can create, edit, delete, and submit-for-approval their own events, and upload/append cover photos, entirely from the mobile app — where zero such UI existed before this plan.
- An organiser can view per-event ticket-sales analytics (tickets sold, revenue in Naira, check-in rate as a percentage, hourly sales as a bar list) against the real backend endpoint.
- `cd mobile && npx tsc --noEmit` passes cleanly.
- No backend file was modified.
</success_criteria>

<output>
After completion, create `.planning/quick/260727-d80-build-organiser-mobile-screens-become-or/260727-d80-SUMMARY.md`
</output>
