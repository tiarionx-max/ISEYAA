---
phase: quick
plan: 260728-mv0
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/layout/Navbar.tsx
  - web/src/app/help/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Terms of Use is reachable from the main site menu on both desktop and mobile, not just the homepage footer"
    - "A Help/Support page exists and is reachable from the main site menu, giving users a way to reach customer care"
  artifacts:
    - path: "web/src/app/help/page.tsx"
      provides: "New Help/Support page with categorized mailto links to support@iseyaa.com"
      contains: "support@iseyaa.com"
    - path: "web/src/components/layout/Navbar.tsx"
      provides: "Help and Terms links in both desktop and mobile menu"
      contains: "/help"
---

<objective>
User (earlier this session, alongside a Growth & Monetization proposal document): "Things to be added on the web: 1. Terms and Conditions T&c — It's important that a legal terms and conditions is in the menu to avoid having stories that touch. 2. HELP — This should also be on the menu to make it for people to get in touch with customers care." Investigated at the time: `web/src/components/layout/Navbar.tsx`'s `NAV_ITEMS` (Explore/Events/Stays/Market/Studio) had no Terms/Help entries in either the desktop nav or the mobile hamburger menu; the only Legal links anywhere lived in a footer block hardcoded inline in `web/src/app/page.tsx`, unreachable from any other page; `/terms` already existed and worked; no Help/Support page existed anywhere in the codebase.

Purpose: Terms of Use must be discoverable from the site menu on every page (not just the homepage footer) for legal-protection reasons the user specifically named ("to avoid having stories"), and a Help page must exist and be menu-reachable so users have a real path to customer care.
Output: `Navbar.tsx` now renders "Help" and "Terms" as small always-visible utility links — desktop (next to the cart icon, `lg:` breakpoint, mirroring the existing "Become a host" utility-link pattern) and mobile (bottom utility section of the hamburger menu, alongside Dashboard/Sign out). A new `/help` page lists 4 support categories (Payments & Wallet, Bookings & Tickets, Account & Verification, Report a problem), each a `mailto:support@iseyaa.com` link with a pre-filled subject line, plus a plain contact card. `support@iseyaa.com` was chosen (via AskUserQuestion) to match the Resend sending domain already confirmed live earlier this session — no support inbox address existed anywhere in the codebase beforehand, confirmed via a repo-wide grep before asking.
</objective>

<context>
Made directly (not through the full plan → worktree-executor cycle), consistent with this session's pattern for small, well-scoped user-directed features. Deliberately scoped to exactly what was asked (menu-reachable Terms + Help) — did NOT extract a shared Footer component or retrofit the other ~20 pages that render `<Navbar/>` without a footer, since the user's ask was specifically about "the menu," not a site-wide footer overhaul; that gap is pre-existing and out of scope here.

Verified via `cd web && npx tsc --noEmit` (clean on both changed files — 3 pre-existing, unrelated jest-dom matcher-typing errors remain in two test files this change didn't touch) and a local `next dev` server confirming `/help` returns HTTP 200. No browser-automation tool was available in this environment to screenshot the rendered page — noted explicitly rather than claiming visual verification that didn't happen.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a Help/Support page</name>
  <files>web/src/app/help/page.tsx</files>
  <action>
    New page following the existing `cookies/page.tsx`/`terms/page.tsx` visual pattern (`Navbar` + a `motion.div`-wrapped `glass` card layout, jungle/gold theme). Renders a "Support" kicker + "Help & Support" heading, an intro paragraph linking `support@iseyaa.com`, a 2-column grid of 4 topic cards (Payments & Wallet, Bookings & Tickets, Account & Verification, Report a problem — each `<a href="mailto:support@iseyaa.com?subject=...">`), and a bottom contact card repeating the email.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit</automated>
  </verify>
  <done>/help renders and returns HTTP 200; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add Help and Terms links to the Navbar</name>
  <files>web/src/components/layout/Navbar.tsx</files>
  <action>
    Imported `HelpCircle`/`FileText` from `lucide-react`. Desktop: inserted two small `lg:`-only text links ("Help" → `/help`, "Terms" → `/terms`) into the `hidden md:flex items-center gap-2` right-side row, immediately before the cart icon — styled identically to existing nav-item links, always visible regardless of auth state (matching the cart icon's own "always visible (logged-in or not)" comment/pattern). Mobile: added a new bottom utility block (Help, Terms) in the hamburger menu, right after the existing Dashboard/Sign-out/Sign-in block, using the same row styling as that block.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit</automated>
  </verify>
  <done>Help and Terms are visible and clickable in both the desktop nav and the mobile hamburger menu; tsc clean.</done>
</task>

</tasks>

<success_criteria>
- Every page that renders `<Navbar/>` now surfaces Help and Terms links, on both desktop and mobile.
- `/help` exists, renders, and gives users a real path to reach customer care.
- `cd web && npx tsc --noEmit` introduces no new errors.
</success_criteria>

<output>
After completion, create `.planning/quick/260728-mv0-add-help-and-terms-links-to-web-navbar-c/260728-mv0-SUMMARY.md`
</output>
