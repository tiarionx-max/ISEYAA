[build]
dockerfilePath = "backend/apps/news-service/Dockerfile"
buildContext = "."

[deploy]
watchPaths = ["backend/apps/news-service/**", "backend/src/modules/news/**", "packages/proto/**"]
healthcheckPath = "/healthz"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
