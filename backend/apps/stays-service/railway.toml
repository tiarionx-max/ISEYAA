[build]
dockerfilePath = "backend/apps/stays-service/Dockerfile"
buildContext = "."

[deploy]
watchPaths = ["backend/apps/stays-service/**", "backend/src/modules/stays/**", "packages/proto/**"]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
