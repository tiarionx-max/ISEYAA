[build]
dockerfilePath = "backend/apps/events-service/Dockerfile"
buildContext = "."

[deploy]
watchPaths = ["backend/apps/events-service/**", "backend/src/modules/events/**", "packages/proto/**"]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
