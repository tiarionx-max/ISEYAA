[build]
dockerfilePath = "backend/apps/ai-service/Dockerfile"
buildContext = "."

[deploy]
watchPaths = ["backend/apps/ai-service/**", "backend/src/modules/ai/**", "packages/proto/**"]
healthcheckPath = "/healthz"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
